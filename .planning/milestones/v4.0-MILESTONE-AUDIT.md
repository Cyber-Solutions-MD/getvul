# v4.0 Milestone Audit — Enriched Risk Exposure & Source-Aware Triage

**Status:** ✅ PASSED (with accepted, non-blocking debt) · **Audited:** 2026-08-13 · **Phases:** 30–35 (6 phases, 25 plans + 2 gap-closures)
**Closeout type:** override_closeout (accepted verification debt is environment-driven — no live/at-scale tenant data — consistent with the v3.0 precedent).

## Requirement coverage — 33/33 v1 complete, 0 orphans

| Family | Complete | Phase |
|--------|----------|-------|
| CORR-01..03 | 3/3 | 30 |
| ENRICH-01..06 | 6/6 | 31 |
| EXPO-01..06 | 6/6 | 32 |
| RISK-01..10 | 10/10 | 33 (01–06), 34 (07–10) |
| SRC-01..08 | 8/8 | 35 |

`EXPO-07` (why-exposed summary chips) and `RISK-11` (consume vendor ML exploit-prediction) are explicitly **future / out-of-v1-scope** items, not gaps.

## Cross-phase integration seams — all connect (verified in code, not SUMMARY prose)

| Seam | Connects? | Evidence (file) |
|------|-----------|-----------------|
| 30 → 33 (corroboration count feeds score) | ✅ | `risk_exposure_service.py` reads `VulnerabilityCorrelation.sources_count` bulk-fetched once/tenant |
| 31 → 33 (EPSS/KEV/native feed score) | ✅ | `risk_exposure_service.py` uses `cisa_kev` (KEV floor via `max()`), `epss`, per-source-normalized `native_priority_*` |
| 32 → 33 (exposure context feeds score) | ✅ | `score_finding` inputs `business_criticality`/`data_sensitivity`/`internet_facing` |
| 33 → 34 (cutover consumers read the new score behind the flag) | ✅ | `service.py`: `sort="triage"` leads with `risk_exposure_score` only when `tenant.cutover_risk_exposure_scoring` (default OFF) |
| 30 → 35 (source filter uses the correlation ARRAY) | ✅ | `service.py` filters on `VulnerabilityCorrelation.sources` `&&`/`@>`, not the per-row `Vulnerability.source` |

## Sanity checks

- `alembic heads` → single head **`045_add_seen_by_sources_gin`**; linear chain 034→…→045.
- Key cross-phase test files (per-file, real Fernet key): `test_risk_exposure_service.py` 12/12, `test_source_filtering.py` 6/6, `test_risk_cutover.py` 5/5 — green.
- Every phase carries a `passed` VERIFICATION.md (independent verifier re-ran suites live for each).

## Accepted-debt ledger (conscious, non-blocking — environment-driven)

The environment has **no live/at-scale tenant data** (empty DB), so all "live" validations are deferred to a human on a real stack — consistent with the v3.0 (Phases 24–27) on-trust-waiver precedent.

| Phase | Accepted debt | Mitigation |
|-------|---------------|------------|
| 31 | Live-vendor field-name confirmation (no scanner creds) | Code 5/5 auto-verified; every field soft-nulls safely if a guess is wrong |
| 32 | IdP-directory inference deferred (kept MDM/HR); internet-facing uses `external_ip`/tag fallback (no vendor payload exposes a real signal today) | Detection mechanism fully wired; one-line enable per vendor when a signal is confirmed |
| 33 | Live DrillPanel visual UAT | Full RTL coverage; shadow/preview-labeled, no automated consumer reads it |
| 34 | Real at-scale backfill + the live consumer cutover flip | Flag stays **OFF** (user decision); machinery built + fixture-proven (kill-mid-run/resume/load); operable via the admin enqueue + gated `enable_cutover` endpoint |
| 35 | Live visual UAT of badges/chips + WCAG AA axe sweep | 22 backend + 217 frontend tests; A3 corroboration tint is a reviewable design choice |

**No real gaps found.** All debt is documented in the per-phase VERIFICATION/CONTEXT files and is closeable by a live-stack validation pass.
