# Milestone v2.2: Deferred UI Features

**Status:** 🚧 IN PROGRESS (opened 2026-07-15)
**Phases:** 16–22 (16–19 executed; 20–22 gap-closure, added 2026-07-20)
**Requirements:** see [v2.2-REQUIREMENTS.md](v2.2-REQUIREMENTS.md) (23 items across UX-D-01/02/03/06)

## Overview

v2.0 rebuilt every authenticated screen on the sunset design system; v2.1 closed the tech-debt
backlog (BL-01..05). This milestone finishes the four **features** deferred out of v2.0 as net-new
work: light-theme visual completion, page-transition motion, the Tickets kanban board, and the
full add-connector wizard. Each phase ships as a vertical slice honoring the phase-15 quality gate
(axe WCAG 2.1 AA in **both** themes, reduced-motion, ≤250 KB First-Load JS/route) and the
`sketch-findings-getvul` design contract, with no regression to the phase-15 mobile a11y work.

**Locked decisions:** page transitions use the native **View Transitions API** (0 KB); the kanban
board uses **@dnd-kit** (maintained, keyboard-accessible). framer-motion and react-beautiful-dnd
were rejected on bundle/maintenance grounds.

**Sequence:** 16 → 17 → 18 → 19. Phase 16 lands first because it gates the axe-in-both-themes
requirement for the other three. Phases 17 and 18 can proceed in parallel after 16. Phase 19
(highest state complexity) lands last.

## Phases

### Phase 16: Light-theme visual completion
**Goal**: Every authenticated route is visually correct and WCAG 2.1 AA in light mode — not just architecturally themed.
**Requirements**: UX-D-03-01..05
**Success criteria**:
1. A per-route light-mode sweep shows no dark-only visual artifacts (borders/shadows/hover/disabled) on any of the ~15 routes.
2. `e2e/a11y-routes.spec.ts` runs under `data-theme="light"` and reports 0 serious/critical axe violations on every route.
3. Severity/status/SLA pills and glyphs are legible and distinct on light surfaces; muted/faint/disabled tokens pass AA (source-palette changes reconciled into the design system).
4. Zero First-Load-JS delta (CSS-only changes); existing dark-mode gate stays green.

### Phase 17: Page-transition motion
**Goal**: Route changes within the app shell cross-fade smoothly, reduced-motion-safe, at zero bundle cost.
**Requirements**: UX-D-06-01..05
**Success criteria**:
1. Navigating between authed routes shows a View-Transitions cross-fade via a single `(authed)/template.tsx`.
2. Under `prefers-reduced-motion: reduce`, transitions are suppressed (animation-duration ≤0.02s) and `e2e/reduced-motion.spec.ts` stays green.
3. A CSS fallback keeps navigation clean in non-supporting browsers (Firefox) — no jank or broken paint.
4. DrillPanel Esc/clickaway close still works during/after a transition; no layout shift; no route over 250 KB.

### Phase 18: Tickets kanban board
**Goal**: Replace the board-view placeholder with a real, keyboard-accessible status kanban backed by a persisting mutation.
**Requirements**: UX-D-01-01..06
**Success criteria**:
1. The board renders four status columns populated from `useTickets`; the list/board URL toggle is preserved.
2. Dragging a ticket to another column (pointer or keyboard, via @dnd-kit) persists its status with optimistic update and rolls back on error.
3. Empty columns show the canonical empty-state; the status chip filter still applies.
4. At <768px the board degrades cleanly without regressing the fixed bottom-nav; the route stays ≤250 KB and passes axe in both themes.

### Phase 19: Add-connector wizard
**Goal**: Replace the single-step connector form with a guided four-step wizard, reusing existing endpoints.
**Requirements**: UX-D-02-01..06
**Success criteria**:
1. Adding a connector runs provider pick → credentials → test → confirm, with step navigation gated on a successful connection test.
2. The credentials step preserves the sentinel-passthrough (untouched secrets not resent); the confirm step shows required scopes before submit.
3. The wizard reuses `POST /connectors/test` and `POST /connectors` (no new backend) and works in the ResponsiveDialog/vaul mobile pattern.
4. The connectors route passes axe in both themes and stays ≤250 KB.

## Gap-closure phases (added 2026-07-20 from v2.2-MILESTONE-AUDIT)

The audit found 2 blockers + 2 warnings the individual phase completions masked. `/gsd-plan-milestone-gaps` grouped them into three follow-on phases.

### Phase 20: Light-theme severity-high AA fix
**Goal**: The light-theme WCAG-AA axe sweep runs green on every route — closing the milestone's headline UX-D-03 promise.
**Requirements**: UX-D-03-02, UX-D-03-03, UX-D-03-05 (unblocks -01/-04 tracking)
**Success criteria**:
1. `--color-severity-high` gains a light-mode `-on-soft` variant (≥4.5:1 on `#f7f2ea`), reconciled into `foundation.md` + the design skill.
2. ~15 bare `text-severity-high` sites use the on-soft variant on light surfaces; the High pill/glyph is legible and distinct.
3. `e2e/a11y-routes.spec.ts` under `data-theme="light"` runs to completion, 0 serious/critical on every route (pasted live-server evidence); dark stays green.
4. CSS-only (0 KB delta); tick UX-D-03-01/-04 once green.

**Plans:** 3 plans, 3 waves
- [ ] 20-01-PLAN.md — Add `--color-severity-high-on-soft` token (#9A3412 light / #FB923C dark no-op) + reconcile into foundation.md, visual-language.md, sunset.css (Wave 1)
- [ ] 20-02-PLAN.md — Migrate 18 foreground `text-severity-high` sites to the on-soft token + update 4 test assertions; static no-regression gate (grep-clean, unit + typecheck + build ≤250 KB / 0-KB JS delta) (Wave 2)
- [ ] 20-03-PLAN.md — Dedicated live-evidence gate: run the BLOCKING both-themes axe sweep green on a live prod build, exit-code-gated (PIPESTATUS + last-route-reached), paste evidence (Wave 3)

### Phase 21: Page-transition verification
**Goal**: Phase 17 (UX-D-06) is formally verified with real combined-scenario coverage and a persisted perceptual UAT.
**Requirements**: UX-D-06-01, UX-D-06-03, UX-D-06-04
**Success criteria**:
1. `e2e/page-transitions.spec.ts` exercises the real DrillPanel-open-during-navigation scenario (replaces the synthetic `PopStateEvent`, IN-01).
2. `17-HUMAN-UAT.md` records the perceptual checkpoint (cross-fade feel, chrome stillness, Firefox fallback) as closed; STATE.md cleared.
3. `17-VERIFICATION.md` exists from a goal-backward verify pass.

### Phase 22: Kanban + wizard test-coverage hardening
**Goal**: Close both audit warnings — kanban keyboard/announcement fixes and wizard later-step axe coverage gain real tests.
**Requirements**: UX-D-01-02 (coverage), UX-D-02-06 (coverage)
**Success criteria**:
1. Kanban tests assert Enter-key drag changes status without opening DrillPanel (CR-01) + gated no-op-drop SR wording (WR-02).
2. `connector-wizard-a11y.spec.ts` axe-sweeps the Test + Confirm steps in both themes (0 serious/critical).
3. No production regression; both routes ≤250 KB, gate green.

## Requirement → Phase coverage

| Phase | Requirements | Count |
|-------|--------------|-------|
| 16 | UX-D-03-01..05 | 5 |
| 17 | UX-D-06-01..05 | 5 |
| 18 | UX-D-01-01..06 | 6 |
| 19 | UX-D-02-01..06 | 6 |
| 20 (gap) | UX-D-03-02, -03, -05 (re-closes) | 3 |
| 21 (gap) | UX-D-06-01, -03, -04 (re-closes) | 3 |
| 22 (gap) | UX-D-01-02, UX-D-02-06 (coverage) | 2 |

All 22 requirement units map to their originating phase (16–19); phases 20–22 re-close the audit's unsatisfied/partial units and warnings. (BL-06 Safari check + print stylesheet are explicitly out of scope.)

## Next step

`/gsd-plan-phase 20` (plan the first gap-closure phase — the headline light-theme AA blocker).
