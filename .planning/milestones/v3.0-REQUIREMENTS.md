# Requirements Archive: GetVul v3.0 — AI-Assisted Triage ("Triage Copilot")

> **ARCHIVED 2026-08-04** on milestone completion. All 21 v1 requirements satisfied
> (confirmed independently by each phase's VERIFICATION.md, the plan SUMMARY
> `requirements-completed` frontmatter, and the traceability table). Source milestone audit:
> [v3.0-MILESTONE-AUDIT.md](v3.0-MILESTONE-AUDIT.md) — status `tech_debt` (0 blockers).

**Defined:** 2026-07-27 · **Shipped:** 2026-08-04 · **Phases:** 23–29

**Core Value:** A vuln-triage analyst can open one dashboard, see the same CVE-on-host correlated
across multiple scanners, identify the asset's owner from IdP/MDM/HR, and ship a Jira/Asana ticket
— without ever opening a scanner console. **v3.0 adds AI that helps the analyst *decide and act*,
grounded in the tenant's own data, using the tenant's own AI key.**

## Foundational Principle — BYOK (bring-your-own-key)

All AI functionality is **client-provided-key only**. Each tenant supplies their **own** Anthropic
API key; their vulnerability data is only ever sent to *their* AI account. There is **no
GetVul-owned, shared, or fallback key** and **no GetVul-proxied inference offering**. AI features
are **inert for a tenant until they configure their own key** (graceful "configure AI" state, not
an error). **Outcome: validated end-to-end** — `get_tenant_anthropic_key` is the single source of
truth; no shared/fallback key exists anywhere; every AI affordance gates on `useAiStatus()`.

## v1 Requirements — all satisfied

### Ingestion Reliability (REL) — precursor

- [x] **REL-01**: Wiz connector completes a full sync end-to-end (fix `authenticate()` return-type wiring) — *satisfied, Phase 23*
- [x] **REL-02**: Rapid7 connector completes a full sync end-to-end (fix the no-arg instantiation `TypeError`) — *satisfied, Phase 23*
- [x] **REL-03**: Every scanner connector (CrowdStrike, Nessus, Defender, Wiz, Qualys, Rapid7) has HTTP-layer integration tests (auth + pagination + mapping, mock transport) — *satisfied, Phase 23*
- [x] **REL-04**: An analyst can create a **Jira** ticket from a vulnerability (create + rule-engine paths, not just status sync) — *satisfied, Phase 23*
- [x] **REL-05**: GitHub ticketing finished end-to-end (create + sync) **or** explicitly retired — no dead stub — *satisfied, Phase 23*
- [x] **REL-06**: An analyst can see per-connector sync health (last sync time, last error, status) in the Connectors UI — *satisfied, Phase 23*

### AI Foundation & Guardrails (AI)

- [x] **AI-01**: Tenant admin configures their **own** Anthropic API key + model preferences (Fernet/`ConnectorConfig` pattern); AI is the only consumer; disabled until set; no shared/fallback key — *satisfied, Phase 24*
- [x] **AI-02**: Untrusted scanner text delimited/encoded and passed to the model as **data**, never instructions; all output schema-validated (Pydantic), never executed — *satisfied, Phase 24*
- [x] **AI-03**: AI responses stream token-by-token into the vuln drill panel (`fetch()` + `ReadableStream`; scoped nginx `location /api/v1/ai/` with `proxy_buffering off`) — *satisfied (mechanism proven; live proxy render = accepted debt), Phase 24*
- [x] **AI-04**: "Explain this vuln" plain-English summary + business-risk framing, grounded, with **two-tier citation** (verbatim scanner text vs AI-interpreted) — *satisfied, Phase 24*
- [x] **AI-05**: AI outputs cached in Redis **tenant-scoped only**, content-hash keyed (no cross-tenant serving) — *satisfied, Phase 24*
- [x] **AI-06**: Every AI call audit-logged (model, tokens, cost estimate, prompt provenance), including scheduler-originated calls (`user_email="system:scheduler"`) — *satisfied, Phase 24*

### AI Remediation Guidance (AIR)

- [x] **AIR-01**: Asset-aware remediation guidance citing the scanner's own solution text; **refuses (cites insufficient evidence) rather than inventing** — *satisfied, Phase 25*
- [x] **AIR-02**: Remediation guidance can populate a draft ticket description for the analyst to review — *satisfied, Phase 25*

### AI Prioritization (AIP)

- [x] **AIP-01**: "What to fix first and why" narrative that **augments and explains — never replaces** — the deterministic risk score, using exploit/KEV/owner/SLA factors — *satisfied, Phase 26*
- [x] **AIP-02**: Prioritization suggestions generated in bulk via the scheduler using the Message Batches API, respecting the tenant's key — *satisfied, Phase 26*

### AI Ticket Drafting (AID)

- [x] **AID-01**: When creating a Jira/Asana ticket, an analyst gets an AI-drafted title/description/remediation/asset-context that they edit before shipping (never auto-submitted) — *satisfied, Phase 27*

### AI Evals, Cost & Observability Gate (AIE)

- [x] **AIE-01**: DeepEval pytest-native eval harness runs in CI against golden sets, asserting on schema/grounding/citation (not brittle prose snapshots) — *satisfied (fixture provenance hand-authored = accepted override; capture script ready), Phase 28*
- [x] **AIE-02**: promptfoo red-team job (prompt-injection resistance over adversarial scanner text) runs as a separate CI check — *satisfied, Phase 28*
- [x] **AIE-03**: **Fail-closed** per-tenant token-cost budget / circuit breaker halts AI calls when the budget is exceeded — *satisfied, Phase 28*
- [x] **AIE-04**: Tenant admin can see AI usage + cost and manage AI settings (key, model, budget) in the UI — *satisfied, Phase 28*

## Additional requirement delivered this milestone

- [x] **WR-02** (backlog-promoted): Forced-rotation endpoint enforces a real password policy —
  configurable complexity + active password-history (`history_count > 0`) + similarity/edit-distance
  guard against the default install credential and the previous password — closing the Phase 06
  WR-01 `Admin1234!` near-variant residual. *satisfied, Phase 29 (verified passed 4/4); not one of
  the 21 v3.0 AI/ingestion requirements.*

## Future Requirements (deferred)

- **AINL-01**: Natural-language query over the vuln inventory — bounded function-calling over the
  existing tenant-scoped filter/search endpoints, never generated SQL. **Deferred to v3.1** so
  tenant-scoping gets a dedicated design-review gate (highest anti-feature risk in the milestone).

## Out of Scope

| Feature | Reason |
|---------|--------|
| GetVul-owned/shared/fallback AI key or GetVul-proxied inference | BYOK privacy guarantee — tenant data goes only to the tenant's own AI account |
| Cross-tenant / global AI-output cache | Output billed on one tenant's key must never serve another; caches are tenant-scoped |
| Fully-autonomous / agentic auto-remediation (execution) | GetVul has no execution layer; a human always approves and ships the fix |
| LLM-to-SQL / model-generated queries | Would bypass `tenant_id` scoping — hard security risk |
| Replacing the deterministic risk score (ASSET-02) with an ML/LLM score | The score stays authoritative; AI explains/augments it, never replaces it |
| Vector DB / embeddings / RAG | Correlation service already assembles grounding via SQL joins; no retrieval need this milestone |
| Non-Claude / self-hosted models | Milestone standardizes on Claude via the tenant's Anthropic key |

## Traceability (final)

Coverage: 21/21 v1 requirements satisfied — no orphans, no unsatisfied, no partials.

| Requirement | Phase | Final Status |
|-------------|-------|--------------|
| REL-01..06 | 23 (Ingestion Reliability Precursor) | Complete |
| AI-01..06 | 24 (AI Foundation + Explain-this-vuln) | Complete |
| AIR-01..02 | 25 (Asset-Aware Remediation Guidance) | Complete |
| AIP-01..02 | 26 (Prioritization Narrative) | Complete |
| AID-01 | 27 (Ticket Auto-Drafting) | Complete |
| AIE-01..04 | 28 (Eval + Cost + Observability Gate) | Complete |
| WR-02 | 29 (Harden Forced-Rotation Password Policy) | Complete (backlog-promoted, not in the 21-req set) |
