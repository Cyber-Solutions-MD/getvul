---
phase: 32-asset-exposure-context
plan: 05
type: execute
wave: 4
depends_on: [32-02, 32-03]
autonomous: false
files_modified:
  - frontend/src/lib/queries/use-asset-detail.ts
  - frontend/src/lib/queries/use-asset-groups.ts
  - frontend/src/lib/queries/use-exposure-override.ts
  - frontend/src/lib/queries/keys.ts
  - frontend/src/components/assets/exposure-context-card.tsx
  - frontend/src/components/assets/exposure-context-card.test.tsx
  - frontend/src/app/(authed)/dashboard/assets/[id]/page.tsx
  - frontend/src/app/(authed)/dashboard/asset-groups/page.tsx
  - frontend/src/app/(authed)/dashboard/asset-groups/page.test.tsx
  - frontend/src/components/assets/asset-group-form.tsx
  - frontend/src/components/shell/nav-items.ts
requirements: [EXPO-01, EXPO-03, EXPO-04]
must_haves:
  truths:
    - "The asset detail page shows an exposure-context card displaying business_criticality, data_sensitivity, internet_facing, each with a source badge (auto / manually overridden / group: {name}) sourced from the *_source fields (EXPO-01)"
    - "An admin can override each field inline from the card (flip-edit), calling PATCH /assets/{id}/exposure-context; non-admins see read-only values with no edit affordance (EXPO-03, backend enforces independently)"
    - "An /dashboard/asset-groups management page lists groups with admin-gated create/edit/delete + member management + per-group exposure override, following the sketch-findings design system with mandatory loading/empty/error states (EXPO-04)"
    - "A non-admin visiting asset-groups sees a read-only/explained view (no create/edit/delete affordances); the nav entry routes to the new page"
  artifacts:
    - path: "frontend/src/components/assets/exposure-context-card.tsx"
      provides: "3-field exposure card with source badges + admin flip-edit override controls"
      contains: "exposure-context"
    - path: "frontend/src/lib/queries/use-asset-groups.ts"
      provides: "list/create/update/delete + member + group-override hooks with toast mutations"
      contains: "useAssetGroupsList"
    - path: "frontend/src/app/(authed)/dashboard/asset-groups/page.tsx"
      provides: "AssetGroup management surface with SkeletonTable/PartialFailureBanner/empty states + admin gating"
      contains: "SkeletonTable"
    - path: "frontend/src/lib/queries/use-asset-detail.ts"
      provides: "AssetDetail type extended with the 6 exposure fields"
      contains: "business_criticality"
  key_links:
    - from: "frontend/src/components/assets/exposure-context-card.tsx"
      to: "PATCH /api/v1/assets/{id}/exposure-context"
      via: "useSetExposureOverride mutation"
      pattern: "exposure-context"
    - from: "frontend/src/app/(authed)/dashboard/assets/[id]/page.tsx"
      to: "ExposureContextCard"
      via: "right-rail render next to OwnerCard"
      pattern: "ExposureContextCard"
    - from: "frontend/src/app/(authed)/dashboard/asset-groups/page.tsx"
      to: "/api/v1/asset-groups"
      via: "useAssetGroupsList + CRUD hooks"
      pattern: "asset-groups"
---

<objective>
Surface exposure context and AssetGroup management in the UI. Two surfaces: (1) an
exposure-context card on the asset detail page showing all three fields with a per-field source
badge (auto / manually overridden / group: {name}) and admin-only inline override controls; (2) a
new /dashboard/asset-groups management page with admin-gated CRUD + membership + per-group
override. Both follow the sketch-findings-getvul design system (sunset tokens, copy voice) with
mandatory loading/empty/error states, and admin-gate their controls in the UI as defense-in-depth
(the backend enforces the real boundary via require_admin/require_role).

Analogs (32-PATTERNS.md, exact file:line): owner-card.tsx flip-edit + identity-metadata-rail.tsx
stacked rows for the card; connectors/page.tsx + use-connectors-admin.ts for the management page +
hooks; connectors/page.tsx:98-99 for the admin-gate idiom.
Output: exposure card + override hooks, asset-groups page + form + query hooks + nav entry,
AssetDetail type extension, vitest coverage, and a human-verify checkpoint.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-asset-exposure-context/32-CONTEXT.md
@.planning/phases/32-asset-exposure-context/32-PATTERNS.md
@.planning/phases/32-asset-exposure-context/32-01-SUMMARY.md
@.planning/phases/32-asset-exposure-context/32-03-SUMMARY.md

Design system (READ before building UI, per project CLAUDE.md + 32-PATTERNS):
- .claude/skills/sketch-findings-getvul/references/page-layouts.md (list/detail patterns for the groups page)
- .claude/skills/sketch-findings-getvul/references/state-patterns.md (mandatory loading/empty/error)
- .claude/skills/sketch-findings-getvul/references/visual-language.md (severity/status pill idioms for source badges)
- .claude/skills/sketch-findings-getvul/references/copy-voice.md (no generic SaaS copy)

<interfaces>
<!-- Backend contracts landed by Plans 01/02/03. Snake_case, no transform layer (use-connectors-admin.ts:15). -->

AssetDetail exposure fields (added to router.py inline detail dict by Plan 01/02):
```typescript
business_criticality: string | null;
business_criticality_source: 'AUTO' | 'ASSET_OVERRIDE' | 'GROUP_OVERRIDE' | null;
data_sensitivity: string | null;
data_sensitivity_source: 'AUTO' | 'ASSET_OVERRIDE' | 'GROUP_OVERRIDE' | null;
internet_facing: boolean | null;
internet_facing_source: 'AUTO' | 'ASSET_OVERRIDE' | 'GROUP_OVERRIDE' | null;
```

Endpoints:
- PATCH /api/v1/assets/{id}/exposure-context  body {field, value}  (admin)
- GET/POST /api/v1/asset-groups ; PATCH/DELETE /api/v1/asset-groups/{id} (admin mutate, any authed list)
- POST/DELETE /api/v1/asset-groups/{id}/members/{asset_id} (admin)
- PATCH /api/v1/asset-groups/{id}/exposure-context  body {field, value} (admin)

Admin gate idiom (connectors/page.tsx:98-99):
```tsx
const { user } = useAuth();
const isAdmin = user?.role === 'OWNER' || user?.role === 'ADMIN';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exposure-context card + per-asset override controls on the asset detail page</name>
  <read_first>
    - frontend/src/components/assets/owner-card.tsx (full — flip-edit interaction, IdpPill pill idiom for source badges)
    - frontend/src/components/assets/identity-metadata-rail.tsx (full — MetadataRow stacked-row shape)
    - frontend/src/lib/queries/use-asset-detail.ts:22-52 (AssetDetail type — extend, don't fork)
    - frontend/src/lib/queries/use-reassign-asset.ts (mutation-with-toast + query-invalidate shape for the override hook)
    - frontend/src/app/(authed)/dashboard/assets/[id]/page.tsx:32-33,228-229 (OwnerCard/IdentityMetadataRail right-rail render site)
    - frontend/src/components/assets/owner-card.test.tsx (vitest component-test convention)
  </read_first>
  <behavior>
    - Card renders all three fields with a human-readable value + a source badge: AUTO -> subtle "auto" pill; ASSET_OVERRIDE -> "manually set" pill; GROUP_OVERRIDE -> "group" pill.
    - isAdmin true: each row has an edit affordance that opens a flip-edit control (select for criticality/sensitivity enums, toggle for internet_facing) and on submit calls useSetExposureOverride (PATCH), invalidates the asset-detail query, and toasts success/error.
    - isAdmin false: values are read-only, no edit affordance rendered.
  </behavior>
  <action>
    Extend AssetDetail in use-asset-detail.ts with the 6 exposure fields (interfaces block). Create use-exposure-override.ts (useSetExposureOverride) mirroring use-reassign-asset.ts's mutation+toast+invalidate shape. Create exposure-context-card.tsx: stacked MetadataRow-style rows (criticality, sensitivity, internet-facing), per-field source badge composed from the sunset pill idiom (owner-card IdpPill), admin-gated flip-edit override controls (enum select / boolean toggle) using useSetExposureOverride. Wire <ExposureContextCard asset={a} /> into the asset detail page right rail beside OwnerCard. Copy per copy-voice.md (no "Click here"/"Please..."). Add exposure-context-card.test.tsx covering: renders 3 fields + badges, admin sees edit affordance + submit calls the mutation, non-admin sees read-only. Update the existing assets/[id]/page.test.tsx if the rail assertion needs the new card.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/assets/exposure-context-card.test.tsx src/lib/queries/use-asset-detail.test.ts</automated>
  </verify>
  <done>Exposure card renders all 3 fields with source badges, admin can override inline (mutation + invalidate + toast), non-admins read-only; wired into the asset detail rail; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AssetGroup management page + hooks + form + nav entry</name>
  <read_first>
    - frontend/src/lib/queries/use-connectors-admin.ts (full — list query + create/update/delete mutation-with-toast hooks + queryKeys usage)
    - frontend/src/lib/queries/keys.ts (queryKeys.connectors entry — add a parallel queryKeys.assetGroups)
    - frontend/src/app/(authed)/dashboard/connectors/page.tsx (full — admin-gated list + SkeletonTable/PartialFailureBanner/empty states + ConfirmModal/ResponsiveDialog reuse)
    - frontend/src/components/connectors/connector-form.tsx (add/edit form shape for asset-group-form.tsx)
    - frontend/src/components/shell/nav-items.ts:42 (Connectors nav entry — add an Asset Groups entry)
    - frontend/src/app/(authed)/dashboard/connectors/page.test.tsx (page-test convention)
  </read_first>
  <behavior>
    - Page lists groups (name, description, member count); isPending -> SkeletonTable; error -> PartialFailureBanner; zero groups -> explained empty state (per state-patterns.md, not a bare "No data").
    - isAdmin: create/edit/delete group (ConfirmModal on delete) + manage members + set per-group exposure override, all via the hooks with toasts.
    - non-admin: read-only list, no mutating affordances.
    - Nav entry routes to /dashboard/asset-groups.
  </behavior>
  <action>
    Create use-asset-groups.ts (useAssetGroupsList, useCreateAssetGroup, useUpdateAssetGroup, useDeleteAssetGroup, useAddGroupMember, useRemoveGroupMember, useSetGroupExposureOverride) mirroring use-connectors-admin.ts; add queryKeys.assetGroups to keys.ts. Create asset-group-form.tsx (name/description add-edit form) mirroring connector-form.tsx. Create /dashboard/asset-groups/page.tsx mirroring connectors/page.tsx composition (useAuth -> isAdmin, list hook, SkeletonTable/PartialFailureBanner/empty states, ConfirmModal/ResponsiveDialog reuse, admin-gated CRUD + member + group-override controls). Add the nav entry in nav-items.ts. Add asset-groups/page.test.tsx covering loading/empty/error states, admin CRUD affordances present, non-admin read-only. Follow sketch-findings page-layouts.md (no Tailwind admin-template grid — explicit CLAUDE.md anti-pattern).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/(authed)/dashboard/asset-groups/page.test.tsx"</automated>
  </verify>
  <done>Asset-groups management page lists + admin-CRUDs groups/members/overrides with mandatory states + admin gating; nav entry wired; tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The exposure-context card on the asset detail page (3 fields + source badges + admin inline override) and the /dashboard/asset-groups management page (list + create/edit/delete + members + per-group override), both on the sunset design system with loading/empty/error states and admin gating.</what-built>
  <how-to-verify>
    1. Run the frontend + backend dev stack (per MEMORY getvul-local-e2e-perf-gate). Log in as an admin user.
    2. Open an asset detail page (/dashboard/assets/{id}). Confirm the exposure card shows Business Criticality, Data Sensitivity, Internet-Facing, each with a source badge; confirm an "auto" asset reads auto. Override one field inline; confirm a success toast, the value updates, and the badge flips to "manually set".
    3. Visit /dashboard/asset-groups. Confirm loading skeleton on first load, then the list (or an explained empty state if none). Create a group, add a member (an asset), set a per-group exposure override; confirm the member asset's detail card badge shows "group". Delete the group via the confirm modal.
    4. Log in as a non-admin (analyst/viewer). Confirm the exposure card is read-only (no edit affordance) and the asset-groups page shows no create/edit/delete controls.
    5. Confirm no raw hex colors / non-sunset styling and copy follows the voice (no "Click here"/"Welcome!").
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (screenshots welcome)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (non-admin) → override / group mutation UI | UI must not expose privileged controls to non-admins (defense-in-depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-13 | Elevation of Privilege | exposure card + asset-groups controls | mitigate | isAdmin gate hides all mutating affordances for analyst/viewer; the real boundary is the backend require_admin (Plans 01/03) — UI gating is defense-in-depth, tested for both roles. |
| T-32-14 | Tampering (XSS) | group name/description + rendered field values | accept | React escapes rendered text by default; values are enum-constrained strings from the tenant's own API; no dangerouslySetInnerHTML. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/assets/exposure-context-card.test.tsx "src/app/(authed)/dashboard/asset-groups/page.test.tsx"` green.
- `cd frontend && npx tsc --noEmit && npx eslint src/components/assets/exposure-context-card.tsx "src/app/(authed)/dashboard/asset-groups/page.tsx"` clean.
- Human-verify checkpoint approved.
</verification>

<success_criteria>
- EXPO-01: exposure fields visible on the asset detail page with per-field source badges.
- EXPO-03: admin inline per-asset override works from the card; non-admins read-only.
- EXPO-04: AssetGroup management page with admin CRUD + membership + per-group override, on the design system with mandatory states.
</success_criteria>

<output>
After completion, create `.planning/phases/32-asset-exposure-context/32-05-SUMMARY.md`.
Record: the source-badge visual mapping, the asset-groups page composition, and the human-verify outcome.
</output>
