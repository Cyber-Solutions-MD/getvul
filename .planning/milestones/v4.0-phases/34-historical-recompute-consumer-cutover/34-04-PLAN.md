---
phase: 34-historical-recompute-consumer-cutover
plan: 04
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - backend/app/vulnerabilities/trends.py
  - backend/app/notifications/alerts.py
  - backend/tests/test_risk_boundary_guard.py
autonomous: true
requirements: [RISK-10]
must_haves:
  truths:
    - "capture_daily_snapshot writes, UNCONDITIONALLY (independent of the cutover flag), three new metrics keys every day: avg_risk_exposure_score (scalar), asset_risk_scores ({asset_id:old_score} — populated for the FIRST time ever, the dead-code fix) and asset_risk_exposure_scores ({asset_id:new_score}), plus risk_model_version_snapshot (34-CONTEXT RESOLVED A2)"
    - "_check_risk_score_changes actually fires now (previously dead: it read snapshot.metrics['asset_risk_scores'] which capture_daily_snapshot never wrote → returned 0 for every tenant every day) — proven by a same-version large-delta control that asserts a NON-zero alert count (Pitfall 2)"
    - "_check_risk_score_changes diffs SAME-VERSION-to-SAME-VERSION only: new-vs-new when the flag is ON, old-vs-new... NO — old-vs-old when OFF; NEVER new-vs-old or old-vs-new (34-RESEARCH.md 308-312)"
    - "A fixture spanning the cutover boundary (yesterday+today snapshots each with BOTH old and new dicts at genuine small drift; flag toggled OFF→ON between them) produces ZERO storm alerts on the boundary day (same-version comparison) — no alert storm"
    - "get_risk_score_trend reads a continuous, non-cliff series across the boundary for whichever series the flag selects — no trend cliff"
    - "The dual-write is what makes both guarantees structural: real new-model history accumulates while the flag is OFF, so the day a human flips it there is no synthetic scale-jump to alert on or to draw a cliff for"
  artifacts:
    - path: "backend/app/vulnerabilities/trends.py"
      provides: "capture_daily_snapshot dual-write (avg_risk_exposure_score + asset_risk_scores + asset_risk_exposure_scores + risk_model_version_snapshot in the metrics dict); get_risk_score_trend continuity-aware read"
      contains: "asset_risk_exposure_scores"
    - path: "backend/app/notifications/alerts.py"
      provides: "_check_risk_score_changes fixed (reads the now-populated dicts) + version-boundary branch (new-vs-new ON, old-vs-old OFF), keyed off tenant.cutover_risk_exposure_scoring"
      contains: "cutover_risk_exposure_scoring"
    - path: "backend/tests/test_risk_boundary_guard.py"
      provides: "snapshot-populates-both-dicts, genuine-spike-still-alerts control, cutover-boundary-no-storm, trend-no-cliff"
      contains: "def test_cutover_boundary_no_storm_no_cliff"
  key_links:
    - from: "backend/app/vulnerabilities/trends.py::capture_daily_snapshot"
      to: "DailySnapshot.metrics[asset_risk_scores / asset_risk_exposure_scores / avg_risk_exposure_score]"
      via: "one bulk Asset select building both dicts, added to the metrics literal unconditionally"
      pattern: "asset_risk_exposure_scores"
    - from: "backend/app/notifications/alerts.py::_check_risk_score_changes"
      to: "tenants.cutover_risk_exposure_scoring + the matching same-version snapshot dict"
      via: "read the flag once, diff new-vs-new (ON) or old-vs-old (OFF)"
      pattern: "asset_risk_exposure_scores"
---

<objective>
RISK-10 version-boundary guards: eliminate the trend cliff and the alert storm STRUCTURALLY by
dual-writing the new-model risk metrics into every DailySnapshot from this phase forward (unconditional
on the cutover flag), fixing the dead _check_risk_score_changes read (it reads a snapshot key
capture_daily_snapshot never wrote → has returned 0 for every tenant every day since it was written),
and version-boundary-guarding both the spike notification and the trend chart so a risk_model_version
change across a day boundary yields neither a storm nor a cliff — proven by a boundary-spanning fixture
(34-CONTEXT RESOLVED A2).

Purpose: By the time a human ever flips cutover_risk_exposure_scoring on a live stack, DailySnapshot rows
back to this phase's deploy already carry real avg_risk_exposure_score + asset_risk_exposure_scores — the
new series has genuine history, so there is no "day 1 of the new metric" at cutover time. The guard is
same-version-only diffing: new-vs-new when ON, old-vs-old when OFF, never cross-version.

Output: the trends.py dual-write + continuity read, the alerts.py dead-code fix + boundary branch, and the
RISK-10 boundary fixture suite (including the Pitfall-2 non-zero control that proves the check actually fires).
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-historical-recompute-consumer-cutover/34-CONTEXT.md
@.planning/phases/34-historical-recompute-consumer-cutover/34-RESEARCH.md
@.planning/phases/34-historical-recompute-consumer-cutover/34-PATTERNS.md

<interfaces>
<!-- Plan 01 landed Tenant.cutover_risk_exposure_scoring. Asset.risk_exposure_score exists from Phase 33. -->

trends.py capture_daily_snapshot (:278-318) — copy the existing avg_risk func.avg pattern for the new avg,
add ONE bulk Asset select building both per-asset dicts, extend the metrics literal (all UNCONDITIONAL):
```python
from app.vulnerabilities.risk_exposure_service import RISK_MODEL_VERSION
avg_risk_exposure = (await db.execute(
    select(func.avg(Asset.risk_exposure_score)).where(
        Asset.tenant_id == tenant_id, Asset.is_ignored.is_(False), Asset.risk_exposure_score.isnot(None))
)).scalar_one()
# one bulk fetch for BOTH dicts (mirror the total_assets filter shape trends.py:288-290):
asset_score_rows = (await db.execute(
    select(Asset.id, Asset.risk_score, Asset.risk_exposure_score).where(
        Asset.tenant_id == tenant_id, Asset.is_ignored.is_(False))
)).all()
metrics["asset_risk_scores"] = {str(r.id): r.risk_score for r in asset_score_rows if r.risk_score is not None}
metrics["asset_risk_exposure_scores"] = {str(r.id): r.risk_exposure_score for r in asset_score_rows if r.risk_exposure_score is not None}
metrics["avg_risk_exposure_score"] = round(float(avg_risk_exposure), 1) if avg_risk_exposure else 0
metrics["risk_model_version_snapshot"] = RISK_MODEL_VERSION
```
get_risk_score_trend (:165-186): keep reading avg_risk_score by default; add avg_risk_exposure to each
row dict so a continuity-aware consumer can read the new series (do NOT change the existing avg_risk field
name — the OFF/pre-flip wire contract stays identical; expose the new value as an additional key). Confirm
the exact additional-key name in the SUMMARY.

alerts.py _check_risk_score_changes (:189-255) — the READ side is otherwise correct; add the version branch:
```python
tenant.cutover_risk_exposure_scoring  # read once
# ON  -> yesterday = snapshot.metrics.get("asset_risk_exposure_scores", {}); new_score = asset.risk_exposure_score or 0
# OFF -> yesterday = snapshot.metrics.get("asset_risk_scores", {});          new_score = asset.risk_score or 0
# the >= 20 delta test, _notification_exists dedup, and create_notification call stay byte-identical.
```
The Asset query at :212-224 filters risk_score.isnot(None) on OFF; on ON it should select assets with
risk_exposure_score.isnot(None) (so both branches compare a populated current value against the
matching-version yesterday dict). _check_risk_score_changes receives (db, tenant: Tenant) — the flag is on
the passed tenant object already.

DO NOT touch sla_service.py, service.py, rule_engine.py. capture_all_snapshots (trends.py:332+) fans this
out per tenant unchanged. The dual-write is unconditional (NOT gated on the flag) — 34-CONTEXT locked.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — snapshot dual-write, genuine-spike control (non-zero), boundary no-storm, trend no-cliff</name>
  <read_first>
    - backend/tests/test_severity_trends.py:50-83 (capture_daily_snapshot output-shape + DailySnapshot direct-construction fixture idiom + tenant_a fixture signature)
    - backend/app/notifications/alerts.py:189-255 (_check_risk_score_changes full body — the dead read at :208, the diff loop at :226-234) and :23-35 (run_alert_checks caller passing a Tenant)
    - backend/app/vulnerabilities/trends.py:278-329 (the avg_risk block + metrics dict to extend)
    - backend/tests/conftest.py (db_session, tenant_a; how to set tenant.cutover_risk_exposure_scoring; DailySnapshot import + snapshot_date construction)
    - MEMORY getvul-backend-pytest-env
  </read_first>
  <behavior>
    - test_snapshot_populates_asset_risk_dicts: seed a tenant with 2 assets (Asset.risk_score and Asset.risk_exposure_score set); call capture_daily_snapshot; the stored DailySnapshot.metrics has non-empty asset_risk_scores AND asset_risk_exposure_scores (keyed by str(asset.id)), a numeric avg_risk_exposure_score, and risk_model_version_snapshot == RISK_MODEL_VERSION.
    - test_genuine_spike_still_alerts (Pitfall 2 control — MUST assert NON-zero): build a "yesterday" DailySnapshot with asset_risk_scores = {asset: 10} (flag OFF), set the live Asset.risk_score = 40 (a real +30 same-version delta); call _check_risk_score_changes → returns >= 1 alert. This proves the check actually fires (not the old dead no-op).
    - test_genuine_spike_new_version_alerts: same control but flag ON — yesterday asset_risk_exposure_scores={asset:10}, live Asset.risk_exposure_score=40 → >= 1 alert (proves the ON branch also fires).
    - test_cutover_boundary_no_storm_no_cliff: yesterday snapshot has BOTH dicts at genuine values (old={a:70}, new={a:22}); today the flag is toggled OFF→ON and the live Asset.risk_exposure_score is ~24 (small genuine new-model drift) while Asset.risk_score is ~72; call _check_risk_score_changes with the flag ON → ZERO alerts (it compares today's new 24 vs yesterday's new 22, a +2 delta < 20 — NOT new 24 vs old 70 which would falsely storm). Assert 0.
    - test_trend_no_cliff: given a 2-day slice where avg_risk_score jumps (old-scale) but avg_risk_exposure_score is continuous, get_risk_score_trend's new-series values are continuous across the boundary (no vertical jump in the new series).
  </behavior>
  <action>
    Author backend/tests/test_risk_boundary_guard.py. Module docstring names Phase 34 Plan 04 + RISK-10 and states the dead-code bug being fixed (asset_risk_scores never written → check always returned 0) and that the boundary guard is meaningless without first making the check fire (Pitfall 2 — hence the non-zero control). Construct DailySnapshot rows directly (mirror test_severity_trends.py), call alerts._check_risk_score_changes / trends.capture_daily_snapshot / trends.get_risk_score_trend directly. Set the tenant flag via a direct column update+commit. RED: capture_daily_snapshot does not write the new dicts yet (dict-key assertions fail) and _check_risk_score_changes still returns 0 for the genuine-spike control (non-zero assertion fails). Env prefix per MEMORY.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret .venv/bin/python -m pytest tests/test_risk_boundary_guard.py -x 2>&1 | grep -Eiq "assert|failed|keyerror|error" && echo RED-OK</automated>
  </verify>
  <done>test_risk_boundary_guard.py exists; the dict-population and genuine-spike (non-zero) tests fail for real, proving the dead-code path and the missing dual-write are falsifiable before the fix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN part 1 — capture_daily_snapshot dual-write + get_risk_score_trend continuity read</name>
  <read_first>
    - backend/app/vulnerabilities/trends.py:278-329 (avg_risk func.avg block + the metrics literal + DailySnapshot construction) and :165-186 (get_risk_score_trend row mapping)
    - backend/app/assets/models.py (Asset.risk_exposure_score exists from Phase 33; Asset.is_ignored)
    - 34-PATTERNS.md §"trends.py:306-318" (the exact dual-write shape + one-bulk-fetch-for-both-dicts note)
  </read_first>
  <behavior>
    - test_snapshot_populates_asset_risk_dicts + test_trend_no_cliff GREEN.
    - The two genuine-spike controls collect (they still need the alerts.py fix in Task 3 for the ON branch, but OFF control can already pass once the write side exists + the read is fixed in Task 3).
  </behavior>
  <action>
    In capture_daily_snapshot: import RISK_MODEL_VERSION; add the avg_risk_exposure func.avg query (copy the avg_risk pattern verbatim, swap the column); add ONE bulk select(Asset.id, Asset.risk_score, Asset.risk_exposure_score) tenant-scoped + is_ignored filter; build both dicts; add asset_risk_scores, asset_risk_exposure_scores, avg_risk_exposure_score, risk_model_version_snapshot to the metrics dict — ALL unconditional (no flag check). In get_risk_score_trend: add avg_risk_exposure (from metrics.get("avg_risk_exposure_score", 0)) as an ADDITIONAL key on each row dict; keep the existing avg_risk field name/source unchanged. Touch nothing else in trends.py.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret .venv/bin/python -m pytest tests/test_risk_boundary_guard.py -x -k "populates or cliff"</automated>
  </verify>
  <done>capture_daily_snapshot dual-writes the 3 metric keys + version snapshot unconditionally via one extra bulk Asset fetch; get_risk_score_trend exposes the continuous new series; the populate + no-cliff tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN part 2 — _check_risk_score_changes dead-code fix + version-boundary branch</name>
  <read_first>
    - backend/app/notifications/alerts.py:189-255 (full _check_risk_score_changes body) — the :208 dead read, the :212-224 Asset query, the :226-234 diff loop, the _notification_exists/create_notification calls
    - 34-RESEARCH.md:308-316 (same-version-only diffing rule + boundary fixture design) and Pitfall 2
    - 34-PATTERNS.md §"alerts.py:189-255" (the version-branch shape; the dedup/notify calls stay byte-identical)
  </read_first>
  <behavior>
    - ALL test_risk_boundary_guard.py tests GREEN: both genuine-spike controls fire (>=1), the boundary case returns 0 (no storm), populate + no-cliff already green from Task 2.
  </behavior>
  <action>
    In _check_risk_score_changes: read tenant.cutover_risk_exposure_scoring once. When ON: read yesterday's asset_risk_exposure_scores dict, select current assets with risk_exposure_score.isnot(None), compare new_score = asset.risk_exposure_score against yesterday's new value. When OFF: read asset_risk_scores, select assets with risk_score.isnot(None), compare asset.risk_score against yesterday's old value. NEVER cross-version. Keep the `>= 20` delta threshold, the _notification_exists(hours=24) dedup, and the create_notification call byte-identical (details still {old_score,new_score,delta}). The early-return-0-if-no-yesterday-dict guard stays (but now the dict is actually populated by Task 2, so the check genuinely fires). Touch no other function in alerts.py.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret .venv/bin/python -m pytest tests/test_risk_boundary_guard.py -x</automated>
  </verify>
  <done>_check_risk_score_changes reads the now-populated dicts, diffs same-version-only (new-vs-new ON, old-vs-old OFF); genuine same-version spikes still alert (>=1, Pitfall-2 control), the boundary fixture produces 0 storm alerts and a continuous trend; all RISK-10 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-tenant snapshot capture → per-asset score dicts | the bulk Asset fetch building both dicts must be tenant-scoped |
| risk_model_version change across a day boundary | a naive cross-version diff would misfire a storm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-13 | Information Disclosure | capture_daily_snapshot bulk Asset fetch for the two dicts | mitigate | The select filters Asset.tenant_id == tenant_id + is_ignored, identical to the existing total_assets/avg_risk queries in the same function; test seeds a single tenant. |
| T-34-14 | Tampering / DoS (alert storm) | _check_risk_score_changes cross-version diff at the boundary | mitigate | Same-version-only diffing (new-vs-new ON, old-vs-old OFF); the boundary fixture asserts 0 storm alerts and the Pitfall-2 control asserts a real same-version spike still fires (so the 0 isn't a vacuous dead-code pass). |
| T-34-15 | Availability (trend cliff) | get_risk_score_trend across the boundary | accept (structural) | The dual-write accumulates real new-model history while the flag is OFF, so the new series is continuous at flip time; the existing avg_risk field name/source is unchanged (no wire-contract break). |
</threat_model>

<verification>
- `cd backend && ENCRYPTION_KEY=... JWT_SECRET_KEY=... .venv/bin/python -m pytest tests/test_risk_boundary_guard.py -x` green.
- Dead-code-actually-fixed gate (Pitfall 2): the suite includes a control asserting a NON-zero alert count for a genuine same-version spike (a green boundary-zero without this control proves nothing).
- Unconditional-dual-write gate: `grep -n "cutover_risk_exposure_scoring" backend/app/vulnerabilities/trends.py` returns NOTHING (the dual-write must NOT be flag-gated).
- Regression: `... pytest tests/test_severity_trends.py -x` still green (existing snapshot shape assertions unbroken by the additive keys).
- Manual re-read (verifier, per 34-VALIDATION sampling): confirm capture_daily_snapshot writes asset_risk_scores and _check_risk_score_changes reads the matching version dict — that the dead-code fix is real, not just fixture-passed.
</verification>

<success_criteria>
- SC#4 (RISK-10): _check_risk_score_changes + the trend chart are version-boundary-guarded; a boundary-spanning fixture produces neither an alert storm (0 alerts, with a non-zero control proving the check fires) nor a trend cliff (continuous new series); the dead asset_risk_scores read is fixed; the dual-write is unconditional (continuity exists before any flip).
</success_criteria>

<output>
After completion, create `.planning/phases/34-historical-recompute-consumer-cutover/34-04-SUMMARY.md`.
Record: the exact new metrics keys written; the additional get_risk_score_trend key name for the new
series; confirmation the dual-write is unconditional (grep evidence) and _check_risk_score_changes now
genuinely fires (the non-zero control value); the same-version-only diff branch; and that test_severity_trends
stayed green.
</output>
