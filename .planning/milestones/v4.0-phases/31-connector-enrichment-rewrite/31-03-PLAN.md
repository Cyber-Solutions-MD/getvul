---
phase: 31-connector-enrichment-rewrite
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/app/connectors/crowdstrike.py
  - backend/app/connectors/nessus.py
  - backend/tests/test_connectors/test_crowdstrike_connector.py
  - backend/tests/test_connectors/test_nessus_connector.py
autonomous: true
requirements: [ENRICH-03, ENRICH-04, ENRICH-06]
must_haves:
  truths:
    - "A CrowdStrike finding whose cached cve metadata has exprt_rating gets native_priority_rating = the raw ExPRT category verbatim (no re-scale), read next to the existing exploit_status read with zero new API calls (SC#3)"
    - "CrowdStrike's own KEV-ish guess (exploit_status>=50) is preserved into source_signals for provenance only — it does NOT set the cisa_kev column (D-04, that authority moved to the write-path in Plan 01)"
    - "A Nessus finding with a VPR-shaped plugin_attributes value gets native_priority_score = the raw VPR number; when the field is absent the value stays None (soft-null, no crash — defensive probe)"
    - "Both connectors' _normalize_vuln set native_priority_score/native_priority_rating/source_signals on every emitted NormalizedVulnerability (ENRICH-06), and source_signals models missing-vs-negative via raw-key reads"
  artifacts:
    - path: "backend/app/connectors/crowdstrike.py"
      provides: "exprt_rating → native_priority_rating (+score if a numeric companion exists) via ad-hoc attr attach; source_signals allowlist incl. the exploit_status>=50 KEV-ish guess as provenance"
    - path: "backend/app/connectors/nessus.py"
      provides: "VPR defensive probe → native_priority_score; source_signals allowlist"
  key_links:
    - from: "backend/app/connectors/crowdstrike.py::_normalize_vuln"
      to: "native_priority_rating / source_signals"
      via: "cve_meta.get('exprt_rating') read at line ~359"
      pattern: "exprt_rating"
    - from: "backend/app/connectors/nessus.py::_normalize_vuln"
      to: "native_priority_score"
      via: "plugin_attributes defensive .get()"
      pattern: "plugin_attributes"
  prohibitions:
    - "Do NOT normalize/re-scale VPR or ExPRT onto a common scale — store raw values verbatim (D-06)"
    - "Do NOT let the connector's KEV-ish guess set the cisa_kev column — provenance only, in source_signals (D-04)"
    - "Do NOT 'fix' CrowdStrike's stale docstring >=30 into the code — the live code checks >=50; preserve >=50 verbatim (Pitfall 3)"
    - "Do NOT duplicate promoted or PII-adjacent fields (cve/cvss/severity/epss/kev/native_priority/hostname/ip_addresses) into source_signals (D-08/V8)"
    - "Do NOT crash on an absent native field — defensive .get() → None (A1)"
---

<objective>
ENRICH-03/04/06 expansion — thread the two native-composite connectors that need ZERO new
API calls or the lowest-risk field probe: CrowdStrike (ExPRT.AI rating, already cached) and
Nessus (VPR, defensive plugin_attributes probe). Each populates native_priority_* from its
own already-fetched payload and captures its risk/exploit-relevant fields into source_signals.

Purpose: Extend the Plan 01 spine to 3 of 6 connectors. Independent of Plans 02/04/05 (disjoint
files).
Output: enriched crowdstrike.py + nessus.py + their connector tests.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-connector-enrichment-rewrite/31-RESEARCH.md
@.planning/phases/31-connector-enrichment-rewrite/31-PATTERNS.md
@.planning/phases/31-connector-enrichment-rewrite/31-01-SUMMARY.md

<interfaces>
<!-- NormalizedVulnerability now carries native_priority_score/native_priority_rating/source_signals (Plan 01). -->

CrowdStrike (crowdstrike.py:350-408): read `cve_meta.get("exprt_rating")` at ~line 359 right
beside the existing `cve_meta.get("exploit_status", 0)`. ExPRT rating is a categorical enum
(UNKNOWN/LOW/MEDIUM/HIGH/CRITICAL) → native_priority_rating (raw string). No confirmed numeric
companion → native_priority_score stays None unless one is present in the payload. Set via the
file's OWN "ad-hoc attribute attach after construction" idiom (like remediation_id/exploit_status_id
at 404-407), NOT constructor kwargs. The exploit_status value (incl. the >=50 KEV-ish threshold)
goes into source_signals as provenance.

Nessus (nessus.py:233-299): mirror `_check_exploit_available`'s `attrs = vuln.get("plugin_attributes", {})`
defensive probe. VPR field name UNVERIFIED (A1) — probe candidates in order: attrs.get("vpr_score"),
attrs.get("vpr"). Coerce to float defensively. Put the result + exploit-relevant fields in the
`base` dict so they flow into every per-CVE NormalizedVulnerability(cve_id=cve, **base) fanout.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CrowdStrike — ExPRT rating → native_priority_rating + source_signals (RED→GREEN)</name>
  <read_first>
    - backend/app/connectors/crowdstrike.py:350-408 (_normalize_vuln; exploit_status read at 359; ad-hoc attr attach at 404-407; docstring >=30 vs code >=50 discrepancy at 11/362)
    - backend/tests/test_connectors/test_crowdstrike_connector.py (extend; _install_mock_transport convention)
    - 31-RESEARCH.md Per-Connector table (CrowdStrike row) + Pitfall 3
    - 31-PATTERNS.md §crowdstrike.py
  </read_first>
  <behavior>
    - Given a cached cve_meta with exprt_rating="HIGH" and exploit_status=50, _normalize_vuln returns a finding with native_priority_rating=="HIGH" (raw), and source_signals contains the exploit_status value (provenance) but NOT the cisa_kev column authority.
    - Given cve_meta WITHOUT exprt_rating, native_priority_rating is None (no crash).
    - No PII-adjacent or already-promoted keys in source_signals.
  </behavior>
  <action>
    Extend test_crowdstrike_connector.py with the RED assertions above (fail: fields not populated yet). Then in crowdstrike.py read cve_meta.get("exprt_rating") beside the existing exploit_status read; attach vuln.native_priority_rating = that raw value (or None), vuln.native_priority_score = a numeric companion if present else None, via the ad-hoc-attribute idiom. Build source_signals inline from the RAW cve_meta dict for the connector's risk/exploit fields (e.g. exploit_status, and its >=50 KEV-ish guess as a provenance boolean) — add keys only when the vendor actually returned them (D-07). Preserve the >=50 threshold verbatim; do not touch the stale docstring's number in code. Exclude promoted/PII fields (D-08/V8).
  </action>
  <acceptance_criteria>
    - Completion state (measurable): CrowdStrike native_priority_rating = raw ExPRT category, source_signals carries provenance (incl. the KEV-ish guess) with no PII/promoted keys, absent-field → None.
    - Automated check passes: `ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret python -m pytest backend/tests/test_connectors/test_crowdstrike_connector.py -x`
  </acceptance_criteria>

  <verify>
    <automated>ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret python -m pytest backend/tests/test_connectors/test_crowdstrike_connector.py -x</automated>
  </verify>
  <done>CrowdStrike native_priority_rating = raw ExPRT category, source_signals carries provenance (incl. the KEV-ish guess) with no PII/promoted keys, absent-field → None.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Nessus — VPR defensive probe → native_priority_score + source_signals (RED→GREEN)</name>
  <read_first>
    - backend/app/connectors/nessus.py:233-299 (_check_exploit_available defensive plugin_attributes probe; _normalize_vuln `base` dict + per-CVE fanout)
    - backend/tests/test_connectors/test_nessus_connector.py (extend; or test_connector_normalization.py's nessus_normalize helper style at 83-122 — but keep edits in the dedicated connector test file to preserve file ownership)
    - 31-RESEARCH.md Per-Connector table (Nessus row) + Assumptions Log A1 (VPR field-name soft-null)
    - 31-PATTERNS.md §nessus.py (put new fields in `base` so they fan out per CVE)
  </read_first>
  <behavior>
    - Given a vuln with plugin_attributes containing a VPR-shaped value (test both vpr_score and vpr key candidates), every emitted per-CVE finding has native_priority_score == the raw float.
    - Given plugin_attributes WITHOUT any VPR key, native_priority_score is None on all emitted findings (soft-null, no crash).
    - source_signals carries Nessus's exploit-relevant plugin_attributes (raw keys, missing-vs-negative), no PII/promoted keys.
  </behavior>
  <action>
    Extend test_nessus_connector.py with the RED assertions. Then in nessus.py add a defensive VPR probe mirroring _check_exploit_available (`attrs = vuln.get("plugin_attributes", {})`; try vpr_score then vpr; float-coerce defensively; None if absent). Add native_priority_score (+ native_priority_rating None — Nessus VPR is numeric-only) and a source_signals dict (raw-key allowlist of exploit-relevant plugin_attributes, missing-vs-negative, excluding promoted/PII) into the `base` dict so they propagate to every NormalizedVulnerability(cve_id=cve, **base). Document the A1 assumption (exact VPR field name unverified → soft-null) in a code comment.
  </action>
  <acceptance_criteria>
    - Completion state (measurable): Nessus native_priority_score = raw VPR (or None when absent, no crash) on every fanned-out CVE; source_signals models missing-vs-negative with no PII/promoted keys.
    - Automated check passes: `ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret python -m pytest backend/tests/test_connectors/test_nessus_connector.py -x`
  </acceptance_criteria>

  <verify>
    <automated>ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret python -m pytest backend/tests/test_connectors/test_nessus_connector.py -x</automated>
  </verify>
  <done>Nessus native_priority_score = raw VPR (or None when absent, no crash) on every fanned-out CVE; source_signals models missing-vs-negative with no PII/promoted keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendor payload → source_signals | Scanner-returned fields cross into persisted JSONB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Information Disclosure | crowdstrike.py / nessus.py source_signals allowlists | mitigate | Curated per-connector allowlist keyed by raw vendor field; explicitly exclude hostname/ip_addresses/last_login_user/serial_number and all promoted columns (D-08/V8); tests assert no PII keys. |
| T-31-09 | Tampering (self-inflicted) | CrowdStrike >=50 KEV threshold drift | mitigate | Preserve the live-code >=50 verbatim when moving it into source_signals provenance; do not adopt the stale docstring's >=30 (Pitfall 3) — avoids silently reclassifying 30-49-band findings. |
</threat_model>

<verification>
- Both connector test files green.
- Regression: `... python -m pytest backend/tests/test_connector_normalization.py -x` still green (this plan does not edit that shared file; the cross-6 sweep is Plan 05).
- A1 (VPR) / ExPRT numeric-companion assumptions are soft-null by construction — flag in SUMMARY for empirical re-verify against a live Nessus 10.5+/Falcon instance.
</verification>

<success_criteria>
- SC#3 (partial): CrowdStrike + Nessus native_priority_* populated from raw vendor values, sortable via the Plan 01 columns/indexes.
- ENRICH-04/06: both connectors set all 3 new dataclass fields with correct missing-vs-negative source_signals.
</success_criteria>

<output>
After completion, create `.planning/phases/31-connector-enrichment-rewrite/31-03-SUMMARY.md`.
Record the exact ExPRT + VPR field paths used, whether a CrowdStrike numeric companion existed,
and each connector's final source_signals allowlist keys. Flag A1/ExPRT-numeric as needing live re-verify.
</output>
