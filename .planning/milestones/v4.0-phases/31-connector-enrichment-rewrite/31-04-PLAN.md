---
phase: 31-connector-enrichment-rewrite
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/app/connectors/qualys.py
  - backend/app/connectors/rapid7.py
  - backend/tests/test_connectors/test_qualys_connector.py
  - backend/tests/test_connectors/test_rapid7_connector.py
autonomous: true
requirements: [ENRICH-03, ENRICH-04, ENRICH-06]
must_haves:
  truths:
    - "A Qualys detection carrying QDS gets native_priority_score = the raw QDS number (1-100), read from the per-DETECTION dict (not the QID-keyed kb_cache), with show_qds_factors=1 added to the detection request params (SC#3, Pitfall 4)"
    - "A Rapid7 finding gets native_priority_score = the raw riskScore read off the per-asset AssetVulnerability association entry (vuln_entry), not the vendor-neutral detail resource (SC#3, Pitfall 5)"
    - "Both connectors soft-null native_priority_score when the field is absent (no crash — the field names are A2/A3 assumptions)"
    - "Both connectors' _normalize_* set all 3 new dataclass fields on every emitted finding (ENRICH-06), with missing-vs-negative source_signals from raw keys"
  artifacts:
    - path: "backend/app/connectors/qualys.py"
      provides: "show_qds_factors=1 param + QDS read from detection dict → native_priority_score; source_signals allowlist"
    - path: "backend/app/connectors/rapid7.py"
      provides: "riskScore off vuln_entry → native_priority_score; source_signals allowlist"
  key_links:
    - from: "backend/app/connectors/qualys.py::_normalize_detection"
      to: "native_priority_score"
      via: "detection dict QDS read (NOT kb_cache)"
      pattern: "show_qds_factors"
    - from: "backend/app/connectors/rapid7.py::fetch_vulnerabilities"
      to: "native_priority_score"
      via: "vuln_entry.get('riskScore') before the per-CVE loop"
      pattern: "riskScore"
  prohibitions:
    - "Do NOT read QDS from kb_cache/kb — it is a per-detection field, not a QID-level KB constant (Pitfall 4)"
    - "Do NOT read Rapid7 riskScore off the vendor-neutral detail resource — it lives on the per-asset vuln_entry association (Pitfall 5)"
    - "Do NOT normalize/re-scale QDS or riskScore — raw values verbatim (D-06)"
    - "Do NOT duplicate promoted or PII-adjacent fields into source_signals (D-08/V8)"
    - "Do NOT crash on absent native field — defensive .get() → None (A2/A3)"
---

<objective>
ENRICH-03/04/06 expansion — thread the two remaining native-composite connectors whose signal
lives in a non-obvious place: Qualys (QDS, a per-detection field requiring a request-param
addition) and Rapid7 (Risk Score, on the per-asset association entry, not the vuln definition).
Both are documented pitfalls (4 and 5); getting the read LOCATION right is the whole task.

Purpose: Complete native_priority_* coverage for all 4 composite-signal connectors (with
CrowdStrike/Nessus from Plan 03). Independent of Plans 02/03/05 (disjoint files).
Output: enriched qualys.py + rapid7.py + their connector tests.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-connector-enrichment-rewrite/31-RESEARCH.md
@.planning/phases/31-connector-enrichment-rewrite/31-PATTERNS.md
@.planning/phases/31-connector-enrichment-rewrite/31-01-SUMMARY.md

<interfaces>
<!-- NormalizedVulnerability carries native_priority_score/native_priority_rating/source_signals (Plan 01). -->

Qualys (qualys.py:190-244 fetch params, 555-604 normalize): add "show_qds_factors": 1 to the
_fetch_all_detections params dict (~198-203). Read QDS from the per-DETECTION `detection` dict
inside _normalize_detection — NOT from kb_cache/kb (Pitfall 4). Exact element name A3-unverified
→ probe defensively (the existing _xml_to_dict fallback surfaces whatever key Qualys returns).
Put QDS + exploit-relevant detection fields in the `base` dict → fans out per CVE.

Rapid7 (rapid7.py:170-258): riskScore lives on `vuln_entry` (the /api/3/assets/{id}/vulnerabilities
association entry, currently only vuln_entry.get("id") is extracted at 213 — everything else
discarded), NOT on `detail`. Capture vuln_entry.get("riskScore") BEFORE the inner `for cve_id in cves:`
loop so it is available at each NormalizedVulnerability(...) construction. Field name A2-unverified
→ defensive .get() → None.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Qualys — QDS from detection dict → native_priority_score + source_signals (RED→GREEN)</name>
  <read_first>
    - backend/app/connectors/qualys.py:190-244 (_fetch_all_detections params) + 555-604 (_normalize_detection: kb_cache reads are QID-level; `base` dict + per-CVE fanout)
    - backend/tests/test_connectors/test_qualys_connector.py (extend; _install_mock_transport)
    - 31-RESEARCH.md Pitfall 4 + Assumptions Log A3 (show_qds_factors param + element name unverified → soft-null)
    - 31-PATTERNS.md §qualys.py
  </read_first>
  <behavior>
    - A detection dict carrying a QDS value → every emitted per-CVE finding has native_priority_score == raw QDS (1-100); native_priority_rating None (QDS is numeric-only).
    - A detection WITHOUT QDS → native_priority_score None on all findings (no crash).
    - QDS is read from the detection dict, not kb_cache (assert: a kb_cache carrying a QDS-shaped key does NOT populate native_priority_score).
    - source_signals from raw detection exploit-relevant keys (missing-vs-negative), no PII/promoted keys.
  </behavior>
  <action>
    Extend test_qualys_connector.py with the RED assertions. Then add "show_qds_factors": 1 to the _fetch_all_detections params dict. In _normalize_detection read the QDS value from the `detection` dict (defensive .get() across the likely element names; float-coerce), add native_priority_score (+ rating None) and a source_signals allowlist (raw detection keys, missing-vs-negative, exclude promoted/PII) to the `base` dict so they fan out per CVE. Comment the A3 assumption (soft-null on wrong element name).
  </action>
  <acceptance_criteria>
    - Completion state (measurable): Qualys native_priority_score = raw QDS from the detection record (not kb_cache), soft-null when absent; source_signals models missing-vs-negative, no PII.
    - Automated check passes: `ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret python -m pytest backend/tests/test_connectors/test_qualys_connector.py -x`
  </acceptance_criteria>

  <verify>
    <automated>ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret python -m pytest backend/tests/test_connectors/test_qualys_connector.py -x</automated>
  </verify>
  <done>Qualys native_priority_score = raw QDS from the detection record (not kb_cache), soft-null when absent; source_signals models missing-vs-negative, no PII.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rapid7 — riskScore off vuln_entry → native_priority_score + source_signals (RED→GREEN)</name>
  <read_first>
    - backend/app/connectors/rapid7.py:170-258 (_fetch_asset_vulns association entries; the `for vuln_entry in asset_vulns:` loop at ~212 where only vuln_entry["id"] is used; detail-resource reads; per-CVE fanout)
    - backend/tests/test_connectors/test_rapid7_connector.py (extend)
    - 31-RESEARCH.md Pitfall 5 + Assumptions Log A2 (riskScore field name + resource location unverified → soft-null)
    - 31-PATTERNS.md §rapid7.py
  </read_first>
  <behavior>
    - A vuln_entry carrying riskScore → every emitted per-CVE finding has native_priority_score == raw riskScore (0-1000); rating None.
    - A vuln_entry WITHOUT riskScore → native_priority_score None (no crash).
    - riskScore is read from vuln_entry, not detail (assert: a detail carrying a riskScore-shaped key does NOT populate the column).
    - source_signals from raw exploit-relevant keys, no PII/promoted keys.
  </behavior>
  <action>
    Extend test_rapid7_connector.py with the RED assertions. In rapid7.py capture `risk_score = vuln_entry.get("riskScore")` (defensive, float-coerce, None if absent) BEFORE the inner per-CVE loop; set native_priority_score=risk_score (+ rating None) and a source_signals allowlist (raw exploit-relevant keys, missing-vs-negative, exclude promoted/PII) on each NormalizedVulnerability. Comment the A2 assumption (soft-null on wrong field name/location).
  </action>
  <acceptance_criteria>
    - Completion state (measurable): Rapid7 native_priority_score = raw riskScore off the per-asset association entry, soft-null when absent; source_signals models missing-vs-negative, no PII.
    - Automated check passes: `ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret python -m pytest backend/tests/test_connectors/test_rapid7_connector.py -x`
  </acceptance_criteria>

  <verify>
    <automated>ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret python -m pytest backend/tests/test_connectors/test_rapid7_connector.py -x</automated>
  </verify>
  <done>Rapid7 native_priority_score = raw riskScore off the per-asset association entry, soft-null when absent; source_signals models missing-vs-negative, no PII.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendor payload → source_signals | Scanner-returned fields cross into persisted JSONB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Information Disclosure | qualys.py / rapid7.py source_signals allowlists | mitigate | Curated per-connector allowlist keyed by raw vendor field; exclude hostname/ip_addresses/last_login_user/serial_number and promoted columns (D-08/V8); tests assert no PII keys. |
| T-31-10 | Tampering (correctness) | QDS/riskScore read location | mitigate | QDS read from the per-detection record not kb_cache (Pitfall 4); riskScore off vuln_entry not detail (Pitfall 5) — tests assert the column stays null when the value is only present in the WRONG source, preventing stale/wrong-context data. |
</threat_model>

<verification>
- Both connector test files green.
- Regression: `... python -m pytest backend/tests/test_connector_normalization.py -x` still green (shared file untouched here; cross-6 sweep is Plan 05).
- A2/A3 field-name/param assumptions are soft-null by construction — flag in SUMMARY for empirical re-verify against a live Qualys/InsightVM account.
</verification>

<success_criteria>
- SC#3 (completes the 4 composite-signal connectors): Qualys QDS + Rapid7 riskScore populated raw, sortable via Plan 01 columns/indexes.
- ENRICH-04/06: both connectors set all 3 new dataclass fields with correct missing-vs-negative source_signals.
</success_criteria>

<output>
After completion, create `.planning/phases/31-connector-enrichment-rewrite/31-04-SUMMARY.md`.
Record the exact QDS element name + show_qds_factors behavior and the Rapid7 riskScore field path
observed, plus each connector's source_signals allowlist keys. Flag A2/A3 as needing live re-verify.
</output>
