---
phase: 30-correlation-schema-fix
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - backend/scripts/recorrelate_all_tenants.py
  - backend/tests/test_correlation_service.py
autonomous: true
requirements: [CORR-01, CORR-02, CORR-03]
user_setup: []

must_haves:
  truths:
    - "backend/scripts/recorrelate_all_tenants.py exposes a testable per-tenant helper `async def _recorrelate_tenant(db, tenant_id) -> dict` that `_main()` calls in its active-tenant loop (accepting an injected db so a test fixture session can drive it)"
    - "A runtime integration test seeds the exact post-backfill bug signature (a correlation row with sources=[], sources_count=2) plus the underlying Qualys+Rapid7 vulns, invokes _recorrelate_tenant, and proves the row is corrected AND the per-tenant COALESCE(array_length(sources,1),0) != sources_count consistency query returns 0 (CORR-02 zero-loss, D-06 step 5)"
    - "run_correlations is re-run per active tenant (looping active tenants like scheduler.py), recovering previously-dropped Qualys/Rapid7 correlations, and is safe to re-run (idempotent)"
    - "sources_count always equals len(sources) for every correlation row across 1-of-6 through 6-of-6 source combinations (standing regression, CORR-03)"
    - "Confidence banding is HIGH for >=4 sources, MEDIUM for 2-3 sources, LOW for 1 (D-08), proven across seeded fixtures"
    - "A correlation created for tenant A is never returned or mutated when re-correlating tenant B (cross-tenant isolation, proven with a tenant_b fixture)"
    - "GET /api/v1/vulnerabilities/{vuln_id}/correlation returns JSON containing sources, sources_count, and source_vuln_ids and NO legacy *_vuln_id keys (crowdstrike_vuln_id/nessus_vuln_id/defender_vuln_id/wiz_vuln_id), under require_viewer auth (D-09 response shape, proven at HTTP level)"
    - { statement: "Verification of zero-loss runs only AFTER the re-correlation step; verifying in the post-FK-drop/pre-re-correlation window shows sources=[] with a stale sources_count and produces a false data-loss signal (RESEARCH Pitfall 5)", verification: backstop }
    - { statement: "The migration → re-correlation → verification steps run in strict order; the re-correlation script and its embedded COALESCE zero-loss query are the sequencing guard", verification: backstop }
  prohibitions:
    - "recorrelate_all_tenants.py must never read or write across tenant_id boundaries; the per-tenant loop scopes every query on tenant_id and the zero-loss verification never runs as a bare global aggregate"
    - "The re-correlation script must not call compute_risk_scores or any connector-sync/dev-seed side effect — it is a correlation-only data-recovery step (out of scope per CONTEXT domain)"
  artifacts:
    - path: "backend/scripts/recorrelate_all_tenants.py"
      provides: "One-time per-tenant re-correlation; testable `_recorrelate_tenant(db, tenant_id)` helper + `_main()` entrypoint + per-tenant zero-loss verification"
      contains: "_recorrelate_tenant"
    - path: "backend/tests/test_correlation_service.py"
      provides: "Runtime re-correlation recovery, confidence-banding, count-invariant, cross-tenant, and D-09 HTTP-shape coverage"
      contains: "sources_count"
  key_links:
    - from: "backend/scripts/recorrelate_all_tenants.py::_main"
      to: "_recorrelate_tenant(db, tenant_id)"
      via: "per-active-tenant loop (mirrors scheduler.py); helper wraps run_correlations + zero-loss check"
      pattern: "_recorrelate_tenant"
    - from: "backend/tests/test_correlation_service.py (HTTP test)"
      to: "GET /api/v1/vulnerabilities/{vuln_id}/correlation"
      via: "require_viewer authed client; asserts D-09 body keys"
      pattern: "/correlation"
---

<objective>
Deliver the one-time per-tenant data-recovery step that recovers the previously-silently-dropped Qualys/Rapid7 correlations (D-06 step 4), prove zero-loss per tenant AT RUNTIME (SC#2 / CORR-02), and expand `test_correlation_service.py` with confidence-banding, count-invariant, cross-tenant, and the D-09 HTTP response-shape coverage the Plan 01 tracer intentionally left out.

Purpose: Plan 01 proved the new shape works for one finding. This plan makes the migration safe for existing data (per-tenant zero-loss recovery, executed and asserted in a real test, not just statically inspected) and locks the CORR-03 count/name invariant and the D-09 API contract so they can never silently regress.
Output: `backend/scripts/recorrelate_all_tenants.py` (idempotent, manually-invoked, with a `_recorrelate_tenant` helper `_main()` calls; mirrors `capture_ai_goldens.py` + `scheduler.py`) and an expanded correlation test suite.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-correlation-schema-fix/30-CONTEXT.md
@.planning/phases/30-correlation-schema-fix/30-RESEARCH.md
@.planning/phases/30-correlation-schema-fix/30-PATTERNS.md
@.planning/phases/30-correlation-schema-fix/30-01-PLAN.md

<interfaces>
<!-- Contracts the executor needs. Plan 01 delivered the schema/service; do not re-derive. -->

run_correlations(db: AsyncSession, tenant_id: uuid.UUID) -> dict  — idempotent (upsert on uq_correlation + prune stale); returns {"correlated": int, "stale_removed": int}. Already the shape of sla_service.backfill_sla_due_dates.

get_correlation_for_vuln(db, tenant_id, cve_id, asset_id) -> dict | None — returns {id, cve_id, asset_id, sources, sources_count, confidence, source_vuln_ids}.

NEW helper THIS PLAN adds to recorrelate_all_tenants.py:
  async def _recorrelate_tenant(db: AsyncSession, tenant_id: uuid.UUID) -> dict
    — diagnostic blind-spot "before" count (sources = '{}'), then run_correlations(db, tenant_id),
      then the per-tenant COALESCE zero-loss check; returns
      {"tenant_id": str, "blind_spot_rows_recovered": int, "inconsistent_rows_after": int, **run_correlations_stats}.
    _main() loops active tenants and calls this helper with its own async_session_factory() session;
    the helper takes an INJECTED db so a test can pass the db_session fixture instead. Helper does NOT commit.

Active-tenant loop idiom (backend/app/connectors/scheduler.py:199-206):
  tenants = (await db.execute(select(Tenant).where(Tenant.is_active.is_(True)))).scalars().all()
  for t in tenants: await <fn>(db, t.id)
  await db.commit()

Standalone-script idiom (backend/scripts/capture_ai_goldens.py): docstring (what/why/usage/when-to-rerun) + `async def _main()` + `if __name__ == "__main__": asyncio.run(_main())`; per-item try/except-continue so one failure never aborts the rest.

Zero-loss query (RESEARCH — proven against the empty-array edge, Pitfall 3):
  SELECT count(*) FROM vulnerability_correlations
  WHERE tenant_id = :tid AND COALESCE(array_length(sources,1),0) != sources_count   -- MUST be 0 per tenant

Model to seed a corrupted row directly: `from app.vulnerabilities.models import VulnerabilityCorrelation` — construct with tenant_id/cve_id/asset_id/sources=[]/sources_count=2/confidence="MEDIUM".

HTTP test fixtures (backend/tests/conftest.py): `client` (httpx AsyncClient) + an authed-viewer dependency override; follow the existing route-test shape in test_vuln_source_filter.py (`await client.get("/api/v1/vulnerabilities?source=QUALYS")`). The correlation route is `GET /api/v1/vulnerabilities/{vuln_id}/correlation`.

Session/tenant model imports: `from app.db.session import async_session_factory`, `from app.tenants.models import Tenant`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: One-time per-tenant re-correlation script with a testable helper</name>
  <read_first>
    - backend/scripts/capture_ai_goldens.py (docstring convention + `_main()`/`__main__` entrypoint + per-item try/except-continue)
    - backend/app/connectors/scheduler.py (lines 199-207 — active-tenant loop + per-iteration error logging)
    - backend/app/vulnerabilities/sla_service.py (backfill_sla_due_dates — the idempotent per-tenant fn shape run_correlations already matches)
    - backend/app/dev_routes.py (run_correlations call-site shape — but do NOT copy its compute_risk_scores call)
    - .planning/phases/30-correlation-schema-fix/30-RESEARCH.md (Code Example #5 — the script; refactor its per-tenant body into the helper)
    - .planning/phases/30-correlation-schema-fix/30-PATTERNS.md (recorrelate_all_tenants.py section — Analogs A-D)
  </read_first>
  <action>
    Create `backend/scripts/recorrelate_all_tenants.py` from RESEARCH Code Example #5, but factor the per-tenant body into `async def _recorrelate_tenant(db: AsyncSession, tenant_id: uuid.UUID) -> dict` so it is unit-testable with an injected session. The helper: (1) a diagnostic "before" blind-spot count (`WHERE tenant_id = :tid AND sources = '{}'`), (2) `stats = await run_correlations(db, tenant_id)`, (3) the per-tenant COALESCE zero-loss check (`WHERE tenant_id = :tid AND COALESCE(array_length(sources,1),0) != sources_count`), (4) `logger.info("recorrelated_tenant", tenant_id=str(tenant_id), blind_spot_rows_recovered=..., inconsistent_rows_after=..., **stats)`, then `return {"tenant_id": str(tenant_id), "blind_spot_rows_recovered": ..., "inconsistent_rows_after": ..., **stats}`. The helper does NOT commit (leave commit to the caller so a test fixture controls its own transaction). `async def _main()`: open `async_session_factory()`, select active tenants (`Tenant.is_active.is_(True)`), loop calling `await _recorrelate_tenant(db, t.id)` inside a try/except-continue (one tenant's failure must not abort the rest — mirror scheduler.py's per-iteration error log), then `await db.commit()` once after the loop. Docstring: what it is, why it is NOT auto-run (D-07), the exact `docker compose exec backend python scripts/recorrelate_all_tenants.py` usage, and that it is safe to re-run. `if __name__ == "__main__": asyncio.run(_main())`. Do NOT call compute_risk_scores. Do NOT add an API route (standalone script only — D-07, RESEARCH Open Question 2 RESOLVED). Every query scopes on `tenant_id`; the zero-loss query is per-tenant, never a global aggregate.
  </action>
  <verify>
    <automated>cd backend && python -c "src=open('scripts/recorrelate_all_tenants.py').read(); import ast; ast.parse(src); assert 'async def _recorrelate_tenant' in src, 'missing testable helper'; assert 'run_correlations' in src; assert 'COALESCE(array_length' in src, 'missing COALESCE zero-loss query'; assert 'compute_risk_scores' not in src, 'must not call compute_risk_scores'; assert 'is_active' in src, 'must loop active tenants'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/scripts/recorrelate_all_tenants.py` parses as valid Python and defines `async def _recorrelate_tenant(db, tenant_id)` returning a dict with `inconsistent_rows_after`
    - `_main()` selects active tenants (`is_active`) and calls `_recorrelate_tenant` per tenant inside a try/except-continue; the helper does not commit
    - `grep -c "COALESCE(array_length(sources,1), 0)" backend/scripts/recorrelate_all_tenants.py` >= 1
    - `grep -c "compute_risk_scores" backend/scripts/recorrelate_all_tenants.py` == 0
    - The zero-loss query is filtered by `tenant_id`, never a bare global aggregate
    - Docstring states it is NOT auto-run and gives the `docker compose exec` usage line
  </acceptance_criteria>
  <done>An idempotent, manually-invoked re-correlation script exists whose per-tenant recovery logic is isolated in `_recorrelate_tenant(db, tenant_id)` (runtime-proven in Task 2), with no risk-score or route side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Runtime re-correlation recovery + per-tenant zero-loss integration test</name>
  <read_first>
    - backend/scripts/recorrelate_all_tenants.py (the `_recorrelate_tenant` helper from Task 1)
    - backend/tests/test_correlation_service.py (the file Plan 01 created — extend it; reuse `_seed_asset`/`_seed_vuln`)
    - backend/app/vulnerabilities/models.py (VulnerabilityCorrelation — to seed a corrupted row directly)
    - backend/tests/conftest.py (db_session / tenant_a fixtures)
    - .planning/phases/30-correlation-schema-fix/30-RESEARCH.md (Pitfall 3 empty-array edge; Pitfall 5 mid-window signature)
  </read_first>
  <behavior>
    - Seed one asset + a Qualys vuln + a Rapid7 vuln sharing cve_id + asset_id
    - Directly insert the post-backfill bug signature: a VulnerabilityCorrelation(tenant_a, cve, asset) with sources=[], sources_count=2, confidence="MEDIUM" (row exists but is inconsistent); commit
    - Before _recorrelate_tenant: the tenant_a COALESCE consistency query returns >=1 (the corrupted row is detected)
    - After `await _recorrelate_tenant(db_session, tenant_a)` + commit: get_correlation_for_vuln returns sources == ["QUALYS","RAPID7"], sources_count == 2 (row corrected, not deleted)
    - The returned stats dict has inconsistent_rows_after == 0, and the tenant_a COALESCE consistency query now returns 0
  </behavior>
  <action>
    Extend `backend/tests/test_correlation_service.py` with `test_recorrelate_tenant_corrects_backfill_bug_signature(db_session, tenant_a)` (@pytest.mark.asyncio). Import `_recorrelate_tenant` from `scripts.recorrelate_all_tenants` (add `backend` to sys.path if needed — mirror how other tests import from `app.*`; the helper takes an injected `db`, so pass `db_session`). Reuse `_seed_asset`/`_seed_vuln` for the Qualys+Rapid7 pair, then `db_session.add(VulnerabilityCorrelation(tenant_id=tenant_a, cve_id=cve, asset_id=asset_id, sources=[], sources_count=2, confidence="MEDIUM"))` and commit to reproduce the exact sources=[]/sources_count=2 bug signature. Assert the pre-state consistency query (COALESCE) is non-zero, call `await _recorrelate_tenant(db_session, tenant_a)`, commit, then assert (a) `get_correlation_for_vuln` returns the corrected sources/count, (b) the returned stats `inconsistent_rows_after == 0`, (c) a direct `text("SELECT count(*) ... WHERE tenant_id=:tid AND COALESCE(array_length(sources,1),0) != sources_count")` returns 0 for tenant_a. Run per-file (repo memory).
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret ENVIRONMENT=test pytest tests/test_correlation_service.py::test_recorrelate_tenant_corrects_backfill_bug_signature -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_recorrelate_tenant_corrects_backfill_bug_signature` PASSES
    - It seeds a real `VulnerabilityCorrelation` row with `sources=[]`, `sources_count=2` and asserts the pre-call COALESCE consistency query is non-zero (the bug is actually present before recovery)
    - After `_recorrelate_tenant(db_session, tenant_a)`: the row's `sources == ["QUALYS","RAPID7"]` and `sources_count == 2` (corrected, not pruned away)
    - The returned stats dict has `inconsistent_rows_after == 0` AND the raw COALESCE query returns 0 for tenant_a
  </acceptance_criteria>
  <done>The script's per-tenant zero-loss recovery is proven at runtime against the exact post-backfill bug signature, closing the static-only verification gap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Confidence-banding, count-invariant, cross-tenant, and D-09 HTTP-shape coverage</name>
  <read_first>
    - backend/tests/test_correlation_service.py (extend it; reuse `_seed_asset`/`_seed_vuln`)
    - backend/tests/test_vuln_source_filter.py (test_source_filter_tenant_scoped tenant-isolation shape 65-77; the `client` route-test shape `await client.get("/api/v1/vulnerabilities?...")`)
    - backend/tests/conftest.py (tenant_a, tenant_b, client fixtures + authed-viewer override)
    - backend/app/vulnerabilities/router.py (get_vuln_correlation 674-694 — require_viewer, returns {"correlated": True, **corr})
    - .planning/phases/30-correlation-schema-fix/30-CONTEXT.md (D-08 bands; D-02 canonical order; D-09 response shape)
  </read_first>
  <behavior>
    - 1-source finding: no correlation row is created (correlation requires >=2 sources) — the empty/single-input edge
    - 2 sources -> "MEDIUM", 3 -> "MEDIUM", 4 -> "HIGH", 5 -> "HIGH", 6 -> "HIGH" (D-08 bands)
    - For every seeded combination, `len(corr["sources"]) == corr["sources_count"]` (CORR-03) and sources is in canonical enum-declaration order
    - A correlation seeded for tenant_a is not returned by get_correlation_for_vuln for tenant_b (cross-tenant isolation)
    - GET /api/v1/vulnerabilities/{vuln_id}/correlation for a Qualys+Rapid7-correlated vuln returns 200 with body containing sources, sources_count, source_vuln_ids and NONE of crowdstrike_vuln_id/nessus_vuln_id/defender_vuln_id/wiz_vuln_id (D-09)
  </behavior>
  <action>
    Extend `backend/tests/test_correlation_service.py` (reuse the Plan 01 helpers): (a) `test_single_source_does_not_correlate` — one source, assert `get_correlation_for_vuln` returns None. (b) parametrized `test_confidence_bands` over source-count 2..6 seeding N distinct sources drawn from VulnSource declaration order on one cve+asset, asserting the D-08 band, `len(sources) == sources_count`, and canonical order for each. (c) `test_correlation_tenant_scoped` — seed+correlate on tenant_a, assert `get_correlation_for_vuln(db, tenant_b, cve, asset)` is None (mirror test_vuln_source_filter.py). (d) `test_correlation_route_returns_d09_shape(client, db_session, tenant_a)` — seed a Qualys+Rapid7 pair on one asset, run_correlations + commit, then GET the correlation route for one of the seeded vuln ids via the authed `client` (require_viewer, same override pattern as test_vuln_source_filter.py's route tests); assert status 200, `body["correlated"] is True`, `set(["sources","sources_count","source_vuln_ids"]).issubset(body)`, and `not any(k in body for k in ["crowdstrike_vuln_id","nessus_vuln_id","defender_vuln_id","wiz_vuln_id"])`. All `@pytest.mark.asyncio`. Run per-file (repo memory).
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret ENVIRONMENT=test pytest tests/test_correlation_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_correlation_service.py` exits 0 with the SC#4 test, `test_recorrelate_tenant_corrects_backfill_bug_signature`, `test_single_source_does_not_correlate`, `test_confidence_bands` (2..6), `test_correlation_tenant_scoped`, and `test_correlation_route_returns_d09_shape` all passing
    - `test_confidence_bands` asserts `len(corr["sources"]) == corr["sources_count"]` and canonical enum order for every count 2-6; 4/5/6 -> "HIGH", 2/3 -> "MEDIUM"
    - `test_correlation_tenant_scoped` proves a tenant_a correlation is None for tenant_b
    - `test_correlation_route_returns_d09_shape` asserts the HTTP body has `sources`/`sources_count`/`source_vuln_ids` and NONE of the 4 legacy `*_vuln_id` keys, under require_viewer auth
  </acceptance_criteria>
  <done>The count/name invariant, D-08 bands, cross-tenant isolation, and the D-09 API contract are locked as standing regressions across every source combination and at the HTTP boundary.</done>
</task>

</tasks>

<assumption_delta_decision>
Inherits Plan 01's decision: **promote** the `sources` array to the canonical source-set. This plan supplies the invariant test Plan 01 flagged — `sources_count == len(sources)` across 1-of-6 through 6-of-6 — the runtime per-tenant zero-loss proof that the promotion loses no existing data, and the HTTP-level proof that the promoted shape is what the API actually returns (D-09).
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| re-correlation script → Postgres | A one-time operator-invoked script mutates tenant-scoped `vulnerability_correlations` rows across all active tenants; a missing tenant filter would bleed or corrupt tenant data |
| client → API (`GET /{vuln_id}/correlation`) | The D-09 response-shape change must keep the route require_viewer-gated and tenant-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05 | Information Disclosure | `_recorrelate_tenant` + zero-loss query | mitigate | `run_correlations(db, t.id)` is tenant-scoped on every internal query; the helper's before/after counts filter `WHERE tenant_id = :tid` and never run as a global aggregate (high severity — blocking) |
| T-30-06 | Tampering / DoS | Re-running the script | accept | `run_correlations` is idempotent (upsert on uq_correlation + prune); a re-run converges, and per-tenant try/except-continue prevents one tenant aborting the batch (D-07) — low residual risk |
| T-30-07 | Information Disclosure / Elevation | D-09 correlation route (HTTP test coverage) | mitigate | `test_correlation_route_returns_d09_shape` exercises the route through the require_viewer authed client, confirming the shape change ships no un-scoped/legacy field (high severity — blocking) |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('backend/scripts/recorrelate_all_tenants.py').read())"` — script is valid Python and exposes `_recorrelate_tenant`.
- `grep -c compute_risk_scores backend/scripts/recorrelate_all_tenants.py` == 0.
- `cd backend && pytest tests/test_correlation_service.py -v` — full correlation suite green (SC#4 + runtime recovery + bands + invariant + tenant scope + D-09 HTTP shape).
</verification>

<success_criteria>
- CORR-02: existing correlation data migrates with zero loss, verified per-tenant at runtime (the `_recorrelate_tenant` recovery test corrects the sources=[]/sources_count=2 bug signature and the COALESCE query returns 0).
- CORR-03: `sources_count == len(sources)` proven across every 1..6 source combination; bands match D-08.
- CORR-01 (D-09): the correlation route returns the promoted `sources`/`sources_count`/`source_vuln_ids` shape with no legacy `*_vuln_id` keys, under require_viewer.
- Cross-tenant isolation of correlation reads and the re-correlation batch is proven.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this plan produces (newly created symbols — exclude from cross-file drift verification)

- **New standalone script:** `backend/scripts/recorrelate_all_tenants.py` defining `async def _recorrelate_tenant(db, tenant_id)` (testable helper) + `async def _main()`; reuses `run_correlations`
- **New tests (appended to the Plan 01 file):** `test_recorrelate_tenant_corrects_backfill_bug_signature`, `test_single_source_does_not_correlate`, `test_confidence_bands`, `test_correlation_tenant_scoped`, `test_correlation_route_returns_d09_shape` in `backend/tests/test_correlation_service.py`
- Consumes (does not create) Plan 01's `sources`/`source_vuln_ids` columns, `ix_vulnerability_correlations_sources` index, `_SOURCE_ORDER`, rewritten `run_correlations`/`get_correlation_for_vuln`, and the `GET /{vuln_id}/correlation` route.
</artifacts_this_phase_produces>

<output>
After completion, create `.planning/phases/30-correlation-schema-fix/30-02-SUMMARY.md`.
</output>
