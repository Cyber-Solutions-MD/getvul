---
phase: 18-tickets-kanban-board
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/e2e/tickets-kanban.spec.ts
  - frontend/src/components/tickets/kanban-reason-prompt.test.tsx
  - frontend/e2e/a11y-routes.spec.ts
  - frontend/e2e/reduced-motion.spec.ts
autonomous: true
requirements: [UX-D-01-01, UX-D-01-02, UX-D-01-03, UX-D-01-04, UX-D-01-05, UX-D-01-06]
must_haves:
  truths:
    - "A NEW e2e spec asserts four columns render, pointer drag into Blocked persists (optimistic + rollback), keyboard drag, empty-column EmptyState, status-chip narrowing"
    - "The fixed mobile bottom-nav stays visible AND focusable at 360px while ?view=board is rendered (board-layout non-regression of the Phase 15 bottom-nav)"
    - "A RED unit spec pins the reason-prompt contract (Save-with-reason, Cancel = no mutation, whitespace→null)"
    - "The axe both-themes sweep and the reduced-motion sweep are extended to cover ?view=board"
  artifacts:
    - path: "frontend/e2e/tickets-kanban.spec.ts"
      provides: "Board behavior e2e (RED until Wave 2)"
      contains: "renders four columns"
    - path: "frontend/src/components/tickets/kanban-reason-prompt.test.tsx"
      provides: "Reason-prompt contract test (RED until Wave 1)"
    - path: "frontend/e2e/a11y-routes.spec.ts"
      provides: "Board-view axe sweep in both themes"
    - path: "frontend/e2e/reduced-motion.spec.ts"
      provides: "Board drop-tween suppression assertion"
  key_links:
    - from: "frontend/e2e/tickets-kanban.spec.ts"
      to: "/dashboard/tickets?view=board"
      via: "page.goto + column-header queries"
      pattern: "view=board"
---

<objective>
Land the Nyquist test scaffolding BEFORE the board exists, so Waves 1–2 turn RED targets GREEN. Create a NEW e2e board spec, a RED reason-prompt unit spec, and extend the existing axe (both-themes) and reduced-motion specs to cover the board view.

Purpose: Per project memory, executors have historically claimed WCAG AA without running the axe sweep, and have shipped features without an end-to-end guard. This plan writes the tests first so "done" is falsifiable. Every test here is EXPECTED to be RED at the end of this plan (the board and reason-prompt do not exist yet) — that is the correct Wave 0 state.
Output: `e2e/tickets-kanban.spec.ts` (new, RED); `kanban-reason-prompt.test.tsx` (new, RED); extended `a11y-routes.spec.ts` + `reduced-motion.spec.ts`.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-tickets-kanban-board/18-CONTEXT.md
@.planning/phases/18-tickets-kanban-board/18-RESEARCH.md
@.planning/phases/18-tickets-kanban-board/18-VALIDATION.md

<interfaces>
<!-- Contracts the tests assert against. The board (Wave 2) must satisfy these. -->

Board DOM contract the e2e will target (Wave 2 must honor these):
- Route: `/dashboard/tickets?view=board`
- Each column exposes an accessible region with an accessible name matching its label: "Open", "In progress", "Completed", "Blocked" (use `getByRole('region', { name })` or a stable `[data-column="open|in_progress|completed|blocked"]` attribute — the e2e uses column header text + data-column).
- Each column header shows a live count badge (assert count text).
- Each card is a draggable element carrying `data-ticket-id={ticket.id}` and shows the mono `external_ticket_id`.
- Empty column renders the canonical EmptyState (role="status"), reachable via the column container.

Existing e2e helpers (frontend/e2e/routes.ts):
```typescript
export const STATIC_ROUTES: readonly string[]   // includes '/dashboard/tickets'
export async function waitForNav(page, viewportWidth): Promise<void>
export async function discoverDetailRoutes(page): Promise<string[]>
export const MOBILE_NAV = 'nav[aria-label="Mobile navigation"]';
```
Axe fixture (frontend/e2e/a11y-routes.spec.ts): `import { test, expect } from './fixtures/axe'` → `makeAxeBuilder()` / `makeAxeBuilderReportOnly()`.
Light-theme sweep pattern: `page.addInitScript(() => localStorage.setItem('getvul_theme','light'))` then defensively force `document.documentElement.setAttribute('data-theme','light')`.

Reason-prompt component contract (Wave 1 kanban-reason-prompt.tsx must export this):
```typescript
export type KanbanReasonPromptProps = {
  ticketLabel: string;                       // e.g. external_ticket_id for the announcement/aria-label
  onSave: (reason: string | null) => void;   // whitespace-only → null already coerced
  onCancel: () => void;
};
export function KanbanReasonPrompt(props: KanbanReasonPromptProps): JSX.Element;
```
Mirror blocked-toggle.tsx: maxLength 500, microcopy.blockedPrompt placeholder, Save/Cancel buttons (microcopy.blockedSave / blockedCancel), Enter=save, Esc=cancel, `reason.trim() || null`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NEW e2e board spec (RED)</name>
  <files>frontend/e2e/tickets-kanban.spec.ts</files>
  <read_first>
    - frontend/e2e/a11y-routes.spec.ts (fixture import, route sweep pattern, existing 360px Bottom-nav assertion to mirror)
    - frontend/e2e/routes.ts (waitForNav, STATIC_ROUTES, MOBILE_NAV)
    - frontend/e2e/page-transitions.spec.ts (client-nav + goto patterns already in this suite)
    - .planning/phases/18-tickets-kanban-board/18-RESEARCH.md §Validation Architecture (Req→Test map, grep titles) + §Pitfall 4 (board must not regress the fixed bottom-nav)
    - 18-VALIDATION.md §Per-Task Verification Map (exact `-g` grep titles)
  </read_first>
  <action>
    Create `e2e/tickets-kanban.spec.ts` using `import { test, expect } from '@playwright/test'` (storageState is set at the project level) and `import { MOBILE_NAV, waitForNav } from './routes'`. Author these tests with titles matching the VALIDATION `-g` filters EXACTLY:
    1. `test('renders four columns', …)` — goto `/dashboard/tickets?view=board`; wait for the desktop nav; assert the four column labels are visible ("Open", "In progress", "Completed", "Blocked") and that the List/Board toggle still shows Board pressed (`button[aria-pressed="true"]` with text "Board"). Assert switching to `?view=list` still renders the table (toggle preserved — UX-D-01-01).
    2. `test('drag into Blocked persists', …)` — locate the first card in a non-Blocked column (`[data-ticket-id]`), perform a pointer drag (`page.mouse` down→move ≥8px→over Blocked column→up), then in the reason popover click Save; assert the card now appears under the Blocked column. For rollback: use `await page.route('**/api/v1/tickets/*/blocked', route => route.fulfill({ status: 500, body: '{}' }))` BEFORE the drag, and assert the card snaps back to its origin column after the failed mutation (UX-D-01-02).
    3. `test('keyboard drag', …)` — focus the first card, press Space (grab), ArrowRight until over Blocked, Space (drop), Save in the prompt; assert the card lands in Blocked (UX-D-01-03).
    4. `test('empty column', …)` — apply `?view=board&status=open` (status chip narrows to Open); assert the In progress / Completed / Blocked columns each render an EmptyState (`role="status"`) while Open holds cards (UX-D-01-04).
    5. `test('board mobile bottom-nav', …)` — set the viewport to 360px BEFORE navigation (`await page.setViewportSize({ width: 360, height: 780 })`); goto `/dashboard/tickets?view=board`; wait for the board to render (`await waitForNav(page, 360)`); assert the fixed mobile bottom-nav is BOTH visible and focusable while the board is rendered: `await expect(page.locator(MOBILE_NAV)).toBeVisible()`, then focus its first link/button and assert focus lands inside the nav (`expect(await page.evaluate((sel) => document.activeElement?.closest(sel) !== null, MOBILE_NAV)).toBe(true)`) and that the nav is not `aria-hidden`. This proves the board's `<768px` horizontal-scroll layout does not overlap or regress the Phase 15 fixed bottom-nav (UX-D-01-05, RESEARCH Pitfall 4). Unlike the existing `a11y-routes.spec.ts` Bottom-nav sweep — which runs over STATIC_ROUTES and only exercises the LIST view of `/dashboard/tickets` — this case asserts the guarantee specifically for `?view=board`.
    Guard against an empty dataset: at the top of each data-dependent test (1–4), if the board has zero cards, `test.skip(true, 'no seeded tickets')` with a visible skip reason (mirror reduced-motion.spec.ts IN-02 pattern) rather than a silent pass. Test 5 (bottom-nav) does NOT skip on empty data — the bottom-nav must render regardless of card count.
    These will FAIL now (board route still renders the placeholder). That is the expected RED state — do not implement the board here.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "tickets-kanban-board\|kanban-card\|kanban-column\|kanban-reason-prompt" | grep "tickets-kanban.spec" && echo "typecheck-references-expected-missing-board" || echo "spec compiles standalone"</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/tickets-kanban.spec.ts` exists and contains the five exact test titles: `renders four columns`, `drag into Blocked persists`, `keyboard drag`, `empty column`, `board mobile bottom-nav`
    - `grep -c "test(" frontend/e2e/tickets-kanban.spec.ts` returns ≥5
    - The rollback test installs a `page.route` 500 interceptor on `**/api/v1/tickets/*/blocked`
    - The spec references `view=board`
    - The `board mobile bottom-nav` test sets a 360px viewport and references `MOBILE_NAV`; once the board renders (Wave 2 target), `cd frontend && npx playwright test e2e/tickets-kanban.spec.ts -g "board mobile bottom-nav"` exits 0 (bottom-nav visible + focusable at 360px on ?view=board). It is RED now (placeholder) — expected.
  </acceptance_criteria>
  <done>New board e2e spec exists with all five requirement-mapped tests (incl. the 360px board-view bottom-nav guard); RED (board not built).</done>
</task>

<task type="auto">
  <name>Task 2: RED reason-prompt unit spec + extend axe both-themes to board view</name>
  <files>frontend/src/components/tickets/kanban-reason-prompt.test.tsx, frontend/e2e/a11y-routes.spec.ts</files>
  <read_first>
    - frontend/src/components/tickets/blocked-toggle.tsx (Save/Cancel + trim()||null + maxLength 500 + Enter/Esc behavior to mirror)
    - frontend/src/components/tickets/microcopy.ts (blockedPrompt/blockedSave/blockedCancel strings)
    - frontend/e2e/a11y-routes.spec.ts (both-themes sweep structure — the file being extended)
    - .planning/phases/18-tickets-kanban-board/18-VALIDATION.md Wave 0 Requirements
  </read_first>
  <action>
    (a) Create `kanban-reason-prompt.test.tsx` (vitest + @testing-library/react) importing `{ KanbanReasonPrompt }` from `./kanban-reason-prompt` (does not exist yet → RED). Tests: (1) typing a reason and clicking Save calls `onSave('the reason')`; (2) clicking Cancel calls `onCancel` and does NOT call `onSave`; (3) typing only whitespace ('   ') then Save calls `onSave(null)`; (4) the input has `maxLength={500}`. Use the `KanbanReasonPromptProps` contract from <interfaces>.
    (b) Extend `e2e/a11y-routes.spec.ts`: add a new `test.describe` (or extend the existing sweep) that visits `/dashboard/tickets?view=board` under BOTH the default (dark) and the `getVul_theme='light'` init-script paths, runs `makeAxeBuilder().analyze()`, and asserts zero critical/serious violations. Reuse the light-theme `addInitScript` + defensive `data-theme` force pattern already in the file. Add a comment noting the mid-drag overlay axe check is covered in tickets-kanban.spec.ts (or add an in-drag axe assertion if straightforward). Skip cleanly if the board has no cards.
    Both the unit spec (missing component) and the board axe sweep (placeholder still shown) are RED now — expected.
  </action>
  <verify>
    <automated>cd frontend && grep -q "KanbanReasonPrompt" src/components/tickets/kanban-reason-prompt.test.tsx && grep -q "view=board" e2e/a11y-routes.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `kanban-reason-prompt.test.tsx` imports `KanbanReasonPrompt` from `./kanban-reason-prompt`
    - `grep -c "it(\|test(" frontend/src/components/tickets/kanban-reason-prompt.test.tsx` returns ≥4
    - A test asserts whitespace-only input → `onSave(null)`
    - `e2e/a11y-routes.spec.ts` contains `view=board` and runs `makeAxeBuilder` against it in both themes
  </acceptance_criteria>
  <done>Reason-prompt contract pinned (RED); axe both-themes sweep extended to the board view.</done>
</task>

<task type="auto">
  <name>Task 3: Extend reduced-motion spec for board drop-tween suppression</name>
  <files>frontend/e2e/reduced-motion.spec.ts</files>
  <read_first>
    - frontend/e2e/reduced-motion.spec.ts (the file being extended — emulateMedia + getAnimations poll pattern)
    - .planning/phases/18-tickets-kanban-board/18-RESEARCH.md §Pitfall 2 (WAAPI drop animation is NOT caught by the CSS blanket → must be gated in code)
  </read_first>
  <action>
    Add a test to `reduced-motion.spec.ts`: `test('board drag drop animation is suppressed under prefers-reduced-motion', …)`. Emulate `reducedMotion: 'reduce'` before navigation; goto `/dashboard/tickets?view=board`; wait for the desktop nav. Perform a pointer drag of the first `[data-ticket-id]` card and drop it (a no-op read-only→read-only drop is fine — it still triggers the DragOverlay drop tween). Poll `page.evaluate(() => document.getAnimations().map(a => a.effect?.getComputedTiming?.().duration))` for ~500ms; assert that no in-flight animation attributable to the drag overlay runs longer than 20ms (the board must pass `dropAnimation={reduced ? null : undefined}`). If the board has no cards, `test.skip(true, 'no seeded tickets')` with a visible reason. This is RED now (board absent) — expected.
  </action>
  <verify>
    <automated>cd frontend && grep -q "board drag drop animation is suppressed" e2e/reduced-motion.spec.ts && grep -q "reducedMotion" e2e/reduced-motion.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `reduced-motion.spec.ts` contains a test titled `board drag drop animation is suppressed under prefers-reduced-motion`
    - The test emulates `reducedMotion: 'reduce'` and polls `getAnimations()`
    - The test navigates to `?view=board`
  </acceptance_criteria>
  <done>Reduced-motion suppression of the DragOverlay drop tween is guarded (RED until Wave 2 gates dropAnimation).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → running app | e2e drives the browser against the local prod build; no new production surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Information Disclosure | e2e spec fixtures | accept | Tests reuse the existing shared `storageState` admin session; no new secrets or credentials introduced. |
| T-18-04 | Tampering | mocked 500 route in rollback test | accept | `page.route` interception is scoped to `**/api/v1/tickets/*/blocked` inside a single test; no persistence, no production impact. |
</threat_model>

<verification>
- All four spec files exist / are extended with the exact test titles the VALIDATION map greps.
- Specs compile (TypeScript may reference not-yet-created board components — that is the expected RED reference).
- `npx vitest run src/components/tickets/kanban-reason-prompt.test.tsx` is RED (missing component) — expected; do NOT force it green here.
</verification>

<success_criteria>
- New `tickets-kanban.spec.ts` with `renders four columns`, `drag into Blocked persists`, `keyboard drag`, `empty column`, `board mobile bottom-nav`.
- New `kanban-reason-prompt.test.tsx` (≥4 cases incl. whitespace→null).
- `a11y-routes.spec.ts` sweeps `?view=board` in both themes; `reduced-motion.spec.ts` asserts drop-tween suppression.
- All new/extended assertions are RED (targets for Waves 1–2), not silently passing.
</success_criteria>

<output>
After completion, create `.planning/phases/18-tickets-kanban-board/18-01-SUMMARY.md`.
</output>
</content>
</invoke>
