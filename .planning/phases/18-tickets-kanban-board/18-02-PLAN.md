---
phase: 18-tickets-kanban-board
plan: 02
type: execute
wave: 1
depends_on: ["18-00"]
files_modified:
  - frontend/src/components/tickets/severity-glyph.ts
  - frontend/src/components/tickets/tickets-table.tsx
  - frontend/src/components/tickets/kanban-card.tsx
  - frontend/src/components/tickets/kanban-column.tsx
  - frontend/src/components/tickets/kanban-reason-prompt.tsx
autonomous: true
requirements: [UX-D-01-01, UX-D-01-02]
must_haves:
  truths:
    - "A compact draggable card renders provider mark + mono ID + truncated title + severity glyph + SLA pill + assignee avatar, with a red Blocked accent and NO status pill"
    - "A droppable column renders a header with status accent + live count badge, its cards, and the canonical EmptyState when empty, and dims when it is not a valid drop target during a drag"
    - "The reason prompt mirrors blocked-toggle (Save/Cancel, whitespace→null, maxLength 500, Enter/Esc)"
  artifacts:
    - path: "frontend/src/components/tickets/severity-glyph.ts"
      provides: "Shared SEVERITY_GLYPH + SEVERITY_CLASS maps"
      contains: "export const SEVERITY_GLYPH"
    - path: "frontend/src/components/tickets/kanban-card.tsx"
      provides: "useDraggable compact card + distance-gated click→onOpen"
      contains: "useDraggable"
    - path: "frontend/src/components/tickets/kanban-column.tsx"
      provides: "useDroppable column w/ header, count badge, EmptyState, dim cue"
      contains: "useDroppable"
    - path: "frontend/src/components/tickets/kanban-reason-prompt.tsx"
      provides: "Inline optional-reason popover"
      contains: "KanbanReasonPrompt"
  key_links:
    - from: "frontend/src/components/tickets/kanban-card.tsx"
      to: "onOpen(ticket)"
      via: "onClick handler (distance-gated by PointerSensor)"
      pattern: "onClick"
    - from: "frontend/src/components/tickets/tickets-table.tsx"
      to: "frontend/src/components/tickets/severity-glyph.ts"
      via: "import { SEVERITY_GLYPH, SEVERITY_CLASS }"
      pattern: "severity-glyph"
---

<objective>
Build the three leaf kanban components — the draggable compact card, the droppable column, and the inline reason prompt — plus extract the shared severity-glyph maps so the card and the list table share one source. These are pure presentational/interaction leaves; the `DndContext` container and page wiring come in Wave 2 (18-03).

Purpose: Interface-first ordering. The board container (18-03) consumes these components; defining them first means the container is pure assembly. This plan turns the RED `kanban-reason-prompt.test.tsx` (from 18-01) GREEN.
Output: `severity-glyph.ts`, `kanban-card.tsx`, `kanban-column.tsx`, `kanban-reason-prompt.tsx`; `tickets-table.tsx` rewired to the shared severity module.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-tickets-kanban-board/18-CONTEXT.md
@.planning/phases/18-tickets-kanban-board/18-RESEARCH.md

# Design contract — MUST read (frontend phase; CLAUDE.md routing):
@.claude/skills/sketch-findings-getvul/references/visual-language.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md
@.claude/skills/sketch-findings-getvul/references/foundation.md

<interfaces>
<!-- Real signatures. Reuse these primitives verbatim — do not re-derive. -->

From frontend/src/components/tickets/bucket-tickets.ts (created in 18-00):
```typescript
export type ColumnKey = 'open' | 'in_progress' | 'completed' | 'blocked';
export const COLUMN_ORDER: ColumnKey[];
export const COLUMN_LABELS: Record<ColumnKey, string>;
```

From frontend/src/components/tickets/provider-mark.tsx:
```typescript
export function ProviderMark({ provider, className }: { provider: 'jira'|'asana'|'github'; className?: string }): JSX.Element;
// Narrow with: function isTicketProvider(v: string|null): v is TicketProvider { return v==='jira'||v==='asana'||v==='github'; }
```
From frontend/src/components/tickets/sla-pill.tsx: `export function SlaPill({ dueAt, className }: { dueAt: string|null; className?: string }): JSX.Element;`
From frontend/src/components/ui/Avatar: `<Avatar name={string|undefined} email={string|undefined} size={number} />`
From frontend/src/lib/utils: `export function cn(...): string`
From frontend/src/components/tickets/microcopy.ts: `microcopy.blockedPrompt`, `microcopy.blockedSave`, `microcopy.blockedCancel`
From frontend/src/lib/queries/use-tickets.ts: `TicketSummary` (see 18-00 interfaces — snake_case fields: external_ticket_id, max_severity, sla_due_at, blocked, blocked_reason, assignee).

Severity maps to extract from tickets-table.tsx (verbatim):
```typescript
const SEVERITY_GLYPH: Record<string,string> = { critical:'■', high:'▲', medium:'◆', low:'○', info:'□' };
const SEVERITY_CLASS: Record<string,string> = { critical:'text-severity-critical', high:'text-severity-high', medium:'text-severity-medium', low:'text-severity-low', info:'text-severity-info' };
```

@dnd-kit/core 6.x (stable API — NOT the v2 DragDropProvider):
```typescript
import { useDraggable, useDroppable } from '@dnd-kit/core';
const { attributes, listeners, setNodeRef, isDragging } = useDraggable({ id });
const { setNodeRef, isOver } = useDroppable({ id });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared severity-glyph module + build the draggable KanbanCard</name>
  <files>frontend/src/components/tickets/severity-glyph.ts, frontend/src/components/tickets/tickets-table.tsx, frontend/src/components/tickets/kanban-card.tsx</files>
  <read_first>
    - frontend/src/components/tickets/tickets-table.tsx (SEVERITY_GLYPH/SEVERITY_CLASS local consts + isTicketProvider — the file being rewired)
    - frontend/src/components/tickets/provider-mark.tsx (ProviderMark signature + role="img")
    - frontend/src/components/tickets/sla-pill.tsx (SlaPill signature)
    - .planning/phases/18-tickets-kanban-board/18-RESEARCH.md §Code Examples (KanbanCard reference)
    - 18-CONTEXT.md D-CARD-01 (compact card fields, red Blocked accent, NO status pill), D-CARD-02 (click opens DrillPanel)
    - .claude/skills/sketch-findings-getvul/references/visual-language.md (severity glyph + card conventions), sketch 006 variant C
  </read_first>
  <action>
    (a) Create `severity-glyph.ts` exporting `SEVERITY_GLYPH` and `SEVERITY_CLASS` exactly as they appear today in tickets-table.tsx (values in <interfaces>). Pure module, no React.
    (b) Edit `tickets-table.tsx`: delete its local `SEVERITY_GLYPH`/`SEVERITY_CLASS` consts and `import { SEVERITY_GLYPH, SEVERITY_CLASS } from './severity-glyph';`. Change nothing else — the rendered output must be byte-identical.
    (c) Create `kanban-card.tsx` (`'use client'`) exporting `KanbanCard`. Props: `{ ticket: TicketSummary; onOpen: (t: TicketSummary) => void; overlay?: boolean }`. Use `useDraggable({ id: ticket.id })` UNLESS `overlay` is true (the DragOverlay clone is non-interactive — when `overlay`, skip the hook and render a static clone). Layout per D-CARD-01:
    - Top line (`flex items-center gap-2`): `<ProviderMark>` when `isTicketProvider(ticket.provider)`, then `<span className="font-mono text-xs text-text shrink-0">{ticket.external_ticket_id}</span>`, then `<span className="truncate text-sm text-text" title={ticket.title}>{ticket.title}</span>`.
    - Bottom line (`mt-2 flex items-center justify-between`): severity glyph `<span aria-label={ticket.max_severity ?? 'unknown'} className={cn('text-sm', SEVERITY_CLASS[sev] ?? 'text-text-faint')}>{SEVERITY_GLYPH[sev] ?? '○'}</span>` where `sev = ticket.max_severity?.toLowerCase() ?? ''`; then `<SlaPill dueAt={ticket.sla_due_at} />`; then `{ticket.assignee && <Avatar name={ticket.assignee} email={ticket.assignee} size={20} />}`.
    - Container: `ref={setNodeRef}`, spread `{...attributes} {...listeners}`, `onClick={() => onOpen(ticket)}` (distance-gated by the PointerSensor in 18-03 — a click <8px fires onClick, a drag ≥8px does not), classes `cn('cursor-grab rounded-lg border border-border-subtle bg-surface p-3 focus-visible:outline focus-visible:outline-2 focus-visible:outline-violet', ticket.blocked && 'border-l-2 border-l-severity-critical', isDragging && 'opacity-40')`.
    - D-CARD-01: NO StatusPill on the card. The only status signal is the red `border-l-severity-critical` accent when `ticket.blocked`.
    - All colors via sunset tokens (no raw hex). Add a keyboard-focusable affordance: the draggable attributes from dnd-kit include `role`/`tabIndex`/`aria-roledescription` — rely on them; do not override role.
  </action>
  <verify>
    <automated>cd frontend && grep -q "useDraggable" src/components/tickets/kanban-card.tsx && grep -q "border-l-severity-critical" src/components/tickets/kanban-card.tsx && ! grep -q "StatusPill" src/components/tickets/kanban-card.tsx && grep -q "from './severity-glyph'" src/components/tickets/tickets-table.tsx && npx vitest run src/components/tickets/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `severity-glyph.ts` contains `export const SEVERITY_GLYPH` and `export const SEVERITY_CLASS`
    - `tickets-table.tsx` imports from `./severity-glyph` and no longer declares a local `SEVERITY_GLYPH` const (`grep -c "const SEVERITY_GLYPH" tickets-table.tsx` → 0)
    - `kanban-card.tsx` contains `useDraggable`, contains `border-l-severity-critical`, does NOT contain `StatusPill`
    - `kanban-card.tsx` contains no raw hex (`grep -E "#[0-9a-fA-F]{3,6}" kanban-card.tsx` → no matches)
    - Existing tickets unit tests still pass (`npx vitest run src/components/tickets/` exits 0 for pre-existing specs)
  </acceptance_criteria>
  <done>Shared severity module extracted, list table rewired byte-identically, compact draggable card built per D-CARD-01/02.</done>
</task>

<task type="auto">
  <name>Task 2: Build the droppable KanbanColumn</name>
  <files>frontend/src/components/tickets/kanban-column.tsx</files>
  <read_first>
    - frontend/src/components/tickets/status-pill.tsx (D-P-04 status accent classes per column: Open=violet, In progress=amber, Completed=success, Blocked=severity-critical)
    - frontend/src/components/states/empty-state.tsx (EmptyState compound API: EmptyState / .Title / .Body, role="status")
    - .planning/phases/18-tickets-kanban-board/18-RESEARCH.md §Pattern 5 (mobile scroll-snap widths) + §Pattern 3 (dim cue)
    - 18-CONTEXT.md D-COL-02 (always render 4 columns; empty→EmptyState), D-COL-04 (header order + status accent + live count), D-DRAG-03 (dim read-only lanes during drag)
    - .claude/skills/sketch-findings-getvul/references/state-patterns.md (canonical empty state)
  </read_first>
  <action>
    Create `kanban-column.tsx` (`'use client'`) exporting `KanbanColumn`. Props:
    `{ columnKey: ColumnKey; label: string; count: number; isValidTarget: boolean; isDragActive: boolean; children: React.ReactNode; isEmpty: boolean }`.
    Use `const { setNodeRef, isOver } = useDroppable({ id: columnKey })` — keep the droppable ENABLED for all columns (per RESEARCH Pattern 3: gate in onDragEnd, not via `disabled`).
    Structure:
    - Outer column wrapper: `cn('snap-start shrink-0 basis-[85vw] md:basis-0 md:flex-1 flex flex-col rounded-lg', isDragActive && !isValidTarget && 'opacity-40', isOver && isValidTarget && 'ring-2 ring-violet')` and `aria-disabled={isDragActive && !isValidTarget}`. Wrap the droppable region with `role="region"` and `aria-label={label}` (so the e2e `getByRole('region',{name})` and axe both work) and add `data-column={columnKey}`.
    - Header (`flex items-center gap-2 px-1 pb-2`): a status-accent dot + `label` + a count badge. Reuse the D-P-04 accent color per column via a local map: open→`text-[var(--color-violet-on-soft)] border-violet/40 bg-violet-soft`, in_progress→`text-[var(--color-amber-on-soft)] border-amber/40 bg-amber/10`, completed→`text-success border-success/40 bg-success/10`, blocked→`text-severity-critical border-severity-critical/40 bg-severity-critical/10` (mirror status-pill.tsx classes so light+dark AA holds). The count badge is a small pill: `<span className="ml-auto rounded-full border border-border-subtle px-2 py-0.5 text-xs text-text-muted">{count}</span>`.
    - Body (`ref={setNodeRef}`, `flex flex-col gap-2 min-h-[4rem]`): render `children` (the cards). When `isEmpty`, render the canonical `<EmptyState><EmptyState.Title>Nothing here</EmptyState.Title><EmptyState.Body>{empty copy per column}</EmptyState.Body></EmptyState>` instead of cards. Use peer-voice copy (copy-voice.md) — e.g. Blocked empty: "No blockers right now." Open empty under a status filter: "Nothing in this column."
    - No raw hex; sunset tokens only. Do NOT use position:fixed/sticky (Pitfall 4 — must not collide with the bottom-nav).
  </action>
  <verify>
    <automated>cd frontend && grep -q "useDroppable" src/components/tickets/kanban-column.tsx && grep -q "EmptyState" src/components/tickets/kanban-column.tsx && grep -q "data-column" src/components/tickets/kanban-column.tsx && grep -q "role=\"region\"" src/components/tickets/kanban-column.tsx && ! grep -qE "#[0-9a-fA-F]{3,6}" src/components/tickets/kanban-column.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `kanban-column.tsx` contains `useDroppable`, `EmptyState`, `data-column`, and `role="region"` with `aria-label={label}`
    - Renders EmptyState when `isEmpty` (contains a conditional on `isEmpty`)
    - Dim cue present: `isDragActive && !isValidTarget && 'opacity-40'`
    - No raw hex (grep for `#[0-9a-fA-F]{3,6}` → no matches)
    - `useDroppable` is NOT called with `disabled:` (gating happens in onDragEnd)
  </acceptance_criteria>
  <done>Droppable column with status-accent header, live count, canonical per-column EmptyState, and drag-dim cue.</done>
</task>

<task type="auto">
  <name>Task 3: Build the KanbanReasonPrompt (turns 18-01 RED test GREEN)</name>
  <files>frontend/src/components/tickets/kanban-reason-prompt.tsx</files>
  <read_first>
    - frontend/src/components/tickets/blocked-toggle.tsx (Save/Cancel + trim()||null + maxLength 500 + Enter/Esc + setTimeout focus — the exact pattern to mirror)
    - frontend/src/components/tickets/microcopy.ts (blockedPrompt/blockedSave/blockedCancel)
    - frontend/src/components/tickets/kanban-reason-prompt.test.tsx (the RED contract from 18-01 this must satisfy)
    - 18-CONTEXT.md D-DRAG-02 (optional reason, Cancel = no mutation, whitespace→null)
    - .planning/phases/18-tickets-kanban-board/18-RESEARCH.md §Pitfall 6 (focus timing after keyboard drop)
  </read_first>
  <action>
    Create `kanban-reason-prompt.tsx` (`'use client'`) exporting `KanbanReasonPrompt` with props `{ ticketLabel: string; onSave: (reason: string | null) => void; onCancel: () => void }`. Render a small accessible popover: `role="dialog"` + `aria-label={`Blocked reason for ${ticketLabel}`}`, an `<input maxLength={500}>` with `placeholder={microcopy.blockedPrompt}` and `aria-label="Blocked reason"`, plus Save and Cancel buttons using `microcopy.blockedSave` / `microcopy.blockedCancel`. Behavior mirrors blocked-toggle.tsx exactly:
    - Local `reason` state; `handleSave = () => onSave(reason.trim() || null)`; Cancel button → `onCancel()` (never calls onSave).
    - `onKeyDown`: Enter → handleSave (preventDefault); Escape → onCancel (preventDefault).
    - Autofocus the input on the next frame: `useEffect(() => { const t = setTimeout(() => inputRef.current?.focus(), 0); return () => clearTimeout(t); }, [])` (Pitfall 6 — avoids racing dnd-kit's post-drop focus restore).
    - Styling: reuse blocked-toggle's inline-editor Tailwind classes (surface-2 input, severity-critical Save, border-subtle Cancel). No raw hex.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/tickets/kanban-reason-prompt.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `kanban-reason-prompt.tsx` exports `KanbanReasonPrompt` with the exact prop names `ticketLabel`, `onSave`, `onCancel`
    - Input has `maxLength={500}`
    - `onSave` receives `reason.trim() || null` (whitespace→null)
    - `npx vitest run src/components/tickets/kanban-reason-prompt.test.tsx` exits 0 (the 18-01 RED test is now GREEN)
    - No raw hex
  </acceptance_criteria>
  <done>Reason prompt matches the pinned contract; the Wave 0 RED unit test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → mutation body | The reason prompt captures a free-text string later sent as `blocked_reason` |
| ticket data → DOM | Card/column render ticket title, id, assignee, reason as React children |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering | kanban-reason-prompt input | mitigate | `maxLength=500` on the input + `reason.trim() || null` coercion (mirrors blocked-toggle T-13-20/T-13-21); backend Pydantic bounds it independently. |
| T-18-06 | Tampering (XSS) | kanban-card / kanban-column | mitigate | All ticket fields (title, external_ticket_id, blocked_reason, assignee) render as React text nodes; no `dangerouslySetInnerHTML`. Provider narrowed via `isTicketProvider` literal guard (no `as` launder) before ProviderMark. |
| T-18-07 | Tampering | severity/status class lookup | mitigate | Severity + column-accent classes come from literal lookup objects keyed by lowercased known values with a neutral fallback — no user-string interpolation into className. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/tickets/` exits 0 (bucket + reason-prompt + pre-existing table specs).
- `grep` acceptance gates on each component pass.
</verification>

<success_criteria>
- Shared `severity-glyph.ts` is the single source; `tickets-table.tsx` consumes it with no visual change.
- `KanbanCard` is draggable, compact, red-accented when blocked, has no status pill, opens the drill on click.
- `KanbanColumn` is droppable, always renders, shows count + status accent + per-column EmptyState + drag-dim.
- `KanbanReasonPrompt` mirrors blocked-toggle and turns the 18-01 RED unit test GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/18-tickets-kanban-board/18-02-SUMMARY.md`.
</output>
