---
phase: 18-tickets-kanban-board
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/components/tickets/bucket-tickets.ts
  - frontend/src/components/tickets/bucket-tickets.test.ts
  - frontend/src/lib/queries/use-mark-blocked.ts
  - frontend/src/lib/queries/use-mark-blocked.test.ts
autonomous: true
requirements: [UX-D-01-01, UX-D-01-02]
must_haves:
  truths:
    - "@dnd-kit/core@6.3.1 is installed and importable"
    - "bucketTickets() buckets a TicketSummary[] into 4 columns with Blocked-wins and unknown→Open"
    - "useMarkBlocked optimistically flips the blocked flag in the ['tickets','list',*] cache (all permutations) and restores it on error"
  artifacts:
    - path: "frontend/src/components/tickets/bucket-tickets.ts"
      provides: "Pure bucketTickets() + COLUMN_ORDER + ColumnKey type + column labels"
      contains: "export function bucketTickets"
    - path: "frontend/src/components/tickets/bucket-tickets.test.ts"
      provides: "Unit coverage for Blocked-wins, in_progress alias, unknown→Open, order"
    - path: "frontend/src/lib/queries/use-mark-blocked.ts"
      provides: "Optimistic block/unblock mutation with fuzzy list-cache patch"
      contains: "setQueriesData"
    - path: "frontend/src/lib/queries/use-mark-blocked.test.ts"
      provides: "Pitfall 1 regression guard (onMutate flips list caches, onError restores)"
  key_links:
    - from: "frontend/src/lib/queries/use-mark-blocked.ts"
      to: "['tickets','list',{filters,page,view}] caches"
      via: "qc.setQueriesData({ queryKey: ['tickets','list'] }, updater)"
      pattern: "setQueriesData"
---

<objective>
Land the code foundation the kanban board consumes: install `@dnd-kit/core@6.3.1`, author the pure `bucketTickets()` projection helper (with tests), and fix the latent `useMarkBlocked` optimistic-cache-key bug (Pitfall 1) so optimistic column re-projection and rollback work for both the board AND the existing list view.

Purpose: The board is a pure projection of the `useTickets` list cache. Two things must be true before the board is built — (1) the projection function exists and is proven, and (2) the block/unblock mutation actually patches the list cache optimistically. Today `useMarkBlocked` patches `['tickets']` (exact) but list data lives under `['tickets','list',{…}]`, so the optimistic list patch is a NO-OP. This plan fixes that.
Output: `@dnd-kit/core` in package.json; `bucket-tickets.ts` (+ test, GREEN); patched `use-mark-blocked.ts` (+ regression test, GREEN).
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-tickets-kanban-board/18-CONTEXT.md
@.planning/phases/18-tickets-kanban-board/18-RESEARCH.md
@.planning/phases/18-tickets-kanban-board/18-VALIDATION.md

<interfaces>
<!-- Real signatures the executor uses directly — no codebase exploration needed. -->

From frontend/src/lib/queries/use-tickets.ts:
```typescript
export type TicketSummary = {
  id: string;
  provider: string;                 // backend-lowercased: 'jira' | 'asana' | 'github'
  external_ticket_id: string;
  title: string;
  external_status: string | null;   // 'open' | 'in_progress' | 'completed' | null (case-insensitive)
  blocked: boolean;
  blocked_reason: string | null;
  sla_due_at: string | null;
  assignee: string | null;
  max_severity: string | null;
  vuln_count: number;
  critical_count: number;
  high_count: number;
  external_ticket_url: string;
};
export type TicketsResponse = { items: TicketSummary[]; total: number; page: number; page_size: number; pages: number; };
```

From frontend/src/lib/queries/keys.ts:
```typescript
queryKeys.tickets.all = ['tickets']                                  // fuzzy prefix
queryKeys.tickets.list = (opts:{filters,page,view}) => ['tickets','list', opts]
queryKeys.tickets.byId = (id) => ['tickets','detail', id]
```

From frontend/src/components/tickets/use-tickets STATUS_ALLOW (mirror it): `['open','in_progress','completed','blocked']`

TanStack Query v5 (^5.100.10):
- `qc.setQueryData(key, updater)` → EXACT key match (the current bug)
- `qc.setQueriesData({ queryKey }, updater)` → FUZZY/prefix match (the fix)
- `qc.getQueriesData({ queryKey })` → returns `Array<[QueryKey, TData]>` for snapshot/restore
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @dnd-kit/core</name>
  <read_first>
    - frontend/package.json (confirm React 19, existing deps, no @dnd-kit yet)
    - .planning/phases/18-tickets-kanban-board/18-RESEARCH.md §Standard Stack (install command + peer-range note)
  </read_first>
  <action>
    From the frontend directory run `npm install @dnd-kit/core@6.3.1`. Do NOT add `@dnd-kit/sortable` (no intra-column reordering) and do NOT add `@dnd-kit/utilities` (DragOverlay-only rendering inlines its own transform). Do NOT pass `--legacy-peer-deps`: `@dnd-kit/core@6.3.1` declares peer `react: ">=16.8.0"`, which React 19 satisfies, so the plain install resolves cleanly. Do NOT use the v2 experimental packages (`@dnd-kit/react`, `@dnd-kit/dom`) that Context7 indexes — this project uses the stable 6.x `@dnd-kit/core` API only. Confirm `package.json` dependencies now list `"@dnd-kit/core": "^6.3.1"` (or the exact pinned `6.3.1`) and that `package-lock.json` updated.
  </action>
  <verify>
    <automated>cd frontend && node -e "require('@dnd-kit/core'); console.log('dnd-kit resolves')" && grep -q '@dnd-kit/core' package.json && ! grep -q '@dnd-kit/sortable' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` dependencies contains `@dnd-kit/core` at `6.3.1` (or `^6.3.1`)
    - `frontend/package.json` does NOT contain `@dnd-kit/sortable` or `@dnd-kit/react`
    - `node -e "require('@dnd-kit/core')"` exits 0 from the frontend directory
  </acceptance_criteria>
  <done>@dnd-kit/core@6.3.1 installed and importable; no sortable/v2 packages added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure bucketTickets() projection + tests</name>
  <files>frontend/src/components/tickets/bucket-tickets.ts, frontend/src/components/tickets/bucket-tickets.test.ts</files>
  <read_first>
    - frontend/src/lib/queries/use-tickets.ts (TicketSummary shape, STATUS_ALLOW values)
    - frontend/src/components/tickets/types.ts (TicketStatus union)
    - .planning/phases/18-tickets-kanban-board/18-RESEARCH.md §Pattern 1 (reference implementation)
    - 18-CONTEXT.md D-COL-01 (Blocked wins), D-COL-02 (STATUS_ALLOW map), D-COL-04 (order), Claude's Discretion (unknown/null → Open)
  </read_first>
  <behavior>
    - bucketTickets([]) → { open:[], in_progress:[], completed:[], blocked:[] }
    - A ticket with blocked=true goes to `blocked` REGARDLESS of external_status (Blocked wins — D-COL-01)
    - external_status 'open' → open; 'in_progress' AND 'in progress' → in_progress; 'completed' → completed
    - external_status null, '', or any unrecognized value → open (unknown→Open discretion)
    - Matching is case-insensitive (external_status?.toLowerCase())
    - COLUMN_ORDER === ['open','in_progress','completed','blocked'] (D-COL-04)
    - COLUMN_LABELS maps each key to its display string: open→'Open', in_progress→'In progress', completed→'Completed', blocked→'Blocked'
  </behavior>
  <action>
    Create `bucket-tickets.ts` exporting: `type ColumnKey = 'open' | 'in_progress' | 'completed' | 'blocked'`; `const COLUMN_ORDER: ColumnKey[] = ['open','in_progress','completed','blocked']`; `const COLUMN_LABELS: Record<ColumnKey,string> = { open:'Open', in_progress:'In progress', completed:'Completed', blocked:'Blocked' }`; and `function bucketTickets(rows: TicketSummary[]): Record<ColumnKey, TicketSummary[]>`. Implementation exactly per RESEARCH §Pattern 1: iterate rows; if `t.blocked` push to `cols.blocked` and continue; else lowercase `external_status`, map `'in_progress'`/`'in progress'`→in_progress, `'completed'`→completed, else→open. Import `TicketSummary` as a type from `@/lib/queries/use-tickets`. No React, no side effects — pure module.
    Write `bucket-tickets.test.ts` (vitest) covering every bullet in <behavior>: empty input; blocked-wins (blocked=true + external_status='open' lands in blocked); the in_progress alias pair; unknown/null/'' → open; case-insensitivity ('OPEN', 'Completed'); and COLUMN_ORDER value assertion.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/tickets/bucket-tickets.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bucket-tickets.ts` contains `export function bucketTickets` and `export const COLUMN_ORDER`
    - `grep -c "it(\|test(" frontend/src/components/tickets/bucket-tickets.test.ts` returns ≥6
    - `npx vitest run src/components/tickets/bucket-tickets.test.ts` exits 0
    - A test asserts a blocked=true + external_status='open' ticket lands in the `blocked` bucket (not `open`)
  </acceptance_criteria>
  <done>bucketTickets is a proven pure projection; all behavior bullets covered and GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix useMarkBlocked optimistic list-cache key (Pitfall 1) + regression guard</name>
  <files>frontend/src/lib/queries/use-mark-blocked.ts, frontend/src/lib/queries/use-mark-blocked.test.ts</files>
  <read_first>
    - frontend/src/lib/queries/use-mark-blocked.ts (CURRENT onMutate/onError/onSuccess — the file being modified)
    - frontend/src/lib/queries/keys.ts (tickets.all vs tickets.list key shapes)
    - .planning/phases/18-tickets-kanban-board/18-RESEARCH.md §Pitfall 1 (exact vs fuzzy match, the fix)
  </read_first>
  <behavior>
    - onMutate flips `blocked` + `blocked_reason` in EVERY cached `['tickets','list',*]` query (all filter/page/view permutations), not just an exact `['tickets']` key
    - onMutate still patches the byId detail cache (unchanged)
    - onError restores every list snapshot captured via getQueriesData AND the byId snapshot
    - The mutation body still sends ONLY `{ blocked, blocked_reason }` (T-13-23 mass-assignment guard — do not add fields)
    - onSuccess invalidation unchanged (fuzzy invalidate ['tickets'] + predicate assets remediations)
  </behavior>
  <action>
    In `onMutate`: keep the byId cancel + snapshot + patch as-is. REPLACE the list-cache handling — instead of `qc.setQueryData(allKey, …)` (exact, a no-op), do:
    `await qc.cancelQueries({ queryKey: ['tickets','list'] });`
    `const listSnapshots = qc.getQueriesData<{ items?: unknown[] }>({ queryKey: ['tickets','list'] });`
    then `qc.setQueriesData({ queryKey: ['tickets','list'] }, (prev) => { if (!prev || typeof prev !== 'object') return prev; const typed = prev as { items?: unknown[] }; if (!typed.items) return prev; return { ...typed, items: typed.items.map((item) => { const ticket = item as Record<string,unknown>; return ticket?.id === id ? { ...ticket, blocked, blocked_reason } : item; }) }; });`
    Return `{ snapshot: { byId, listSnapshots } }`.
    In `onError`: restore byId as before, then iterate `listSnapshots` (`Array<[QueryKey, data]>`) and call `qc.setQueryData(key, data)` for each captured pair. Keep the toast.
    Keep `onSuccess` and `retry: 0` unchanged. Keep the snake_case `blocked_reason` keys (WR-07). Update the file's header comment block to describe the fuzzy `setQueriesData` list patch and reference Pitfall 1.
    Create (or extend if it exists) `use-mark-blocked.test.ts`: render the hook with a QueryClientProvider, seed a `['tickets','list',{filters:{},page:1,view:'list'}]` cache with an items array containing the target ticket (blocked:false), mock `api` to (a) succeed and (b) reject; assert after `mutate({id,blocked:true,blocked_reason:'x'})` that `getQueryData` for that list key shows the ticket `blocked:true` DURING the in-flight window (onMutate applied); and assert onError path restores `blocked:false`. Mock `@/lib/api` and `@/components/ui/ToastProvider`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/queries/use-mark-blocked.test.ts && grep -q "setQueriesData" src/lib/queries/use-mark-blocked.ts && grep -q "getQueriesData" src/lib/queries/use-mark-blocked.ts</automated>
  </verify>
  <acceptance_criteria>
    - `use-mark-blocked.ts` contains `setQueriesData` and `getQueriesData`
    - `use-mark-blocked.ts` no longer calls `setQueryData(allKey` / `setQueryData(queryKeys.tickets.all` for the list patch (byId patch may remain)
    - `use-mark-blocked.ts` mutation body still `JSON.stringify({ blocked, blocked_reason })` — no extra fields
    - `use-mark-blocked.test.ts` asserts the `['tickets','list',*]` cache flips in onMutate and restores in onError
    - `npx vitest run src/lib/queries/use-mark-blocked.test.ts` exits 0
  </acceptance_criteria>
  <done>Optimistic list-cache patch uses fuzzy matching; board reprojection + rollback and the existing list view are both repaired and guarded by a test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | `useMarkBlocked` POSTs to `/api/v1/tickets/{id}/blocked` (existing, tenant-scoped, hardened) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | `useMarkBlocked` mutation body | mitigate | Body sends ONLY `{blocked, blocked_reason}` (T-13-23 carried forward); backend Pydantic rejects extras. Acceptance criteria greps the body shape. |
| T-18-02 | Tampering | list-cache updater | accept | The optimistic updater only rewrites `blocked`/`blocked_reason` on the matching `id`; it operates on already-fetched, tenant-scoped data client-side. No new trust boundary. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/tickets/bucket-tickets.test.ts src/lib/queries/use-mark-blocked.test.ts` exits 0 (the phase quick-run command).
- `node -e "require('@dnd-kit/core')"` resolves from frontend.
</verification>

<success_criteria>
- @dnd-kit/core@6.3.1 installed (no sortable/v2).
- bucketTickets proven by ≥6 unit cases (Blocked-wins, in_progress alias, unknown→Open, case-insensitive, order).
- useMarkBlocked patches `['tickets','list',*]` via setQueriesData and restores via captured getQueriesData snapshots on error.
</success_criteria>

<output>
After completion, create `.planning/phases/18-tickets-kanban-board/18-00-SUMMARY.md`.
</output>
