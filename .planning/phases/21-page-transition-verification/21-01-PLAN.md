---
phase: 21-page-transition-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/page-transitions.spec.ts
  - frontend/e2e/playwright.config.ts
autonomous: true
requirements: [UX-D-06-01, UX-D-06-03, UX-D-06-04]

must_haves:
  truths:
    - "The synthetic history.pushState + PopStateEvent proxy is no longer the PRIMARY no-fade evidence in page-transitions.spec.ts — a real router-driven trigger (ChipBar chip or DrillPanel Escape-close) drives the searchParams-only assertion"
    - "A real DrillPanel-open-during-pathname-change navigation (deep-link ?open=drill&cve=… then a sidebar Link click) fires >=1 named authed-page-content VT animation AND leaves no ghost panel ([data-drill-panel] count 0 after nav)"
    - "A DrillPanel Escape-close (real router.replace, searchParams-only) produces 0 named authed-page-content VT animations AND 0px persistent-chrome bounding-box delta (no layout shift), with the panel removed"
    - "The Firefox CSS-keyframe fallback (page-fade-in on [data-no-vt].authed-page-content) is asserted and PROVABLY executes under a Firefox Playwright project — the project collects and runs >=1 test, not 0"
    - "reduced-motion.spec.ts stays green (UX-D-06-02 not regressed) and the D-12 blanket suppresses the fallback keyframe under prefers-reduced-motion"
  artifacts:
    - path: "frontend/e2e/page-transitions.spec.ts"
      provides: "Real router-driven VT assertions: hardened no-fade, DrillPanel-during-nav fade, close-race+layout-shift, Firefox fallback"
      contains: "data-drill-panel"
      min_lines: 180
    - path: "frontend/e2e/playwright.config.ts"
      provides: "A Firefox project whose testMatch collects page-transitions.spec.ts (firefox-transitions)"
      contains: "firefox-transitions"
  key_links:
    - from: "frontend/e2e/page-transitions.spec.ts"
      to: "real DrillPanel shell"
      via: "page.goto('/dashboard/vulnerabilities?cve=CVE-2024-0001&open=drill') + [data-drill-panel] waitFor"
      pattern: "open=drill"
    - from: "frontend/e2e/page-transitions.spec.ts"
      to: "real searchParams-only router.replace"
      via: "ChipBar severity chip click OR page.keyboard.press('Escape') on an open drill"
      pattern: "keyboard.press\\('Escape'\\)"
    - from: "frontend/e2e/playwright.config.ts firefox-transitions project"
      to: "page-transitions.spec.ts"
      via: "testMatch regex"
      pattern: "page-transitions"
---

<objective>
Harden `frontend/e2e/page-transitions.spec.ts` so every Phase 17 (UX-D-06) transition claim is
proven by REAL Next.js App Router navigation instead of the synthetic `PopStateEvent` proxy
(IN-01 from `17-REVIEW.md`). Replace the proxy no-fade trigger with a real router-driven control,
add the marquee DrillPanel-open-during-pathname-change fade test, add a dedicated Esc close-race +
layout-shift test, and add an automated Firefox CSS-fallback assertion — proving it actually runs
under a Firefox project (RESEARCH Pitfall 2).

Purpose: this is the SC#1 arbiter for Phase 21 and the direct answer to the project's recurring
"claimed-but-never-run" anti-pattern (memory `getvul-axe-sweep-not-run-during-exec`). The test —
not the file list — is the arbiter: if a real VT×DrillPanel race or layout shift surfaces, fixing
it becomes in-scope (do NOT pre-plan a production fix; none is expected).
Output: a hardened `page-transitions.spec.ts` + a Firefox-collecting project in `playwright.config.ts`.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-page-transition-verification/21-CONTEXT.md
@.planning/phases/21-page-transition-verification/21-RESEARCH.md
@.planning/phases/21-page-transition-verification/21-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs — extracted directly from the shipped codebase. No exploration needed. -->

From frontend/src/app/(authed)/template.tsx (the transition driver — DO NOT MODIFY):
- Wrapper: `<div className="authed-page-content" {...(noVt ? {'data-no-vt': ''} : {})}>`
- `view-transition-name: authed-page-content` is applied via globals.css class, so native VT
  produces pseudo-elements `::view-transition-old(authed-page-content)` / `-new(authed-page-content)`.
- Module-level `isFirstMount` guard: NO fade on the first client render (hard nav / page.goto /
  refresh). Fades only fire on SUBSEQUENT client-side navigations. → never assert VT on the initial goto.
- `data-no-vt` is set only when `!('startViewTransition' in document)` AND past first mount → this is
  the Firefox / no-VT fallback marker.

From frontend/src/app/globals.css (lines 178-216 — DO NOT MODIFY):
- Native VT cross-fade: `::view-transition-old(authed-page-content),::view-transition-new(authed-page-content){animation-duration:320ms;...}`
- Firefox/no-VT fallback: `@keyframes page-fade-in{from{opacity:0}to{opacity:1}}` +
  `[data-no-vt].authed-page-content{animation:page-fade-in 320ms var(--ease-standard) both;}`
- D-12 reduced-motion blanket (lines 120-127): `*,*::before,*::after{animation-duration:0.01ms!important;...}`
  under `@media (prefers-reduced-motion: reduce)` — catches the fallback keyframe (a real element),
  distinct from the explicit VT-pseudo-element suppression rule at lines 198-205.

From frontend/src/components/vulnerabilities/drill-panel.tsx (DO NOT MODIFY):
- `const isOpen = params?.get('open') === 'drill' && effectiveId !== null;` → deep-link
  `?cve=<any-id>&open=drill` mounts `<aside role="dialog" data-drill-panel …>` with ZERO seeded-data
  dependency (DrillContent may show loading/error inside, but the shell mounts).
- Escape handler (line 62-63): `if (e.key === 'Escape') close();` where `close()` does
  `router.replace(qs ? \`${pathname}?${qs}\` : pathname, { scroll: false })` after deleting `open` + id
  key → a REAL, production, searchParams-only router.replace fired by a real keyboard event.

From frontend/src/components/ui/ChipBar.tsx + ChipBar.test.tsx:
- The severity axis on `/dashboard/vulnerabilities` renders `getByRole('button', { name: /critical/i })`
  (and /high/, /medium/, /low/) from a STATIC allowList; a click calls `router.replace` synchronously
  mutating `?severity=…` only (pathname unchanged). Hidden ONLY in the "empty-filtered" EmptyState branch.

From frontend/e2e/page-transitions.spec.ts (EXISTING — reuse the polling harness verbatim):
- `clickAndPoll()` polls `document.getAnimations()` filtering
  `(a.effect as CSSEffect|null)?.pseudoElement` for `.includes('authed-page-content')`, 50ms interval,
  1500ms deadline (no-fade test uses 800ms). Type helper: `type CSSEffect = { pseudoElement: string | null };`

From frontend/e2e/playwright.config.ts:
- Projects: setup, chromium-a11y (no testMatch → runs ALL specs), chromium-smoke / webkit-smoke /
  firefox-smoke (each `testMatch: /smoke\.spec\.ts/`). firefox-smoke carries
  `launchOptions:{firefoxUserPrefs:{'ui.systemUsesDarkTheme':1}}` + `storageState:'e2e/.auth/state.json'`.
  → No existing project collects page-transitions.spec.ts under Firefox. THIS IS THE PITFALL-2 GAP.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a firefox-transitions project + live-probe the two Wave-0 unknowns</name>
  <read_first>
    - frontend/e2e/playwright.config.ts — the full `projects` array; mirror `firefox-smoke`'s Firefox
      launchOptions + storageState + colorScheme for the new project.
    - .planning/phases/21-page-transition-verification/21-RESEARCH.md §"Architecture Patterns → Recommended
      Test File Structure" (Option 1 vs Option 2), §"Pitfall 2", §"Open Questions" (1 = ChipBar presence,
      A3 = getAnimations shape on Firefox), §"Wave 0 Gaps".
    - Project memory recipe `getvul-local-e2e-perf-gate` for the local prod-build + :3000 + admin-login run.
  </read_first>
  <files>frontend/e2e/playwright.config.ts</files>
  <action>
    (a) CONFIG — Add a dedicated Firefox project (Option 2 from RESEARCH; cleaner isolation + explicit
    proof-of-run than broadening firefox-smoke's testMatch). Append to the `projects` array:

      {
        name: 'firefox-transitions',
        use: {
          ...devices['Desktop Firefox'],
          colorScheme: 'dark',
          launchOptions: { firefoxUserPrefs: { 'ui.systemUsesDarkTheme': 1 } },
          storageState: 'e2e/.auth/state.json',
        },
        testMatch: /page-transitions\.spec\.ts/,
        dependencies: ['setup'],
      },

    Do NOT change any existing project. `chromium-a11y` (no testMatch) already collects
    page-transitions.spec.ts on Chromium — keep it.

    (b) PROBE (record findings in the SUMMARY, no code beyond config) — with the local server up per
    the memory recipe, run two live probes to de-risk Tasks 2 & 3:
      1. ChipBar presence (Open Question 1): `page.goto('/dashboard/vulnerabilities')`, then check
         `getByRole('button', { name: /critical/i }).isVisible()`. Record present/absent — this decides
         whether Task 2's no-fade test uses the ChipBar chip (Pattern 3) or must `test.skip` visibly and
         lean on the close-race test as the guaranteed no-fade evidence.
      2. Firefox getAnimations shape (Assumption A3): under `--project=firefox-transitions`, after a real
         nav, confirm `document.getAnimations()` exposes a keyframe animation whose
         `effect.target` is the `.authed-page-content[data-no-vt]` element (vs. needing `animationName`).
         Record which discriminator works so Task 3's filter is correct on Firefox specifically.

    Proof-of-collection (Pitfall 2): `npx playwright test e2e/page-transitions.spec.ts --project=firefox-transitions --list`
    MUST list page-transitions tests (a non-empty list), NOT "0 tests". At this point the Firefox test
    does not exist yet, so the list will show the existing (Chromium-only) tests being collected under the
    project — that is fine and is the proof the file is now selected. The green run happens after Task 3.
  </action>
  <acceptance_criteria>
    - `grep -c 'firefox-transitions' frontend/e2e/playwright.config.ts` returns `>=1`
    - `grep -nE "testMatch: /page-transitions" frontend/e2e/playwright.config.ts` matches inside the new project block
    - `grep -c "firefoxUserPrefs" frontend/e2e/playwright.config.ts` returns `>=2` (existing firefox-smoke + new project)
    - `cd frontend && npx playwright test e2e/page-transitions.spec.ts --project=firefox-transitions --list` prints at least one test line (NOT "Total: 0")
    - No existing project block removed: `grep -c "name: 'firefox-smoke'" frontend/e2e/playwright.config.ts` returns `1` and `grep -c "name: 'chromium-a11y'" frontend/e2e/playwright.config.ts` returns `1`
    - SUMMARY records both probe results (ChipBar present/absent; Firefox getAnimations discriminator)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q 'firefox-transitions' e2e/playwright.config.ts && npx playwright test e2e/page-transitions.spec.ts --project=firefox-transitions --list 2>&1 | grep -qv 'Total: 0' && echo FIREFOX_PROJECT_COLLECTS</automated>
  </verify>
  <done>A `firefox-transitions` project exists mirroring firefox-smoke's Firefox launch options and pointed at page-transitions.spec.ts; the file is provably collected under it; ChipBar-presence and Firefox-getAnimations-shape probe results are recorded for Tasks 2 & 3.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the PopStateEvent proxy with real router triggers + add DrillPanel-fade and close-race tests (Chromium)</name>
  <read_first>
    - frontend/e2e/page-transitions.spec.ts — the existing `clickAndPoll` harness (lines 44-67) and the
      TEST B PopStateEvent block (lines 85-141). Reuse the poll body verbatim; change only the trigger.
    - frontend/src/components/vulnerabilities/drill-panel.tsx — isOpen gate (line 45), Escape close (60-68),
      close() router.replace (47-57).
    - frontend/src/components/ui/ChipBar.tsx — the severity chip buttons (real searchParams-only control).
    - .planning/phases/21-page-transition-verification/21-RESEARCH.md §"Architecture Patterns" Patterns 2/3/4/5,
      §"Pitfall 1" (first-mount suppression), §"Pitfall 4" (wait for panel-gone before measuring layout).
    - Task 1 SUMMARY probe result (ChipBar present? → Pattern 3 vs skip-visible).
  </read_first>
  <files>frontend/e2e/page-transitions.spec.ts</files>
  <action>
    Guard the whole existing describe block so native-VT tests never run under Firefox (they would always
    fail — Firefox has no startViewTransition). Add at the top of `test.describe('Page-transition motion', …)`:
      `test.skip(({ browserName }) => browserName === 'firefox', 'Native View Transitions path — Chromium only');`

    Then make THREE changes inside the describe:

    1. HARDEN the no-fade test (D-02, replaces IN-01). Rename it
       `'searchParams-only change does NOT trigger a page fade — real router (D-02)'`. Replace the
       `page.evaluate(() => { history.pushState…; dispatchEvent(new PopStateEvent('popstate')); })` block with a
       REAL router-driven searchParams-only mutation:
         - IF Task 1 probe found the ChipBar present: `await page.goto('/dashboard/vulnerabilities');`
           wait for `nav[aria-label="Primary navigation"]`, capture `pathnameBefore`, then
           `await page.getByRole('button', { name: /critical/i }).click();` (real router.replace, ?severity= only).
         - IF the probe found ChipBar absent: `test.skip(true, 'ChipBar not rendered in this e2e data state — no-fade proven by the zero-dependency close-race test below');` — a VISIBLE skip, never a bare return (Anti-pattern from RESEARCH / IN-02).
       Reuse the existing 800ms/50ms poll body unchanged; assert `maxNamedVtCount === 0` and
       `pathnameAfter === pathnameBefore`. You MAY keep the old PopStateEvent block as an explicitly-labeled
       secondary/legacy regression `test(… 'legacy PopStateEvent proxy (IN-01, superseded)')`, but it must NOT
       be the primary evidence.

    2. ADD the marquee fade test (D-02 fade case / D-11, Pattern 5) — SC#1's core:
       `test('DrillPanel open during a real pathname change fades with the content, no ghost panel (UX-D-06-04)', …)`:
         - `await page.goto('/dashboard/vulnerabilities?cve=CVE-2024-0001&open=drill');`
         - `await page.locator('[data-drill-panel]').waitFor({ state: 'visible', timeout: 10_000 });`
           (do NOT poll VT here — this is first mount, Pitfall 1)
         - Capture chrome box: `const navBefore = await page.locator('nav[aria-label="Primary navigation"]').boundingBox();`
         - Click a real sidebar Link to change pathname while the drill is open:
           `const dashLink = page.locator('nav[aria-label="Primary navigation"]').getByRole('link', { name: /dashboard/i });`
           then run the existing clickAndPoll harness with `dashLink` as the trigger (extract/parameterize the
           harness so the trigger locator is an argument, or inline a copy). `expect(namedVtCount).toBeGreaterThanOrEqual(1)`.
         - `await expect(page.locator('[data-drill-panel]')).toHaveCount(0);` (no stuck/ghost panel)
         - After panel-gone, `const navAfter = …boundingBox(); expect(navAfter).toEqual(navBefore);` (chrome stillness)

    3. ADD the dedicated close-race + layout-shift test (D-05, Pattern 4):
       `test('DrillPanel Escape-close fires 0 page fades and causes no layout shift (UX-D-06-04 close-race)', …)`:
         - `await page.goto('/dashboard/vulnerabilities?cve=CVE-2024-0001&open=drill');`
         - `await page.locator('[data-drill-panel]').waitFor({ state: 'visible' });`
         - `const navBefore = await page.locator('nav[aria-label="Primary navigation"]').boundingBox();`
         - `await page.keyboard.press('Escape');` (real close() → router.replace, searchParams-only)
         - Poll the 800ms/50ms harness for named authed-page-content VT animations; assert `maxNamedVtCount === 0`.
         - Wait for panel gone BEFORE measuring (Pitfall 4): `await expect(page.locator('[data-drill-panel]')).toHaveCount(0);`
         - `const navAfter = await page.locator('nav[aria-label="Primary navigation"]').boundingBox(); expect(navAfter).toEqual(navBefore);`
         - Assert pathname unchanged across the close (`expect(pathnameAfter).toBe(pathnameBefore)`).

    Keep TEST A (the existing real-Link pathname cross-fade test) — it is already real; leave its trigger
    unchanged. Do NOT modify template.tsx, globals.css, or drill-panel.tsx (no production change expected).
  </action>
  <acceptance_criteria>
    - `grep -c 'PopStateEvent' frontend/e2e/page-transitions.spec.ts` — if >0, each occurrence is inside a test whose title contains "legacy" or "superseded" (the primary no-fade test must NOT use PopStateEvent). Verify by reading.
    - `grep -c "open=drill" frontend/e2e/page-transitions.spec.ts` returns `>=2` (fade test + close-race test)
    - `grep -c "keyboard.press('Escape')" frontend/e2e/page-transitions.spec.ts` returns `>=1`
    - `grep -c "toHaveCount(0)" frontend/e2e/page-transitions.spec.ts` returns `>=2` (no-ghost-panel + close-race)
    - `grep -c "boundingBox" frontend/e2e/page-transitions.spec.ts` returns `>=3` (fade before/after + close-race before/after)
    - `grep -c "browserName === 'firefox'" frontend/e2e/page-transitions.spec.ts` returns `>=1` (describe-level skip guard)
    - `cd frontend && npx playwright test e2e/page-transitions.spec.ts --project=chromium-a11y` exits 0
    - Any absent-data path uses `test.skip(true, …)` (grep for `test.skip(true` if the probe forced the fallback) — never a bare `return`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx playwright test e2e/page-transitions.spec.ts --project=chromium-a11y</automated>
  </verify>
  <done>page-transitions.spec.ts drives every Chromium assertion via real router navigation: the no-fade test uses a real ChipBar chip (or a visible skip), a new test proves a DrillPanel fades out with the content on a real pathname change with no ghost panel and no chrome shift, and a dedicated Esc close-race test proves 0 fades + 0px chrome delta. The chromium-a11y run is green; no production file is touched.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Firefox CSS-fallback assertion and prove it runs under firefox-transitions</name>
  <read_first>
    - frontend/src/app/globals.css lines 207-216 — the `page-fade-in` keyframe + `[data-no-vt].authed-page-content` rule.
    - frontend/src/app/(authed)/template.tsx lines 46-75 — the `pastFirstMount` + `noVt` gate that sets `data-no-vt`.
    - frontend/e2e/reduced-motion.spec.ts — the `emulateMedia({ reducedMotion: 'reduce' })` idiom + the ≤0.02s
      computed-duration assertion pattern to mirror for the reduced-motion confirmation.
    - .planning/phases/21-page-transition-verification/21-RESEARCH.md §"Code Examples → Firefox fallback keyframe
      detection", §"Pitfall 3" (filter on [data-no-vt]+page-fade-in, NOT pseudoElement).
    - Task 1 SUMMARY probe result A3 (which getAnimations discriminator resolves on Firefox: effect.target vs animationName).
  </read_first>
  <files>frontend/e2e/page-transitions.spec.ts</files>
  <action>
    Add a Firefox-only test OUTSIDE the Chromium describe block (so the describe-level Chromium skip does not
    apply), gated to Firefox:

      test('CSS-keyframe fallback fires on Firefox real pathname change (UX-D-06-03)', async ({ page, browserName }) => {
        test.skip(browserName !== 'firefox', 'Fallback path only exercised on engines without the View Transitions API');
        await page.goto('/dashboard');
        await page.locator('nav[aria-label="Primary navigation"]').waitFor({ state: 'visible', timeout: 10_000 });
        const vulnLink = page.locator('nav[aria-label="Primary navigation"]').getByRole('link', { name: /vulnerab/i });
        await vulnLink.click();  // real client-side pathname change → past first mount → data-no-vt fade
        // Poll for the fallback keyframe on the REAL element (Pitfall 3 — NOT pseudoElement):
        const deadline = Date.now() + 1000;
        let sawFallback = false;
        while (Date.now() < deadline) {
          sawFallback = await page.evaluate(() =>
            document.getAnimations().some((a) => {
              const effect = a.effect as KeyframeEffect | null;
              return effect?.target instanceof Element
                && effect.target.classList.contains('authed-page-content')
                && effect.target.hasAttribute('data-no-vt');
            }),
          );
          if (sawFallback) break;
          await page.waitForTimeout(50);
        }
        expect(sawFallback, 'Expected [data-no-vt] page-fade-in fallback to run on Firefox').toBe(true);
      });

    Use whichever discriminator Task 1's probe confirmed reliable on Firefox (`effect.target` per the snippet,
    or fall back to `(effect as any).getKeyframes()` / an `animationName` check on the computed style if
    `effect.target` proved unreliable — record the chosen form in the SUMMARY).

    Add a SECOND Firefox test confirming the D-12 blanket suppresses the fallback under reduced motion
    (D-04's "confirm the reduced-motion blanket still suppresses the fallback keyframe"):

      test('Firefox fallback is suppressed under prefers-reduced-motion (UX-D-06-02 no regression)', async ({ page, browserName }) => {
        test.skip(browserName !== 'firefox', 'Fallback-only assertion');
        await page.emulateMedia({ reducedMotion: 'reduce' });
        await page.goto('/dashboard');
        await page.locator('nav[aria-label="Primary navigation"]').waitFor({ state: 'visible' });
        await page.locator('nav[aria-label="Primary navigation"]').getByRole('link', { name: /vulnerab/i }).click();
        // Mirror reduced-motion.spec.ts: computed animation-duration on the wrapper must be near-zero.
        await page.waitForTimeout(100);
        const durationOk = await page.evaluate(() => {
          const el = document.querySelector('.authed-page-content');
          if (!el) return false;
          const d = getComputedStyle(el).animationDuration; // "0.00001s" (0.01ms) under the D-12 blanket
          return parseFloat(d) <= 0.02;
        });
        expect(durationOk, 'D-12 blanket must reduce the fallback keyframe duration to <=0.02s under reduce').toBe(true);
      });

    PROVE-IT-RUNS (Pitfall 2): after authoring, the firefox-transitions run must report these Firefox tests
    as RUN (passed), and the Chromium-gated tests as skipped under that project — a non-zero passed count.
  </action>
  <acceptance_criteria>
    - `grep -c "browserName !== 'firefox'" frontend/e2e/page-transitions.spec.ts` returns `>=2` (fallback + reduced-motion Firefox tests)
    - `grep -c "data-no-vt" frontend/e2e/page-transitions.spec.ts` returns `>=1`
    - `grep -c "page-fade-in\|hasAttribute('data-no-vt')\|animationDuration" frontend/e2e/page-transitions.spec.ts` returns `>=1` (fallback detected by element+fallback marker, NOT pseudoElement)
    - `grep -c "pseudoElement" frontend/e2e/page-transitions.spec.ts` occurrences all belong to the native-VT (Chromium) tests, not the Firefox fallback test — verify by reading
    - `cd frontend && npx playwright test e2e/page-transitions.spec.ts --project=firefox-transitions` exits 0 AND its output shows a NON-ZERO passed count for the Firefox tests (not "0 passed" / all skipped) — this is the Pitfall-2 proof the assertion actually executed
    - `cd frontend && npx playwright test e2e/reduced-motion.spec.ts --project=chromium-a11y` still exits 0 (UX-D-06-02 not regressed)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx playwright test e2e/page-transitions.spec.ts --project=firefox-transitions 2>&1 | tee /dev/stderr | grep -Eq '[1-9][0-9]* passed' && echo FIREFOX_FALLBACK_RAN</automated>
  </verify>
  <done>A Firefox-gated test asserts the `[data-no-vt]` page-fade-in fallback actually fires on a real pathname change, plus a companion test confirms the D-12 blanket suppresses it under reduced motion; both provably EXECUTE (non-zero passed count) under `--project=firefox-transitions`, closing the Pitfall-2 silent-skip gap. reduced-motion.spec.ts stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced or affected. This plan adds Playwright e2e test code and one Playwright project
config entry. There is no production request path, no user input handling, no auth/session/crypto
code, no data flow, and no dependency change. The `?cve=<any-id>&open=drill` deep-link is read-only
test-driving of an already-shipped, already-reviewed URL contract.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Spoofing | (none) | accept | No identity/auth surface touched. Tests reuse the existing `e2e/.auth/state.json` storageState; no new auth code. N/A. |
| T-21-02 | Tampering | (none) | accept | No persisted data or production request path. Test-only files + one test-runner config entry. N/A. |
| T-21-03 | Repudiation | (none) | accept | No audit-relevant runtime action. N/A. |
| T-21-04 | Information Disclosure | (none) | accept | The `?cve=CVE-2024-0001` id is a synthetic literal used to mount the panel shell; no real data read/written. N/A. |
| T-21-05 | Denial of Service | (none) | accept | e2e test code; no runtime input surface. N/A. |
| T-21-06 | Elevation of Privilege | (none) | accept | No privilege boundary in test/config code. N/A. |

No high/medium threats. Zero new attack surface (ASVS L1: nothing in scope). CONTEXT.md contingency:
if a test SURFACES a genuine VT×DrillPanel race or layout shift, any resulting production-code fix
would need its own security re-assessment at that time — out of scope until/unless discovered.
</threat_model>

<verification>
- `npx playwright test e2e/page-transitions.spec.ts --project=chromium-a11y` exits 0 with the real
  router-driven no-fade, DrillPanel-fade, and close-race tests green (Task 2).
- `npx playwright test e2e/page-transitions.spec.ts --project=firefox-transitions` exits 0 with a
  NON-ZERO passed count for the Firefox fallback tests (Task 3 — Pitfall-2 proof).
- `npx playwright test e2e/reduced-motion.spec.ts --project=chromium-a11y` stays green (UX-D-06-02).
- The primary no-fade evidence is a real `router.replace` (ChipBar chip or Esc-close), not PopStateEvent.
</verification>

<success_criteria>
- page-transitions.spec.ts exercises the REAL DrillPanel-open-during-navigation scenario (searchParams-only
  vs. pathname change), replacing the synthetic PopStateEvent proxy — confirming no VT×DrillPanel race and
  no layout shift (SC#1; UX-D-06-01, UX-D-06-04).
- The Firefox CSS-fallback cross-fade is automatically asserted and provably runs (UX-D-06-03).
- No production code changed (unless a test surfaces a real defect — the test is the arbiter).
</success_criteria>

<output>
After completion, create `.planning/phases/21-page-transition-verification/21-01-SUMMARY.md`
(record: ChipBar-presence probe result, the chosen Firefox getAnimations discriminator, whether the
no-fade test used ChipBar or a visible skip, and any real defect the new tests surfaced).
</output>
