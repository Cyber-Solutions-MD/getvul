---
phase: 21-page-transition-verification
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - .planning/phases/17-page-transition-motion/17-HUMAN-UAT.md
  - .planning/phases/17-page-transition-motion/17-VERIFICATION.md
  - .planning/STATE.md
autonomous: false
requirements: [UX-D-06-01, UX-D-06-03, UX-D-06-04]

must_haves:
  truths:
    - "17-HUMAN-UAT.md exists with status resolved and records the four perceptual checklist items (a cross-fade feel, b chrome stillness, c DrillPanel-during-transition, d Firefox fallback feel) each with a real user sign-off result"
    - "STATE.md no longer contains the string 'human-UAT checkpoint OUTSTANDING'"
    - "17-VERIFICATION.md exists, is goal-backward, confirms UX-D-06-01..05 with per-requirement evidence (live-run commands + the 21-01 hardened tests + the closed 17-HUMAN-UAT.md), status passed"
  artifacts:
    - path: ".planning/phases/17-page-transition-motion/17-HUMAN-UAT.md"
      provides: "Persisted perceptual sign-off, status: resolved, 4 checklist items"
      contains: "status: resolved"
    - path: ".planning/phases/17-page-transition-motion/17-VERIFICATION.md"
      provides: "Goal-backward verification of Phase 17 UX-D-06-01..05"
      contains: "UX-D-06"
    - path: ".planning/STATE.md"
      provides: "OUTSTANDING flag cleared / replaced with CLOSED wording"
      contains: "17-HUMAN-UAT.md"
  key_links:
    - from: ".planning/phases/17-page-transition-motion/17-VERIFICATION.md"
      to: "frontend/e2e/page-transitions.spec.ts (hardened in 21-01)"
      via: "pasted live command output as evidence"
      pattern: "page-transitions"
    - from: ".planning/phases/17-page-transition-motion/17-VERIFICATION.md"
      to: ".planning/phases/17-page-transition-motion/17-HUMAN-UAT.md"
      via: "Human Verification Required section cites the closed perceptual sign-off"
      pattern: "17-HUMAN-UAT"
---

<objective>
Close the two documentation gaps that keep Phase 17 (UX-D-06) formally unverified: (1) run the
guided perceptual sign-off as a REAL interactive human-action checkpoint and persist it to
`17-HUMAN-UAT.md`, clearing STATE.md's `human-UAT checkpoint OUTSTANDING` line; and (2) author the
goal-backward `17-VERIFICATION.md` confirming UX-D-06-01..05 against the shipped code + the 21-01
hardened tests + the now-closed perceptual sign-off.

Purpose: convert unproven claims into auditable artifacts (the exact anti-pattern this phase exists
to eliminate — memory `getvul-axe-sweep-not-run-during-exec`). This plan runs AFTER 21-01 so the
verification cites GREEN hardened tests as live evidence, not a stale prior run.
Output: `17-HUMAN-UAT.md` (resolved), `17-VERIFICATION.md` (passed), STATE.md flag cleared.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-page-transition-verification/21-CONTEXT.md
@.planning/phases/21-page-transition-verification/21-VALIDATION.md
@.planning/phases/17-page-transition-motion/17-CONTEXT.md
@.planning/phases/17-page-transition-motion/17-VALIDATION.md

<references>
<!-- Format exemplars + authoritative "feel" contract — read before authoring. -->
- .planning/phases/16-light-theme-visual-completion/16-HUMAN-UAT.md — the HUMAN-UAT file shape:
  frontmatter (status/phase/source/started/updated) + `## Current Test` + `## Tests` (numbered,
  each `expected:` / `result:`) + `## Summary` (total/passed/issues/pending/skipped/blocked) + `## Gaps`.
- .planning/phases/16-light-theme-visual-completion/16-VERIFICATION.md — the project's actual
  VERIFICATION.md shape (frontmatter phase/verified/status/score + Goal Achievement Observable-Truths
  table + Requirements Coverage + Anti-Patterns + Human Verification + Gaps Summary).
- /Users/chemencedji/.claude/get-shit-done/templates/verification-report.md — the canonical template.
- .claude/skills/sketch-findings-getvul/references/foundation.md §Motion (lines 147-179) — the locked
  durations (fast 120 / base 220 / slow 320 / xslow 520ms), ease-standard cubic-bezier(0.2,0,0,1),
  "cross-fade only, no transforms", "Page transitions do NOT animate [beyond cross-fade]", reduced-motion
  substitution rule. This is the authoritative wording for the perceptual checklist ("feel") items.
- .claude/skills/sketch-findings-getvul/references/app-shell.md — the persistent-chrome contract
  (sidebar + topbar mounted once, must stay still) behind the D-05 chrome-stillness UAT item.
- .planning/phases/17-page-transition-motion/17-01-SUMMARY.md + 17-02-SUMMARY.md — what shipped and the
  17-02 Task 4 perceptual checkpoint left OUTSTANDING (the source of the 4 checklist items).
</references>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Guided perceptual UAT — persist 17-HUMAN-UAT.md and clear the STATE.md OUTSTANDING flag</name>
  <read_first>
    - .planning/phases/16-light-theme-visual-completion/16-HUMAN-UAT.md — mirror this exact file shape.
    - .claude/skills/sketch-findings-getvul/references/foundation.md §Motion (147-179) — exact "feel" wording.
    - .claude/skills/sketch-findings-getvul/references/app-shell.md — chrome-stillness contract.
    - .planning/STATE.md line 29 — the exact line carrying "human-UAT checkpoint OUTSTANDING" to clear.
    - Project memory `getvul-local-e2e-perf-gate` — how to stand up the prod build on :3000 with admin login.
  </read_first>
  <what-built>
    Phase 17's View Transitions cross-fade is already shipped and (as of 21-01) has hardened, green
    automated coverage. What was never persisted is the PERCEPTUAL human sign-off. This checkpoint stands
    up the running app and walks the user through the four "feel" items, then records the result.
  </what-built>
  <how-to-verify>
    1. Stand up the app per memory `getvul-local-e2e-perf-gate` (prod build + :3000 + admin login; kill
       stray next-server children first). Confirm the authed shell loads at http://localhost:3000/dashboard.
    2. Present this concise checklist and collect a per-item result from the user (Chromium for a-c, Firefox for d):
       a) CROSS-FADE FEEL — Navigate Dashboard → Vulnerabilities → Assets via the sidebar. The content
          region should cross-fade snappily (~220-320ms), PURE OPACITY, no slide/scale/drift
          (foundation.md §Motion: cross-fade only, no transforms).
       b) CHROME STILLNESS — During those same route changes, the sidebar + topbar must NOT move or fade
          (app-shell.md: persistent chrome mounted once). Only the content region transitions.
       c) DRILLPANEL-DURING-TRANSITION — On /dashboard/vulnerabilities open a drill (a row, or
          `?cve=CVE-2024-0001&open=drill`), then navigate to another page via the sidebar. The open drill
          should fade out cleanly WITH the content — no stuck/ghost panel, no layout jump.
       d) FIREFOX FALLBACK FEEL — Repeat (a) in Firefox. The CSS-keyframe fallback cross-fade should look
          equivalent (clean opacity fade, no jank/broken nav).
    3. Record each item's result verbatim from the user in 17-HUMAN-UAT.md.
  </how-to-verify>
  <action>
    This is a REAL interactive pause (autonomous: false) — do NOT auto-approve. After the user signs off
    item-by-item:

    (a) CREATE `.planning/phases/17-page-transition-motion/17-HUMAN-UAT.md` mirroring 16-HUMAN-UAT.md:

      ---
      status: resolved
      phase: 17-page-transition-motion
      source: [21-CONTEXT.md, 17-02-SUMMARY.md]
      started: <ISO timestamp at session start>
      updated: <ISO timestamp at sign-off>
      ---

      ## Current Test

      [complete]

      ## Tests

      ### 1. Cross-fade feel (snappy ~220-320ms, pure opacity, no drift)
      expected: Content region cross-fades on a sidebar route change — pure opacity, no transform, ~220-320ms per foundation.md §Motion.
      result: <verbatim user result — e.g. "passed — approved by user <date>">

      ### 2. Chrome stillness (sidebar/topbar do not move or fade)
      expected: During a pathname change the persistent chrome stays fixed; only the content region transitions.
      result: <verbatim user result>

      ### 3. DrillPanel-during-transition (open drill fades out with content, no ghost panel)
      expected: An open DrillPanel fades out cleanly with the content on a pathname change; no stuck/ghost panel, no layout jump.
      result: <verbatim user result>

      ### 4. Firefox CSS-fallback feel (equivalent cross-fade)
      expected: On Firefox (no VT API) the CSS-keyframe fallback cross-fade looks equivalent — clean opacity fade, no jank.
      result: <verbatim user result>

      ## Summary

      total: 4
      passed: <n>
      issues: <n>
      pending: 0
      skipped: 0
      blocked: 0

      ## Gaps

      <none, or any issue the user reported — if a real defect surfaces, escalate: it becomes an in-scope fix per CONTEXT.md>

    If any item FAILS, set `status: gaps_found`, record the failure under `## Gaps`, and STOP — do NOT
    clear the STATE.md flag (a failed perceptual check means Phase 17 is not verified). Escalate to the user.

    (b) CLEAR the STATE.md flag (only if all 4 items passed). In `.planning/STATE.md`, the "Prior:" line
    (currently line ~29) reads:
      "Prior: Phase 17 (page-transition-motion) COMPLETE — human-UAT checkpoint OUTSTANDING (17-02 Task 4: …)."
    Replace it so the string "human-UAT checkpoint OUTSTANDING" no longer appears, e.g.:
      "Prior: Phase 17 (page-transition-motion) COMPLETE & VERIFIED — human-UAT checkpoint CLOSED (see 17-HUMAN-UAT.md, resolved); 17-VERIFICATION.md authored in Phase 21."
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/17-page-transition-motion/17-HUMAN-UAT.md` succeeds
    - `grep -c 'status: resolved' .planning/phases/17-page-transition-motion/17-HUMAN-UAT.md` returns `1` (assuming all items passed)
    - The UAT file has all four `### ` test headings (cross-fade feel, chrome stillness, DrillPanel-during-transition, Firefox fallback) each with a non-empty `result:` line: `grep -c '^result:' 17-HUMAN-UAT.md` returns `4`
    - `grep -c 'human-UAT checkpoint OUTSTANDING' .planning/STATE.md` returns `0`
    - `grep -c '17-HUMAN-UAT.md' .planning/STATE.md` returns `>=1` (the replacement wording references the persisted artifact)
  </acceptance_criteria>
  <resume-signal>Type "approved" (all four items pass) or describe which item(s) failed.</resume-signal>
  <done>17-HUMAN-UAT.md exists with status resolved and all four perceptual items signed off by the user; STATE.md no longer marks the checkpoint OUTSTANDING and points to the persisted UAT file.</done>
</task>

<task type="auto">
  <name>Task 2: Author 17-VERIFICATION.md (goal-backward, UX-D-06-01..05)</name>
  <read_first>
    - /Users/chemencedji/.claude/get-shit-done/templates/verification-report.md — canonical structure.
    - .planning/phases/16-light-theme-visual-completion/16-VERIFICATION.md — the project's actual applied shape.
    - .planning/phases/17-page-transition-motion/17-VALIDATION.md — the Per-Task Verification Map with the
      per-requirement commands (bundle budget via `next build | grep "First Load JS"`, reduced-motion spec).
    - .planning/phases/21-page-transition-verification/21-01-SUMMARY.md — the 21-01 test results to cite.
    - frontend/e2e/page-transitions.spec.ts + frontend/e2e/reduced-motion.spec.ts — the hardened tests being cited.
    - The just-created 17-HUMAN-UAT.md — cite it for the perceptual truths.
  </read_first>
  <files>.planning/phases/17-page-transition-motion/17-VERIFICATION.md</files>
  <action>
    Create `.planning/phases/17-page-transition-motion/17-VERIFICATION.md` following
    verification-report.md, back-dated in INTENT to Phase 17's goal but authored now (note this explicitly
    in a one-line preamble). RE-RUN each command live and paste the actual output as evidence (RESEARCH Open
    Question 2 recommendation — do NOT "trust the old green run"; that is the anti-pattern being fixed):

    Frontmatter:
      ---
      phase: 17-page-transition-motion
      verified: <ISO timestamp>
      status: passed
      score: 5/5 requirements verified
      human_verification_result: "<summary from 17-HUMAN-UAT.md — e.g. 'approved by user <date> — all 4 perceptual items passed'>"
      authored_retroactively: "Phase 17 shipped without a VERIFICATION.md; authored in Phase 21 (gap closure) against the 21-01-hardened tests + shipped code + persisted 17-HUMAN-UAT.md"
      ---

    Body sections (per template):
    - Phase Goal (from ROADMAP Phase 17) + Verified timestamp + Status.
    - ## Goal Achievement → Observable Truths table, one row per UX-D-06 requirement, each with Status
      (✓ VERIFIED) + Evidence (the concrete command + its live output / the UAT item):
        · UX-D-06-01 (cross-fade via View Transitions, single template.tsx): TEST A + the new DrillPanel-fade
          test in page-transitions.spec.ts — paste `npx playwright test e2e/page-transitions.spec.ts --project=chromium-a11y` result.
        · UX-D-06-02 (fully suppressed under reduced-motion): `npx playwright test e2e/reduced-motion.spec.ts --project=chromium-a11y` + the Firefox reduced-motion assertion.
        · UX-D-06-03 (CSS fallback, no jank in Firefox): the Firefox fallback test (21-01 Task 3) — paste the `--project=firefox-transitions` result + the UAT item (d).
        · UX-D-06-04 (no race with DrillPanel Esc/clickaway, no layout shift): the close-race + DrillPanel-fade tests — paste the chromium-a11y result showing 0 fades + 0px chrome delta.
        · UX-D-06-05 (no route exceeds 250 KB First-Load JS; native API adds 0 KB): run `cd frontend && npm run build` and paste the First-Load-JS lines (confirm all authed routes ≤250 KB; native VT adds 0 KB — no new dependency).
    - ## Requirements Coverage table (UX-D-06-01..05 → SATISFIED).
    - ## Anti-Patterns Found — expected "0 blockers"; explicitly note the IN-01 PopStateEvent proxy is now
      superseded by real router triggers (21-01), and IN-02 (silent-pass-on-absent-data) is guarded via visible test.skip.
    - ## Human Verification Required — reference the CLOSED 17-HUMAN-UAT.md (perceptual "feel" is human-only)
      with its result; nothing outstanding.
    - ## Gaps Summary — "No gaps found. Phase goal achieved." (if all green). If any command comes back red,
      set status: gaps_found, record the gap, and STOP (do not fabricate a pass).
    - Verification Metadata footer (approach: goal-backward; automated checks N passed/M failed; human checks: 1 — the UAT).
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/17-page-transition-motion/17-VERIFICATION.md` succeeds
    - `grep -c 'status: passed' 17-VERIFICATION.md` returns `1` (assuming all evidence green)
    - All five requirement IDs present: `grep -oE 'UX-D-06-0[1-5]' 17-VERIFICATION.md | sort -u | wc -l` returns `5`
    - `grep -c 'page-transitions.spec.ts' 17-VERIFICATION.md` returns `>=1` (cites the hardened tests)
    - `grep -c '17-HUMAN-UAT' 17-VERIFICATION.md` returns `>=1` (cites the perceptual sign-off)
    - `grep -c 'First Load JS\|First-Load JS\|250' 17-VERIFICATION.md` returns `>=1` (UX-D-06-05 bundle evidence)
    - The evidence rows contain PASTED live command output, not "trust the prior run" language
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/17-page-transition-motion/17-VERIFICATION.md && test "$(grep -oE 'UX-D-06-0[1-5]' .planning/phases/17-page-transition-motion/17-VERIFICATION.md | sort -u | wc -l | tr -d ' ')" = "5" && grep -q 'page-transitions.spec.ts' .planning/phases/17-page-transition-motion/17-VERIFICATION.md && echo VERIFICATION_COMPLETE</automated>
  </verify>
  <done>17-VERIFICATION.md exists as a goal-backward artifact confirming UX-D-06-01..05 with live-run evidence from the 21-01 hardened tests, the bundle build, and the closed 17-HUMAN-UAT.md; status passed, 5/5 requirements satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced or affected. This plan authors two Markdown planning artifacts and edits one line of
STATE.md. No production code, no request path, no input handling, no auth/data flow, no dependency change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07 | Spoofing | (none) | accept | Documentation-only; no identity/auth surface. N/A. |
| T-21-08 | Tampering | (none) | accept | Markdown artifacts + one STATE.md line; no runtime data or code path. N/A. |
| T-21-09 | Repudiation | (none) | accept | The plan INCREASES auditability (persists a previously-informal sign-off); no repudiation risk. N/A. |
| T-21-10 | Information Disclosure | (none) | accept | No sensitive data; planning docs only. N/A. |
| T-21-11 | Denial of Service | (none) | accept | No runtime surface. N/A. |
| T-21-12 | Elevation of Privilege | (none) | accept | No privilege boundary in docs. N/A. |

No high/medium threats. Zero new attack surface (ASVS L1: nothing in scope).
</threat_model>

<verification>
- 17-HUMAN-UAT.md exists (status resolved) with all four perceptual items signed off (Task 1).
- STATE.md no longer contains "human-UAT checkpoint OUTSTANDING" and references 17-HUMAN-UAT.md (Task 1).
- 17-VERIFICATION.md exists (status passed), confirms UX-D-06-01..05 with live-run evidence, cites both
  the 21-01 hardened tests and the closed UAT (Task 2).
</verification>

<success_criteria>
- 17-HUMAN-UAT.md records the perceptual checkpoint as CLOSED and STATE.md no longer marks it OUTSTANDING
  (SC#2; UX-D-06-03).
- 17-VERIFICATION.md is authored from a goal-backward verify pass confirming UX-D-06-01..05 (SC#3;
  UX-D-06-01, UX-D-06-04).
</success_criteria>

<output>
After completion, create `.planning/phases/21-page-transition-verification/21-02-SUMMARY.md`
(record: the four UAT item results, the live-run outputs pasted into 17-VERIFICATION.md, and whether
any perceptual or automated check surfaced a real defect requiring an in-scope fix).
</output>
