---
phase: 36-remediation-sla-engine-escalation
plan: 03
type: execute
wave: 3
depends_on: [36-01, 36-02]
autonomous: true
requirements: [SLA-03]
files_modified:
  - backend/app/vulnerabilities/sla_tier_service.py
  - backend/app/connectors/scheduler.py
  - backend/app/notifications/alerts.py
  - backend/app/vulnerabilities/router.py
  - backend/tests/test_escalation_engine.py

must_haves:
  truths:
    # SLA-03 + specless SLA-03 exactly-once probe + D-08
    - "An approaching or breach transition fires the tenant-configured channels for that transition type exactly once per (finding, to_state, channel) — re-running the tick logic twice produces no second escalation-event row and no second POST (D-07, specless SLA-03 probe)"
    - "The once-only gate holds even if the tick runs twice concurrently: the DB UniqueConstraint uq_escalation_once backstops the check-before-insert (D-07)"
    - "Only findings at or above the configured tier floor fire escalations; findings below the floor still track SLA state but never page (D-06)"
    - "Each transition type routes independently to its configured channel set: approaching -> its channels, breached -> its channels (D-05)"
    - "Every escalation fire goes through the fail-closed audit() path with action sla.escalation_fire (D-07)"
    - "One breach yields exactly one in-app signal (category=sla_escalation) plus the channel fan-out — the legacy alerts.py _check_sla_breaches no longer independently fires an sla_breach notification for the same finding (D-08 reconciliation)"
    - "A finding remediated / returned to on_track does not fire escalations; NULL-score fallback findings are gated by their severity-mapped tier consistently with scored findings (D-03/Pitfall 5)"
    - "GET /vulnerabilities/{id}/escalations returns that finding's escalation history (from_state, to_state, channel, fired_at, delivery_status, error_message), tenant-scoped (D-07 user-visible history)"
    - "A failed channel POST is recorded on the escalation-event row (delivery_status=failed, error_message) and audited, but does not block the transition record or other tenants (Pattern 1)"
  artifacts:
    - path: "backend/app/vulnerabilities/sla_tier_service.py"
      provides: "detect_and_escalate, _escalation_already_fired"
      contains: "def detect_and_escalate"
    - path: "backend/app/vulnerabilities/router.py"
      provides: "GET /vulnerabilities/{id}/escalations endpoint"
      contains: "escalations"
  key_links:
    - from: "backend/app/connectors/scheduler.py"
      to: "sla_tier_service.detect_and_escalate"
      via: "SLA tick block, after run_sla_tier_pass"
      pattern: "detect_and_escalate"
    - from: "backend/app/notifications/alerts.py"
      to: "_check_sla_breaches reconciled to no-op"
      via: "D-08 double-fire prevention"
      pattern: "_check_sla_breaches"
  prohibitions:
    - "Do not let alerts.py _check_sla_breaches keep firing an sla_breach notification alongside the new engine (D-08 / Pitfall 1)"
    - "Do not register a new scheduler — extend the existing tick block after run_sla_tier_pass"
    - "Do not re-POST an escalation whose (finding,to_state,channel) row already exists"
    - "Do not use a time-window on the once-only check — D-07 is 'ever fired for this exact transition+channel', no lookback"
---

<objective>
Drive the escalation infrastructure from the scheduler tick: detect approaching/breach transitions, gate by tier floor + per-transition routing, fire exactly once (DB-gated), audit every fire, emit the single in-app twin, record delivery status, and reconcile the legacy breach path so one breach = one signal. Expose the escalation history for the drill panel.

Purpose: Deliver SLA-03 firing semantics + the D-08 no-double-fire invariant.
Output: detect_and_escalate wired into the tick, alerts.py reconciled, escalation-history read endpoint.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-remediation-sla-engine-escalation/36-CONTEXT.md
@.planning/phases/36-remediation-sla-engine-escalation/36-RESEARCH.md
@.planning/phases/36-remediation-sla-engine-escalation/36-PATTERNS.md

<interfaces>
From Plan 01 sla_tier_service.py: resolve_state_for_vuln, get_tier_policy, tier_for_score, severity_to_tier, run_sla_tier_pass
From Plan 02 escalation_channels.py: dispatch_channel(channel, config, context)->dict{ok,error}
From Plan 02 models.py: SlaEscalationEvent (tenant_id, vulnerability_id, from_state, to_state, channel, fired_at, delivery_status, error_message)
From backend/app/notifications/alerts.py:100-143 _check_sla_breaches (legacy, category="sla_breach"); :276-296 _notification_exists (dedup shape to mirror)
From backend/app/notifications/service.py:19-33 create_notification(..., category=..., resource_type=, resource_id=, details=)
From backend/app/audit.py:143 audit(db, user_or_none, action, resource_type, resource_id, details) — scheduler passes user=None; use user_email="system:scheduler" precedent (encryption.py:264)
From backend/app/connectors/scheduler.py: the SLA tick block now calling run_sla_tier_pass (Plan 01)
Config: sla_config.tier_floor (D-06), sla_config.routing {approaching:[...], breached:[...]} (D-05), sla_config.channels (secrets, decrypt server-side)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Failing tests — exactly-once, floor, routing, audit, D-08</name>
  <read_first>
    - backend/tests/test_scheduler_enrichment_refresh.py (direct-await the dispatcher convention)
    - backend/tests/conftest.py
    - backend/app/notifications/alerts.py:100-143, 276-296
    - .planning/phases/36-remediation-sla-engine-escalation/36-RESEARCH.md (Pitfall 1 double-fire; 429-452 audit + notification shapes)
  </read_first>
  <behavior>
    - Calling detect_and_escalate twice for the same breached finding creates exactly one SlaEscalationEvent per (finding,to_state,channel) and calls dispatch_channel once per configured channel
    - A finding below tier_floor produces zero escalation-event rows but a valid (non-not_tracked) tracked state
    - approaching routing fires only approaching-configured channels; breached routing fires only breached-configured channels
    - Every fire produces exactly one audit row (action sla.escalation_fire)
    - After reconciliation, a single breach produces exactly one in-app notification for the finding: category=sla_escalation present, legacy category=sla_breach absent for the same resource_id
    - A monkeypatched dispatch_channel returning {ok:False} still writes the escalation-event row (delivery_status=failed) and audits, and does not raise
    - GET /vulnerabilities/{id}/escalations returns the rows tenant-scoped
  </behavior>
  <action>
    Create backend/tests/test_escalation_engine.py. Seed a tenant with a tier policy + tier_floor + routing + (fake) channel config and a breached-past-due scored finding. Direct-await detect_and_escalate (monkeypatch dispatch_channel to a recording fake). Assert every case in <behavior>, including the double-invocation exactly-once assertion and the D-08 no-double-fire assertion (query Notification for both categories). Add an API test for the escalations read endpoint. Do not implement yet.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_escalation_engine.py -q` is RED
    - The double-invocation exactly-once case and the D-08 category-pair case are both present
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret pytest tests/test_escalation_engine.py -q; test $? -ne 0</automated>
  </verify>
  <done>RED baseline for firing semantics committed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement detect_and_escalate + reconcile alerts.py + wire scheduler (GREEN)</name>
  <read_first>
    - backend/app/vulnerabilities/sla_tier_service.py (Plan 01 output — extend)
    - backend/app/notifications/alerts.py:100-143 (retire/no-op _check_sla_breaches)
    - backend/app/connectors/scheduler.py (SLA tick block calling run_sla_tier_pass)
    - backend/app/audit.py:143-175; backend/app/notifications/service.py:19-33
    - .planning/phases/36-remediation-sla-engine-escalation/36-PATTERNS.md (exactly-once gate, in-app twin, scheduler isolation, alerts reconcile)
  </read_first>
  <behavior>
    - All Task-1 tests GREEN with no test edits
  </behavior>
  <action>
    In sla_tier_service.py add _escalation_already_fired(db, tenant_id, vuln_id, to_state, channel)->bool (check-before-insert, no time window, mirroring alerts._notification_exists) and detect_and_escalate(db, tenant) that: for each OPEN/IN_PROGRESS finding, resolves current state + tier (resolve_state_for_vuln); if to_state in {approaching, breached} AND tier >= tier_floor (compare via an ordered tier ranking so NULL-score severity-fallback tiers gate consistently, D-03/Pitfall 5); for each channel in sla_config.routing[to_state]; if not _escalation_already_fired: decrypt the channel secret server-side, call dispatch_channel, insert a SlaEscalationEvent row (delivery_status sent/failed + error_message), audit(db, None-with-system:scheduler-actor, "sla.escalation_fire", "vulnerability", str(vuln_id), {...}); after the channel fan-out for a breach, emit exactly one create_notification(category="sla_escalation", ...) as the in-app twin. Wrap the whole pass in the Pattern-1 isolation shape (a channel/tenant failure must not stall others). In alerts.py reconcile _check_sla_breaches to a no-op (or gate it off) so it no longer creates an sla_breach notification (D-08). In scheduler.py add `await detect_and_escalate(db, t)` immediately after `await run_sla_tier_pass(db, t)` in the same isolated block. Add GET /vulnerabilities/{id}/escalations to vulnerabilities/router.py returning the tenant-scoped escalation history ordered by fired_at.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_escalation_engine.py -q` fully GREEN, no test edits
    - `grep -c detect_and_escalate backend/app/connectors/scheduler.py` >= 1
    - alerts.py no longer creates an sla_breach notification (`grep -c '\"sla_breach\"' backend/app/notifications/alerts.py` == 0, or the create_notification call in _check_sla_breaches is removed/gated — assert via the D-08 test)
    - `grep -c 'sla.escalation_fire' backend/app/vulnerabilities/sla_tier_service.py` >= 1
    - `grep -c 'escalations' backend/app/vulnerabilities/router.py` >= 1
    - Full touched-module backend suite green: run test_escalation_engine.py + test_sla_tier_service.py + test_escalation_channels.py
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret pytest tests/test_escalation_engine.py tests/test_sla_tier_service.py tests/test_escalation_channels.py -q</automated>
  </verify>
  <done>Exactly-once firing, tier-floor + routing gating, audit, single in-app twin, D-08 reconciliation, and escalation-history endpoint all GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| scheduler tick -> channels | fan-out of finding data to external channels |
| scheduler tick -> audit_logs | every fire must be recorded |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-fire-once | Denial of Service (alert flood) | detect_and_escalate | mitigate (HIGH) | check-before-insert _escalation_already_fired + DB UniqueConstraint backstop; idempotent under double-tick |
| T-36-fire-dup | Denial of Service | legacy _check_sla_breaches double-fire | mitigate (HIGH) | reconcile legacy path to no-op; one breach = one signal (D-08); asserted by the category-pair test |
| T-36-fire-audit | Repudiation | escalation fire without audit | mitigate | fail-closed audit("sla.escalation_fire") on every fire, success or failure |
| T-36-fire-spoof | Spoofing | forged escalation history | mitigate | rows written only by the server tick; read endpoint tenant-scoped |
| T-36-fire-isolation | Denial of Service | one bad tenant/channel stalls the tick | mitigate | Pattern-1 own-try/except isolation; failed POST recorded, never raised |
</threat_model>

<verification>
- Exactly-once across double invocation; floor + routing gating; audit per fire
- D-08: single in-app signal, legacy path silent
- Escalation-history endpoint tenant-scoped
</verification>

<success_criteria>
SLA-03 fully delivered: approaching/breach transitions fire the configured channels exactly once, audited, with the legacy path reconciled and history exposed.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)
- EXTENDED `sla_tier_service.py`: `_escalation_already_fired()`, `detect_and_escalate()`
- MODIFIED `scheduler.py` SLA tick block -> calls `detect_and_escalate` after `run_sla_tier_pass`
- MODIFIED `alerts.py` `_check_sla_breaches` -> reconciled to no-op (D-08)
- NEW endpoint `GET /vulnerabilities/{id}/escalations` in `vulnerabilities/router.py`
- New audit action string `sla.escalation_fire`; new notification category `sla_escalation`
- NEW test `backend/tests/test_escalation_engine.py`
</artifacts_produced>

<output>
After completion, create `.planning/phases/36-remediation-sla-engine-escalation/36-03-SUMMARY.md`
</output>
