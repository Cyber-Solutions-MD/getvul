---
phase: 36-remediation-sla-engine-escalation
plan: 06
type: execute
wave: 4
depends_on: [36-01, 36-03, 36-05]
autonomous: false
requirements: [SLA-01, SLA-02, SLA-03]
files_modified:
  - frontend/src/components/settings/sla-escalation-pane.tsx
  - frontend/src/components/settings/microcopy.ts
  - frontend/src/components/settings/settings-sidebar-shell.tsx
  - frontend/src/components/vulnerabilities/drill-content.tsx
  - frontend/src/components/settings/sla-escalation-pane.test.tsx

must_haves:
  truths:
    # SLA-01/02/03 UI + D-10/D-11/D-13/D-15 + UI-SPEC ## UI Considerations lift (Surfaces E1, E3, E4 — all covered; E2 lifted in Plan 01)
    - "A new 'SLA & Escalation' category appears in the /settings sidebar, admin/owner-gated (D-10): added to microcopy Category union + CATEGORY_LABELS ('SLA & Escalation') and to ALL_CATEGORIES + ADMIN_ONLY in settings-sidebar-shell"
    - "The pane renders three section cards — SLA policy, Escalation channels, Escalation floor — using surface/border/text tokens only, with the SaveBar 'Save changes' CTA as the sole gradient element (UI-SPEC Visual Hierarchy)"
    - "The pane wires useTenantSettings + useUpdateTenantSettings + useDirtyState with a single SaveBar (isDirty/isSaving/onSave/onDiscard) and reports dirty state up via onDirtyChange (D-10 Phase 14 pattern)"
    - "Channel secret fields (Slack/Teams webhook URL, PagerDuty routing key) seed EMPTY with a •••••••• placeholder and are included in the PATCH body only when touched (D-14 mask-on-read twin, notifications-pane passwordTouched pattern)"
    - "The tier-floor control renders as 'Escalate at' with options Critical only / High and critical / All tracked tiers (D-06)"
    - "Per-transition routing renders per channel as Approaching / Breach checkboxes (D-05)"
    - "The PagerDuty field shows the mandatory manual-resolution helper copy (D-13); the Teams field shows the mandatory Workflows-not-classic-connector setup copy (D-15)"
    - "The extended SlaPill renders the server sla_state on the finding drill panel, matching the finding row (D-11 drill panel)"
    - "The drill panel shows an escalation-history list (ActivityTimeline-style) from GET /vulnerabilities/{id}/escalations; a failed delivery renders an amber-tinted, audit-only row (NO retry button, D-08) with '{Channel} delivery failed — HTTP {code} {error_message} · fired {fired_at}' and the transition record stays visible (D-07)"
    # UI-SPEC Surface E1 — SLA & Escalation settings pane (8 covered)
    - "E1 loading: initial fetch reuses SkeletonTable verbatim"
    - "E1 error: fetch failure reuses PartialFailureBanner verbatim (HTTP code + request id + retry)"
    - "E1 empty: zero channels renders the 'No escalation channels configured' EmptyState"
    - "E1 populated: three token-styled section cards with the single gradient SaveBar CTA"
    - "E1 partial: each policy field renders independently with its own helper copy — a partially-filled policy is a valid intermediate state, no all-or-nothing gate"
    - "E1 overflow: a long webhook-URL/API-key value truncates with text-ellipsis overflow-hidden + a title attr showing the full value on hover"
    - "E1 zero-one-many: card-per-channel list, empty at 0, no awkward reflow 1->4"
    - "E1 long-text: long freeform input strings use the same truncate + title-on-hover treatment"
    # UI-SPEC Surface E3 — escalation-history list in drill panel (8 covered)
    - "E3 empty: 'No escalations yet' compact inline empty (not the full gradient-icon empty-card shell)"
    - "E3 populated: ActivityTimeline-style chronological event list"
    - "E3 error: per-event delivery failure -> amber-tinted row + failure copy, audit-only, NO retry button (D-08); the transition record stays visible (D-07)"
    - "E3 partial: a fired-but-failed event still shows its transition record with the failure annotation — a partial delivery is never a hidden event"
    - "E3 loading: opens inside an already-loaded drill panel; the sub-list uses the drill panel's existing load treatment (no separate skeleton)"
    - "E3 overflow: a long error_message truncates + title-attr full value on hover"
    - "E3 long-text: long error_message/URL uses the same truncate + title-on-hover treatment"
    - "E3 zero-one-many: the timeline handles 0/1/many events without reflow"
    # UI-SPEC Surface E4 — channel chips + channel-config list (8 covered)
    - "E4 empty: 'No escalation channels configured' EmptyState (NotificationsPane card pattern)"
    - "E4 populated: one tinted chip per channel (Slack violet / Teams blue / PagerDuty red / Email neutral) with card-per-channel config rows"
    - "E4 partial: a configured-but-unmapped channel renders its chip with unchecked Approaching/Breach boxes — no error, just unmapped"
    - "E4 loading: shares the settings-pane SkeletonTable (same fetch as E1)"
    - "E4 error: shares the settings-pane PartialFailureBanner (same fetch as E1)"
    - "E4 overflow: a long webhook URL in a channel-config input truncates + title-attr full value on hover"
    - "E4 long-text: long webhook URL/channel label uses the same truncate + title-on-hover treatment"
    - "E4 zero-one-many: card-based layout, empty at 0, no awkward reflow 1->4"
  artifacts:
    - path: "frontend/src/components/settings/sla-escalation-pane.tsx"
      provides: "SLA & Escalation admin pane"
      contains: "data-pane=\"sla-escalation\""
    - path: "frontend/src/components/settings/sla-escalation-pane.test.tsx"
      provides: "pane render/dirty/save/RBAC + state-coverage tests"
  key_links:
    - from: "frontend/src/components/settings/sla-escalation-pane.tsx"
      to: "useTenantSettings / useUpdateTenantSettings"
      via: "settings read/write"
      pattern: "useTenantSettings"
    - from: "frontend/src/components/vulnerabilities/drill-content.tsx"
      to: "GET /vulnerabilities/{id}/escalations"
      via: "escalation-history list in the drill panel"
      pattern: "escalations"
  prohibitions:
    - "Do not pick freehand hex — use foundation.md CSS-variable Tailwind tokens (bg-surface, text-text, border-border-subtle, severity/success tones)"
    - "Do not substitute fonts — Inter + JetBrains Mono locked; SLA-state pill + durations + timestamps stay font-mono"
    - "Do not ship any surface without loading/empty/error states (v1 audit top pain point)"
    - "Do not add a retry button to a failed escalation-history row — audit-only (D-08, UI-SPEC E3 user decision)"
    - "Do not describe the retired classic Teams connector in setup copy — describe the Workflows flow (D-15)"
    - "Do not compose generic SaaS copy — use the Copywriting Contract strings verbatim"
    - "Do not invent a hero/stat MTTR display — MTTR display is deferred to Phase 42/43 (UI-SPEC Typography Display note)"
---

<objective>
Build the "SLA & Escalation" admin settings pane and surface live SLA state + escalation history in the vulnerability drill panel — the two user-facing halves of D-10/D-11. Every element extends an already-shipped GetVul primitive (SlaPill, NotificationsPane/SettingsSidebarShell/SaveBar/useDirtyState, ActivityTimeline, PartialFailureBanner/SkeletonTable/EmptyState) per the approved UI-SPEC.

Purpose: Make the SLA policy editable (SLA-01) and make SLA state + escalation history visible (SLA-02/SLA-03, D-07 audit history) — honoring the sketch-findings design system.
Output: A validated, token-compliant settings pane + a drill-panel SLA pill and escalation-history list.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-remediation-sla-engine-escalation/36-CONTEXT.md
@.planning/phases/36-remediation-sla-engine-escalation/36-UI-SPEC.md
@.planning/phases/36-remediation-sla-engine-escalation/36-PATTERNS.md
@.claude/skills/sketch-findings-getvul/references/foundation.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md
@.claude/skills/sketch-findings-getvul/references/visual-language.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md

<interfaces>
Analog to mirror structurally: frontend/src/components/settings/notifications-pane.tsx (FULL)
  useTenantSettings()/useUpdateTenantSettings() (:126-127); useDirtyState<T>() (:129-132); onDirtyChange (:120-137)
  seed via useEffect([settings]) + reset({...}) (:141-149); passwordTouched secret pattern (:44-58,168-175)
  SMTP_PASSWORD_PLACEHOLDER (:75); SkeletonTable/PartialFailureBanner/EmptyState (:32-34,205-219)
  SaveBar (:438-443); data-pane hook (:204)
Test analog: frontend/src/components/settings/saml-pane.test.tsx (notifications-pane has NO test file — do not mirror an untested pane)
microcopy.ts: Category union (:10-17) + CATEGORY_LABELS (:20-28)
settings-sidebar-shell.tsx: ALL_CATEGORIES (:40-48) + ADMIN_ONLY (:54-60); isAdmin gate (:70-76)
SlaPill state prop (Plan 01) + drill-content.tsx (existing drill panel; add SlaPill + escalation history)
Escalation history endpoint (Plan 03): GET /vulnerabilities/{id}/escalations
Copywriting Contract: 36-UI-SPEC.md ## Copywriting Contract (verbatim strings)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SLA & Escalation settings pane + sidebar wiring (with tests)</name>
  <read_first>
    - frontend/src/components/settings/notifications-pane.tsx (FULL — structure to mirror)
    - frontend/src/components/settings/microcopy.ts; settings-sidebar-shell.tsx
    - frontend/src/components/settings/saml-pane.test.tsx (test structure)
    - .claude/skills/sketch-findings-getvul/references/foundation.md + state-patterns.md + visual-language.md + copy-voice.md
    - .planning/phases/36-remediation-sla-engine-escalation/36-UI-SPEC.md (## Copywriting Contract, ## Color, ## UI Considerations E1/E4)
  </read_first>
  <action>
    Create frontend/src/components/settings/sla-escalation-pane.tsx mirroring notifications-pane.tsx: useTenantSettings/useUpdateTenantSettings/useDirtyState/onDirtyChange, seed-from-settings, single SaveBar, data-pane="sla-escalation". Render three token-styled section cards — SLA policy (per-tier day inputs critical/high/moderate as mono number fields + approaching-% field with the section subtitle copy), Escalation channels (card-per-channel Slack/Teams/PagerDuty/Email with tinted channel chips per the Color table, secret inputs using the touched-flag EMPTY-seed + •••••••• placeholder, per-transition Approaching/Breach checkboxes, and the mandatory D-13 PagerDuty + D-15 Teams helper copy verbatim), and Escalation floor ('Escalate at': Critical only / High and critical / All tracked tiers). Wire mandatory SkeletonTable loading, PartialFailureBanner error, and the 'No escalation channels configured' EmptyState. Add 'sla' to microcopy Category union + CATEGORY_LABELS ('SLA & Escalation') and to ALL_CATEGORIES + ADMIN_ONLY in settings-sidebar-shell.tsx. Create sla-escalation-pane.test.tsx (mirroring saml-pane.test.tsx) asserting: renders three cards, dirty-state enables SaveBar, save calls the mutation with touched secrets only, RBAC-aware render, loading/empty/error branches, and the overflow truncate+title behavior on a long webhook URL. Use design tokens only — no raw hex, mono for durations/URLs.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/settings/sla-escalation-pane.test.tsx` passes
    - `npx vitest run src/components/settings/settings-sidebar-shell.test.tsx` passes (category count updated)
    - `grep -c 'data-pane="sla-escalation"' frontend/src/components/settings/sla-escalation-pane.tsx` == 1
    - Mandatory helper copy present: `grep -c 'manual resolution' frontend/src/components/settings/sla-escalation-pane.tsx` >= 1 (D-13) and `grep -ci 'workflow' frontend/src/components/settings/sla-escalation-pane.tsx` >= 1 (D-15)
    - No raw hex: `grep -nE '#[0-9a-fA-F]{3,6}' frontend/src/components/settings/sla-escalation-pane.tsx` returns nothing
    - `npx tsc --noEmit` and eslint clean for the touched files
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/settings/sla-escalation-pane.test.tsx src/components/settings/settings-sidebar-shell.test.tsx</automated>
  </verify>
  <done>Settings pane renders, saves, RBAC-gated, all states present, tokens/copy compliant.</done>
</task>

<task type="auto">
  <name>Task 2: Drill-panel SLA pill + escalation-history list</name>
  <read_first>
    - frontend/src/components/vulnerabilities/drill-content.tsx (existing drill panel structure)
    - frontend/src/components/tickets/sla-pill.tsx (Plan 01 state prop)
    - frontend/src/components/tickets/... ActivityTimeline usage precedent (locate the shared ActivityTimeline)
    - .planning/phases/36-remediation-sla-engine-escalation/36-UI-SPEC.md (## Copywriting Contract empty/error rows; ## UI Considerations E3)
  </read_first>
  <action>
    In drill-content.tsx render the extended SlaPill with the finding's server sla_state (matching the row, D-11). Add an escalation-history sub-section fetching GET /vulnerabilities/{id}/escalations via a query hook (mirror the existing drill-panel data hooks) rendered as an ActivityTimeline-style chronological list: each row shows from_state->to_state, channel chip, mono fired_at; a delivery_status=failed row renders amber-tinted with the verbatim '{Channel} delivery failed — HTTP {code} {error_message} · fired {fired_at}' copy and NO retry button (D-08 audit-only); long error_message truncates + title-on-hover (E3 overflow/long-text). Empty renders the compact inline 'No escalations yet' (not the full empty-card shell). Loading inherits the drill panel's existing load treatment. Add drill-panel tests asserting the SlaPill state render, the populated timeline, the failed-row amber+copy+no-retry-button, and the empty state.
  </action>
  <acceptance_criteria>
    - Drill-panel tests pass, including: SlaPill state render, populated escalation timeline, failed-delivery amber row with the exact copy and no button, and the 'No escalations yet' empty state
    - No retry affordance on failed rows: the failed-row test asserts no button/clickable retry element is present (D-08)
    - `grep -c 'No escalations yet' frontend/src/components/vulnerabilities/drill-content.tsx` >= 1
    - `npx tsc --noEmit` clean; no raw hex in the touched file
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/vulnerabilities/drill-content.test.tsx</automated>
  </verify>
  <done>Drill panel shows server SLA state + auditable escalation history matching the UI-SPEC.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — pane visual + live channel delivery</name>
  <what-built>The SLA & Escalation admin pane (three cards, tinted channel chips, masked secrets, tier-floor + routing controls, mandatory PagerDuty/Teams copy) and the drill-panel SLA pill + escalation-history list, all on the sunset design system.</what-built>
  <how-to-verify>
    1. Log in as an admin/owner. Visit /settings and select "SLA & Escalation". Confirm the three cards render on the sunset theme (no zinc-gray, no raw hex), Inter + JetBrains Mono only, and the SaveBar "Save changes" is the only gradient element.
    2. Confirm loading (reload), empty (no channels), and error (offline) states each render per state-patterns.md.
    3. Enter a real Slack (or Teams Workflow / PagerDuty) webhook in a scratch tenant, map it to the approaching transition, save, and force an approaching transition. Confirm the message arrives with correct formatting (manual-only per VALIDATION.md — third-party delivery cannot be asserted in CI).
    4. Open a finding's drill panel: confirm the SlaPill matches the row and the escalation-history list shows the fired event (and a failed delivery renders amber with no retry button).
    5. Confirm the PagerDuty manual-resolution copy (D-13) and the Teams Workflows setup copy (D-15) are present.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (contrast, token, copy, or delivery problems)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser -> settings API | admin edits policy + secrets |
| secret input UX | must never display stored plaintext |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-ui-secret | Information Disclosure | secret fields round-tripping plaintext | mitigate | seed EMPTY + •••••••• placeholder + touched-flag; only touched secrets sent (mirrors backend mask, D-14) |
| T-36-ui-rbac | Elevation of Privilege | non-admin sees the pane | mitigate | 'sla' added to ADMIN_ONLY; isAdmin gate in settings-sidebar-shell (D-10) |
| T-36-ui-xss | Tampering | rendering server error_message/URL text | accept | React escapes text by default; values rendered as text, not HTML; truncate+title only |
</threat_model>

<verification>
- Pane renders three cards, saves touched secrets only, RBAC-gated, all states present, tokens/copy compliant
- Drill panel SLA pill + auditable escalation history (failed row audit-only)
- Human-verify: visual + live channel delivery (manual per VALIDATION.md)
</verification>

<success_criteria>
SLA-01 editable admin pane + SLA-02 state on the drill panel + SLA-03 escalation history visible, all on the approved design system.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 06)
- NEW component `frontend/src/components/settings/sla-escalation-pane.tsx`
- MODIFIED `microcopy.ts` (Category union + CATEGORY_LABELS gain 'sla') and `settings-sidebar-shell.tsx` (ALL_CATEGORIES + ADMIN_ONLY gain 'sla')
- MODIFIED `drill-content.tsx`: SlaPill server-state render + escalation-history list (consumes GET /vulnerabilities/{id}/escalations)
- NEW test `sla-escalation-pane.test.tsx`; extended drill-content tests
</artifacts_produced>

<output>
After completion, create `.planning/phases/36-remediation-sla-engine-escalation/36-06-SUMMARY.md`
</output>
