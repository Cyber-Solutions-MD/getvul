---
phase: 36-remediation-sla-engine-escalation
plan: 04
type: execute
wave: 4
depends_on: [36-01, 36-02, 36-03]
autonomous: false
requirements: [SLA-04]
files_modified:
  - backend/app/vulnerabilities/models.py
  - backend/alembic/versions/047_add_remediation_events.py
  - backend/app/vulnerabilities/service.py
  - backend/app/ticketing/service.py
  - backend/app/ticketing/daily_sync.py
  - backend/app/vulnerabilities/router.py
  - backend/tests/test_mttr.py

must_haves:
  truths:
    # SLA-04 + specless SLA-04 probe
    - "On every REMEDIATED transition, a remediation_events row is written capturing tier_at_remediation (frozen final tier via tier_for_score, severity fallback if score NULL), duration_seconds (first_detected_at -> remediated_at), first_detected_at, remediated_at (D-09)"
    - "All six REMEDIATED write sites route through a single mark_vulnerability_remediated() helper — none writes status=REMEDIATED + remediated_at without also writing the remediation-event row (Pitfall 6)"
    - "A finding remediated while its risk_exposure_score is NULL freezes tier via the severity fallback map; a finding that never entered a named tier (score < 20) records tier_at_remediation deterministically as 'not_tracked' rather than dropping the row (specless SLA-04 probe)"
    - "get_mttr_by_tier returns the average duration and count grouped by tier_at_remediation for a tenant (SLA-04 queryable)"
    - "GET /vulnerabilities/mttr/by-tier exposes the tier-grouped MTTR aggregate, admin-gated and tenant-scoped (SLA-04 exposed for Phase 42/43)"
  artifacts:
    - path: "backend/app/vulnerabilities/models.py"
      provides: "RemediationEvent model"
      contains: "class RemediationEvent"
    - path: "backend/alembic/versions/047_add_remediation_events.py"
      provides: "remediation_events table migration"
      contains: "create_table"
    - path: "backend/app/vulnerabilities/service.py"
      provides: "mark_vulnerability_remediated helper + get_mttr_by_tier"
      contains: "def mark_vulnerability_remediated"
  key_links:
    - from: "backend/app/ticketing/service.py"
      to: "mark_vulnerability_remediated"
      via: "the 2 ticketing REMEDIATED sites route through the helper"
      pattern: "mark_vulnerability_remediated"
    - from: "backend/app/ticketing/daily_sync.py"
      to: "mark_vulnerability_remediated"
      via: "the 3 daily_sync REMEDIATED sites route through the helper"
      pattern: "mark_vulnerability_remediated"
    - from: "backend/app/vulnerabilities/router.py"
      to: "get_mttr_by_tier"
      via: "GET /vulnerabilities/mttr/by-tier"
      pattern: "mttr"
  prohibitions:
    - "Do not touch the pre-existing flat MTTR queries (service.py:571-579, dashboard.py:212-228, trends.py:129-159) — they are out of scope (Pitfall 11)"
    - "Do not write status=REMEDIATED + remediated_at at any site without routing through mark_vulnerability_remediated (Pitfall 6 — missing one silently drops MTTR)"
    - "Do not use tier-at-detection — freeze tier-at-remediation (final risk tier) per D-09"
---

<objective>
Capture MTTR-by-tier durably: a new remediation_events table, a single mark_vulnerability_remediated() helper that all six REMEDIATED write sites route through (freezing tier-at-remediation), and a tier-grouped MTTR aggregate exposed for Phase 42/43.

Purpose: Deliver SLA-04 without silently dropping data at any of the six scattered status-write sites.
Output: remediation_events table + centralizing helper + MTTR-by-tier query + endpoint.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-remediation-sla-engine-escalation/36-CONTEXT.md
@.planning/phases/36-remediation-sla-engine-escalation/36-RESEARCH.md
@.planning/phases/36-remediation-sla-engine-escalation/36-PATTERNS.md

<interfaces>
From Plan 01 sla_tier_service.py: tier_for_score(score)->str|None, severity_to_tier(severity)->str
The six REMEDIATED write sites (Pitfall 6, VERIFIED line:col):
  vulnerabilities/service.py:332 (update_vulnerability_status, guard new_status=="REMEDIATED")
  vulnerabilities/service.py:349 (bulk_update_status, guard body.status=="REMEDIATED")
  ticketing/service.py:1177-1179 (guard vuln.status != "REMEDIATED")
  ticketing/service.py:1326-1327 (guard vuln.status not in ("REMEDIATED","SUPPRESSED"))
  ticketing/daily_sync.py:243-245, :327-328, :417-418 (guard not in ("REMEDIATED","SUPPRESSED"))
Canonical status write (analog): update_vulnerability_status service.py:325-338
MTTR aggregate analog: trends.py:129-159 get_mttr_trend (group by tier instead of week)
Migration analog: 044_add_risk_backfill_job.py; remediation_events chains off 046 (down_revision "046_add_sla_escalation_events") per Task-1 option-a
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Reversibility gate — confirm the remediation-event table (one-way, D-09)</name>
  <decision>Introduce the remediation_events table via Alembic migration 047 (chaining off 046), capturing tier_at_remediation + duration_seconds per remediation. This is one-way (dropping it later loses MTTR history that Phase 42/43 consume directly).</decision>
  <context>D-09 rates this one-way. Confirm the columns (tenant_id, vulnerability_id, tier_at_remediation String, duration_seconds Integer, first_detected_at, remediated_at, created_at) and that 047 chains off 046 (matching the option-a decision from Plan 02 Task 1). If Plan 02 Task 1 chose the combined-migration option-b, adjust: no separate 047 — extend the combined migration instead.</context>
  <options>
    <option id="option-a">
      <name>Separate migration 047 chaining off 046</name>
      <pros>Matches Plan 02 option-a; independent downgrade; clean per-plan ownership</pros>
      <cons>Second migration file</cons>
    </option>
    <option id="option-b">
      <name>Fold into the combined migration from Plan 02</name>
      <pros>Single schema change if Plan 02 chose option-b</pros>
      <cons>Only valid if Plan 02 Task 1 selected the combined option</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (recommended, matches Plan 02) or option-b (only if Plan 02 combined)</resume-signal>
</task>

<task type="auto">
  <name>Task 2 (Wave 0): Failing tests — remediation-event capture at all 6 sites + MTTR aggregate</name>
  <read_first>
    - backend/tests/conftest.py (vuln/ticket fixtures)
    - backend/app/vulnerabilities/service.py:325-349
    - backend/app/ticketing/service.py:1170-1330; backend/app/ticketing/daily_sync.py:240-420
    - .planning/phases/36-remediation-sla-engine-escalation/36-RESEARCH.md (Pitfall 6 six sites; MTTR aggregate 543-558; Pitfall 13 tier-at-remediation)
  </read_first>
  <behavior>
    - Each of the six REMEDIATED paths, when exercised, produces exactly one remediation_events row with correct tier_at_remediation + duration_seconds
    - A scored finding (score>=20) freezes its tier; a NULL-score finding freezes via severity fallback; a score<20 finding records tier_at_remediation="not_tracked"
    - get_mttr_by_tier returns grouped {tier_at_remediation, avg_seconds, count} for the tenant
    - GET /vulnerabilities/mttr/by-tier returns the aggregate (admin-gated, tenant-scoped)
  </behavior>
  <action>
    Create backend/tests/test_mttr.py. Author tests exercising all six REMEDIATED entry points (direct-call the two service.py functions; drive the two ticketing/service.py paths and the three daily_sync paths via their public functions with monkeypatched provider I/O per existing ticketing test conventions) and assert exactly one remediation-event row per remediation with the correct frozen tier + duration. Assert the NULL-score and score<20 freezing behavior. Assert get_mttr_by_tier grouping and the endpoint. Do not implement yet.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_mttr.py -q` is RED
    - All six sites + NULL/low-score freezing + aggregate + endpoint are covered
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret pytest tests/test_mttr.py -q; test $? -ne 0</automated>
  </verify>
  <done>RED baseline for MTTR capture committed.</done>
</task>

<task type="auto">
  <name>Task 3: Migration + model + mark_vulnerability_remediated + 6-site routing + aggregate (GREEN)</name>
  <read_first>
    - backend/alembic/versions/044_add_risk_backfill_job.py (FULL)
    - backend/app/vulnerabilities/service.py:325-349, 571-579 (canonical status write; DO NOT touch the flat MTTR at 571-579)
    - backend/app/ticketing/service.py:1177-1179, 1326-1327; backend/app/ticketing/daily_sync.py:243-245, 327-328, 417-418
    - backend/app/vulnerabilities/trends.py:129-159 (aggregate shape)
    - .planning/phases/36-remediation-sla-engine-escalation/36-PATTERNS.md (models, migration, centralize-remediation, MTTR aggregate assignments)
  </read_first>
  <behavior>
    - All Task-2 tests GREEN with no test edits
  </behavior>
  <action>
    Add class RemediationEvent to models.py (UUID PK, tenant_id + vulnerability_id FKs ondelete=CASCADE + indexes, tier_at_remediation String(20), duration_seconds Integer, first_detected_at, remediated_at, created_at server_default now()) mirroring RiskExposureBackfillJob conventions. Create migration 047_add_remediation_events.py per Task-1 (revision "047_add_remediation_events", down_revision "046_add_sla_escalation_events"). Add mark_vulnerability_remediated(db, vuln) to vulnerabilities/service.py that sets status="REMEDIATED" + remediated_at=now (matching the existing canonical write) AND inserts a RemediationEvent freezing tier_at_remediation (tier_for_score on the frozen score, else severity_to_tier, else "not_tracked" for score<20) + duration_seconds. Route all six REMEDIATED write sites through this helper, preserving each site's existing guard condition. Add get_mttr_by_tier(db, tenant_id) grouping RemediationEvent by tier_at_remediation (avg duration_seconds + count), mirroring trends.py:129-159 shape. Add GET /vulnerabilities/mttr/by-tier to vulnerabilities/router.py (admin-gated, tenant-scoped) returning it. Assert the migration round-trips.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_mttr.py -q` fully GREEN, no test edits
    - `alembic upgrade head` succeeds; `alembic downgrade -1` then `alembic upgrade head` both succeed (047 defines downgrade)
    - `grep -rc 'mark_vulnerability_remediated' backend/app/ticketing/service.py backend/app/ticketing/daily_sync.py backend/app/vulnerabilities/service.py` shows the helper referenced at all touched files (>=1 each); no remaining bare `status = "REMEDIATED"` write outside the helper (`grep -rn 'REMEDIATED' backend/app/ | grep -v mark_vulnerability_remediated` reviewed — only guard comparisons remain, no status assignments)
    - The pre-existing flat MTTR at service.py:571-579 / dashboard.py / trends.py is unchanged (`git diff --name-only` does NOT include dashboard.py or trends.py)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret pytest tests/test_mttr.py -q</automated>
  </verify>
  <done>MTTR captured at all six sites via one helper; tier-grouped aggregate queryable + exposed; migration reversible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| ticketing sync -> vuln status write | provider-driven remediation transitions |
| API -> browser | MTTR aggregate is non-sensitive but tenant-scoped |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-mttr-tenant | Information Disclosure | remediation_events cross-tenant | mitigate | tenant_id FK + index; get_mttr_by_tier and endpoint filter by tenant_id |
| T-36-mttr-rbac | Elevation of Privilege | MTTR endpoint access | mitigate | admin-gated endpoint |
| T-36-mttr-drop | Tampering (data integrity) | a missed REMEDIATED site drops MTTR | mitigate | single helper + test exercising all six sites (Pitfall 6) |
</threat_model>

<verification>
- Remediation-event written at all six sites with frozen tier + duration
- NULL/low-score freezing deterministic; aggregate grouped correctly; endpoint tenant-scoped
- Migration upgrades + downgrades; flat MTTR untouched
</verification>

<success_criteria>
SLA-04 delivered: MTTR captured per risk tier and queryable/exposed for Phase 42/43.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)
- NEW SQLAlchemy model `RemediationEvent` + table `remediation_events`
- NEW Alembic migration `047_add_remediation_events.py`
- NEW helper `mark_vulnerability_remediated()` in `vulnerabilities/service.py` (routes all 6 REMEDIATED sites)
- NEW aggregate `get_mttr_by_tier()` + endpoint `GET /vulnerabilities/mttr/by-tier`
- MODIFIED `ticketing/service.py` (2 sites) + `ticketing/daily_sync.py` (3 sites) -> route through helper
- NEW test `backend/tests/test_mttr.py`
</artifacts_produced>

<output>
After completion, create `.planning/phases/36-remediation-sla-engine-escalation/36-04-SUMMARY.md`
</output>
