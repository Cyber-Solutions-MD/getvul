---
phase: 36-remediation-sla-engine-escalation
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [SLA-01, SLA-02]
files_modified:
  - backend/app/vulnerabilities/sla_tier_service.py
  - backend/app/vulnerabilities/schemas.py
  - backend/app/vulnerabilities/service.py
  - backend/app/connectors/scheduler.py
  - backend/tests/test_sla_tier_service.py
  - frontend/src/components/tickets/sla-pill.tsx
  - frontend/src/components/tickets/sla-pill.test.tsx
  - frontend/src/components/vulnerabilities/vuln-table.tsx

assumption_delta_decision:
  now_primary_noun: "tier-keyed SLA state per finding (computed off risk_exposure_score)"
  decision: promote
  rationale: "Per D-08 the new tier engine becomes the primary source of truth for SLA state + due dates; the old severity path is demoted to the NULL-score fallback only (D-03), and Vulnerability.sla_breached becomes a derived mirror, not a second breach signal."
  invariant_test: "one breach yields exactly one breach signal across channels + in-app (reconciled in Plan 03; asserted in test_escalation_engine.py::test_no_double_fire)"

must_haves:
  truths:
    # Success criteria SLA-01 / SLA-02 + specless edge probe (SLA-01 boundaries, SLA-02 adjacency/empty) + UI-SPEC Surface E2
    - "tier_for_score returns 'critical' at score==80, 'high' at score==50, 'moderate' at score==20, and None below 20 (D-12: no SLA, always on_track, no due date, never escalates)"
    - "A finding with elapsed time exactly at the approaching-% boundary (default 80% of the tier window) resolves to 'approaching'; exactly at the due date (100%) resolves to 'breached'"
    - "The approaching window scales per tier automatically (80% of 7d for critical vs 80% of 90d for moderate) — D-02"
    - "A finding with NULL risk_exposure_score resolves via the severity fallback map CRITICAL->critical, HIGH->high, MEDIUM/LOW/INFO->moderate (D-03) and still returns a valid on_track/approaching/breached state"
    - "GET /vulnerabilities (list) and GET /vulnerabilities/{id} (detail) responses each carry sla_state and sla_due_at fields (Pitfall 3 — blocking prerequisite for D-11)"
    - "The SlaPill renders the server-computed on_track/approaching/breached/not_tracked state on the finding row when a state prop is supplied, without re-deriving the tier formula client-side (D-11)"
    - "A finding below the escalation floor / with no SLA data renders the not_tracked faint 'No SLA' pill, never a blank or broken pill (UI-SPEC E2 empty/error)"
    - "Default tier policy critical 7d / high 30d / moderate 90d applies when the tenant has no custom sla_config tier_policy (SLA-01)"
  artifacts:
    - path: "backend/app/vulnerabilities/sla_tier_service.py"
      provides: "tier_for_score, severity_to_tier, compute_sla_state, get_tier_policy, resolve_state_for_vuln, run_sla_tier_pass"
      contains: "def tier_for_score"
    - path: "backend/app/vulnerabilities/schemas.py"
      provides: "sla_state + sla_due_at on VulnerabilityResponse AND VulnerabilitySummary"
      contains: "sla_state"
    - path: "frontend/src/components/tickets/sla-pill.tsx"
      provides: "optional server-truth state prop path"
      contains: "state"
  key_links:
    - from: "backend/app/connectors/scheduler.py"
      to: "sla_tier_service.run_sla_tier_pass"
      via: "SLA block in the 60s tick (scheduler.py:314-328) replaces backfill_sla_due_dates+check_sla_breaches calls"
      pattern: "run_sla_tier_pass"
    - from: "frontend/src/components/vulnerabilities/vuln-table.tsx"
      to: "SlaPill state prop"
      via: "server sla_state replaces the local slaBand() function"
      pattern: "SlaPill"
  backstops:
    # SLA-02 ordering probe — resolved to backstop (finding-row sort contract not explicit for SLA state)
    - statement: "Introducing sla_state does not change existing finding-row ordering; the existing sort=sla_due_at behavior and default ordering are unchanged"
      verification: backstop
  prohibitions:
    - "Do not re-derive tier boundaries — import RISK_SCORE_TIER_CRITICAL/HIGH/MEDIUM from app.assets.risk_score, never hardcode 80/50/20"
    - "Do not re-derive the v4.0 risk_exposure_score — consume the persisted value only"
    - "Do not compute SLA state client-side for findings — server is authoritative (D-01/D-02)"
    - "Do not touch the ticket SlaPill call sites (tickets-table.tsx, kanban-card.tsx, ticket-drill-content.tsx) — the new state prop is optional and additive"
    - "Do not register a new scheduler or external queue — extend the existing 60s asyncio tick in place"
---

<objective>
LEAD TRACER — prove the risk-tier SLA engine end-to-end for one production path: default tier policy -> engine computes sla_due_at + live sla_state off the v4.0 risk_exposure_score -> surfaced on the finding row via the extended SlaPill. This is the thinnest vertical slice that must be verified before any horizontal expansion (escalation channels, MTTR, admin pane, drill panel).

Purpose: De-risk the core tier+elapsed-% state formula and the schema-wiring blocking prerequisite (Pitfall 3) before multiplying them across channels and surfaces.
Output: A working, tested tier engine; SLA fields on both vuln schemas; a scheduler pass writing tier-based due dates; a server-state-aware SlaPill on the finding row.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-remediation-sla-engine-escalation/36-CONTEXT.md
@.planning/phases/36-remediation-sla-engine-escalation/36-PATTERNS.md
@.planning/phases/36-remediation-sla-engine-escalation/36-UI-SPEC.md

<interfaces>
From backend/app/assets/risk_score.py (import — DO NOT redefine):
  RISK_SCORE_TIER_CRITICAL = 80
  RISK_SCORE_TIER_HIGH = 50
  RISK_SCORE_TIER_MEDIUM = 20

From backend/app/vulnerabilities/sla_service.py (KEEP — becomes D-03 fallback path):
  DEFAULT_SLA_DAYS = {"CRITICAL":7,"HIGH":30,"MEDIUM":90,"LOW":180,"INFO":365}
  def get_sla_days(tenant) -> dict[str,int]

From backend/app/vulnerabilities/models.py: Vulnerability has columns
  risk_exposure_score:int|None, severity:str, status:str, first_detected_at:datetime,
  remediated_at:datetime|None, sla_due_at:datetime|None, sla_breached:bool

From backend/app/vulnerabilities/schemas.py:74-98 VulnerabilitySummary + :27-71 VulnerabilityResponse
  both use model_config = {"from_attributes": True}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): Failing unit tests for the tier engine</name>
  <read_first>
    - backend/tests/test_sla_service.py (existing SLA test conventions + fixtures)
    - backend/tests/conftest.py (async db fixtures, tenant/vuln factories)
    - backend/app/assets/risk_score.py:56-61 (tier constants)
    - .planning/phases/36-remediation-sla-engine-escalation/36-RESEARCH.md (Validation Architecture, Code Examples lines 393-427)
  </read_first>
  <behavior>
    - tier_for_score: 80->critical, 79->high, 50->high, 49->moderate, 20->moderate, 19->None, None->None
    - severity_to_tier: CRITICAL->critical, HIGH->high, MEDIUM->moderate, LOW->moderate, INFO->moderate
    - compute_sla_state: on_track before approaching_at; approaching at exactly approaching_at boundary; breached at exactly sla_due_at; approaching window scales per tier (7d vs 90d at 0.8)
    - resolve_state_for_vuln: NULL score uses severity fallback; score<20 -> (None due date, "not_tracked"); scored -> tier-based state
  </behavior>
  <action>
    Create backend/tests/test_sla_tier_service.py mirroring the fixtures + async style of test_sla_service.py. Write failing tests (import-error RED is acceptable) for tier_for_score at the exact boundary scores listed in <behavior>, severity_to_tier for all five severities, compute_sla_state at the exact approaching-% and 100% boundaries for at least critical(7d) and moderate(90d) with approaching_pct=0.8, and resolve_state_for_vuln for the NULL-score fallback and the score<20 not_tracked case. Use frozen `now` datetimes computed from first_detected_at so boundary assertions are exact. Do NOT implement the module yet.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_sla_tier_service.py -q` fails with ModuleNotFoundError or AssertionError (genuine RED), not a collection error unrelated to this module
    - At least one boundary test exists for each of: score==80, score==50, score==20, score==19, approaching-boundary, due-date boundary, NULL-score fallback, score<20 not_tracked
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret pytest tests/test_sla_tier_service.py -q; test $? -ne 0</automated>
  </verify>
  <done>Failing test file committed as the RED baseline for the engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the tier engine + add SLA fields to both schemas</name>
  <read_first>
    - backend/app/vulnerabilities/sla_service.py (analog: module shape, get_sla_days custom-or-default merge, DEFAULT_SLA_DAYS)
    - backend/app/assets/risk_score.py:56-61
    - backend/app/vulnerabilities/schemas.py:27-98 (both models + risk_exposure_score optional-field precedent at :67)
    - .planning/phases/36-remediation-sla-engine-escalation/36-PATTERNS.md (sla_tier_service assignment)
  </read_first>
  <behavior>
    - All Task-1 tests turn GREEN with no test edits
  </behavior>
  <action>
    Create backend/app/vulnerabilities/sla_tier_service.py importing RISK_SCORE_TIER_CRITICAL/HIGH/MEDIUM from app.assets.risk_score. Define: DEFAULT_TIER_POLICY = {"critical":7,"high":30,"moderate":90}; DEFAULT_APPROACHING_PCT = 0.8; tier_for_score(score:int|None)->str|None (per D-12, None below RISK_SCORE_TIER_MEDIUM); severity_to_tier(severity:str)->str (CRITICAL->critical, HIGH->high, else moderate — D-03/Pitfall 5); get_tier_policy(tenant)->dict returning keys tier_days (custom-or-default merge over sla_config["tier_policy"]) and approaching_pct (sla_config.get("approaching_pct", DEFAULT_APPROACHING_PCT)); compute_sla_state(*, first_detected_at, tier_days, approaching_pct, now)->tuple[datetime,str] per RESEARCH lines 416-426; resolve_state_for_vuln(vuln, policy, now)->tuple[datetime|None,str] that (a) uses tier_for_score on risk_exposure_score, (b) falls back to severity_to_tier when the score is NULL, (c) returns (None,"not_tracked") when tier_for_score yields None for a scored finding <20. Add sla_state:str|None=None and sla_due_at:datetime|None=None to BOTH VulnerabilityResponse and VulnerabilitySummary in schemas.py (do not remove any existing field).
  </action>
  <acceptance_criteria>
    - `pytest tests/test_sla_tier_service.py -q` is fully GREEN with no edits to Task-1 tests
    - `grep -v '^#' backend/app/vulnerabilities/sla_tier_service.py | grep -c 'RISK_SCORE_TIER_'` >= 3 (constants imported/used, not re-hardcoded); `grep -c '= 80' backend/app/vulnerabilities/sla_tier_service.py` == 0
    - Both VulnerabilitySummary and VulnerabilityResponse declare sla_state and sla_due_at (`grep -c sla_state backend/app/vulnerabilities/schemas.py` >= 2)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret pytest tests/test_sla_tier_service.py -q</automated>
  </verify>
  <done>Engine GREEN; SLA fields present on both schemas.</done>
</task>

<task type="tracer">
  <name>Task 3: Wire the tracer end-to-end — scheduler write, read-time state, SlaPill on the row</name>
  <read_first>
    - backend/app/connectors/scheduler.py:314-328 (SLA block to replace — Pattern 1 isolation shape)
    - backend/app/vulnerabilities/service.py:143-165 (list_vulnerabilities; sort=sla_due_at usage)
    - backend/app/ticketing/service.py:72-115 (recompute_ticket_sla — Pitfall 2 resync)
    - backend/app/vulnerabilities/router.py:226-263 (existing recompute-all ticket-URL precedent)
    - frontend/src/components/tickets/sla-pill.tsx (full — TIER_CONFIG color map, existing computeTier path)
    - frontend/src/components/vulnerabilities/vuln-table.tsx (local slaBand() to replace)
    - .planning/phases/36-remediation-sla-engine-escalation/36-UI-SPEC.md (## Color state->tone table)
  </read_first>
  <action>
    Backend: add run_sla_tier_pass(db, tenant) to sla_tier_service.py that, for OPEN/IN_PROGRESS vulns, computes tier-based sla_due_at (via resolve_state_for_vuln + policy) and writes sla_due_at plus the sla_breached derived mirror (D-08), then db.flush() and calls recompute_ticket_sla for every affected external_ticket_url (Pitfall 2, recompute-all to start per Open Question #5). In scheduler.py:314-328 replace the `backfill_sla_due_dates(...)` + `check_sla_breaches(...)` calls with `await run_sla_tier_pass(db, t)`, preserving the exact own-session / own-try-except / own-commit isolation shape. In service.py list_vulnerabilities and the detail path, after loading the tenant policy once, attach the read-time sla_state (via resolve_state_for_vuln with now=datetime.now(UTC)) and sla_due_at onto each returned object so the schema fields populate. Frontend: extend SlaPill with an optional `state?: 'on_track'|'approaching'|'breached'|'not_tracked'` prop — when present, map directly to the existing TIER_CONFIG tones (breached->overdue/red, approaching->soon/amber, on_track->ok/green, not_tracked->unknown/faint 'No SLA') and skip computeTier(); when absent, keep the existing dueAt path unchanged. In vuln-table.tsx replace the local slaBand(row.sla_due_at) rendering with `<SlaPill state={row.sla_state ?? undefined} dueAt={...} />`. Add a frontend test in sla-pill.test.tsx asserting each of the four states renders its expected label/tone via the state prop.
  </action>
  <acceptance_criteria>
    - `grep -c run_sla_tier_pass backend/app/connectors/scheduler.py` >= 1 and the old block no longer calls check_sla_breaches (`grep -c 'check_sla_breaches' backend/app/connectors/scheduler.py` == 0)
    - A live/integration assertion: GET /api/v1/vulnerabilities response objects include a `sla_state` key (test extends the existing vuln router test file)
    - `npx vitest run src/components/tickets/sla-pill.test.tsx` passes including the new 4-state prop cases
    - vuln-table.tsx no longer defines/uses a local slaBand for the SLA column (`grep -c slaBand frontend/src/components/vulnerabilities/vuln-table.tsx` == 0)
    - Ticket SlaPill call sites unchanged (`git diff --name-only` does NOT include tickets-table.tsx / kanban-card.tsx / ticket-drill-content.tsx)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret pytest tests/test_sla_tier_service.py -q && cd ../frontend && npx vitest run src/components/tickets/sla-pill.test.tsx</automated>
  </verify>
  <done>Tracer proven: default policy -> engine -> sla_due_at + sla_state on the API -> SlaPill on the finding row. Ready for horizontal expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| scheduler tick -> DB | server-computed sla_due_at/sla_breached writes; no external input |
| API -> browser | sla_state/sla_due_at are server-computed, non-sensitive; client never re-derives the formula |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | client-side SLA state | mitigate | State computed server-side and returned as a string; SlaPill renders it directly, never recomputes tier client-side (Anti-Pattern, RESEARCH:298) |
| T-36-02 | Information Disclosure | cross-tenant sla_due_at write | mitigate | run_sla_tier_pass and list/detail queries filter by tenant_id (project-wide V4 convention) |
| T-36-03 | Denial of Service | recompute-all ticket resync each 60s tick | accept | Start-simple recompute-all matches existing admin-endpoint precedent; optimize only if tick duration is a measured problem (Open Question #5) |
</threat_model>

<verification>
- Engine unit tests green (boundaries, fallback, not_tracked)
- List + detail responses carry sla_state/sla_due_at
- SlaPill 4-state prop path green; ticket call sites untouched
</verification>

<success_criteria>
SLA-01 (default policy applied, tier-keyed) and SLA-02 (live state on the row) demonstrated end-to-end for the default-policy path. Editability (admin pane) is delivered in Plan 05/06; drill panel in Plan 06.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)
- NEW module `backend/app/vulnerabilities/sla_tier_service.py`: `DEFAULT_TIER_POLICY`, `DEFAULT_APPROACHING_PCT`, `tier_for_score()`, `severity_to_tier()`, `get_tier_policy()`, `compute_sla_state()`, `resolve_state_for_vuln()`, `run_sla_tier_pass()`
- NEW config keys read from `Tenant.sla_config`: `tier_policy` (per-tier days), `approaching_pct`
- MODIFIED schemas: `sla_state: str | None`, `sla_due_at: datetime | None` added to `VulnerabilityResponse` AND `VulnerabilitySummary`
- MODIFIED `scheduler.py` SLA tick block -> calls `run_sla_tier_pass`
- MODIFIED `vulnerabilities/service.py` list/detail -> attaches read-time `sla_state`
- EXTENDED frontend `SlaPill` with optional server `state` prop; `vuln-table.tsx` consumes it
- NEW test `backend/tests/test_sla_tier_service.py`
</artifacts_produced>

<output>
After completion, create `.planning/phases/36-remediation-sla-engine-escalation/36-01-SUMMARY.md`
</output>
