---
phase: 32-asset-exposure-context
plan: 04
type: execute
wave: 4
depends_on: [32-02, 32-03]
files_modified:
  - backend/alembic/versions/041_add_inet_facing_signal.py
  - backend/app/assets/models.py
  - backend/app/assets/exposure.py
  - backend/app/connectors/base.py
  - backend/app/connectors/sync.py
  - backend/app/connectors/wiz.py
  - backend/app/connectors/qualys.py
  - backend/app/connectors/nessus.py
  - backend/app/connectors/rapid7.py
  - backend/app/connectors/defender.py
  - backend/app/connectors/crowdstrike.py
  - backend/tests/test_asset_exposure.py
  - backend/tests/test_connector_internet_facing.py
autonomous: true
requirements: [EXPO-02]
must_haves:
  truths:
    - "internet_facing detection uses a REAL per-connector vendor signal where the payload supports it (captured on a durable internet_facing_detected column via NormalizedVulnerability), and falls back to external_ip IS NOT NULL OR the 'internet-facing' tag only where the vendor provides no signal (EXPO-02, CONTEXT [USER] real-detection decision)"
    - "When a connector supplies a real internet_facing signal, it wins over the external_ip/tag proxy; when the signal is absent (None), the proxy is used — provable in one test each"
    - "Per-connector coverage (which of Wiz/Qualys/Nessus/Rapid7/Defender/CrowdStrike yield a real signal vs the external_ip/tag fallback) is documented honestly in code, not guessed"
    - "An admin internet_facing override still permanently wins over any real vendor signal (source=ASSET_OVERRIDE never overwritten)"
  artifacts:
    - path: "backend/app/connectors/base.py"
      provides: "NormalizedVulnerability.internet_facing: bool | None field (None = vendor gave no signal)"
      contains: "internet_facing"
    - path: "backend/app/assets/models.py"
      provides: "Asset.internet_facing_detected column (durable raw vendor signal, nullable)"
      contains: "internet_facing_detected"
    - path: "backend/app/assets/exposure.py"
      provides: "infer_exposure_context prefers detected signal over the external_ip/tag proxy"
      contains: "internet_facing"
    - path: "backend/alembic/versions/041_add_inet_facing_signal.py"
      provides: "internet_facing_detected column on assets"
      contains: "op.add_column"
    - path: "backend/tests/test_connector_internet_facing.py"
      provides: "per-connector real-signal extraction + coverage documentation tests"
      contains: "internet_facing"
  key_links:
    - from: "backend/app/connectors/{wiz,...}.py parsers"
      to: "NormalizedVulnerability.internet_facing"
      via: "raw vendor payload field read (where present)"
      pattern: "internet_facing"
    - from: "backend/app/connectors/sync.py::_upsert_asset"
      to: "Asset.internet_facing_detected"
      via: "getattr passthrough (raw provenance, always captured)"
      pattern: "internet_facing_detected"
    - from: "backend/app/assets/exposure.py::infer_exposure_context"
      to: "detected signal vs proxy"
      via: "detected if not None else (external_ip or tag)"
      pattern: "internet_facing_detected|external_ip"
---

<objective>
Upgrade internet_facing from Plan 02's honest v1 proxy (external_ip/tag) to REAL per-connector
detection wherever the vendor payload actually supports it, per CONTEXT's [USER] decision. This is
exploratory: no connector currently maps a public-exposure/internet-facing field (verified in
32-RESEARCH Pitfall 1 + 32-PATTERNS "No Analog Found") — the only existing end-to-end
network-exposure-adjacent signal is CrowdStrike external_ip. The executor must inspect each
vendor's actual payload, wire a real signal where one genuinely exists, fall back to the proxy
where it does not, and document per-connector coverage honestly (no guessed field names).

The real signal is captured on a durable Asset.internet_facing_detected column (raw provenance,
like external_ip) so inference is deterministic and the AUTO-gate + admin override still hold.
Output: 1 migration, the NormalizedVulnerability.internet_facing field, the detected column,
sync passthrough, inference precedence, per-connector detection + an honest coverage doc, tests.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-asset-exposure-context/32-CONTEXT.md
@.planning/phases/32-asset-exposure-context/32-RESEARCH.md
@.planning/phases/32-asset-exposure-context/32-PATTERNS.md
@.planning/phases/32-asset-exposure-context/32-02-SUMMARY.md

<interfaces>
<!-- Extends the internet_facing v1 proxy from Plan 02. -->

base.py NormalizedVulnerability (append optional field, mirror external_ip at base.py:36):
```python
internet_facing: bool | None = None   # None = vendor emitted no usable signal (use proxy)
```

Asset column (models.py — nullable, raw vendor provenance, mirror external_ip:56):
```python
internet_facing_detected: Mapped[bool | None] = mapped_column(Boolean)   # None until a vendor signal arrives
```

exposure.py precedence (extend infer_exposure_context signature — add one kwarg):
```python
def infer_exposure_context(*, tags, department, job_title, external_ip,
                           internet_facing_detected: bool | None = None) -> tuple[str, bool, bool]:
    # internet_facing = internet_facing_detected if internet_facing_detected is not None
    #                   else (("internet-facing" in (tags or [])) or external_ip is not None)
# apply_inference_to_asset passes asset.internet_facing_detected into infer_exposure_context.
```

sync.py passthrough (mirror external_ip at sync.py:248 create + :279 update — always capture raw provenance):
```python
internet_facing_detected=getattr(v, "internet_facing", None)   # create branch
if getattr(v, "internet_facing", None) is not None: asset.internet_facing_detected = v.internet_facing  # update
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — detected-signal-beats-proxy + proxy-fallback + override-still-wins tests</name>
  <read_first>
    - backend/tests/test_asset_exposure.py (Plan 02 internet_facing proxy tests to extend)
    - backend/tests/test_connector_normalization.py (per-connector pure-transform assert style)
    - backend/app/connectors/base.py:9-44 (NormalizedVulnerability dataclass)
  </read_first>
  <behavior>
    - infer_exposure_context(..., external_ip=None, tags=None, internet_facing_detected=True) -> internet_facing True (detected wins with no proxy).
    - infer_exposure_context(..., external_ip="1.2.3.4", tags=None, internet_facing_detected=False) -> internet_facing False (detected False wins over the external_ip proxy).
    - infer_exposure_context(..., external_ip="1.2.3.4", internet_facing_detected=None) -> True (proxy used when no signal).
    - Integration: an asset with an ASSET_OVERRIDE internet_facing=False stays False even after a connector supplies internet_facing_detected=True (override permanent).
  </behavior>
  <action>
    Add the RED tests above to test_asset_exposure.py and create test_connector_internet_facing.py with a placeholder per-connector test (filled in Task 3). Must fail for real (internet_facing_detected kwarg/column/field absent). Env prefix per MEMORY.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret .venv/bin/python -m pytest tests/test_asset_exposure.py -k "detected or proxy" -x 2>&1 | grep -Eiq "error|failed|no attribute|cannot import" && echo RED-OK</automated>
  </verify>
  <done>Detected-vs-proxy precedence tests exist and fail for real before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN part 1 — schema spine: migration 041, detected column, dataclass field, sync passthrough, inference precedence</name>
  <read_first>
    - backend/alembic/versions/040_add_group_exposure_ovr.py (Plan 03 — down_revision chaining; 041 down_revision="040_add_group_exposure_ovr")
    - backend/app/connectors/sync.py:248,279 (external_ip passthrough in create + update branches — mirror exactly)
    - backend/app/assets/exposure.py (Plan 02 infer_exposure_context internet_facing proxy line + apply_inference_to_asset)
    - backend/app/assets/models.py:56 (external_ip column style)
  </read_first>
  <behavior>
    - Task 1's precedence tests go GREEN using a synthetic NormalizedVulnerability.internet_facing value (no real connector wiring yet).
    - `alembic heads` single head 041_add_inet_facing_signal; chain 040 → 041.
  </behavior>
  <action>
    Create 041_add_inet_facing_signal.py (revision "041_add_inet_facing_signal" [26 chars ≤32], down_revision "040_add_group_exposure_ovr"): add_column internet_facing_detected Boolean nullable on assets; symmetric downgrade. Add the column to the Asset model. Append internet_facing to NormalizedVulnerability in base.py. Add the sync.py passthrough in BOTH branches per the interfaces block (always capture raw provenance, like external_ip). Extend infer_exposure_context with the internet_facing_detected kwarg + precedence (detected wins, else proxy) and make apply_inference_to_asset pass asset.internet_facing_detected. Confirm the AUTO-gate is unchanged so an ASSET_OVERRIDE internet_facing is never overwritten.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret .venv/bin/python -m pytest tests/test_asset_exposure.py -k "detected or proxy" -x</automated>
  </verify>
  <done>The detected-signal spine is landed (column + dataclass field + sync passthrough + inference precedence); precedence tests green; migration 041 chains 040→041.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN part 2 — per-connector real detection + honest coverage documentation</name>
  <read_first>
    - backend/app/connectors/crowdstrike.py:319,438 (external_ip extraction — the ONLY existing network-exposure-adjacent signal; the process analog for a new raw-field read)
    - backend/app/connectors/wiz.py:19-75 (VULNERABILITY_QUERY GraphQL + WizGraphQLSchemaError fallback pattern — inspect whether a public-exposure field is genuinely available before mapping it)
    - backend/app/connectors/{qualys,nessus,rapid7,defender}.py normalize functions (inspect each raw payload for a real internet-facing/public-exposure/security-group signal; do NOT invent field names)
    - 32-PATTERNS.md §"Real per-connector internet-facing detection" (three-hop wiring shape) + §"No Analog Found"
    - 32-CONTEXT.md ("document per-connector coverage honestly")
  </read_first>
  <behavior>
    - For each connector that genuinely exposes a usable signal, its normalize sets NormalizedVulnerability.internet_facing to a real bool; a per-connector test asserts extraction from a representative payload.
    - For each connector with no usable signal, internet_facing stays None (proxy applies downstream) and this is documented, not faked.
  </behavior>
  <action>
    Inspect each of the 6 connectors' actual payload/response shape. Where a real internet-facing / public-exposure / public-IP / security-group signal genuinely exists (e.g. Wiz cloud public exposure if the GraphQL schema supports it; CrowdStrike may derive from external_ip presence), map it to NormalizedVulnerability.internet_facing via the three-hop shape in 32-PATTERNS. Where none exists, leave internet_facing=None and record it. Add an honest per-connector coverage table as a module docstring (in exposure.py or a shared connectors location) listing each connector → REAL SIGNAL (field name) or FALLBACK (external_ip/tag proxy). Fill test_connector_internet_facing.py with one extraction test per connector that has a real signal, and one assertion that a fallback connector yields None. Do NOT guess a vendor field name that inspection does not confirm — a connector that legitimately has no signal is a documented FALLBACK, not a failure.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret .venv/bin/python -m pytest tests/test_connector_internet_facing.py tests/test_asset_exposure.py -x</automated>
  </verify>
  <done>Real per-connector internet_facing detection wired where the vendor supports it, proxy fallback elsewhere, per-connector coverage documented honestly in code, tests green; regression on connector normalization still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendor payload → NormalizedVulnerability.internet_facing → Asset.internet_facing_detected | untrusted scanner-returned exposure signal crosses into a persisted column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-11 | Tampering | connector-supplied internet_facing signal | mitigate | Signal coerced to bool|None at the parser; only the materialized internet_facing column feeds the risk model, and it is AUTO-gated so an admin override always wins — a spoofed vendor signal cannot override an admin decision (EXPO-03). |
| T-32-12 | Spoofing (data quality) | guessed vendor field names | accept | Mitigated by process: no field is mapped without payload inspection; unconfirmed connectors documented as FALLBACK, not faked — prevents silent under/over-detection. |
</threat_model>

<verification>
- `... .venv/bin/python -m pytest tests/test_connector_internet_facing.py tests/test_asset_exposure.py -x` green.
- Regression: `... .venv/bin/python -m pytest tests/test_connector_normalization.py -x` green.
- `cd backend && alembic upgrade head` applies 041; `alembic heads` single head 041_add_inet_facing_signal.
</verification>

<success_criteria>
- EXPO-02: internet_facing uses real per-connector detection where the vendor payload supports it, external_ip/tag proxy elsewhere; detected signal beats proxy; admin override still permanent; per-connector coverage documented honestly.
</success_criteria>

<output>
After completion, create `.planning/phases/32-asset-exposure-context/32-04-SUMMARY.md`.
Record: the honest per-connector coverage table (connector → real field name or FALLBACK),
the detected-vs-proxy precedence rule, and any vendor whose schema was inspected and found to
have no usable signal.
</output>
