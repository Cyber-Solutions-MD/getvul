---
phase: 32-asset-exposure-context
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - backend/app/assets/exposure.py
  - backend/app/assets/router.py
  - backend/app/tenants/models.py
  - backend/alembic/versions/038_add_exposure_cal_cfg.py
  - backend/tests/test_asset_exposure.py
autonomous: true
requirements: [EXPO-01, EXPO-02, EXPO-06]
must_haves:
  truths:
    - "data_sensitivity is auto-inferred at upsert from real signals (dept/job_title/tags e.g. pci/finance→CONFIDENTIAL/RESTRICTED) and internet_facing is seeded from the 'internet-facing' tag OR external_ip IS NOT NULL — both AUTO-gated and never mutating tags (EXPO-01/EXPO-02)"
    - "check_criticality_calibration measures ONLY AUTO-sourced highest-tier (CRITICAL) proportion; admin/group overrides are exempt (EXPO-06, CONTEXT calibration decision)"
    - "Against a realistic 100-asset fixture with a skewed dept/tag distribution, the calibration report returns pct, cap, over_cap and a per-tenant configurable threshold (default 15%); hard-cap enforcement is available but OFF by default (flag+report only)"
    - "GET /assets/exposure-context/calibration (admin) returns the report; a non-admin gets 403"
    - "Admin-set CRITICAL overrides do NOT count toward the calibration proportion (test proves manual overrides are exempt)"
  artifacts:
    - path: "backend/app/assets/exposure.py"
      provides: "real data_sensitivity + internet_facing inference; check_criticality_calibration()"
      contains: "def check_criticality_calibration"
    - path: "backend/app/assets/router.py"
      provides: "GET /assets/exposure-context/calibration admin endpoint"
      contains: "calibration"
    - path: "backend/alembic/versions/038_add_exposure_cal_cfg.py"
      provides: "per-tenant calibration cap + hard-cap-enabled columns on tenants (default 0.15, false)"
      contains: "op.add_column"
    - path: "backend/tests/test_asset_exposure.py"
      provides: "all-3-field inference + calibration-against-fixture + manual-exempt tests"
      contains: "def test_calibration_check_against_realistic_fixture"
  key_links:
    - from: "backend/app/assets/exposure.py::check_criticality_calibration"
      to: "assets table aggregate"
      via: "count(business_criticality==CRITICAL AND source==AUTO) / count(*)"
      pattern: "business_criticality_source"
    - from: "backend/app/assets/router.py GET calibration"
      to: "check_criticality_calibration"
      via: "admin endpoint"
      pattern: "require_role\\(\"admin\"\\)"
---

<objective>
EXPANSION: complete the inference triad and land the EXPO-06 calibration check. Plan 01 landed
the schema (all 6 columns) + spine but left data_sensitivity/internet_facing returning defaults.
This plan (a) fills in real data_sensitivity + internet_facing (tag/external_ip proxy) inference,
still AUTO-gated and never mutating tags, and (b) builds check_criticality_calibration + its
admin report endpoint + per-tenant configurable cap, provable against a realistic seed fixture.

Internet-facing here is the honest v1 proxy (external_ip IS NOT NULL OR "internet-facing" tag);
Plan 04 upgrades it to real per-connector detection where the vendor payload supports it.
Calibration default is flag+report ONLY (hard-cap configurable but off) per CONTEXT — silently
downranking a genuinely critical asset is worse than flagging.
Output: real triad inference, calibration function + endpoint, per-tenant cap config, expanded tests.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-asset-exposure-context/32-CONTEXT.md
@.planning/phases/32-asset-exposure-context/32-RESEARCH.md
@.planning/phases/32-asset-exposure-context/32-PATTERNS.md
@.planning/phases/32-asset-exposure-context/32-01-SUMMARY.md

<interfaces>
<!-- Built on Plan 01's landed contracts (infer_exposure_context / apply_inference_to_asset / EXPOSURE_FIELDS). -->

Extend exposure.py (do NOT change the signatures Plan 01 landed):
```python
def infer_exposure_context(*, tags, department, job_title, external_ip) -> tuple[str, bool, bool]:
    # Plan 02: replace the `# PLAN 02:` default-returns for data_sensitivity + internet_facing.
    # data_sensitivity: pci/pii/restricted/finance/legal signals -> RESTRICTED/CONFIDENTIAL;
    #   internal-only -> INTERNAL; public/marketing/www -> PUBLIC (Claude's Discretion tiers, documented).
    # internet_facing: ("internet-facing" in (tags or [])) OR (external_ip is not None)  [v1 proxy]

async def check_criticality_calibration(db, tenant_id) -> dict:
    """Returns {'pct': float, 'cap': float, 'over_cap': bool, 'critical_auto': int,
    'total': int, 'hard_cap_enabled': bool}. Numerator counts ONLY
    business_criticality=='CRITICAL' AND business_criticality_source=='AUTO'
    (admin/group overrides EXEMPT). cap + hard_cap_enabled read per-tenant from the
    columns added in migration 038 (default 0.15 / false)."""
```

Per-tenant config columns (backend/app/tenants/models.py — Tenant model; mirror existing tenant-config column style):
```python
exposure_criticality_cap: Mapped[float] = mapped_column(Float, default=0.15, server_default="0.15")
exposure_hard_cap_enabled: Mapped[bool] = mapped_column(Boolean, default=False, server_default="false")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED + GREEN — real data_sensitivity + internet_facing inference</name>
  <read_first>
    - backend/app/assets/exposure.py (Plan 01's infer_exposure_context — the `# PLAN 02:` markers on the two default-returns)
    - backend/tests/test_asset_exposure.py (Plan 01 tests — extend, keep the inline-seed convention)
    - 32-CONTEXT.md (internet-facing v1 fallback rule; seed-from-never-overwrite tags rule)
  </read_first>
  <behavior>
    - infer_exposure_context(tags=["pci"], department="Finance", ...) -> data_sensitivity in {CONFIDENTIAL, RESTRICTED}; tags=["public"]/marketing -> PUBLIC; no signal -> INTERNAL.
    - internet_facing True when "internet-facing" in tags OR external_ip is not None; False otherwise.
    - apply_inference_to_asset now updates data_sensitivity/internet_facing on AUTO assets; still skips ASSET_OVERRIDE fields; still never mutates tags.
  </behavior>
  <action>
    Add RED tests (test_infer_all_three_fields, test_internet_facing_from_tag_or_external_ip, test_reinference_skips_all_overridden_fields) then replace the two `# PLAN 02:` default-returns in infer_exposure_context with real ordered-priority logic per the interfaces block. Document the data_sensitivity tier mapping in the module docstring. Confirm apply_inference_to_asset (unchanged from Plan 01) now propagates the two newly-real fields because EXPOSURE_FIELDS already includes them.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret .venv/bin/python -m pytest tests/test_asset_exposure.py -k "infer or internet_facing or overridden" -x</automated>
  </verify>
  <done>All three exposure fields auto-infer from real signals; internet_facing uses the external_ip/tag v1 proxy; overrides on any field survive re-inference; tags never mutated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED + GREEN — calibration check + per-tenant cap config + admin report endpoint</name>
  <read_first>
    - backend/app/tenants/models.py (Tenant model — existing per-tenant config column style to mirror for the 2 new columns)
    - backend/alembic/versions/037_add_exposure_context.py (Plan 01 — down_revision chaining + add_column shape; 038 down_revision="037_add_exposure_context")
    - backend/app/assets/router.py (asset_stats risk_distribution aggregate ~:256-263 for the count-aggregate shape; recompute-risk-scores :536-546 for the admin endpoint shape)
    - 32-CONTEXT.md calibration decision (AUTO-only, exempt overrides, flag+report default, hard-cap off, 15% default, tenant-configurable)
  </read_first>
  <behavior>
    - check_criticality_calibration over a 100-asset fixture with a controlled AUTO-CRITICAL count returns pct = critical_auto/total and over_cap = pct > cap.
    - An asset set to CRITICAL via ASSET_OVERRIDE (source != AUTO) is NOT counted in critical_auto (exempt).
    - GET /assets/exposure-context/calibration returns the report for admin; analyst/viewer get 403.
    - Default flag+report: over_cap true never mutates any asset's criticality (hard_cap_enabled false).
  </behavior>
  <action>
    Create migration 038_add_exposure_cal_cfg.py (revision "038_add_exposure_cal_cfg" [24 chars ≤32], down_revision "037_add_exposure_context"): add_column exposure_criticality_cap Float server_default "0.15" and exposure_hard_cap_enabled Boolean server_default "false" on tenants; symmetric downgrade. Add the 2 columns to the Tenant model.
    Implement check_criticality_calibration in exposure.py using a single aggregate query (mirror asset_stats): total = count(assets in tenant), critical_auto = count where business_criticality=='CRITICAL' AND business_criticality_source=='AUTO'; read cap + hard_cap_enabled from the tenant row; return the dict from the interfaces block. If hard_cap_enabled is True, expose a documented (but default-off) enforcement path — for this plan just include hard_cap_enabled in the return and a `# hard-cap enforcement (off by default)` stub; do NOT downrank on the flag path.
    Add GET /assets/exposure-context/calibration (require_role("admin")) returning the report. Add a RED-then-GREEN test_calibration_check_against_realistic_fixture (inline-construct ~100 assets with a skewed dept/tag distribution, per 32-RESEARCH §5 seed-fixture guidance — build in the test file, NOT app/seed.py) and test_calibration_exempts_manual_overrides.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret .venv/bin/python -m pytest tests/test_asset_exposure.py -k "calibration" -x</automated>
  </verify>
  <done>Calibration measures AUTO-only CRITICAL proportion against a per-tenant configurable cap (default 15%), exempts manual overrides, defaults to flag+report (no downranking), and is admin-gated; migration 038 chains 037→038.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyst/viewer session → GET calibration | non-admin attempts to read the tenant calibration report |
| tenant-A → tenant-B calibration | aggregate must be scoped to the caller's tenant only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | Information Disclosure / EoP | GET /assets/exposure-context/calibration | mitigate | require_role("admin"); aggregate WHERE tenant_id == user.tenant_id; test asserts 403 for non-admin (V4). |
| T-32-06 | Tampering | hard-cap enforcement path | accept | Hard-cap defaults OFF (flag+report only) per CONTEXT; enforcement would downrank AUTO assets — deliberately opt-in, documented, exempts overrides so it can never contradict EXPO-03. |
</threat_model>

<verification>
- `... .venv/bin/python -m pytest tests/test_asset_exposure.py -x` fully green (Plan 01 + Plan 02 tests).
- `cd backend && alembic upgrade head` applies 038; `alembic heads` single head 038_add_exposure_cal_cfg.
</verification>

<success_criteria>
- EXPO-01/02: all three exposure fields auto-infer from real signals at upsert, AUTO-gated, tags never mutated.
- EXPO-06: calibration measures AUTO-only highest-tier proportion against a tenant-configurable cap (default 15%), exempts overrides, provable against a realistic fixture, flag+report by default.
</success_criteria>

<output>
After completion, create `.planning/phases/32-asset-exposure-context/32-02-SUMMARY.md`.
Record: the data_sensitivity tier mapping, the internet_facing v1 proxy rule (Plan 04 upgrade point),
and the exact calibration numerator/denominator SQL.
</output>
