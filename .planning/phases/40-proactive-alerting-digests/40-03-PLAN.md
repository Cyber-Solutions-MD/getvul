---
phase: 40-proactive-alerting-digests
plan: 03
type: execute
wave: 3
depends_on: [40-01, 40-02]
autonomous: true
requirements: [ALERT-02]
files_modified:
  - backend/app/email.py
  - backend/app/notifications/digests.py
  - backend/app/connectors/scheduler.py
  - backend/tests/test_digests.py

must_haves:
  truths:
    - "A digest is sent only when the tenant is past its configured send-hour (in Tenant.timezone) AND has not been sent this period (D-12); it never fires at process-start time"
    - "The last-sent marker is persisted on Tenant.alerting_last_digest_sent_at so a scheduler restart does not re-send the same period's digest (Pitfall 4)"
    - "Digest sections are assembled in fixed order due + breaching + newly-critical + expiring-exceptions (D-13); due/breaching read resolve_state_for_vuln, expiring-exceptions read ExceptionRecord.expires_at"
    - "Every section query excludes status IN (SUPPRESSED, FALSE_POSITIVE) and ~active_exception_subquery (D-20)"
    - "When all sections are empty for a recipient, nothing is sent (D-14 — no all-clear digest)"
    - "Each section caps at top-N by risk (RISK_SCORE_TIER bands) with an 'and N more' overflow line + a dashboard deep-link (D-15)"
    - "Owner digests deliver via email; team (AssetGroup) digests deliver to the group's shared Slack/Teams channel via dispatch_channel (D-08/D-09)"
    - "send_email attaches a multipart/alternative body when html_body is provided, keeping the existing plain part and the {'ok': bool} contract (never raises)"
    - "empty/single/many inputs resolve: zero recipients → no sends; a single owned+in-team asset appears in BOTH the owner email digest AND the team channel digest (overlap by design, D-21); one finding vs many render identically (row format is singular-safe)"
    - "ordering is stable: section order is fixed and within-section rows are ordered by risk tier deterministically"
    - { statement: "Digest email finding rows truncate a hostname longer than ~40 chars with an ellipsis rather than breaking the table layout, verified in Gmail web + Apple Mail", verification: backstop }
  prohibitions:
    - "Must NOT copy reports.py::_is_due or the _last_ticket_sync elapsed-hours gate for the send-hour check (D-12 needs a wall-clock, business-hour gate)"
    - "Must NOT use an in-memory-only last-sent marker (restarts double-send — Pitfall 4)"
    - "Must NOT emit unescaped finding-derived strings (CVE names, hostnames) into the HTML body (XSS in email clients)"
    - "Must NOT include excepted/suppressed findings in any section (D-20)"
    - "Must NOT read due dates from the retired _check_sla_breaches or sla_service.py — use resolve_state_for_vuln"
    - "Must NOT send an all-clear/empty digest (D-14)"
  artifacts:
    - path: "backend/app/notifications/digests.py"
      provides: "run_digests, _send_hour_due, _assemble_sections, _render_digest_html"
      contains: "def run_digests"
    - path: "backend/app/email.py"
      provides: "send_email html_body multipart/alternative support"
      contains: "html_body"
  key_links:
    - from: "backend/app/connectors/scheduler.py::_scheduler_loop"
      to: "run_digests"
      via: "new isolated try/except block per tick"
      pattern: "run_digests"
    - from: "run_digests"
      to: "resolve_state_for_vuln + active_exception_subquery + ExceptionRecord.expires_at"
      via: "per-section content readers"
      pattern: "resolve_state_for_vuln"
    - from: "run_digests"
      to: "Tenant.alerting_last_digest_sent_at"
      via: "persisted send-then-stamp"
      pattern: "alerting_last_digest_sent_at"
---

<objective>
Build ALERT-02: scheduled per-owner (email) and per-team (shared channel) digests assembled from Phase 36 SLA state + Phase 39 exception expiry, gated by a NEW wall-clock send-hour check, rendered as escaped HTML, empty-suppressed, and dispatched on the existing in-process scheduler (no new infra).

Purpose: Turns the point-in-time exposure state into a predictable morning push. Expansion wave — runs only after the ALERT-01 tracer is verified.
Output: `email.py` html_body support, `notifications/digests.py`, a scheduler digest-dispatch block, and a green `test_digests.py`.
</objective>

<artifacts_produced>
- `backend/app/email.py::send_email(..., html_body: str | None = None)` — multipart/alternative
- `backend/app/notifications/digests.py`: `run_digests(db)`, `_send_hour_due(tenant, now)` (wall-clock gate), `_assemble_sections(db, tenant, recipient_scope)`, `_render_digest_html(sections, ...)`, per-owner + per-team dispatch loops
- new isolated digest-dispatch block inside `backend/app/connectors/scheduler.py::_scheduler_loop`
- consumes `Tenant.alerting_last_digest_sent_at` (persisted send marker from migration 051)
</artifacts_produced>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-proactive-alerting-digests/40-CONTEXT.md
@.planning/phases/40-proactive-alerting-digests/40-RESEARCH.md
@.planning/phases/40-proactive-alerting-digests/40-PATTERNS.md
@.planning/phases/40-proactive-alerting-digests/40-UI-SPEC.md
@.planning/phases/40-proactive-alerting-digests/40-02-SUMMARY.md

<interfaces>
send_email today (email.py:48-52): builds MIMEMultipart(), attaches MIMEText(body, "plain") only; returns {"ok": True} / {"ok": False, "error": ...}; NEVER raises. Add optional html_body → MIMEMultipart("alternative") with both plain and html parts; preserve the return contract and the attachment path.
Dispatch-loop analog — reports.py:124-152 run_due_reports (select enabled rows → per-row due-gate → send-then-stamp → commit-if-any-sent). Do NOT copy _is_due (reports.py:155-166).
Send-hour gate (NO analog — build new): convert now into Tenant.timezone (models.py:37, default "UTC"); due when local_now.hour >= send_hour AND alerting_last_digest_sent_at is not within the current period (day for daily, ISO-week for weekly).
Section readers: sla_tier_service.py:144 resolve_state_for_vuln(vuln, policy, now, excepted_seconds=...) → (sla_due_at, "on_track"|"approaching"|"breached"); exceptions/models.py ExceptionRecord.expires_at; exclusion exceptions/service.py:50-91 active_exception_subquery.
Top-N ordering: assets/risk_score.py:56-61 RISK_SCORE_TIER_* bands.
Team dispatch: sla_tier_service.py:328 _build_channel_config + escalation_channels.py:272 dispatch_channel (never raises).
Config: notifications/alerting_config.py merged_alerting_config(tenant) → cadence, send_hour, per_owner_digests, per_team_digests, routing.
Scheduler block placement — scheduler.py: isolated `try: async with async_session_factory() as db: ... except Exception as e: logger.error(...)` blocks; tick = 60s; run_alert_checks gated `_loop_count % 5 == 0`.
UI-SPEC digest email copy: subject "Your GetVul digest — {N} findings need attention"; section headers "Breaching SLA" → "Due soon" → "Newly critical" → "Exceptions expiring soon"; row "{glyph} {CVE-ID} on {hostname} — {sla-delta/badge}"; overflow "and {N} more →"; light-background HTML (hex literals, no CSS vars).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: send_email html_body multipart/alternative</name>
  <read_first>
    - backend/app/email.py:17-93 (send_email — extend in place)
    - backend/tests/test_digests.py (the html test to turn green)
  </read_first>
  <behavior>
    - test_html_body_renders_sections: send_email with html_body attaches both a plain and an html MIMEText part under multipart/alternative; without html_body, behavior is unchanged (single plain part)
    - the {"ok": bool, "error": ...} contract holds; send_email never raises
  </behavior>
  <action>
    Add `html_body: str | None = None` to send_email. When html_body is provided, build `MIMEMultipart("alternative")` and attach `MIMEText(body, "plain")` THEN `MIMEText(html_body, "html")` (order matters — clients pick the last supported part). When html_body is None, keep the current single-plain-part path. Preserve the attachment branch and all exception handling / return values verbatim.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$ENCRYPTION_KEY JWT_SECRET_KEY=$JWT_SECRET_KEY python -m pytest tests/test_digests.py -k html -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "html_body" backend/app/email.py` >= 2
    - `email.py` contains `MIMEMultipart("alternative")`
    - test_html_body_renders_sections passes; existing email/report tests still pass
  </acceptance_criteria>
  <done>send_email transports HTML digests without breaking the plain-text path or its non-raising contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: digests.py — send-hour gate, section assembly, escaped HTML, per-owner/per-team routing, empty suppression</name>
  <read_first>
    - backend/app/reports.py:124-166 (run_due_reports loop shape; _is_due is the ANTI-pattern for the gate)
    - backend/app/vulnerabilities/sla_tier_service.py:144 (resolve_state_for_vuln) + 328 (_build_channel_config)
    - backend/app/exceptions/service.py:50-91 (active_exception_subquery) + exceptions/models.py (ExceptionRecord.expires_at)
    - backend/app/assets/risk_score.py:56-61 (RISK_SCORE_TIER bands)
    - backend/app/assets/directory.py (get_directory_user for per-owner grouping)
    - backend/app/notifications/alerting_config.py (merged_alerting_config)
    - .planning/phases/40-proactive-alerting-digests/40-UI-SPEC.md (Digest email copy + color/typography tables)
  </read_first>
  <behavior>
    - test_send_hour_gate_fires_past_target_not_before: due only when local hour >= send_hour; not before
    - test_not_sent_twice_per_period: a second call same period is a no-op (reads persisted alerting_last_digest_sent_at)
    - test_empty_digest_suppressed: recipient with all-empty sections gets no send
    - test_sections_read_sla_and_exception_state: due/breaching reflect resolve_state_for_vuln; expiring-exceptions reflect ExceptionRecord.expires_at; excepted/suppressed excluded
    - test_per_owner_email_vs_per_team_channel: owner scope → email; team scope → dispatch_channel
  </behavior>
  <action>
    Create backend/app/notifications/digests.py with `run_digests(db)`: for each active tenant, read `merged_alerting_config`; skip if neither per_owner nor per_team digests enabled. Implement `_send_hour_due(tenant, now)` — the NEW wall-clock gate (convert now to `Tenant.timezone`; due if `local_now.hour >= send_hour` AND `alerting_last_digest_sent_at` is not within the current period, period = calendar day for daily / ISO week for weekly). Implement `_assemble_sections(db, tenant, scope_assets)` returning the four ordered sections (due, breaching, newly-critical, expiring-exceptions), each query tenant-scoped with `status.notin_(["SUPPRESSED","FALSE_POSITIVE"])` + `~active_exception_subquery(tenant.id, now)` (D-20); due/breaching classified via `resolve_state_for_vuln`; expiring-exceptions via `ExceptionRecord.expires_at` within a horizon; order rows by risk tier and cap at a top-N constant (10) appending an "and N more →" line with a dashboard deep-link. Implement `_render_digest_html(sections, recipient)` producing the UI-SPEC light-background HTML with inline hex CSS and HTML-escaping EVERY finding-derived string (use html.escape on CVE ids/hostnames — XSS). Per-owner: group scope assets by resolved owner email (via get_directory_user), build+escape HTML, send via email.py send_email(html_body=...); suppress if all sections empty (D-14). Per-team: iterate AssetGroups that have content (A4), build the summary, `dispatch_channel` to the group's routed channel (never abort on failure). After a tenant's sends, set `tenant.alerting_last_digest_sent_at = now`, flag_modified if needed, and commit once.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$ENCRYPTION_KEY JWT_SECRET_KEY=$JWT_SECRET_KEY python -m pytest tests/test_digests.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/notifications/digests.py` contains `def run_digests`, `_send_hour_due`, and `_render_digest_html`
    - digests.py imports `resolve_state_for_vuln`, `active_exception_subquery`, and calls `html.escape` (grep all three present)
    - digests.py does NOT import or call `_is_due` (grep count 0)
    - `alerting_last_digest_sent_at` is written in digests.py (grep present)
    - all of tests/test_digests.py passes
  </acceptance_criteria>
  <done>Digests assemble correct, excluded, capped, escaped content; gate on wall-clock send-hour with a persisted marker; suppress empties; route owner-email vs team-channel.</done>
</task>

<task type="auto">
  <name>Task 3: Scheduler digest-dispatch block</name>
  <read_first>
    - backend/app/connectors/scheduler.py:300-406 (isolated tick blocks + the SLA pass shape + run_alert_checks gating)
    - backend/app/notifications/digests.py (run_digests entry point)
  </read_first>
  <action>
    Add a NEW isolated `try/except` block inside `_scheduler_loop` (mirroring the SLA-pass and alert-check blocks) that opens its own `async_session_factory()` session and calls `await run_digests(db)`, logging the sent count when > 0 and logging on error without aborting the tick. Place it after the SLA pass / alert-check blocks. The wall-clock gate lives inside run_digests, so the block itself runs every tick (like the SLA pass) — do NOT add an elapsed-hours guard here. Confirm `_check_new_kev_epss` (Plan 02) already rides inside the existing `run_alert_checks` call and does not need a separate block.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$ENCRYPTION_KEY JWT_SECRET_KEY=$JWT_SECRET_KEY python -m pytest tests/test_digests.py -x && python -c "import ast,sys; ast.parse(open('app/connectors/scheduler.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "run_digests" backend/app/connectors/scheduler.py` >= 1
    - scheduler.py parses (no syntax error) and the new block is wrapped in its own try/except with its own session
    - tests/test_digests.py remains green
  </acceptance_criteria>
  <done>Digests dispatch on the in-process scheduler tick, fail-isolated, no new infra.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finding-derived text → HTML email body | CVE names/hostnames (scanner-origin, untrusted) rendered in email clients |
| scheduler → SMTP + team webhooks | outbound delivery of tenant data |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-09 | Tampering (XSS) | _render_digest_html | mitigate | html.escape every finding-derived string before interpolation (asserted in Task 2) |
| T-40-10 | Info-disclosure | digest section queries | mitigate | Every section tenant_id-scoped + D-20 exclusion; owner grouping via same-tenant get_directory_user |
| T-40-11 | DoS/fatigue | duplicate morning digests | mitigate | Persisted alerting_last_digest_sent_at + "not sent this period" gate survives restarts |
| T-40-12 | SSRF | team-digest channel POST | mitigate | Reuse dispatch_channel SSRF guard (https-only, blocks private/loopback/metadata) |
| T-40-13 | Info-disclosure | empty-digest churn revealing cadence | accept | D-14 suppression sends nothing on empty; no signal leaked |
</threat_model>

<verification>
- `pytest tests/test_digests.py` fully green
- Send-hour gate proven (fires past hour, not before; not twice per period across a simulated restart)
- D-20 exclusion + D-14 suppression + HTML escaping asserted
- E4 backstop: executor manually confirms hostname truncation in Gmail web + Apple Mail (record evidence in SUMMARY)
</verification>

<success_criteria>
Per-owner and per-team digests deliver on the scheduler with correct content, exclusions, caps, escaping, and a restart-safe wall-clock send-hour gate.
</success_criteria>

<output>
After completion, create `.planning/phases/40-proactive-alerting-digests/40-03-SUMMARY.md`
</output>
