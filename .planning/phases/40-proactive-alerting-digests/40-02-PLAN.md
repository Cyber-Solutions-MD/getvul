---
phase: 40-proactive-alerting-digests
plan: 02
type: execute
wave: 2
depends_on: [40-01]
autonomous: true
requirements: [ALERT-01]
files_modified:
  - backend/app/notifications/alerts.py
  - backend/app/assets/directory.py
  - backend/app/assets/router.py
  - backend/tests/test_alerts_kev_epss.py

must_haves:
  truths:
    - "A CVE that newly becomes KEV-listed OR newly crosses the tenant EPSS threshold (tenant-overridable numeric epss_score, default 0.5 — D-01) on a matched tenant asset fires exactly one alert per (cve, asset, trigger_type) — transition-only, once per newly-qualifying pair (D-02), at (CVE, asset) granularity guarded per (tenant, cve, asset, trigger_type) (D-04)"
    - "The guard table subtraction prevents any re-fire on subsequent ticks for the same (tenant, cve, asset, trigger_type)"
    - "On the first alerting pass for a (tenant, trigger_type) slice, all currently-qualifying pairs are inserted into the guard WITHOUT firing (D-06 silent baseline)"
    - "A qualifying asset with a resolved owner emails that owner; an unowned asset falls back to admins + the tenant alert channel (D-10)"
    - "Multiple resolved owners all receive the alert; a matched asset also pushes to the tenant Slack/Teams channel via dispatch_channel (D-07)"
    - "Every fired alert creates an in-app notification with category 'new_kev_epss' and a scheduler-side AuditLog row (action 'alert.fire', user_email 'system:scheduler', real tenant_id)"
    - "empty/single/null inputs resolve: no qualifying vulns → zero alerts + zero guard writes; a single owner → one email; asset_id null → guarded and routed via D-10 fallback"
    - "ordering is stable: qualifiers are subtracted against the guard set deterministically; trigger_type kev and epss are evaluated as independent slices"
  prohibitions:
    - "Must NOT fold KEV/EPSS detection into _check_new_critical_vulns (D-03) — it is a distinct sibling"
    - "Must NOT fire on the first/baseline pass (D-06 seed-silent) — no launch-day backlog storm"
    - "Must NOT include findings with status IN (SUPPRESSED, FALSE_POSITIVE) or an active exception (D-20)"
    - "Must NOT alert on assets outside the tenant — every qualifier query is tenant_id-scoped"
    - "Must NOT call audit(db, None, ...) from the scheduler path — construct AuditLog directly with the real tenant_id"
    - "Must NOT use _notification_exists (time-windowed) for dedup — use the durable guard subtraction"
  artifacts:
    - path: "backend/app/notifications/alerts.py"
      provides: "_check_new_kev_epss sibling + run_alert_checks call-site line"
      contains: "async def _check_new_kev_epss"
    - path: "backend/app/assets/directory.py"
      provides: "get_directory_user extracted for reuse in the notifications layer"
      contains: "def get_directory_user"
  key_links:
    - from: "backend/app/notifications/alerts.py::run_alert_checks"
      to: "_check_new_kev_epss"
      via: "await call inside the per-tenant loop"
      pattern: "_check_new_kev_epss"
    - from: "_check_new_kev_epss"
      to: "alerting_guard"
      via: "qualifiers minus guard rows, then insert"
      pattern: "AlertingGuard"
    - from: "_check_new_kev_epss"
      to: "dispatch_channel"
      via: "_build_channel_config + dispatch_channel push"
      pattern: "dispatch_channel"
---

<objective>
LEAD TRACER (TRACER_MODE): prove ALERT-01 end-to-end through the real dispatch seam — a newly-qualifying KEV/EPSS finding on a tenant asset is detected via guard-table subtraction, seeded silently on first pass, then on a genuine future transition fires to the tenant channel + resolved owner email + in-app twin, audited, and guarded against re-fire. This is the highest-integration-risk slice; it must be green before the digest/config expansion waves.

Purpose: Validates the guard mechanism, the D-20 exclusion, owner resolution, the shared-credential dispatch reuse (D-19), and scheduler-side audit — the primitives every remaining plan reuses.
Output: `_check_new_kev_epss`, the extracted `get_directory_user`, and a green `test_alerts_kev_epss.py`.
</objective>

<artifacts_produced>
- `backend/app/notifications/alerts.py::_check_new_kev_epss` (new sibling check)
- one new line in `run_alert_checks` per-tenant loop calling it (D-03)
- `backend/app/assets/directory.py::get_directory_user` (extracted from assets/router.py so the notifications layer can import it without router coupling — research A5)
- notification category string `new_kev_epss`; audit action `alert.fire`
</artifacts_produced>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-proactive-alerting-digests/40-CONTEXT.md
@.planning/phases/40-proactive-alerting-digests/40-RESEARCH.md
@.planning/phases/40-proactive-alerting-digests/40-PATTERNS.md
@.planning/phases/40-proactive-alerting-digests/40-01-SUMMARY.md

<interfaces>
Call site — backend/app/notifications/alerts.py:28-34 (add one line, keep existing paths):
  alerts += await _check_new_kev_epss(db, tenant)   # NEW (D-03)
Analog to mirror — _check_new_critical_vulns (alerts.py:46-97): select(Vulnerability, Asset).outerjoin(Asset, ...).where(tenant-scoped ...) → create_notification(...) → _email_owners_and_admins(...).
Reusable helpers (alerts.py): _email_owners_and_admins (270-296, selects role IN OWNER/ADMIN), create_notification (from service.py). Do NOT reuse _notification_exists (247-267) for dedup.
Owner resolution — assets/router.py:81 `async def _get_directory_user(db, tenant_id, asset) -> dict | None` returns a dict {email, display_name, ...} or None (mdm humaans_email → assigned_user → last_login_user precedence).
Shared-credential dispatch — sla_tier_service.py:328 `_build_channel_config(sla_config, channel, tenant)` (Fernet-decrypts) + escalation_channels.py:272 `dispatch_channel(channel, config, context)` (never raises; returns {"ok": bool, "error": str|None}).
Exclusion — exceptions/service.py:50-91 `active_exception_subquery(tenant_id, now)`; apply `~active_exception_subquery(...)` + `Vulnerability.status.notin_(["SUPPRESSED","FALSE_POSITIVE"])`.
Config — backend/app/notifications/alerting_config.py `merged_alerting_config(tenant)` → keys kev_enabled, epss_threshold, routing.
Guard — backend/app/notifications/models.py `AlertingGuard` (uq_alerting_guard_once).
Scheduler audit — construct AuditLog(tenant_id=tenant.id, user_id=None, user_email="system:scheduler", action="alert.fire", ...); epss_score is Numeric(5,4) — do the >= comparison in SQL.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: _check_new_kev_epss — qualifier query, D-20 exclusion, guard subtraction, seed-silent</name>
  <read_first>
    - backend/app/notifications/alerts.py:23-97 (run_alert_checks loop + _check_new_critical_vulns analog)
    - backend/app/notifications/models.py (AlertingGuard columns/constraint)
    - backend/app/notifications/alerting_config.py (merged_alerting_config keys)
    - backend/app/exceptions/service.py:50-91 (active_exception_subquery — reuse verbatim)
    - backend/app/vulnerabilities/models.py (cisa_kev, epss_score Numeric(5,4), status, cve_id, asset_id)
    - backend/tests/test_alerts_kev_epss.py (the RED tests to turn green)
  </read_first>
  <behavior>
    - test_new_kev_match_fires_once: with a non-empty guard slice, a newly-qualifying (cve,asset,kev) pair produces exactly one fire and one guard row
    - test_guard_prevents_refire: a second tick over the same qualifiers produces zero fires
    - test_first_pass_seeds_silently: an EMPTY guard slice inserts all current qualifiers with zero fires (D-06)
    - test_excepted_suppressed_excluded: a SUPPRESSED/FALSE_POSITIVE finding and one under an active exception are never in the qualifier set (D-20)
  </behavior>
  <action>
    Add `async def _check_new_kev_epss(db, tenant) -> int` to alerts.py. For each `trigger_type` in `("kev","epss")`: read `merged_alerting_config(tenant)`; skip the slice if that trigger is disabled (kev_enabled false, or epss disabled). Build the qualifier `select(Vulnerability, Asset).outerjoin(Asset, ...)` tenant-scoped, applying the trigger predicate — `Vulnerability.cisa_kev.is_(True)` for kev, or `Vulnerability.epss_score >= literal(threshold)` for epss (comparison in SQL so Postgres coerces the Decimal/float boundary, Pitfall 3) — AND `Vulnerability.status.notin_(["SUPPRESSED","FALSE_POSITIVE"])` AND `~active_exception_subquery(tenant.id, now)` (D-20). Load the guard set for `(tenant, trigger_type)`; compute `new_pairs = qualifiers − guard` keyed on `(cve_id, asset_id)`. If the guard slice is EMPTY → insert AlertingGuard rows for every new_pair with fired_at NULL and DO NOT fire (D-06). Else → hand new_pairs to the fire/route step (Task 2), then insert AlertingGuard rows (fired_at=now). Return the count of fired pairs. Add the one call-site line in run_alert_checks after `_check_risk_score_changes` (D-03) — keep all existing checks untouched.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$ENCRYPTION_KEY JWT_SECRET_KEY=$JWT_SECRET_KEY python -m pytest tests/test_alerts_kev_epss.py -k "fires_once or refire or seeds or excluded" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def _check_new_kev_epss" backend/app/notifications/alerts.py` returns 1
    - `grep -c "_check_new_kev_epss" backend/app/notifications/alerts.py` returns 2 (definition + call site)
    - The four named tests pass
    - The epss comparison is emitted in SQL (grep: `epss_score >=` or `epss_score.__ge__`/`>=` against a literal in alerts.py)
    - `active_exception_subquery` is imported and applied (grep present)
  </acceptance_criteria>
  <done>Detection + guard subtraction + silent baseline + D-20 exclusion pass; no re-fire across ticks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Owner resolution, channel push, in-app twin, scheduler audit</name>
  <read_first>
    - backend/app/assets/router.py:81-114 (_get_directory_user to extract)
    - backend/app/notifications/alerts.py:270-296 (_email_owners_and_admins fallback)
    - backend/app/vulnerabilities/sla_tier_service.py:328-388 (_build_channel_config + direct-AuditLog scheduler audit pattern)
    - backend/app/notifications/escalation_channels.py:272-298 (dispatch_channel contract)
    - backend/app/audit.py (AuditLog model fields)
  </read_first>
  <behavior>
    - test_owner_fallback_to_admins_and_channel: a qualifying asset with NO resolvable owner routes to _email_owners_and_admins AND the tenant channel; a resolved single owner emails that owner
    - multiple owners → all emailed; every fire also attempts the tenant Slack/Teams push via dispatch_channel and never raises on channel failure
  </behavior>
  <action>
    Extract `_get_directory_user` from assets/router.py into a new `backend/app/assets/directory.py` as `async def get_directory_user(db, tenant_id, asset) -> dict | None` (move the body verbatim; re-export or import it back into assets/router.py so existing callers keep working — no behavior change). In the fire step of `_check_new_kev_epss`, per new_pair: resolve the owner via `get_directory_user`; if None → `_email_owners_and_admins(db, tenant, title, message, "new_kev_epss")` (D-10 fallback); else email the resolved owner(s) (all when multiple). Always also build the tenant channel config via `_build_channel_config(tenant.sla_config or {}, channel, tenant)` for each routed channel in the config's ALERT-01 routing and `await dispatch_channel(...)` (fail-isolated — log on `not outcome["ok"]`, never abort). Create the in-app twin with `create_notification(..., category="new_kev_epss", resource_type="vulnerability", resource_id=cve_id, details={...})`. Write a scheduler-side audit row by constructing `AuditLog(tenant_id=tenant.id, user_id=None, user_email="system:scheduler", action="alert.fire", resource_type="vulnerability", resource_id=cve_id, ...)` — never `audit(db, None, ...)`.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$ENCRYPTION_KEY JWT_SECRET_KEY=$JWT_SECRET_KEY python -m pytest tests/test_alerts_kev_epss.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/assets/directory.py` contains `def get_directory_user` and assets/router.py imports it (existing asset tests still pass)
    - `grep -c "system:scheduler" backend/app/notifications/alerts.py` >= 1 and no `audit(db, None" call in the new code path
    - `dispatch_channel` and `create_notification` are both invoked in the fire path (grep present)
    - All of `tests/test_alerts_kev_epss.py` passes
  </acceptance_criteria>
  <done>ALERT-01 fires end-to-end through the real dispatch seam with correct owner routing, in-app twin, and tenant-scoped audit; full ALERT-01 test file green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler tick → outbound webhook/SMTP | untrusted tenant-configured URLs + finding-derived text leave the process |
| Vulnerability row (scanner text) → notification/channel payload | CVE names/hostnames originate from scanners (untrusted) |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04 | Info-disclosure | qualifier query cross-tenant leak | mitigate | Every qualifier + guard + owner query filtered by `tenant.id`; `get_directory_user` filters `User.tenant_id` — asserted by fixtures using two tenants |
| T-40-05 | Spoofing/SSRF | dispatch_channel push to tenant URL | mitigate | Reuse `dispatch_channel` (`_validate_webhook_url` https-only, blocks private/loopback/metadata, follow_redirects=False) — inherited free, no custom POST |
| T-40-06 | Repudiation | scheduler-side fire audit | mitigate | Direct `AuditLog(tenant_id=tenant.id, user_email="system:scheduler", action="alert.fire")` — real tenant, not nil (audit.py None-user branch avoided) |
| T-40-07 | DoS/fatigue | launch-day backlog storm | mitigate | D-06 silent baseline seeding on empty guard slice |
| T-40-08 | Info-disclosure | channel secrets in audit/log | mitigate | Audit details are secret-free; credentials only decrypted inside `_build_channel_config`, never logged |
</threat_model>

<verification>
- `pytest tests/test_alerts_kev_epss.py` fully green
- No re-fire across repeated ticks; silent first-pass seed; D-20 exclusion honored
- assets/router.py existing tests still pass after the extraction
</verification>

<success_criteria>
ALERT-01 fires exactly once per (cve, asset, trigger) transition through the real dispatch seam, seeds silently on first pass, routes owners/admins/channel correctly, and is audited — proven before any expansion wave.
</success_criteria>

<output>
After completion, create `.planning/phases/40-proactive-alerting-digests/40-02-SUMMARY.md`
</output>
