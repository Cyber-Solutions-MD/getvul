---
phase: 40-proactive-alerting-digests
plan: 05
type: execute
wave: 5
depends_on: [40-04]
autonomous: false
requirements: [ALERT-03]
files_modified:
  - frontend/src/components/settings/alerting-digests-pane.tsx
  - frontend/src/components/settings/microcopy.ts
  - frontend/src/components/settings/settings-sidebar-shell.tsx
  - frontend/src/app/(authed)/dashboard/settings/page.tsx
  - frontend/src/components/settings/alerting-digests-pane.test.tsx

must_haves:
  truths:
    - "A new 'Alerting & Digests' pane renders three section cards (New exposure alerts, Scheduled digests, Delivery channels) pre-filled from Tenant.alerting_config, saving via updateSettings.mutateAsync({ alerting_config })"
    - "The pane is registered: 'alerting' added to microcopy Category union + CATEGORY_LABELS ('Alerting & Digests'), to settings-sidebar-shell ALL_CATEGORIES + ADMIN_ONLY, and to page.tsx renderPane as case 'alerting'"
    - "RBAC: a non-owner viewer sees the pane (admin-visible) with every control disabled (isOwner gate); owner can edit and save (asymmetric, mirrors sla-escalation-pane)"
    - "empty state: when no delivery channels are configured under SLA & Escalation, the pane shows the 'No delivery channels configured' EmptyState (reuses the sla-escalation anyChannelEnabled guard)"
    - "loading state uses SkeletonTable; error state uses PartialFailureBanner (mandatory states — CLAUDE.md; no screen without empty/loading/error)"
    - "the nav label 'Alerting & Digests' fits the sidebar in the same length class as 'SLA & Escalation'; long webhook/threshold text uses the truncate + title tooltip pattern"
    - "zero-one-many: per-team digest is a single tenant-wide on/off toggle (D-16 confirmed), so there is no variable-count control that reads wrong at zero/one/many"
    - "Send test digest button shows disabled + 'Sending…' in flight; on success/empty/error branches on the Plan 04 response status"
    - { statement: "Send test digest with zero matching findings shows the inline 'Nothing to send right now — no due, breaching, newly-critical, or expiring-exception findings match your current settings.' message rather than silently no-op'ing the button", verification: backstop }
  prohibitions:
    - "Must NOT introduce a second gradient CTA (Save changes via the shared SaveBar is the sole gradient CTA)"
    - "Must NOT pick freehand hex colors or substitute fonts — use design tokens + Inter/JetBrains Mono (CLAUDE.md / foundation.md)"
    - "Must NOT re-collect or display channel webhook secrets in this pane (D-19 — channels are configured under SLA & Escalation)"
    - "Must NOT ship the pane without empty/loading/error states"
    - "Must NOT compose generic SaaS copy — use the exact UI-SPEC Copywriting Contract strings"
  artifacts:
    - path: "frontend/src/components/settings/alerting-digests-pane.tsx"
      provides: "Alerting & Digests settings pane"
      contains: "data-pane=\"alerting-digests\""
    - path: "frontend/src/app/(authed)/dashboard/settings/page.tsx"
      provides: "renderPane case 'alerting'"
      contains: "AlertingDigestsPane"
  key_links:
    - from: "alerting-digests-pane.tsx"
      to: "PATCH /settings"
      via: "useUpdateTenantSettings mutateAsync({ alerting_config })"
      pattern: "alerting_config"
    - from: "settings-sidebar-shell.tsx + microcopy.ts + page.tsx"
      to: "alerting-digests-pane.tsx"
      via: "'alerting' Category registered in all three"
      pattern: "alerting"
---

<objective>
Ship the "Alerting & Digests" settings pane (D-17) as a structural clone of `sla-escalation-pane.tsx`, register it across microcopy/sidebar/page, and verify it live. This is the only new frontend surface in the phase — everything else (in-app alert twin, audit rows, digest email) is backend-generated or reused verbatim.

Purpose: Gives admins/owners the tenant-configurable control surface ALERT-03 requires, on the established settings pattern.
Output: the pane + its registration + a green vitest + a human-verify sign-off.
</objective>

<artifacts_produced>
- `frontend/src/components/settings/alerting-digests-pane.tsx` (new pane; `data-pane="alerting-digests"`)
- `microcopy.ts`: `'alerting'` in the `Category` union + `CATEGORY_LABELS['alerting'] = 'Alerting & Digests'`
- `settings-sidebar-shell.tsx`: `'alerting'` in `ALL_CATEGORIES` + `ADMIN_ONLY`
- `page.tsx`: `case 'alerting': return <AlertingDigestsPane onDirtyChange={handleDirtyChange} />;` + import
</artifacts_produced>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-proactive-alerting-digests/40-UI-SPEC.md
@.planning/phases/40-proactive-alerting-digests/40-PATTERNS.md
@.claude/skills/sketch-findings-getvul/SKILL.md
@.planning/phases/40-proactive-alerting-digests/40-04-SUMMARY.md

<interfaces>
Clone target — frontend/src/components/settings/sla-escalation-pane.tsx: imports useAuth, useTenantSettings/useUpdateTenantSettings (@/lib/queries/use-tenant-settings), useDirtyState (./use-dirty-state), SaveBar (./save-bar), SkeletonTable/PartialFailureBanner/EmptyState (@/components/states), queryKeys. Props `{ onDirtyChange?: (dirty: boolean) => void }` + `useEffect(() => onDirtyChange?.(isDirty), [isDirty, onDirtyChange])`. RBAC `const isOwner = user?.role === 'OWNER';` + `disabled={!isOwner}` on every control. Root `<div data-pane="sla-escalation" className="space-y-6 p-6">`. States at :391-467 (PartialFailureBanner on isError, SkeletonTable on isPending, EmptyState for empty channels). Save at :328-384: `await updateSettings.mutateAsync({ sla_config: {...} }); reset();`.
Registration points: microcopy.ts:10-31 (Category union ends `| 'sla';` at line 18; CATEGORY_LABELS at 21, `sla: 'SLA & Escalation'` at 30). settings-sidebar-shell.tsx:40-63 (ALL_CATEGORIES includes 'sla' at 49; ADMIN_ONLY Set includes 'sla' at 62). page.tsx:47 (import SlaEscalationPane), :118-135 (renderPane; `case 'sla'` at 134-135), :83 handleDirtyChange.
UI-SPEC copy: Section 1 "New exposure alerts" (KEV toggle "Alert on new CISA KEV listings" + EPSS threshold "EPSS threshold" helper "Fire when a CVE's EPSS score reaches this value or higher. Default 0.5."); Section 2 "Scheduled digests" (Cadence Daily/Weekly, "Send at" send-hour, "Send per-owner digests"/"Send per-team digests"); Section 3 "Delivery channels" (routing per alert type; reuse-SLA-channels subtitle). Secondary action "Send test digest". Empty: "No delivery channels configured" / body. Test-digest empty inline: "Nothing to send right now — …". Alerting_config key set from backend/app/notifications/alerting_config.py DEFAULT_ALERTING_CONFIG.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: alerting-digests-pane.tsx (clone) + vitest green</name>
  <read_first>
    - frontend/src/components/settings/sla-escalation-pane.tsx (full — structural clone)
    - frontend/src/components/settings/sla-escalation-pane.test.tsx (test patterns)
    - frontend/src/components/settings/alerting-digests-pane.test.tsx (the RED scaffold to satisfy)
    - .claude/skills/sketch-findings-getvul/SKILL.md + references/state-patterns.md + references/copy-voice.md
    - .planning/phases/40-proactive-alerting-digests/40-UI-SPEC.md (Copywriting Contract + Color/Typography/Spacing tables + UI Considerations)
  </read_first>
  <action>
    Create alerting-digests-pane.tsx as a structural clone of sla-escalation-pane.tsx: same imports/hooks (useAuth, useTenantSettings/useUpdateTenantSettings, useDirtyState, SaveBar, SkeletonTable/PartialFailureBanner/EmptyState, queryKeys), same `onDirtyChange` prop + effect, same `isOwner` RBAC (`disabled={!isOwner}` on every control), root `<div data-pane="alerting-digests" className="space-y-6 p-6">`. Three section cards using the exact UI-SPEC copy: (1) New exposure alerts — KEV toggle + EPSS threshold input (JetBrains Mono for the numeric value); (2) Scheduled digests — Cadence select (Daily/Weekly), Send-at hour, per-owner + per-team toggles; (3) Delivery channels — per-alert-type routing referencing SLA-configured channels. Seed form state from `settings.alerting_config` (fall back to DEFAULT_ALERTING_CONFIG defaults). Save handler: `await updateSettings.mutateAsync({ alerting_config: {...} }); reset();`. Mandatory states: PartialFailureBanner on isError, SkeletonTable on isPending, the "No delivery channels configured" EmptyState keyed off whether any SLA channel is enabled (mirror the anyChannelEnabled guard). Add a "Send test digest" secondary button that POSTs to /settings/alerting/test-digest, shows disabled + "Sending…" in flight, and branches on the response status: sent → success affordance, empty → inline "Nothing to send right now — …" message (E1 backstop), error → the UI-SPEC error copy. Design tokens only (no raw hex), Inter + JetBrains Mono only, no second gradient CTA. Update alerting-digests-pane.test.tsx from RED scaffold to full assertions (render, three headings, RBAC disabled-for-non-owner, empty-channels EmptyState, save calls mutateAsync with alerting_config, test-digest empty-branch renders the inline message).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run alerting-digests-pane</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-pane="alerting-digests"' frontend/src/components/settings/alerting-digests-pane.tsx` returns 1
    - the pane calls `mutateAsync` with an `alerting_config` key (grep present)
    - the pane renders PartialFailureBanner, SkeletonTable, and an EmptyState (all three grep present)
    - `npx vitest run alerting-digests-pane` passes including the RBAC, empty-channels, and test-digest-empty assertions
    - no raw hex color literals in the pane (grep `#[0-9A-Fa-f]{6}` count 0)
  </acceptance_criteria>
  <done>The pane renders/saves/handles all mandatory + test-digest states on the design system, vitest green.</done>
</task>

<task type="auto">
  <name>Task 2: Register the pane (microcopy + sidebar shell + page)</name>
  <read_first>
    - frontend/src/components/settings/microcopy.ts:10-31 (Category union + CATEGORY_LABELS)
    - frontend/src/components/settings/settings-sidebar-shell.tsx:40-63 (ALL_CATEGORIES + ADMIN_ONLY)
    - frontend/src/app/(authed)/dashboard/settings/page.tsx:47,118-135 (import + renderPane)
  </read_first>
  <action>
    In microcopy.ts add `'alerting'` to the `Category` union and `alerting: 'Alerting & Digests'` to `CATEGORY_LABELS` (title-cased "&"-joined nav label, matching the 'SLA & Escalation' precedent per UI-SPEC). In settings-sidebar-shell.tsx add `'alerting'` to the `ALL_CATEGORIES` array (placed adjacent to 'sla') AND to the `ADMIN_ONLY` Set (admin/owner-gated, D-17). In page.tsx import `AlertingDigestsPane` and add `case 'alerting': return <AlertingDigestsPane onDirtyChange={handleDirtyChange} />;` to renderPane.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run settings-sidebar-shell</automated>
  </verify>
  <acceptance_criteria>
    - microcopy.ts Category union contains `'alerting'` and CATEGORY_LABELS has `alerting: 'Alerting & Digests'`
    - settings-sidebar-shell.tsx ALL_CATEGORIES and ADMIN_ONLY both contain `'alerting'`
    - page.tsx imports AlertingDigestsPane and has `case 'alerting'`
    - `npx tsc --noEmit` passes (the Category union change type-checks across all consumers)
  </acceptance_criteria>
  <done>The pane is reachable from the settings sidebar, admin/owner-gated, and type-safe across all Category consumers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The "Alerting & Digests" settings pane (three section cards, RBAC owner-gate, mandatory states, Send test digest) plus the backend ALERT-01 real-time alert and ALERT-02 digest delivery through real Slack/Teams/email channels.</what-built>
  <how-to-verify>
    1. Log in as an OWNER, open /dashboard/settings, click "Alerting & Digests" in the sidebar. Confirm three cards render with the UI-SPEC copy and the pane pre-fills from saved config.
    2. Toggle KEV on, set EPSS threshold, pick a cadence + send-hour, enable per-owner and per-team digests, choose channel routing, click "Save changes" — confirm the SaveBar dirty→saved flow and that a reload shows the persisted values.
    3. Log in as a non-owner ADMIN — confirm the pane is visible but all controls are disabled.
    4. With NO SLA channels configured, confirm the "No delivery channels configured" EmptyState.
    5. Configure a real Slack/Teams webhook + SMTP in a test tenant (under SLA & Escalation). Click "Send test digest" — confirm the email arrives to your own address and renders (light background, sections, escaped CVE/hostnames, "and N more →"), OR the "Nothing to send right now…" inline message when there is no content. (Manual-only per 40-VALIDATION.md: live third-party delivery cannot be unit-asserted.)
    6. Trigger a KEV/EPSS transition on a matched asset (or use the seeded fixture path) — confirm the real-time alert posts to the channel + the owner email + the in-app bell, and that a repeat tick does NOT re-fire.
    7. Open the Audit log pane — confirm an "updated alerting configuration" row for the save.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per-step)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → PATCH /settings | owner-entered config values |
| pane → test-digest action | admin-triggered self-send |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-19 | Elevation of privilege | client-side RBAC | mitigate | isOwner disables controls in UI, but the server PATCH require_owner is authoritative (Plan 04) — UI gate is defense-in-depth, not the control |
| T-40-20 | Info-disclosure | secrets rendered in pane | mitigate | Pane never fetches/renders channel secrets (D-19); only routing/enablement/threshold values are shown |
| T-40-21 | Tampering (XSS) | test-digest inline messages | mitigate | Inline states use fixed copy strings; any server-echoed error text rendered as React text (auto-escaped), never dangerouslySetInnerHTML |
</threat_model>

<verification>
- `npx vitest run alerting-digests-pane` and `settings-sidebar-shell` green; `npx tsc --noEmit` clean
- Human-verify sign-off on live render/save/RBAC/empty-state + real channel delivery + no re-fire + audit row
</verification>

<success_criteria>
Admins/owners configure alert rules and channel routing on a design-system-compliant pane with all mandatory states; live delivery and audit verified end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/40-proactive-alerting-digests/40-05-SUMMARY.md`
</output>
