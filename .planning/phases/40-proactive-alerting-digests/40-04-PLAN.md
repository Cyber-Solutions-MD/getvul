---
phase: 40-proactive-alerting-digests
plan: 04
type: execute
wave: 4
depends_on: [40-01, 40-03]
autonomous: true
requirements: [ALERT-03]
files_modified:
  - backend/app/tenants/router.py
  - backend/tests/test_alerting_settings.py

must_haves:
  truths:
    - "PATCH /settings with an alerting_config body validates it via AlertingConfigUpdate (epss_threshold 0..1, send_hour 0..23, cadence Literal daily|weekly, kev_enabled bool) and 422s on out-of-bounds values"
    - "A valid alerting_config persists to Tenant.alerting_config (JSONB) with flag_modified, and the change is recorded via the fail-closed audit() path with action 'alerting.config_update'"
    - "GET /settings exposes the current alerting_config (secret-free) so the pane can pre-fill"
    - "PATCH /settings requires owner (require_owner); GET requires admin (asymmetric, inherited)"
    - "POST /settings/alerting/test-digest sends the acting admin's own tenant digest to the acting admin's email only, and returns a response that distinguishes 'sent' from 'nothing to send' from 'error'"
    - "adjacency/overlap resolves: overlapping routing scopes (an alert type routed to multiple channels; per-owner AND per-team both enabled) are all honored, none silently dropped (D-21 overlap-by-design)"
    - "empty/null resolves: an empty/absent alerting_config body leaves stored config unchanged or resets to an explicit empty object without 500; all-disabled config validates and simply produces no alerts/digests"
  prohibitions:
    - "Must NOT write any channel secret into alerting_config or into the audit details (secret-free — D-18/D-19)"
    - "Must NOT omit flag_modified on the JSONB write (silent no-op — Pitfall 5)"
    - "Must NOT let the config save commit if its audit row cannot write (fail-closed audit ordering)"
    - "test-digest must NOT send to anyone other than the acting admin (no broadcast from a preview action)"
  artifacts:
    - path: "backend/app/tenants/router.py"
      provides: "AlertingConfigUpdate, _safe_alerting, alerting_config PATCH branch, alerting.config_update audit, POST /settings/alerting/test-digest"
      contains: "class AlertingConfigUpdate"
  key_links:
    - from: "backend/app/tenants/router.py::update_tenant_settings"
      to: "Tenant.alerting_config"
      via: "validate → assign → flag_modified → audit → commit"
      pattern: "alerting_config"
    - from: "POST /settings/alerting/test-digest"
      to: "notifications/digests.py"
      via: "assemble+render the acting tenant's digest for a single recipient"
      pattern: "digests"
---

<objective>
Build ALERT-03's backend: a validated, audited `alerting_config` save (cloning the proven `sla_config` branch), its exposure on GET /settings, and a "Send test digest" endpoint that previews the tenant's current digest to the acting admin only — including the distinguishable empty-vs-error response the pane's E1 backstop depends on.

Purpose: Makes alert rules and channels tenant-configurable and audited; supplies the contract the settings pane (Plan 05) consumes.
Output: `AlertingConfigUpdate`, the PATCH branch, the test-digest route, and a green `test_alerting_settings.py`.
</objective>

<artifacts_produced>
- `backend/app/tenants/router.py`: `class AlertingConfigUpdate` (Pydantic gate), `_safe_alerting` (pass-through/no-secret masking), `alerting_config` branch in `update_tenant_settings`, audit action `alerting.config_update`, `alerting_config` in the GET /settings response, `POST /settings/alerting/test-digest`
</artifacts_produced>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-proactive-alerting-digests/40-CONTEXT.md
@.planning/phases/40-proactive-alerting-digests/40-RESEARCH.md
@.planning/phases/40-proactive-alerting-digests/40-PATTERNS.md
@.planning/phases/40-proactive-alerting-digests/40-03-SUMMARY.md

<interfaces>
sla_config branch to mirror — backend/app/tenants/router.py:332-380: `if "sla_config" in body:` → SlaConfigUpdate.model_validate → assign → flag_modified(tenant, "sla_config") → await audit(db, user, "sla.policy_update", "tenant", str(tenant.id), {secret-free}). Handler committed at end.
SlaConfigUpdate gate — router.py:104-124 (validation-only; @field_validator for enums). _safe_sla — router.py:127+ (mask-on-read; alerting has no secrets so _safe_alerting is a pass-through).
GET response — router.py:265 exposes `"sla_config": _safe_sla(...)`; add `"alerting_config": _safe_alerting(getattr(tenant, "alerting_config", None))`.
RBAC — update_tenant_settings is Depends(require_owner) (router.py:284); GET settings is require_admin. Imports already present: `from app.auth.rbac import require_admin, require_owner`.
Audit — app/audit.py `await audit(db, user, action, resource_type, resource_id, details)` is fail-closed (commit short-circuits if audit can't write). Real CurrentUser here (NOT the scheduler path).
Config contract — backend/app/notifications/alerting_config.py DEFAULT_ALERTING_CONFIG (canonical keys/defaults to validate against).
Digest builder — backend/app/notifications/digests.py (_assemble_sections + _render_digest_html) for the test-digest preview to a single recipient.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AlertingConfigUpdate gate + alerting_config PATCH branch + GET exposure + audit</name>
  <read_first>
    - backend/app/tenants/router.py:104-135 (SlaConfigUpdate + _safe_sla clone targets)
    - backend/app/tenants/router.py:281-410 (update_tenant_settings handler + sla_config branch + audit loop)
    - backend/app/tenants/router.py:255-270 (GET /settings response assembly)
    - backend/app/notifications/alerting_config.py (DEFAULT_ALERTING_CONFIG keys/bounds)
    - backend/tests/test_alerting_settings.py (tests to turn green)
  </read_first>
  <behavior>
    - test_alerting_config_validates_bounds: epss_threshold outside 0..1, send_hour outside 0..23, or cadence not in {daily,weekly} → 422
    - test_alerting_config_persists_to_jsonb: a valid body round-trips into Tenant.alerting_config and appears in GET /settings
    - test_alerting_config_change_audited: a save writes an audit row with action "alerting.config_update" and secret-free details
    - test_patch_requires_owner: a non-owner PATCH is rejected
  </behavior>
  <action>
    Add `class AlertingConfigUpdate(BaseModel)` near SlaConfigUpdate with fields: `kev_enabled: bool`, `epss_threshold: float = Field(0.5, ge=0, le=1)`, `cadence: Literal["daily","weekly"] = "daily"`, `send_hour: int = Field(ge=0, le=23)`, `per_owner_digests: bool`, `per_team_digests: bool`, and a `routing` dict (validated shape referencing channel names, no secrets). Add `_safe_alerting(cfg)` as a pass-through (alerting stores no secrets — D-19; document why no mask dance). Add an `if "alerting_config" in body:` branch in update_tenant_settings that validates via `AlertingConfigUpdate.model_validate(new_cfg)` (422 on ValidationError), assigns `tenant.alerting_config = new_cfg`, calls `flag_modified(tenant, "alerting_config")`, and `await audit(db, user, "alerting.config_update", "tenant", str(tenant.id), {kev_enabled, epss_threshold, cadence, send_hour, per_owner_digests, per_team_digests})` — secret-free. Add `alerting_config` (via _safe_alerting) to the GET /settings response and to the audit-loop's secret-free field set. Do not alter existing branches.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$ENCRYPTION_KEY JWT_SECRET_KEY=$JWT_SECRET_KEY python -m pytest tests/test_alerting_settings.py -k "validates or persists or audited or owner" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class AlertingConfigUpdate" backend/app/tenants/router.py` returns 1
    - router.py contains `"alerting.config_update"` and `flag_modified(tenant, "alerting_config")`
    - GET /settings response includes `alerting_config`
    - the four named tests pass
  </acceptance_criteria>
  <done>alerting_config validates, persists, exposes, and audits through the fail-closed path; owner-gated.</done>
</task>

<task type="auto">
  <name>Task 2: POST /settings/alerting/test-digest (single-recipient preview, distinguishable empty vs error)</name>
  <read_first>
    - backend/app/tenants/router.py (route registration style near update_tenant_settings; require_admin dependency)
    - backend/app/notifications/digests.py (_assemble_sections + _render_digest_html reuse)
    - backend/app/email.py (send_email html_body)
    - .planning/phases/40-proactive-alerting-digests/40-UI-SPEC.md (E1 test-digest states + copy)
  </read_first>
  <action>
    Add `POST /settings/alerting/test-digest` (Depends(require_admin)) that assembles the acting tenant's current digest sections for the acting admin as the single recipient (reuse `_assemble_sections` + `_render_digest_html`), and sends via `send_email(html_body=...)` to the acting admin's own email ONLY. Return a typed JSON body distinguishing three outcomes: `{"status":"sent"}`, `{"status":"empty"}` (all sections empty — D-14, so the pane shows the "Nothing to send right now…" inline message rather than a false error), and `{"status":"error","error":...}` (send failure). Never broadcast to other users. This backs the E1 backstop (distinguishable empty-vs-error test-send response).
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$ENCRYPTION_KEY JWT_SECRET_KEY=$JWT_SECRET_KEY python -m pytest tests/test_alerting_settings.py -x</automated>
  </verify>
  <acceptance_criteria>
    - router.py registers a `test-digest` route gated by require_admin
    - the endpoint returns distinct `status` values for sent / empty / error (grep: `"empty"` and `"sent"` present)
    - the send targets only the acting admin's email (no tenant-wide recipient list in this path)
    - all of tests/test_alerting_settings.py passes
  </acceptance_criteria>
  <done>Admins can preview the digest to themselves with a response the pane can branch on for the empty-vs-error backstop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PATCH /settings | tenant-supplied config values cross into stored routing/thresholds |
| test-digest action → SMTP | on-demand outbound send triggered by an admin |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-14 | Tampering | alerting_config input | mitigate | AlertingConfigUpdate bounds (epss 0..1, send_hour 0..23, cadence enum) → 422 on violation |
| T-40-15 | Elevation of privilege | config write | mitigate | PATCH require_owner; GET require_admin (asymmetric, inherited) |
| T-40-16 | Repudiation | config change | mitigate | Fail-closed audit("alerting.config_update"); commit short-circuits if audit can't write |
| T-40-17 | Info-disclosure | secrets in config/audit | mitigate | alerting_config holds no secrets (D-19); audit details secret-free; _safe_alerting pass-through documented |
| T-40-18 | Spoofing/abuse | test-digest as a send primitive | mitigate | require_admin + recipient hard-pinned to the acting admin's own email; no arbitrary recipient |
</threat_model>

<verification>
- `pytest tests/test_alerting_settings.py` fully green
- 422 on out-of-bounds; audit row on save; owner-gate enforced
- test-digest returns sent/empty/error distinctly and targets only the caller
</verification>

<success_criteria>
Alert rules and channel routing are tenant-configurable, bounded, audited, and owner-gated; a self-targeted test-digest preview backs the pane's empty-vs-error state.
</success_criteria>

<output>
After completion, create `.planning/phases/40-proactive-alerting-digests/40-04-SUMMARY.md`
</output>
