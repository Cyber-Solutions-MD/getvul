---
phase: 10-dashboard
plan: 03
type: execute
wave: 1
depends_on:
  - 10-02
files_modified:
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/stat.tsx
  - frontend/src/components/ui/stat-strip.tsx
  - frontend/src/components/ui/activity-feed.tsx
  - frontend/src/components/ui/error-boundary.tsx
  - frontend/src/components/ui/card.test.tsx
  - frontend/src/components/ui/stat.test.tsx
  - frontend/src/components/ui/stat-strip.test.tsx
  - frontend/src/components/ui/activity-feed.test.tsx
  - frontend/src/components/ui/error-boundary.test.tsx
  - frontend/src/app/dev/primitives/page.tsx
autonomous: true
requirements:
  - UX-02-02
  - UX-02-05
  - UX-02-06
tags:
  - phase-10
  - frontend
  - primitives
  - ui
  - accessibility

must_haves:
  truths:
    - "Five new primitives ship in components/ui/ with the API shapes locked in D-P-01..04, D-P-06"
    - "Each primitive has a .test.tsx file with at least one axe-core assertion (vitest-axe) per D-Test-01"
    - "Every visual token in every primitive consumes a CSS variable from sunset.css — no hex literals, no Tailwind raw colors"
    - "StatStrip handles `delta=null` as 'Δ —' per Pitfall 8 / D-S-04"
    - "ActivityFeed empty state renders 'No recent activity. We'll show events here as they happen.' verbatim per D-A-03"
    - "/dev/primitives extends with 5 new entries showing state matrix per D-Test-04"
    - "ErrorBoundary is a React 19 class component with `fallback: (err, reset) => ReactNode` prop per D-P-06"
  artifacts:
    - path: frontend/src/components/ui/card.tsx
      provides: "Card + Card.Header + Card.Body + Card.Footer with variant=surface|elevated|outline, padding=sm|md|lg per D-P-01"
      contains: "variant"
    - path: frontend/src/components/ui/stat.tsx
      provides: "Stat primitive with label/value/delta/deltaDirection/deltaIsGood/hint/icon per D-P-02 + D-S-03..05"
      contains: "deltaIsGood"
    - path: frontend/src/components/ui/stat-strip.tsx
      provides: "StatStrip responsive grid (1col/2col/Ncol) wrapping 1–6 children per D-P-03 + D-M-02"
      contains: "StatStrip"
    - path: frontend/src/components/ui/activity-feed.tsx
      provides: "ActivityFeed with items+emptyCopy, category→icon-variant mapping per D-P-04 + D-A-01..02"
      contains: "category"
    - path: frontend/src/components/ui/error-boundary.tsx
      provides: "React 19 class ErrorBoundary with fallback render-prop per D-P-06"
      contains: "componentDidCatch"
    - path: frontend/src/app/dev/primitives/page.tsx
      provides: "Extended state matrix with 5 new primitive entries per D-Test-04"
      contains: "card"
  key_links:
    - from: frontend/src/components/ui/stat-strip.tsx
      to: frontend/src/components/ui/stat.tsx
      via: "renders children — caller passes Stat elements; StatStrip handles grid breakpoints"
      pattern: "children"
    - from: frontend/src/components/ui/activity-feed.tsx
      to: lucide-react
      via: "named imports ShieldAlert, Clock, WifiOff, TrendingUp, TrendingDown per D-A-02"
      pattern: "from 'lucide-react'"
    - from: frontend/src/components/ui/stat.tsx
      to: frontend/src/styles/sunset.css
      via: "var(--color-success), var(--color-danger), var(--color-text-muted) consumption per D-S-03"
      pattern: "var\\(--color-"
---

<objective>
Ship five of the six new Phase 10 presentation primitives — Card, Stat, StatStrip, ActivityFeed, ErrorBoundary — plus extend `/dev/primitives` with state-matrix entries. These are pure-presentation primitives (no data fetching, no business logic) that Phases 11–14 will reuse verbatim. TrendChart is intentionally split into Plan 04 because of its recharts + dynamic-import complexity.

Purpose: The dashboard page (Plan 05) composes these primitives. Locking the API shape and a11y baseline here is non-negotiable for downstream phases — Stat/StatStrip/Card especially will appear on /vulnerabilities, /assets, /tickets, /cspm screens.

Output: Five new TSX files + five .test.tsx files (each with at least one axe assertion) + one extended /dev/primitives state matrix page. All primitives consume CSS variables from sunset.css — no hex literals, no admin-template patterns.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-dashboard/10-CONTEXT.md
@.planning/phases/10-dashboard/10-RESEARCH.md
@.planning/phases/10-dashboard/10-VALIDATION.md

@.claude/skills/sketch-findings-getvul/SKILL.md
@.claude/skills/sketch-findings-getvul/sources/002-dashboard-sunset/index.html
@.claude/skills/sketch-findings-getvul/references/foundation.md
@.claude/skills/sketch-findings-getvul/references/visual-language.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md

@frontend/src/styles/sunset.css
@frontend/src/components/ui/button.tsx
@frontend/src/components/ui/input.tsx
@frontend/src/components/ui/gradient-text.tsx
@frontend/src/components/ui/ToastProvider.tsx
@frontend/src/app/dev/primitives/page.tsx
@frontend/vitest.setup.ts

<interfaces>
<!-- Phase 9 primitive conventions — every new primitive matches this style. -->

Phase 9 shadcn-style CVA pattern (from button.tsx, D-19/D-20):
```tsx
'use client';
import { cva, type VariantProps } from 'class-variance-authority';
import { cn } from '@/lib/utils';

const cardVariants = cva(
  // base classes — sunset.css variables only, no hex
  'rounded-lg border transition-colors',
  {
    variants: {
      variant: {
        surface: 'border-border-subtle bg-surface',
        elevated: 'border-border bg-surface-2 shadow-card',
        outline: 'border-border bg-transparent',
      },
      padding: { sm: 'p-3', md: 'p-5', lg: 'p-7' },
    },
    defaultVariants: { variant: 'surface', padding: 'md' },
  }
);
```

CSS variables available (from sunset.css — verified):
- `--color-severity-critical`, `--color-severity-high`, `--color-severity-medium`, `--color-severity-low`
- `--color-success`, `--color-danger`, `--color-warning`, `--color-info`
- `--color-surface`, `--color-surface-2`, `--color-text`, `--color-text-muted`, `--color-text-faint`
- `--color-border`, `--color-border-subtle`
- `--color-activity-critical-tint`, `--color-activity-sla-tint`, `--color-activity-sync-tint`, `--color-activity-risk-tint` (per visual-language.md activity feed mappings — if not present, fall back to severity / status / success token map below)
- `--space-1` through `--space-12`, `--radius-sm`, `--radius-md`, `--radius-lg`
- `--motion-fast`, `--motion-base`, `--motion-slow`

If a CSS variable referenced in this plan does NOT exist in sunset.css, do NOT invent a hex. Either (a) pick the nearest existing token from foundation.md, or (b) flag the gap in the SUMMARY for a follow-up token addition.

Phase 9 test convention (from button.test.tsx — already wired):
```tsx
import { describe, it, expect } from 'vitest';
import { render } from '@testing-library/react';
import { axe } from 'vitest-axe';
import { Card } from './card';

it('has no axe violations', async () => {
  const { container } = render(<Card>content</Card>);
  expect(await axe(container)).toHaveNoViolations();
});
```

Lucide icon import style (Phase 9 D-20 — tree-shaking):
```tsx
import { ShieldAlert, Clock, WifiOff, TrendingUp, TrendingDown } from 'lucide-react';
// NEVER: import * as Icons from 'lucide-react' (pulls all 1500 icons)
```

ActivityCategory type (from Plan 02, useRecentNotifications):
```ts
export type ActivityCategory = 'new_critical_vuln' | 'sla_breach' | 'sync_failure' | 'risk_change';
export type ActivityItem = {
  id: string; category: ActivityCategory;
  title: string; body: string | null;
  occurred_at: string;
  href?: string;
};
```

D-A-01 category → icon-variant mapping (verbatim):
- `new_critical_vuln` → pink tint + ShieldAlert
- `sla_breach`        → amber tint + Clock
- `sync_failure`      → violet tint + WifiOff
- `risk_change`       → success tint + TrendingUp or TrendingDown (TrendingDown if risk decreased; this primitive picks TrendingDown by default; consumer can override)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Card + Stat + StatStrip primitives + tests</name>
  <files>frontend/src/components/ui/card.tsx, frontend/src/components/ui/stat.tsx, frontend/src/components/ui/stat-strip.tsx, frontend/src/components/ui/card.test.tsx, frontend/src/components/ui/stat.test.tsx, frontend/src/components/ui/stat-strip.test.tsx</files>
  <read_first>
    - .claude/skills/sketch-findings-getvul/sources/002-dashboard-sunset/index.html (read for the visual contract of Card/Stat/StatStrip; verify variant B)
    - .claude/skills/sketch-findings-getvul/references/foundation.md (CSS variable inventory)
    - .claude/skills/sketch-findings-getvul/references/visual-language.md (Stat tile layout, delta direction-aware coloring)
    - .claude/skills/sketch-findings-getvul/references/state-patterns.md (loading skeleton + empty / error states — every primitive MUST handle these)
    - frontend/src/styles/sunset.css (token names — DO NOT invent CSS variables)
    - frontend/src/components/ui/button.tsx (CVA pattern reference — match it)
    - frontend/src/components/ui/gradient-text.tsx (existing primitive with axe test — reference shape)
    - .planning/phases/10-dashboard/10-CONTEXT.md (D-P-01, D-P-02, D-P-03, D-S-01..05, D-M-02 mobile breakpoints)
    - .planning/phases/10-dashboard/10-RESEARCH.md (Pattern 13: Stat tile with animated number + delta — `Stat` code skeleton)
    - .planning/phases/10-dashboard/10-02-PLAN.md (`<interfaces>` block — re-use TileValue type)
  </read_first>
  <behavior>
    Test 1 (Card): Renders `<Card variant="surface">content</Card>` — DOM contains 'content', root has classes consuming `--color-surface` + `--color-border-subtle` (grep className for 'bg-surface' and 'border-border-subtle' Tailwind tokens).
    Test 2 (Card): variant='elevated' applies `bg-surface-2` and `shadow-card`.
    Test 3 (Card): padding='sm' applies `p-3`; padding='lg' applies `p-7`.
    Test 4 (Card): Card.Header, Card.Body, Card.Footer subcomponents render their children inside a Card without throwing.
    Test 5 (Card): axe-core reports 0 violations for `<Card>content</Card>`.
    Test 6 (Stat): Renders label, value, hint correctly.
    Test 7 (Stat): When delta > 0 and deltaIsGood='down' (e.g., critical_open went up — bad), the delta block has `text-danger` class AND `TrendingUp` icon is present (DOM lookup by `aria-hidden` SVG or data-testid).
    Test 7b (Stat): When `deltaSuffix='from yesterday'` (default), the delta block contains 'from yesterday'. When `deltaSuffix='from last hour'`, the delta block contains 'from last hour' (Warning 13 — prop with sensible default).
    Test 8 (Stat): When delta < 0 and deltaIsGood='down', delta block has `text-success` class AND `TrendingDown` icon present.
    Test 9 (Stat): When delta=null, the delta block renders 'Δ —' (the Pitfall 8 case) — NOT a spinner, NOT empty.
    Test 10 (Stat): When delta=0 and deltaIsGood='down', the delta block does not render an arrow (direction='flat').
    Test 11 (Stat): icon prop renders top-right when provided.
    Test 12 (Stat): value rendered with `font-mono` and `tabular-nums` classes (D-S — mono numeric).
    Test 13 (Stat): axe-core 0 violations on `<Stat label="Critical · open" value={3} delta={1} deltaIsGood="down" />`.
    Test 14 (StatStrip): Wraps 4 children in a grid. At default desktop (jsdom default), `display: grid` and grid-template-columns has 4 fr units (or Tailwind class `grid-cols-4` present).
    Test 15 (StatStrip): With 2 children, grid is 2 columns.
    Test 16 (StatStrip): Has responsive Tailwind classes `grid-cols-1 md:grid-cols-2 xl:grid-cols-4` (D-M-02 — 4 → 2 → 1).
    Test 17 (StatStrip): axe-core 0 violations.
  </behavior>
  <action>
**Card primitive (`card.tsx`)** — D-P-01:

```tsx
'use client';
import { cva, type VariantProps } from 'class-variance-authority';
import { cn } from '@/lib/utils';
import { forwardRef, type HTMLAttributes } from 'react';

const cardVariants = cva('rounded-lg border transition-colors', {
  variants: {
    variant: {
      surface: 'border-border-subtle bg-surface',
      elevated: 'border-border bg-surface-2 shadow-card',
      outline: 'border-border bg-transparent',
    },
    padding: { sm: 'p-3', md: 'p-5', lg: 'p-7' },
  },
  defaultVariants: { variant: 'surface', padding: 'md' },
});

type CardProps = HTMLAttributes<HTMLDivElement> & VariantProps<typeof cardVariants>;

const CardRoot = forwardRef<HTMLDivElement, CardProps>(({ className, variant, padding, ...props }, ref) => (
  <div ref={ref} className={cn(cardVariants({ variant, padding }), className)} {...props} />
));
CardRoot.displayName = 'Card';

const CardHeader = forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement>>(({ className, ...props }, ref) => (
  <div ref={ref} className={cn('mb-3 flex items-center justify-between', className)} {...props} />
));
CardHeader.displayName = 'Card.Header';

const CardBody = forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement>>(({ className, ...props }, ref) => (
  <div ref={ref} className={cn('', className)} {...props} />
));
CardBody.displayName = 'Card.Body';

const CardFooter = forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement>>(({ className, ...props }, ref) => (
  <div ref={ref} className={cn('mt-3 flex items-center justify-between', className)} {...props} />
));
CardFooter.displayName = 'Card.Footer';

export const Card = Object.assign(CardRoot, { Header: CardHeader, Body: CardBody, Footer: CardFooter });
export type { CardProps };
```

**Stat primitive (`stat.tsx`)** — D-P-02 + D-S-03..05; verbatim base from RESEARCH Pattern 13 with Pitfall 8 (`delta=null → Δ —`) added:

```tsx
'use client';
import type { ReactNode } from 'react';
import { TrendingUp, TrendingDown } from 'lucide-react';
import { cn } from '@/lib/utils';

export type StatProps = {
  label: string;
  value: number | string;
  delta?: number | null;
  /** When the delta direction is "good for the user". E.g., critical_open: down=good. */
  deltaIsGood?: 'up' | 'down';
  /** Suffix appended after the delta number (e.g., 'from yesterday'). Defaults sensibly so the
   * primitive stays presentation-only; consumers can override (Warning 13). */
  deltaSuffix?: string;
  hint?: string;
  icon?: ReactNode;
  className?: string;
};

export function Stat({ label, value, delta, deltaIsGood = 'down', deltaSuffix = 'from yesterday', hint, icon, className }: StatProps) {
  let body: ReactNode;
  if (delta === null || delta === undefined) {
    // Pitfall 8: render 'Δ —' for tenants without 7d of snapshot history
    body = <span className="font-mono text-xs text-text-muted">Δ —</span>;
  } else if (delta === 0) {
    body = null; // flat — no arrow, no row
  } else {
    const direction = delta > 0 ? 'up' : 'down';
    const isGood = direction === deltaIsGood;
    const Arrow = direction === 'up' ? TrendingUp : TrendingDown;
    body = (
      <span className={cn(
        'inline-flex items-center gap-1 font-mono text-xs',
        isGood ? 'text-success' : 'text-danger',
      )}>
        <Arrow className="h-3 w-3" aria-hidden="true" />
        {delta > 0 ? '+' : ''}{delta} {deltaSuffix}
      </span>
    );
  }

  return (
    <div className={cn('relative rounded-lg border border-border-subtle bg-surface p-5', className)}>
      {icon && (
        <div className="absolute right-3 top-3 grid h-6 w-6 place-items-center rounded-md text-text-muted" aria-hidden="true">
          {icon}
        </div>
      )}
      <div className="mb-2 text-xs uppercase tracking-wide text-text-muted">{label}</div>
      <div className="font-mono text-4xl font-bold leading-none tabular-nums text-text">{value}</div>
      {body !== null && <div className="mt-2">{body}</div>}
      {hint && delta === undefined && <div className="mt-2 text-xs text-text-faint">{hint}</div>}
    </div>
  );
}
```

**StatStrip primitive (`stat-strip.tsx`)** — D-P-03 + D-M-02:

```tsx
'use client';
import { Children, type ReactNode } from 'react';
import { cn } from '@/lib/utils';

export type StatStripProps = {
  children: ReactNode;
  className?: string;
};

export function StatStrip({ children, className }: StatStripProps) {
  const count = Children.count(children);
  // D-M-02: 4 → 2 → 1 col. For N<4 use N up to 4; the responsive ladder caps at 4.
  const desktop = Math.min(count, 4);
  const desktopClass =
    desktop === 1 ? 'xl:grid-cols-1' :
    desktop === 2 ? 'xl:grid-cols-2' :
    desktop === 3 ? 'xl:grid-cols-3' :
                    'xl:grid-cols-4';
  return (
    <div className={cn('grid grid-cols-1 gap-4 md:grid-cols-2', desktopClass, className)}>
      {children}
    </div>
  );
}
```

**Tests** match the `<behavior>` block above. Each test file:
1. Imports the primitive + `render` + `axe`
2. Asserts visible rendering + class names with `toHaveClass(...)` or `container.querySelector(...)`
3. Includes at least one `expect(await axe(container)).toHaveNoViolations()` block per D-Test-01

For the Stat `delta=null → 'Δ —'` test, the assertion is `expect(container.textContent).toContain('Δ —')`.

For the icon presence assertion, render with `icon={<ShieldAlert data-testid="icon" />}` and assert `getByTestId('icon')` is in the document.
  </action>
  <verify>
    <automated>cd /Users/chemencedji/Desktop/getvul/frontend && npm run test -- --run src/components/ui/card.test.tsx src/components/ui/stat.test.tsx src/components/ui/stat-strip.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Three primitive files exist with `'use client';` directive on line 1.
    - Three .test.tsx files exist; running them via `npm run test -- --run` exits 0.
    - `grep -E "toHaveNoViolations" frontend/src/components/ui/card.test.tsx frontend/src/components/ui/stat.test.tsx frontend/src/components/ui/stat-strip.test.tsx | wc -l` returns ≥ 3 (one axe assertion per file).
    - `grep -E "Δ —" frontend/src/components/ui/stat.tsx` matches ≥ 1 (Pitfall 8 / D-S-04 behavior present).
    - `grep -E "deltaIsGood" frontend/src/components/ui/stat.tsx` matches ≥ 1 (D-S-03 direction-aware API).
    - `grep -E "deltaSuffix" frontend/src/components/ui/stat.tsx` matches ≥ 1 (Warning 13 — prop with default).
    - `grep -E "grid-cols-1.*md:grid-cols-2" frontend/src/components/ui/stat-strip.tsx` matches ≥ 1 (D-M-02 ladder).
    - No hex literals in any of the three primitives: `grep -E "#[0-9a-fA-F]{3,6}" frontend/src/components/ui/card.tsx frontend/src/components/ui/stat.tsx frontend/src/components/ui/stat-strip.tsx | wc -l` returns 0.
    - Forbidden import not present: `grep -E "import \\* as.*lucide-react" frontend/src/components/ui/stat.tsx | wc -l` returns 0.
  </acceptance_criteria>
  <done>Card, Stat, StatStrip primitives ship with D-P-01..03 APIs, full direction-aware delta rendering, Pitfall 8 null handling, axe-clean, no hex literals. Phase 11+ can consume directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ActivityFeed + ErrorBoundary primitives + tests</name>
  <files>frontend/src/components/ui/activity-feed.tsx, frontend/src/components/ui/error-boundary.tsx, frontend/src/components/ui/activity-feed.test.tsx, frontend/src/components/ui/error-boundary.test.tsx</files>
  <read_first>
    - .claude/skills/sketch-findings-getvul/references/visual-language.md (activity icon-variant tints — pink/amber/violet/success)
    - .claude/skills/sketch-findings-getvul/references/copy-voice.md (relative-time format "12m ago" rules; empty state copy)
    - .planning/phases/10-dashboard/10-CONTEXT.md (D-A-01..07 activity-feed; D-P-04, D-P-06)
    - .planning/phases/10-dashboard/10-RESEARCH.md (Pattern 12: ErrorBoundary React-19 class skeleton)
    - .planning/phases/10-dashboard/10-02-PLAN.md (ActivityItem + ActivityCategory types — re-use from useRecentNotifications)
    - frontend/src/components/dashboard/microcopy.ts (after Plan 02 ships — empty-state string lives there; primitive accepts `emptyCopy` so consumer can pass `microcopy.activity.empty`)
  </read_first>
  <behavior>
    Test 1 (ActivityFeed): With 3 items, renders 3 rows containing the title text.
    Test 2 (ActivityFeed): With items=[], renders the `emptyCopy` prop string (default: "No recent activity. We'll show events here as they happen." — D-A-03 verbatim).
    Test 3 (ActivityFeed): Each category renders the correct icon (data-testid="icon-<category>"): new_critical_vuln → ShieldAlert; sla_breach → Clock; sync_failure → WifiOff; risk_change → TrendingDown (default).
    Test 4 (ActivityFeed): Each category-row root has a class consuming the tint variable (grep className for substring like 'bg-activity-critical-tint' OR 'bg-severity-critical-soft' — pick whichever exists in sunset.css).
    Test 5 (ActivityFeed): Items with `href` render an `<a>` element wrapping the row; items without `href` render a non-link row.
    Test 6 (ActivityFeed): Relative-time format renders "Xm ago", "Xh ago", or "Xd ago" based on `occurred_at` diff from now (use `Intl.RelativeTimeFormat` per RESEARCH "Don't Hand-Roll" table).
    Test 7 (ActivityFeed): axe-core 0 violations with 3 items + 1 empty state.
    Test 8 (ErrorBoundary): Renders children when no error thrown.
    Test 9 (ErrorBoundary): When a child throws, renders `fallback(error, reset)` instead of children.
    Test 10 (ErrorBoundary): Calling the `reset` function from fallback re-mounts children (state.error cleared).
    Test 11 (ErrorBoundary): axe-core 0 violations on fallback render.
  </behavior>
  <action>
**ActivityFeed primitive (`activity-feed.tsx`)** — D-P-04 + D-A-01..05:

```tsx
'use client';
import { ShieldAlert, Clock, WifiOff, TrendingUp, TrendingDown } from 'lucide-react';
import Link from 'next/link';
import { cn } from '@/lib/utils';

export type ActivityCategory = 'new_critical_vuln' | 'sla_breach' | 'sync_failure' | 'risk_change';

export type ActivityItem = {
  id: string;
  category: ActivityCategory;
  title: string;
  body?: string | null;
  occurred_at: string;  // ISO
  href?: string;
};

export type ActivityFeedProps = {
  items: ActivityItem[];
  emptyCopy?: string;
};

const CATEGORY_META: Record<ActivityCategory, { Icon: typeof ShieldAlert; tintClass: string }> = {
  new_critical_vuln: { Icon: ShieldAlert,    tintClass: 'bg-severity-critical-soft text-severity-critical' },
  sla_breach:        { Icon: Clock,          tintClass: 'bg-severity-medium-soft text-severity-medium' },
  sync_failure:      { Icon: WifiOff,        tintClass: 'bg-info-soft text-info' },
  risk_change:       { Icon: TrendingDown,   tintClass: 'bg-success-soft text-success' },
};
// NOTE: tintClass refers to Tailwind tokens that wrap CSS variables from sunset.css.
// If `*-soft` and base color tokens don't exist in tailwind.config.ts, grep tailwind.config.ts
// and adapt names. NEVER invent hex literals.

function relativeTime(iso: string): string {
  const then = new Date(iso).getTime();
  const now = Date.now();
  const diffSec = Math.round((then - now) / 1000);
  const rtf = new Intl.RelativeTimeFormat('en', { numeric: 'auto' });
  const absSec = Math.abs(diffSec);
  if (absSec < 60) return rtf.format(diffSec, 'second');
  if (absSec < 3600) return rtf.format(Math.round(diffSec / 60), 'minute');
  if (absSec < 86400) return rtf.format(Math.round(diffSec / 3600), 'hour');
  return rtf.format(Math.round(diffSec / 86400), 'day');
}

export function ActivityFeed({ items, emptyCopy = "No recent activity. We'll show events here as they happen." }: ActivityFeedProps) {
  if (items.length === 0) {
    return <p className="text-sm text-text-muted">{emptyCopy}</p>;
  }
  return (
    <ul className="space-y-3">
      {items.map((item) => {
        const meta = CATEGORY_META[item.category];
        const Icon = meta.Icon;
        const inner = (
          <div className="flex items-start gap-3">
            <div className={cn('grid h-7 w-7 place-items-center rounded-md', meta.tintClass)} aria-hidden="true">
              <Icon className="h-4 w-4" />
            </div>
            <div className="min-w-0 flex-1">
              <p className="truncate text-sm text-text">{item.title}</p>
              {item.body && <p className="truncate text-xs text-text-muted">{item.body}</p>}
              <p className="font-mono text-xs text-text-faint" suppressHydrationWarning>{relativeTime(item.occurred_at)}</p>
            </div>
          </div>
        );
        return (
          <li key={item.id} data-testid={`row-${item.category}`}>
            {item.href
              ? <Link href={item.href} className="block rounded-md hover:bg-surface-2 focus-visible:bg-surface-2 focus-visible:outline focus-visible:outline-2 focus-visible:outline-info">{inner}</Link>
              : inner}
          </li>
        );
      })}
    </ul>
  );
}
```

**ErrorBoundary primitive (`error-boundary.tsx`)** — D-P-06 + Pattern 12:

```tsx
'use client';
import { Component, type ReactNode } from 'react';

type Props = {
  children: ReactNode;
  fallback: (error: Error, reset: () => void) => ReactNode;
};
type State = { error: Error | null };

export class ErrorBoundary extends Component<Props, State> {
  state: State = { error: null };

  static getDerivedStateFromError(error: Error): State {
    return { error };
  }

  componentDidCatch(error: Error, info: unknown) {
    if (process.env.NODE_ENV !== 'production') {
      // eslint-disable-next-line no-console
      console.error('[ErrorBoundary]', error, info);
    }
  }

  reset = () => this.setState({ error: null });

  render() {
    if (this.state.error) return this.props.fallback(this.state.error, this.reset);
    return this.props.children;
  }
}
```

**Tests** (`activity-feed.test.tsx`, `error-boundary.test.tsx`):

For ActivityFeed, build 3 sample items (one per of three categories, exercising the icon mapping). Mock `Date.now()` to deterministic timestamp before testing relativeTime output. Use `getByTestId('row-new_critical_vuln')` etc.

For ErrorBoundary, build a `<Bomb />` test component that throws when prop `boom={true}`. Render `<ErrorBoundary fallback={(err, reset) => <button onClick={reset} data-testid="reset">{err.message}</button>}><Bomb boom /></ErrorBoundary>`. Assert the reset button renders. Then `act(() => fireEvent.click(getByTestId('reset')))` — but to verify reset, re-render with `boom={false}` and assert children show. (Vitest may warn about act on a class component; `act` from react is sufficient.)
  </action>
  <verify>
    <automated>cd /Users/chemencedji/Desktop/getvul/frontend && npm run test -- --run src/components/ui/activity-feed.test.tsx src/components/ui/error-boundary.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ui/activity-feed.tsx` and `error-boundary.tsx` exist with `'use client';` on line 1.
    - `grep -E "CATEGORY_META" frontend/src/components/ui/activity-feed.tsx` matches ≥ 1; all four categories appear as keys (`grep -c "new_critical_vuln\\|sla_breach\\|sync_failure\\|risk_change" frontend/src/components/ui/activity-feed.tsx` returns ≥ 4).
    - `grep -F "No recent activity. We'll show events here as they happen." frontend/src/components/ui/activity-feed.tsx` matches ≥ 1 (D-A-03 verbatim default).
    - `grep -E "Intl\\.RelativeTimeFormat" frontend/src/components/ui/activity-feed.tsx` matches ≥ 1 (no hand-rolled date math).
    - `grep -E "getDerivedStateFromError" frontend/src/components/ui/error-boundary.tsx` matches ≥ 1 (React 19 boundary correctness).
    - `grep -E "componentDidCatch" frontend/src/components/ui/error-boundary.tsx` matches ≥ 1.
    - Both test files exist; `npm run test -- --run` exits 0 on them.
    - `grep -E "toHaveNoViolations" frontend/src/components/ui/activity-feed.test.tsx frontend/src/components/ui/error-boundary.test.tsx | wc -l` returns ≥ 2 (axe assertion per file).
    - No hex literals: `grep -E "#[0-9a-fA-F]{3,6}" frontend/src/components/ui/activity-feed.tsx frontend/src/components/ui/error-boundary.tsx | wc -l` returns 0.
    - Forbidden barrel import not present: `grep -E "import \\* as.*lucide-react" frontend/src/components/ui/activity-feed.tsx | wc -l` returns 0.
  </acceptance_criteria>
  <done>ActivityFeed renders category-tinted rows with `Intl.RelativeTimeFormat` timestamps and D-A-03 verbatim empty copy; ErrorBoundary catches + resets with axe clean. Both primitives ready for Plans 05–06 consumption.</done>
</task>

<task type="auto">
  <name>Task 3: Extend /dev/primitives state matrix with the five new primitive entries</name>
  <files>frontend/src/app/dev/primitives/page.tsx</files>
  <read_first>
    - frontend/src/app/dev/primitives/page.tsx (Phase 9 baseline — match the existing entry shape for Button/Input/SsoButton/GradientText/Form/DropdownMenu)
    - .planning/phases/10-dashboard/10-CONTEXT.md (D-Test-04 — /dev/primitives is the substitute for Storybook)
    - .planning/phases/09-login-foundation/09-CONTEXT.md (D-31 — /dev/primitives gated by NODE_ENV; keep the gate intact)
    - frontend/src/components/ui/card.tsx, stat.tsx, stat-strip.tsx, activity-feed.tsx, error-boundary.tsx (the primitives this entry showcases — import each)
  </read_first>
  <action>
Open `frontend/src/app/dev/primitives/page.tsx` and append new sections to the existing state matrix. Match the existing section shape (one `<section>` per primitive, displaying default / hover / focus-visible / disabled / loading / error variants in a grid).

For each new primitive, render the state matrix below. Use sentinel data — do NOT call into TanStack hooks here (the page is a static showcase, not wired to live data).

**Card section** — D-P-01 variants:
- variant='surface' padding='md'
- variant='elevated' padding='md'
- variant='outline' padding='md'
- padding='sm' / 'md' / 'lg' row
- With Card.Header + Card.Body + Card.Footer composition example

**Stat section** — D-P-02 + D-S-03 direction matrix:
- label='Critical · open' value={3} delta={+1} deltaIsGood='down' (renders RED ▲ +1)
- label='SLA · at risk' value={12} delta={-2} deltaIsGood='down' (renders GREEN ▼ -2)
- label='MTTR · 30d' value={'4.2d'} delta={null} (renders 'Δ —')
- label='CISA KEV' value={5} delta={0} deltaIsGood='down' (flat — no arrow)
- icon prop demo (ShieldAlert)

**StatStrip section** — D-P-03 + D-M-02:
- 4-tile strip (typical /dashboard layout)
- 2-tile strip (showing column collapse)
- 1-tile strip (showing single-column fallback)

**ActivityFeed section** — D-P-04 + D-A-01..03:
- 4 items (one per category) — exercises icon-variant mapping
- Empty state: `items={[]}` — shows D-A-03 verbatim copy
- Single item with href (link variant) AND without href (non-link variant)

**ErrorBoundary section** — D-P-06:
- "Click to throw" button that flips a state to render a `<Bomb />` child
- Fallback renders an inline error block with a Retry button
- Show "Reset" demo — after reset, the bomb un-mounts

If `/dev/primitives` is gated by `NODE_ENV !== 'development'`, preserve the gate; do not remove it (D-31 from Phase 9).

Match the Phase 9 file's section heading style (likely `<h2>` per primitive name).
  </action>
  <verify>
    <automated>cd /Users/chemencedji/Desktop/getvul/frontend && npm run build 2>&1 | tail -5 && grep -c "Card\\|Stat\\|StatStrip\\|ActivityFeed\\|ErrorBoundary" src/app/dev/primitives/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from '@/components/ui/card'" frontend/src/app/dev/primitives/page.tsx` returns 1.
    - `grep -c "from '@/components/ui/stat'" frontend/src/app/dev/primitives/page.tsx` returns 1.
    - `grep -c "from '@/components/ui/stat-strip'" frontend/src/app/dev/primitives/page.tsx` returns 1.
    - `grep -c "from '@/components/ui/activity-feed'" frontend/src/app/dev/primitives/page.tsx` returns 1.
    - `grep -c "from '@/components/ui/error-boundary'" frontend/src/app/dev/primitives/page.tsx` returns 1.
    - The page contains all five primitive section headings: `grep -Ec "Card|Stat|StatStrip|ActivityFeed|ErrorBoundary" frontend/src/app/dev/primitives/page.tsx` returns ≥ 5.
    - The page contains the Pitfall 8 demo: `grep -F "delta={null}" frontend/src/app/dev/primitives/page.tsx` matches ≥ 1.
    - The page contains the D-A-03 empty state demo: `grep -F "items={[]}" frontend/src/app/dev/primitives/page.tsx` matches ≥ 1.
    - `npm run build` exits 0 (page compiles).
    - Phase 9 entries preserved: `grep -c "Button\\|Input\\|SsoButton\\|GradientText" frontend/src/app/dev/primitives/page.tsx` returns ≥ 4.
  </acceptance_criteria>
  <done>/dev/primitives renders state matrices for all five new primitives alongside the existing Phase 9 entries; serves as the project's Storybook substitute (D-Test-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ActivityItem.title / body → DOM | Server-provided strings; React's default escaping is the mitigation (no `dangerouslySetInnerHTML`) |
| ActivityItem.href → `<Link>` | Server-provided URL; restricted to internal routes by backend contract (`/dashboard/...`); external links rejected at backend |
| ErrorBoundary error message → fallback render | Error messages must NOT include stack traces or backend detail when shown to users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-16 | Tampering (XSS) | ActivityItem.title / body in ActivityFeed | mitigate | React default text escaping; NO `dangerouslySetInnerHTML` in any primitive. Verified by grep gate. |
| T-10-17 | Open-redirect | ActivityItem.href → Link | mitigate | Backend contract requires href to start with `/dashboard/` or `/`; primitive does NOT prepend or interpret. If href is an absolute URL, Next `<Link>` blocks external by default in App Router (it errors at build for unsupported protocols). Phase 14 audit-log feature handles audit row internalization. |
| T-10-18 | Information Disclosure | ErrorBoundary fallback leaks stack trace | mitigate | `componentDidCatch` logs only in `NODE_ENV !== 'production'`. Fallback receives only `Error` object; consumer (Plan 05) renders sanitized message — `<error code> · Request ID` only, per copy-voice.md. ASVS V7. |
| T-10-19 | Tampering | Hex literals slipping into primitives → bypasses sunset palette | mitigate | grep gate at acceptance: `grep -E "#[0-9a-fA-F]{3,6}" ...` returns 0. Enforces sketch-findings palette contract. |
| T-10-20 | Tree-shake bloat / DoS | `import * as Icons from 'lucide-react'` pulls 1500 icons | mitigate | Named-import-only convention; grep gate at acceptance returns 0. Phase 9 D-20 baseline. |
</threat_model>

<verification>
- Five .test.tsx files all pass: `cd frontend && npm run test -- --run src/components/ui/{card,stat,stat-strip,activity-feed,error-boundary}.test.tsx` exits 0.
- All five primitives axe-clean (each test file has a toHaveNoViolations block).
- No hex literals anywhere in the five primitives: `grep -RE "#[0-9a-fA-F]{3,6}" frontend/src/components/ui/{card,stat,stat-strip,activity-feed,error-boundary}.tsx | wc -l` returns 0.
- `/dev/primitives` renders the five new sections; `npm run build` succeeds.
- Full vitest suite remains green (no Phase 9 or Plan 02 regression): `npm run test -- --run 2>&1 | tail -3` shows 0 failures.
</verification>

<success_criteria>
- Card (with Header/Body/Footer subcomponents), Stat, StatStrip, ActivityFeed, ErrorBoundary all exist with the locked API shapes.
- Stat handles `delta=null` as 'Δ —' (Pitfall 8); StatStrip ladders 1 → 2 → N (D-M-02).
- ActivityFeed: category → icon-variant mapping correct for all 4 categories; D-A-03 empty copy verbatim; `Intl.RelativeTimeFormat` for "Xm ago".
- ErrorBoundary catches + resets; logs only in dev.
- /dev/primitives extends with 5 new sections; preserves Phase 9 entries.
- 5 test files all axe-clean; zero hex literals; zero barrel imports.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard/10-03-SUMMARY.md` covering:
- Files created (5 primitives + 5 tests + 1 extended page).
- Any sunset.css token gaps surfaced (e.g., if `--color-activity-critical-tint` didn't exist and the executor fell back to `--color-severity-critical-soft`).
- Confirmation that no hex literals were introduced.
- Note for Plan 05: import paths for downstream consumption (e.g., `import { Stat, StatStrip } from '@/components/ui'` if a barrel was added, OR direct paths).
</output>
