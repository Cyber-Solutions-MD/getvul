---
phase: 10-dashboard
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/app/layout.tsx
  - frontend/src/app/providers.tsx
  - frontend/src/app/(authed)/layout.tsx
  - frontend/src/lib/api.ts
  - frontend/src/lib/auth.tsx
  - frontend/src/lib/query-client.ts
  - frontend/src/lib/queries/keys.ts
  - frontend/src/lib/queries/use-stats.ts
  - frontend/src/lib/queries/use-trends.ts
  - frontend/src/lib/queries/use-top-triage.ts
  - frontend/src/lib/queries/use-recent-notifications.ts
  - frontend/src/lib/mutations/use-snooze.ts
  - frontend/src/lib/mutations/use-undo-snooze.ts
  - frontend/src/hooks/use-document-title.ts
  - frontend/src/hooks/use-url-state.ts
  - frontend/src/hooks/use-prefers-reduced-motion.ts
  - frontend/src/components/dashboard/microcopy.ts
  - frontend/src/components/ui/Toast.tsx
  - frontend/src/components/ui/ToastProvider.tsx
  - frontend/src/lib/queries/use-stats.test.tsx
  - frontend/src/lib/queries/use-trends.test.tsx
  - frontend/src/lib/queries/use-top-triage.test.tsx
  - frontend/src/lib/queries/use-recent-notifications.test.tsx
  - frontend/src/lib/mutations/use-snooze.test.tsx
  - frontend/src/lib/mutations/use-undo-snooze.test.tsx
  - frontend/src/hooks/use-document-title.test.ts
  - frontend/src/hooks/use-url-state.test.ts
  - frontend/src/hooks/use-prefers-reduced-motion.test.ts
  - frontend/src/components/ui/toast.test.tsx
  - frontend/src/lib/auth.logout.test.tsx
  - frontend/src/lib/api.test.ts
autonomous: true
requirements:
  - UX-02-01
  - UX-02-02
  - UX-02-03
  - UX-02-04
  - UX-02-05
  - UX-02-06
tags:
  - phase-10
  - frontend
  - tanstack-query
  - data-layer
  - hooks

must_haves:
  truths:
    - "TanStack Query v5 is installed and its QueryClientProvider wraps every route (root layout) per D-D-02 (hoisted from (authed)/layout.tsx to root so AuthProvider can call useQueryClient() safely on /login)"
    - "Query-key factory (`queryKeys.vulnerabilities.*`, `queryKeys.notifications.*`) is the single source of cache keys per D-D-03"
    - "Four query hooks exist (useStats, useTrends, useTopTriage, useRecentNotifications) with per-query staleTime + retry per D-D-06 / D-D-07"
    - "useSnoozeMutation invalidates the three keys listed in D-D-13 on success"
    - "useUndoSnoozeMutation POSTs to /api/v1/vulnerabilities/{id}/unsnooze and invalidates the same three keys per D-H-08"
    - "useAuth().logout() calls queryClient.clear() per D-D-09 — verified by unit test"
    - "api() accepts and passes through AbortSignal per RESEARCH A5 — TanStack cancels in-flight fetches"
    - "useUrlState clamps the URL param value to the allowed enum BEFORE returning, mitigating XSS-via-?range= per Pitfall 7"
    - "microcopy.ts contains the verbatim copy-voice exemplars: '1 critical CVE needs your eyes', '3 critical CVEs need your eyes', 'Top one is on prod-db-01 — Postgres path, CVSS 9.8, exploited in the wild.', 'Nothing critical right now', 'Snoozed CVE-… for 1h · Undo', 'No recent activity. We\\'ll show events here as they happen.'"
    - "microcopy.ts contains D-Ax-01 section h2 strings verbatim: stats.h2='Today at a glance', trend.h2='30-day vulnerability trend', top5.h2='Top 5 to triage', activity.h2='Recent activity'"
    - "Toast primitive supports optional `duration` (default 3000ms) and `action: { label, onClick }` slot, consumes sunset CSS variables (no raw Tailwind palette), honors prefers-reduced-motion"
  artifacts:
    - path: frontend/src/app/providers.tsx
      provides: "Client wrapper component mounting QueryClientProvider at root layout (lazy useState init)"
      contains: "QueryClientProvider"
    - path: frontend/src/app/layout.tsx
      provides: "Root layout wraps AuthProvider in <Providers> so useQueryClient() works on /login + (authed)/* alike"
      contains: "Providers"
    - path: frontend/src/lib/query-client.ts
      provides: "makeQueryClient() with global defaults (staleTime, retry, refetchOnFocus)"
      contains: "makeQueryClient"
    - path: frontend/src/lib/queries/keys.ts
      provides: "queryKeys factory (domain-first per D-D-03)"
      contains: "vulnerabilities"
    - path: frontend/src/lib/mutations/use-snooze.ts
      provides: "useSnoozeMutation with 3-key invalidation"
      contains: "invalidateQueries"
    - path: frontend/src/lib/mutations/use-undo-snooze.ts
      provides: "useUndoSnoozeMutation — POSTs /vulnerabilities/{id}/unsnooze, 3-key invalidation (D-H-08 undo path)"
      contains: "unsnooze"
    - path: frontend/src/hooks/use-url-state.ts
      provides: "Type-safe URL param sync with enum clamping (XSS mitigation)"
      contains: "useUrlState"
    - path: frontend/src/components/dashboard/microcopy.ts
      provides: "All dashboard user-facing strings extracted per copy-voice.md — includes D-Ax-01 section h2 labels"
      contains: "Today at a glance"
    - path: frontend/src/components/ui/Toast.tsx
      provides: "Toast with `duration` + `action` slot; sunset CSS variables; prefers-reduced-motion aware"
      contains: "action"
    - path: frontend/src/components/ui/ToastProvider.tsx
      provides: "ToastProvider accepting `duration` + `action` on toast() input, backward compat with Phase 9 callers"
      contains: "duration"
    - path: frontend/src/lib/auth.tsx
      provides: "logout() also calls queryClient.clear() — D-D-09"
      contains: "queryClient.clear"
  key_links:
    - from: frontend/src/app/layout.tsx
      to: frontend/src/app/providers.tsx
      via: "<Providers><AuthProvider>{children}</AuthProvider></Providers> at root so /login + (authed)/* share one cache"
      pattern: "Providers"
    - from: frontend/src/app/providers.tsx
      to: frontend/src/lib/query-client.ts
      via: "useState(() => makeQueryClient()) lazy init per RESEARCH Pattern 1 (Pitfall 1 mitigation)"
      pattern: "useState\\(\\s*\\(\\)\\s*=>\\s*makeQueryClient"
    - from: frontend/src/lib/mutations/use-snooze.ts
      to: frontend/src/lib/queries/keys.ts
      via: "invalidateQueries({ queryKey: queryKeys.vulnerabilities.* })"
      pattern: "queryKeys\\.vulnerabilities"
    - from: frontend/src/lib/mutations/use-undo-snooze.ts
      to: frontend/src/lib/queries/keys.ts
      via: "invalidateQueries({ queryKey: queryKeys.vulnerabilities.* }) — same 3-key set"
      pattern: "queryKeys\\.vulnerabilities"
    - from: frontend/src/lib/auth.tsx
      to: frontend/src/lib/query-client.ts
      via: "useQueryClient().clear() in logout callback per D-D-09"
      pattern: "queryClient\\.clear\\(\\)"
    - from: frontend/src/lib/queries/use-stats.ts
      to: frontend/src/lib/api.ts
      via: "api(path, { signal }) — AbortSignal pass-through per RESEARCH Pattern 5 / A5"
      pattern: "signal"
---

<objective>
Install TanStack Query v5 and scaffold the entire frontend data layer + supporting hooks that Phase 10 page composition (Plans 03–06) consumes. This is pure plumbing: query client, four query hooks, two mutation hooks (snooze + undo-snooze), three utility hooks, a microcopy module, a root-layout wire-up, a logout cache-clear, and a Toast primitive extension (duration + action slot). No page-level composition or styling yet.

Purpose: Phases 11–14 reuse this data layer verbatim. Locking the patterns now (query-key shape, retry policy, staleTime, AbortSignal pass-through, cache-clear-on-logout, URL-state clamping, toast-with-undo) means future phases compose against a known surface instead of inventing.

Output: One new dependency (@tanstack/react-query@^5), 16 new TypeScript files, 3 modified TypeScript files (api.ts, auth.tsx, app/layout.tsx), 1 new client wrapper (app/providers.tsx), Toast + ToastProvider extended (additive — Phase 9 callers continue to work), 12 vitest test files, all green, with axe-core where DOM is involved.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dashboard/10-CONTEXT.md
@.planning/phases/10-dashboard/10-RESEARCH.md
@.planning/phases/10-dashboard/10-VALIDATION.md
@.planning/phases/09-login-foundation/09-CONTEXT.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md

@frontend/src/app/layout.tsx
@frontend/src/app/(authed)/layout.tsx
@frontend/src/lib/api.ts
@frontend/src/lib/auth.tsx
@frontend/src/components/ui/Toast.tsx
@frontend/src/components/ui/ToastProvider.tsx
@frontend/package.json
@frontend/vitest.setup.ts

<interfaces>
<!-- Existing contracts the executor consumes. Extracted from RESEARCH.md Pattern sections.
     Do NOT re-explore the codebase — these are the load-bearing facts. -->

Existing api() wrapper (frontend/src/lib/api.ts — Phase 9 baseline):
```ts
export type FetchOptions = {
  method?: string;
  body?: string | FormData;
  headers?: Record<string, string>;
  // Phase 10 extends with: signal?: AbortSignal
};
export async function api<T = unknown>(path: string, options?: FetchOptions): Promise<T>;
// Internal behavior (verified): attaches Bearer token, on 401 calls tryRefreshToken()
// → retry once → on retry failure redirects to /login and throws "Session expired".
// One-line Phase 10 change: pass options.signal through to fetch().
```

Existing useAuth (frontend/src/lib/auth.tsx — Phase 9):
```ts
const { token, user, login, logout, loginSSO } = useAuth();
// logout() currently: fetch POST /auth/logout, clearAuth(), router.replace('/login').
// Phase 10 adds: qc.clear() between clearAuth and router.replace.
```

Existing root layout (frontend/src/app/layout.tsx — Phase 9):
```tsx
// Mounts ThemeProvider > AuthProvider > {children} (so AuthProvider wraps BOTH /login and (authed)/*).
// Phase 10 wraps AuthProvider in a new <Providers> client component that owns QueryClientProvider.
// Why: useAuth().logout() calls useQueryClient(); /login is OUTSIDE (authed)/* so the provider
// must live above AuthProvider in the tree, not inside (authed)/layout.tsx.
```

Existing ToastProvider (frontend/src/components/ui/ToastProvider.tsx — Phase 9):
```ts
// Current API: useToast() → { toast: (input: { title?, message, variant? }) => void }
// Phase 9 callers (e.g., login error toasts) pass { message, variant } only — MUST keep working.
// Phase 10 extends `ToastInput` with optional `duration?: number` (default 3000) and
// optional `action?: { label: string, onClick: () => void }` — backward compatible additions.
```

Existing Toast (frontend/src/components/ui/Toast.tsx — Phase 9):
```ts
// Current: hardcoded 3000ms timeout, raw Tailwind palette (border-emerald-500/40, bg-gray-900,
// text-red-400, text-indigo-400 etc.).
// Phase 10 changes (additive — no Phase 9 caller breakage):
//   1. Accept optional `duration` (default 3000) and `action` props.
//   2. Replace raw Tailwind palette with sunset CSS variables consumed via project Tailwind tokens
//      (bg-surface-elevated → var(--color-surface-elevated); border-border-subtle; text-success;
//      text-text-secondary). Match Phase 9 file naming convention (kebab-case Tailwind tokens
//      that wrap CSS variables in tailwind.config.ts).
//   3. Render an action button when `action` is present — visually matches copy-voice.md inline
//      action style (ghost button or underlined link, no separate container).
//   4. Honor prefers-reduced-motion — instant fade (no slide) when reduced.
//   5. Maintain the existing dismiss-on-X behavior + auto-dismiss timer (parameterized by duration).
```

Existing AppShell (frontend/src/components/shell/app-shell.tsx — Phase 9): client component, no data fetching today. Phase 10 leaves it untouched in this plan (Plan 06 wires the sidebar nav-chip counts).

Phase 9 file naming convention (D-29):
- `components/ui/` is FLAT — `button.tsx`, `input.tsx`, `dropdown-menu.tsx` (kebab-case).
- Phase 10 follows this for new lib files: `query-client.ts`, `use-stats.ts`, `use-snooze.ts`, `use-undo-snooze.ts`.
- NOTE: existing Phase 9 files `Toast.tsx` / `ToastProvider.tsx` keep their PascalCase names (renaming would require touching every Phase 9 import site — out of scope for Phase 10 plumbing).

Phase 9 test convention (D-30):
- One `.test.tsx` per primitive/hook.
- vitest-axe imported in `vitest.setup.ts` (already wired: `import 'vitest-axe/extend-expect'`).
- Use `import { axe } from 'vitest-axe'` and `expect(container).toHaveNoViolations()`.

Backend shapes (locked by Plan 01 — read 10-01-PLAN.md before locking types):
```ts
// /api/v1/vulnerabilities/stats response
type DashboardStatsResponse = {
  // existing v1 fields preserved
  dashboard_tiles: {
    critical_open: { value: number; delta: number | null; delta_direction: 'up'|'down'|'flat' | null };
    sla_at_risk:   { value: number; delta: number | null; delta_direction: 'up'|'down'|'flat' | null };
    kev:           { value: number; delta: number | null; delta_direction: 'up'|'down'|'flat' | null };
    mttr_30d:      { value: number | string; delta: null; delta_direction: null };  // value union: int OR formatted string '4.2d' per Plan 01
  };
  top_vuln: { id: string; cve_id: string; host: string; path: string; cvss: number; on_kev: boolean; exploited: boolean } | null;  // NOTE: id (vuln UUID) is REQUIRED — drives snooze + undo
  vuln_open_count: number;
  asset_total_count: number;
  ticket_open_count: number;
  onboarding_state: 'no_scanners' | 'no_data_yet' | 'ready';
};

// /api/v1/vulnerabilities/trends?days=N response (NEW field highlighted)
type TrendsResponse = {
  vuln_trends: { period_days: number; timeline: Array<{ date: string; new: number; resolved: number; net: number; by_severity: Record<string, number> }>; totals: { new: number; resolved: number } };
  mttr_trend: unknown;
  risk_trend: unknown;
  severity_trends: Record<string, { critical: number; high: number; medium: number; low: number }>;  // D-C-09
};

// /api/v1/vulnerabilities?sort=triage&limit=5 response (existing shape, same as base list)
type TopTriageResponse = {
  items: Array<{
    id: string; cve_id: string; severity: 'CRITICAL'|'HIGH'|'MEDIUM'|'LOW';
    host: string; cvss_v3_score: number | null;
    cisa_kev: boolean; sla_due_at: string | null;
    /* … other fields … */
  }>;
  total: number;
};

// /api/v1/notifications?page=1&page_size=5 response (Phase 9 baseline — UNCHANGED)
type RecentNotificationsResponse = {
  items: Array<{
    id: string; category: 'new_critical_vuln'|'sla_breach'|'sync_failure'|'risk_change';
    title: string; body: string | null;
    occurred_at: string;  // ISO
    href?: string;
  }>;
  total: number;
};
```

Verbatim copy-voice strings (from CONTEXT.md `<specifics>` and copy-voice.md — DO NOT REPHRASE):
- Headline singular: `1 critical CVE needs your eyes`
- Headline plural: `N critical CVEs need your eyes` (where N is rendered with no thousands separator below 1000)
- Sub-line shape: `Top one is on prod-db-01 — Postgres path, CVSS 9.8, exploited in the wild.`
- Quiet-win: `Nothing critical right now`
- Snooze toast: `Snoozed CVE-2024-… for 1h · Undo` (8s auto-dismiss; rendered via Toast `action` slot, not embedded in `message` string)
- Error pattern: `Trend unavailable. HTTP 503 · Request ID req_abc123 · Retry now`
- Activity empty: `No recent activity. We'll show events here as they happen.`
- D-Ax-01 section h2 labels (LOCKED): `Today at a glance` (stat strip), `30-day vulnerability trend` (chart), `Top 5 to triage` (top-5 card), `Recent activity` (activity rail). Hero h2 = the rendered headline string itself.
- FORBIDDEN: `Please`, `Welcome`, `Click here`, exclamation marks, generic SaaS lines.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave-0 RED): Extend Toast primitive to support `duration` + `action` slot + sunset CSS variables — RED test first</name>
  <files>frontend/src/components/ui/toast.test.tsx, frontend/src/components/ui/Toast.tsx, frontend/src/components/ui/ToastProvider.tsx</files>
  <read_first>
    - frontend/src/components/ui/Toast.tsx (Phase 9 baseline — hardcoded 3s, raw Tailwind palette, no action slot)
    - frontend/src/components/ui/ToastProvider.tsx (Phase 9 API: `toast({title?, message, variant?})`)
    - .claude/skills/sketch-findings-getvul/references/copy-voice.md (inline action style: `Snoozed CVE-… for 1h · [Undo]` — the bracketed Undo is the action, NOT part of the message string)
    - .claude/skills/sketch-findings-getvul/references/foundation.md (CSS variable inventory: `--color-surface-elevated`, `--color-border-subtle`, `--color-success`, `--color-text-secondary`)
    - frontend/tailwind.config.ts (grep for existing `surface-elevated`, `text-secondary` tokens — verify Phase 9 wired them; if not, use `bg-surface-2`, `text-text-muted` as Phase 9's nearest equivalents)
    - .planning/phases/10-dashboard/10-CONTEXT.md (D-H-08 — Snooze toast with 8s undo window)
  </read_first>
  <behavior>
    Test 1 (default duration unchanged): Render `<Toast id="x" message="hi" onDismiss={fn} />`; advance fake timers by 2999ms — fn NOT called; advance to 3000ms — fn called (Phase 9 backward compat).
    Test 2 (custom duration honored): Render `<Toast id="x" message="hi" duration={8000} onDismiss={fn} />`; advance 3000ms — fn NOT called; advance to 8000ms — fn called (D-H-08).
    Test 3 (action button renders + onClick fires): Render `<Toast id="x" message="Snoozed" action={{ label: 'Undo', onClick: actionFn }} onDismiss={fn} />`; `getByRole('button', { name: /undo/i })` is in the document; clicking it calls actionFn exactly once.
    Test 4 (variant=success consumes --color-success): Render `<Toast id="x" message="ok" variant="success" onDismiss={fn} />`; assert the rendered root has a class consuming the sunset success token (e.g., Tailwind class `text-success` or `border-success` whose underlying value is `var(--color-success)` per tailwind.config.ts). At minimum: `container.querySelector('[class*="success"]')` is not null. Forbidden: raw `text-emerald-400` or `border-emerald-500/40` literals.
    Test 5 (ToastProvider passes duration + action through): Wrap a `useToast` consumer; call `toast({ message: 'Snoozed', duration: 8000, action: { label: 'Undo', onClick: fn } })`; the rendered Toast receives both props (verify by asserting the rendered tree has an Undo button AND that after 8000ms the toast disappears).
    Test 6 (prefers-reduced-motion: instant fade): Mock `window.matchMedia` to return `matches: true` for `(prefers-reduced-motion: reduce)`. Render a Toast. Assert the rendered root has NO `transition-all` / `duration-200` class OR has explicit `motion-reduce:transition-none` token (whichever the implementation chooses). Either approach is acceptable — the goal is that animations don't run in reduce-motion.
    Test 7 (axe-core 0 violations): With a Toast that has both message and action, axe reports 0 violations.
  </behavior>
  <action>
**Step A — write the failing test file first** (`frontend/src/components/ui/toast.test.tsx`):

```tsx
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { render, screen, act, fireEvent } from '@testing-library/react';
import { axe } from 'vitest-axe';
import Toast from './Toast';
import ToastProvider, { useToast } from './ToastProvider';

describe('Toast primitive (Phase 10 extension)', () => {
  beforeEach(() => { vi.useFakeTimers(); });
  afterEach(() => { vi.useRealTimers(); });

  it('defaults to 3000ms duration (Phase 9 backward compat)', () => {
    const fn = vi.fn();
    render(<Toast id="x" message="hi" onDismiss={fn} />);
    act(() => { vi.advanceTimersByTime(2999); });
    expect(fn).not.toHaveBeenCalled();
    act(() => { vi.advanceTimersByTime(1 + 250 /* fade-out window */); });
    expect(fn).toHaveBeenCalled();
  });

  it('honors custom duration (D-H-08 — 8s undo window)', () => {
    const fn = vi.fn();
    render(<Toast id="x" message="Snoozed" duration={8000} onDismiss={fn} />);
    act(() => { vi.advanceTimersByTime(3000); });
    expect(fn).not.toHaveBeenCalled();
    act(() => { vi.advanceTimersByTime(5000 + 250); });
    expect(fn).toHaveBeenCalled();
  });

  it('renders an action button and fires onClick (D-H-08)', () => {
    const actionFn = vi.fn();
    render(<Toast id="x" message="Snoozed CVE-…" action={{ label: 'Undo', onClick: actionFn }} onDismiss={vi.fn()} />);
    const btn = screen.getByRole('button', { name: /undo/i });
    fireEvent.click(btn);
    expect(actionFn).toHaveBeenCalledTimes(1);
  });

  it('variant=success consumes the sunset success token (no raw Tailwind palette)', () => {
    const { container } = render(<Toast id="x" message="ok" variant="success" onDismiss={vi.fn()} />);
    // Inspect outerHTML for the success token class. Forbid emerald-/red-/indigo- raw palette.
    const html = container.innerHTML;
    expect(html).toMatch(/(text-success|border-success|bg-success)/);
    expect(html).not.toMatch(/emerald-500\/40|emerald-400|red-500\/40|red-400|indigo-500\/40|indigo-400|bg-gray-900/);
  });

  it('ToastProvider passes duration + action through to Toast', () => {
    const actionFn = vi.fn();
    function Trigger() {
      const { toast } = useToast();
      return <button onClick={() => toast({ message: 'Snoozed', duration: 8000, action: { label: 'Undo', onClick: actionFn } })}>fire</button>;
    }
    render(<ToastProvider><Trigger /></ToastProvider>);
    fireEvent.click(screen.getByRole('button', { name: /fire/i }));
    expect(screen.getByRole('button', { name: /undo/i })).toBeInTheDocument();
  });

  it('has no axe violations with action present', async () => {
    const { container } = render(<Toast id="x" message="Snoozed" action={{ label: 'Undo', onClick: vi.fn() }} onDismiss={vi.fn()} />);
    expect(await axe(container)).toHaveNoViolations();
  });
});
```

**Step B — extend `Toast.tsx`** (additive, backward compat preserved):

```tsx
'use client';
import { useEffect, useState } from 'react';
import { X } from 'lucide-react';

export interface ToastAction {
  label: string;
  onClick: () => void;
}

export interface ToastData {
  id: string;
  title?: string;
  message: string;
  variant?: 'success' | 'error' | 'info';
  duration?: number;             // NEW — defaults to 3000ms (Phase 9 backward compat)
  action?: ToastAction;          // NEW — optional inline action (e.g., Undo)
}

interface ToastProps extends ToastData {
  onDismiss: (id: string) => void;
}

export default function Toast({ id, title, message, variant = 'info', duration = 3000, action, onDismiss }: ToastProps) {
  const [visible, setVisible] = useState(false);

  useEffect(() => {
    requestAnimationFrame(() => setVisible(true));
    const timer = setTimeout(() => {
      setVisible(false);
      setTimeout(() => onDismiss(id), 200);
    }, duration);
    return () => clearTimeout(timer);
  }, [id, duration, onDismiss]);

  // Sunset CSS variable tokens (consumed via tailwind.config.ts mapping):
  //   --color-surface-elevated → bg-surface-2 (Phase 9 nearest token; verify exact name)
  //   --color-border-subtle    → border-border-subtle
  //   --color-success          → text-success / border-success
  //   --color-danger           → text-danger / border-danger
  //   --color-info             → text-info / border-info
  //   --color-text-secondary   → text-text-muted (Phase 9 nearest token)
  const accentClass =
    variant === 'success' ? 'text-success'
    : variant === 'error' ? 'text-danger'
    : 'text-info';
  const borderClass =
    variant === 'success' ? 'border-success/40'
    : variant === 'error' ? 'border-danger/40'
    : 'border-info/40';

  return (
    <div
      className={[
        'pointer-events-auto w-80 rounded-lg border p-4 shadow-card',
        'bg-surface-2',  // var(--color-surface-elevated) via tailwind.config.ts
        borderClass,
        'transition-all duration-200 motion-reduce:transition-none',
        visible ? 'translate-x-0 opacity-100' : 'translate-x-4 opacity-0 motion-reduce:translate-x-0',
      ].join(' ')}
      role={variant === 'error' ? 'alert' : 'status'}
    >
      <div className="flex items-start justify-between gap-3">
        <div className="min-w-0">
          {title && <p className={`text-sm font-medium ${accentClass}`}>{title}</p>}
          <p className={`text-sm text-text-muted ${title ? 'mt-0.5' : ''}`}>{message}</p>
          {action && (
            <button
              type="button"
              onClick={action.onClick}
              className={`mt-2 text-xs font-medium underline underline-offset-2 ${accentClass} hover:opacity-80 focus-visible:outline focus-visible:outline-2 focus-visible:outline-info`}
            >
              {action.label}
            </button>
          )}
        </div>
        <button
          type="button"
          aria-label="Dismiss"
          onClick={() => { setVisible(false); setTimeout(() => onDismiss(id), 200); }}
          className="shrink-0 text-text-muted hover:text-text transition"
        >
          <X className="h-4 w-4" />
        </button>
      </div>
    </div>
  );
}
```

If `bg-surface-2`, `border-success`, `border-danger`, `border-info`, `text-success`, `text-danger`, `text-info`, `text-text-muted`, `text-text` Tailwind utilities do NOT exist in the project's `tailwind.config.ts`, EITHER (a) add them to map onto the sunset CSS variables in foundation.md, OR (b) use `[--token]` arbitrary-value Tailwind syntax like `bg-[var(--color-surface-elevated)]`. Pick whichever matches Phase 9's existing pattern (grep `frontend/src/components/ui/button.tsx` for the convention).

**Step C — extend `ToastProvider.tsx`** (additive to `ToastInput`):

```tsx
'use client';
import { createContext, useCallback, useContext, useState } from 'react';
import Toast, { type ToastData, type ToastAction } from './Toast';

interface ToastInput {
  title?: string;
  message: string;
  variant?: 'success' | 'error' | 'info';
  duration?: number;        // NEW
  action?: ToastAction;     // NEW
}

interface ToastContextValue {
  toast: (input: ToastInput) => void;
}

const ToastContext = createContext<ToastContextValue>({ toast: () => {} });
export function useToast() { return useContext(ToastContext); }

let toastCounter = 0;

export default function ToastProvider({ children }: { children: React.ReactNode }) {
  const [toasts, setToasts] = useState<ToastData[]>([]);

  const addToast = useCallback((input: ToastInput) => {
    const id = `toast-${++toastCounter}-${Date.now()}`;
    setToasts((prev) => [...prev, { id, ...input }]);
  }, []);

  const dismissToast = useCallback((id: string) => {
    setToasts((prev) => prev.filter((t) => t.id !== id));
  }, []);

  return (
    <ToastContext.Provider value={{ toast: addToast }}>
      {children}
      <div className="fixed top-4 right-4 z-[60] flex flex-col gap-2 pointer-events-none">
        {toasts.map((t) => (<Toast key={t.id} {...t} onDismiss={dismissToast} />))}
      </div>
    </ToastContext.Provider>
  );
}
```

This is fully backward compatible: Phase 9 callers that pass only `{title?, message, variant?}` continue to work because `duration` defaults to 3000 and `action` is optional.
  </action>
  <verify>
    <automated>cd /Users/chemencedji/Desktop/getvul/frontend && npm run test -- --run src/components/ui/toast.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ui/toast.test.tsx` exists; 6+ tests pass under `npm run test -- --run`.
    - `grep -E "duration\\?:\\s*number" frontend/src/components/ui/Toast.tsx` matches ≥ 1.
    - `grep -E "action\\?:\\s*(ToastAction|\\{)" frontend/src/components/ui/Toast.tsx` matches ≥ 1.
    - `grep -E "(emerald-500/40|emerald-400|red-500/40|red-400|indigo-500/40|indigo-400|bg-gray-900)" frontend/src/components/ui/Toast.tsx | wc -l` returns 0 (raw palette purged).
    - `grep -E "motion-reduce" frontend/src/components/ui/Toast.tsx` matches ≥ 1 (prefers-reduced-motion respected).
    - `grep -E "duration\\?:\\s*number" frontend/src/components/ui/ToastProvider.tsx` matches ≥ 1.
    - `grep -E "action\\?:" frontend/src/components/ui/ToastProvider.tsx` matches ≥ 1.
    - Phase 9 toast call sites still compile: `cd frontend && npx tsc --noEmit` exits 0 (no breaking type changes).
    - Full vitest suite still green: existing Phase 9 toast tests (if any) pass without modification.
  </acceptance_criteria>
  <done>Toast supports duration + action; sunset CSS variables replace raw Tailwind palette; prefers-reduced-motion honored; backward-compatible with Phase 9 callers. D-H-08 toast surface is ready for Plan 05's Hero consumption.</done>
</task>

<task type="auto">
  <name>Task 1: Install TanStack Query v5; create query-client, query-key factory, four query hooks, snooze + undo-snooze mutations, AbortSignal pass-through</name>
  <files>frontend/package.json, frontend/src/lib/api.ts, frontend/src/lib/query-client.ts, frontend/src/lib/queries/keys.ts, frontend/src/lib/queries/use-stats.ts, frontend/src/lib/queries/use-trends.ts, frontend/src/lib/queries/use-top-triage.ts, frontend/src/lib/queries/use-recent-notifications.ts, frontend/src/lib/mutations/use-snooze.ts, frontend/src/lib/mutations/use-undo-snooze.ts, frontend/src/lib/queries/use-stats.test.tsx, frontend/src/lib/queries/use-trends.test.tsx, frontend/src/lib/queries/use-top-triage.test.tsx, frontend/src/lib/queries/use-recent-notifications.test.tsx, frontend/src/lib/mutations/use-snooze.test.tsx, frontend/src/lib/mutations/use-undo-snooze.test.tsx, frontend/src/lib/api.test.ts</files>
  <read_first>
    - frontend/src/lib/api.ts (Phase 9 wrapper — preserve 401 retry logic; add signal pass-through only)
    - frontend/package.json (confirm @tanstack/react-query is NOT already present)
    - .planning/phases/10-dashboard/10-RESEARCH.md (Patterns 1–6: makeQueryClient, key factory, useQuery overrides, useMutation, 401 reuse, queryClient.clear)
    - .planning/phases/10-dashboard/10-CONTEXT.md (D-D-01..13 — all 13 data-layer decisions; D-H-08 — undo path)
    - .planning/phases/10-dashboard/10-VALIDATION.md (Wave 0 files list — verify every test path below matches verbatim)
    - .planning/phases/10-dashboard/10-01-PLAN.md (`<interfaces>` block + must_haves — locks the backend response shapes; verify `top_vuln.id` (UUID) field is present, verify `/vulnerabilities/{id}/unsnooze` route is present)
  </read_first>
  <action>
**Step A — install:**
```bash
cd /Users/chemencedji/Desktop/getvul/frontend
npm install @tanstack/react-query@^5
npm install --save-dev @tanstack/react-query-devtools@^5
```
Record the exact installed version (`npm view @tanstack/react-query version` output) in the SUMMARY at end of plan.

**Step B — `frontend/src/lib/api.ts` — single one-line change (Pattern 5 + Assumption A5):**

Extend the FetchOptions type with `signal?: AbortSignal` and pass it through to the underlying `fetch()`. Do NOT alter the 401 refresh-retry path. Do NOT alter any other behavior. The diff should be ≤ 3 lines.

```ts
export type FetchOptions = {
  method?: string;
  body?: string | FormData;
  headers?: Record<string, string>;
  signal?: AbortSignal;  // NEW — Phase 10 (D-D / RESEARCH Pattern 5)
};

// Inside the fetch call (both initial and retry-after-refresh sites):
const res = await fetch(url, { method, headers: builtHeaders, body, signal: options?.signal });
```

If `api.ts` has two `fetch()` call sites (initial + retry), pass `signal` to both. If the retry inherits options via spread already, confirm signal is included.

**Step C — `frontend/src/lib/query-client.ts` (NEW):**
```ts
import { QueryClient } from '@tanstack/react-query';

export function makeQueryClient() {
  return new QueryClient({
    defaultOptions: {
      queries: {
        staleTime: 60_000,            // D-D-06 base — per-query overrides allowed
        retry: 0,                      // D-D-07 default — useStats overrides to 1
        refetchOnWindowFocus: true,   // D-D-12 cross-tab sync via focus
        refetchOnReconnect: true,
      },
      mutations: { retry: 0 },
    },
  });
}
```

**Step D — `frontend/src/lib/queries/keys.ts` (NEW — D-D-03 domain-first):**
```ts
export const queryKeys = {
  vulnerabilities: {
    all: ['vulnerabilities'] as const,
    stats: () => ['vulnerabilities', 'stats'] as const,
    trends: (range: '7d' | '30d' | '90d') =>
      ['vulnerabilities', 'trends', { range }] as const,
    topTriage: (limit = 5) =>
      ['vulnerabilities', 'top-triage', { limit }] as const,
    dashboardTiles: () => ['vulnerabilities', 'dashboard-tiles'] as const,
  },
  notifications: {
    all: ['notifications'] as const,
    recent: (limit = 5) => ['notifications', 'recent', { limit }] as const,
  },
} as const;
```

**Step E — four query hooks.** Each is short and follows the same shape.

`frontend/src/lib/queries/use-stats.ts`:
```ts
import { useQuery } from '@tanstack/react-query';
import { api } from '@/lib/api';
import { queryKeys } from './keys';

export type TileValue = { value: number | string; delta: number | null; delta_direction: 'up'|'down'|'flat' | null };
// NOTE: value is number | string because mttr_30d is formatted server-side as e.g. "4.2d" (Plan 01 Open Q2).
// Critical-open / sla-at-risk / kev are always numbers; mttr is always string. TileValue.value union is intentional.

export type DashboardStatsResponse = {
  dashboard_tiles: {
    critical_open: TileValue; sla_at_risk: TileValue; kev: TileValue; mttr_30d: TileValue;
  };
  top_vuln: { id: string; cve_id: string; host: string; path: string; cvss: number; on_kev: boolean; exploited: boolean } | null;
  vuln_open_count: number;
  asset_total_count: number;
  ticket_open_count: number;
  onboarding_state: 'no_scanners' | 'no_data_yet' | 'ready';
};

export function useStats() {
  return useQuery({
    queryKey: queryKeys.vulnerabilities.stats(),
    queryFn: ({ signal }) =>
      api<DashboardStatsResponse>('/api/v1/vulnerabilities/stats', { signal }),
    staleTime: 60_000,           // D-D-06
    retry: 1,                     // D-D-07: 1× on 5xx for stats (most visible)
    refetchOnWindowFocus: true,  // D-D-06 explicit (matches default; documents intent)
  });
}
```

`frontend/src/lib/queries/use-trends.ts`:
```ts
import { useQuery } from '@tanstack/react-query';
import { api } from '@/lib/api';
import { queryKeys } from './keys';

export type Range = '7d' | '30d' | '90d';
export type TrendsResponse = {
  severity_trends: Record<string, { critical: number; high: number; medium: number; low: number }>;
};

export function useTrends(range: Range = '30d') {
  const days = range === '7d' ? 7 : range === '90d' ? 90 : 30;
  return useQuery({
    queryKey: queryKeys.vulnerabilities.trends(range),  // key includes range param per D-D-03
    queryFn: ({ signal }) =>
      api<TrendsResponse>(`/api/v1/vulnerabilities/trends?days=${days}`, { signal }),
    staleTime: 60_000,           // D-D-06
    retry: 1,                     // D-D-07: trends is dashboard-tiles-tier (most-visible)
    refetchOnWindowFocus: false,  // chart redraw on alt-tab is jarring
  });
}
```

(Note: D-D-07 retry policy text says "1× retry on 5xx for /stats + dashboard-tiles queries"; trends is part of the dashboard-tiles tier per the spirit of the decision. If checker reads this differently, change to `retry: 0` — both are defensible.)

`frontend/src/lib/queries/use-top-triage.ts`:
```ts
import { useQuery } from '@tanstack/react-query';
import { api } from '@/lib/api';
import { queryKeys } from './keys';

export type TriageRow = {
  id: string; cve_id: string;
  severity: 'CRITICAL' | 'HIGH' | 'MEDIUM' | 'LOW';
  host: string; cvss_v3_score: number | null;
  cisa_kev: boolean; sla_due_at: string | null;
};
export type TopTriageResponse = { items: TriageRow[]; total: number };

export function useTopTriage(limit = 5) {
  return useQuery({
    queryKey: queryKeys.vulnerabilities.topTriage(limit),
    queryFn: ({ signal }) =>
      api<TopTriageResponse>(`/api/v1/vulnerabilities?sort=triage&limit=${limit}`, { signal }),
    staleTime: 60_000,
    retry: 0,
  });
}
```

`frontend/src/lib/queries/use-recent-notifications.ts`:
```ts
import { useQuery } from '@tanstack/react-query';
import { api } from '@/lib/api';
import { queryKeys } from './keys';

export type ActivityCategory = 'new_critical_vuln'|'sla_breach'|'sync_failure'|'risk_change';
export type ActivityItem = {
  id: string; category: ActivityCategory;
  title: string; body: string | null;
  occurred_at: string;
  href?: string;
};
export type RecentNotificationsResponse = { items: ActivityItem[]; total: number };

export function useRecentNotifications() {
  return useQuery({
    queryKey: queryKeys.notifications.recent(5),
    queryFn: ({ signal }) =>
      api<RecentNotificationsResponse>('/api/v1/notifications?page=1&page_size=5', { signal }),
    staleTime: 30_000,  // D-D-06: 30s for notifications
    retry: 0,
  });
}
```

**Step F — snooze + undo-snooze mutations.**

`frontend/src/lib/mutations/use-snooze.ts` (D-D-13):
```ts
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { api } from '@/lib/api';
import { queryKeys } from '@/lib/queries/keys';

type SnoozeArgs = { id: string; until?: string };

export function useSnoozeMutation() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: ({ id, until }: SnoozeArgs) =>
      api<{ message: string; until: string }>(`/api/v1/vulnerabilities/${id}/snooze`, {
        method: 'POST',
        body: JSON.stringify({ until }),
        headers: { 'Content-Type': 'application/json' },
      }),
    onSuccess: async () => {
      // D-D-13 verbatim: invalidate 3 keys
      await Promise.all([
        qc.invalidateQueries({ queryKey: queryKeys.vulnerabilities.stats() }),
        qc.invalidateQueries({ queryKey: queryKeys.vulnerabilities.dashboardTiles() }),
        qc.invalidateQueries({ queryKey: queryKeys.vulnerabilities.all }),
      ]);
    },
  });
}
```

`frontend/src/lib/mutations/use-undo-snooze.ts` (NEW — D-H-08 reverse path):
```ts
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { api } from '@/lib/api';
import { queryKeys } from '@/lib/queries/keys';

type UndoSnoozeArgs = { id: string };

export function useUndoSnoozeMutation() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: ({ id }: UndoSnoozeArgs) =>
      // Backend semantics (per Plan 01 Task 3 + recommendation): a separate
      // POST /vulnerabilities/{id}/unsnooze endpoint that emits a `vuln.unsnooze`
      // audit event and resets status to OPEN. Symmetric to snooze for audit clarity.
      api<{ message: string }>(`/api/v1/vulnerabilities/${id}/unsnooze`, {
        method: 'POST',
        body: JSON.stringify({}),
        headers: { 'Content-Type': 'application/json' },
      }),
    onSuccess: async () => {
      // Same 3-key set as snooze (D-D-13) — undo reverses snooze so it invalidates the same surfaces.
      await Promise.all([
        qc.invalidateQueries({ queryKey: queryKeys.vulnerabilities.stats() }),
        qc.invalidateQueries({ queryKey: queryKeys.vulnerabilities.dashboardTiles() }),
        qc.invalidateQueries({ queryKey: queryKeys.vulnerabilities.all }),
      ]);
    },
  });
}
```

**Step G — test files (one per query, one per mutation, one for api signal pass-through).**

Each test wraps the hook in a `QueryClientProvider` with a fresh `new QueryClient({ defaultOptions: { queries: { retry: 0, staleTime: 0 } } })`, uses `renderHook` from `@testing-library/react`, mocks `api` via `vi.mock('@/lib/api', () => ({ api: vi.fn() }))`, and asserts the hook resolves with the mocked data.

**Per-hook behavioral assertions (Warning 11 fix):**

- `use-stats.test.tsx`: assert `staleTime: 60000`, `retry: 1`, `refetchOnWindowFocus: true`. Test by spying on `useQuery`'s options OR by triggering 5xx failures and asserting fetch was called exactly twice (1 retry).
- `use-trends.test.tsx`: assert query-key includes `range` param via `expect(callArgs.queryKey).toEqual(['vulnerabilities', 'trends', { range: '7d' }])` when called with `useTrends('7d')`. Assert `staleTime: 60000`, `retry: 1`.
- `use-top-triage.test.tsx`: assert `staleTime: 60000`, `retry: 0` (D-D-07 — only stats + dashboard-tiles tier retry). Verify by simulating 5xx and asserting fetch called exactly once.
- `use-recent-notifications.test.tsx`: assert `staleTime: 30000`, `retry: 0`.

Pattern for options assertion (one approach):
```tsx
import * as rq from '@tanstack/react-query';
const spy = vi.spyOn(rq, 'useQuery');
// … render hook …
expect(spy).toHaveBeenCalledWith(expect.objectContaining({ staleTime: 60_000, retry: 1 }));
```

`use-snooze.test.tsx` MUST assert all three invalidation keys are present after `mutate(...)` resolves — use a spy on the `QueryClient.invalidateQueries` method:

```tsx
import { describe, it, expect, vi } from 'vitest';
import { renderHook, waitFor } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { useSnoozeMutation } from './use-snooze';
import { queryKeys } from '@/lib/queries/keys';

vi.mock('@/lib/api', () => ({ api: vi.fn().mockResolvedValue({ message: 'Snoozed', until: '...' }) }));

it('invalidates the 3 D-D-13 keys on success', async () => {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: 0 } } });
  const invalidate = vi.spyOn(qc, 'invalidateQueries');
  const wrapper = ({ children }: any) => <QueryClientProvider client={qc}>{children}</QueryClientProvider>;

  const { result } = renderHook(() => useSnoozeMutation(), { wrapper });
  result.current.mutate({ id: 'vuln-1' });

  await waitFor(() => expect(result.current.isSuccess).toBe(true));
  const callKeys = invalidate.mock.calls.map(c => c[0]?.queryKey);
  expect(callKeys).toContainEqual(queryKeys.vulnerabilities.stats());
  expect(callKeys).toContainEqual(queryKeys.vulnerabilities.dashboardTiles());
  expect(callKeys).toContainEqual(queryKeys.vulnerabilities.all);
});
```

`use-undo-snooze.test.tsx` mirrors the snooze test:
- Mock `api` to resolve.
- Spy on `invalidateQueries`.
- Call `result.current.mutate({ id: 'vuln-1' })`.
- Assert `api` was called with `'/api/v1/vulnerabilities/vuln-1/unsnooze'` and `method: 'POST'`.
- Assert the same 3-key invalidation set.

`api.test.ts` MUST assert the signal pass-through: mock `globalThis.fetch`, call `api('/x', { signal: new AbortController().signal })`, assert the fetch was called with `expect.objectContaining({ signal: expect.any(AbortSignal) })`. (If Phase 9 already covers 401 retry, extend rather than duplicate; if not, add the 401 retry test cited in VALIDATION.md.)

Use `vitest-axe` matcher on any test that renders DOM — though most of these are hook tests with no DOM, the api test is pure unit.
  </action>
  <verify>
    <automated>cd /Users/chemencedji/Desktop/getvul/frontend && npm run test -- --run src/lib/queries/use-stats.test.tsx src/lib/queries/use-trends.test.tsx src/lib/queries/use-top-triage.test.tsx src/lib/queries/use-recent-notifications.test.tsx src/lib/mutations/use-snooze.test.tsx src/lib/mutations/use-undo-snooze.test.tsx src/lib/api.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '"@tanstack/react-query": "\\^5' frontend/package.json` matches exactly 1.
    - `grep -E '"@tanstack/react-query-devtools": "\\^5' frontend/package.json` matches exactly 1 (under devDependencies).
    - `grep -E "signal\\?:\\s*AbortSignal" frontend/src/lib/api.ts` matches ≥ 1.
    - `grep -E "fetch\\(.*signal:" frontend/src/lib/api.ts` matches ≥ 1 (signal is actually passed to fetch).
    - `frontend/src/lib/query-client.ts` exists and exports `makeQueryClient`; `grep -E "staleTime:\\s*60_000" frontend/src/lib/query-client.ts` matches ≥ 1.
    - `frontend/src/lib/queries/keys.ts` exists; `grep -E "topTriage" frontend/src/lib/queries/keys.ts` matches ≥ 1; `grep -E "notifications" frontend/src/lib/queries/keys.ts` matches ≥ 1.
    - Four query hook files exist with their typed response types exported (DashboardStatsResponse, TrendsResponse, TopTriageResponse, RecentNotificationsResponse).
    - `grep -E "id:\\s*string" frontend/src/lib/queries/use-stats.ts` matches ≥ 1 (top_vuln.id field present in TS type — Blocker 2 fix).
    - `grep -E "value:\\s*number\\s*\\|\\s*string" frontend/src/lib/queries/use-stats.ts` matches ≥ 1 (TileValue.value union — Warning 6 fix).
    - `grep -E "retry:\\s*1" frontend/src/lib/queries/use-stats.ts` matches exactly 1 (D-D-07 override on stats).
    - `grep -E "staleTime:\\s*30_000" frontend/src/lib/queries/use-recent-notifications.ts` matches exactly 1 (D-D-06).
    - `grep -E "refetchOnWindowFocus:\\s*false" frontend/src/lib/queries/use-trends.ts` matches exactly 1 (jarring redraw mitigation).
    - `frontend/src/lib/mutations/use-snooze.ts` exists; `grep -E "invalidateQueries" frontend/src/lib/mutations/use-snooze.ts | wc -l` returns ≥ 3 (one per D-D-13 key).
    - `frontend/src/lib/mutations/use-undo-snooze.ts` exists; `grep -E "unsnooze" frontend/src/lib/mutations/use-undo-snooze.ts` matches ≥ 1; `grep -E "invalidateQueries" frontend/src/lib/mutations/use-undo-snooze.ts | wc -l` returns ≥ 3.
    - All 7 test files pass: `npm run test -- --run` exits 0 on the listed file paths.
    - `frontend/src/lib/mutations/use-snooze.test.tsx` contains an assertion referencing each of the three keys (grep for `queryKeys.vulnerabilities.stats`, `dashboardTiles`, `all`).
    - `frontend/src/lib/queries/use-stats.test.tsx` asserts `staleTime: 60_000` AND `retry: 1` (per-hook behavior spec — Warning 11).
    - `frontend/src/lib/queries/use-trends.test.tsx` asserts query-key includes `range` param.
    - `frontend/src/lib/queries/use-top-triage.test.tsx` asserts `retry: 0`.
    - `frontend/src/lib/queries/use-recent-notifications.test.tsx` asserts `staleTime: 30_000` AND `retry: 0`.
  </acceptance_criteria>
  <done>TanStack Query v5 is installed, the entire data layer (provider factory + key factory + 4 queries + 2 mutations + api signal pass-through) ships with green tests, top_vuln.id (UUID) field is typed, TileValue.value union covers MTTR's formatted string. The contract for Plans 03–06 is locked.</done>
</task>

<task type="auto">
  <name>Task 2: Three utility hooks (useDocumentTitle, useUrlState, usePrefersReducedMotion) + their tests</name>
  <files>frontend/src/hooks/use-document-title.ts, frontend/src/hooks/use-url-state.ts, frontend/src/hooks/use-prefers-reduced-motion.ts, frontend/src/hooks/use-document-title.test.ts, frontend/src/hooks/use-url-state.test.ts, frontend/src/hooks/use-prefers-reduced-motion.test.ts</files>
  <read_first>
    - .planning/phases/10-dashboard/10-RESEARCH.md (Pattern 7: useUrlState; Pattern 8: useDocumentTitle; Pitfall 7: clamp to enum mitigates XSS; Pitfall 9: title cleanup race)
    - .planning/phases/10-dashboard/10-CONTEXT.md (D-Tab-01 tab title; D-D-04, D-D-05 URL range source-of-truth; D-Ax-04 reduce-motion)
    - frontend/src/components/ui/ToastProvider.tsx (project's 'use client' directive style — match it)
  </read_first>
  <action>
Three small hooks, kebab-case file names, one default-export OR named-export pattern matching Phase 9 — grep `frontend/src/components/ui/button.tsx` to see whether the project uses named-export and follow.

**`frontend/src/hooks/use-document-title.ts`** (D-Tab-01):
```ts
'use client';
import { useEffect } from 'react';

export function useDocumentTitle(title: string) {
  useEffect(() => {
    const previous = document.title;
    document.title = title;
    return () => { document.title = previous; };
  }, [title]);
}
```

**`frontend/src/hooks/use-url-state.ts`** (D-D-04, D-D-05, Pitfall 7 — XSS mitigation):
```ts
'use client';
import { usePathname, useRouter, useSearchParams } from 'next/navigation';
import { useCallback } from 'react';

export function useUrlState<T extends string>(
  key: string,
  allowed: readonly T[],
  defaultValue: T
): [T, (next: T) => void] {
  const router = useRouter();
  const pathname = usePathname();
  const params = useSearchParams();

  const raw = params.get(key);
  // Pitfall 7: URL is user-controllable; ALWAYS clamp to allowed enum BEFORE rendering.
  const value: T = (allowed as readonly string[]).includes(raw ?? '') ? (raw as T) : defaultValue;

  const setValue = useCallback((next: T) => {
    const sp = new URLSearchParams(params.toString());
    if (next === defaultValue) sp.delete(key);
    else sp.set(key, next);
    const qs = sp.toString();
    router.replace(qs ? `${pathname}?${qs}` : pathname, { scroll: false });
  }, [router, pathname, params, key, defaultValue]);

  return [value, setValue];
}
```

**`frontend/src/hooks/use-prefers-reduced-motion.ts`** (D-Ax-04, D-C-04 helper):
```ts
'use client';
import { useEffect, useState } from 'react';

export function usePrefersReducedMotion(): boolean {
  const [reduced, setReduced] = useState(false);
  useEffect(() => {
    const mql = window.matchMedia('(prefers-reduced-motion: reduce)');
    setReduced(mql.matches);
    const handler = (e: MediaQueryListEvent) => setReduced(e.matches);
    mql.addEventListener('change', handler);
    return () => mql.removeEventListener('change', handler);
  }, []);
  return reduced;
}
```

**Tests (one per hook):**

`use-document-title.test.ts`:
- `renderHook(() => useDocumentTitle('foo'))` asserts `document.title === 'foo'`.
- On unmount, `document.title` reverts to the prior value.
- Re-render with new title updates `document.title`.

`use-url-state.test.ts`:
- Wrap with a mock Next.js router context (or use `next/navigation` mock setup; project may already have a helper — check Phase 9 tests; if not, create a minimal mock).
- `useUrlState('range', ['7d','30d','90d'], '30d')` with `?range=7d` returns `'7d'`.
- With `?range=garbage` returns `'30d'` (clamp to default — **XSS-via-URL mitigation test** — REQUIRED per ASVS V5 / T-10-XX).
- With `?range=<script>alert(1)</script>` returns `'30d'` (never echoes raw).
- `setValue('90d')` calls `router.replace` with `?range=90d` and `{ scroll: false }`.
- `setValue('30d')` (default) calls `router.replace` removing the param entirely.

`use-prefers-reduced-motion.test.ts`:
- Mock `window.matchMedia` to return `{ matches: true, addEventListener, removeEventListener }`.
- Hook returns `true`.
- Dispatch a change event with `matches: false` → next render returns `false`.

If the project doesn't have a `next/navigation` mock helper, create one inline at the top of `use-url-state.test.ts`:

```ts
const mockReplace = vi.fn();
const mockParams = new URLSearchParams();
vi.mock('next/navigation', () => ({
  useRouter: () => ({ replace: mockReplace }),
  usePathname: () => '/dashboard',
  useSearchParams: () => ({ get: (k: string) => mockParams.get(k), toString: () => mockParams.toString() }),
}));
```
  </action>
  <verify>
    <automated>cd /Users/chemencedji/Desktop/getvul/frontend && npm run test -- --run src/hooks/use-document-title.test.ts src/hooks/use-url-state.test.ts src/hooks/use-prefers-reduced-motion.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All three hook files exist with `'use client'` directive on line 1: `head -1 frontend/src/hooks/use-document-title.ts` returns `'use client';`.
    - `grep -E "document\\.title" frontend/src/hooks/use-document-title.ts` matches ≥ 2 (set + restore).
    - `grep -E "allowed.*includes" frontend/src/hooks/use-url-state.ts` matches ≥ 1 (clamp-to-enum present).
    - `grep -E "scroll:\\s*false" frontend/src/hooks/use-url-state.ts` matches exactly 1 (router.replace option).
    - `grep -E "addEventListener\\('change'" frontend/src/hooks/use-prefers-reduced-motion.ts` matches exactly 1.
    - `grep -E "removeEventListener" frontend/src/hooks/use-prefers-reduced-motion.ts` matches exactly 1 (cleanup).
    - Three test files exist; npm run test -- --run on those three files exits 0.
    - `use-url-state.test.ts` contains an assertion for the XSS-via-URL case (grep `<script>` returns ≥ 1).
  </acceptance_criteria>
  <done>Three utility hooks ship with green tests; URL-state-clamping XSS mitigation is automated; tab title and reduce-motion hooks ready for Plans 03–05 consumption.</done>
</task>

<task type="auto">
  <name>Task 3: Hoist QueryClientProvider to ROOT layout via `<Providers>` wrapper; wire queryClient.clear() into logout; ship microcopy.ts with D-Ax-01 section h2s; write logout cache-clear test</name>
  <files>frontend/src/app/providers.tsx, frontend/src/app/layout.tsx, frontend/src/app/(authed)/layout.tsx, frontend/src/lib/auth.tsx, frontend/src/components/dashboard/microcopy.ts, frontend/src/lib/auth.logout.test.tsx</files>
  <read_first>
    - frontend/src/app/layout.tsx (current Phase 9 root: ThemeProvider > AuthProvider — preserve order; insert <Providers> as outermost wrapper around AuthProvider)
    - frontend/src/app/(authed)/layout.tsx (current Phase 9 wrap: ToastProvider > AppShell — preserve; do NOT add QueryClientProvider here)
    - frontend/src/lib/auth.tsx (current logout — keep all existing behavior; ADD qc.clear() between clearAuth and router.replace)
    - .claude/skills/sketch-findings-getvul/references/copy-voice.md (entire file — every string in microcopy.ts must match the voice)
    - .planning/phases/10-dashboard/10-CONTEXT.md (`<specifics>` block — the verbatim copy exemplars; D-Ax-01 section h2 labels)
    - .planning/phases/10-dashboard/10-RESEARCH.md (Pattern 1: useState(() => makeQueryClient()) — NOT module-level; Pattern 6: queryClient.clear on logout; Pitfall 1: NO module-level QueryClient instantiation)
  </read_first>
  <action>
**Step A — `frontend/src/app/providers.tsx`** (NEW client wrapper — Blocker 4 fix; D-D-02):

```tsx
'use client';

import { useState, type ReactNode } from 'react';
import { QueryClientProvider } from '@tanstack/react-query';
import { makeQueryClient } from '@/lib/query-client';

export function Providers({ children }: { children: ReactNode }) {
  // Lazy init per RESEARCH Pattern 1 / Pitfall 1 — NEVER module-level `new QueryClient()`.
  // useState's lazy initializer runs exactly once per component tree, guaranteeing a single
  // QueryClient instance survives strict-mode double-mounts and HMR without leaking caches.
  const [queryClient] = useState(() => makeQueryClient());
  return <QueryClientProvider client={queryClient}>{children}</QueryClientProvider>;
}
```

**Step B — `frontend/src/app/layout.tsx`** (modify — wrap AuthProvider in <Providers> so /login + (authed)/* share one cache):

The current Phase 9 root layout has `<ThemeProvider><AuthProvider>{children}</AuthProvider></ThemeProvider>`. Change to:

```tsx
import { Providers } from './providers';
// … other existing imports unchanged …

<body>
  <ThemeProvider>
    <Providers>
      <AuthProvider>{children}</AuthProvider>
    </Providers>
  </ThemeProvider>
</body>
```

Preserve ALL other Phase 9 root-layout behavior:
- `THEME_BOOTSTRAP_SCRIPT` inline script in `<head>` (FOUC bootstrap)
- `Inter` + `JetBrains_Mono` font wiring + `--font-sans` / `--font-mono` CSS variables
- `<html lang="en" suppressHydrationWarning data-theme="dark" className={...}>` chrome
- `metadata` export

**Bundle-size note (Warning 4 follow-up):** Hoisting `QueryClientProvider` to root means TanStack Query (~13 kB) is now in the shared bundle that `/login` loads. This is acceptable per D-Perf-01 (budgets `/dashboard` ≤ 180 kB; `/login` has no explicit ceiling). Plan 04's `check-bundle.mjs --route /dashboard --max-kb 180` continues to be the binding gate.

**Step C — `frontend/src/app/(authed)/layout.tsx`** (modify — REMOVE the QueryClientProvider mount, it's now redundant; keep ToastProvider + AppShell):

```tsx
import type { ReactNode } from 'react';
import { AppShell } from '@/components/shell/app-shell';
import ToastProvider from '@/components/ui/ToastProvider';

// Route-group layout — D-33 single chrome owner for the (authed)/ subtree.
// QueryClientProvider is mounted at the ROOT layout (via <Providers>) so /login
// can also call useQueryClient() (Phase 9 D-D-09 logout cache-clear).
export default function AuthedLayout({ children }: { children: ReactNode }) {
  return (
    <ToastProvider>
      <AppShell>{children}</AppShell>
    </ToastProvider>
  );
}
```

This is the existing Phase 9 file — DO NOT add QueryClientProvider here.

**Step D — `frontend/src/lib/auth.tsx`** (D-D-09):

In the `logout` callback, after `clearAuth()` and BEFORE `router.replace('/login')`, call `qc.clear()`. Use `useQueryClient()` from `@tanstack/react-query` to get the client. With `<Providers>` at the root, AuthProvider is guaranteed to be a descendant of QueryClientProvider on BOTH `/login` and `(authed)/*`, so `useQueryClient()` is safe to call inside AuthProvider.

```tsx
import { useQueryClient } from '@tanstack/react-query';

// Inside AuthProvider:
const qc = useQueryClient();

const logout = useCallback(() => {
  fetch(`${API}/auth/logout`, {
    method: 'POST',
    headers: token ? { Authorization: `Bearer ${token}` } : {},
  }).catch(() => {});
  clearAuth();
  qc.clear();                  // D-D-09: prevents cross-user cache leakage. T-10-11 (Information Disclosure).
  router.replace('/login');
}, [token, router, qc]);
```

Add `qc` to the dependency array as shown.

**Step E — `frontend/src/components/dashboard/microcopy.ts`** (Wave 0 per VALIDATION.md; copy-voice.md compliance; D-Ax-01 section h2s — Blocker 3 fix; Warning 13 deltaSuffix as data; Warning 16 compact range labels):

Extract every user-facing dashboard string. Sentence case. No "Please", no "Welcome", no "Click here", no exclamation marks. Use the verbatim exemplars from CONTEXT.md `<specifics>` block:

```ts
export const microcopy = {
  hero: {
    // D-H-02 grammar (singular/plural picked client-side)
    headlineSingular: '1 critical CVE needs your eyes',
    headlinePlural: (n: number) => `${n} critical CVEs need your eyes`,
    // D-H-03 sub-line shape; this is the exemplar — actual sub-line interpolates real data
    subLineTemplate: (host: string, path: string, cvss: number, exploited: boolean) =>
      `Top one is on ${host} — ${path}, CVSS ${cvss.toFixed(1)}${exploited ? ', exploited in the wild' : ''}.`,
    quietWin: 'Nothing critical right now',
    ctaPrimary: 'Start triage',
    ctaSecondary: 'Snooze 1h',
  },
  stats: {
    // D-Ax-01 section h2 (sr-only on rendered page)
    h2: 'Today at a glance',
    // D-S-04 — value rendered server-side as TileValue.value; suffix only:
    deltaSuffix: 'from yesterday',
    deltaUnknown: 'Δ —',  // Pitfall 8 — when delta is null
    labels: {
      critical_open: 'Critical · open',
      sla_at_risk: 'SLA · at risk',
      kev: 'CISA KEV',
      mttr_30d: 'MTTR · 30d',
    },
  },
  trend: {
    h2: '30-day vulnerability trend',     // D-Ax-01 section heading (VERBATIM)
    todaySoFar: 'Today (so far)',         // D-C-07
    // Compact visual labels per sketch 002 variant B (Warning 16):
    range7d: '7d',
    range30d: '30d',
    range90d: '90d',
    // Verbose a11y labels for screen readers (aria-label on each range button):
    range7dA11y: '7 days',
    range30dA11y: '30 days',
    range90dA11y: '90 days',
  },
  top5: {
    h2: 'Top 5 to triage',                // D-Ax-01 (VERBATIM)
  },
  activity: {
    h2: 'Recent activity',                // D-Ax-01 (VERBATIM)
    empty: "No recent activity. We'll show events here as they happen.",  // D-A-03 verbatim
  },
  snooze: {
    // D-H-08 — message + action are separate. The `· Undo` lives in the action slot, not the message.
    // 8s undo window is the toast `duration`; consumer wires `action: { label: 'Undo', onClick: … }`.
    toastTitle: (cveId: string) => `Snoozed ${cveId} for 1h`,
    toastMessage: (cveId: string) => `Snoozed ${cveId} for 1h`,
    toastActionLabel: 'Undo',
    toastError: (errCode: string | number) => `Couldn't snooze. HTTP ${errCode} · Retry`,
    undoToastMessage: 'Snooze undone',
  },
  error: {
    // copy-voice.md exemplar pattern
    inline: (section: string, code: number | string, reqId: string) =>
      `${section} unavailable. HTTP ${code} · Request ID ${reqId} · Retry now`,
  },
  onboarding: {
    noScannersTitle: 'No scanners connected yet',
    noScannersBody: 'Connect a scanner so we can start aggregating findings.',
    noScannersCta: 'Connect a scanner',
    noDataYetTitle: 'Your first sync is in progress',
    noDataYetBody: 'Findings will appear as soon as the sync completes.',
    noDataYetCta: 'Refresh',
  },
  tabTitle: {
    // D-Tab-01
    base: 'Dashboard · GetVul',
    withCount: (n: number) => `(${n}) Dashboard · GetVul`,
  },
} as const;
```

Note for Warning 13: `microcopy.stats.deltaSuffix = 'from yesterday'` is the data; the `Stat` primitive accepts a `deltaSuffix?: string = 'from yesterday'` prop. `StatStripWired` (Plan 05) passes `microcopy.stats.deltaSuffix` explicitly. Primitive stays presentation-only with a sensible default.

**Step F — `frontend/src/lib/auth.logout.test.tsx`** (Wave 0; D-D-09 verification):

Render `<AuthProvider>` inside a `QueryClientProvider` (set up the queryClient inline with a sentinel query). Pre-seed the queryClient with `qc.setQueryData(['x'], 'sentinel')`, then call the logout function exposed by `useAuth()`, then assert `qc.getQueryData(['x']) === undefined` (i.e., `clear()` removed it).

```tsx
import { describe, it, expect, vi } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { AuthProvider, useAuth } from './auth';

// Mock router + fetch as Phase 9 tests do
vi.mock('next/navigation', () => ({ useRouter: () => ({ replace: vi.fn() }) }));
global.fetch = vi.fn().mockResolvedValue({ ok: true } as any);

it('logout() clears the TanStack cache (D-D-09 / T-10-11)', async () => {
  const qc = new QueryClient();
  qc.setQueryData(['x'], 'sentinel');
  expect(qc.getQueryData(['x'])).toBe('sentinel');

  const wrapper = ({ children }: any) => (
    <QueryClientProvider client={qc}>
      <AuthProvider>{children}</AuthProvider>
    </QueryClientProvider>
  );

  const { result } = renderHook(() => useAuth(), { wrapper });
  await act(async () => { result.current.logout(); });

  expect(qc.getQueryData(['x'])).toBeUndefined();
});
```
  </action>
  <verify>
    <automated>cd /Users/chemencedji/Desktop/getvul/frontend && npm run test -- --run src/lib/auth.logout.test.tsx 2>&1 | tail -15 && grep -c "Providers" src/app/layout.tsx && grep -c "qc.clear()" src/lib/auth.tsx && grep -c "Today at a glance" src/components/dashboard/microcopy.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/providers.tsx` exists, has `'use client';` on line 1, exports `Providers`, uses `useState(() => makeQueryClient())` (lazy init — Pitfall 1 mitigation).
    - `frontend/src/app/layout.tsx` imports `Providers` and wraps `<AuthProvider>` in `<Providers>` (grep `<Providers>` returns ≥ 1).
    - `frontend/src/app/layout.tsx` preserves `THEME_BOOTSTRAP_SCRIPT`, fonts, and metadata (no regression in Phase 9 root behavior).
    - `frontend/src/app/(authed)/layout.tsx` does NOT contain `QueryClientProvider` (grep returns 0).
    - `frontend/src/lib/auth.tsx` contains `useQueryClient` import AND a call to `qc.clear()` (or equivalent reference like `queryClient.clear()`) inside the `logout` callback (grep ≥ 1).
    - `frontend/src/components/dashboard/microcopy.ts` exists and contains all verbatim exemplars: `grep -F "1 critical CVE needs your eyes"` (≥ 1), `grep -F "Nothing critical right now"` (≥ 1), `grep -F "Top one is on"` (≥ 1), `grep -F "No recent activity. We'll show events here as they happen."` (≥ 1), `grep -F "Snoozed"` (≥ 1), `grep -F "Start triage"` (≥ 1).
    - microcopy.ts contains D-Ax-01 section h2 strings VERBATIM: `grep -F "Today at a glance" frontend/src/components/dashboard/microcopy.ts` (≥ 1), `grep -F "30-day vulnerability trend"` (≥ 1), `grep -F "Top 5 to triage"` (≥ 1), `grep -F "Recent activity"` (≥ 1). [Blocker 3 fix]
    - microcopy.ts compact range labels present: `grep -E "range7d:\\s*'7d'" frontend/src/components/dashboard/microcopy.ts` ≥ 1; `range7dA11y: '7 days'` ≥ 1 (Warning 16).
    - microcopy.ts splits snooze toast into title/message/actionLabel (D-H-08 — `Undo` lives in `action` slot, not the message string): `grep -F "toastActionLabel: 'Undo'"` ≥ 1.
    - microcopy.ts contains NO forbidden tokens in user-facing strings: manual review confirms no occurrences of `Please`, `Welcome`, `Click here`, or trailing `!` in any string literal.
    - `frontend/src/lib/auth.logout.test.tsx` exists; `npm run test -- --run src/lib/auth.logout.test.tsx` exits 0.
    - Full vitest suite still green (Phase 9 tests do not regress): `npm run test -- --run 2>&1 | tail -3` shows no failures.
    - `/login` route still renders correctly with the hoisted Providers (smoke check: `npm run dev` then navigate to /login — no errors in browser console).
  </acceptance_criteria>
  <done>QueryClientProvider hoisted to root layout via `<Providers>`; (authed)/layout.tsx no longer mounts a duplicate provider; logout clears the cache (verified by test); microcopy.ts holds all verbatim dashboard strings INCLUDING D-Ax-01 section h2 labels and compact range labels; Wave 0 frontend scaffolding is complete and unblocks Plans 03–06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query string → React component render | User-controllable; ASVS V5 input validation applies |
| Cached query data → Next user on shared machine | Session boundary; cache must be cleared on logout per D-D-09 |
| api() wrapper → backend | JWT bearer flows over TLS; 401 retry path inherited from Phase 9 |
| Toast `action.onClick` → caller-supplied callback | Callback runs in user's own session; no cross-user surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Tampering (XSS reflection) | `?range=` URL param → component render | mitigate | `useUrlState` clamps `raw` to `allowed` enum BEFORE returning; default fallback `'30d'`. Test: `use-url-state.test.ts` includes `<script>alert(1)</script>` case asserting fallback to default. ASVS V5. |
| T-10-11 | Information Disclosure | Cross-user cache leak on shared machine | mitigate | `useAuth().logout()` calls `qc.clear()` (D-D-09). Test: `auth.logout.test.tsx` pre-seeds query and asserts it's gone after logout. With `<Providers>` at root, the clear() also impacts /login subsequent loads — no cross-user data survives a logout. |
| T-10-12 | Session Management | 401 → silent refresh → retry → if fail → /login | mitigate | EXISTING Phase 9 behavior in `api.ts`. Phase 10 changes only one line (signal pass-through) — does NOT alter the 401 chain. Test: `api.test.ts` regression-checks the chain still works. ASVS V3. |
| T-10-13 | Information Disclosure | Bundle inlines secrets | accept | Only `NEXT_PUBLIC_*` env vars reach the bundle; no new env vars added in Phase 10. Phase 9 baseline upheld. |
| T-10-14 | Tampering | Module-level `new QueryClient()` shared across React trees in tests → cache pollution / accidental state leak | mitigate | `useState(() => makeQueryClient())` lazy init in `providers.tsx` per Pattern 1; Pitfall 1 explicitly avoids module-level singleton. Code-review-time grep gate: `grep -RE "new QueryClient\\(\\)" frontend/src/ | grep -v "test"` returns 0 results outside test files. |
| T-10-15 | Repudiation | Logout doesn't actually clear server-side session | accept | Phase 9 sends `POST /auth/logout` before clearing local state; server-side session invalidation is a Phase 9 contract. Phase 10 doesn't touch it. |
| T-10-15a | Tampering | Toast action.onClick could be wired to a destructive callback by malicious caller | accept | Callbacks are caller-controlled; Toast primitive is pure-presentation. No cross-user surface — the callback runs in the user's own session. Callers (e.g., Hero's Undo button) are reviewed at the call site. |
</threat_model>

<verification>
- `npm run test -- --run` exits 0 across all 12 new test files (4 queries + 2 mutations + 3 hooks + 1 toast + 1 logout + 1 api).
- `npm run build` (or `next build`) completes cleanly with no new TypeScript errors.
- `grep -RE "new QueryClient\\(\\)" frontend/src/ | grep -v "test"` returns 0 results outside test files (Pitfall 1 — no module-level instantiation).
- `grep -F "Please" frontend/src/components/dashboard/microcopy.ts | wc -l` returns 0 (copy-voice compliance).
- `grep -RE "(emerald-500/40|emerald-400|red-500/40|red-400|indigo-500/40|indigo-400|bg-gray-900)" frontend/src/components/ui/Toast.tsx | wc -l` returns 0 (raw palette purged from Toast).
- Manual smoke (optional): open `http://localhost:3000/login` — confirm useAuth() consumers function without throwing (proves Blocker 4 fix is correct).
</verification>

<success_criteria>
- @tanstack/react-query@^5 + devtools installed; package.json updated.
- Root layout wraps AuthProvider in `<Providers>` (QueryClientProvider via useState lazy init).
- (authed)/layout.tsx does NOT mount a duplicate QueryClientProvider.
- query-client.ts, queries/keys.ts, 4 query hooks, 2 mutation hooks (snooze + undo-snooze) exist with typed responses + correct staleTime + correct retry policy per D-D-06/07.
- api.ts accepts and passes AbortSignal — no other behavior altered.
- auth.tsx logout() also calls qc.clear() — verified by test.
- Toast primitive supports `duration` + `action`, consumes sunset CSS variables, honors prefers-reduced-motion, backward compatible with Phase 9 callers.
- Three utility hooks (useDocumentTitle, useUrlState, usePrefersReducedMotion) ship with tests including XSS clamp coverage.
- microcopy.ts holds verbatim copy-voice exemplars INCLUDING D-Ax-01 section h2s, compact range labels with a11y aliases, and split snooze toast (title/message/actionLabel).
- 12 new vitest test files all pass.
- top_vuln.id (UUID) typed in DashboardStatsResponse so Plan 05 hero can fire snooze + undo against the correct backend identifier.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard/10-02-SUMMARY.md` covering:
- Exact installed versions of `@tanstack/react-query` and `@tanstack/react-query-devtools` (record from `npm view` output).
- Confirmation that QueryClientProvider is mounted at root layout via `<Providers>` (NOT in (authed)/layout.tsx).
- Confirmation that `useQueryClient()` inside AuthProvider works on `/login` (Blocker 4 resolution).
- Note that Toast was extended additively — Phase 9 call sites continue to work (`grep -RE "useToast\\(\\)" frontend/src/` should still find Phase 9 consumers).
- List of files created/modified with one-line purpose each.
- Confirmation that Phase 9 tests still pass (no regression).
- Note for Plans 03–06: import the shared types from `@/lib/queries/use-stats` etc., do NOT redefine response shapes. `top_vuln.id` is the UUID — use it for `useSnoozeMutation().mutate({ id: topVuln.id })` and `useUndoSnoozeMutation().mutate({ id: topVuln.id })`.
</output>
