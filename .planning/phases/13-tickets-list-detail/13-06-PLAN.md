---
phase: 13-tickets-list-detail
plan: 06
type: execute
wave: 2
depends_on: [13-04]
files_modified:
  - frontend/src/components/tickets/watcher-stack.tsx
  - frontend/src/components/tickets/watcher-stack.test.tsx
  - frontend/src/components/tickets/activity-timeline.tsx
  - frontend/src/components/tickets/activity-timeline.test.tsx
  - frontend/src/components/tickets/comment-input.tsx
  - frontend/src/components/tickets/comment-input.test.tsx
  - frontend/src/components/tickets/blocked-toggle.tsx
  - frontend/src/components/tickets/blocked-toggle.test.tsx
  - frontend/src/components/tickets/ticket-asset-card.tsx
  - frontend/src/components/tickets/ticket-asset-card.test.tsx
  - frontend/src/components/tickets/microcopy.ts
autonomous: true
requirements: [UX-05-04, UX-05-05]
must_haves:
  truths:
    - "WatcherStack shows first 3 by initials + a +N overflow chip with a hover/focus popover; sorted assignee→reporter→others"
    - "ActivityTimeline composes local comments + provider sync events chronologically, grouped by day"
    - "CommentInput renders below the timeline, validates 1..10000, submits via an onSubmit callback"
    - "BlockedToggle renders the inline-edit reason capture (reassign-combobox shape) and calls an onToggle callback"
    - "TicketAssetCard cross-links to the asset detail page"
  artifacts:
    - path: "frontend/src/components/tickets/watcher-stack.tsx"
      provides: "avatar stack with +N overflow + popover"
      contains: "WatcherStack"
    - path: "frontend/src/components/tickets/activity-timeline.tsx"
      provides: "day-grouped comments + sync events"
      contains: "ActivityTimeline"
    - path: "frontend/src/components/tickets/blocked-toggle.tsx"
      provides: "shared inline blocked-reason editor"
      contains: "BlockedToggle"
  key_links:
    - from: "frontend/src/components/tickets/watcher-stack.tsx"
      to: "Avatar (ui)"
      via: "imports Avatar for 2-char initials"
      pattern: "Avatar"
    - from: "frontend/src/components/tickets/ticket-asset-card.tsx"
      to: "/assets/[id]"
      via: "Link to asset detail"
      pattern: "/assets/"
---

<objective>
Build the `/tickets/[id]` detail-surface presentational primitives (D-W-03/04, D-C-01/04, D-P-03, asset cross-link): WatcherStack, ActivityTimeline, CommentInput, the shared BlockedToggle, and TicketAssetCard. All are presentational/interaction components that take callbacks for mutations (the actual TanStack mutation hooks wire in Plan 08). They depend only on Plan 04 primitives + existing `ui/Avatar`, so this plan runs parallel with Plan 05 in Wave 2.

**BlockedToggle is shared** by both the drill footer (Plan 05 passes it as `renderBlockedToggle`) and the detail right-rail Details card (Plan 08) — single component, single UX (D-D-01 + D-P-03). It uses the Phase 12 reassign-combobox inline-edit shape (D-A): click the Blocked pill → inline text input for reason + Save/Cancel.

Purpose: Plan 08 composes these into `/tickets/[id]`; Plan 05's drill footer renders the shared BlockedToggle.
Output: 5 presentational components + microcopy.ts + co-located tests.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-tickets-list-detail/13-RESEARCH.md

<interfaces>
From Plan 04: StatusPill({externalStatus, blocked}), type TicketStatus
Existing ui primitives:
- Avatar({name, email, size, className}) — 2-char initials, sunset gradient (WR-09)
- frontend/src/components/ui/Breadcrumb.tsx (Breadcrumb/Crumb)
Phase 12 reassign-combobox inline-edit UX = template for BlockedToggle reason capture.
D-W-04 watcher order: assignee → reporter → others chronological; dedupe, prefer strongest role; max 50, +N popover.
D-C-04: timeline chronological ascending (oldest top); comment input below last row; provider sync events interleave by created_at; group by day ("Today"/"Yesterday"/"MMM D").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WatcherStack + TicketAssetCard + microcopy</name>
  <files>frontend/src/components/tickets/watcher-stack.tsx, frontend/src/components/tickets/watcher-stack.test.tsx, frontend/src/components/tickets/ticket-asset-card.tsx, frontend/src/components/tickets/ticket-asset-card.test.tsx, frontend/src/components/tickets/microcopy.ts</files>
  <read_first>
    - frontend/src/components/ui/Avatar.tsx (2-char initials API + sizing)
    - .claude/skills/sketch-findings-getvul/references/copy-voice.md (peer-not-butler microcopy strings)
    - .claude/skills/sketch-findings-getvul/references/interaction-patterns.md (avatar stack + cross-link card patterns)
    - .planning/phases/13-tickets-list-detail/13-RESEARCH.md §D-W-04 (avatar stack render rules verbatim)
  </read_first>
  <behavior>
    - WatcherStack Test 1: 5 watchers → renders 3 Avatars + a "+2" chip.
    - WatcherStack Test 2: sort order assignee→reporter→others (a watcher tagged role="assignee" appears first).
    - WatcherStack Test 3: focus/hover on the +N chip reveals a popover listing all watchers (keyboard-accessible: the chip is a button, popover toggles on focus/click).
    - WatcherStack Test 4: a user appearing as both assignee and watcher is deduped (rendered once, strongest role).
    - TicketAssetCard Test 1: renders the asset hostname + a Link to /assets/{assetId}.
  </behavior>
  <action>
**`frontend/src/components/tickets/microcopy.ts`** — export a `microcopy` object with peer-voice strings used across the detail surface (e.g. `watchersEmpty: 'No one's watching this yet.'`, `commentPlaceholder: 'Add a note for the team…'`, `blockedPrompt: 'What's blocking this?'`, `markBlocked: 'Mark blocked'`, `unblock: 'Unblock'`, `openFull: 'Open full detail'`). Follow copy-voice.md: no "Please", no "Click here", no "Welcome".

**`frontend/src/components/tickets/watcher-stack.tsx`** (UX-05-05 / D-W-04):
```ts
type Watcher = { userId: string; displayName: string | null; email?: string | null; role?: 'assignee' | 'reporter' | 'watcher'; createdAt?: string };
export function WatcherStack({ watchers }: { watchers: Watcher[] }) { ... }
```
- Dedupe by userId, preferring the strongest role (assignee > reporter > watcher).
- Sort: assignee first, reporter second, then watchers ascending by createdAt.
- Render first 3 as `<Avatar name={w.displayName} email={w.email} size={28} />` overlapping (`-ml-2` ring). If >3, render a `<button>` `+{n-3}` chip that toggles a popover (`<ul>` of all watchers, displayName + role tag), max 50 entries. Popover opens on click/focus, closes on blur/Esc — keyboard accessible.
- Empty → `microcopy.watchersEmpty`.

**`frontend/src/components/tickets/ticket-asset-card.tsx`** (UX-05-04 rail Asset card):
```ts
export function TicketAssetCard({ assetId, hostname, osName, riskScore }: {...}) { ... }
```
- Render a rail card with hostname (mono), os, risk score, and a `<Link href={'/assets/' + assetId}>` cross-link (label like "View asset →"). If `assetId` is null (ticket spans multiple hosts), render a neutral "Multiple hosts" summary with no single link.
- No inline hex; use sunset tokens.
  </action>
  <acceptance_criteria>
    - `grep -q "WatcherStack" frontend/src/components/tickets/watcher-stack.tsx`
    - `grep -q "Avatar" frontend/src/components/tickets/watcher-stack.tsx`
    - `grep -qE "\+\{|\+ *\$\{|overflow|popover|\\+N" frontend/src/components/tickets/watcher-stack.tsx` (overflow chip present)
    - `grep -q "/assets/" frontend/src/components/tickets/ticket-asset-card.tsx`
    - `grep -q "export const microcopy\|export const " frontend/src/components/tickets/microcopy.ts`
    - NO banned copy: `grep -ciE "please|click here|welcome!" frontend/src/components/tickets/microcopy.ts` returns 0
    - `cd frontend && npx vitest run src/components/tickets/watcher-stack.test.tsx src/components/tickets/ticket-asset-card.test.tsx` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/tickets/watcher-stack.test.tsx src/components/tickets/ticket-asset-card.test.tsx</automated>
  </verify>
  <done>WatcherStack dedupes/sorts/overflows with an accessible +N popover; TicketAssetCard cross-links to /assets/{id}; microcopy follows peer voice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ActivityTimeline + CommentInput</name>
  <files>frontend/src/components/tickets/activity-timeline.tsx, frontend/src/components/tickets/activity-timeline.test.tsx, frontend/src/components/tickets/comment-input.tsx, frontend/src/components/tickets/comment-input.test.tsx</files>
  <read_first>
    - frontend/src/components/tickets/microcopy.ts (Task 1)
    - .claude/skills/sketch-findings-getvul/references/interaction-patterns.md (timeline pattern)
    - .claude/skills/sketch-findings-getvul/references/copy-voice.md
    - .planning/phases/13-tickets-list-detail/13-RESEARCH.md §D-C-01/D-C-04 (ordering + interleave + day grouping)
  </read_first>
  <behavior>
    - Timeline Test 1: given local comments + provider sync events, rows render ascending by createdAt (oldest top), interleaved.
    - Timeline Test 2: rows are grouped under day headers ("Today" / "Yesterday" / "MMM D").
    - Timeline Test 3: comment body renders as a plain-text node with newlines preserved (whitespace-pre-wrap), and an attempt to inject `<script>` renders as escaped text (no DOM script node).
    - CommentInput Test 1: typing + submit calls onSubmit(body); empty/whitespace submit is blocked (button disabled or no call).
    - CommentInput Test 2: body >10000 chars is prevented/flagged client-side (mirrors backend bound).
  </behavior>
  <action>
**`frontend/src/components/tickets/activity-timeline.tsx`** (D-C-01/04):
```ts
type TimelineEntry =
  | { kind: 'comment'; id: string; author: string | null; body: string; createdAt: string }
  | { kind: 'sync'; id: string; label: string; createdAt: string };
export function ActivityTimeline({ entries }: { entries: TimelineEntry[] }) { ... }
```
- Sort `entries` ascending by `createdAt` (oldest top — D-C-04).
- Group consecutive entries by calendar day; render a day header ("Today"/"Yesterday"/"MMM D" — use a small date util, Claude's Discretion grouping).
- Comment rows: author (Avatar small + displayName) + body rendered in a `<p class="whitespace-pre-wrap">{body}</p>` (React text node = escaped; newlines preserved per D-C-03). Sync rows: a muted `label` (e.g. "Synced from Jira — moved to In progress").
- NEVER use dangerouslySetInnerHTML.

**`frontend/src/components/tickets/comment-input.tsx`** (D-C-04, renders below the timeline):
```ts
export function CommentInput({ onSubmit, submitting }: { onSubmit: (body: string) => void; submitting?: boolean }) { ... }
```
- A `<textarea>` with `microcopy.commentPlaceholder`, maxLength=10000, and a Send button. On submit, trim and call `onSubmit(trimmed)` only if `1 <= len <= 10000`; block blank/whitespace (button disabled when empty). Show a subtle char-count near the limit. Disable the button while `submitting`.
- Peer voice button label (e.g. "Post note", not "Submit").
  </action>
  <acceptance_criteria>
    - `grep -q "ActivityTimeline" frontend/src/components/tickets/activity-timeline.tsx`
    - `grep -q "whitespace-pre-wrap" frontend/src/components/tickets/activity-timeline.tsx`
    - `grep -cE "dangerouslySetInnerHTML" frontend/src/components/tickets/activity-timeline.tsx` returns 0
    - `grep -qE "Today|Yesterday" frontend/src/components/tickets/activity-timeline.tsx`
    - `grep -q "maxLength={10000}\|maxLength=\"10000\"\|10000" frontend/src/components/tickets/comment-input.tsx`
    - `grep -q "onSubmit" frontend/src/components/tickets/comment-input.tsx`
    - `cd frontend && npx vitest run src/components/tickets/activity-timeline.test.tsx src/components/tickets/comment-input.test.tsx` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/tickets/activity-timeline.test.tsx src/components/tickets/comment-input.test.tsx</automated>
  </verify>
  <done>Timeline renders ascending day-grouped comments + sync events as escaped plain text (newlines preserved, no innerHTML); CommentInput enforces the 1..10000 bound and calls onSubmit with the trimmed body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BlockedToggle (shared inline reason editor)</name>
  <files>frontend/src/components/tickets/blocked-toggle.tsx, frontend/src/components/tickets/blocked-toggle.test.tsx</files>
  <read_first>
    - frontend/src/components/tickets/status-pill.tsx (Plan 04 — the Blocked pill it toggles)
    - frontend/src/components/tickets/microcopy.ts (Task 1)
    - .planning/phases/12-assets-list-detail/12-CONTEXT.md §D-A (inline-combobox UX — the reason-capture template)
    - .planning/phases/13-tickets-list-detail/13-RESEARCH.md §D-P-03 (detail-page inline edit + optimistic)
  </read_first>
  <behavior>
    - Test 1: when not blocked, renders a "Mark blocked" control; clicking opens an inline reason text input + Save/Cancel (reassign-combobox shape).
    - Test 2: entering a reason + Save calls onToggle({ blocked: true, blockedReason }); reason capped at 500 chars; whitespace-only reason allowed→treated as no reason.
    - Test 3: when blocked, renders "Unblock"; clicking Unblock calls onToggle({ blocked: false, blockedReason: null }) (no reason prompt for unblock per D-P-03 detail UX).
    - Test 4: Cancel closes the inline editor without calling onToggle.
  </behavior>
  <action>
**`frontend/src/components/tickets/blocked-toggle.tsx`** (D-P-03, shared by drill footer + detail rail):
```ts
export function BlockedToggle({
  blocked, blockedReason, onToggle, pending,
}: {
  blocked: boolean;
  blockedReason: string | null;
  onToggle: (next: { blocked: boolean; blockedReason: string | null }) => void;
  pending?: boolean;
}) { ... }
```
- Not-blocked state: a button (`microcopy.markBlocked`). On click → reveal an inline `<input maxLength={500}>` (placeholder `microcopy.blockedPrompt`) + Save/Cancel buttons (Phase 12 reassign-combobox inline-edit shape). Save → `onToggle({ blocked: true, blockedReason: trimmedReason || null })`. Cancel → collapse, no call.
- Blocked state: render the Blocked indicator + a `microcopy.unblock` button → `onToggle({ blocked: false, blockedReason: null })` immediately (no reason prompt).
- Disable controls while `pending` (optimistic in-flight). The optimistic UI + mutation lives in the consumer (Plan 08); this component is the controlled UI shell.
- Keyboard accessible (Esc cancels the inline editor, Enter saves). No inline hex.
  </action>
  <acceptance_criteria>
    - `grep -q "BlockedToggle" frontend/src/components/tickets/blocked-toggle.tsx`
    - `grep -q "onToggle" frontend/src/components/tickets/blocked-toggle.tsx`
    - `grep -q "maxLength={500}\|maxLength=\"500\"\|500" frontend/src/components/tickets/blocked-toggle.tsx`
    - `grep -qE "pending" frontend/src/components/tickets/blocked-toggle.tsx`
    - NO raw hex: `grep -cE "#[0-9A-Fa-f]{6}" frontend/src/components/tickets/blocked-toggle.tsx` returns 0
    - `cd frontend && npx vitest run src/components/tickets/blocked-toggle.test.tsx` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/tickets/blocked-toggle.test.tsx</automated>
  </verify>
  <done>BlockedToggle is a controlled inline-edit shell (reassign-combobox shape) that captures a ≤500-char reason on block, immediate unblock with null reason, disables while pending, and emits onToggle — shared by drill footer + detail rail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend comment/watcher data → DOM | user-authored body + display names rendered |
| user input (comment, blocked reason) → onSubmit/onToggle | untrusted text leaves the form |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-19 | Tampering / Stored XSS | comment body + watcher display name in timeline/stack | mitigate | Rendered as React text nodes (`whitespace-pre-wrap`, no dangerouslySetInnerHTML); Avatar uses XSS-safe text node (T-12-04) |
| T-13-20 | Tampering | comment body / blocked reason oversize | mitigate | Client maxLength 10000 / 500 mirrors the backend Pydantic bound (defense in depth; backend is authoritative) |
| T-13-21 | Input validation | whitespace-only comment/reason | mitigate | trim + block blank before onSubmit/onToggle; reason coerces to null |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/tickets/` (all Plan 06 component tests) green
- `grep -rcE "dangerouslySetInnerHTML" frontend/src/components/tickets/*.tsx` returns 0
- `grep -rcE "#[0-9A-Fa-f]{6}" frontend/src/components/tickets/watcher-stack.tsx frontend/src/components/tickets/activity-timeline.tsx frontend/src/components/tickets/comment-input.tsx frontend/src/components/tickets/blocked-toggle.tsx frontend/src/components/tickets/ticket-asset-card.tsx` returns 0
</verification>

<success_criteria>
- WatcherStack, ActivityTimeline, CommentInput, BlockedToggle, TicketAssetCard built as controlled presentational components
- All take callbacks for mutations (hooks wire in Plan 08)
- Plain-text escaped rendering, no innerHTML, no inline hex, peer-voice copy
- All co-located tests green
</success_criteria>

<output>
After completion, create `.planning/phases/13-tickets-list-detail/13-06-SUMMARY.md`
</output>
