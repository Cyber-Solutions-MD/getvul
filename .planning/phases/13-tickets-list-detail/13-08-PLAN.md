---
phase: 13-tickets-list-detail
plan: 08
type: execute
wave: 3
depends_on: [13-03, 13-04, 13-06]
files_modified:
  - frontend/src/lib/queries/use-ticket-detail.ts
  - frontend/src/lib/queries/use-ticket-detail.test.ts
  - frontend/src/lib/queries/use-ticket-comments.ts
  - frontend/src/lib/queries/use-ticket-comments.test.tsx
  - frontend/src/lib/queries/use-ticket-watch.ts
  - frontend/src/lib/queries/use-ticket-watch.test.tsx
  - frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx
  - frontend/src/app/(authed)/dashboard/tickets/[id]/page.test.tsx
autonomous: true
requirements: [UX-05-04, UX-05-05, UX-05-01]
must_haves:
  truths:
    - "/tickets/[id] renders a two-column layout (1fr_340px, sticky rail @900px) cloned from assets/[id]/page.tsx"
    - "Main column shows linked vulnerabilities (3+ rich rows: severity glyph + CVE + CVSS), description, ActivityTimeline with CommentInput below it, and the status pills row"
    - "Right rail shows a Details card (StatusPill + SlaPill + inline BlockedToggle per D-P-03), a People card (assignee + reporter + WatcherStack with Watch/Watching toggle per D-W-03), and a TicketAssetCard cross-link"
    - "Adding a comment is optimistic; watch toggle is optimistic with rollback-on-error toast; blocked toggle reuses useMarkBlocked from 13-07 (no duplication)"
    - "Loading→skeleton, bad id→EmptyState/not-found, error→PartialFailureBanner (mutually exclusive branches)"
  artifacts:
    - path: "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"
      provides: "two-column ticket detail page composing Plan 04/06 primitives"
      contains: "WatcherStack"
    - path: "frontend/src/lib/queries/use-ticket-detail.ts"
      provides: "ticket detail query hook (logical-ticket group resolution, O1)"
      contains: "useTicketDetail"
    - path: "frontend/src/lib/queries/use-ticket-comments.ts"
      provides: "comment list query + optimistic create mutation"
      contains: "useAddComment"
    - path: "frontend/src/lib/queries/use-ticket-watch.ts"
      provides: "optimistic watch/unwatch toggle with rollback"
      contains: "useTicketWatch"
  key_links:
    - from: "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"
      to: "ActivityTimeline + CommentInput + WatcherStack + BlockedToggle + TicketAssetCard"
      via: "two-column composition of Plan 06 primitives"
      pattern: "ActivityTimeline"
    - from: "frontend/src/lib/queries/use-ticket-watch.ts"
      to: "POST/DELETE /api/v1/tickets/{id}/watch"
      via: "optimistic onMutate snapshot + onError rollback + toast (use-reassign-asset pattern)"
      pattern: "onMutate"
    - from: "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"
      to: "useMarkBlocked (13-07)"
      via: "BlockedToggle onToggle → markBlocked.mutate (no duplicate hook)"
      pattern: "useMarkBlocked"
---

<objective>
Build the `/tickets/[id]` two-column detail page (Success Criterion #4 / UX-05-04 / UX-05-05) by CLONING the structure of `assets/[id]/page.tsx` (verified two-column `grid-cols-[1fr_340px]` with `min-[900px]` gate + sticky right rail). Compose the Plan 04 presentational primitives (ProviderMark/StatusPill/SlaPill) and the Plan 06 detail-surface primitives (WatcherStack/ActivityTimeline/CommentInput/BlockedToggle/TicketAssetCard) into the main column + right rail. Add the three detail hooks (`use-ticket-detail`, `use-ticket-comments`, `use-ticket-watch`) matching the query-key + predicate-invalidation conventions from 13-07's `use-tickets.ts` and Phase 12's `useReassignAsset` (WR-01). Reuse `useMarkBlocked` from 13-07 for the blocked toggle — do NOT duplicate it.

**Logical-ticket identity (O1, RESOLVED — inherited from Plan 03):** A logical ticket is the group of `tickets` rows sharing one `external_ticket_url`; the route id is the canonical `first_ticket_id` (the MIN row id in the group, exactly what `GET /tickets` returns as `id`). All detail hooks pass that id through verbatim to `/tickets/{id}/...` — the backend (Plan 03 `_resolve_group`) maps it to the group. The frontend never reconstructs the group itself.

Purpose: This is the primary UX-05-04/05 deliverable surface — the analyst's full ticket workspace.
Output: use-ticket-detail, use-ticket-comments, use-ticket-watch + the `[id]/page.tsx` route + tests.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-tickets-list-detail/13-RESEARCH.md
@.planning/phases/13-tickets-list-detail/13-03-PLAN.md
@.planning/phases/13-tickets-list-detail/13-04-PLAN.md
@.planning/phases/13-tickets-list-detail/13-06-PLAN.md
@.planning/phases/13-tickets-list-detail/13-07-PLAN.md

<interfaces>
Backend (Plan 03):
- GET /api/v1/tickets/{id} → { id, provider, external_ticket_url, external_status, blocked, blocked_reason, sla_due_at, assignee:{userId, displayName, email?}|null, reporter:{userId, displayName, email?}|null, title, description?, subtitle, max_severity, vuln_count, critical_count, high_count, linked_vulns:[{cve, severity, cvss}], watchers:[{userId, displayName, email?, role?, createdAt?}], asset?:{assetId, hostname, osName, riskScore} }. **`reporter` is the ticket creator** (Plan 03 derives it from the Ticket's created_by / creator field, or the earliest `ticket.created` audit actor; null if unknowable → UI renders '—'). Cross-tenant id → 404.
- GET /api/v1/tickets/{id}/comments → [{ id, user_id, user_display_name, body, created_at, edited_at }] ascending by created_at (D-C-04).
- POST /api/v1/tickets/{id}/comments body { body } (1..10000, stripped). Returns the created comment.
- POST /api/v1/tickets/{id}/watch → { watching: true } (idempotent). DELETE /api/v1/tickets/{id}/watch → { watching: false } (idempotent).
- POST /api/v1/tickets/{id}/blocked body { blocked, blocked_reason } — consumed via 13-07 useMarkBlocked.

Frontend templates (READ before writing — exact patterns):
- assets/[id]/page.tsx: two-column `grid grid-cols-1 gap-6 p-6 min-[900px]:grid-cols-[1fr_340px]`; main `<section aria-label>` (NOT a nested <main>, BL-03); rail `<aside className="space-y-4 min-[900px]:sticky min-[900px]:top-4 min-[900px]:self-start">`; ErrorBoundary > Suspense > Inner; mutually-exclusive state branches; PartialFailureBanner uses FULL err.message (WR-10/WR-15, no slice).
- use-asset-detail.ts: useQuery keyed by queryKeys.{entity}.byId(id), enabled:!!id, staleTime 30_000, retry 1.
- use-reassign-asset.ts: optimistic onMutate (cancelQueries + snapshot + setQueryData patch) + onError (restore snapshot + error toast) + onSuccess (invalidate byId + all + predicate-invalidate cross-prefix) + retry:0.
- keys.ts tickets.*: { all:['tickets'], list(opts), byId(id)=['tickets','detail',id], comments(id)=['tickets',id,'comments'], watchers(id)=['tickets',id,'watchers'], rules() } — defined ONCE in Plan 04 (Wave 1). Import `queryKeys` read-only; this plan does NOT edit keys.ts.

Plan 04 primitives: ProviderMark, StatusPill({externalStatus, blocked}), SlaPill({dueAt}), VulnCount, type TicketProvider.
Plan 06 primitives:
- WatcherStack({ watchers: Watcher[] }) — Watcher = { userId, displayName, email?, role?, createdAt? }.
- ActivityTimeline({ entries: TimelineEntry[] }) — TimelineEntry = {kind:'comment', id, author, body, createdAt} | {kind:'sync', id, label, createdAt}.
- CommentInput({ onSubmit:(body:string)=>void, submitting? }).
- BlockedToggle({ blocked, blockedReason, onToggle:(next:{blocked, blockedReason})=>void, pending? }).
- TicketAssetCard({ assetId, hostname, osName, riskScore }).
- microcopy (peer-voice strings).
13-07 hooks: useMarkBlocked(id) — optimistic POST /tickets/{id}/blocked {blocked, blocked_reason}. REUSE for the rail BlockedToggle.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: use-ticket-detail + use-ticket-comments (list + optimistic create)</name>
  <files>frontend/src/lib/queries/use-ticket-detail.ts, frontend/src/lib/queries/use-ticket-detail.test.ts, frontend/src/lib/queries/use-ticket-comments.ts, frontend/src/lib/queries/use-ticket-comments.test.tsx</files>
  <read_first>
    - frontend/src/lib/queries/use-asset-detail.ts (FULL — useQuery byId(id) template: enabled:!!id, staleTime 30_000, retry 1)
    - frontend/src/lib/queries/use-reassign-asset.ts (FULL — optimistic onMutate/onError/onSuccess + predicate invalidation; clone for the comment create)
    - frontend/src/lib/queries/keys.ts (READ-ONLY — tickets.* keys defined in Plan 04; use byId/comments verbatim, do NOT edit)
    - frontend/src/lib/queries/use-tickets.ts (13-07 — match the TicketSummary field-naming + api.get convention)
    - .planning/phases/13-tickets-list-detail/13-03-PLAN.md §interfaces (detail + comments response shapes)
    - .planning/phases/13-tickets-list-detail/13-RESEARCH.md §Code Examples (tickets keys) + §Pattern 4
  </read_first>
  <behavior>
    - useTicketDetail Test: hits `/api/v1/tickets/${id}`, keyed by queryKeys.tickets.byId(id), enabled only when id is truthy (assert the queryKey + enabled gating; mock api).
    - useAddComment Test 1: onMutate cancels + snapshots `tickets.comments(id)` and optimistically appends a temp comment to the cached list (assert the cache contains the new body before resolve, via a mocked QueryClient).
    - useAddComment Test 2: onError restores the snapshot (optimistic temp comment removed) AND emits an error toast.
    - useAddComment Test 3: onSuccess invalidates `tickets.comments(id)` AND `tickets.byId(id)` (timeline + detail counts reconcile).
  </behavior>
  <action>
**`use-ticket-detail.ts`** — clone `use-asset-detail.ts`:
- Define `type TicketDetail` = the GET /tickets/{id} shape (id, provider: TicketProvider, externalTicketUrl, externalStatus, blocked, blockedReason, slaDueAt, assignee: Person | null, reporter: Person | null, title, description, maxSeverity, vulnCount, criticalCount, highCount, linkedVulns: {cve, severity, cvss}[], watchers: Watcher[], asset: {assetId, hostname, osName, riskScore} | null) where `Person = { userId: string; displayName: string; email?: string }`. **`reporter` is always present in the type (nullable)** — the People card renders it (falls back to '—' when null), so it must not be dropped as optional. Document the O1 identity rule in a header comment: the `id` is the canonical `first_ticket_id` group key; the backend resolves it to the `external_ticket_url` group.
- `export function useTicketDetail(id: string | null | undefined)` → `useQuery({ queryKey: queryKeys.tickets.byId(id ?? ''), queryFn: ({signal}) => api<TicketDetail>(\`/api/v1/tickets/${id}\`, {signal}), enabled: !!id, staleTime: 30_000, retry: 1 })`.

**`use-ticket-comments.ts`** — TWO exports:
- `export function useTicketComments(id: string | null | undefined)` → `useQuery({ queryKey: queryKeys.tickets.comments(id ?? ''), queryFn: ... api<Comment[]>(\`/api/v1/tickets/${id}/comments\`), enabled: !!id, staleTime: 30_000, retry: 1 })`. `type Comment = { id, userId, userDisplayName, body, createdAt, editedAt }`. List is already ascending server-side (D-C-04).
- `export function useAddComment(id: string)` — clone `use-reassign-asset.ts` optimistic shape:
  - `mutationFn: (body: string) => api(\`/api/v1/tickets/${id}/comments\`, { method:'POST', body: JSON.stringify({ body }), headers })` — send ONLY `{ body }` (mass-assignment guard T-12-08).
  - `onMutate`: cancel `tickets.comments(id)`, snapshot, optimistically append a temp comment (`{ id: 'optimistic-'+Date.now(), body, createdAt: new Date().toISOString(), userDisplayName: 'You', userId: '', editedAt: null }`) to the cached array.
  - `onError`: restore snapshot + error toast (useToast) `microcopy.commentError` or "Couldn't post that note. Try again." (peer voice, no "Please").
  - `onSuccess`: invalidate `tickets.comments(id)` + `tickets.byId(id)`.
  - `retry: 0`.
  </action>
  <acceptance_criteria>
    - `grep -q "export function useTicketDetail" frontend/src/lib/queries/use-ticket-detail.ts`
    - `grep -q "queryKeys.tickets.byId" frontend/src/lib/queries/use-ticket-detail.ts`
    - `grep -q "first_ticket_id\|external_ticket_url\|logical ticket" frontend/src/lib/queries/use-ticket-detail.ts` (O1 identity documented)
    - `grep -q "export function useTicketComments" frontend/src/lib/queries/use-ticket-comments.ts`
    - `grep -q "export function useAddComment" frontend/src/lib/queries/use-ticket-comments.ts`
    - `grep -q "onMutate" frontend/src/lib/queries/use-ticket-comments.ts`
    - `grep -q "JSON.stringify({ body })\|{ body }" frontend/src/lib/queries/use-ticket-comments.ts` (only the body field sent — T-12-08)
    - `grep -cE "retry: 0" frontend/src/lib/queries/use-ticket-comments.ts` returns >= 1
    - `cd frontend && npx vitest run src/lib/queries/use-ticket-detail.test.ts src/lib/queries/use-ticket-comments.test.tsx` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/queries/use-ticket-detail.test.ts src/lib/queries/use-ticket-comments.test.tsx</automated>
  </verify>
  <done>useTicketDetail clones use-asset-detail keyed by tickets.byId with the O1 identity documented; useTicketComments lists ascending comments; useAddComment is an optimistic-append mutation sending only {body} with rollback toast + invalidation of comments + byId.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: use-ticket-watch (optimistic toggle + rollback)</name>
  <files>frontend/src/lib/queries/use-ticket-watch.ts, frontend/src/lib/queries/use-ticket-watch.test.tsx</files>
  <read_first>
    - frontend/src/lib/queries/use-reassign-asset.ts (FULL — the optimistic+rollback+predicate-invalidation reference)
    - frontend/src/lib/queries/use-ticket-detail.ts (Task 1 — TicketDetail shape: watchers[] lives on the detail payload, patch it optimistically)
    - frontend/src/lib/queries/keys.ts (READ-ONLY — tickets.byId / tickets.watchers defined in Plan 04)
    - .planning/phases/13-tickets-list-detail/13-03-PLAN.md §interfaces (POST/DELETE /watch idempotent → {watching})
    - .planning/phases/13-tickets-list-detail/13-RESEARCH.md §Pattern 4 + §Pitfall 6 (missing snapshot hazard)
  </read_first>
  <behavior>
    - Test 1: useTicketWatch.toggle(true) issues POST /tickets/{id}/watch; toggle(false) issues DELETE (assert method via mocked api).
    - Test 2: onMutate snapshots `tickets.byId(id)` and optimistically flips the current-user watcher membership (button reflects Watching immediately).
    - Test 3: onError restores the snapshot AND emits an error toast (Pitfall 6 — never leave the button stuck "Watching").
    - Test 4: onSuccess invalidates `tickets.byId(id)` + `tickets.watchers(id)`.
  </behavior>
  <action>
**`use-ticket-watch.ts`** — clone `use-reassign-asset.ts` optimistic shape; the mutation variable is the desired boolean state:
- `export function useTicketWatch(id: string, currentUserId: string)`.
- `mutationFn: (next: boolean) => api(\`/api/v1/tickets/${id}/watch\`, { method: next ? 'POST' : 'DELETE' })` — empty body (watch carries no payload — send `{}`/no body, mass-assignment guard T-12-08).
- `onMutate: async (next) => { const key = queryKeys.tickets.byId(id); await qc.cancelQueries({queryKey:key}); const snapshot = qc.getQueryData(key); qc.setQueryData(key, patch watchers: if next add {userId: currentUserId, role:'watcher', ...}, else drop the currentUserId entry); return { snapshot }; }`.
- `onError`: restore `snapshot` to `tickets.byId(id)` + error toast (`microcopy` peer voice e.g. "Couldn't update watch. Try again."). (Pitfall 6.)
- `onSuccess`: invalidate `tickets.byId(id)` + `tickets.watchers(id)`.
- `retry: 0`.
- Header comment cites Pitfall 6 (snapshot mandatory) + the optimistic-toggle contract (D-W-03).
  </action>
  <acceptance_criteria>
    - `grep -q "export function useTicketWatch" frontend/src/lib/queries/use-ticket-watch.ts`
    - `grep -qE "method: next \? 'POST' : 'DELETE'|POST.*DELETE|'DELETE'" frontend/src/lib/queries/use-ticket-watch.ts`
    - `grep -q "onMutate" frontend/src/lib/queries/use-ticket-watch.ts`
    - `grep -q "snapshot" frontend/src/lib/queries/use-ticket-watch.ts`
    - `grep -q "queryKeys.tickets.byId" frontend/src/lib/queries/use-ticket-watch.ts`
    - `grep -cE "retry: 0" frontend/src/lib/queries/use-ticket-watch.ts` returns >= 1
    - `cd frontend && npx vitest run src/lib/queries/use-ticket-watch.test.tsx` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/queries/use-ticket-watch.test.tsx</automated>
  </verify>
  <done>useTicketWatch is an optimistic POST/DELETE toggle that snapshots tickets.byId, flips current-user watcher membership immediately, rolls back with a toast on error, and invalidates byId + watchers on success — no payload beyond the method.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /tickets/[id]/page.tsx two-column detail (main + rail + states)</name>
  <files>frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx, frontend/src/app/(authed)/dashboard/tickets/[id]/page.test.tsx</files>
  <read_first>
    - frontend/src/app/(authed)/dashboard/assets/[id]/page.tsx (FULL — CLONE the two-column structure, ErrorBoundary > Suspense > Inner, mutually-exclusive state branches, the min-[900px] grid + sticky aside, WR-10 full err.message)
    - frontend/src/app/(authed)/dashboard/assets/[id]/page.test.tsx (page test template)
    - frontend/src/components/tickets/{provider-mark,status-pill,sla-pill,vuln-count}.tsx (Plan 04 cells)
    - frontend/src/components/tickets/{watcher-stack,activity-timeline,comment-input,blocked-toggle,ticket-asset-card,microcopy}.tsx (Plan 06 primitives — exact props)
    - frontend/src/lib/queries/{use-ticket-detail,use-ticket-comments,use-ticket-watch}.ts (Tasks 1-2) + frontend/src/lib/queries/use-mark-blocked.ts (13-07 — REUSE, do not duplicate)
    - frontend/src/components/ui/Breadcrumb.tsx (Breadcrumb/Crumb)
    - .claude/skills/sketch-findings-getvul/references/page-layouts.md (two-column detail) — read only the two-column section
  </read_first>
  <behavior>
    - Test 1: given detail data, renders the two-column layout — assert the grid wrapper has `min-[900px]:grid-cols-[1fr_340px]`, the rail `<aside>` is present (data-testid), and the main column renders linked-vuln rows (CVE strings present), ActivityTimeline, and CommentInput below it.
    - Test 2: right rail renders StatusPill + SlaPill + BlockedToggle in the Details card, WatcherStack + a Watch/Watching button in the People card, and TicketAssetCard with a /assets/{assetId} link.
    - Test 3: adding a comment via CommentInput calls useAddComment (optimistic append visible in the timeline); the watch button toggles via useTicketWatch; the BlockedToggle Save calls useMarkBlocked.
    - Test 4: loading → skeleton; bad/missing id (404/no data) → EmptyState not-found (peer voice, links back to /tickets); error → PartialFailureBanner with the FULL err.message (no slice — WR-10/15). Branches mutually exclusive (WR-13).
  </behavior>
  <action>
Create `frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx`. CLONE the `assets/[id]/page.tsx` skeleton verbatim, then swap content:
- `'use client'`, `ErrorBoundary > Suspense > TicketDetailInner`. Same `SKELETON_COLUMNS` shape.
- `TicketDetailInner`: `const { id } = useParams<{ id: string }>();` → `const detail = useTicketDetail(id);` `const comments = useTicketComments(id);` `const watch = useTicketWatch(id, currentUserId);` `const markBlocked = useMarkBlocked(id);` (REUSE 13-07's hook — import it, do NOT redefine). Get `currentUserId` from the existing session/user hook used elsewhere (grep for the established current-user accessor; if none, read it from a context the app-shell already provides).
- **State branches (mutually exclusive, WR-13):** `detail.isLoading ? <SkeletonTable .../> : (detail.error || !detail.data) ? (is-404 ? <EmptyState not-found links /tickets> : <PartialFailureBanner errors=[{code:'http_error', requestId: String((detail.error as Error)?.message || 'unknown')}] onRetry={detail.refetch}/>) : <body>`. Pass FULL err.message (WR-10/15 — NO `.slice`).
- **Layout:** `<div className="grid grid-cols-1 gap-6 p-6 min-[900px]:grid-cols-[1fr_340px]">`.
- **Main column** `<section className="space-y-6" aria-label="Ticket details">`:
  1. `<header>`: `<Breadcrumb><Crumb href="/tickets">Tickets</Crumb><Crumb>{t.title}</Crumb></Breadcrumb>` then an H1 with `<ProviderMark provider={t.provider}/>` + ticket title + the status pills row (`<StatusPill externalStatus={t.externalStatus} blocked={t.blocked}/>` + `<SlaPill dueAt={t.slaDueAt}/>`).
  2. **Linked vulnerabilities** `<section aria-label="Linked vulnerabilities">` — render 3+ rich rows from `t.linkedVulns`, each row: severity glyph (Critical `■` / High `▲` / Medium `◆` / Low `○` per visual-language.md, colored by severity token) + CVE (mono) + CVSS score. Empty → `<EmptyState>` "No linked vulnerabilities".
  3. **Description** — `<p className="whitespace-pre-wrap text-text-muted">{t.description}</p>` (escaped text node; no dangerouslySetInnerHTML).
  4. **Activity** `<section aria-label="Activity">` — `<ActivityTimeline entries={mapCommentsAndSyncToEntries(comments.data)} />` then `<CommentInput onSubmit={(body)=>addComment.mutate(body)} submitting={addComment.isPending} />` BELOW the timeline (D-C-04). Map comment rows to `{kind:'comment', id, author: userDisplayName, body, createdAt}`. (Provider sync events: if the detail payload carries them, interleave; otherwise comments only.)
- **Right rail** `<aside className="space-y-4 min-[900px]:sticky min-[900px]:top-4 min-[900px]:self-start" data-testid="ticket-detail-rail">`:
  1. **Details card** — `<StatusPill externalStatus blocked/>` + `<SlaPill dueAt/>` + `<BlockedToggle blocked={t.blocked} blockedReason={t.blockedReason} pending={markBlocked.isPending} onToggle={(next)=>markBlocked.mutate({ blocked: next.blocked, blocked_reason: next.blockedReason })} />` (inline editor per D-P-03; the 13-07 useMarkBlocked sends only {blocked, blocked_reason}).
  2. **People card** — assignee row (`<Avatar>` + name) + reporter row (`<Avatar>` + `t.reporter?.displayName ?? '—'` — reporter is the ticket creator from Plan 03's detail payload; render '—' if null) + `<WatcherStack watchers={watcherList}/>` + a Watch/Watching `<button>`.
     - **Build the role-tagged watcher list (D-W-04, resolves the role-tag injection seam):** construct `watcherList` IN THIS PAGE before passing to WatcherStack — merge `t.assignee` (tag `role:'assignee'`), `t.reporter` (tag `role:'reporter'`), and `t.watchers` (each already tagged `role:'watcher'` unless the backend tagged it otherwise), then **dedupe by `userId` preferring the strongest role** (assignee > reporter > watcher) and **sort** assignee → reporter → others-chronological-by-createdAt. WatcherStack only renders + overflows; the ordering/dedupe is established here so it has role-tagged data to render. (CONTEXT D-W-04.)
     - Watch button wired to `watch.mutate(!isWatching)` where `isWatching = t.watchers.some(w => w.userId === currentUserId)`. Button label flips Watch↔Watching (D-W-03, eye icons from lucide-react).
  3. `<TicketAssetCard assetId={t.asset?.assetId ?? null} hostname={t.asset?.hostname} osName={t.asset?.osName} riskScore={t.asset?.riskScore} />`.
- `useDocumentTitle` (or the established title hook) → the ticket title. Peer-voice copy from `microcopy.ts`. NO inline hex (severity glyph colors use `text-severity-*` tokens).
  </action>
  <acceptance_criteria>
    - `grep -q "min-\[900px\]:grid-cols-\[1fr_340px\]" "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"`
    - `grep -q "min-\[900px\]:sticky" "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"`
    - `grep -q "WatcherStack" "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"`
    - `grep -q "ActivityTimeline" "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"`
    - `grep -q "CommentInput" "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"`
    - `grep -q "BlockedToggle" "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"`
    - `grep -q "TicketAssetCard" "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"`
    - `grep -q "useMarkBlocked" "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"` (REUSE 13-07, not redefined)
    - `grep -q "useTicketWatch" "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"`
    - `grep -q "PartialFailureBanner" "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"`
    - `grep -q "EmptyState" "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"`
    - `grep -q "whitespace-pre-wrap" "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"` (escaped description)
    - NO err.message slice: `grep -cE "\.message\)?\.slice\(|\.message\.substring" "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"` returns 0
    - NO dangerous HTML: `grep -cE "dangerouslySetInnerHTML" "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"` returns 0
    - NO raw hex: `grep -cE "#[0-9A-Fa-f]{6}" "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"` returns 0
    - `cd frontend && npx vitest run "src/app/(authed)/dashboard/tickets/[id]/page.test.tsx"` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/(authed)/dashboard/tickets/[id]/page.test.tsx"</automated>
  </verify>
  <done>/tickets/[id] is a two-column detail (1fr_340px + sticky rail @900px) cloned from assets/[id]: main column has linked vulns + description + ActivityTimeline + CommentInput + status pills; rail has Details (StatusPill+SlaPill+BlockedToggle via reused useMarkBlocked) + People (assignee+reporter+WatcherStack+Watch toggle via useTicketWatch) + TicketAssetCard; three mutually-exclusive states; no slice/innerHTML/hex.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /comments, POST/DELETE /watch, POST /blocked | mutation bodies + path id cross here |
| backend comment/watcher data → DOM | user-authored body + display names rendered into the timeline/stack |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-26 | Tampering | mass assignment on add-comment / watch | mitigate | useAddComment sends ONLY {body}; useTicketWatch sends no body (method only) — T-12-08; backend Pydantic + idempotent endpoints reject extras |
| T-13-27 | Tampering / Stored XSS | comment body + description rendered in the detail page | mitigate | Description + comments rendered as React text nodes with `whitespace-pre-wrap` (newline preservation, no dangerouslySetInnerHTML); ActivityTimeline (Plan 06) already escapes; watcher names via Avatar text node (T-12-04) |
| T-13-28 | Info Disclosure | cross-tenant ticket detail via crafted id | accept | Enforced backend-side (Plan 03 `_resolve_group` tenant filter → 404 on foreign id); frontend renders only the authorized payload it receives |
| T-13-29 | Info Disclosure | error message leakage on detail load | accept | Full err.message passed to PartialFailureBanner which truncates visually (WR-10); backend scrubs sensitive details server-side |
| T-13-30 | Repudiation | watch/comment without optimistic-rollback integrity | mitigate | onMutate snapshot + onError restore (Pitfall 6) prevents the UI asserting a state the server never committed; backend audit (Plan 03) is authoritative |
</threat_model>

<verification>
- `cd frontend && npx vitest run "src/app/(authed)/dashboard/tickets/[id]/page.test.tsx" src/lib/queries/use-ticket-detail.test.ts src/lib/queries/use-ticket-comments.test.tsx src/lib/queries/use-ticket-watch.test.tsx` green
- `grep -rcE "#[0-9A-Fa-f]{6}" "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"` returns 0
- `grep -rcE "dangerouslySetInnerHTML" "frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx"` returns 0
- `grep -c "useMarkBlocked" frontend/src/lib/queries/use-mark-blocked.ts` confirms the 13-07 hook is reused (NOT redefined in this plan's files)
</verification>

<success_criteria>
- /tickets/[id] renders the two-column layout (1fr_340px + sticky rail @900px) with main (linked vulns + description + timeline + comment input + status pills) and rail (Details + People + Asset card)
- Comment add is optimistic; watch toggle optimistic with rollback toast; blocked toggle reuses 13-07 useMarkBlocked
- Loading/not-found/error states are mutually exclusive; full err.message preserved
- O1 logical-ticket identity carried verbatim through the hooks; no inline hex; no innerHTML
</success_criteria>

<output>
After completion, create `.planning/phases/13-tickets-list-detail/13-08-SUMMARY.md`
</output>
