---
phase: 13-tickets-list-detail
plan: 05
type: execute
wave: 2
depends_on: [13-04]
files_modified:
  - frontend/src/components/vulnerabilities/drill-panel.tsx
  - frontend/src/components/vulnerabilities/drill-panel-mobile.tsx
  - frontend/src/components/tickets/ticket-drill-content.tsx
  - frontend/src/components/tickets/ticket-drill-content.test.tsx
autonomous: true
requirements: [UX-05-01]
must_haves:
  truths:
    - "DrillPanel + DrillPanelMobile accept an optional idKey + id + renderContent + ariaLabel; existing vuln callers (passing only cveId) behave identically"
    - "close() deletes 'open' + the active idKey (cve for vuln, ticket for tickets)"
    - "TicketDrillContent renders header (provider mark + ID + title + close), body (top-3 vulns, truncated description, status+SLA pills), sticky footer (Open in provider / Open full detail / Mark blocked)"
    - "Existing vuln drill-panel tests stay green unchanged"
  artifacts:
    - path: "frontend/src/components/vulnerabilities/drill-panel.tsx"
      provides: "generalized DrillPanel chrome with content slot + idKey"
      contains: "renderContent"
    - path: "frontend/src/components/tickets/ticket-drill-content.tsx"
      provides: "ticket drill body slotted into the shared chrome"
      contains: "TicketDrillContent"
  key_links:
    - from: "frontend/src/components/tickets/ticket-drill-content.tsx"
      to: "ProviderMark / StatusPill / SlaPill / VulnCount"
      via: "imports from components/tickets primitives"
      pattern: "provider-mark"
    - from: "frontend/src/components/vulnerabilities/drill-panel.tsx"
      to: "URL state"
      via: "close() deletes idKey (default 'cve')"
      pattern: "idKey"
---

<objective>
Generalize the Phase 11 `DrillPanel` / `DrillPanelMobile` chrome with a content slot + parameterized URL key (D-D-02 — additive refactor, NOT a fork), then build `<TicketDrillContent>` (D-D-01) that lives inside that shared chrome. The URL contract for ticket drills becomes `?ticket=...&open=drill` (mirroring vuln `?cve=...`).

**Regression-safety contract (the gate):** Existing vuln callers pass only `cveId` (no new props). With `idKey` defaulting to `'cve'` and the content defaulting to `<DrillContent idOrCve={...} />`, vuln drill behavior is byte-for-byte preserved. **The existing `drill-panel.test.tsx` + `drill-panel-mobile.test.tsx` MUST stay green WITHOUT modification** — they are the regression gate (RESEARCH Pitfall 1, VALIDATION D-D-02 row). Do not edit those test files except to add NEW ticket-mode cases (if you add any, append; never weaken existing assertions).

Purpose: The `/tickets` list (Plan 07) opens the drill with `idKey="ticket"` + `renderContent={() => <TicketDrillContent/>}`. Depends on Plan 04 primitives (ProviderMark/StatusPill/SlaPill/VulnCount).
Output: generalized drill-panel*.tsx + TicketDrillContent + tests.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-tickets-list-detail/13-RESEARCH.md

<interfaces>
Current DrillPanel (VERIFIED):
- `DrillPanel({ cveId: string | null, originRowRef? })` — isOpen = params.get('open')==='drill' && cveId!==null; close() deletes 'open'+'cve'; renders <DrillContent idOrCve={cveId} onClose={close}/> inside <aside w-[420px] aria-label="Vulnerability detail">.
- `DrillPanelMobile({ cveId: string | null })` — vaul Drawer, open = isMobile && params.get('open')==='drill' && cveId!==null; close() deletes 'open'+'cve'.
From Plan 04 (components/tickets):
- ProviderMark({provider}), StatusPill({externalStatus, blocked}), SlaPill({dueAt}), VulnCount({total,critical,high}), type TicketProvider
DrillContent ALREADY accepts a renderConfirm render-prop (slot precedent — proves the pattern works).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize DrillPanel + DrillPanelMobile chrome (additive, back-compat)</name>
  <files>frontend/src/components/vulnerabilities/drill-panel.tsx, frontend/src/components/vulnerabilities/drill-panel-mobile.tsx</files>
  <read_first>
    - frontend/src/components/vulnerabilities/drill-panel.tsx (FULL — current chrome)
    - frontend/src/components/vulnerabilities/drill-panel-mobile.tsx (FULL — vaul chrome)
    - frontend/src/components/vulnerabilities/drill-panel.test.tsx (the regression gate — understand what it asserts so you don't break it)
    - frontend/src/components/vulnerabilities/drill-content.tsx (renderConfirm slot precedent)
    - .planning/phases/13-tickets-list-detail/13-RESEARCH.md §Pattern 3 (the recommended additive refactor steps verbatim)
  </read_first>
  <action>
Make the refactor STRICTLY ADDITIVE (RESEARCH Pattern 3). For BOTH `drill-panel.tsx` and `drill-panel-mobile.tsx`:

1. Widen `Props` to:
```ts
type Props = {
  cveId?: string | null;              // back-compat alias (vuln callers keep using this)
  id?: string | null;                 // generic entity id
  idKey?: string;                     // URL param key, default 'cve'
  renderContent?: (args: { id: string; onClose: () => void }) => React.ReactNode;
  ariaLabel?: string;                 // default 'Vulnerability detail'
  originRowRef?: React.RefObject<HTMLElement | null> | null;
};
```
2. Resolve `const effectiveId = id ?? cveId ?? null;` and `const key = idKey ?? 'cve';`. `isOpen` becomes `params?.get('open') === 'drill' && effectiveId !== null`.
3. `close()` deletes `'open'` + `key` (so it removes `ticket` when `idKey='ticket'`, `cve` by default). Keep the `originRowRef?.current?.focus()` line.
4. `aria-label` uses `ariaLabel ?? 'Vulnerability detail'`.
5. Body: `renderContent ? renderContent({ id: effectiveId, onClose: close }) : <DrillContent idOrCve={effectiveId} onClose={close} />`. (Mobile: same logic inside the vaul Drawer.Content.)
6. **Do not change any default behavior**: with no new props, `idKey='cve'`, content = DrillContent, aria-label unchanged → existing callers + tests unaffected.

Confirm the existing `?cve=` deletion path still works: vuln callers pass `cveId` → `effectiveId=cveId`, `key='cve'`, close deletes `cve`. Identical to before.
  </action>
  <acceptance_criteria>
    - `grep -q "renderContent" frontend/src/components/vulnerabilities/drill-panel.tsx`
    - `grep -q "idKey" frontend/src/components/vulnerabilities/drill-panel.tsx`
    - `grep -q "renderContent" frontend/src/components/vulnerabilities/drill-panel-mobile.tsx`
    - `grep -q "ariaLabel" frontend/src/components/vulnerabilities/drill-panel.tsx`
    - default still 'cve': `grep -qE "idKey \?\? 'cve'|idKey = 'cve'" frontend/src/components/vulnerabilities/drill-panel.tsx`
    - REGRESSION GATE: `cd frontend && npx vitest run src/components/vulnerabilities/drill-panel.test.tsx src/components/vulnerabilities/drill-panel-mobile.test.tsx` passes WITHOUT edits to those test files (verify via `git diff --stat` showing the .test.tsx files unchanged, OR only additions)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/vulnerabilities/drill-panel.test.tsx src/components/vulnerabilities/drill-panel-mobile.test.tsx</automated>
  </verify>
  <done>Both panels accept idKey/id/renderContent/ariaLabel with vuln-preserving defaults; close() deletes the active idKey; existing vuln drill tests pass unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TicketDrillContent body (D-D-01)</name>
  <files>frontend/src/components/tickets/ticket-drill-content.tsx, frontend/src/components/tickets/ticket-drill-content.test.tsx</files>
  <read_first>
    - frontend/src/components/vulnerabilities/drill-content.tsx (header/body/footer layout template + close button + renderConfirm slot for the mobile nested-confirm)
    - frontend/src/components/tickets/provider-mark.tsx, status-pill.tsx, sla-pill.tsx, vuln-count.tsx (Plan 04 primitives to compose)
    - .claude/skills/sketch-findings-getvul/sources/006-tickets-sunset/README.md (sketch 006 variant A drill content)
    - .claude/skills/sketch-findings-getvul/references/interaction-patterns.md (drill-down panel chrome contract)
    - .planning/phases/13-tickets-list-detail/13-RESEARCH.md §D-D-01 (panel content spec verbatim)
  </read_first>
  <behavior>
    - Test 1: given a ticket with provider="jira", id, title, and 3 linked vulns, renders the header (ProviderMark + mono ID + truncated title + a close button calling onClose).
    - Test 2: body renders the top-3 linked vulns (severity glyph + CVE + CVSS), and when >3 vulns shows "+N more" linking to /tickets/[id].
    - Test 3: body renders a status+SLA pill row (StatusPill + SlaPill) and a description truncated with a "Show full" link to /tickets/[id].
    - Test 4: sticky footer renders 3 actions — "Open in {provider}" (external link, target=_blank), "Open full detail" (internal link to /tickets/[id]), and a "Mark blocked / Unblock" toggle slot (renders the BlockedToggle placeholder/prop; the real toggle wires in Plan 06/08).
  </behavior>
  <action>
**`frontend/src/components/tickets/ticket-drill-content.tsx`** (D-D-01). Props:
```ts
type TicketDrillContentProps = {
  ticketId: string;
  ticket?: TicketDrillData;          // fetched by the caller (list passes the row's summary; full fetch optional)
  onClose: () => void;
  renderBlockedToggle?: (args: { ticketId: string }) => React.ReactNode; // slot — real toggle from Plan 06
};
```
Layout mirrors `drill-content.tsx`:
- **Header:** `<ProviderMark provider={ticket.provider}/>` + `<span class="font-mono text-sm">{ticket.externalId}</span>` + truncated title (`truncate`) + a close `<button onClick={onClose} aria-label="Close">` with the lucide `X` icon (import from lucide-react as drill-content does).
- **Body:**
  1. Linked-vulns mini-list — map top 3 by severity, each ~28px row: severity glyph (Critical `■` / High `▲` / Medium `◆` / Low `○` from visual-language) + CVE (mono) + CVSS score. If `total > 3`, render `<Link href={'/tickets/' + ticketId}>+{total-3} more</Link>`.
  2. Description truncated to ~6 lines (`line-clamp-6`) + `<Link href={'/tickets/' + ticketId} class="text-text-muted">Show full →</Link>`.
  3. Status + SLA row: `<StatusPill externalStatus={ticket.externalStatus} blocked={ticket.blocked}/> <SlaPill dueAt={ticket.slaDueAt}/>`.
- **Footer (sticky bottom):** action bar with 3 controls:
  - `Open in {provider}` — `<a href={ticket.externalUrl} target="_blank" rel="noopener noreferrer">` (D-D-01).
  - `Open full detail` — `<Link href={'/tickets/' + ticketId}>`.
  - Blocked toggle — render `renderBlockedToggle?.({ ticketId })` if provided, else a disabled placeholder button labelled "Mark blocked" (the wired toggle arrives in Plan 06 as `<BlockedToggle>` and is passed in by the list page).
- Copy: peer-not-butler ("Open in Jira", "Open full detail" — no "Click to..."). NO raw hex; severity glyph colors via `text-severity-*` classes.

Define a minimal `TicketDrillData` type here (or import from a shared ticket types file) so Plan 07 has the contract. Keep it presentational — no data fetching inside this component (the caller supplies `ticket`).
  </action>
  <acceptance_criteria>
    - `grep -q "TicketDrillContent" frontend/src/components/tickets/ticket-drill-content.tsx`
    - `grep -q "provider-mark" frontend/src/components/tickets/ticket-drill-content.tsx` (imports ProviderMark)
    - `grep -q "status-pill" frontend/src/components/tickets/ticket-drill-content.tsx`
    - `grep -q "sla-pill" frontend/src/components/tickets/ticket-drill-content.tsx`
    - `grep -qE "target=.\"?_blank" frontend/src/components/tickets/ticket-drill-content.tsx`
    - `grep -q "/tickets/" frontend/src/components/tickets/ticket-drill-content.tsx` (full-detail link)
    - `grep -q "renderBlockedToggle" frontend/src/components/tickets/ticket-drill-content.tsx`
    - NO raw hex: `grep -cE "#[0-9A-Fa-f]{6}" frontend/src/components/tickets/ticket-drill-content.tsx` returns 0
    - `cd frontend && npx vitest run src/components/tickets/ticket-drill-content.test.tsx` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/tickets/ticket-drill-content.test.tsx</automated>
  </verify>
  <done>TicketDrillContent renders the D-D-01 header/body/footer composing Plan 04 primitives, with an "Open in provider" external link, "Open full detail" internal link, and a blocked-toggle slot; pure presentational, no inline hex.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ticket.externalUrl → anchor href | provider-supplied URL rendered as a link |
| URL search params → drill open state | crafted ?ticket= / ?open= params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-16 | Tampering | "Open in provider" anchor (external URL) | mitigate | `rel="noopener noreferrer"` on target=_blank; externalUrl originates from the connector-created ticket (backend-controlled), rendered as href text node |
| T-13-17 | Tampering | crafted ?ticket= URL param | mitigate | Param value is passed to a fetch keyed by uuid on the backend (BL-02 422 on malformed); drill only renders when a matching row exists; no class/var derived from the raw param |
| T-13-18 | DoS / regression | drill chrome refactor breaks vuln panel | mitigate | Additive-only props with vuln-preserving defaults; existing vuln drill tests are the unmodified regression gate |
</threat_model>

<verification>
- REGRESSION: `cd frontend && npx vitest run src/components/vulnerabilities/drill-panel*.test.tsx` green (unchanged test files)
- `cd frontend && npx vitest run src/components/tickets/ticket-drill-content.test.tsx` green
- `git diff --stat frontend/src/components/vulnerabilities/drill-panel.test.tsx` shows no deletions/weakened assertions
</verification>

<success_criteria>
- DrillPanel/DrillPanelMobile slot-generalized, vuln behavior preserved (regression gate green)
- TicketDrillContent composes Plan 04 primitives per D-D-01
- URL contract ?ticket=&open=drill works for ticket drills
</success_criteria>

<output>
After completion, create `.planning/phases/13-tickets-list-detail/13-05-SUMMARY.md`
</output>
