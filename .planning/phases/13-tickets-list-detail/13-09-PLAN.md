---
phase: 13-tickets-list-detail
plan: 09
type: execute
wave: 2
depends_on: [13-04]
files_modified:
  - frontend/src/app/(authed)/dashboard/tickets/rules/page.tsx
  - frontend/src/app/(authed)/dashboard/tickets/rules/page.test.tsx
  - frontend/src/lib/queries/use-ticket-rules.ts
  - frontend/src/lib/queries/use-ticket-rules.test.ts
  - frontend/src/components/shell/sidebar.tsx
autonomous: true
requirements: [UX-05-01]
must_haves:
  truths:
    - "/tickets/rules is a standalone route (app/(authed)/dashboard/tickets/rules/page.tsx) — a full sunset rewrite of the v1 ?tab=rules surface (D-S-01)"
    - "The rules page reuses sunset tokens + ChipBar + the state primitives (SkeletonTable on load, EmptyState on no rules, PartialFailureBanner on error) with mutually-exclusive branches"
    - "The sidebar Tickets-group rules link points to /tickets/rules (not the v1 ?tab=rules query param)"
    - "No v1 carryover (no RulesPanel/CommentModal import) and no raw hex on the new files"
  artifacts:
    - path: "frontend/src/app/(authed)/dashboard/tickets/rules/page.tsx"
      provides: "standalone sunset /tickets/rules route"
      contains: "ChipBar"
    - path: "frontend/src/lib/queries/use-ticket-rules.ts"
      provides: "ticket-rules list query hook (GET /api/v1/tickets/rules)"
      contains: "useTicketRules"
    - path: "frontend/src/components/shell/sidebar.tsx"
      provides: "sidebar Rules link → /tickets/rules"
      contains: "/dashboard/tickets/rules"
  key_links:
    - from: "frontend/src/components/shell/sidebar.tsx"
      to: "/dashboard/tickets/rules"
      via: "WORKFLOW_ITEMS / sub-nav link to the new route (replaces v1 ?tab=rules)"
      pattern: "/dashboard/tickets/rules"
    - from: "frontend/src/app/(authed)/dashboard/tickets/rules/page.tsx"
      to: "GET /api/v1/tickets/rules"
      via: "useTicketRules query hook"
      pattern: "useTicketRules"
---

<objective>
Split the v1 `?tab=rules` surface into its own standalone route `app/(authed)/dashboard/tickets/rules/page.tsx` with a FULL sunset rewrite (D-S-01 / UX-05-01). The rules data already exists server-side (`GET /api/v1/tickets/rules` — verified present), so this plan adds NO backend. It reuses the existing sunset tokens, the generic `<ChipBar>` (Phase 12 primitive), and the state primitives (SkeletonTable / EmptyState / PartialFailureBanner) exactly as the main `/tickets` list does — zero v1 carryover, no raw hex. The sidebar Tickets-group link for rules is repointed from v1's `?tab=rules` query param to `/tickets/rules`, and any old `?tab=rules` branch on the existing tickets surface is removed (the v1 page is fully replaced by 13-07, so the cleanup is confirming no surviving `?tab=rules` reference).

Purpose: Eliminates the last v1 carryover on the tickets surface and gives automation rules a first-class, state-covered sunset route.
Output: rules/page.tsx + use-ticket-rules hook + sidebar link update + tests.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-tickets-list-detail/13-RESEARCH.md
@.planning/phases/13-tickets-list-detail/13-07-PLAN.md

<interfaces>
Backend rules endpoints (VERIFIED present — NO changes in this plan):
- GET    /api/v1/tickets/rules            → list rules [{ id, name, enabled, conditions, action, ... }]
- POST   /api/v1/tickets/rules            → create
- PATCH  /api/v1/tickets/rules/{rule_id}  → update
- DELETE /api/v1/tickets/rules/{rule_id}  → delete
- POST   /api/v1/tickets/rules/{rule_id}/run → run-now
(Confirm the exact rule row shape by reading backend/app/ticketing/router.py rules handlers + schemas.py during execution; mirror the field names verbatim.)

Frontend templates (READ before writing):
- frontend/src/app/(authed)/dashboard/assets/page.tsx — sunset list-page composition (ErrorBoundary > Suspense > Inner, mutually-exclusive state branches WR-13, full err.message WR-10).
- frontend/src/components/ui/ChipBar.tsx — generic descriptor-driven ChipBar (each axis carries a hardcoded allowList, T-12-05).
- frontend/src/lib/queries/use-assets.ts — useQuery + co-located exported buildSearchParams convention.
- frontend/src/components/states/* — SkeletonTable / EmptyState / PartialFailureBanner.
- frontend/src/components/shell/sidebar.tsx — WORKFLOW_ITEMS has the Tickets entry (href '/dashboard/tickets'); the v1 rules link was a `?tab=rules` query param.

v1 anti-pattern reference (do NOT salvage): frontend/src/app/(authed)/dashboard/tickets/page.tsx `RulesPanel` (line ~566), `?tab=rules` state (line ~44), `CommentModal`. Salvage only the understanding of which rules endpoints exist.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: use-ticket-rules query hook</name>
  <files>frontend/src/lib/queries/use-ticket-rules.ts, frontend/src/lib/queries/use-ticket-rules.test.ts</files>
  <read_first>
    - frontend/src/lib/queries/use-assets.ts (FULL — useQuery shape: queryKey, queryFn api<...>, staleTime 30_000, retry 1)
    - frontend/src/lib/queries/keys.ts (READ-ONLY — `queryKeys.tickets.rules()` is defined in Plan 04; import and use it, do NOT edit this file)
    - backend/app/ticketing/router.py rules handlers (~494-620) + backend/app/ticketing/schemas.py (the rule row shape — mirror field names verbatim)
  </read_first>
  <behavior>
    - Test 1: useTicketRules hits `/api/v1/tickets/rules`, keyed by a stable rules query key (assert queryKey + the GET path via mocked api).
    - Test 2: the hook's TicketRule type exposes the verbatim backend field names (id, name, enabled, ...) — assert by feeding a mocked response and reading a row field.
  </behavior>
  <action>
**`use-ticket-rules.ts`** — clone the `use-assets.ts` query shape (read-only list, no mutation needed in this plan — CRUD affordances on rules are existing endpoints but the sunset rewrite scope is the read + state-covered surface; surface create/run as caller-provided handlers if the page wires them, but the hook itself is the list query):
- Define `type TicketRule` with the EXACT field names returned by `GET /api/v1/tickets/rules` (read schemas.py — likely `{ id: string; name: string; enabled: boolean; conditions: ...; action: ...; created_at?: string }`; mirror verbatim, do not invent).
- Use the `queryKeys.tickets.rules()` key that is ALREADY defined in `keys.ts` by Plan 04 (Wave 1) — `import { queryKeys } from '@/lib/queries/keys'`. Do NOT edit keys.ts or re-declare any tickets key here (keys.ts is Plan 04's single-owner foundation; this is why this plan depends_on 13-04).
- `export function useTicketRules()` → `useQuery({ queryKey: queryKeys.tickets.rules(), queryFn: ({signal}) => api<TicketRule[]>('/api/v1/tickets/rules', {signal}), staleTime: 30_000, retry: 1 })`.
  </action>
  <acceptance_criteria>
    - `grep -q "export function useTicketRules" frontend/src/lib/queries/use-ticket-rules.ts`
    - `grep -q "/api/v1/tickets/rules" frontend/src/lib/queries/use-ticket-rules.ts`
    - `grep -q "export type TicketRule\|type TicketRule" frontend/src/lib/queries/use-ticket-rules.ts`
    - `cd frontend && npx vitest run src/lib/queries/use-ticket-rules.test.ts` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/queries/use-ticket-rules.test.ts</automated>
  </verify>
  <done>useTicketRules is a read-only list query against GET /api/v1/tickets/rules keyed under the tickets prefix, with a TicketRule type mirroring the backend field names verbatim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /tickets/rules/page.tsx sunset rewrite + sidebar link update</name>
  <files>frontend/src/app/(authed)/dashboard/tickets/rules/page.tsx, frontend/src/app/(authed)/dashboard/tickets/rules/page.test.tsx, frontend/src/components/shell/sidebar.tsx</files>
  <read_first>
    - frontend/src/app/(authed)/dashboard/assets/page.tsx (FULL — clone the ErrorBoundary > Suspense > Inner composition + mutually-exclusive state branches + full err.message WR-10)
    - frontend/src/components/ui/ChipBar.tsx (ChipAxis descriptor shape + allowList requirement T-12-05)
    - frontend/src/components/shell/sidebar.tsx (FULL — WORKFLOW_ITEMS / NavItem shape; add the Rules link as a /dashboard/tickets/rules entry)
    - frontend/src/lib/queries/use-ticket-rules.ts (Task 1)
    - frontend/src/components/states/* (SkeletonTable / EmptyState / PartialFailureBanner)
    - .claude/skills/sketch-findings-getvul/sources/006-tickets-sunset/README.md (rules surface intent) — read only the rules section
  </read_first>
  <behavior>
    - Test 1: the rules page renders a ChipBar (e.g. an Enabled/Disabled status filter axis) + the rules table/list given list data; rows show the rule name + enabled state.
    - Test 2: state coverage is mutually exclusive (WR-13) — loading → SkeletonTable; no rules → EmptyState (peer voice, e.g. "No automation rules yet — create one to auto-route new findings."); error → PartialFailureBanner with the FULL err.message (no slice, WR-10/15).
    - Test 3 (sidebar): the sidebar renders a link to `/dashboard/tickets/rules` (assert the href is present) and contains NO `?tab=rules` query-param reference.
    - Test 4: no inline hex on the new page; no v1 RulesPanel/CommentModal import.
  </behavior>
  <action>
**`frontend/src/app/(authed)/dashboard/tickets/rules/page.tsx`** — full sunset rewrite cloning `assets/page.tsx`:
- `'use client'`, `ErrorBoundary > Suspense > RulesInner`. Same `SKELETON_COLUMNS` style.
- `RulesInner`: `const q = useTicketRules();` Filters via `useUrlStateList`/`useUrlState` if a chip axis is wired (status: enabled/disabled).
- **ChipBar:** wrap `<ChipBar>` with at least a Status axis (`allowList: ['enabled','disabled']`, T-12-05) + a search box (matches rule name) — mirror the assets-chip-bar wiring. (Keep axes minimal; the surface is rules, not tickets.)
- **State branches (mutually exclusive, WR-13):** `q.error ? <PartialFailureBanner errors=[{code:'http_error', requestId:String((q.error as Error)?.message || 'unknown')}] onRetry={q.refetch}/> : q.isLoading ? <SkeletonTable .../> : (q.data?.length ?? 0) === 0 ? <EmptyState>...</EmptyState> : <RulesList rules={filteredRules}/>`. Full err.message (WR-10/15, NO `.slice`).
- **Rules list:** render each rule row — name, enabled pill (sunset tokens, e.g. `bg-severity-low/10 text-severity-low` for enabled / `bg-surface-2 text-text-faint` for disabled), and a summary of conditions/action. Existing CRUD/run endpoints can be wired as row actions (PATCH toggle, DELETE, run-now) using the established mutation pattern — but at minimum ship the read + state-covered surface; if wiring mutations, send only the minimal field per row action (no mass assignment, T-12-08).
- Header: `useDocumentTitle('Automation rules')` (or the established title hook) + a peer-voice heading. NO inline hex; sunset tokens only. NO import of v1 `RulesPanel`/`CommentModal`.

**`frontend/src/components/shell/sidebar.tsx`** — update the Tickets-group rules link (D-S-01, CONTEXT `<code_context>` integration point):
- The v1 rules link was a `?tab=rules` query param. Add a Rules nav entry pointing to `/dashboard/tickets/rules` (either as a sibling NavItem in `WORKFLOW_ITEMS` or a sub-link under Tickets — match the existing NavItem shape; no chip per D-N-01). Ensure the rendered href is exactly `/dashboard/tickets/rules`.
- Confirm NO surviving `?tab=rules` reference remains in the sidebar.

**Old `?tab=rules` branch cleanup:** the v1 `frontend/src/app/(authed)/dashboard/tickets/page.tsx` is fully replaced by 13-07 (the v1 `?tab=rules` state lived only in that file). Confirm 13-07's rewritten `page.tsx` carries no `?tab=rules` branch; if any survives, remove it. (No separate redirect file is needed — the route is new and the v1 query param is dropped with the v1 page.)
  </action>
  <acceptance_criteria>
    - `grep -q "ChipBar" "frontend/src/app/(authed)/dashboard/tickets/rules/page.tsx"`
    - `grep -q "useTicketRules" "frontend/src/app/(authed)/dashboard/tickets/rules/page.tsx"`
    - `grep -q "SkeletonTable" "frontend/src/app/(authed)/dashboard/tickets/rules/page.tsx"`
    - `grep -q "EmptyState" "frontend/src/app/(authed)/dashboard/tickets/rules/page.tsx"`
    - `grep -q "PartialFailureBanner" "frontend/src/app/(authed)/dashboard/tickets/rules/page.tsx"`
    - `grep -q "/dashboard/tickets/rules" frontend/src/components/shell/sidebar.tsx`
    - `grep -c "tab=rules" frontend/src/components/shell/sidebar.tsx` returns 0
    - NO v1 salvage: `grep -cE "RulesPanel|CommentModal" "frontend/src/app/(authed)/dashboard/tickets/rules/page.tsx"` returns 0
    - NO err.message slice: `grep -cE "\.message\)?\.slice\(|\.message\.substring" "frontend/src/app/(authed)/dashboard/tickets/rules/page.tsx"` returns 0
    - NO raw hex: `grep -cE "#[0-9A-Fa-f]{6}" "frontend/src/app/(authed)/dashboard/tickets/rules/page.tsx"` returns 0
    - `cd frontend && npx vitest run "src/app/(authed)/dashboard/tickets/rules/page.test.tsx"` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/(authed)/dashboard/tickets/rules/page.test.tsx"</automated>
  </verify>
  <done>/tickets/rules is a standalone sunset route reusing ChipBar + state primitives with mutually-exclusive branches and full err.message; the sidebar links to /dashboard/tickets/rules with no surviving ?tab=rules; no v1 RulesPanel/CommentModal salvage; no inline hex.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL filter params → ChipBar / DOM | reflected rule-filter chip values |
| backend rules data → DOM | rule names / conditions rendered into the list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-31 | Tampering | reflected XSS via rules chip URL params | mitigate | Each ChipAxis carries a hardcoded allowList; useUrlStateList clamps on read+write (T-12-05); out-of-list values dropped |
| T-13-32 | Tampering / Stored XSS | rule name / conditions rendered in the list | mitigate | Rendered as React text nodes (escaped); no dangerouslySetInnerHTML |
| T-13-33 | Access control | rules surface authz | accept | Read-only surface reusing the existing GET /tickets/rules endpoint; authz + tenant scoping inherited from the established backend route (no new attack surface introduced by this plan) |
| T-13-34 | Info Disclosure | error message leakage on rules load | accept | Full err.message passed to PartialFailureBanner which truncates visually (WR-10); backend scrubs server-side |
</threat_model>

<verification>
- `cd frontend && npx vitest run "src/app/(authed)/dashboard/tickets/rules/page.test.tsx" src/lib/queries/use-ticket-rules.test.ts` green
- `grep -c "tab=rules" frontend/src/components/shell/sidebar.tsx` returns 0 (v1 query-param link removed)
- `grep -q "/dashboard/tickets/rules" frontend/src/components/shell/sidebar.tsx` (new route linked)
- `grep -rcE "#[0-9A-Fa-f]{6}" "frontend/src/app/(authed)/dashboard/tickets/rules/page.tsx"` returns 0
- REGRESSION: `cd frontend && npx vitest run src/components/shell/sidebar.test.tsx` still green
</verification>

<success_criteria>
- /tickets/rules is a standalone sunset route (full v1 rewrite, D-S-01) reusing ChipBar + state primitives with mutually-exclusive coverage
- Sidebar links to /dashboard/tickets/rules; no surviving ?tab=rules reference
- useTicketRules reads GET /api/v1/tickets/rules with a verbatim-field TicketRule type
- Zero v1 carryover; no inline hex
</success_criteria>

<output>
After completion, create `.planning/phases/13-tickets-list-detail/13-09-SUMMARY.md`
</output>
