---
phase: 13-tickets-list-detail
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/globals.css
  - frontend/src/lib/queries/keys.ts
  - frontend/src/lib/queries/keys.test.ts
  - frontend/src/components/tickets/provider-mark.tsx
  - frontend/src/components/tickets/provider-mark.test.tsx
  - frontend/src/components/tickets/status-pill.tsx
  - frontend/src/components/tickets/status-pill.test.tsx
  - frontend/src/components/tickets/sla-pill.tsx
  - frontend/src/components/tickets/sla-pill.test.tsx
  - frontend/src/components/tickets/vuln-count.tsx
  - frontend/src/components/tickets/vuln-count.test.tsx
  - frontend/src/components/tickets/types.ts
autonomous: true
requirements: [UX-05-01, UX-05-02, UX-05-03, UX-05-06]
must_haves:
  truths:
    - "queryKeys.tickets namespace is defined ONCE in keys.ts (list/byId/comments/watchers/rules) — the single source every Wave-2/3 hook reads"
    - "ProviderMark renders a CSS-variable gradient square per provider (jira/asana/github), no inline hex, no real logo asset"
    - "StatusPill renders the 4 locked color states with a leading dot; Blocked renders alongside provider status"
    - "SlaPill computes tier client-side (Overdue/Soon/OK/Unknown) from a due timestamp"
    - "VulnCount renders T·C·H with the locked colors and edge cases (0→'—', >99→'99+')"
  artifacts:
    - path: "frontend/src/lib/queries/keys.ts"
      provides: "queryKeys.tickets namespace (list/byId/comments/watchers/rules) — Wave-1 foundation"
      contains: "tickets:"
    - path: "frontend/src/app/globals.css"
      provides: "provider gradient CSS variables"
      contains: "--gradient-provider-jira"
    - path: "frontend/src/components/tickets/provider-mark.tsx"
      provides: "ProviderMark primitive"
      contains: "ProviderMark"
    - path: "frontend/src/components/tickets/status-pill.tsx"
      provides: "StatusPill primitive (4 states + leading dot)"
      contains: "StatusPill"
    - path: "frontend/src/components/tickets/sla-pill.tsx"
      provides: "SlaPill client-side tier compute"
      contains: "SlaPill"
    - path: "frontend/src/components/tickets/vuln-count.tsx"
      provides: "T·C·H vuln-count cell"
      contains: "VulnCount"
  key_links:
    - from: "frontend/src/components/tickets/provider-mark.tsx"
      to: "globals.css --gradient-provider-*"
      via: "background: var(--gradient-provider-{provider})"
      pattern: "gradient-provider"
    - from: "frontend/src/lib/queries/keys.ts"
      to: "Wave-2/3 ticket hooks (use-tickets/use-ticket-detail/use-ticket-comments/use-ticket-watch/use-ticket-rules)"
      via: "queryKeys.tickets defined once here, imported (read-only) by every ticket hook"
      pattern: "tickets:"
---

<objective>
Ship the Phase 13 frontend presentational foundation PLUS the shared query-key namespace: define `queryKeys.tickets` ONCE in `keys.ts` (the single source every Wave-2/3 ticket hook reads — Plans 07/08/09 only import these keys, they never re-declare them), add the three provider gradient CSS variables to `globals.css` (resolving RESEARCH A4 — `--color-blue`/`--color-coral` do NOT exist; we add named gradient tokens using the locked visual-language.md hex stops so components carry zero inline hex), plus the four pure presentational primitives consumed by the list, drill, and detail surfaces.

`queryKeys.tickets` and all four primitives are self-contained (no backend, no hooks) → this plan runs fully parallel with the backend plans in Wave 1. The frontend `TicketProvider` type is defined here in `types.ts` for downstream reuse.

**Key-ownership rule (resolves the Wave/ownership inversion):** `keys.ts` is a Wave-1 foundation owned by THIS plan. Plans 07, 08, and 09 are forbidden from listing `keys.ts` in their `files_modified` — they import `queryKeys.tickets.*` read-only. The namespace defined here (`list`/`byId`/`comments`/`watchers`/`rules`) is the complete, final contract; do not add tickets keys anywhere else.

Purpose: The list table (Plan 07), drill content (Plan 05), detail page (Plan 08), and rules page (Plan 09) all compose these primitives and read these query keys. Defining them first (interface-first) prevents downstream scavenger hunts and merge-conflict surface on keys.ts.
Output: queryKeys.tickets in keys.ts + globals.css gradient tokens + ProviderMark, StatusPill, SlaPill, VulnCount + types.ts + co-located tests.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-tickets-list-detail/13-RESEARCH.md

<interfaces>
Existing keys.ts (VERIFIED — NO `tickets` key today; domain-first nesting, `as const` tuples per D-D-03):
```ts
export const queryKeys = {
  vulnerabilities: { all, stats, ..., list(opts), detail(id) },
  notifications: { all, recent },
  connectors: { all, list },
  savedFilters: { all, list },
  assets: { all, list(opts), byId(id), vulnerabilities(id), remediations(id), savedFilters() },
  assignableUsers: { all, search(q) },
} as const;
```
Locked visual contracts (CONTEXT D-P-04 / D-L-02 / D-SLA-04, visual-language.md):
- Provider gradients (hex stops from visual-language.md):
  jira: linear-gradient(135deg, #5C9CFF, #2684FF)
  asana: linear-gradient(135deg, #FF8AA0, #F1506E)
  github: linear-gradient(135deg, #C7BAFF, #A78BFA)
- Status pill Tailwind (leading dot <span class="size-1.5 rounded-full bg-current">):
  Open: border-violet/40 bg-violet-soft text-violet
  In progress: border-amber/40 bg-amber/10 text-amber
  Completed: border-severity-low/40 bg-severity-low/10 text-severity-low
  Blocked: border-severity-critical/40 bg-severity-critical/10 text-severity-critical
- SLA thresholds (client-side, now()-relative): Overdue (<now, severity-critical), Soon (<now+7d, amber), OK (>=now+7d, severity-low), Unknown (null, text-text-faint)
- Vuln count T·C·H: total text-text, critical text-severity-critical, high text-severity-high; zeros explicit; total 0 → '—'; total>99 → '99+'
Existing tokens already in tailwind.config.ts: violet, violet-soft, amber, severity-critical, severity-high, severity-low, text-text-faint, text-text-muted
external_status from backend is lowercase ("open"/"completed"/...) — StatusPill must map case-insensitively.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: queryKeys.tickets namespace + provider gradient tokens + ProviderMark + types.ts</name>
  <files>frontend/src/lib/queries/keys.ts, frontend/src/lib/queries/keys.test.ts, frontend/src/app/globals.css, frontend/src/components/tickets/types.ts, frontend/src/components/tickets/provider-mark.tsx, frontend/src/components/tickets/provider-mark.test.tsx</files>
  <read_first>
    - frontend/src/lib/queries/keys.ts (FULL — the existing `assets:` block is the exact shape template; add a sibling `tickets:` block, `as const` tuples, alongside it inside the same object)
    - frontend/src/app/globals.css (existing :root token block — add new vars alongside --gradient-sunset)
    - .claude/skills/sketch-findings-getvul/references/visual-language.md (provider-mark gradients lines ~178-181, provider chip tints ~174-175)
    - .claude/skills/sketch-findings-getvul/references/foundation.md (confirm no --color-blue/--color-coral; "no inline hex in components" rule)
    - frontend/src/components/ui/Avatar.tsx (component style template — gradient via CSS var, XSS-safe text node)
    - .planning/phases/13-tickets-list-detail/13-RESEARCH.md §Code Examples (tickets keys)
  </read_first>
  <behavior>
    - keys Test 1: `queryKeys.tickets.list({ filters:{}, page:1, view:'list' })` returns a stable tuple beginning `['tickets','list', ...]`; `byId('x')` returns `['tickets','detail','x']`; `comments('x')` returns `['tickets','x','comments']`; `watchers('x')` returns `['tickets','x','watchers']`; `rules()` returns `['tickets','rules']`. (Pure-tuple assertions, no TanStack.)
    - keys Test 2: `queryKeys.tickets.all` is `['tickets']` so it prefix-invalidates the whole subtree.
    - ProviderMark Test 1: provider="jira" renders an element whose style/class references the jira gradient token (not raw hex in the component file).
    - ProviderMark Test 2: each of jira/asana/github renders a distinct gradient class/var.
    - ProviderMark Test 3: renders NO <img> and NO logo asset reference (text/glyph only).
  </behavior>
  <action>
**`frontend/src/lib/queries/keys.ts`** — add a `tickets:` block as a sibling of the existing `assets:` block, INSIDE the same `queryKeys` object, matching the `as const` tuple style verbatim (RESEARCH §Code Examples). This is the COMPLETE namespace — defined ONCE here, never re-declared in Plans 07/08/09:
```ts
// Phase 13 — /tickets surface. Single source; Plans 07/08/09 import these read-only.
tickets: {
  all: ['tickets'] as const,
  list: (opts: { filters: object; page: number; view: string }) =>
    ['tickets', 'list', opts] as const,
  byId: (id: string) => ['tickets', 'detail', id] as const,
  comments: (id: string) => ['tickets', id, 'comments'] as const,
  watchers: (id: string) => ['tickets', id, 'watchers'] as const,
  rules: () => ['tickets', 'rules'] as const,
},
```
Place it after the `assets:` block (and before/after `assignableUsers:` — position is cosmetic; keep the object valid). Do NOT modify any existing key.

**`frontend/src/app/globals.css`** — add inside the existing `:root` block, next to `--gradient-sunset` (D-PROV-03, A4 resolution — hex lives ONCE here, never in components):
```css
--gradient-provider-jira:   linear-gradient(135deg, #5C9CFF, #2684FF);
--gradient-provider-asana:  linear-gradient(135deg, #FF8AA0, #F1506E);
--gradient-provider-github: linear-gradient(135deg, #C7BAFF, #A78BFA);
```

**`frontend/src/components/tickets/types.ts`**:
```ts
export type TicketProvider = 'jira' | 'asana' | 'github';
export type TicketStatus = 'open' | 'in_progress' | 'completed' | 'blocked';
```

**`frontend/src/components/tickets/provider-mark.tsx`** (UX-05-02 — gradient mark, NOT a real logo):
- `export function ProviderMark({ provider, className }: { provider: TicketProvider; className?: string })`.
- Render `<span class="size-3.5 rounded-[3px] inline-grid place-items-center" style={{ background: 'var(--gradient-provider-' + provider + ')' }} aria-label={provider} />`. (14px = size-3.5 per visual-language .provider-mark.) Optionally render a tiny mono initial glyph inside (`J`/`A`/`G`) as a text node — NEVER an `<img>` or external logo. No raw hex in this file (the gradient comes from the CSS var).
- Map provider→var via a literal lookup object so the var name is exact (`{ jira: 'var(--gradient-provider-jira)', ... }`), not string concatenation that could produce an invalid var.
  </action>
  <acceptance_criteria>
    - `grep -q "tickets:" frontend/src/lib/queries/keys.ts`
    - `grep -c "byId" frontend/src/lib/queries/keys.ts` returns >= 2 (assets.byId + tickets.byId)
    - `grep -q "rules:" frontend/src/lib/queries/keys.ts` (tickets.rules present — defined here for Plan 09)
    - `grep -qE "comments:|watchers:" frontend/src/lib/queries/keys.ts`
    - `grep -q "\-\-gradient-provider-jira" frontend/src/app/globals.css`
    - `grep -q "\-\-gradient-provider-asana" frontend/src/app/globals.css`
    - `grep -q "\-\-gradient-provider-github" frontend/src/app/globals.css`
    - `grep -q "export type TicketProvider" frontend/src/components/tickets/types.ts`
    - `grep -q "gradient-provider" frontend/src/components/tickets/provider-mark.tsx`
    - NO raw hex in the component: `grep -cE "#[0-9A-Fa-f]{6}" frontend/src/components/tickets/provider-mark.tsx` returns 0
    - NO image/logo: `grep -ciE "<img|\.svg\"|\.png\"|logo" frontend/src/components/tickets/provider-mark.tsx` returns 0
    - `cd frontend && npx vitest run src/lib/queries/keys.test.ts src/components/tickets/provider-mark.test.tsx` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/queries/keys.test.ts src/components/tickets/provider-mark.test.tsx</automated>
  </verify>
  <done>queryKeys.tickets (list/byId/comments/watchers/rules) defined ONCE in keys.ts as the single source for Wave-2/3 hooks; gradient tokens added once in globals.css; ProviderMark renders the correct CSS-variable gradient per provider with zero inline hex and zero logo assets; TicketProvider type exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StatusPill (4 states + leading dot, Blocked alongside)</name>
  <files>frontend/src/components/tickets/status-pill.tsx, frontend/src/components/tickets/status-pill.test.tsx</files>
  <read_first>
    - .claude/skills/sketch-findings-getvul/references/visual-language.md (status-pill markup ~78-81, dot)
    - frontend/src/components/tickets/types.ts (TicketStatus from Task 1)
    - .planning/phases/13-tickets-list-detail/13-RESEARCH.md §Visual Contracts (D-P-04 Tailwind table verbatim)
  </read_first>
  <behavior>
    - Test 1: StatusPill status="open" has classes `border-violet/40 bg-violet-soft text-violet` and a leading dot span `size-1.5 rounded-full bg-current`.
    - Test 2: external_status "completed" (lowercase from backend) maps to the Completed pill (severity-low classes); case-insensitive mapping.
    - Test 3: when blocked=true, the component renders BOTH the provider-status pill AND a Blocked pill (severity-critical classes) — "Open · Blocked" (two pills, not replacement) per D-P-04.
  </behavior>
  <action>
**`frontend/src/components/tickets/status-pill.tsx`** (UX-05-03 / D-P-04):
- `export function StatusPill({ externalStatus, blocked }: { externalStatus: string | null; blocked?: boolean })`.
- Normalize `externalStatus?.toLowerCase()` and map to one of the three provider states (open / in progress / completed). Unknown/null → render nothing for provider status (or a neutral "—"); choose neutral and document.
- Class map (verbatim D-P-04):
  - open → `border-violet/40 bg-violet-soft text-violet`
  - in progress (`in_progress`/`"in progress"`) → `border-amber/40 bg-amber/10 text-amber`
  - completed → `border-severity-low/40 bg-severity-low/10 text-severity-low`
  - blocked → `border-severity-critical/40 bg-severity-critical/10 text-severity-critical`
- Base pill classes: `inline-flex items-center gap-1.5 rounded-full border px-2 py-0.5 text-xs`. Leading dot: `<span class="size-1.5 rounded-full bg-current" />`.
- When `blocked` is true, render the provider pill AND a second Blocked pill side by side (wrap both in a `inline-flex items-center gap-1.5` row). Do NOT replace the provider pill.
- Copy: label text "Open" / "In progress" / "Completed" / "Blocked" (peer-not-butler voice — plain labels, no "Status:" prefix).
  </action>
  <acceptance_criteria>
    - `grep -q "border-violet/40 bg-violet-soft text-violet" frontend/src/components/tickets/status-pill.tsx`
    - `grep -q "border-severity-critical/40 bg-severity-critical/10 text-severity-critical" frontend/src/components/tickets/status-pill.tsx`
    - `grep -q "size-1.5 rounded-full bg-current" frontend/src/components/tickets/status-pill.tsx`
    - `grep -q "toLowerCase" frontend/src/components/tickets/status-pill.tsx`
    - NO raw hex: `grep -cE "#[0-9A-Fa-f]{6}" frontend/src/components/tickets/status-pill.tsx` returns 0
    - `cd frontend && npx vitest run src/components/tickets/status-pill.test.tsx` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/tickets/status-pill.test.tsx</automated>
  </verify>
  <done>StatusPill maps external_status case-insensitively to the 4 locked color states with a leading dot; Blocked renders alongside (not replacing) the provider pill.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SlaPill (client-side tier) + VulnCount (T·C·H)</name>
  <files>frontend/src/components/tickets/sla-pill.tsx, frontend/src/components/tickets/sla-pill.test.tsx, frontend/src/components/tickets/vuln-count.tsx, frontend/src/components/tickets/vuln-count.test.tsx</files>
  <read_first>
    - .claude/skills/sketch-findings-getvul/references/visual-language.md (sla-pill ~97-110, severity glyphs ~477 in RESEARCH)
    - .planning/phases/13-tickets-list-detail/13-RESEARCH.md §Visual Contracts (D-SLA-04 thresholds + D-L-02 T·C·H edge cases verbatim)
  </read_first>
  <behavior>
    - SlaPill Test 1: due in the past → Overdue, `text-severity-critical` family.
    - SlaPill Test 2: due in 3 days → Soon (within 7d), amber.
    - SlaPill Test 3: due in 30 days → OK, severity-low/green.
    - SlaPill Test 4: due null → Unknown, `text-text-faint`.
    - VulnCount Test 1: total=3 crit=2 high=1 → renders "3 ·2 ·1" with total text-text, crit text-severity-critical, high text-severity-high.
    - VulnCount Test 2: total=3 crit=0 high=0 → "3 ·0 ·0" (zeros explicit).
    - VulnCount Test 3: total=0 → "—" (em dash, no breakdown).
    - VulnCount Test 4: total=150 → "99+ ·C ·H".
  </behavior>
  <action>
**`frontend/src/components/tickets/sla-pill.tsx`** (D-SLA-04, client-side — thresholds defined ONCE here, the single source per Pitfall 5):
- `export function SlaPill({ dueAt }: { dueAt: string | null })`.
- Compute tier: `const due = dueAt ? new Date(dueAt) : null; const now = Date.now();` → if null → Unknown; else if `due.getTime() < now` → Overdue; else if `due.getTime() < now + 7*24*60*60*1000` → Soon; else → OK.
- Class map: Overdue `border-severity-critical/30 bg-severity-critical/10 text-severity-critical`, Soon `border-severity-high/30 bg-severity-high/10 text-severity-high`, OK `border-severity-low/30 bg-severity-low/10 text-severity-low`, Unknown `border-border-subtle bg-surface-2 text-text-faint`. Base: `inline-flex items-center rounded-full border px-2 py-0.5 text-xs font-mono` (mono per visual-language .sla-pill). Label: a short relative string ("−2h", "3d left", "Unknown") — keep concise, peer voice.
- Render is a pure function of `dueAt` + current time; no backend "state" column (D-SLA-04).

**`frontend/src/components/tickets/vuln-count.tsx`** (D-L-02):
- `export function VulnCount({ total, critical, high }: { total: number; critical: number; high: number })`.
- If `total === 0` → render `<span class="text-text-faint">—</span>` (em dash, no breakdown).
- Else render `<span class="font-mono text-text">{total > 99 ? '99+' : total}</span> <span class="text-severity-critical">·{critical}</span> <span class="text-severity-high">·{high}</span>` matching the "T ·C ·H" format (zeros explicit). Use the middot `·` separator as in the spec.
  </action>
  <acceptance_criteria>
    - `grep -q "text-text-faint" frontend/src/components/tickets/sla-pill.tsx`
    - `grep -q "7" frontend/src/components/tickets/sla-pill.tsx` (7-day threshold present)
    - `grep -q "font-mono" frontend/src/components/tickets/sla-pill.tsx`
    - `grep -q "99+" frontend/src/components/tickets/vuln-count.tsx`
    - `grep -q "text-severity-critical" frontend/src/components/tickets/vuln-count.tsx`
    - `grep -qE "—|\\\\u2014" frontend/src/components/tickets/vuln-count.tsx` (em dash for total 0)
    - NO raw hex in either file: `grep -cE "#[0-9A-Fa-f]{6}" frontend/src/components/tickets/sla-pill.tsx frontend/src/components/tickets/vuln-count.tsx` returns 0
    - `cd frontend && npx vitest run src/components/tickets/sla-pill.test.tsx src/components/tickets/vuln-count.test.tsx` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/tickets/sla-pill.test.tsx src/components/tickets/vuln-count.test.tsx</automated>
  </verify>
  <done>SlaPill computes the 4 tiers client-side from dueAt with locked colors; VulnCount renders T·C·H with zeros explicit, '—' for total 0, '99+' for >99, no inline hex.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend data → presentational props | externalStatus / counts / dueAt rendered into DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-14 | Tampering / XSS | externalStatus / provider strings rendered | mitigate | Values rendered as React text nodes (escaped) and mapped through literal lookup objects (provider→var, status→class); arbitrary input falls through to a neutral default rather than injecting a class/var |
| T-13-15 | Info Disclosure | none (pure presentational, no secrets) | accept | Components receive only already-authorized display data |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/queries/keys.test.ts src/components/tickets/` — keys + all four primitive test files pass
- `grep -rcE "#[0-9A-Fa-f]{6}" frontend/src/components/tickets/*.tsx` returns 0 across new components (no inline hex)
- `cd frontend && npx tsc --noEmit` clean (or the project's typecheck command)
</verification>

<success_criteria>
- queryKeys.tickets namespace (list/byId/comments/watchers/rules) defined once in keys.ts — the single source Plans 07/08/09 read
- Provider gradient tokens added once in globals.css; primitives carry zero inline hex
- ProviderMark, StatusPill, SlaPill, VulnCount match the locked visual contracts
- TicketProvider/TicketStatus types exported for downstream reuse
- keys + all four primitive test files green
</success_criteria>

<output>
After completion, create `.planning/phases/13-tickets-list-detail/13-04-SUMMARY.md`
</output>
