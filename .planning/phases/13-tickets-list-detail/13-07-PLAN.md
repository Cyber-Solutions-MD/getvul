---
phase: 13-tickets-list-detail
plan: 07
type: execute
wave: 3
depends_on: [13-03, 13-04, 13-05]
files_modified:
  - frontend/src/lib/queries/use-tickets.ts
  - frontend/src/lib/queries/use-tickets.test.ts
  - frontend/src/lib/queries/use-mark-blocked.ts
  - frontend/src/components/tickets/tickets-chip-bar.tsx
  - frontend/src/components/tickets/tickets-table.tsx
  - frontend/src/components/tickets/tickets-table.test.tsx
  - frontend/src/components/tickets/ticket-bulk-bar.tsx
  - frontend/src/app/(authed)/dashboard/tickets/page.tsx
  - frontend/src/app/(authed)/dashboard/tickets/page.test.tsx
autonomous: true
requirements: [UX-05-01, UX-05-02, UX-05-03, UX-05-06]
must_haves:
  truths:
    - "/tickets renders the 4-axis chip-bar + 8-column table; rows open the generalized DrillPanel via ?ticket=&open=drill"
    - "List/Board segmented toggle persists in ?view; Board shows the placeholder copy"
    - "Loading→SkeletonTable, no-results→EmptyState, error→PartialFailureBanner (mutually exclusive)"
    - "Unconfigured-Asana empty state deep-links to /dashboard/connectors"
    - "Bulk bar exposes Close + Mark blocked/Unblock when rows are selected"
  artifacts:
    - path: "frontend/src/app/(authed)/dashboard/tickets/page.tsx"
      provides: "rewritten /tickets list page (sunset)"
      contains: "TicketsTable"
    - path: "frontend/src/components/tickets/tickets-table.tsx"
      provides: "8-column table composing Plan 04 primitives"
      contains: "VulnCount"
    - path: "frontend/src/lib/queries/use-tickets.ts"
      provides: "list query hook + buildSearchParams"
      contains: "useTickets"
  key_links:
    - from: "frontend/src/app/(authed)/dashboard/tickets/page.tsx"
      to: "DrillPanel (generalized)"
      via: "renderContent={<TicketDrillContent/>} idKey='ticket'"
      pattern: "idKey"
    - from: "frontend/src/components/tickets/tickets-table.tsx"
      to: "ProviderMark/StatusPill/SlaPill/VulnCount"
      via: "8-column cell composition"
      pattern: "ProviderMark"
---

<objective>
Rewrite `/tickets` as a sunset list page (full replacement of the 1186-line v1 — the v1 page is the anti-pattern reference, do NOT salvage its components). Clone the `assets/page.tsx` composition (ErrorBoundary > Suspense > Inner with mutually-exclusive state branches), wire the 4-axis tickets chip-bar, the 8-column table composing Plan 04 primitives, the generalized DrillPanel (Plan 05) via `idKey="ticket"`, the List/Board toggle, and the bulk bar (Close + Mark blocked/Unblock). Add the TanStack list query + the optimistic mark-blocked mutation.

Purpose: This is the primary UX-05-01/02/03/06 deliverable surface.
Output: use-tickets, use-mark-blocked, chip-bar, table (+ table test), bulk-bar, page + tests. (Query keys come from `queryKeys.tickets` defined ONCE in Plan 04 — this plan imports them read-only and never edits keys.ts.)
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-tickets-list-detail/13-RESEARCH.md
@.planning/phases/13-tickets-list-detail/13-04-PLAN.md
@.planning/phases/13-tickets-list-detail/13-05-PLAN.md

<interfaces>
Backend (Plan 03): GET /api/v1/tickets returns items[{id, provider, external_ticket_url, external_status, blocked, blocked_reason, sla_due_at, assignee, title, subtitle, max_severity, vuln_count, critical_count, high_count, ...}] + {total,page,page_size,pages}. POST /api/v1/tickets/{id}/blocked {blocked, blocked_reason}. POST /api/v1/tickets/bulk-action {action:"block"|"unblock"|"close", external_ticket_urls:[...], blocked_reason?}.
Frontend templates:
- assets/page.tsx (composition, state branches, XSS allow-lists, SKELETON_COLUMNS, useUrlState/useUrlStateList)
- use-assets.ts (query hook + exported buildSearchParams)
- use-reassign-asset.ts (optimistic onMutate/onError/onSuccess + predicate invalidation)
- ChipBar/ChipAxis (each axis needs a hardcoded allowList — T-12-05)
Plan 04 primitives: ProviderMark, StatusPill, SlaPill, VulnCount, TicketProvider.
Plan 05: DrillPanel(idKey, id, renderContent, ariaLabel) + TicketDrillContent({ticketId, ticket, onClose, renderBlockedToggle}).
Plan 06: BlockedToggle (pass as renderBlockedToggle into the drill).
keys.ts: `queryKeys.tickets` ({ all, list(opts), byId(id), comments(id), watchers(id), rules() }) is defined ONCE in Plan 04 (Wave 1). This plan imports it read-only — do NOT add keys.ts to files_modified or re-declare any tickets key here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useTickets list hook + useMarkBlocked optimistic mutation</name>
  <files>frontend/src/lib/queries/use-tickets.ts, frontend/src/lib/queries/use-tickets.test.ts, frontend/src/lib/queries/use-mark-blocked.ts</files>
  <read_first>
    - frontend/src/lib/queries/keys.ts (READ-ONLY — `queryKeys.tickets` is defined in Plan 04; import `queryKeys.tickets.list/byId/all` here, do NOT edit this file)
    - frontend/src/lib/queries/use-assets.ts (FULL — query hook + exported buildSearchParams template)
    - frontend/src/lib/queries/use-reassign-asset.ts (FULL — optimistic onMutate/onError/onSuccess + predicate invalidation)
    - .planning/phases/13-tickets-list-detail/13-RESEARCH.md §Code Examples (tickets keys) + §Pattern 4
  </read_first>
  <behavior>
    - useTickets Test 1: buildSearchParams maps filters {status,provider,severity,sla,search,page,view} to the wire query string (assert the URL contract without TanStack).
    - useTickets Test 2: allow-list clamp — an out-of-list status value is dropped.
    - useMarkBlocked Test: onMutate snapshots + optimistically flips blocked; onError restores snapshot; onSuccess invalidates tickets.byId + tickets.all (assert via a mocked QueryClient).
  </behavior>
  <action>
**Query keys** — DO NOT define them here. `queryKeys.tickets` ({ all, list(opts), byId(id), comments(id), watchers(id), rules() }) is created ONCE in Plan 04 (Wave 1, `keys.ts`). Import it: `import { queryKeys } from '@/lib/queries/keys'` and reference `queryKeys.tickets.list(...)` / `.byId(...)` / `.all`.

**`use-tickets.ts`** — clone `use-assets.ts`. Define `TicketsFilters` ({ status?, provider?, severity?, sla?, search? }) + `TicketSummary` (id, provider, externalId/title, externalStatus, blocked, blockedReason, slaDueAt, assignee, maxSeverity, vulnCount, criticalCount, highCount, externalTicketUrl) + `TicketsResponse`. Export `buildSearchParams(filters, page)` (co-located + exported for URL-shape tests, Phase 11 D-D-03 pattern). `useTickets` calls `api.get('/tickets', ...)` with the built params, keyed by `queryKeys.tickets.list(...)`.

**`use-mark-blocked.ts`** — clone `use-reassign-asset.ts` optimistic shape:
- `useMutation` POST `/tickets/${id}/blocked` body `{ blocked, blocked_reason }` (send ONLY these two — mass-assignment guard T-12-08).
- `onMutate`: cancel `tickets.byId(id)` + `tickets.all`, snapshot, optimistically patch the cached ticket's `blocked`/`blockedReason`.
- `onError`: restore snapshot + error toast (useToast).
- `onSuccess`: invalidate `tickets.byId(id)` + `tickets.all`; ALSO predicate-invalidate any `['assets', *, 'remediations']` that may embed ticket state (RESEARCH Pattern 4 predicate technique).
- `retry: 0`.
  </action>
  <acceptance_criteria>
    - keys.ts NOT modified by this task — `queryKeys.tickets` is imported read-only from Plan 04: `grep -q "queryKeys.tickets" frontend/src/lib/queries/use-tickets.ts`
    - `grep -q "export function useTickets" frontend/src/lib/queries/use-tickets.ts`
    - `grep -q "buildSearchParams" frontend/src/lib/queries/use-tickets.ts`
    - `grep -q "onMutate" frontend/src/lib/queries/use-mark-blocked.ts`
    - `grep -q "blocked_reason" frontend/src/lib/queries/use-mark-blocked.ts`
    - `cd frontend && npx vitest run src/lib/queries/use-tickets.test.ts` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/queries/use-tickets.test.ts</automated>
  </verify>
  <done>queryKeys.tickets imported read-only from Plan 04 (not edited here); useTickets + buildSearchParams clone use-assets; useMarkBlocked is an optimistic+rollback mutation sending only {blocked, blocked_reason} with predicate invalidation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TicketsChipBar (4 axes) + TicketsTable (8 cols) + TicketBulkBar</name>
  <files>frontend/src/components/tickets/tickets-chip-bar.tsx, frontend/src/components/tickets/tickets-table.tsx, frontend/src/components/tickets/tickets-table.test.tsx, frontend/src/components/tickets/ticket-bulk-bar.tsx</files>
  <read_first>
    - frontend/src/components/assets/assets-chip-bar.tsx (ChipBar wrapper template — allowList per axis)
    - frontend/src/components/assets/assets-table.tsx (table row/selection template)
    - frontend/src/components/assets/assets-table.test.tsx (table test template — header + cell assertions)
    - frontend/src/components/ui/ChipBar.tsx (ChipAxis descriptor shape)
    - frontend/src/components/tickets/{provider-mark,status-pill,sla-pill,vuln-count}.tsx (Plan 04 cells)
    - .planning/phases/13-tickets-list-detail/13-RESEARCH.md §D-L-01/02/04 (8 columns + chip axes verbatim)
  </read_first>
  <behavior>
    - tickets-table.test.tsx Test 1 (UX-05-01 8-column contract): given one TicketSummary row, the table renders all 8 column headers — Severity, Provider, ID, Title, Vulns, Assignee, Status, SLA — in order (assert each header string present).
    - tickets-table.test.tsx Test 2: the row composes each Plan-04 primitive — assert the row renders a ProviderMark (gradient mark), a StatusPill, an SlaPill, and a VulnCount cell (query by their test-ids/roles), proving the cells are wired (not just typechecked).
  </behavior>
  <action>
**`tickets-chip-bar.tsx`** (D-L-04) — wrap `ChipBar` with 4 axes, each carrying a hardcoded `allowList` (T-12-05):
- Status (multi): `['open','in_progress','completed','blocked']`
- Provider (multi): `['jira','asana','github']` (only render options for providers present in the data — pass available providers in)
- Severity (multi): `['critical','high','medium','low']`
- SLA (single-select): `['overdue','soon','ok']`
Plus a search box (matches ID + title + assignee) with 250ms debounce (reuse the assets-chip-bar debounce/useUrlStateList wiring).

**`tickets-table.tsx`** (D-L-01/02 — 8 columns): Severity (glyph) · Provider (`<ProviderMark>`) · ID (mono) · Title (`truncate` + title attr for hover) · Vulns (`<VulnCount total critical high>`) · Assignee (`<Avatar>` + name truncated) · Status (`<StatusPill externalStatus blocked>`) · SLA (`<SlaPill dueAt>`). Row click → caller-provided `onRowClick(ticket)` (the page sets `?ticket=<id>&open=drill`). Row checkbox selection feeds the bulk bar. Mobile (<900px) collapses to the card layout (Severity · Provider · ID · Title · Status · SLA primary; vuln-count + assignee secondary line) per D-L-01 — use `min-[900px]:` variants (Pitfall 3).

**`ticket-bulk-bar.tsx`** (D-S-03) — instantiate the Phase 11 BulkActionBar pattern (bottom-anchored, appears on selection) with two actions: **Close** (ConfirmModal, no input) and **Mark blocked / Unblock** (modal collecting one shared `blocked_reason`). Calls back to the page which fires `/tickets/bulk-action`.
  </action>
  <acceptance_criteria>
    - `grep -q "allowList" frontend/src/components/tickets/tickets-chip-bar.tsx`
    - `grep -qE "'jira'.*'asana'.*'github'|jira.*asana.*github" frontend/src/components/tickets/tickets-chip-bar.tsx`
    - `grep -q "ProviderMark" frontend/src/components/tickets/tickets-table.tsx`
    - `grep -q "VulnCount" frontend/src/components/tickets/tickets-table.tsx`
    - `grep -q "StatusPill" frontend/src/components/tickets/tickets-table.tsx`
    - `grep -q "SlaPill" frontend/src/components/tickets/tickets-table.tsx`
    - `grep -q "min-\[900px\]" frontend/src/components/tickets/tickets-table.tsx`
    - `grep -qiE "blocked|close" frontend/src/components/tickets/ticket-bulk-bar.tsx`
    - NO raw hex: `grep -rcE "#[0-9A-Fa-f]{6}" frontend/src/components/tickets/tickets-table.tsx frontend/src/components/tickets/tickets-chip-bar.tsx` returns 0
    - `cd frontend && npx vitest run src/components/tickets/tickets-table.test.tsx` passes (8 headers + 4 primitive cells asserted)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/tickets/tickets-table.test.tsx</automated>
  </verify>
  <done>4-axis chip-bar with allow-lists; 8-column table composing Plan 04 primitives with mobile card collapse, asserted by tickets-table.test.tsx (8 headers + 4 primitive cells); bulk bar with Close + Mark blocked/Unblock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /tickets/page.tsx rewrite (states + drill + List/Board toggle)</name>
  <files>frontend/src/app/(authed)/dashboard/tickets/page.tsx, frontend/src/app/(authed)/dashboard/tickets/page.test.tsx</files>
  <read_first>
    - frontend/src/app/(authed)/dashboard/assets/page.tsx (FULL — composition + mutually-exclusive state branches WR-13 + error fallback WR-10)
    - frontend/src/app/(authed)/dashboard/assets/page.test.tsx (page test template)
    - frontend/src/components/tickets/{tickets-chip-bar,tickets-table,ticket-bulk-bar,ticket-drill-content,blocked-toggle}.tsx (Task 2 + Plans 05/06)
    - frontend/src/components/vulnerabilities/drill-panel.tsx + drill-panel-mobile.tsx (generalized — pass idKey="ticket")
    - .planning/phases/13-tickets-list-detail/13-RESEARCH.md §Pattern 1 + §D-L-03 (Board placeholder copy)
  </read_first>
  <behavior>
    - Test 1: renders 8 column headers; given list data, rows render; row click sets URL `?ticket=<id>&open=drill`.
    - Test 2: error branch shows PartialFailureBanner with the FULL error message (no slice — WR-10/15); loading shows SkeletonTable; empty shows EmptyState — branches mutually exclusive (WR-13).
    - Test 3: `?view=board` renders the Board placeholder copy ("Board view coming in a future update — for now, use the List view with the Status chip filter to organize work by status."); `?view=list` (default) renders the table; toggle updates the URL.
    - Test 4: when no Asana connector configured (empty + connector-missing signal), EmptyState deep-links to /dashboard/connectors (D-S-02).
  </behavior>
  <action>
Replace the entire v1 `page.tsx`. Structure mirrors `assets/page.tsx`:
- `'use client'`, ErrorBoundary > Suspense > Inner.
- Module-scope XSS allow-lists (status/provider/severity/sla) matching the chip-bar (T-12-05).
- `SKELETON_COLUMNS` = 8-col shape.
- Inner reads filters via `useUrlStateList` per axis + `useUrlState` for `view`/`page`/`search`; memoizes `filters`; calls `useTickets`.
- Page-head-actions: List/Board segmented toggle (D-L-03), persists `?view` via useUrlState (default 'list'). Board branch renders the placeholder copy (verbatim string above) — no table.
- State branches (mutually exclusive, WR-13): `q.error ? <PartialFailureBanner message={q.error.message}/> : isLoading ? <SkeletonTable .../> : items.length===0 ? <EmptyState .../> : <TicketsTable .../>`. Pass FULL `err.message` (WR-10).
- EmptyState: if the response/error indicates Asana not configured, render the connector deep-link variant (`<Link href="/dashboard/connectors">`), else the generic no-results empty state.
- Drill: render `<DrillPanel idKey="ticket" id={ticketIdFromUrl} ariaLabel="Ticket detail" renderContent={({id, onClose}) => <TicketDrillContent ticketId={id} ticket={selectedTicket} onClose={onClose} renderBlockedToggle={({ticketId}) => <BlockedToggle blocked={...} blockedReason={...} onToggle={(n) => markBlocked.mutate({id: ticketId, ...n})} pending={markBlocked.isPending}/>} />} />` and the mobile `<DrillPanelMobile idKey="ticket" id={ticketIdFromUrl} renderContent={...}/>`. Row click sets `?ticket=<id>&open=drill`.
- Bulk bar wired to selection + `/tickets/bulk-action`.
- `useDocumentTitle('Tickets')`.
Use peer-voice copy from microcopy.ts for empty/error states. No inline hex.
  </action>
  <acceptance_criteria>
    - `grep -q "idKey=\"ticket\"" frontend/src/app/(authed)/dashboard/tickets/page.tsx`
    - `grep -q "open=drill\|'drill'\|open: 'drill'" frontend/src/app/(authed)/dashboard/tickets/page.tsx`
    - `grep -q "PartialFailureBanner" frontend/src/app/(authed)/dashboard/tickets/page.tsx`
    - `grep -q "SkeletonTable" frontend/src/app/(authed)/dashboard/tickets/page.tsx`
    - `grep -q "EmptyState" frontend/src/app/(authed)/dashboard/tickets/page.tsx`
    - `grep -q "/dashboard/connectors" frontend/src/app/(authed)/dashboard/tickets/page.tsx`
    - `grep -qE "Board view coming" frontend/src/app/(authed)/dashboard/tickets/page.tsx`
    - NO err.message slice: `grep -cE "\.message\)?\.slice\(|\.message\.substring" frontend/src/app/(authed)/dashboard/tickets/page.tsx` returns 0
    - NO v1 salvage: `grep -cE "AsanaSetupModal|TicketBulkActions|RulesPanel|CommentModal" frontend/src/app/(authed)/dashboard/tickets/page.tsx` returns 0
    - `cd frontend && npx vitest run "src/app/(authed)/dashboard/tickets/page.test.tsx"` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/(authed)/dashboard/tickets/page.test.tsx"</automated>
  </verify>
  <done>/tickets fully rewritten: 8-col table, 4-axis chip-bar, ticket drill via idKey="ticket", List/Board toggle with placeholder, mutually-exclusive states, connector deep-link empty state, bulk bar — zero v1 carryover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL filter params → query / DOM | reflected chip values |
| browser → POST /blocked, /bulk-action | mutation bodies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-22 | Tampering | reflected XSS via chip URL params | mitigate | Each ChipAxis carries a hardcoded `allowList`; `useUrlStateList` clamps on read+write (T-12-05); out-of-list values dropped |
| T-13-23 | Tampering | mass assignment on mark-blocked | mitigate | useMarkBlocked sends only {blocked, blocked_reason} (T-12-08); backend Pydantic rejects extras |
| T-13-24 | Info Disclosure | error message leakage | accept | Full err.message passed to PartialFailureBanner which truncates visually (WR-10); backend already scrubs sensitive details server-side |
| T-13-25 | Access control | ticket data scoped per tenant | mitigate | Enforced backend-side (Plan 03 _resolve_group tenant filter); frontend renders only authorized rows |
</threat_model>

<verification>
- `cd frontend && npx vitest run "src/app/(authed)/dashboard/tickets/page.test.tsx" src/lib/queries/use-tickets.test.ts` green
- REGRESSION: `cd frontend && npx vitest run src/components/vulnerabilities/drill-panel*.test.tsx` still green (drill generalization unaffected)
- `grep -rcE "#[0-9A-Fa-f]{6}" "frontend/src/app/(authed)/dashboard/tickets/page.tsx"` returns 0
</verification>

<success_criteria>
- /tickets renders 8-col table + 4-axis chip-bar + ticket drill + List/Board toggle + bulk bar
- Mutually-exclusive states + connector deep-link empty state
- Optimistic mark-blocked mutation; zero v1 carryover; no inline hex
</success_criteria>

<output>
After completion, create `.planning/phases/13-tickets-list-detail/13-07-SUMMARY.md`
</output>
