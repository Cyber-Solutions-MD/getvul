---
phase: 30-correlation-schema-fix
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - backend/scripts/recorrelate_all_tenants.py
  - backend/tests/test_correlation_service.py
autonomous: true
requirements: [CORR-02, CORR-03]
user_setup: []

must_haves:
  truths:
    - "backend/scripts/recorrelate_all_tenants.py re-runs run_correlations per active tenant (looping active tenants like scheduler.py), recovering previously-dropped Qualys/Rapid7 correlations, and is safe to re-run (idempotent)"
    - "After re-correlation, every tenant has zero rows where COALESCE(array_length(sources,1),0) != sources_count — the per-tenant zero-loss proof (CORR-02, D-06 step 5)"
    - "sources_count always equals len(sources) for every correlation row across 1-of-6 through 6-of-6 source combinations (standing regression, CORR-03)"
    - "Confidence banding is HIGH for >=4 sources, MEDIUM for 2-3 sources, LOW for 1 (D-08), proven across seeded fixtures"
    - "A correlation created for tenant A is never returned or mutated when re-correlating tenant B (cross-tenant isolation, proven with a tenant_b fixture)"
    - { statement: "Verification of zero-loss runs only AFTER the re-correlation step; verifying in the post-FK-drop/pre-re-correlation window shows sources=[] with a stale sources_count and produces a false data-loss signal (RESEARCH Pitfall 5)", verification: backstop }
    - { statement: "The migration → re-correlation → verification steps run in strict order; the re-correlation script and its embedded COALESCE zero-loss query are the sequencing guard", verification: backstop }
  prohibitions:
    - "recorrelate_all_tenants.py must never read or write across tenant_id boundaries; the per-tenant loop scopes every query on tenant_id and the zero-loss verification never runs as a bare global aggregate"
    - "The re-correlation script must not call compute_risk_scores or any connector-sync/dev-seed side effect — it is a correlation-only data-recovery step (out of scope per CONTEXT domain)"
  artifacts:
    - path: "backend/scripts/recorrelate_all_tenants.py"
      provides: "One-time, manually-invoked per-tenant re-correlation + per-tenant zero-loss verification"
      contains: "run_correlations"
    - path: "backend/tests/test_correlation_service.py"
      provides: "Confidence-banding + count-invariant + cross-tenant coverage across 1..6 sources"
      contains: "sources_count"
  key_links:
    - from: "backend/scripts/recorrelate_all_tenants.py"
      to: "backend/app/vulnerabilities/correlation_service.py::run_correlations"
      via: "per-active-tenant loop (mirrors scheduler.py)"
      pattern: "run_correlations"
    - from: "recorrelate_all_tenants.py zero-loss query"
      to: "vulnerability_correlations"
      via: "COALESCE(array_length(sources,1),0) != sources_count per tenant"
      pattern: "COALESCE\\(array_length"
---

<objective>
Deliver the one-time per-tenant data-recovery step that recovers the previously-silently-dropped Qualys/Rapid7 correlations (D-06 step 4), verifies zero-loss per tenant (SC#2 / CORR-02), and expands `test_correlation_service.py` with the confidence-banding, count-invariant, and cross-tenant coverage the Plan 01 tracer intentionally left out.

Purpose: Plan 01 proved the new shape works for one finding. This plan makes the migration safe for existing data (per-tenant zero-loss recovery) and locks the CORR-03 count/name invariant across every source combination so it can never silently regress.
Output: `backend/scripts/recorrelate_all_tenants.py` (idempotent, manually-invoked, mirrors `capture_ai_goldens.py` + `scheduler.py` tenant-loop) and an expanded correlation test suite.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-correlation-schema-fix/30-CONTEXT.md
@.planning/phases/30-correlation-schema-fix/30-RESEARCH.md
@.planning/phases/30-correlation-schema-fix/30-PATTERNS.md
@.planning/phases/30-correlation-schema-fix/30-01-PLAN.md

<interfaces>
<!-- Contracts the executor needs. Plan 01 delivered these; do not re-derive. -->

run_correlations(db: AsyncSession, tenant_id: uuid.UUID) -> dict  — idempotent (upsert on uq_correlation + prune stale); returns {"correlated": int, "stale_removed": int}. Already the shape of sla_service.backfill_sla_due_dates.

Active-tenant loop idiom (backend/app/connectors/scheduler.py:199-206):
  tenants = (await db.execute(select(Tenant).where(Tenant.is_active.is_(True)))).scalars().all()
  for t in tenants: await <fn>(db, t.id)
  await db.commit()

Standalone-script idiom (backend/scripts/capture_ai_goldens.py): docstring (what/why/usage/when-to-rerun) + `async def _main()` + `if __name__ == "__main__": asyncio.run(_main())`; per-item try/except-continue so one failure never aborts the rest.

Zero-loss query (RESEARCH — proven against the empty-array edge, Pitfall 3):
  SELECT count(*) FROM vulnerability_correlations
  WHERE tenant_id = :tid AND COALESCE(array_length(sources,1),0) != sources_count   -- MUST be 0 per tenant

Session/tenant model imports: `from app.db.session import async_session_factory`, `from app.tenants.models import Tenant`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: One-time per-tenant re-correlation script</name>
  <read_first>
    - backend/scripts/capture_ai_goldens.py (docstring convention + `_main()`/`__main__` entrypoint + per-item try/except-continue)
    - backend/app/connectors/scheduler.py (lines 199-207 — active-tenant loop + per-iteration error logging)
    - backend/app/vulnerabilities/sla_service.py (backfill_sla_due_dates — the idempotent per-tenant fn shape run_correlations already matches)
    - backend/app/dev_routes.py (run_correlations call-site shape — but do NOT copy its compute_risk_scores call)
    - .planning/phases/30-correlation-schema-fix/30-RESEARCH.md (Code Example #5 — the exact script)
    - .planning/phases/30-correlation-schema-fix/30-PATTERNS.md (recorrelate_all_tenants.py section — Analogs A-D)
  </read_first>
  <action>
    Create `backend/scripts/recorrelate_all_tenants.py` per RESEARCH Code Example #5. Docstring: what it is, why it is NOT auto-run (D-07), the exact `docker compose exec backend python scripts/recorrelate_all_tenants.py` usage, and that it is safe to re-run. `async def _main()`: open `async_session_factory()`, select active tenants (`Tenant.is_active.is_(True)`), loop; per tenant — (1) a diagnostic "before" count of blind-spot rows (`sources = '{}'`), (2) call `run_correlations(db, t.id)`, (3) the per-tenant COALESCE zero-loss check (MUST be 0 after), (4) `logger.info("recorrelated_tenant", tenant_id=..., blind_spot_rows_recovered=..., inconsistent_rows_after=..., **stats)`. Wrap each tenant iteration in try/except-continue (one tenant's failure must not abort the rest — mirror scheduler.py's per-iteration error log). `await db.commit()` once after the loop. `if __name__ == "__main__": asyncio.run(_main())`. Do NOT call compute_risk_scores. Do NOT add an API route (standalone script only — D-07, RESEARCH Open Question 2). Every query scopes on `tenant_id`; the zero-loss query is per-tenant, never a global aggregate.
  </action>
  <verify>
    <automated>cd backend && python -c "src=open('scripts/recorrelate_all_tenants.py').read(); import ast; ast.parse(src); assert 'run_correlations' in src; assert 'COALESCE(array_length' in src, 'missing COALESCE zero-loss query'; assert 'compute_risk_scores' not in src, 'must not call compute_risk_scores'; assert 'is_active' in src, 'must loop active tenants'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/scripts/recorrelate_all_tenants.py` parses as valid Python and calls `run_correlations`
    - `grep -c "COALESCE(array_length(sources,1), 0)" backend/scripts/recorrelate_all_tenants.py` >= 1
    - `grep -c "compute_risk_scores" backend/scripts/recorrelate_all_tenants.py` == 0
    - Script selects active tenants (`is_active`) and loops per tenant; the zero-loss query is filtered by `tenant_id`, never a bare global aggregate
    - Docstring states it is NOT auto-run and gives the `docker compose exec` usage line
  </acceptance_criteria>
  <done>An idempotent, manually-invoked per-tenant re-correlation script exists that recovers blind-spot rows and proves per-tenant zero-loss, with no risk-score or route side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confidence-banding, count-invariant, and cross-tenant coverage</name>
  <read_first>
    - backend/tests/test_correlation_service.py (the file Plan 01 created — extend it, do not recreate)
    - backend/tests/test_vuln_source_filter.py (test_source_filter_tenant_scoped — the cross-tenant isolation shape at lines 65-77)
    - backend/tests/conftest.py (tenant_a, tenant_b fixtures)
    - .planning/phases/30-correlation-schema-fix/30-CONTEXT.md (D-08 bands; D-02 canonical order)
  </read_first>
  <behavior>
    - 1-source finding: no correlation row is created (correlation requires >=2 sources) — asserts the empty/single-input edge
    - 2 sources -> confidence "MEDIUM", 3 sources -> "MEDIUM", 4 sources -> "HIGH", 5 -> "HIGH", 6 -> "HIGH" (D-08 bands)
    - For every seeded combination, `len(corr["sources"]) == corr["sources_count"]` (CORR-03 invariant) and sources is in canonical enum-declaration order
    - A correlation seeded for tenant_a is not returned by get_correlation_for_vuln for tenant_b (cross-tenant isolation)
  </behavior>
  <action>
    Extend `backend/tests/test_correlation_service.py` (reuse the `_seed_asset`/`_seed_vuln` helpers from Plan 01). Add: (a) `test_single_source_does_not_correlate` — one source, assert `get_correlation_for_vuln` returns None. (b) A parametrized `test_confidence_bands` over source-count 2..6 seeding N distinct sources (drawn from VulnSource declaration order) on one cve+asset, asserting the D-08 band and `len(sources) == sources_count` and canonical order for each. (c) `test_correlation_tenant_scoped` — seed+correlate on tenant_a, assert `get_correlation_for_vuln(db, tenant_b, cve, asset)` is None (mirror test_vuln_source_filter.py's tenant-scoped shape). All `@pytest.mark.asyncio`. Run per-file (repo memory).
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret ENVIRONMENT=test pytest tests/test_correlation_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_correlation_service.py` exits 0 with the SC#4 test plus `test_single_source_does_not_correlate`, `test_confidence_bands` (2..6), and `test_correlation_tenant_scoped` all passing
    - `test_confidence_bands` asserts `len(corr["sources"]) == corr["sources_count"]` and canonical enum order for every count 2-6
    - 4/5/6 sources assert `confidence == "HIGH"`; 2/3 assert `"MEDIUM"`
    - `test_correlation_tenant_scoped` proves a tenant_a correlation is None for tenant_b
  </acceptance_criteria>
  <done>The count/name invariant and D-08 bands are locked as standing regressions across every source combination, and cross-tenant isolation of correlation reads is proven.</done>
</task>

</tasks>

<assumption_delta_decision>
Inherits Plan 01's decision: **promote** the `sources` array to the canonical source-set. This plan supplies the invariant test Plan 01 flagged — `sources_count == len(sources)` across 1-of-6 through 6-of-6 — and the per-tenant zero-loss proof that the promotion loses no existing data.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| re-correlation script → Postgres | A one-time operator-invoked script mutates tenant-scoped `vulnerability_correlations` rows across all active tenants; a missing tenant filter would bleed or corrupt tenant data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05 | Information Disclosure | `recorrelate_all_tenants.py` per-tenant loop + zero-loss query | mitigate | `run_correlations(db, t.id)` is already tenant-scoped on every internal query; the diagnostic before/after counts filter `WHERE tenant_id = :tid` and never run as a global aggregate (high severity — blocking) |
| T-30-06 | Tampering / DoS | Re-running the script | accept | `run_correlations` is idempotent (upsert on uq_correlation + prune); a re-run converges, and per-tenant try/except-continue prevents one tenant aborting the batch (D-07) — low residual risk |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('backend/scripts/recorrelate_all_tenants.py').read())"` — script is valid Python.
- `grep -c compute_risk_scores backend/scripts/recorrelate_all_tenants.py` == 0.
- `cd backend && pytest tests/test_correlation_service.py -v` — full correlation suite green (SC#4 + bands + invariant + tenant scope).
</verification>

<success_criteria>
- CORR-02: existing correlation data migrates with zero loss, verified per-tenant (the script's COALESCE query returns 0 for every tenant).
- CORR-03: `sources_count == len(sources)` proven across every 1..6 source combination; bands match D-08.
- Cross-tenant isolation of correlation reads and the re-correlation batch is proven.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this plan produces (newly created symbols — exclude from cross-file drift verification)

- **New standalone script:** `backend/scripts/recorrelate_all_tenants.py` defining `async def _main()` (no new public functions; reuses `run_correlations`)
- **New tests (appended to the Plan 01 file):** `test_single_source_does_not_correlate`, `test_confidence_bands`, `test_correlation_tenant_scoped` in `backend/tests/test_correlation_service.py`
- Consumes (does not create) Plan 01's `sources`/`source_vuln_ids` columns, `ix_vulnerability_correlations_sources` index, `_SOURCE_ORDER`, and the rewritten `run_correlations`/`get_correlation_for_vuln`.
</artifacts_this_phase_produces>

<output>
After completion, create `.planning/phases/30-correlation-schema-fix/30-02-SUMMARY.md`.
</output>
