---
phase: 43-executive-compliance-reporting
plan: 04
type: execute
wave: 4
depends_on: [1, 3]
files_modified:
  - backend/app/vulnerabilities/router.py
  - backend/tests/test_sla_route.py
  - frontend/src/lib/queries/keys.ts
  - frontend/src/lib/queries/use-mttr-by-tier.ts
  - frontend/src/lib/queries/use-sla-metrics.ts
  - frontend/src/hooks/use-lens.ts
  - frontend/src/components/dashboard/lens-switcher.tsx
  - frontend/src/components/dashboard/leadership-hero.tsx
  - frontend/src/components/dashboard/mttr-by-tier-tile.tsx
  - frontend/src/components/dashboard/sla-compliance-tile.tsx
  - frontend/src/components/dashboard/framework-posture-strip.tsx
  - frontend/src/app/(authed)/dashboard/page.tsx
  - frontend/src/app/(authed)/dashboard/page.test.tsx
  - frontend/src/components/dashboard/lens-switcher.test.tsx
  - frontend/src/components/dashboard/framework-posture-strip.test.tsx
autonomous: false
requirements: [RPT-02]
must_haves:
  truths:
    - "The /dashboard page renders per job-function lens (analyst / IT-ops / compliance / leadership); any authenticated user can switch lens — lens is a job-function view, NOT the RBAC role tier (D-05) (D-06: the existing /dashboard is reconfigured per lens rather than net-new per-persona routes)"
    - "Analyst and IT-ops lenses render the existing action-first dashboard byte-for-byte unchanged (hero + stat-strip + activity-rail + top5-card)"
    - "Leadership lens renders: Export board report CTA, risk-trend hero chart (reused), MTTR-by-tier tile, SLA-compliance % tile, framework-posture strip — no triage widgets (D-07: leadership/compliance lenses = trend-&-posture widgets; analyst/IT-ops lenses = the existing action-first triage widgets)"
    - "Compliance lens renders posture-first: hero-sized framework-posture strip, compact SLA-compliance tile, compact risk-trend chart, 'View full compliance page' link"
    - "Lens selection persists via URL param ?lens= with a localStorage fallback for bare /dashboard visits; default lens = analyst (zero disruption)"
    - "The framework-posture strip aggregates pass/partial/fail/not-measured counts per framework from /api/v1/compliance/overview; each pill links to /dashboard/compliance?framework="
    - "Every lens widget is tenant-scoped (data comes only from tenant-scoped endpoints)"
    - "The leadership/compliance-lens SLA-compliance tile is exception-consistent with /dashboard/compliance and the exported PDF: it reads GET /api/v1/vulnerabilities/sla/metrics with exclude_exceptions=true (additive query param, default false preserves existing consumers), so a tenant with active accepted-risk exceptions sees the SAME SLA-compliance % on the tile, the compliance page (Plan 01), and the board PDF (Plan 02) — never a divergent number (Pitfall 2)"
    - "The existing onboarding gate (no_scanners / no_data_yet) remains the OUTERMOST check, unchanged — the lens switcher only appears once onboarding clears"
    # UI-SPEC ## UI Considerations E1 + E8 — all `covered`:
    - "E1 long-text: lens segment labels are a fixed closed set; the control never wraps (single row, top-right of header)"
    - "E8 no-data ★: each metric tile (MTTR-by-tier, SLA-compliance %) reuses the 'Not yet measured' neutral zero-denominator treatment when its metric has no data — never 0/0%, which would read as a real value to a board audience"
    - "E8 risk-trend no-history ★: with <2 data points the trend hero renders a neutral centered 'not enough history to plot a trend yet — check back after N days' note, not a misleading line"
    - "E8 populated/composition: ordered focal points per lens are exactly Leadership items 1–5 and Compliance items 1–4"
  artifacts:
    - path: "frontend/src/components/dashboard/lens-switcher.tsx"
      provides: "4-segment control (Analyst/IT-ops/Compliance/Leadership), ChipBar active-state chrome, aria-pressed"
    - path: "frontend/src/hooks/use-lens.ts"
      provides: "useUrlState('lens') + localStorage fallback (new URL+localStorage dual-persistence plumbing)"
    - path: "frontend/src/components/dashboard/framework-posture-strip.tsx"
      provides: "per-framework aggregate pass/partial/fail/not-measured pills; shared compact (leadership) + hero-sized (compliance) variants; consumes useComplianceOverview"
    - path: "frontend/src/components/dashboard/mttr-by-tier-tile.tsx"
      provides: "3 tier cells (Critical/High/Moderate), Display mono days, 'Not yet measured' on null"
    - path: "frontend/src/components/dashboard/sla-compliance-tile.tsx"
      provides: "Display % + 3-segment bar; 'Not yet measured' on null"
    - path: "frontend/src/app/(authed)/dashboard/page.tsx"
      provides: "lens-branched composition preserving onboarding gate + per-section ErrorBoundary"
  key_links:
    - from: "frontend/src/components/dashboard/framework-posture-strip.tsx"
      to: "/api/v1/compliance/overview"
      via: "useComplianceOverview() (Plan 01)"
      pattern: "useComplianceOverview"
    - from: "frontend/src/components/dashboard/leadership-hero.tsx"
      to: "frontend/src/components/dashboard/export-board-report-dialog.tsx"
      via: "Export board report CTA opens the dialog (Plan 03)"
      pattern: "ExportBoardReportDialog|export-board-report"
    - from: "frontend/src/app/(authed)/dashboard/page.tsx"
      to: "frontend/src/hooks/use-lens.ts"
      via: "lens value branches the returned JSX"
      pattern: "useLens|useUrlState.*lens"
    - from: "frontend/src/components/dashboard/mttr-by-tier-tile.tsx"
      to: "/api/v1/vulnerabilities/mttr/by-tier"
      via: "useMttrByTier()"
      pattern: "mttr/by-tier"
    - from: "frontend/src/lib/queries/use-sla-metrics.ts"
      to: "/api/v1/vulnerabilities/sla/metrics?exclude_exceptions=true"
      via: "useSlaMetrics() requests exclude_exceptions=true (matches Plan 01/02 convention)"
      pattern: "exclude_exceptions"
    - from: "backend/app/vulnerabilities/router.py::sla_metrics"
      to: "backend/app/vulnerabilities/sla_service.py::get_sla_metrics"
      via: "additive exclude_exceptions query param threaded into get_sla_metrics (Plan 01 extension)"
      pattern: "exclude_exceptions"
  prohibitions:
    - "Lens availability must not be gated on the RBAC role tier (owner/admin/analyst/viewer) — D-05: lens is job function, not permission"
    - "Metric tiles must not render 0/0% for absent data — gate on the metric's own null signal, never on a falsy/zero value"
---

<objective>
RPT-02: reconfigure the existing /dashboard into four job-function lenses (analyst / IT-ops / compliance / leadership) via a switchable view-lens model. Analyst/IT-ops keep the current triage dashboard unchanged; leadership/compliance surface trend-and-posture widgets — including the framework-posture strip (consuming Plan 01's /compliance/overview) and the Export board report CTA (opening Plan 03's dialog). This is the composition capstone that pulls both prior surfaces together.

Purpose: Each viewer sees only what their job function needs — a CISO sees outcomes and posture, an analyst sees the work queue — on one home page, no new routes.
Output: lens switcher + client-persisted lens state + leadership/compliance widget sets on /dashboard.
</objective>

<assumption_delta_decision>
Detector fires: RPT-02 introduces four persona "lenses". Per CONTEXT D-05 this is an ACCEPTED, deliberate add-alongside decision — lenses are a job-function view model DECOUPLED from the RBAC role tier (owner/admin/analyst/viewer). Not promoted onto the role column; not re-opened. Lens availability is never gated on `User.role`; persistence is client-side only (URL param + localStorage), no backend user-preference column this phase.
</assumption_delta_decision>

<edge_coverage_assumptions>
The spec-less edge probe returned RPT-02 as `unclassified/unresolved`. FLAGGED ASSUMPTION (planner-derived): RPT-02's real edge coverage is the E1/E8 UI-SPEC considerations authored into must_haves (single-row non-wrapping switcher; per-tile 'Not yet measured' on null; <2-point trend neutral note; onboarding gate preserved as outermost check per RESEARCH Open Question 3). If a lens interacts with the onboarding empty state in a way not covered here, flag it rather than inventing a new onboarding variant.
</edge_coverage_assumptions>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-executive-compliance-reporting/43-CONTEXT.md
@.planning/phases/43-executive-compliance-reporting/43-UI-SPEC.md
@.planning/phases/43-executive-compliance-reporting/43-PATTERNS.md
@.claude/skills/sketch-findings-getvul/references/page-layouts.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md
@.claude/skills/sketch-findings-getvul/references/visual-language.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md

<interfaces>
Existing read endpoints for the leadership/compliance tiles:
- GET /api/v1/vulnerabilities/mttr/by-tier -> list[dict] per tier (avg_seconds: null on empty tier)  [existing route]
- GET /api/v1/vulnerabilities/sla/metrics -> dict incl compliance_pct, remediated_total  [existing route]
- GET /api/v1/compliance/overview -> { controls: ControlStatus[] }  [Plan 01; via useComplianceOverview]
Components to reuse verbatim:
- frontend/src/components/dashboard/{hero,stat-strip-wired,activity-rail,top5-card}.tsx (analyst/IT-ops, unchanged)
- frontend/src/components/dashboard/trend-section.tsx / components/ui/risk-trend-chart.tsx (Phase 42, reused for leadership/compliance trend)
- frontend/src/components/dashboard/export-board-report-dialog.tsx (Plan 03; opened by leadership CTA)
- frontend/src/lib/queries/use-compliance.ts::useComplianceOverview (Plan 01)
Patterns:
- frontend/src/hooks/use-url-state.ts (useUrlState<T>(key, allowed, default)) — NOTE: no localStorage fallback built in; use-lens.ts adds it
- frontend/src/components/analytics/scope-window-controls.tsx / components/ui/trend-chart.tsx RangeToggle (segmented control + aria-pressed) — lens-switcher analog
- frontend/src/components/analytics/burndown-tile.tsx (stat-tile-with-states shape for MTTR/SLA tiles)
- frontend/src/components/coverage/coverage-connector-card.tsx (pill/card strip for posture strip)
- frontend/src/lib/queries/use-coverage-summary.ts (no-arg hook shape for use-mttr-by-tier/use-sla-metrics)
- frontend/src/app/(authed)/dashboard/page.tsx (existing composition + onboarding early-return L46-92)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lens switcher + URL/localStorage persistence + page.tsx lens branching (onboarding gate preserved)</name>
  <files>frontend/src/hooks/use-lens.ts, frontend/src/components/dashboard/lens-switcher.tsx, frontend/src/components/dashboard/lens-switcher.test.tsx, frontend/src/app/(authed)/dashboard/page.tsx, frontend/src/app/(authed)/dashboard/page.test.tsx</files>
  <read_first>
    - frontend/src/app/(authed)/dashboard/page.tsx (full — onboarding early-return L46-92, existing per-section ErrorBoundary/Suspense composition to preserve for analyst/IT-ops)
    - frontend/src/hooks/use-url-state.ts (useUrlState signature; Suspense/useSearchParams CSR-bailout requirement)
    - .planning/phases/43-executive-compliance-reporting/43-PATTERNS.md sections "dashboard/page.tsx", "use-url-state.ts", "lens-switcher.tsx" (localStorage-fallback note: NEW plumbing)
    - .planning/phases/43-executive-compliance-reporting/43-UI-SPEC.md "RPT-02 — Lens switcher" (4-segment, top-right of header, ChipBar active chrome, default analyst) + "## UI Considerations" E1 + RESEARCH Open Question 3 (onboarding gate outermost)
    - .claude/skills/sketch-findings-getvul/references/visual-language.md (active-chip: --color-pink-soft fill + --color-pink label/border; inactive: --color-surface-2 + --color-text-muted)
  </read_first>
  <behavior>
    - Test: default lens is 'analyst' with no URL param and no localStorage; analyst/IT-ops render the existing widget set (hero/stat-strip/activity-rail/top5).
    - Test: ?lens=leadership renders the leadership widget set; switching updates the URL param via router.replace and writes localStorage.
    - Test: a bare /dashboard visit with a stored lens in localStorage (no URL param) seeds that lens.
    - Test: the onboarding empty state (no_scanners/no_data_yet) still preempts ALL lenses (outermost early-return unchanged) — the lens switcher does not render.
    - Test: lens availability does NOT depend on User.role (all four selectable regardless of role tier).
  </behavior>
  <action>
    Create use-lens.ts: wrap useUrlState<Lens>('lens', ['analyst','it-ops','compliance','leadership'] as const, 'analyst') and add localStorage dual-persistence — on mount, if no URL param, seed from localStorage.getItem('dashboard-lens'); on set, write localStorage AND the URL param. Create lens-switcher.tsx: a 4-segment control (role="group", aria-pressed) with ChipBar active-state chrome (active = --color-pink-soft fill + --color-pink; inactive = --color-surface-2 + --color-text-muted), top-right of the dashboard header, single row (never wraps, E1). In page.tsx: keep the onboarding early-return as the OUTERMOST check byte-for-byte (RESEARCH Open Question 3); after it, read useLens() (wrap consumers in Suspense per the useSearchParams CSR-bailout note), render the lens switcher, and branch: analyst/it-ops → the exact existing block unchanged; leadership/compliance → the new widget composition (Task 2 components), each widget in its own ErrorBoundary matching the existing per-section isolation convention. Add tests per <behavior>.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- dashboard/page.test.tsx lens-switcher.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - use-lens.ts persists lens via URL param AND localStorage; default 'analyst'
    - lens-switcher.tsx renders 4 segments with aria-pressed and ChipBar active chrome, single row
    - page.tsx onboarding early-return is unchanged and preempts the lens switcher
    - analyst/it-ops branch renders the existing widgets unchanged; lens is not gated on User.role
    - npm run test -- dashboard/page.test.tsx lens-switcher.test.tsx passes
  </acceptance_criteria>
  <done>Lens switcher + persistence in place; analyst/IT-ops unchanged; onboarding gate preserved; lens decoupled from RBAC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Leadership + compliance lens widgets (tiles, posture strip, leadership hero) with no-data honesty</name>
  <files>backend/app/vulnerabilities/router.py, backend/tests/test_sla_route.py, frontend/src/lib/queries/keys.ts, frontend/src/lib/queries/use-mttr-by-tier.ts, frontend/src/lib/queries/use-sla-metrics.ts, frontend/src/components/dashboard/leadership-hero.tsx, frontend/src/components/dashboard/mttr-by-tier-tile.tsx, frontend/src/components/dashboard/sla-compliance-tile.tsx, frontend/src/components/dashboard/framework-posture-strip.tsx, frontend/src/components/dashboard/framework-posture-strip.test.tsx</files>
  <read_first>
    - .planning/phases/43-executive-compliance-reporting/43-UI-SPEC.md "RPT-02 — Lens switcher + per-lens widget composition" (Leadership items 1-5, Compliance items 1-4), "Color" (four-state posture + Display typography for hero numbers), "## UI Considerations" E8
    - .planning/phases/43-executive-compliance-reporting/43-PATTERNS.md "leadership-hero.tsx/mttr-by-tier-tile.tsx/sla-compliance-tile.tsx/framework-posture-strip.tsx" (analogs + the never-gate-on-falsy null discipline from use-analytics.ts L120-127)
    - frontend/src/components/analytics/burndown-tile.tsx (stat-tile-with-no-data shape), frontend/src/components/dashboard/hero.tsx (slot precedent for leadership-hero), frontend/src/components/dashboard/trend-section.tsx (reused trend)
    - backend/app/vulnerabilities/router.py::sla_metrics (L202-210 — the existing `GET /sla/metrics` route calling `get_sla_metrics(db, user.tenant_id)` with NO exception exclusion; add an additive `exclude_exceptions: bool = Query(False)` param and thread it into the call — get_sla_metrics gained this param in Plan 01)
    - backend/app/vulnerabilities/sla_service.py::get_sla_metrics (Plan 01 added `exclude_exceptions` + `remediated_total` in the return dict — the exception-consistent source the compliance page and PDF already use)
    - backend/tests/test_coverage.py / test_compliance.py (route-level test client + tenant fixture shape to mirror for the new test_sla_route.py); project memory getvul-backend-pytest-env
    - frontend/src/lib/queries/use-coverage-summary.ts (hook shape) + use-compliance.ts (Plan 01, for the posture strip)
    - frontend/src/components/dashboard/export-board-report-dialog.tsx (Plan 03 — the leadership CTA opens it)
    - .claude/skills/sketch-findings-getvul/references/visual-language.md, copy-voice.md, state-patterns.md
  </read_first>
  <behavior>
    - Test: mttr-by-tier-tile renders 'Not yet measured' for a tier whose avg_seconds is null; renders Display-size mono days for a real value (never '0 days' for null).
    - Test: sla-compliance-tile renders 'Not yet measured' when remediated_total==0 (null signal), never 0%.
    - Test (exception-consistency, backend regression in test_sla_route.py): GET /api/v1/vulnerabilities/sla/metrics?exclude_exceptions=true drops an actively-excepted finding from the breach/compliance computation — returning the SAME compliance_pct the compliance page (Plan 01's get_sla_metrics(exclude_exceptions=True)) computes; the default (no param / false) preserves today's behavior for existing consumers.
    - Test: framework-posture-strip aggregates pass/partial/fail/not-measured counts per framework from useComplianceOverview and each pill links to /dashboard/compliance?framework=<fw>.
    - Test: the risk-trend hero renders the neutral 'not enough history' note with <2 points (E8), not a line.
    - Test: leadership-hero renders the 'Export board report' CTA that opens the export dialog (Plan 03).
  </behavior>
  <action>
    First make the SLA source exception-consistent (Pitfall 2 on this new surface): in backend/app/vulnerabilities/router.py::sla_metrics add an additive `exclude_exceptions: bool = Query(False)` param and pass it into `get_sla_metrics(db, user.tenant_id, exclude_exceptions=exclude_exceptions)` (the param exists after Plan 01; default false keeps every existing consumer byte-identical). Add backend/tests/test_sla_route.py with the exception-consistency regression per <behavior>. Then add use-mttr-by-tier.ts + use-sla-metrics.ts (mirror use-coverage-summary.ts: no-arg useQuery, staleTime:0) hitting /api/v1/vulnerabilities/mttr/by-tier and /sla/metrics — use-sla-metrics.ts MUST request `?exclude_exceptions=true` so the leadership/compliance tile matches the compliance page and PDF; add matching keys.ts factory entries. Build the four widgets per the UI-SPEC composition: leadership-hero.tsx (Export board report gradient CTA opening ExportBoardReportDialog + the leadership top strip); mttr-by-tier-tile.tsx (3 tier cells Critical/High/Moderate, Display-size mono days, tier hues; 'Not yet measured' on null — gate on the metric's OWN null, never on 0); sla-compliance-tile.tsx (Display % + thin 3-segment on-track/approaching/breached bar using --color-success/-warning/-danger; 'Not yet measured' on null); framework-posture-strip.tsx (one pill per framework aggregating pass/partial/fail/not-measured counts from useComplianceOverview; each pill → /dashboard/compliance?framework=; shared compact [leadership] + hero-sized [compliance] variants via a prop). Compose them in page.tsx's leadership branch (items 1-5: Export CTA, reused trend hero, MTTR tile, SLA tile, posture strip — no triage widgets) and compliance branch (items 1-4: hero-sized posture strip, compact SLA tile, compact trend, 'View full compliance page' link). Apply the <2-point neutral 'not enough history' note to the reused trend hero (E8). Add the posture-strip test per <behavior>. All copy per copy-voice.md.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test .venv/bin/pytest tests/test_sla_route.py -x</automated>
    <automated>cd frontend && npm run test -- framework-posture-strip.test.tsx dashboard/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - sla_metrics route accepts additive `exclude_exceptions` (default false = unchanged for existing consumers); use-sla-metrics.ts requests exclude_exceptions=true; test_sla_route.py proves an actively-excepted finding is excluded and the returned compliance_pct matches Plan 01's exception-consistent source
    - mttr-by-tier-tile and sla-compliance-tile render 'Not yet measured' on a null metric signal, never 0/0%
    - framework-posture-strip aggregates per-framework counts from /api/v1/compliance/overview; pills link to /dashboard/compliance?framework=
    - leadership branch renders items 1-5 (no triage widgets); compliance branch renders items 1-4
    - leadership-hero's Export CTA opens the Plan 03 export dialog
    - npm run test -- framework-posture-strip.test.tsx dashboard/page.test.tsx passes
  </acceptance_criteria>
  <done>Leadership + compliance lens widgets composed with tenant-scoped data and mandatory no-data honesty; posture strip wired to the compliance endpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the four dashboard lenses</name>
  <files>(none — visual verification of /dashboard lenses)</files>
  <what-built>The lens switcher and the leadership/compliance widget sets on /dashboard (analyst/IT-ops unchanged).</what-built>
  <action>Human-only visual verification. Claude has already automated the lens switcher, persistence, and all lens widgets (Tasks 1-2). Follow the how-to-verify steps below, then pause for the resume-signal. Do NOT proceed until the user approves.</action>
  <read_first>
    - .planning/phases/43-executive-compliance-reporting/43-UI-SPEC.md "RPT-02" section
    - project memory getvul-local-e2e-perf-gate
  </read_first>
  <how-to-verify>
    1. Start the local stack (prod build, admin user) and open /dashboard.
    2. Confirm the 4-segment lens switcher renders top-right; default is Analyst; Analyst and IT-ops show the existing triage dashboard unchanged.
    3. Switch to Leadership: verify Export board report CTA, risk-trend hero, MTTR-by-tier tile, SLA-compliance % tile, framework-posture strip — and NO triage widgets.
    4. Switch to Compliance: verify hero-sized posture strip, compact SLA tile, compact trend, 'View full compliance page' link. Click a framework pill → lands on /dashboard/compliance?framework=.
    5. Reload the page: the last-selected lens persists (URL param / localStorage). On a bare /dashboard (no param) the stored lens is used.
    6. On a fresh/no-data tenant: metric tiles read 'Not yet measured' (never 0%); the onboarding panel still preempts everything if the tenant has no scanners/data.
  </how-to-verify>
  <verify>Human confirms each lens renders its specified widget set and no-data honesty; no automated command (visual verification).</verify>
  <acceptance_criteria>
    - All four lenses render their specified widget sets; analyst/IT-ops unchanged
    - Lens persists across reload and on bare /dashboard
    - No-data tiles read 'Not yet measured', not 0%
    - Framework pills deep-link to /dashboard/compliance?framework=
  </acceptance_criteria>
  <done>User approves the four dashboard lenses (or issues are logged and addressed before proceeding).</done>
  <resume-signal>Type "approved" or describe issues (e.g. wrong widget set, lens not persisting, 0% shown for no data)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser lens widgets → tenant-scoped read endpoints | Each widget reads only the caller's own-tenant data; lens selection is presentation-only and never changes authorization |

## STRIDE Threat Register (ASVS L1, block on: high)
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-13 | Elevation of Privilege | lens vs RBAC confusion | mitigate | lens is a client-side job-function view (D-05) with zero authorization effect; all data still flows through the existing tenant-scoped + require_viewer endpoints; lens is never gated on nor grants role — HIGH-visibility design invariant, tested |
| T-43-14 | Information Disclosure | mttr/sla/compliance reads | mitigate | reuse existing tenant-scoped routes (/vulnerabilities/mttr/by-tier, /sla/metrics, /compliance/overview); no new endpoint, no client-supplied tenant id |
| T-43-15 | Tampering-by-omission | metric tiles / SLA source consistency | mitigate | never render 0/0% for absent data — 'Not yet measured' on the null signal; AND the SLA tile reads /sla/metrics with `exclude_exceptions=true` so its % matches the compliance page + PDF (no divergent board number from an active-exception discrepancy — Pitfall 2); regression-tested in test_sla_route.py — HIGH |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)
- `frontend/src/hooks/use-lens.ts` (URL+localStorage lens persistence)
- `frontend/src/components/dashboard/lens-switcher.tsx`, `leadership-hero.tsx`, `mttr-by-tier-tile.tsx`, `sla-compliance-tile.tsx`, `framework-posture-strip.tsx`
- `frontend/src/lib/queries/use-mttr-by-tier.ts`, `use-sla-metrics.ts` + new keys.ts factory entries
- Extended `frontend/src/app/(authed)/dashboard/page.tsx` (lens-branched composition)
</artifacts_produced>

<verification>
- `npm run test -- dashboard/page.test.tsx lens-switcher.test.tsx framework-posture-strip.test.tsx` green.
- Phase gate: full frontend `npm run test` + (UI changed) the Playwright quality gate per project memory getvul-local-e2e-perf-gate.
- Human-verify checkpoint approved.
</verification>

<success_criteria>
- RPT-02 shipped: four job-function lenses on one /dashboard, tenant-scoped, decoupled from RBAC, with leadership/compliance surfacing trend-and-posture widgets and honest no-data states.
- Phase goal met: a CISO/compliance owner can prove the program works (board PDF + leadership lens) and show framework posture (compliance lens + /dashboard/compliance) without an analyst hand-building a deck.
</success_criteria>

<output>
After completion, create `.planning/phases/43-executive-compliance-reporting/43-04-SUMMARY.md`
</output>
