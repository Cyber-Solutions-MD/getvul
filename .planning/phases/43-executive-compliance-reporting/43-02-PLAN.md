---
phase: 43-executive-compliance-reporting
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - backend/pyproject.toml
  - backend/app/export.py
  - backend/app/vulnerabilities/service.py
  - backend/app/main.py
  - backend/app/reports.py
  - backend/tests/test_export.py
  - backend/tests/test_reports.py
  - backend/tests/test_mttr.py
autonomous: true
requirements: [RPT-01]
must_haves:
  truths:
    - "The exec/board PDF exports a PERIOD-SCOPED risk-trend chart and PERIOD-SCOPED MTTR-by-tier (both honor the selected period), plus an SLA-compliance section that remains a fixed trailing-90-day metric (per RESEARCH Pitfall 3, get_sla_metrics has no period param) — the SLA section carries a caption stating it is a trailing-90-day metric independent of the selected period; the three sections are appended after the existing summary-stats block and before top-hosts/top-remediations/tickets"
    - "The three new PDF sections render as real chart images (matplotlib Agg PNG embedded via pdf.image), producing a board-deck look on the existing tenant-branded light surface (D-02: charts are rendered server-side to raster PNGs and embedded — matplotlib added as a new backend dependency)"
    - "The period selector supports presets (Last 30 days / Last 90 days / Last quarter / Last year) plus a validated custom start/end range"
    - "The new SLA-compliance section excludes actively-excepted findings (exclude_exceptions=True), consistent with the rest of the same PDF, and is captioned as a trailing-90-day metric independent of the selected period"
    - "A section whose evidencing metric has zero underlying data renders 'Not yet measured', never a fabricated 0 or 100%"
    - "The risk-trend chart with <2 data points renders a neutral 'not enough history' note on the white PDF surface, never a fabricated line (E9)"
    - "Existing scheduled reports keep their exact shape — the three new section keys are appended to the default sections list, never inserted mid-list, at all 3 call sites"
    - "A scheduled board report (ScheduledReport with the new section keys) is picked up by run_due_reports/_is_due and generates the extended PDF on cadence, reusing the existing SMTP delivery path (no second delivery codepath)"
    # UI-SPEC ## UI Considerations E9 (PDF board report) — all `covered`:
    - "E9 populated: new sections use the existing tenant-branded light layout with print-safe light-mode hex values (critical #DC2626 / high #EA580C / medium #B45309; SLA #15803D/#B45309/#DC2626), never dark-theme tokens"
    - "E9 empty: zero-denominator MTTR/SLA sections render 'Not yet measured' rather than 0; version-boundary marker (if period spans a model-version change) is neutral gray dashed, never colored"
    - "E9 error: a chart-render failure surfaces a 'charts unavailable' flag the route can act on (retry with charts off / tables only), not a silently dropped section"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "matplotlib>=3.9 + Pillow>=10.0 dependency declarations"
      contains: "matplotlib"
    - path: "backend/app/export.py"
      provides: "_render_*_chart helpers (Figure+FigureCanvasAgg, no pyplot) + 3 new PDF sections + 3 new keys in _collect_summary_data"
      contains: "FigureCanvasAgg"
    - path: "backend/app/vulnerabilities/service.py"
      provides: "get_mttr_by_tier extended with optional (start,end) period filter (landmine 3)"
      contains: "def get_mttr_by_tier"
    - path: "backend/app/main.py"
      provides: "export_resource period preset/from/to query params + validation + span cap + calendar-quarter + audit-section extension"
    - path: "backend/tests/test_export.py"
      provides: "chart PNG decodable headless; 3-section generation; section-toggle compatibility; zero-denom not-measured"
    - path: "backend/tests/test_reports.py"
      provides: "scheduled board report picked up + extended PDF generated on cadence"
  key_links:
    - from: "backend/app/export.py"
      to: "backend/app/analytics/service.py + sla_service.py + vulnerabilities/service.py"
      via: "direct service calls (D-01a, no HTTP), get_sla_metrics(exclude_exceptions=True)"
      pattern: "get_scoped_trend_series|get_mttr_by_tier|get_sla_metrics"
    - from: "backend/app/export.py"
      to: "matplotlib Figure/FigureCanvasAgg → pdf.image"
      via: "io.BytesIO PNG buffer"
      pattern: "pdf\\.image\\("
    - from: "backend/app/reports.py"
      to: "backend/app/export.py::generate_executive_summary_pdf"
      via: "run_due_reports → _send_report with new section keys"
      pattern: "generate_executive_summary_pdf"
  prohibitions:
    - "The board PDF's SLA section must not silently include actively-excepted findings inconsistently with the rest of the same document"
    - "The board PDF must not present an SLA/MTTR section as a real value when its metric has zero data (never a fabricated 0 or 100%)"
    - "Must not import matplotlib.pyplot anywhere (thread-unsafe global state in a shared web process) — use Figure + FigureCanvasAgg only"
---

<objective>
RPT-01 backend: extend the existing branded exec-summary PDF (`export.py`, fpdf2) with three new server-rendered chart sections (risk trend, MTTR-by-tier, SLA compliance) for a selected period, add the matplotlib dependency, and make the board report a schedulable format via the existing `ScheduledReport` delivery path. Threads landmine 3 (MTTR/SLA have no period parameter today → additive extension) and reuses Plan 01's `exclude_exceptions` extension.

Purpose: A CISO exports a board-ready PDF proving the program's trajectory without an analyst hand-building a deck.
Output: Extended `export.py`/`main.py`/`reports.py`, an additively period-aware `get_mttr_by_tier`, and the matplotlib/Pillow deps.
</objective>

<reversibility_note>
D-02 (server-side chart rendering) is rated COSTLY (not one-way): it adds `matplotlib` — a new backend dependency baked into the Docker image. Falling back to tables/numbers later means re-authoring the PDF sections. RESEARCH.md verified matplotlib works headless in the existing `python:3.12-slim` image with zero apt-get changes (manylinux wheels bundle FreeType/libpng; bundled DejaVu Sans; writable $HOME already present). No checkpoint is required (reversible-but-costly, not one-way), but the executor must add the dep exactly as `matplotlib>=3.9` + explicit `Pillow>=10.0` and never `import matplotlib.pyplot`.
</reversibility_note>

<edge_coverage_assumptions>
The spec-less edge probe returned RPT-01 as `unclassified/unresolved`. FLAGGED ASSUMPTION (planner-derived): RPT-01's real edge coverage is the E4/E9 UI-SPEC considerations authored into must_haves (chart-render failure → tables-only fallback flag; zero-denominator → 'Not yet measured'; <2-point trend → neutral 'not enough history' note; historical-custom-range burndown mismatch handled per Pitfall 4) plus the exception-exclusion + no-fabricated-value prohibitions. Pitfall 4 decision (planner): restrict RPT-01's custom range to always end "today" (matches every preset; sidesteps the burndown-vs-trend historical-window mismatch); if a fully historical end date is supplied, the burndown figure must be flagged "not available for historical periods" rather than silently mismatched.
</edge_coverage_assumptions>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-executive-compliance-reporting/43-CONTEXT.md
@.planning/phases/43-executive-compliance-reporting/43-RESEARCH.md
@.planning/phases/43-executive-compliance-reporting/43-PATTERNS.md
@.planning/phases/43-executive-compliance-reporting/43-UI-SPEC.md

<interfaces>
Data sources to call DIRECTLY (no HTTP round-trip, D-01a):
- backend/app/analytics/service.py::get_scoped_trend_series(db, tenant_id, start, end, ...) + detect_version_boundaries(...) + get_burndown_rate(db, tenant_id, days=..., ...) + MAX_ANALYTICS_WINDOW_DAYS (=1096)
- backend/app/vulnerabilities/service.py::get_mttr_by_tier(db, tenant_id)  (extend with optional start,end here)
- backend/app/vulnerabilities/sla_service.py::get_sla_metrics(db, tenant_id, *, exclude_exceptions=True)  (extended in Plan 01; returns compliance_pct + remediated_total)
Existing PDF scaffold to extend:
- backend/app/export.py::_collect_summary_data (L308, `sections` default list + `open_filter` exception-exclusion), ::generate_executive_summary_pdf (L623, section()/row() closures, branding primary/accent colors)
- backend/app/reports.py::create_report (L61 default sections), ::_send_report (L182 fallback sections), ::run_due_reports/_is_due
- backend/app/main.py::export_resource (L385) + analytics/router.py L74-86 custom-range validation block to copy
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add matplotlib/Pillow deps + chart-render helpers (Figure+FigureCanvasAgg, no pyplot)</name>
  <files>backend/pyproject.toml, backend/app/export.py, backend/tests/test_export.py</files>
  <read_first>
    - .planning/phases/43-executive-compliance-reporting/43-RESEARCH.md "Standard Stack" (matplotlib>=3.9 + explicit Pillow>=10.0; Pillow already transitive) + Pattern 1 (verified render code) + Pitfall 5 (never pyplot) + Pitfall 6 (sync CPU-bound in async route)
    - .planning/phases/43-executive-compliance-reporting/43-PATTERNS.md "export.py" section (chart-render helper code, branding color values L636-641, print-safe hex values, best-effort image-embed convention L653-661)
    - .planning/phases/43-executive-compliance-reporting/43-UI-SPEC.md "RPT-01 — PDF Rendering Contract" (white bg, tenant primary_color for trend line, neutral-gray dashed version-boundary marker, light-mode severity/SLA hexes, 2x scale ~180mm)
    - backend/app/export.py (imports L1-18; existing pdf.image logo embed)
    - backend/pyproject.toml (existing `fpdf2>=2.8` dependency line)
  </read_first>
  <behavior>
    - Test: each chart-render helper returns an io.BytesIO whose bytes decode as a valid PNG (PIL.Image.open) headless (DISPLAY unset).
    - Test: no module in backend/app imports matplotlib.pyplot (grep assertion in the test or a source assertion).
    - Test: the risk-trend helper given <2 points returns a "not enough history" placeholder figure/signal (E9), not a plotted line.
  </behavior>
  <action>
    Add `"matplotlib>=3.9"` and `"Pillow>=10.0"` to backend/pyproject.toml [project] dependencies (next to fpdf2). Run the install per RESEARCH.md (`uv pip install -e ".[dev]"` or pip in .venv). In export.py add three private helpers — `_render_risk_trend_chart(...)`, `_render_mttr_by_tier_chart(tiers, days, colors)`, `_render_sla_compliance_chart(...)` — each constructing `matplotlib.figure.Figure` + `matplotlib.backends.backend_agg.FigureCanvasAgg` (import lazily inside the helper module scope; NEVER `import matplotlib.pyplot`), rendering to `io.BytesIO` PNG at dpi=200, figsize ~7.2x4.0in. Use the UI-SPEC print-safe hexes for MTTR bars (#DC2626/#EA580C/#B45309) and SLA bar (#15803D/#B45309/#DC2626); use the tenant's `primary_color_r/g/b` for the trend line; neutral gray dashed for version-boundary markers. The trend helper must handle <2 points → neutral "not enough history" note (E9). Add a top-of-helper comment documenting the no-pyplot rule (mirroring analytics/service.py's inline-convention comment). Add tests per <behavior> to a new test_export.py.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test .venv/bin/pytest tests/test_export.py -x -k chart</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `matplotlib>=3.9` and `Pillow>=10.0`
    - export.py contains a def that renders a matplotlib Figure to PNG bytes via FigureCanvasAgg; grep for `import matplotlib.pyplot` across backend/app returns zero matches
    - the chart helpers produce PIL-decodable PNG bytes with DISPLAY unset
    - pytest tests/test_export.py -x -k chart exits 0
  </acceptance_criteria>
  <done>matplotlib/Pillow declared; three no-pyplot chart-render helpers produce valid headless PNGs; <2-point trend degrades to a neutral note.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Three new PDF sections + get_mttr_by_tier period extension (landmine 3)</name>
  <files>backend/app/vulnerabilities/service.py, backend/app/export.py, backend/tests/test_export.py, backend/tests/test_mttr.py</files>
  <read_first>
    - backend/app/vulnerabilities/service.py::get_mttr_by_tier (L507 — RemediationEvent has remediated_at DateTime; add optional .where(between))
    - .planning/phases/43-executive-compliance-reporting/43-RESEARCH.md Pitfall 3 (MTTR/SLA no-period-param — additive extension) + Pitfall 4 (burndown trailing-from-now) + Common Pitfalls #1/#2
    - .planning/phases/43-executive-compliance-reporting/43-PATTERNS.md "export.py" (section()/row() closures L677-775, section-toggle guard `if "vulns" not in sec`, new-section ordering)
    - .planning/phases/43-executive-compliance-reporting/43-UI-SPEC.md "RPT-01 — PDF Rendering Contract" (section order: risk trend → MTTR by tier → SLA compliance) + E9
    - backend/tests/test_mttr.py (RemediationEvent-seeding helper to reuse) and backend/tests/test_analytics.py (trend/burndown fixture style)
  </read_first>
  <behavior>
    - Test: get_mttr_by_tier(db, tenant_id, start=..., end=...) filters RemediationEvent.remediated_at to the window; default (no window) returns byte-identical all-time results.
    - Test: generate_executive_summary_pdf with the 3 new section keys returns non-empty valid PDF bytes containing embedded images.
    - Test: with sections omitting the new keys, output matches the pre-existing behavior (backward compatible).
    - Test: a tenant with zero remediation history renders the SLA section as "Not yet measured", not 100% (Pitfall 1 consumption).
    - Test: the SLA section is computed with exclude_exceptions=True (an actively-excepted finding does not inflate the breach count).
  </behavior>
  <action>
    Extend get_mttr_by_tier with keyword-only optional `start: date|datetime|None=None, end=None`; when both set, add `.where(RemediationEvent.remediated_at.between(start, end))` (additive; default unchanged). In export.py::_collect_summary_data, append `"risk_trend"`, `"mttr_by_tier"`, `"sla_compliance"` to the default sections list (append only — never mid-list) and add three top-level return keys computed via the D-01a direct service calls (get_scoped_trend_series+detect_version_boundaries for trend; get_mttr_by_tier for MTTR; get_sla_metrics(exclude_exceptions=True) for SLA, with a `remediated_total==0 -> not measured` guard). In generate_executive_summary_pdf, after the existing summary-stats block and before top-hosts/top-remediations/tickets, draw the three sections gated by `if "risk_trend" in sec:` etc., embedding the Task-1 chart images via `pdf.image(buf, w=180)`; render "Not yet measured" (never 0/100) for zero-denom metrics; render a small caption under the SLA-compliance section reading "Trailing 90-day metric, independent of the selected period" (the SLA source is not period-scoped — Pitfall 3), while the risk-trend and MTTR-by-tier sections DO honor the selected period; wrap chart embedding in the best-effort try/except but set a `charts_unavailable` flag (do NOT silently drop the whole section — surface tables-only fallback per E9). Apply the Pitfall-4 decision: pass the trend/burndown window ending "today"; if a historical end date is supplied, mark burndown "not available for historical periods". Add tests per <behavior>.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test .venv/bin/pytest tests/test_export.py tests/test_mttr.py -x</automated>
  </verify>
  <acceptance_criteria>
    - get_mttr_by_tier accepts optional start/end and default call is byte-identical to today (grep existing call sites unchanged)
    - generate_executive_summary_pdf emits the 3 new sections in order risk trend → MTTR by tier → SLA compliance, after summary-stats, before top-hosts
    - a zero-remediation tenant's SLA section reads "Not yet measured", never 100%
    - the SLA section calls get_sla_metrics with exclude_exceptions=True
    - the SLA section renders the caption "Trailing 90-day metric, independent of the selected period"; the MTTR-by-tier section reflects the selected period (period-filtered get_mttr_by_tier)
    - pytest tests/test_export.py tests/test_mttr.py exits 0
  </acceptance_criteria>
  <done>PDF gains 3 period-aware, exception-consistent, zero-data-honest chart sections; MTTR period filter added additively; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Export route period params + validation + calendar-quarter + audit + scheduled-report section keys</name>
  <files>backend/app/main.py, backend/app/reports.py, backend/tests/test_reports.py, backend/tests/test_export.py</files>
  <read_first>
    - backend/app/main.py::export_resource (L385-449 — signature, filters dict, audit call L446-449)
    - backend/app/analytics/router.py (L74-86 custom-range validation block: both-or-neither, to>=from, span cap MAX_ANALYTICS_WINDOW_DAYS)
    - .planning/phases/43-executive-compliance-reporting/43-RESEARCH.md "Calendar-quarter helper" code + Open Question 1 (Last quarter = most-recently-completed calendar quarter) + Security Domain V5/V7
    - backend/app/reports.py (create_report L61 default sections; _send_report L182 fallback sections; run_due_reports/_is_due L124-166; delivery L187-235)
    - .planning/phases/43-executive-compliance-reporting/43-PATTERNS.md "reports.py" (3 call sites for the default sections list) + "main.py::export_resource"
  </read_first>
  <behavior>
    - Test: export_resource with period=quarter resolves to the most-recently-completed calendar quarter [start,end].
    - Test: custom from/to — both-or-neither (422 if one missing), to>=from (422 otherwise), span > MAX_ANALYTICS_WINDOW_DAYS -> 422.
    - Test: the audit call records which new sections were requested.
    - Test: a ScheduledReport carrying the new section keys is picked up by run_due_reports/_is_due and produces the extended PDF (via _send_report), reusing the existing SMTP path.
    - Test: reports.py default sections list at all 3 call sites includes the 3 new keys appended at the end (existing scheduled reports unchanged in shape).
  </behavior>
  <action>
    Add `period: str | None = Query(None)` (enum 30d/90d/quarter/year), `from_: date | None = Query(None, alias="from")`, `to: date | None = Query(None, alias="to")` to export_resource. Port the analytics/router.py custom-range validation block verbatim (both-or-neither, to>=from, span cap using analytics MAX_ANALYTICS_WINDOW_DAYS or an export-local equivalent — do NOT accept an unbounded range: V5/DoS). Add a `last_completed_quarter(today)` helper (RESEARCH.md sketch) for the `quarter` preset; default preset when none supplied = last quarter (D-03/UI-SPEC). Resolve the period into start/end and thread into filters passed to generate_executive_summary_pdf. Extend the `audit(db, user, "export.summary", ...)` filters payload to include the requested sections/period. In reports.py, append `"risk_trend"`, `"mttr_by_tier"`, `"sla_compliance"` to the default sections list at BOTH call sites (create_report L61, _send_report L182) — keep in lockstep with export.py's default. Add test_reports.py (new file) + route tests to test_export.py per <behavior>.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test .venv/bin/pytest tests/test_reports.py tests/test_export.py -x</automated>
  </verify>
  <acceptance_criteria>
    - export_resource accepts period + from/to (alias) with the ported validation (422 on both-or-neither / to<from / over-cap)
    - a `last_completed_quarter` helper exists and `period=quarter` uses it; default = last quarter
    - reports.py default sections list at both call sites ends with the 3 new keys appended
    - test_reports.py proves a ScheduledReport with new keys generates the extended PDF via run_due_reports
    - pytest tests/test_reports.py tests/test_export.py exits 0
  </acceptance_criteria>
  <done>On-demand + scheduled board reports accept a bounded period and the 3 new sections, audited, with no second delivery codepath; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → GET /api/v1/export/summary | Authenticated user requests own-tenant PDF; period/date-range params are untrusted input |
| scheduler → _send_report → SMTP | Scheduled report must deliver only the owning tenant's data to that tenant's recipients |

## STRIDE Threat Register (ASVS L1, block on: high)
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-05 | Denial of Service | export_resource custom date range | mitigate | server-side span cap (MAX_ANALYTICS_WINDOW_DAYS) + both-or-neither + to>=from validation; reject unbounded ranges — HIGH, blocking |
| T-43-06 | Information Disclosure | export_resource / _send_report | mitigate | existing tenant-scoped `_collect_summary_data`/data-source calls use the caller's tenant_id; scheduled reports deliver to `report.recipients` for `report.tenant_id` only; new sections add no cross-tenant read |
| T-43-07 | Tampering-by-omission (data integrity) | new SLA/MTTR PDF sections | mitigate | exclude_exceptions=True + remediated_total==0 -> "Not yet measured"; a fabricated board number is treated as HIGH |
| T-43-08 | Repudiation | export audit trail | mitigate | extend existing `audit(..., "export.summary", ...)` to log requested sections/period |
| T-43-09 | Injection (V5) | chart labels | accept | chart text comes from tenant's own already-trusted DB data (hostnames/product names) — same risk profile as the existing PDF; matplotlib renders text as raster, no code execution surface |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)
New/changed symbols (exclude from source-drift verification):
- `backend/app/export.py`: `_render_risk_trend_chart`, `_render_mttr_by_tier_chart`, `_render_sla_compliance_chart`; 3 new `_collect_summary_data` return keys (`risk_trend`/`mttr_by_tier`/`sla_compliance`); 3 new PDF section blocks
- `backend/app/vulnerabilities/service.py::get_mttr_by_tier`: new optional `start`/`end` params
- `backend/app/main.py`: `export_resource` new `period`/`from`/`to` params + a `last_completed_quarter()` helper (location: main.py or export.py per executor)
- `backend/app/reports.py`: extended default `sections` lists (new report "format"/section-set convention using the existing JSONB columns — new `ScheduledReport` section-key values: `risk_trend`, `mttr_by_tier`, `sla_compliance`)
- New deps: `matplotlib`, explicit `Pillow` in pyproject.toml
- New test files: `test_export.py`, `test_reports.py`; new cases in `test_mttr.py`
</artifacts_produced>

<verification>
- `pytest tests/test_export.py tests/test_reports.py tests/test_mttr.py -x` green (env vars set).
- Per-wave: full backend suite green.
- `grep -rn "import matplotlib.pyplot" backend/app` returns zero matches.
</verification>

<success_criteria>
- RPT-01 backend complete: a bounded-period board PDF with three real chart sections, exception-consistent and zero-data-honest, schedulable via the existing delivery path.
- Landmine 3 (no-period-param) addressed additively; landmines 1+2 consumed correctly from Plan 01.
</success_criteria>

<output>
After completion, create `.planning/phases/43-executive-compliance-reporting/43-02-SUMMARY.md`
</output>
