---
phase: 43-executive-compliance-reporting
plan: 03
type: execute
wave: 3
depends_on: [2]
files_modified:
  - frontend/src/components/dashboard/export-board-report-dialog.tsx
  - frontend/src/components/dashboard/export-board-report-dialog.test.tsx
autonomous: false
requirements: [RPT-01]
must_haves:
  truths:
    - "A user can open the 'Export board report' dialog, choose a period (presets Last 30 days / Last 90 days / Last quarter / Last year + custom start/end), and download the branded PDF"
    - "The dialog's default period preset is 'Last quarter' (board cadence)"
    - "The dialog offers a secondary 'Also send this report {cadence} by email' disclosure that reveals the existing ScheduledReport cadence/recipients fields inline — no second dialog, no second codepath (D-04). On open it pre-loads existing schedule state via GET /api/v1/reports and seeds the toggle CHECKED when a board ScheduledReport already exists (otherwise unchecked)"
    - "The dialog uses the authenticated blob-download fetch (401-refresh-retry) against /api/v1/export/summary with the period query params"
    # UI-SPEC ## UI Considerations E4 (export dialog) + E7 — all `covered`:
    - "E4 loading ★: on submit the 'Export board report' CTA enters an inline loading state (spinner + 'Generating…' label, disabled to block double-submit); the dialog stays open until the file is ready or an error shows"
    - "E4 empty: dialog always has defaults (period=Last quarter, scheduling unchecked); no empty form state exists"
    - "E4 error: generation failure names the failure and offers 'retry with charts off (tables only)'; dialog remains open"
    - "E4 partial: chart-render partial failure degrades to tables-only via the same retry path"
    - "E4 long-text: recipient list / custom date values render in the existing ScheduledReport inline fields (mono for dates); no new long-text surface"
    - "E7 destructive confirm (stop scheduled report) is BUILT: unchecking an already-enabled board-report toggle opens the fixed two-button confirm (Cancel / Stop sending), {cadence} interpolated in mono, no typed-reason; confirming stops delivery via the existing reports API (DELETE /api/v1/reports/{id}), Cancel re-checks the toggle and makes no request"
  artifacts:
    - path: "frontend/src/components/dashboard/export-board-report-dialog.tsx"
      provides: "ResponsiveDialog with period presets + custom range + scheduling disclosure + auth'd blob download + Generating…/error states"
      min_lines: 60
  key_links:
    - from: "frontend/src/components/dashboard/export-board-report-dialog.tsx"
      to: "/api/v1/export/summary"
      via: "authenticated fetch → blob → download (period query params)"
      pattern: "export/summary"
  prohibitions:
    - "The scheduling disclosure must not visually compete with the primary gradient 'Export board report' CTA (btn-secondary weight)"
---

<objective>
RPT-01 frontend: the on-demand "Export board report" dialog (D-03 period selector + D-04 scheduling disclosure) that drives the Plan 02 export endpoint. This is the web-UI surface that the RPT-02 leadership lens CTA (Plan 04) opens. Includes a human-verify checkpoint that eyeballs the actual generated PDF (board-ready look, real chart images, branding, section order).

Purpose: Give the CISO a one-click, board-ready PDF with a familiar period picker and optional recurring email delivery.
Output: `export-board-report-dialog.tsx` + a human-verified board PDF.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-executive-compliance-reporting/43-CONTEXT.md
@.planning/phases/43-executive-compliance-reporting/43-UI-SPEC.md
@.planning/phases/43-executive-compliance-reporting/43-PATTERNS.md
@.claude/skills/sketch-findings-getvul/references/visual-language.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md

<interfaces>
Patterns to copy (read the analog files, do not paste full bodies here):
- frontend/src/components/exceptions/exception-grant-dialog.tsx (ResponsiveDialog wrapper + FIELD_CLASS/FIELD_LABEL_CLASS form idiom, L37-64)
- frontend/src/components/analytics/scope-window-controls.tsx (preset-toggle + custom-date-range reveal, L48-60; plain useState for custom from/to)
- frontend/src/components/ui/ExportButton.tsx (authenticated blob-download fetch + 401-refresh-retry, L16-83)
- frontend/src/components/settings/notifications-pane.tsx (checkbox-reveals-inline-fields disclosure precedent — open directly to match the scheduling-toggle shape)
Endpoint (Plan 02): GET /api/v1/export/summary?format=pdf&section=...&period=quarter (or &from=YYYY-MM-DD&to=YYYY-MM-DD)
Scheduled-report CRUD (pre-existing, reused by the scheduling disclosure + E7 stop-confirm — no new backend this phase): GET /api/v1/reports (list, seed toggle state), POST /api/v1/reports (enable), DELETE /api/v1/reports/{id} (E7 stop sending). See backend/app/main.py L502-534.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the export-board-report dialog (period presets + custom range + scheduling disclosure + states)</name>
  <files>frontend/src/components/dashboard/export-board-report-dialog.tsx, frontend/src/components/dashboard/export-board-report-dialog.test.tsx</files>
  <read_first>
    - .planning/phases/43-executive-compliance-reporting/43-UI-SPEC.md "Copywriting Contract" (Primary CTA 'Export board report', Error state 'retry with charts off (tables only)', Destructive confirmation copy), "RPT-01 — PDF Rendering Contract" (Period selector D-03 default Last quarter; Scheduling toggle D-04), "## UI Considerations" E4 + E7
    - .planning/phases/43-executive-compliance-reporting/43-PATTERNS.md "export-board-report-dialog.tsx" (analogs: exception-grant-dialog, scope-window-controls, ExportButton; the blob-download flow)
    - frontend/src/components/exceptions/exception-grant-dialog.tsx, frontend/src/components/analytics/scope-window-controls.tsx, frontend/src/components/ui/ExportButton.tsx, frontend/src/components/settings/notifications-pane.tsx
    - .claude/skills/sketch-findings-getvul/references/copy-voice.md (no 'Please', sentence case, mono for cadence/dates)
  </read_first>
  <behavior>
    - Test: dialog renders with default period preset = 'Last quarter' and scheduling toggle unchecked.
    - Test: selecting 'custom' reveals From/To native date inputs; submit is blocked / validated when To < From.
    - Test: on submit the CTA shows a disabled 'Generating…' spinner state (blocks double-submit) and the dialog stays open.
    - Test: checking 'Also send this report {cadence} by email' reveals inline cadence/recipients fields (single dialog).
    - Test: a generation error renders the name-the-failure copy with a 'retry with charts off' affordance and keeps the dialog open.
    - Test: with an existing board ScheduledReport (GET /api/v1/reports returns one), the dialog opens with the scheduling toggle CHECKED; unchecking it opens the E7 'Stop scheduled board report' confirm ({cadence} in mono, Cancel / Stop sending, no typed-reason).
    - Test: confirming 'Stop sending' calls DELETE /api/v1/reports/{id}; 'Cancel' re-checks the toggle and issues no request.
  </behavior>
  <action>
    Create export-board-report-dialog.tsx using ResponsiveDialog + the FIELD_CLASS/FIELD_LABEL_CLASS idiom. Period control mirrors scope-window-controls.tsx but with the D-03 preset set (`30d`/`90d`/`quarter`/`year`/`custom`) and default `quarter` ('Last quarter'); custom reveals native `<input type="date">` From/To (plain useState, no date-picker dep) with To>=From validation. Scheduling disclosure = a `btn-secondary`-weight checkbox 'Also send this report {cadence} by email' that reveals inline cadence (daily/weekly/monthly) + recipients fields — never a second dialog. On mount, fetch GET /api/v1/reports and seed the toggle CHECKED when a board ScheduledReport already exists (else unchecked). Checking an unchecked toggle → reveal fields (enable → POST /api/v1/reports on submit); UNCHECKING an already-enabled toggle → open the E7 destructive confirm 'Stop scheduled board report' (fixed two-button Cancel / Stop sending, {cadence} interpolated in mono, no typed-reason — UI-SPEC Destructive confirmation copy verbatim): 'Stop sending' → DELETE /api/v1/reports/{id}; 'Cancel' → re-check the toggle, no request. Submit reuses ExportButton's authenticated fetch→blob→download flow (401-refresh-retry) against `/api/v1/export/summary` with `format=pdf`, the board section keys, and the resolved period params; while in flight show the disabled 'Generating…' spinner (E4). On error, render the UI-SPEC error copy + a 'retry with charts off (tables only)' action that re-submits without chart sections. Primary CTA is the gradient 'Export board report'; the scheduling disclosure must not visually compete with it. All copy verbatim from the UI-SPEC Copywriting Contract; mono for cadence/date values. Add the test file per <behavior>.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- export-board-report-dialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - dialog default period is 'Last quarter'; scheduling toggle unchecked by default
    - custom preset reveals From/To date inputs with To>=From validation
    - submit enters a disabled 'Generating…' state and keeps the dialog open until success/error
    - error state renders 'retry with charts off (tables only)'
    - unchecking an already-enabled scheduling toggle opens the E7 'Stop scheduled board report' confirm (Cancel / Stop sending, {cadence} in mono); 'Stop sending' calls DELETE /api/v1/reports/{id}, 'Cancel' re-checks and issues no request
    - export-board-report-dialog.tsx fetches '/api/v1/export/summary' with period params via the auth'd blob-download flow
    - npm run test -- export-board-report-dialog.test.tsx passes
  </acceptance_criteria>
  <done>The export dialog composes period selection, scheduling disclosure, and the auth'd PDF download with all E4/E7 states; tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the generated board PDF</name>
  <files>(none — visual verification of the generated PDF artifact)</files>
  <what-built>Plan 02's extended PDF (matplotlib chart sections) + this plan's export dialog. The dialog triggers a real PDF download.</what-built>
  <action>Human-only visual verification. Claude has already automated PDF generation (Plan 02) and the export dialog (Task 1). Follow the how-to-verify steps below, then pause for the resume-signal. Do NOT proceed until the user approves.</action>
  <read_first>
    - .planning/phases/43-executive-compliance-reporting/43-UI-SPEC.md "RPT-01 — PDF Rendering Contract" + E9
    - project memory getvul-local-e2e-perf-gate (running the local stack)
  </read_first>
  <how-to-verify>
    1. Start the local stack (admin user, prod build per project memory) and open /dashboard.
    2. Open the 'Export board report' dialog; leave the default 'Last quarter' period; click 'Export board report'.
    3. Confirm the CTA shows a 'Generating…' spinner, then a PDF downloads.
    4. Open the PDF and verify: (a) three NEW sections appear — risk trend, MTTR by tier, SLA compliance — after the summary-stats block and before top-hosts/top-remediations/tickets; (b) charts are real raster images on a white/branded surface (NOT dark-theme colors); trend line uses the tenant brand color, MTTR bars use light-mode severity hexes; (c) a zero-data metric reads 'Not yet measured', never 0/100%.
    5. (If a scheduled report can be exercised) check the 'Also send…' disclosure reveals cadence/recipients inline.
  </how-to-verify>
  <verify>Human confirms the PDF matches the RPT-01 PDF Rendering Contract; no automated command (visual artifact).</verify>
  <acceptance_criteria>
    - The PDF contains the three new chart-image sections in the specified order on the branded light surface
    - No dark-theme hex values appear in the chart images
    - Zero-data sections render 'Not yet measured', not a fabricated number
  </acceptance_criteria>
  <done>User approves the generated board PDF (or issues are logged and addressed before proceeding).</done>
  <resume-signal>Type "approved" or describe issues (e.g. wrong section order, dark colors, fabricated numbers)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser dialog → /api/v1/export/summary | Period/date-range chosen client-side; server (Plan 02) re-validates all bounds — the dialog is not the trust boundary |

## STRIDE Threat Register (ASVS L1, block on: high)
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-10 | Denial of Service | client custom range | mitigate | client validates To>=From, but the server-side span cap (Plan 02 T-43-05) is authoritative — the dialog never bypasses it |
| T-43-11 | Information Disclosure | blob download auth | mitigate | reuse ExportButton's Bearer-token fetch + 401-refresh-retry; no token in URL; download is the caller's own tenant PDF |
| T-43-12 | Spoofing | scheduled-report recipients | accept | recipients are configured by an authed user for their own tenant via the existing ScheduledReport path; no new trust surface |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)
- `frontend/src/components/dashboard/export-board-report-dialog.tsx` (new component; opened by the RPT-02 leadership lens CTA in Plan 04)
- `frontend/src/components/dashboard/export-board-report-dialog.test.tsx`
</artifacts_produced>

<verification>
- `npm run test -- export-board-report-dialog.test.tsx` green.
- Human-verify checkpoint approved (board PDF eyeballed).
</verification>

<success_criteria>
- RPT-01 fully shipped: the CISO can export a board-ready PDF for a chosen period and optionally schedule recurring delivery, verified visually.
</success_criteria>

<output>
After completion, create `.planning/phases/43-executive-compliance-reporting/43-03-SUMMARY.md`
</output>
