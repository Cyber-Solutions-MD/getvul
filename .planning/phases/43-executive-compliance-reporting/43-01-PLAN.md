---
phase: 43-executive-compliance-reporting
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified:
  - backend/app/vulnerabilities/sla_service.py
  - backend/app/compliance/__init__.py
  - backend/app/compliance/catalog.py
  - backend/app/compliance/service.py
  - backend/app/compliance/schemas.py
  - backend/app/compliance/router.py
  - backend/app/main.py
  - backend/tests/test_sla_service.py
  - backend/tests/test_compliance.py
  - frontend/src/lib/queries/keys.ts
  - frontend/src/lib/queries/use-compliance.ts
  - frontend/src/app/(authed)/dashboard/compliance/page.tsx
  - frontend/src/app/(authed)/dashboard/compliance/page.test.tsx
  - frontend/src/components/compliance/control-card.tsx
  - frontend/src/components/shell/nav-items.ts
autonomous: true
requirements: [RPT-03]
must_haves:
  truths:
    - "A CISO can open /dashboard/compliance and see per-framework control cards (SOC 2 / ISO 27001 / PCI DSS / NIST CSF) each with a deterministic pass/partial/fail/not-measured status"
    - "GET /api/v1/compliance/overview returns 200 with per-framework control-status rows, tenant-scoped, require_viewer-gated"
    - "A control whose evidencing metric has zero underlying data renders the neutral 'Not yet measured' state, never a fabricated pass or fail"
    - "Each of the ~5 underlying metrics is computed exactly once per request; the catalog is a pure function issuing zero additional queries"
    - "Control status is program-level (evidenced by posture metrics), never per-CVE tagged"
    # UI-SPEC ## UI Considerations (all `covered`) — RPT-03 surfaces E2/E3/E5:
    - "Compliance page loading state renders skeleton control cards (.skeleton/.skel-pill shimmer), never a spinner-only page (E2 loading)"
    - "Compliance page empty state is a two-branch zero-denominator full-page centered card whose branch is selected from useCoverageSummary(): when !has_scanner_connector render the no-scanner branch ('Connect a scanner' → /dashboard/connectors); otherwise render the no-SLA-policy branch ('Configure SLA policy' → /dashboard/settings SLA pane); cause-specific copy per D-11/UI-SPEC — NO backend schema change (E2/E5 empty)"
    - "Compliance page error state is an amber inline banner; framework cards for metrics that DID resolve still render — page is never blanked for one failed metric source (E2 partial/error)"
    - "Populated state is a grid of control cards grouped by framework, 2-col desktop / 1-col mobile (E2 populated)"
    - "Overflow: many controls scroll normally; grid reflows to 1-col on mobile; no fixed-height clipping (E2 overflow)"
    - "zero-one-many: framework chip bar + per-framework grouping reads identically at 1 or many frameworks; All chip is default (E2 zero-one-many)"
    - "long-text: control name and evidencing-metric line wrap freely; card grows to fit (grid row aligns to tallest card); no hidden/clipped text (E2/E3 long-text ★)"
    - "Control card 'Not yet measured' neutral status (faint text on --color-surface-2, dashed border) IS the zero-denominator empty state — mandatory, never rendered as fail (E3 empty)"
    - "Control card populated: control ID in mono + name + evidencing line + status pill with leading dot (E3 populated)"
  artifacts:
    - path: "backend/app/compliance/catalog.py"
      provides: "ControlDef frozen dataclass + CATALOG list (all 4 frameworks) + evaluate_catalog() pure function with None->not_measured short-circuit"
      contains: "def evaluate_catalog"
    - path: "backend/app/compliance/service.py"
      provides: "get_compliance_overview() — compute ~5 metrics once (zero-denom guarded), evaluate catalog"
      contains: "async def get_compliance_overview"
    - path: "backend/app/compliance/router.py"
      provides: "GET /overview, require_viewer, tenant-scoped"
      contains: "require_viewer"
    - path: "backend/app/compliance/schemas.py"
      provides: "ComplianceOverviewResponse + ControlStatusResponse (status Literal pass/partial/fail/not_measured)"
    - path: "frontend/src/app/(authed)/dashboard/compliance/page.tsx"
      provides: "Compliance page: ErrorBoundary>Suspense>Inner + loading/two-branch-empty/error/populated states + framework chip bar + control-card grid"
      min_lines: 40
    - path: "frontend/src/components/compliance/control-card.tsx"
      provides: "Presentation-only control card (never re-derives status)"
    - path: "frontend/src/lib/queries/use-compliance.ts"
      provides: "useComplianceOverview() hook + ControlStatus/ComplianceOverviewResponse types"
  key_links:
    - from: "backend/app/compliance/service.py"
      to: "backend/app/vulnerabilities/sla_service.py::get_sla_metrics"
      via: "direct call with exclude_exceptions=True + remediated_total==0 -> None guard"
      pattern: "get_sla_metrics.*exclude_exceptions=True"
    - from: "backend/app/compliance/service.py"
      to: "backend/app/compliance/catalog.py::evaluate_catalog"
      via: "pass metrics dict to pure evaluator"
      pattern: "evaluate_catalog\\("
    - from: "frontend/src/app/(authed)/dashboard/compliance/page.tsx"
      to: "/api/v1/compliance/overview"
      via: "useComplianceOverview()"
      pattern: "useComplianceOverview"
    - from: "backend/app/main.py"
      to: "backend/app/compliance/router.py"
      via: "app.include_router(compliance_router, prefix='/api/v1/compliance')"
      pattern: "compliance_router"
  prohibitions:
    - "The compliance view must not present a control as PASS when its underlying metric has zero data (fake-100 landmine — a PASS with no remediation history is a lie to an auditor)"
    - "The compliance view must not claim per-CVE / per-finding framework-control coverage — D-08 is program-level evidence only"
---

<objective>
Tracer slice for the whole phase: prove the RPT-03 compliance vertical end-to-end — a new `compliance/` backend package (curated framework-control catalog + compute-once metric service + tenant-scoped read endpoint) rendered as a new `/dashboard/compliance` page. This slice threads two of the three RESEARCH.md landmines (Pitfall 1 fake-100 on zero data; Pitfall 2 missing exception-exclusion) and establishes the `/api/v1/compliance/overview` endpoint that RPT-02's framework-posture strip (Plan 04) later consumes.

Purpose: A CISO/compliance owner can see, per named framework, whether the vuln-management program's posture satisfies each mapped control — deterministic, explainable, honest about missing data.
Output: `backend/app/compliance/` package, an additively-extended `get_sla_metrics`, and the `/dashboard/compliance` page + nav entry.
</objective>

<tracer_note>
This is the phase's TRACER (verified before expansion). It is a complete, production-quality vertical of RPT-03 — full 4-framework catalog, real metrics, all mandatory states, all landmine guards. Waves 2-4 (RPT-01 PDF, RPT-02 lenses) depend on this plan's endpoint and its `exclude_exceptions` extension. Do not stub — every metric and state must be real.
</tracer_note>

<assumption_delta_decision>
Detector fires: RPT-03 introduces a second compliance surface alongside the existing CSPM `get_compliance_dashboard`. Per CONTEXT D-10 this is an ACCEPTED, deliberate add-alongside decision (vuln-compliance stays SEPARATE from CSPM compliance; a unified rollup is explicitly deferred). Not re-opened. The new package is `app/compliance/` and never imports or merges `app/cspm/service.py`.
</assumption_delta_decision>

<edge_coverage_assumptions>
The spec-less edge probe returned RPT-03 as `unclassified/unresolved` (the deterministic classifier could not map the one-line requirement to a known edge family). FLAGGED ASSUMPTION (planner-derived, not auto-backstopped): RPT-03's real edge coverage is the E2/E3/E5 UI-SPEC considerations authored into `must_haves.truths` above (loading skeleton, two-branch zero-denominator empty, amber partial-error with resolved cards still rendering, long-text wrap, not-measured-never-fail) plus the zero-denominator/exception-exclusion prohibitions. If any edge beyond these surfaces during execution, flag it rather than silently handling it.
</edge_coverage_assumptions>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-executive-compliance-reporting/43-CONTEXT.md
@.planning/phases/43-executive-compliance-reporting/43-RESEARCH.md
@.planning/phases/43-executive-compliance-reporting/43-PATTERNS.md
@.planning/phases/43-executive-compliance-reporting/43-UI-SPEC.md
@.claude/skills/sketch-findings-getvul/references/page-layouts.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md
@.claude/skills/sketch-findings-getvul/references/visual-language.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md

<interfaces>
Existing read routes the compliance service reuses directly (no HTTP round-trip — import the service functions):
- backend/app/coverage/service.py::get_coverage_summary(db, tenant_id) -> object with .coverage_pct (int|None, None on zero denom), .has_scanner_connector, .has_authoritative_inventory
- backend/app/vulnerabilities/sla_service.py::get_sla_metrics(db, tenant_id, *, severity=None, exclude_exceptions=False) -> dict incl "compliance_pct", "remediated_total"  (extended in Task 1)
- backend/app/vulnerabilities/service.py::get_mttr_by_tier(db, tenant_id) -> list[dict] (per-tier avg_seconds: None on empty tier)
- backend/app/analytics/service.py::get_aging_distribution(db, tenant_id, now=...) -> per-severity within_sla/recently_breached/long_overdue buckets (already exception-excluded)
- backend/app/vulnerabilities/sla_tier_service.py::get_tier_policy(...) -> tier_days mapping (for PR.PS-02 tenant-calibrated threshold)

RBAC/router shape to copy verbatim: backend/app/coverage/router.py (require_viewer + DBSession + CurrentUser + inline user.tenant_id).
Frontend hook/page shape to copy: frontend/src/lib/queries/use-coverage-summary.ts, frontend/src/app/(authed)/dashboard/coverage/page.tsx, frontend/src/app/(authed)/dashboard/analytics/page.tsx.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additively extend get_sla_metrics with severity + exclude_exceptions (landmine 1+2 guards)</name>
  <files>backend/app/vulnerabilities/sla_service.py, backend/tests/test_sla_service.py</files>
  <read_first>
    - backend/app/vulnerabilities/sla_service.py (get_sla_metrics, L118-228 — note L191 `compliance_pct = ... if remediated_total > 0 else 100.0` fake-100; and that NO query excludes active exceptions)
    - backend/app/analytics/service.py::_open_backlog_conditions (L188-208 — the exact `~active_exception_subquery(tenant_id, now)` predicate shape to port in)
    - backend/app/exceptions/service.py::active_exception_subquery (the predicate)
    - .planning/phases/43-executive-compliance-reporting/43-RESEARCH.md Common Pitfalls #1 and #2; 43-PATTERNS.md "sla_service.py::get_sla_metrics (extend, additive)"
    - backend/tests/test_sla_service.py (existing coverage + fixture/seed style)
    - project memory getvul-backend-pytest-env (ENCRYPTION_KEY/JWT_SECRET_KEY, per-file run)
  </read_first>
  <behavior>
    - Test: get_sla_metrics with default args returns byte-identical results to today (existing call sites unaffected).
    - Test: exclude_exceptions=True drops a finding under an active accepted-risk exception from breached/at_risk counts (Pitfall 2).
    - Test: severity="critical" scopes the open-backlog conditions to critical only.
    - Test (regression for Pitfall 1 consumption): a tenant with remediated_total==0 still returns compliance_pct==100.0 from this function (documented existing behavior) BUT remediated_total==0 is present in the return dict so callers can detect "not measured".
  </behavior>
  <action>
    Add two keyword-only optional params to get_sla_metrics: `severity: str | None = None` and `exclude_exceptions: bool = False`, both with defaults that preserve exact current behavior. When `severity` is set, append `Vulnerability.severity == severity` to the open-backlog conditions; when `exclude_exceptions` is True, append `~active_exception_subquery(tenant_id, now)` (import from app.exceptions.service, mirroring analytics/service.py::_open_backlog_conditions verbatim). Do NOT change the compliance_pct fake-100 fallback here (it is guarded at the call site in Task 2 via remediated_total) — this keeps the function additive per D-01a "never re-derive". Keep `remediated_total` in the return dict. Add regression tests per <behavior> to test_sla_service.py using the existing seed helpers.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test .venv/bin/pytest tests/test_sla_service.py -x</automated>
  </verify>
  <acceptance_criteria>
    - sla_service.py get_sla_metrics signature contains `exclude_exceptions: bool = False` and `severity: str | None = None`
    - test_sla_service.py contains a test asserting an actively-excepted finding is excluded when exclude_exceptions=True and included when False
    - pytest tests/test_sla_service.py exits 0
    - Every pre-existing get_sla_metrics call site (router.py sla/metrics) is unchanged (grep shows no new args at those sites)
  </acceptance_criteria>
  <done>get_sla_metrics accepts additive severity/exclude_exceptions params; exception-exclusion + severity scoping tested; existing behavior preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: compliance backend package — catalog + compute-once service + tenant-scoped endpoint</name>
  <files>backend/app/compliance/__init__.py, backend/app/compliance/catalog.py, backend/app/compliance/service.py, backend/app/compliance/schemas.py, backend/app/compliance/router.py, backend/app/main.py, backend/tests/test_compliance.py</files>
  <read_first>
    - .planning/phases/43-executive-compliance-reporting/43-PATTERNS.md sections "compliance/catalog.py", "compliance/service.py", "compliance/schemas.py", "compliance/router.py", "main.py (register the new router)" — these contain the exact code shapes and the full CATALOG rows
    - .planning/phases/43-executive-compliance-reporting/43-RESEARCH.md Code Examples "D-09: The authoritative framework-control catalog" (the full control table: control IDs, paraphrased-vs-verbatim reproduction rule, metric_key, thresholds, PR.PS-02 tenant-calibrated note) + Pattern 2 + Pattern 3
    - backend/app/coverage/service.py::get_coverage_summary (L169-215 — zero-denominator discipline: `= round(...) if total else None`)
    - backend/app/coverage/schemas.py (ConfigDict(from_attributes=True) response-envelope convention)
    - backend/app/coverage/router.py (full — require_viewer + DBSession + CurrentUser inline tenant_id)
    - backend/app/main.py (existing coverage_router/analytics_router import + include_router lines, ~L35 and ~L324)
    - backend/tests/test_coverage.py (tenant-isolation + zero-denominator test shape to mirror)
  </read_first>
  <behavior>
    - Test: evaluate_catalog with metrics[key]=None -> status "not_measured" for that control (never pass, never fail).
    - Test: boundary values — value at exactly the pass threshold -> "pass"; just below pass but >= partial -> "partial"; below partial -> "fail".
    - Test: boolean control (has_active_scanning) and tenant-calibrated control (mttr_by_tier / PR.PS-02) are special-cased, not forced through the numeric >= path.
    - Test: get_compliance_overview computes each underlying metric exactly once (assert via call-count spy or query-count) — the catalog loop issues zero queries.
    - Test: GET /api/v1/compliance/overview returns 200 for an authed viewer; a user from tenant B cannot see tenant A's data (mirror test_tenant_isolation pattern); require_viewer is enforced.
    - Test: sla_compliance_pct is None (=> ID.RA-06 not_measured) when get_sla_metrics returns remediated_total==0 (Pitfall 1 guard at the call site).
  </behavior>
  <action>
    Create the `app/compliance/` package. catalog.py: `@dataclass(frozen=True) class ControlDef(framework, control_id, title, metric_key, thresholds)` + the full `CATALOG` list (all 4 frameworks — use the 43-PATTERNS.md/43-RESEARCH.md rows verbatim: SOC2 CC7.1; ISO A.8.8, A.8.9; PCI 6.3.1, 6.3.3, 11.3.1, 11.3.1.1; NIST ID.RA-01, ID.RA-06, PR.PS-02) + `evaluate_catalog(metrics: dict[str, float|None]) -> list[dict]` that short-circuits `None -> "not_measured"` BEFORE any threshold compare, and special-cases the boolean control (`has_active_scanning`) and the tenant-calibrated `mttr_by_tier`/PR.PS-02 control. Cite control IDs + paraphrased intent only for SOC2/ISO/PCI (copyrighted); NIST CSF 2.0 text may be verbatim. service.py: `get_compliance_overview(db, tenant_id)` computes the ~5 metrics ONCE — coverage_pct + has_scanner_connector via get_coverage_summary; sla_compliance_pct via get_sla_metrics(..., exclude_exceptions=True) guarded so `remediated_total==0 -> None` (Pitfall 1); critical_sla_health_pct derived from get_aging_distribution buckets with a zero-denom guard (0-of-0 tier -> None); mttr_by_tier via get_mttr_by_tier; then `evaluate_catalog(metrics)`. schemas.py: ComplianceOverviewResponse{controls: list[ControlStatusResponse]}, each row {framework, control_id, title, metric_key, value: float|None, status: Literal["pass","partial","fail","not_measured"]}. router.py: `GET /overview` copying coverage/router.py exactly (require_viewer, inline user.tenant_id). main.py: import + `app.include_router(compliance_router, prefix="/api/v1/compliance", tags=["Compliance"])`. Add tests per <behavior>.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test .venv/bin/pytest tests/test_compliance.py -x</automated>
  </verify>
  <acceptance_criteria>
    - catalog.py contains `def evaluate_catalog` and a CATALOG with >=1 control for each of soc2/iso27001/pci_dss/nist_csf
    - evaluate_catalog returns status "not_measured" (never "pass"/"fail") when a metric value is None
    - service.py get_compliance_overview calls get_sla_metrics with exclude_exceptions=True and guards remediated_total==0 -> None
    - GET /api/v1/compliance/overview is registered under /api/v1/compliance and gated by require_viewer; cross-tenant read is blocked
    - pytest tests/test_compliance.py exits 0
  </acceptance_criteria>
  <done>Compliance package returns per-framework control statuses from a compute-once service over a pure catalog, tenant-scoped, with zero-data honesty; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: /dashboard/compliance page + control card + hook + nav entry</name>
  <files>frontend/src/lib/queries/keys.ts, frontend/src/lib/queries/use-compliance.ts, frontend/src/app/(authed)/dashboard/compliance/page.tsx, frontend/src/app/(authed)/dashboard/compliance/page.test.tsx, frontend/src/components/compliance/control-card.tsx, frontend/src/components/shell/nav-items.ts</files>
  <read_first>
    - .planning/phases/43-executive-compliance-reporting/43-UI-SPEC.md sections "RPT-03 — /dashboard/compliance page", "Color" (four-state pass/partial/fail/not-measured tokens + mandatory not-measured), "Copywriting Contract" (empty two-branch + error copy), "## UI Considerations" E2/E3/E5
    - .planning/phases/43-executive-compliance-reporting/43-PATTERNS.md sections "compliance/page.tsx", "use-compliance.ts", "control-card.tsx", "nav-items.ts"
    - frontend/src/app/(authed)/dashboard/coverage/page.tsx (multi-branch empty-state precedent) and analytics/page.tsx (ErrorBoundary>Suspense>Inner shell, L219-229)
    - frontend/src/lib/queries/use-coverage-summary.ts — CONSUMED here as a second data source (RESEARCH.md Pattern 3): its response exposes `has_scanner_connector` / `has_authoritative_inventory`, which drive the two-branch empty-state selection. Also the hook-shape model for use-compliance.ts. keys.ts (key-factory convention; note existing cspm-nested `compliance` at L87 — the NEW top-level key must not collide)
    - frontend/src/components/coverage/coverage-connector-card.tsx (card w/ status pill + metric line)
    - frontend/src/components/ui/ChipBar.tsx (framework chip bar chrome)
    - frontend/src/components/shell/nav-items.ts (WORKFLOW_ITEMS array; Coverage/Analytics entries L53-59)
    - .claude/skills/sketch-findings-getvul/references/state-patterns.md, visual-language.md, copy-voice.md
  </read_first>
  <action>
    Add `compliance: { all: ['compliance'] as const, overview: () => ['compliance','overview'] as const }` to keys.ts (top-level factory, distinct from the cspm-nested one). Create use-compliance.ts mirroring use-coverage-summary.ts (no-arg useQuery, staleTime:0, retry:1) exporting `useComplianceOverview()` + `ControlStatus`/`ComplianceOverviewResponse` types (status union 'pass'|'partial'|'fail'|'not_measured'). Create control-card.tsx (presentation-only — control_id in JetBrains Mono, small colored framework glyph per provider-mark visual language, status pill dot+label using the four-state tokens `--color-success`/`--color-warning`/`--color-danger`/`--color-text-faint`+dashed for not_measured, evidencing-metric line in Body with value in mono; wraps freely, card grows — never clip; never re-derive status client-side). Create page.tsx: ErrorBoundary>Suspense>Inner (copy analytics/page.tsx shell) with branch order error(amber PartialFailureBanner) > loading(skeleton control cards) > empty(two-branch — the page ALSO calls useCoverageSummary() alongside useComplianceOverview() and selects the branch from the coverage-summary signal (RESEARCH.md Pattern 3): `!has_scanner_connector` → the no-scanner branch (UI-SPEC 'Connect a scanner' copy, deep-link /dashboard/connectors); otherwise → the no-SLA-policy branch (UI-SPEC 'Configure SLA policy' copy, deep-link /dashboard/settings SLA pane). This needs NO change to ComplianceOverviewResponse — the empty condition itself is 'overview has zero controls OR every control is not_measured', and the coverage-summary picks WHICH cause-specific branch to show) > populated(framework chip bar All/SOC2/ISO27001/PCI DSS/NIST CSF + control-card grid grouped by framework, 2-col desktop/1-col mobile). Use UI-SPEC empty/error copy verbatim; no generic SaaS copy. Add the `Compliance` entry to WORKFLOW_ITEMS after Analytics (icon ShieldCheck from lucide, href /dashboard/compliance, no chip). Add page.test.tsx covering the four state branches + branch order.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- compliance/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - use-compliance.ts exports useComplianceOverview hitting '/api/v1/compliance/overview'
    - control-card.tsx renders the not_measured neutral state (faint text + dashed border) and never renders fail for a null value
    - nav-items.ts WORKFLOW_ITEMS contains `{ label: 'Compliance', href: '/dashboard/compliance', ... }`
    - page.tsx renders loading skeleton, two-branch empty, amber error, and populated grid grouped by framework
    - page.test.tsx asserts BOTH empty branches render for the CORRECT root cause: with useCoverageSummary()→has_scanner_connector=false the no-scanner 'Connect a scanner' branch renders; with has_scanner_connector=true (no SLA policy) the 'Configure SLA policy' branch renders — not merely that 'an empty state' renders
    - npm run test -- compliance/page.test.tsx passes
  </acceptance_criteria>
  <done>Compliance page renders per-framework control cards with all mandatory states and the new nav entry; consuming the tenant-scoped endpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → /api/v1/compliance/overview | Authenticated viewer requests own-tenant posture; must never read another tenant's findings/metrics |
| compliance/service.py → underlying services | Reuses tenant-scoped read functions; must pass the caller's tenant_id, never a client-supplied one |

## STRIDE Threat Register (ASVS L1, block on: high)
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information Disclosure | GET /api/v1/compliance/overview | mitigate | require_viewer dependency + inline `user.tenant_id` passed to every service call; copy coverage/router.py shape exactly; test cross-tenant read is blocked (test_compliance.py) — HIGH, blocking |
| T-43-02 | Tampering-by-omission (data integrity) | evaluate_catalog / service metric guards | mitigate | zero-denominator -> None -> not_measured; get_sla_metrics(exclude_exceptions=True) + remediated_total==0 guard; a fabricated PASS on a board/auditor surface is treated as HIGH |
| T-43-03 | Spoofing | require_viewer auth | accept | existing auth stack (JWT/session) unchanged; no new auth surface |
| T-43-04 | Elevation of Privilege | lens/role confusion | accept | endpoint is read-only viewer-gated; no role tier is elevated; catalog is static in-code data (no injection surface) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)
New symbols/files (exclude from source-drift verification):
- `backend/app/compliance/` package (`__init__.py`, `catalog.py`, `service.py`, `schemas.py`, `router.py`)
- `backend/app/compliance/catalog.py`: `ControlDef` dataclass, `CATALOG` list, `evaluate_catalog()` function
- `backend/app/compliance/service.py`: `get_compliance_overview()` (+ any private metric-derivation helpers, e.g. `_critical_health_from_aging`)
- `backend/app/compliance/schemas.py`: `ComplianceOverviewResponse`, `ControlStatusResponse`
- `backend/app/compliance/router.py`: `router` (GET /overview), registered in main.py as `compliance_router` at `/api/v1/compliance`
- Additive params on existing `backend/app/vulnerabilities/sla_service.py::get_sla_metrics`: `severity`, `exclude_exceptions`
- `frontend/src/lib/queries/use-compliance.ts`: `useComplianceOverview()`, `ControlStatus`, `ComplianceOverviewResponse` types
- `frontend/src/app/(authed)/dashboard/compliance/page.tsx`, `frontend/src/components/compliance/control-card.tsx`
- New `compliance` query-key factory in `keys.ts`; new `Compliance` `nav-items.ts` WORKFLOW_ITEMS entry
</artifacts_produced>

<verification>
- Backend: `pytest tests/test_sla_service.py tests/test_compliance.py -x` green (ENCRYPTION_KEY/JWT_SECRET_KEY set, per project memory).
- Frontend: `npm run test -- compliance/page.test.tsx` green.
- Manual smoke (optional): GET /api/v1/compliance/overview returns per-framework control rows; a fresh tenant with no data shows all not_measured (no fabricated pass).
</verification>

<success_criteria>
- RPT-03 satisfied end-to-end: a compliance view maps program posture to SOC 2 / ISO 27001 / PCI DSS / NIST CSF controls with deterministic status.
- Landmines 1 (fake-100) and 2 (exception-exclusion) guarded and tested.
- `/api/v1/compliance/overview` available for Plan 04's framework-posture strip.
</success_criteria>

<output>
After completion, create `.planning/phases/43-executive-compliance-reporting/43-01-SUMMARY.md`
</output>
