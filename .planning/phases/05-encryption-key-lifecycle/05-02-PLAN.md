---
phase: 05-encryption-key-lifecycle
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - backend/app/main.py
  - backend/tests/test_encryption_rotation.py
  - docs/16-security.md
autonomous: true
requirements: [PROD-05-01, PROD-05-03, PROD-05-04]
tags: [encryption, startup-check, structlog, runbook, backend, docs]

must_haves:
  truths:
    - "Backend startup hard-fails (refuses to boot) in production when ENCRYPTION_KEY is unset, the placeholder, or an invalid Fernet key"
    - "Backend startup hard-fails in production when JWT_SECRET_KEY is still the CHANGE-ME-IN-PRODUCTION placeholder"
    - "Backend startup logs a loud structlog warning (but continues) for the same conditions in development"
    - "docs/16-security.md has an 'Encryption Key Backup & Rotation' section with concrete commands, an RTO statement, off-box vault storage guidance, lost-key recovery steps, and the single-global-key model documented"
  artifacts:
    - path: "backend/app/main.py"
      provides: "_check_secrets_at_startup() invoked at top of lifespan; hard-fail in prod, warn in dev"
      contains: "_check_secrets_at_startup"
    - path: "docs/16-security.md"
      provides: "Encryption Key Backup & Rotation runbook (D-13..D-17)"
      contains: "Encryption Key Backup & Rotation"
    - path: "backend/tests/test_encryption_rotation.py"
      provides: "Startup-check unit tests for encryption key + JWT placeholder, dev-warn vs prod-raise"
      contains: "def test_startup_check_encryption"
  key_links:
    - from: "backend/app/main.py lifespan"
      to: "_check_secrets_at_startup()"
      via: "called at top of lifespan body before scheduler start"
      pattern: "_check_secrets_at_startup\\(\\)"
    - from: "_check_secrets_at_startup()"
      to: "settings.encryption_key / settings.jwt_secret_key"
      via: "placeholder compare + Fernet(key) validity check + environment gate"
      pattern: "settings\\.environment == \"production\""
---

<objective>
Deliver the two operator-facing safety nets: (1) a startup secrets check in the `main.py`
`lifespan` that hard-fails in production and warns in development when `ENCRYPTION_KEY` or
`JWT_SECRET_KEY` is a placeholder / unset / invalid; and (2) the "Encryption Key Backup &
Rotation" runbook in `docs/16-security.md`.

Purpose: PROD-05-04 (operator alert on placeholder/missing key), PROD-05-01 (documented
backup procedure with RTO), and the runbook half of PROD-05-03 (documents the `python -m
app.encryption` invocation delivered in Plan 01).

Output:
- `backend/app/main.py` — `_check_secrets_at_startup()` + lifespan wiring.
- `docs/16-security.md` — new "Encryption Key Backup & Rotation" section.
- `backend/tests/test_encryption_rotation.py` — appended startup-check unit tests.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-encryption-key-lifecycle/05-CONTEXT.md
@.planning/phases/05-encryption-key-lifecycle/05-RESEARCH.md
@.planning/phases/05-encryption-key-lifecycle/05-VALIDATION.md
@.planning/phases/05-encryption-key-lifecycle/05-01-PLAN.md

<interfaces>
<!-- Contracts extracted from the codebase. Use these directly. -->

From backend/app/main.py:33-43 — module-level structlog logger + lifespan head (insert the check at the TOP of the lifespan body, BEFORE the scheduler start):
```python
logger = structlog.get_logger()

@asynccontextmanager
async def lifespan(app: FastAPI):
    """Startup and shutdown events."""
    # <-- INSERT _check_secrets_at_startup() HERE (before scheduler start)
    if settings.environment in ("development", "production"):
        from app.connectors.scheduler import start_scheduler
        start_scheduler()
    app.state.redis = redis.from_url(...)
    ...
    yield
    ...
```

From backend/app/config.py — exact literals to compare against (copy verbatim):
```python
environment: str = "production"                                  # line 11 — default is production (safe default)
jwt_secret_key: str = "CHANGE-ME-IN-PRODUCTION"                  # line 16
encryption_key: str = "CHANGE-ME-generate-with-python-c-from-cryptography.fernet-import-Fernet-Fernet.generate_key"  # line 22
```

From cryptography:
```python
from cryptography.fernet import Fernet   # Fernet(key.encode()) raises ValueError for any invalid key form
```

From backend/tests/conftest.py — patching settings in tests (mirror the redis_test_url pattern):
```python
# monkeypatch.setattr on app.config.settings attributes, or monkeypatch.setattr(app.main.settings, "environment", "production")
```

From docs/16-security.md — existing structure (append the new section; the file already has a "## Credential Encryption" section at line 134 and "## Audit Logging" at 141 — put the runbook AFTER "## Credential Encryption"):
```
## Credential Encryption   (line 134)
## Audit Logging           (line 141)
```
NOTE: line 136 currently claims "SMTP passwords encrypted with Fernet" — research §Open Questions found only connector credentials use Fernet. The runbook must state the single ENCRYPTION_KEY covers CONNECTOR credentials specifically.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _check_secrets_at_startup() to main.py, wire into lifespan, and unit-test it</name>
  <read_first>
    - backend/app/main.py:1-90 (imports, logger, lifespan)
    - backend/app/config.py (exact placeholder literals + environment default)
    - backend/tests/conftest.py (settings-patching pattern; _reset_engine_pool autouse)
    - .planning/phases/05-encryption-key-lifecycle/05-RESEARCH.md §5 (Startup Check) + Pitfall 4
    - .planning/phases/05-encryption-key-lifecycle/05-CONTEXT.md D-10, D-11, D-12
  </read_first>
  <behavior>
    - Test test_startup_check_encryption_placeholder_dev: with settings.environment="development" and encryption_key set to the placeholder literal, `_check_secrets_at_startup()` returns a non-empty issues list and does NOT raise.
    - Test test_startup_check_encryption_placeholder_prod: with environment="production" and placeholder encryption_key, `_check_secrets_at_startup()` raises RuntimeError.
    - Test test_startup_check_encryption_invalid_prod: environment="production", encryption_key="short-not-fernet" → raises RuntimeError (Fernet(key) ValueError path).
    - Test test_startup_check_valid_key_ok: environment="production", a real `generate_key()` value for encryption_key AND a non-placeholder jwt_secret_key → returns [] (empty) and does NOT raise.
    - Test test_startup_check_jwt_placeholder_prod: environment="production", valid encryption_key but jwt_secret_key=="CHANGE-ME-IN-PRODUCTION" → raises RuntimeError.
    - Test test_startup_check_jwt_placeholder_dev: environment="development", jwt placeholder → returns non-empty issues, no raise.
  </behavior>
  <action>
    In backend/app/main.py, add module-level placeholder constants copied VERBATIM from config.py:

    ```python
    ENCRYPTION_KEY_PLACEHOLDER = (
        "CHANGE-ME-generate-with-python-c-from-cryptography.fernet-import-Fernet-Fernet.generate_key"
    )
    JWT_SECRET_PLACEHOLDER = "CHANGE-ME-IN-PRODUCTION"
    ```

    Add `_check_secrets_at_startup() -> list[str]` (import `from cryptography.fernet import Fernet`):
    - Build an `issues: list[str]` (D-11 conditions):
      - encryption_key: if `not settings.encryption_key` OR `== ENCRYPTION_KEY_PLACEHOLDER` → append "ENCRYPTION_KEY is unset or uses the default placeholder". Else `try: Fernet(settings.encryption_key.encode()) except ValueError: append "ENCRYPTION_KEY is set but is not a valid Fernet key"`.
      - jwt_secret_key (D-12): if `settings.jwt_secret_key == JWT_SECRET_PLACEHOLDER` → append "JWT_SECRET_KEY uses the default placeholder".
    - Log each issue: `logger.critical("startup_secret_check_failed", issue=msg)` when `settings.environment == "production"`, else `logger.warning("startup_secret_check_warning", issue=msg)`. NEVER log the key value itself — only the issue string.
    - D-10 hard-fail: if `issues and settings.environment == "production"`: `raise RuntimeError("Backend refused to start: insecure secrets detected. Set ENCRYPTION_KEY and JWT_SECRET_KEY to non-placeholder values.")`.
    - Return `issues` (so tests can assert dev-mode behavior without a raise).

    Wire it into `lifespan`: call `_check_secrets_at_startup()` as the FIRST statement inside the
    lifespan body (before the `if settings.environment in (...)` scheduler block). A prod RuntimeError
    here propagates through FastAPI startup and prevents uvicorn serving traffic (RESEARCH §5).

    IMPORTANT (Pitfall 4): the check MUST run only inside `lifespan`, not at import time — the test
    suite calls `create_app()` and CI does not set ENVIRONMENT, so an import-time check on the
    default "production" would break collection. Keep it in the lifespan body only.

    Append the six startup-check tests from <behavior> to backend/tests/test_encryption_rotation.py.
    Import `from app.main import _check_secrets_at_startup`. Patch settings with monkeypatch:
    `monkeypatch.setattr("app.main.settings.environment", ...)` etc., or
    `monkeypatch.setattr(main.settings, "encryption_key", ...)`. These tests need NO DB — they must
    not depend on db_session (fast unit tests, Pitfall 4).
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_encryption_rotation.py -k "startup_check" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def _check_secrets_at_startup" backend/app/main.py` succeeds
    - `grep -q "_check_secrets_at_startup()" backend/app/main.py` succeeds inside lifespan (called, not just defined)
    - `grep -q "CHANGE-ME-generate-with-python" backend/app/main.py` succeeds (encryption placeholder literal present)
    - `grep -q "CHANGE-ME-IN-PRODUCTION" backend/app/main.py` succeeds (JWT placeholder literal present)
    - `grep -q 'settings.environment == "production"' backend/app/main.py` succeeds (prod gate)
    - `grep -q "raise RuntimeError" backend/app/main.py` succeeds (hard-fail path)
    - `cd backend && pytest tests/test_encryption_rotation.py -k "startup_check" -x -q` passes (all six)
    - `cd backend && pytest tests/ -q --tb=short` — full suite still green (no import-time regression)
  </acceptance_criteria>
  <done>_check_secrets_at_startup validates encryption + JWT keys (placeholder/unset/invalid), warns in dev and raises in prod, runs at the top of lifespan (not import time), logs no key material, and all six startup-check unit tests pass without regressing the suite.</done>
</task>

<task type="auto">
  <name>Task 2: Write the "Encryption Key Backup & Rotation" runbook in docs/16-security.md</name>
  <read_first>
    - docs/16-security.md:134-168 ("Credential Encryption" + "Audit Logging" sections)
    - .planning/phases/05-encryption-key-lifecycle/05-01-PLAN.md (exact CLI commands delivered: generate-key / rotate --new-key / verify)
    - .planning/phases/05-encryption-key-lifecycle/05-CONTEXT.md D-13..D-17
    - .planning/phases/05-encryption-key-lifecycle/05-RESEARCH.md §Open Questions (SMTP scope, zero-tenant edge)
  </read_first>
  <action>
    Insert a new section titled exactly `## Encryption Key Backup & Rotation` (D-13) into
    docs/16-security.md, immediately AFTER the existing "## Credential Encryption" section (line 134
    block) and BEFORE "## Audit Logging". Use `docs/16-security.md` — NOT `doc/security.md` (that path
    does not exist; the roadmap/requirements reference is stale drift). Content MUST cover:

    - **Single-key model (D-17):** State plainly that one global `ENCRYPTION_KEY` (a Fernet key)
      encrypts every tenant's CONNECTOR credentials — rotation affects all tenants at once and a lost
      key impacts everyone. Note (per RESEARCH §Open Questions) that this scope is connector
      credentials in `connector_configs.credentials_secret_arn`; do not overstate SMTP coverage.

    - **Backup procedure (D-15, PROD-05-01):** Store the current `ENCRYPTION_KEY` in the org's
      existing secrets vault / password manager (1Password, HashiCorp Vault, or a cloud secrets
      manager) — off-box, access-controlled, NOT in the repo, NOT on the same VM as the DB, NOT in the
      DB backup. Name an owner: "the platform/security owner who owns the vault entry restores it."

    - **RTO statement (D-14, PROD-05-01):** "Recovery Time Objective: ≤ 15 minutes." Restore =
      retrieve the backed-up key from the vault, paste it into `.env` as `ENCRYPTION_KEY=<key>`, and
      restart the backend container. 15 min gives margin to locate the key. Honest for a config-file
      restore, not a data restore.

    - **Rotation runbook (PROD-05-02/03):** Concrete command sequence, exactly:
      ```bash
      # 1. Back up the CURRENT key to the vault + snapshot the DB first.
      # 2. Generate a new key:
      docker compose exec -T backend python3 -m app.encryption generate-key
      # 3. Dry-run to preview affected rows (writes nothing):
      docker compose exec -T backend python3 -m app.encryption rotate --new-key <NEW_KEY> --dry-run
      # 4. Rotate for real (re-encrypts all connector creds in one transaction, verifies round-trip):
      docker compose exec -T backend python3 -m app.encryption rotate --new-key <NEW_KEY>
      # 5. Set ENCRYPTION_KEY=<NEW_KEY> in .env and restart the backend:
      #    (the tool does NOT edit .env for you)
      docker compose restart backend
      # 6. Confirm:
      docker compose exec -T backend python3 -m app.encryption verify
      ```
      State that rotation is a single atomic transaction that aborts and rolls back on any failure
      (no mixed-key state), and that the tool prints a confirmation prompt (skippable with `--yes`).
      Note the operational safety of `--new-key` (T-05-03): prefer generating/pasting the key in a
      shell whose history is cleared, or pass via an environment variable, since a CLI arg can appear
      in `ps aux` on a shared VM.

    - **Lost-key recovery (D-16):** State plainly that connector credentials are cryptographically
      UNRECOVERABLE without the key. Recovery steps: (1) generate a fresh key with
      `python -m app.encryption generate-key`, (2) set it in `.env` and restart, (3) re-enter each
      connector's credentials through the Settings > Connectors UI (which re-encrypts under the new
      key). Existing ciphertexts stay unreadable and are overwritten on re-entry.
  </action>
  <verify>
    <automated>grep -q "Encryption Key Backup & Rotation" docs/16-security.md && grep -qi "RTO" docs/16-security.md && grep -q "python3 -m app.encryption rotate" docs/16-security.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## Encryption Key Backup & Rotation" docs/16-security.md` succeeds (D-13 exact title)
    - `grep -qi "RTO\|Recovery Time Objective" docs/16-security.md` succeeds (D-14/PROD-05-01)
    - `grep -qi "15 min" docs/16-security.md` succeeds (RTO value)
    - `grep -q "python3 -m app.encryption generate-key" docs/16-security.md` and `grep -q "python3 -m app.encryption rotate --new-key" docs/16-security.md` and `grep -q "python3 -m app.encryption verify" docs/16-security.md` all succeed
    - off-box vault storage present: `grep -qi "vault\|1Password\|secrets manager" docs/16-security.md`
    - lost-key recovery present: `grep -qi "unrecoverable\|re-enter" docs/16-security.md`
    - single-key model present: `grep -qi "one global\|single .*key\|all tenants" docs/16-security.md`
  </acceptance_criteria>
  <done>docs/16-security.md contains the "Encryption Key Backup & Rotation" section with the single-key model, off-box vault backup, ≤15-min RTO, the concrete generate-key/dry-run/rotate/restart/verify command sequence, and the lost-key UI re-entry recovery story.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator `.env` → backend process | `ENCRYPTION_KEY` / `JWT_SECRET_KEY` cross into the process at startup; a placeholder/unset value is a silent footgun |
| backup key → storage location | Where the backed-up key rests determines blast radius if the VM is compromised |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Spoofing / weak default | placeholder `ENCRYPTION_KEY` or `JWT_SECRET_KEY` shipped to production | mitigate | `_check_secrets_at_startup()` hard-fails the boot in production for unset/placeholder/invalid keys (Task 1, D-10..D-12); dev logs a loud structlog warning. |
| T-05-06 | Information Disclosure | logging the key value while reporting the startup issue | mitigate | Startup check logs only the issue string, never `settings.encryption_key`/`jwt_secret_key` (Task 1 action + acceptance). |
| T-05-07 | Elevation of Privilege | backup key stored on the same VM as the DB (compromise → decrypt everything) | mitigate (doc) | Runbook prescribes off-box vault storage, not in repo/VM/DB backup, with a named owner (Task 2, D-15). |
| T-05-08 | Information Disclosure | `--new-key` visible in `ps aux` / shell history during rotation | accept (documented) | Runbook documents the safer env-var/cleared-history invocation (Task 2); operational, not code-blockable (matches Plan 01 T-05-03). |
</threat_model>

<verification>
- `cd backend && pytest tests/test_encryption_rotation.py -k "startup_check" -x -q` — all six startup-check tests green.
- `cd backend && pytest tests/ -q --tb=short` — full suite green (no import-time hard-fail regression; Pitfall 4).
- `grep -q "## Encryption Key Backup & Rotation" docs/16-security.md` — runbook section present.
- Manual (per 05-VALIDATION.md Manual-Only): reviewer reads the runbook and confirms the paste-key-and-restart restore, ≤15-min RTO, off-box vault, single-key model, and UI re-entry recovery read operationally correctly.
</verification>

<success_criteria>
- PROD-05-04: startup hard-fails in production and warns in development for unset/placeholder/invalid `ENCRYPTION_KEY` and placeholder `JWT_SECRET_KEY`; no key material logged.
- PROD-05-01: `docs/16-security.md` documents backup location (off-box vault), owner, ≤15-min RTO, and lost-key recovery.
- PROD-05-03 (docs half): runbook shows the exact `python -m app.encryption` rotate/verify/generate-key command sequence delivered in Plan 01.
- Full backend suite remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-encryption-key-lifecycle/05-02-SUMMARY.md`.
</output>
