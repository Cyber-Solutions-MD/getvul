---
phase: 23-ingestion-reliability-precursor
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/connectors/sync.py
  - backend/tests/test_connector_health.py
autonomous: true
gap_closure: true
requirements: [REL-06]
must_haves:
  truths:
    - "On a failed background sync, no scanner/ticketing credential present in an upstream HTTP error body reaches SyncLog.error_message, the sync_logs table, or the structured `background_sync_complete` log line — REL-06 secret hygiene now holds for BOTH last_error and its sibling error_message."
  artifacts:
    - "backend/app/connectors/sync.py — line ~196 `log.error_message` assigned the `_sanitize_error(e)` result, not `str(e)[:2000]`"
    - "backend/tests/test_connector_health.py — regression test driving a secret-bearing exception through scheduler._run_single_sync and asserting the secret is absent from log.error_message AND the captured structured-log line"
  key_links:
    - "sync.py exception handler → _sanitize_error → log.error_message (redact-before-store, mirroring connector_config.last_error two lines below)"
    - "sync.py log.error_message → scheduler.py background_sync_complete `error=` field (already-sanitized value flows through, so the emitted log line is clean by construction)"
  prohibitions:
    - "log.error_message MUST NOT contain a raw unsanitized exception string — the `str(e)[:2000]` assignment must not survive (CR-03)."
    - "The scheduler's `background_sync_complete` log event MUST NOT emit an unredacted `Bearer`/`Basic`/api-key-shaped substring."
---

<objective>
Close verification gap CR-03 (REL-06): `SyncLog.error_message` bypasses the `_sanitize_error`
redaction that its sibling `connector_config.last_error` already goes through, so a secret echoed
in an upstream HTTP error body survives verbatim into the `sync_logs` table and into the scheduler's
`background_sync_complete` structured-log event on every failed background sync.

Purpose: Make REL-06's "trustworthy grounding data" claim hold across the whole error-capture path,
not just the UI-facing field. This is the same D-18/D-19 secret-hygiene guarantee, extended to the
sibling field the phase's own tests never asserted on.
Output: A one-line source fix in `sync.py` plus a regression test that would fail if the fix were reverted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ingestion-reliability-precursor/23-VERIFICATION.md
@.planning/phases/23-ingestion-reliability-precursor/23-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sanitize log.error_message before persisting it on the SyncLog row</name>
  <read_first>
    - backend/app/connectors/sync.py — the `except Exception as e:` handler (lines ~192-199). Note the ASYMMETRY: `sanitized = _sanitize_error(e)` is already computed and assigned to `connector_config.last_error` (line ~199), but `log.error_message` (line ~196) is assigned the RAW `str(e)[:2000]`.
    - backend/app/connectors/sync.py — `_sanitize_error(exc, cap=500)` definition (lines ~41-59): redact-then-truncate; `SyncLog.error_message` is a `Text` column so the 500-char cap is compatible.
    - backend/app/connectors/scheduler.py — `_run_single_sync` `background_sync_complete` event (lines ~38-46) which logs `error=log.error_message` verbatim; this is the downstream consumer that becomes clean once the source value is sanitized.
  </read_first>
  <action>
    In the `except Exception as e:` handler of `run_sync` (sync.py ~192-199), change the `log.error_message`
    assignment to use the already-computed `sanitized` value: `log.error_message = sanitized` (replacing
    `log.error_message = str(e)[:2000]`). Reuse the existing `sanitized = _sanitize_error(e)` binding — do NOT
    add a second `_sanitize_error` call, and do NOT introduce a separate redactor. Leave the `logger.error("sync_error", error=sanitized)` line and the `connector_config.last_error = sanitized` line unchanged; the fix is a single-line change that brings `log.error_message` to parity with its sibling. Do not alter the SUCCESS branch or the `finally` block.
  </action>
  <verify>
    <automated>ENCRYPTION_KEY=test-encryption-key-32-bytes-long-xx JWT_SECRET_KEY=test-jwt-secret cd backend && python -c "import ast,sys; src=open('app/connectors/sync.py').read(); assert 'str(e)[:2000]' not in src, 'raw assignment still present'; assert 'log.error_message = sanitized' in src, 'sanitized assignment missing'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep confirms `str(e)[:2000]` no longer appears in backend/app/connectors/sync.py.
    - grep confirms `log.error_message = sanitized` appears in the exception handler.
    - No new `_sanitize_error(` call site is added (still exactly one call, at line ~193); the `sanitized` binding is reused.
    - The SUCCESS branch (`log.status = "SUCCESS"` etc.) and `finally` block are byte-for-byte unchanged.
  </acceptance_criteria>
  <reversibility rating="reversible">One-line field assignment swap; trivially revertible with no data-shape change (SyncLog.error_message stays Text).</reversibility>
</task>

<task type="auto">
  <name>Task 2: Add scheduler-path regression test asserting the secret is absent from error_message AND the emitted log line</name>
  <read_first>
    - backend/tests/test_connector_health.py — `test_scheduler_path_failure_parity` (lines ~180-205): it already constructs `Exception("Authorization: Bearer sk-scheduler-secret")`, drives `scheduler_module._run_single_sync`, and asserts on `connector.last_error` only. Reuse its `_seed_fake_connector`, `_FakeConnector`, `db_session`, `tenant_a`, and `monkeypatch` fixtures and its `expire_on_commit=False` refresh pattern.
    - backend/app/connectors/scheduler.py — `_run_single_sync` `background_sync_complete` event (lines ~38-46) emits `error=log.error_message` via `structlog.get_logger()`; use `structlog.testing.capture_logs()` to capture that event.
    - backend/app/connectors/sync.py — `_sanitize_error` (lines ~41-59) to know the expected redaction token (`[REDACTED]`).
  </read_first>
  <action>
    Add a new async test (e.g. `test_scheduler_path_error_message_and_log_are_sanitized`) to test_connector_health.py alongside `test_scheduler_path_failure_parity`. Seed a fake connector, set `_FakeConnector.RAISE` to an exception whose message embeds a `Bearer`-shaped secret (e.g. `Exception("upstream 401: Authorization: Bearer sk-log-leak-42 rejected")`), wrap the `await scheduler_module._run_single_sync(...)` call in `structlog.testing.capture_logs()`, then reload the persisted SyncLog row for that connector (query `SyncLog` by `connector_config_id`/tenant, ordered newest-first) and assert: (1) the raw secret token and the literal `Bearer` do NOT appear in `log.error_message`; (2) `[REDACTED]` DOES appear in `log.error_message` (proving it went through `_sanitize_error`, not merely a truncation); (3) the captured `background_sync_complete` event's `error` value — and the full repr of the captured events list — contains neither the secret token nor `Bearer`. Keep the existing `test_scheduler_path_failure_parity` intact (it may additionally gain the same `log.error_message` assertions, but do not delete its `connector.last_error` assertions).
  </action>
  <verify>
    <automated>ENCRYPTION_KEY=test-encryption-key-32-bytes-long-xx JWT_SECRET_KEY=test-jwt-secret cd backend && python -m pytest tests/test_connector_health.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_connector_health.py -q` exits 0 with the new test collected and passing (env vars ENCRYPTION_KEY + JWT_SECRET_KEY set, per the backend pytest-env memory).
    - The new test asserts `"sk-log-leak-42" not in log.error_message` AND `"Bearer" not in log.error_message` AND `"[REDACTED]" in log.error_message`.
    - The new test asserts the secret token and `"Bearer"` are absent from the captured `background_sync_complete` structured-log event (via `structlog.testing.capture_logs()`).
    - Regression proof: reverting Task 1 (restoring `log.error_message = str(e)[:2000]`) causes this test to FAIL on the `log.error_message`/log-line assertions — it is a live guard, not a tautology.
  </acceptance_criteria>
  <reversibility rating="reversible">Additive test only; no production behavior change.</reversibility>
</task>

</tasks>

<deferred_adjacent_items>
The code review flagged two adjacent backend issues NOT in this closure's scope. Both are explicitly DEFERRED (not silently dropped):

- **CR-02 (WARNING) — duplicate `POST /sync-status` route** (ticketing/router.py:348 vs 1251): the legacy `trigger_ticket_sync` handler is unreachable, so its `ticket.sync_status` audit entry never fires. DEFERRED: removing/merging a route handler is a behavior-changing edit to the ticketing router that touches the audit trail and `daily_sync` divergence — out of scope for a 2-gap secret-hygiene/mobile-provider closure, and carries its own regression-test surface. Route to a follow-up backlog item (ticketing router cleanup), not this plan.
- **WR-01 (WARNING) — `TicketRuleAction.provider` lacks enum/pattern validation** (schemas.py:100-107): a garbage `action.provider` 500s the manual rule-run endpoint instead of 400ing. DEFERRED: not a gap (no REL truth fails); the scheduler path already guards it. A one-line `Field("ASANA", pattern="^(ASANA|JIRA|GITHUB)$")` is cheap but changes request-validation behavior on a separate endpoint with its own test needs — fold into the same ticketing-router follow-up rather than expand this closure's blast radius.
</deferred_adjacent_items>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream scanner/ticketing API → GetVul | HTTP error bodies from third-party APIs are untrusted and may echo back credentials submitted by GetVul |
| GetVul app → sync_logs table / structured log stream | error strings persisted + emitted here are read by DB admins and log-aggregator operators |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-10-01 | Information Disclosure | `SyncLog.error_message` (sync.py ~196) | high | mitigate | Route through `_sanitize_error` before persist (Task 1); regression-tested (Task 2) |
| T-23-10-02 | Information Disclosure | `background_sync_complete` log event (scheduler.py ~45) | high | mitigate | Sanitizing the source value (Task 1) makes the emitted log line clean by construction; asserted via `capture_logs` (Task 2) |
</threat_model>

<verification>
- `ENCRYPTION_KEY=... JWT_SECRET_KEY=... python -m pytest tests/test_connector_health.py -q` passes (run per-file, not the whole tests/ dir — per backend pytest-env memory).
- `grep -n "str(e)\[:2000\]" backend/app/connectors/sync.py` returns no matches.
- The `background_sync_complete` structured-log event carries only sanitized error text.
</verification>

<success_criteria>
- Gap CR-03 (REL-06) closed: secret-bearing upstream error bodies cannot reach `SyncLog.error_message`, the `sync_logs` table, or the application log stream on a failed background sync.
- The regression test fails against the pre-fix code and passes after the fix.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces / changes

- **Changed behavior — `backend/app/connectors/sync.py` `run_sync` exception handler:** `log.error_message` now receives the `_sanitize_error(e)` result (`sanitized`) instead of the raw `str(e)[:2000]`. No new symbols; the existing `sanitized` binding is reused. The `str(e)[:2000]` expression is removed.
- **New test — `backend/tests/test_connector_health.py`:** `test_scheduler_path_error_message_and_log_are_sanitized` (async) — drives a `Bearer`-bearing exception through `scheduler._run_single_sync`, asserts the secret and `Bearer` are absent (and `[REDACTED]` present) in the persisted `SyncLog.error_message`, and absent from the captured `background_sync_complete` structured-log event.
- No schema, migration, API, or frontend changes.
</artifacts_produced>

<output>
Create `.planning/phases/23-ingestion-reliability-precursor/23-10-SUMMARY.md` when done
</output>
