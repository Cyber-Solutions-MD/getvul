---
phase: 23-ingestion-reliability-precursor
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/ticketing/providers.py
  - backend/app/ticketing/dispatch.py
  - backend/app/ticketing/jira_client.py
  - backend/app/ticketing/github_client.py
  - backend/app/connectors/jira_client.py
  - backend/app/ticketing/daily_sync.py
  - frontend/src/lib/ticketing/providers.ts
  - frontend/src/lib/mutations/use-create-ticket.ts
  - backend/tests/test_ticketing_clients.py
autonomous: true
requirements: [REL-04, REL-05]
must_haves:
  truths:
    - "A single TicketProvider enum (backend) and a matching TS union type (frontend) are the one source of truth for ASANA/JIRA/GITHUB, preserving the uppercase-backend/lowercase-frontend wire convention"
    - "A TicketingClient Protocol with create/get/close/comment verbs exists, and AsanaClient/JiraClient/GitHubClient are each reachable through it via adapters keyed by provider"
    - "The canonical app/ticketing/jira_client.py gains comment + status-transition (close) methods ported from the deleted app/connectors/jira_client.py, so daily_sync retains Jira comment/auto-close capability"
    - "app/connectors/jira_client.py is deleted and daily_sync imports only the canonical ticketing JiraClient"
    - "GitHubClient gains add_comment + close_issue methods mirroring the Asana auto-close template"
  artifacts:
    - path: "backend/app/ticketing/providers.py"
      provides: "TicketProvider str-Enum (ASANA/JIRA/GITHUB)"
      contains: "class TicketProvider"
    - path: "backend/app/ticketing/dispatch.py"
      provides: "TicketingClient Protocol + Asana/Jira/GitHub adapters + build factory"
      contains: "class TicketingClient"
    - path: "backend/app/ticketing/jira_client.py"
      provides: "canonical JiraClient with create_ticket + get_issue + comment + transition/close"
    - path: "backend/app/ticketing/github_client.py"
      provides: "GitHubClient with create + get + add_comment + close_issue"
      contains: "add_comment"
    - path: "frontend/src/lib/ticketing/providers.ts"
      provides: "TicketProvider TS union + labels"
  key_links:
    - from: "backend/app/ticketing/daily_sync.py"
      to: "backend/app/ticketing/jira_client.py"
      via: "import repointed off app/connectors/jira_client.py"
      pattern: "from app.ticketing.jira_client import"
    - from: "backend/app/ticketing/dispatch.py adapters"
      to: "AsanaClient / JiraClient / GitHubClient method surfaces"
      via: "verb normalization (create/get/close/comment)"
      pattern: "class .*Adapter"
---

<objective>
Establish the multi-provider ticketing contracts — a formal `TicketProvider` enum (D-23), a `TicketingClient` Protocol + per-provider adapters + factory (D-06), the JiraClient consolidation (D-08, a real MERGE not a delete), and the missing GitHubClient comment/close methods (D-12) — that Plans 04/05/08 and Phase 27 all build on. Interface-first: this plan ships contracts, the wiring plans consume them.

Purpose: Today `provider` is a bare regex-validated string dispatched via scattered `if provider == "ASANA"` checks, there are TWO divergent JiraClients (the canonical one LACKS the comment/transition methods daily_sync depends on), and GitHubClient has no comment/close methods at all. None of the create-side dispatch can work until these contracts exist.
Output: One enum, one Protocol + adapters + factory, one consolidated JiraClient, a completed GitHubClient, one deleted duplicate, and the frontend provider type.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-ingestion-reliability-precursor/23-CONTEXT.md
@.planning/phases/23-ingestion-reliability-precursor/23-RESEARCH.md

<interfaces>
Two-JiraClient method diff (23-RESEARCH.md "Two JiraClients" table) — the MERGE target:
- Canonical `app/ticketing/jira_client.py` (API v3/ADF): has `create_ticket`, `get_issue`, `test_connection`, `close()`. LACKS comment + status-transition.
- To-delete `app/connectors/jira_client.py` (API v2): has `create_issue`, `get_issue`, `update_issue(issue_key, comment=None, status=None)` (posts comment AND does transition lookup+POST — this is what daily_sync._sync_jira_tickets uses), plus list/search/delete.
=> PORT `update_issue`'s comment-post and status-transition logic into the canonical v3 client as two protocol-shaped methods (`comment(issue_key, body)` and `transition/close(issue_key)`), keeping v3/ADF. THEN delete the connectors version and repoint daily_sync's import.

GitHubClient current surface (complete-but-orphaned) — add the two missing methods:
```python
class GitHubClient:
    def __init__(self, token: str, owner: str, repo: str) -> None: ...
    async def test_connection(self) -> dict: ...
    async def create_ticket(self, title: str, body: str) -> GitHubIssue | None: ...   # single 429-retry
    async def get_issue(self, number: int) -> dict | None: ...
    async def get_watchers(self) -> list: ...   # [] stub — KEEP as-is (D-12: GitHub has no watcher primitive)
    async def close(self) -> None: ...           # HTTP client cleanup — do NOT confuse with close_issue
    # ADD: async def add_comment(self, number: int, body: str) -> None   (POST /repos/{owner}/{repo}/issues/{number}/comments)
    # ADD: async def close_issue(self, number: int) -> None              (PATCH /repos/{owner}/{repo}/issues/{number} {"state":"closed"})
```
Asana auto-close template to mirror (service.py:1015-1031): `asana_client.update_task(gid, completed=True)` then `asana_client.add_comment(gid, "...")`.

Wire convention (CR-06): provider stored/compared UPPERCASE on the backend, lowercased only at the serialization boundary. Enum values are uppercase; do not change existing lowercasing at emit sites.

Backend pytest form (per-file + env vars):
```bash
cd backend && export ENCRYPTION_KEY=$(.venv/bin/python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=$(.venv/bin/python -c "import secrets;print(secrets.token_urlsafe(48))") ENVIRONMENT=development && .venv/bin/pytest tests/test_ticketing_clients.py -v
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TicketProvider enum + TicketingClient Protocol + adapters + factory + TS union (D-23, D-06)</name>
  <files>backend/app/ticketing/providers.py, backend/app/ticketing/dispatch.py, frontend/src/lib/ticketing/providers.ts, frontend/src/lib/mutations/use-create-ticket.ts</files>
  <read_first>
    - backend/app/ticketing/asana_client.py — reference client shape: `create_task`, `get_task`, `update_task`, `add_comment` (the verbs the Protocol generalizes).
    - backend/app/ticketing/jira_client.py + backend/app/ticketing/github_client.py — each client's actual method names (they differ: create_task vs create_ticket; get_task vs get_issue).
    - backend/app/ticketing/schemas.py:53-63 — the existing `provider: str = Field(..., pattern="^(ASANA|JIRA|GITHUB)$")` validators the enum must stay consistent with.
    - frontend/src/lib/mutations/use-create-ticket.ts:8 — the existing inline `'ASANA'|'JIRA'|'GITHUB'` union to replace with the shared type.
    - 23-RESEARCH.md Open Question 6 — typing.Protocol recommendation (codebase has no Protocol; BaseConnector uses ABC) + thin adapters keeping each client's method names.
  </read_first>
  <action>
    Create backend/app/ticketing/providers.py with `class TicketProvider(str, Enum): ASANA="ASANA"; JIRA="JIRA"; GITHUB="GITHUB"`.
    Create backend/app/ticketing/dispatch.py: a `typing.Protocol` `TicketingClient` with async verbs `create(self, title: str, body: str, **kwargs) -> str | None` (returns external ticket URL/id), `get(self, ref: str) -> dict | None`, `comment(self, ref: str, body: str) -> None`, `close(self, ref: str) -> None`. Add adapter classes `AsanaAdapter`/`JiraAdapter`/`GitHubAdapter` wrapping the concrete clients and normalizing their existing method names to those verbs (do NOT rename methods on the concrete clients). Add `def build_ticketing_client(provider: TicketProvider, credentials: dict, config: dict) -> TicketingClient` returning the matching adapter around a freshly-constructed concrete client. Keep credential decryption OUT of this file — Plan 04's helpers pass already-decrypted creds in.
    Create frontend/src/lib/ticketing/providers.ts: `export type TicketProvider = 'ASANA' | 'JIRA' | 'GITHUB';` + `export const PROVIDER_LABELS: Record<TicketProvider, string> = { ASANA: 'Asana', JIRA: 'Jira', GITHUB: 'GitHub' };`. Edit use-create-ticket.ts to import `TicketProvider` from that module and type `CreateTicketRequest.provider: TicketProvider` (remove the inline literal).
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -c "from app.ticketing.dispatch import TicketingClient, build_ticketing_client; from app.ticketing.providers import TicketProvider; print([p.value for p in TicketProvider])" && cd ../frontend && npx tsc --noEmit 2>&1 | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class TicketProvider" backend/app/ticketing/providers.py` present with three uppercase members.
    - `grep -n "class TicketingClient\|def build_ticketing_client\|class AsanaAdapter\|class JiraAdapter\|class GitHubAdapter" backend/app/ticketing/dispatch.py` all present.
    - `grep -n "TicketProvider" frontend/src/lib/mutations/use-create-ticket.ts` shows the import; no inline `'ASANA' | 'JIRA' | 'GITHUB'` literal remains there.
    - `tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>Enum, Protocol, adapters, factory, and shared TS type exist and import cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consolidate JiraClient — port comment + status-transition into the canonical client, delete the duplicate, repoint daily_sync (D-08)</name>
  <files>backend/app/ticketing/jira_client.py, backend/app/connectors/jira_client.py, backend/app/ticketing/daily_sync.py, backend/tests/test_ticketing_clients.py</files>
  <read_first>
    - backend/app/connectors/jira_client.py — the whole file, especially `update_issue(issue_key, comment=None, status=None)` (the transition-lookup + POST logic to PORT) and `get_issue`.
    - backend/app/ticketing/jira_client.py — the canonical v3/ADF client (`create_ticket`, `get_issue`, `test_connection`, `close()`).
    - backend/app/ticketing/daily_sync.py — `_sync_jira_tickets` (~line 271) and its import of `app.connectors.jira_client` (~line 56 JIRA branch); this is the caller that must keep comment/transition working.
    - backend/tests/test_provider_stubs.py — the JiraClient MockTransport setup (`_patch_transport(client._client, ...)`).
  </read_first>
  <behavior>
    - RED: a MockTransport test asserts the canonical JiraClient can (a) post a comment (POST to `/rest/api/3/issue/{key}/comment`) and (b) transition an issue to a done/closed status (GET transitions, POST matching transition id).
    - Test asserts `get_issue` returns None on 404 (canonical convention) rather than raising.
    - Test asserts daily_sync's Jira status-sync path still resolves the ported methods (import smoke: `from app.ticketing.jira_client import JiraClient` and the methods exist).
  </behavior>
  <action>
    Port `update_issue`'s two capabilities from backend/app/connectors/jira_client.py into the canonical backend/app/ticketing/jira_client.py as protocol-shaped methods: `async def comment(self, issue_key: str, body: str) -> None` (v3 ADF comment body) and `async def transition(self, issue_key: str, target_status: str) -> None` (GET `/rest/api/3/issue/{key}/transitions`, find the transition whose `to.name`/`name` matches, POST it) — plus a thin `async def close(self, issue_key: str) -> None` that calls `transition` with the tenant's done status (mirror the status string daily_sync currently passes). Keep v3/ADF throughout. Update the `JiraAdapter` in dispatch.py (Task 1) to map its `comment`/`close` verbs onto these. Then `git rm backend/app/connectors/jira_client.py` and repoint daily_sync.py's JIRA branch import to `from app.ticketing.jira_client import JiraClient`, adapting the call sites from `update_issue(key, comment=..., status=...)` to the new `comment()` / `transition()` methods. Grep the whole repo for `app.connectors.jira_client` and fix every remaining importer (tests included). Add MockTransport tests for the two ported methods to test_ticketing_clients.py.
  </action>
  <verify>
    <automated>cd backend && test -z "$(grep -rln 'app.connectors.jira_client' app/ tests/)" && export ENCRYPTION_KEY=$(.venv/bin/python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=$(.venv/bin/python -c "import secrets;print(secrets.token_urlsafe(48))") ENVIRONMENT=development && .venv/bin/pytest tests/test_ticketing_clients.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f backend/app/connectors/jira_client.py` (file deleted).
    - `grep -rn "app.connectors.jira_client" backend/` returns zero hits.
    - `grep -n "async def comment\|async def transition\|async def close" backend/app/ticketing/jira_client.py` all present.
    - `grep -n "from app.ticketing.jira_client import" backend/app/ticketing/daily_sync.py` present.
    - pytest exits 0 with new comment/transition tests passing.
  </acceptance_criteria>
  <done>One canonical JiraClient with full create/get/comment/close; duplicate deleted; daily_sync repointed and still comment/transition-capable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete GitHubClient — add_comment + close_issue mirroring the Asana auto-close template (D-12)</name>
  <files>backend/app/ticketing/github_client.py, backend/tests/test_ticketing_clients.py</files>
  <read_first>
    - backend/app/ticketing/github_client.py — the whole file; note `create_ticket` (single 429-retry pattern to reuse), `get_issue`, `close()` (HTTP cleanup — NOT issue close), `get_watchers` ([] stub to keep).
    - backend/app/ticketing/service.py:1015-1031 — the Asana auto-close template (`update_task(completed=True)` + `add_comment`) that GitHub must mirror.
    - backend/tests/test_provider_stubs.py — the GitHubClient MockTransport setup.
  </read_first>
  <behavior>
    - RED: MockTransport test asserts `add_comment(number, body)` POSTs to `/repos/{owner}/{repo}/issues/{number}/comments` with the body.
    - Test asserts `close_issue(number)` PATCHes `/repos/{owner}/{repo}/issues/{number}` with `{"state": "closed"}`.
    - Test asserts `get_watchers()` still returns `[]` (unchanged stub).
  </behavior>
  <action>
    Add to backend/app/ticketing/github_client.py: `async def add_comment(self, number: int, body: str) -> None` (POST `/repos/{owner}/{repo}/issues/{number}/comments` with `{"body": body}`) and `async def close_issue(self, number: int) -> None` (PATCH `/repos/{owner}/{repo}/issues/{number}` with `{"state": "closed"}`), reusing the same auth headers + single-429-retry pattern already in `create_ticket`. Do NOT touch `get_watchers` (stays `[]` per D-12). Update the `GitHubAdapter` in dispatch.py (Task 1) so its `comment` verb → `add_comment` and `close` verb → `close_issue`. Add MockTransport tests for both new methods to test_ticketing_clients.py.
  </action>
  <verify>
    <automated>cd backend && export ENCRYPTION_KEY=$(.venv/bin/python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=$(.venv/bin/python -c "import secrets;print(secrets.token_urlsafe(48))") ENVIRONMENT=development && .venv/bin/pytest tests/test_ticketing_clients.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "async def add_comment\|async def close_issue" backend/app/ticketing/github_client.py` both present.
    - `grep -n "get_watchers" backend/app/ticketing/github_client.py` still returns the `[]` stub (unchanged).
    - GitHubAdapter.comment/close in dispatch.py delegate to add_comment/close_issue.
    - pytest exits 0.
  </acceptance_criteria>
  <done>GitHubClient can comment + close issues; adapter wired; all three clients now satisfy the Protocol.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GetVul backend → Jira/GitHub API over TLS | Untrusted network path; external issue/response data untrusted |
| Fernet-decrypted ticketing credentials → concrete client | Secrets live in memory; must never be logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06 | Information Disclosure | JiraClient / GitHubClient auth headers | mitigate | Adapters/clients set `Authorization`/token headers from passed-in decrypted creds only; no `logger.*` call includes the token payload — verify no new log line echoes credentials |
| T-23-07 | Tampering | Deleting app/connectors/jira_client.py while daily_sync still imports it | mitigate | Task 2 ports comment/transition FIRST, then deletes, then repoints, with a repo-wide grep gate (`grep -rn app.connectors.jira_client` == 0) preventing a broken import shipping |
| T-23-08 | Repudiation | GitHub close_issue with no audit trail | accept | Ticket state changes are audit-logged at the service/router layer (Plan 04/05), not inside the low-level client; client stays a thin HTTP wrapper |
</threat_model>

<verification>
- `pytest tests/test_ticketing_clients.py -v` green (env-var + per-file form).
- `grep -rn "app.connectors.jira_client" backend/` returns zero hits; the file is deleted.
- `ruff check app/ticketing/ && ruff format --check app/ticketing/` clean; `tsc --noEmit` clean on the frontend.
</verification>

<success_criteria>
The ticketing provider contracts exist and are complete: one enum, one Protocol with three working adapters, one consolidated Jira client (create/get/comment/close), and a GitHub client that can create/get/comment/close — the foundation Plans 04/05/08 wire into the service, rule engine, router, and UI. Supports ROADMAP SC#3 (REL-04) and SC#4 (REL-05).
</success_criteria>

<output>
After completion, create `.planning/phases/23-ingestion-reliability-precursor/23-03-SUMMARY.md`
</output>
