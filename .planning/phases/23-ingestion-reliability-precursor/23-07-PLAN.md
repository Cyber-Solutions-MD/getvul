---
phase: 23-ingestion-reliability-precursor
plan: 07
type: execute
wave: 3
depends_on: [05, 06]
files_modified:
  - backend/app/connectors/sync.py
  - backend/app/connectors/scheduler.py
  - backend/tests/test_connector_health.py
autonomous: true
requirements: [REL-06]
must_haves:
  truths:
    - "On any failed sync, consecutive_failure_count increments and last_error is set to a sanitized, truncated, secret-redacted string; on any successful sync, consecutive_failure_count resets to 0 and last_error is cleared"
    - "A crafted HTTP-error string containing a credential-shaped token does NOT appear verbatim in last_error (redacted before persist)"
    - "The scheduler-driven sync path applies the same counter/last_error logic as the direct path (one implementation, no divergence)"
  artifacts:
    - path: "backend/app/connectors/sync.py"
      provides: "failure-counter increment/reset + redacted+truncated last_error capture on the SUCCESS/FAILED write-back"
      contains: "consecutive_failure_count"
    - path: "backend/tests/test_connector_health.py"
      provides: "asserts increment/reset semantics + secret redaction + truncation"
  key_links:
    - from: "backend/app/connectors/sync.py write-back (L96-100, L124-145)"
      to: "connector_config.consecutive_failure_count / last_error"
      via: "increment on FAILED, reset on SUCCESS, redact-before-store"
      pattern: "consecutive_failure_count"
    - from: "last_error capture"
      to: "app/logging.py redaction machinery"
      via: "dict-wrap + token-pattern scrub (Pitfall 4 correction)"
      pattern: "redact"
---

<objective>
Implement the runtime side of the connector health signals (D-18, D-19): increment `consecutive_failure_count` on failed syncs and reset it on success, and capture a sanitized, truncated, secret-redacted `last_error` string — reusing the Phase-7 redaction machinery correctly (per the research correction), never a parallel redactor. Applies on both the direct and scheduler sync paths.

Purpose: The columns exist (Plan 06) but nothing populates them. Without this, "failed N times in a row" and the on-card error summary have no data. A naive `str(exc)` capture risks leaking a scanner/ticketing token into the DB and UI.
Output: Populated counter + redacted last_error on every sync outcome, tested for increment/reset semantics and redaction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-ingestion-reliability-precursor/23-CONTEXT.md
@.planning/phases/23-ingestion-reliability-precursor/23-RESEARCH.md

<interfaces>
sync.py write-back region (VERIFIED, backend/app/connectors/sync.py):
- Success (L124-139): `connector_config.last_sync_at=...; connector_config.last_sync_status="SUCCESS"; connector_config.last_sync_record_count=...`
- Failure (L141-145): `except Exception as e: logger.error("sync_error", error=str(e)); log.status="FAILED"; log.error_message=str(e)[:2000]; connector_config.last_sync_status="FAILED"`
- Auth-failure early return (L96-100) ALSO sets FAILED — the counter/last_error logic must cover this path too, not just the except block.

Phase-7 redaction (app/logging.py) — the CORRECTION (23-RESEARCH Pitfall 4 / Open Question 7):
- `redact_sensitive_keys(logger, method, event_dict)` is a structlog processor that redacts by KEY NAME recursively through dicts/lists — it does NOT scan a bare string for secret substrings.
- `SENSITIVE_KEYS`, `_is_sensitive`, `_redact_value` are also exported (underscore = module-private by convention; importable).
- Correct reuse: wrap the exception in a dict `_redact_value({"exception_type": type(e).__name__, "message": str(e)})` AND add a lightweight pattern scrub for `Bearer [\w.-]+`, `Basic [\w+/=]+`, and long api-key shapes, because a raw httpx exception string has no key structure. Compose both. Truncate AFTER redaction (cap = Claude's discretion, e.g. 500 chars).
- Do NOT build a second standalone redactor; reuse the logging module's helpers and layer only the string-pattern scrub on top.

Do NOT change what last_sync_status stores ("SUCCESS"/"FAILED") — Plan 06 normalizes it at the wire boundary.

Backend pytest form (per-file + env vars) — see Plan 01. test_connector_health.py may use the DB fixture; use the env-var form.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failure-counter increment/reset + redacted+truncated last_error capture in sync.py (D-18, D-19)</name>
  <files>backend/app/connectors/sync.py, backend/tests/test_connector_health.py</files>
  <read_first>
    - backend/app/connectors/sync.py — the full run function: the auth-fail early return (L96-100), the success write-back (L124-139), and the `except Exception` block (L141-145). All three outcome paths.
    - backend/app/logging.py — `redact_sensitive_keys`, `SENSITIVE_KEYS`, `_is_sensitive`, `_redact_value` (the machinery to reuse, per the Pitfall-4 correction).
    - 23-RESEARCH Pitfall 4 + Open Question 7 — the exact reuse pattern (dict-wrap + pattern scrub + truncate).
  </read_first>
  <behavior>
    - RED: after a FAILED sync (exception raised), the connector_config has `consecutive_failure_count == previous+1` and `last_error` set to a redacted+truncated string.
    - After a SUCCESS sync, `consecutive_failure_count == 0` and `last_error is None`, regardless of prior value.
    - The auth-failure early-return path (L96-100) also increments the counter + sets last_error ("Authentication failed").
    - Redaction: an exception whose message contains `Authorization: Bearer sk-secret123` yields a last_error where `sk-secret123` / the Bearer token is NOT present.
    - Truncation: a very long exception message is capped at the chosen length.
  </behavior>
  <action>
    Add a module-level helper `def _sanitize_error(exc: Exception, cap: int = 500) -> str` in sync.py that: builds `{"exception_type": type(exc).__name__, "message": str(exc)}`, passes it through the reused `_redact_value` from app.logging (key-based redaction), then applies a regex scrub over the resulting message string for `Bearer\s+[\w.\-]+`, `Basic\s+[\w+/=]+`, and long api-key-shaped tokens (`[A-Za-z0-9_\-]{32,}`) replacing with `[REDACTED]`, then truncates to `cap` chars. In the success write-back (L124-139) add `connector_config.consecutive_failure_count = 0` and `connector_config.last_error = None`. In the `except Exception as e` block (L141-145) add `connector_config.consecutive_failure_count = (connector_config.consecutive_failure_count or 0) + 1` and `connector_config.last_error = _sanitize_error(e)`. In the auth-fail early-return path (L96-100) apply the same increment + `connector_config.last_error = "Authentication failed"` before returning. Keep `log.error_message=str(e)[:2000]` as-is for the SyncLog record, but ensure the PERSISTED `connector_config.last_error` uses the sanitized value. Create backend/tests/test_connector_health.py covering increment, reset, auth-fail-path increment, Bearer-token redaction, and truncation.
  </action>
  <verify>
    <automated>cd backend && export ENCRYPTION_KEY=$(.venv/bin/python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=$(.venv/bin/python -c "import secrets;print(secrets.token_urlsafe(48))") ENVIRONMENT=development && .venv/bin/pytest tests/test_connector_health.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "consecutive_failure_count\|last_error\|_sanitize_error" backend/app/connectors/sync.py` shows increment (except + auth-fail path), reset (success path), and the helper.
    - `grep -n "from app.logging import\|_redact_value" backend/app/connectors/sync.py` shows reuse of the existing redaction (no new standalone redactor class).
    - test asserts a `Bearer sk-secret123`-containing exception yields a last_error without `sk-secret123`.
    - test asserts reset-to-0 on success and increment on failure; pytest exits 0.
  </acceptance_criteria>
  <done>Counter + redacted/truncated last_error populated on every sync outcome; redaction reuses Phase-7 machinery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scheduler path parity for counter/last_error (D-18)</name>
  <files>backend/app/connectors/scheduler.py, backend/tests/test_connector_health.py</files>
  <read_first>
    - backend/app/connectors/scheduler.py — how it invokes the sync harness (does it call sync.py's run function, or duplicate write-back logic?).
    - backend/app/connectors/sync.py — the write-back logic from Task 1 (the single source of truth the scheduler should route through).
  </read_first>
  <behavior>
    - A scheduler-triggered failed sync increments the counter and sets last_error identically to the direct path (no divergence).
    - If the scheduler already delegates to sync.py's run function, parity holds with a confirming test; if it duplicates write-back, route it through the same helper.
  </behavior>
  <action>
    Inspect scheduler.py. If it delegates to sync.py's run function (likely), no counter/last_error logic needs duplicating — add a test asserting a scheduler-driven failure path produces the same counter/last_error as Task 1. If scheduler.py has its OWN write-back that sets last_sync_status, refactor it to call the same sync.py run function (or the same `_sanitize_error` + increment/reset block) so there is exactly one implementation. Do NOT leave two divergent counter implementations. Extend test_connector_health.py with the scheduler-path parity assertion.
  </action>
  <verify>
    <automated>cd backend && export ENCRYPTION_KEY=$(.venv/bin/python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=$(.venv/bin/python -c "import secrets;print(secrets.token_urlsafe(48))") ENVIRONMENT=development && .venv/bin/pytest tests/test_connector_health.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "consecutive_failure_count\|last_error\|run_sync\|_sanitize_error" backend/app/connectors/scheduler.py` shows either a single delegated path or the shared helper — never a second inline counter implementation.
    - test asserts scheduler-driven failure increments the counter identically to the direct path.
    - pytest exits 0.
  </acceptance_criteria>
  <done>Scheduler and direct sync share one counter/last_error implementation; parity proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caught exception string (may echo upstream response text) → persisted last_error → Connectors UI | A secret embedded in an error could reach the DB + a browser |
| Tenant A ↔ Tenant B | Counter/last_error are per connector_config row (already tenant-owned) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-19 | Information Disclosure | last_error capture of exception strings | mitigate | `_sanitize_error` reuses app.logging `_redact_value` (key-based) + a Bearer/Basic/api-key pattern scrub + truncation, per the Pitfall-4 correction; test asserts a crafted token does not survive into last_error |
| T-23-20 | Information Disclosure | logger.error("sync_error", error=str(e)) | mitigate | The structlog processor `redact_sensitive_keys` already runs on the logging pipeline; confirm the error event does not bypass it. Prefer logging the sanitized string |
| T-23-21 | Tampering | Divergent counter logic on scheduler vs direct path | mitigate | Task 2 enforces a single implementation so failure counts cannot drift between paths |
</threat_model>

<verification>
- `pytest tests/test_connector_health.py -v` green (env-var + per-file form).
- No standalone regex-only redactor class is introduced; app.logging helpers are reused (grep confirms the import).
- `ruff check app/connectors/sync.py app/connectors/scheduler.py && ruff format --check ...` clean; mypy baseline not regressed.
</verification>

<success_criteria>
Every sync outcome updates consecutive_failure_count (increment on fail, reset on success) and a secret-safe last_error, on both the direct and scheduler paths — providing the data the REL-06 card surface (Plan 09) renders. Supports ROADMAP SC#5.
</success_criteria>

<output>
After completion, create `.planning/phases/23-ingestion-reliability-precursor/23-07-SUMMARY.md`
</output>
