---
phase: 23-ingestion-reliability-precursor
plan: 05
type: execute
wave: 2
depends_on: [03]
files_modified:
  - backend/app/connectors/schemas.py
  - backend/app/connectors/router.py
  - backend/app/connectors/tester.py
  - backend/app/connectors/sync.py
  - backend/app/ticketing/daily_sync.py
  - backend/tests/test_github_sync.py
autonomous: true
requirements: [REL-05]
must_haves:
  truths:
    - "A GITHUB connector is creatable through the existing add-connector wizard: it is registered in CONNECTOR_TYPES, CONNECTOR_CATEGORIES, tester.py's dispatch, and sync.py's SPECIAL_CONNECTORS set"
    - "GitHub token is Fernet-encrypted (credentials side); owner/repo live in plaintext config — never store the PAT in plaintext"
    - "daily_sync reads a GitHub issue's state via get_issue (closed -> GetVul ticket completed / linked vuln REMEDIATED) and, when all linked vulns resolve, PATCHes the issue closed — mirroring the Asana auto-close"
    - "GITHUB is a no-data-sync connector: sync.py skips vuln-scanning it (SPECIAL_CONNECTORS)"
  artifacts:
    - path: "backend/app/connectors/schemas.py"
      provides: "GITHUB entry in CONNECTOR_TYPES (token/owner/repo, token as credential)"
      contains: "GITHUB"
    - path: "backend/app/ticketing/daily_sync.py"
      provides: "GitHub sync branch: inbound state map + auto-close parity"
    - path: "backend/tests/test_github_sync.py"
      provides: "asserts GitHub inbound-close and outbound auto-close"
  key_links:
    - from: "backend/app/ticketing/daily_sync.py GitHub branch"
      to: "GitHubClient.get_issue / close_issue / add_comment (Plan 03)"
      via: "provider dispatch on Ticket.provider == GITHUB"
      pattern: "GITHUB"
    - from: "add-connector wizard (/connectors/types)"
      to: "connectors/router.py CONNECTOR_CATEGORIES"
      via: "GITHUB category registration"
      pattern: "GITHUB"
---

<objective>
Finish GitHub ticketing end-to-end (D-11, D-12, D-13): register `GITHUB` as a creatable connector type across the four backend registration points, and add the GitHub branch to `daily_sync` (inbound state map + outbound auto-close) mirroring the existing Asana pattern — so GitHub is a first-class ticketing provider, not an orphaned stub.

Purpose: `GitHubClient` is complete (and now has comment/close from Plan 03) but referenced nowhere outside its own file — `GITHUB` isn't registered in any of the four places the Connectors wizard/sync harness need, and daily_sync has no GitHub sync branch. REL-05 requires create + sync both working.
Output: A creatable GitHub connector + a working GitHub status-sync/auto-close branch, tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-ingestion-reliability-precursor/23-CONTEXT.md
@.planning/phases/23-ingestion-reliability-precursor/23-RESEARCH.md

<interfaces>
GITHUB is registered in ZERO of these four backend places today (23-RESEARCH Open Question 5) — all needed:
1. backend/app/connectors/schemas.py — `CONNECTOR_TYPES` dict (add a `"GITHUB"` `ConnectorTypeInfo`: config fields owner/repo + credential field token).
2. backend/app/connectors/router.py — `CONNECTOR_CATEGORIES` dict (drives `/connectors/types`, read by the wizard).
3. backend/app/connectors/tester.py — the provider-test dispatch table (add a GitHub "Test Connection" using `GitHubClient.test_connection`).
4. backend/app/connectors/sync.py — `SPECIAL_CONNECTORS` set (currently `{"JAMF","HUMAANS","ASANA","JIRA","GOOGLE_WORKSPACE","AZURE_ENTRA_ID","OKTA","INTUNE"}` at L40; add `"GITHUB"` so sync treats it as a no-data-sync ticketing connector like ASANA/JIRA at sync.py L77-82).

Frontend visual layer is ALREADY ready (no frontend work here): `ConnectorProvider` includes `'github'`, `--gradient-provider-github` defined in globals.css:135 (violet), connector-mark.tsx supports it.

Credential separation (V6, 23-RESEARCH Security Domain): the existing pattern separates `credentials_secret_arn` (Fernet-encrypted) from `config` (plaintext JSONB). GitHub `token` → encrypted credentials; `owner`/`repo` → plaintext config.

daily_sync GitHub branch mirrors the Asana template: read issue state via `GitHubClient.get_issue(number)` (state `"closed"` → GetVul ticket `completed` + linked vuln `REMEDIATED`); when all linked vulns for a ticket are resolved, call `close_issue(number)` + `add_comment(number, "...")`. `get_watchers` stays `[]` (local `ticket_watchers` remain source of truth).

Backend pytest form (per-file + env vars) — see Plan 01. test_github_sync.py touches the DB fixture, use the env-var form.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register GITHUB connector type across all four backend points (D-13, D-11)</name>
  <files>backend/app/connectors/schemas.py, backend/app/connectors/router.py, backend/app/connectors/tester.py, backend/app/connectors/sync.py, backend/tests/test_github_sync.py</files>
  <read_first>
    - backend/app/connectors/schemas.py — the `CONNECTOR_TYPES` dict shape (`ConnectorTypeInfo`), using an existing ticketing entry (ASANA if present, else JIRA) as the config/credential-field template.
    - backend/app/connectors/router.py — `CONNECTOR_CATEGORIES` dict (find where ASANA/JIRA are categorized as ticketing).
    - backend/app/connectors/tester.py — the dispatch table mapping connector_type → test function; the Asana/Jira test funcs as templates.
    - backend/app/connectors/sync.py:40 (`SPECIAL_CONNECTORS`) and L77-82 (the ASANA/JIRA no-data-sync short-circuit).
    - backend/app/ticketing/github_client.py — `GitHubClient.__init__(token, owner, repo)` + `test_connection()`.
  </read_first>
  <behavior>
    - Test asserts `"GITHUB" in SPECIAL_CONNECTORS` and that `run_sync` for a GITHUB connector short-circuits to SUCCESS with no vuln fetch (mirrors ASANA/JIRA).
    - Test asserts `/connectors/types` (or the CONNECTOR_TYPES/CATEGORIES structures) now includes GITHUB with token as a credential field and owner/repo as config fields.
    - Test asserts the tester dispatch resolves GITHUB to a GitHub test path.
  </behavior>
  <action>
    Add a `"GITHUB"` `ConnectorTypeInfo` to CONNECTOR_TYPES in schemas.py: display name "GitHub", credential field `token` (secret), config fields `owner` and `repo` (plaintext). Add GITHUB to CONNECTOR_CATEGORIES in connectors/router.py under the same category ASANA/JIRA use (ticketing). Add a GitHub entry to tester.py's dispatch table that constructs `GitHubClient(token, owner, repo)` from the submitted payload and calls `test_connection()`, returning the standard `{success, message, ...}` shape. Add `"GITHUB"` to `SPECIAL_CONNECTORS` in sync.py L40, and extend the `if connector_config.connector_type in ("ASANA", "JIRA"):` short-circuit at L77 to include `"GITHUB"` (or reference the set) so a GitHub connector logs SUCCESS with `{"message": "GitHub is a ticketing connector, no data sync needed"}`. Create backend/tests/test_github_sync.py with the registration assertions above (the sync-branch behavior comes in Task 2).
  </action>
  <verify>
    <automated>cd backend && export ENCRYPTION_KEY=$(.venv/bin/python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=$(.venv/bin/python -c "import secrets;print(secrets.token_urlsafe(48))") ENVIRONMENT=development && .venv/bin/python -c "from app.connectors.sync import SPECIAL_CONNECTORS; assert 'GITHUB' in SPECIAL_CONNECTORS; from app.connectors.schemas import CONNECTOR_TYPES; assert 'GITHUB' in CONNECTOR_TYPES; print('ok')" && .venv/bin/pytest tests/test_github_sync.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GITHUB" backend/app/connectors/schemas.py backend/app/connectors/router.py backend/app/connectors/tester.py backend/app/connectors/sync.py` shows a hit in each of the four files.
    - GITHUB's schema entry marks `token` as the credential (encrypted) and owner/repo as config.
    - the python -c registration assertion prints `ok`; pytest exits 0.
  </acceptance_criteria>
  <done>GitHub is registered as a creatable ticketing connector in all four backend registration points, with the token on the encrypted credentials side.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GitHub daily_sync branch — inbound state map + outbound auto-close (D-12)</name>
  <files>backend/app/ticketing/daily_sync.py, backend/tests/test_github_sync.py</files>
  <read_first>
    - backend/app/ticketing/daily_sync.py — the existing ASANA branch (~L50) and JIRA branch (~L56, `_sync_jira_tickets` ~L271) as the templates; how it resolves the per-provider connector + client.
    - backend/app/ticketing/service.py:1015-1031 — the Asana auto-close block (all-linked-vulns-resolved → close + comment) to mirror.
    - backend/app/ticketing/github_client.py — `get_issue`, `close_issue`, `add_comment` (from Plan 03), plus how the issue `number` is derived from the stored external ticket URL/ref.
    - backend/app/ticketing/dispatch.py — `build_ticketing_client` for GITHUB.
  </read_first>
  <behavior>
    - RED: given a GitHub-provider ticket whose remote issue is `state:"closed"`, the sync marks the GetVul ticket `completed` and its linked vuln `REMEDIATED`.
    - Given a GitHub ticket whose linked vulns are ALL resolved, the sync PATCHes the issue closed (`close_issue`) and posts a comment (`add_comment`).
    - `get_watchers` is not called for GitHub (stays local); assert no crash on the GitHub path.
  </behavior>
  <action>
    Add a GITHUB branch to daily_sync (mirror the JIRA branch structure): resolve the tenant's GitHub connector + build the dispatched GitHub client, iterate the tenant's `Ticket.provider == "GITHUB"` open tickets, parse the issue `number` from the stored external ref, call `get_issue(number)` and map GitHub `state=="closed"` → GetVul ticket `completed` + linked vuln `REMEDIATED` (reuse the exact state-mapping helper the Asana/Jira branches use). Then apply the auto-close parity: when all linked vulns for a ticket are resolved, call `client.close(...)` (→ `close_issue`) + `client.comment(...)` (→ `add_comment`) exactly as the Asana block at service.py:1015-1031. Do NOT call `get_watchers` for GitHub. Extend test_github_sync.py with MockTransport-backed cases for inbound-close and outbound-auto-close.
  </action>
  <verify>
    <automated>cd backend && export ENCRYPTION_KEY=$(.venv/bin/python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=$(.venv/bin/python -c "import secrets;print(secrets.token_urlsafe(48))") ENVIRONMENT=development && .venv/bin/pytest tests/test_github_sync.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GITHUB" backend/app/ticketing/daily_sync.py` shows the new branch.
    - test asserts inbound: closed GitHub issue → GetVul ticket completed + vuln REMEDIATED.
    - test asserts outbound: all-vulns-resolved → `close_issue` + `add_comment` invoked.
    - pytest exits 0.
  </acceptance_criteria>
  <done>GitHub status-sync + auto-close works end-to-end, mirroring Asana/Jira; REL-05 create+sync both complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Analyst-submitted GitHub connector config (token/owner/repo) → storage | Token is a secret crossing into persistence |
| GetVul backend → GitHub API over TLS | Untrusted remote issue state |
| Tenant A ↔ Tenant B connector configs | Must stay tenant-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-13 | Information Disclosure | GitHub PAT storage | mitigate | `token` stored via the Fernet `credentials_secret_arn` path (encrypted); owner/repo only in plaintext config. Test/inspection confirms token never written to plaintext `config` JSONB |
| T-23-14 | Elevation of Privilege | GitHub connector lookup in daily_sync | mitigate | Reuse the tenant-scoped connector lookup the ASANA/JIRA branches use; no global-by-provider lookup |
| T-23-15 | Tampering | Untrusted GitHub issue `state`/body fed into GetVul state transitions | mitigate | Only the `state` field drives the closed→completed mapping (enum-checked); issue body text is not executed or used as instructions (relevant to the Phase-24 untrusted-text contract, seeded here) |
</threat_model>

<verification>
- `pytest tests/test_github_sync.py -v` green (env-var + per-file form).
- `grep -rn "GITHUB"` shows registration in schemas.py, connectors/router.py, tester.py, sync.py, and daily_sync.py.
- `grep -rn "GitHubClient" backend/app/` now returns hits OUTSIDE github_client.py (the orphan is wired).
- `ruff check . && ruff format --check .` clean for touched files.
</verification>

<success_criteria>
GitHub ticketing works end-to-end (creatable via wizard + create path via Plan 04 + status-sync/auto-close here) with no dead stub — ROADMAP SC#4 (REL-05).
</success_criteria>

<output>
After completion, create `.planning/phases/23-ingestion-reliability-precursor/23-05-SUMMARY.md`
</output>
