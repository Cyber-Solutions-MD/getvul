---
phase: 23-ingestion-reliability-precursor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/connectors/wiz.py
  - backend/app/connectors/rapid7.py
  - backend/app/connectors/nessus.py
  - backend/app/connectors/tester.py
  - backend/app/connectors/schemas.py
  - backend/tests/test_connectors/test_wiz_connector.py
  - backend/tests/test_connectors/test_rapid7_connector.py
autonomous: true
requirements: [REL-01, REL-02, REL-03]
must_haves:
  truths:
    - "Wiz authenticate() returns True on a successful OAuth token fetch, so the sync harness proceeds to fetch_vulnerabilities instead of reporting 'Authentication failed'"
    - "Rapid7Connector() constructs with no arguments and authenticate() returns True after capturing base_url/username/password from credentials+config"
    - "A Wiz/Rapid7 MockTransport integration test runs in CI (no live creds): authenticate -> multi-page pagination -> fetch_vulnerabilities -> NormalizedVulnerability, field-for-field"
    - "verify=False is gone as a hardcoded literal from rapid7.py, nessus.py, and both tester.py connection-test functions; TLS verification defaults ON, opt-out only via config verify_tls:false"
  artifacts:
    - path: "backend/app/connectors/wiz.py"
      provides: "authenticate() -> bool returning True on success"
      contains: "return True"
    - path: "backend/app/connectors/rapid7.py"
      provides: "no-arg __init__ + async def authenticate(self, credentials, config) -> bool + config-driven verify_tls"
      contains: "async def authenticate"
    - path: "backend/tests/test_connectors/test_wiz_connector.py"
      provides: "Wiz MockTransport integration test (REL-01 + REL-03)"
    - path: "backend/tests/test_connectors/test_rapid7_connector.py"
      provides: "Rapid7 MockTransport integration test (REL-02 + REL-03)"
  key_links:
    - from: "backend/app/connectors/sync.py:95"
      to: "connector.authenticate(...) truthiness check (line 96 `if not authed`)"
      via: "bool return contract"
      pattern: "return True"
    - from: "backend/app/connectors/rapid7.py httpx.AsyncClient"
      to: "config verify_tls"
      via: "verify=self.verify_tls"
      pattern: "verify=.*verify_tls"
---

<objective>
Fix the two connector sync bugs (Wiz REL-01, Rapid7 REL-02) end-to-end and close the silent-MITM TLS exposure (D-21) across all four `verify=False` sites, each proven by a CI-runnable `httpx.MockTransport` integration test that doubles as the REL-03 coverage for Wiz and Rapid7.

Purpose: These are the two connectors that cannot complete a sync today. Wiz's `authenticate()` returns `None` (falsy) so a successful auth is treated as failure; Rapid7 cannot even be instantiated (`TypeError` — abstract `authenticate` unimplemented + non-no-arg `__init__`). Both block the grounding data every later AI phase depends on.
Output: Working Wiz + Rapid7 sync paths, a per-connector `verify_tls` config field (default `True`), and two new MockTransport test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ingestion-reliability-precursor/23-CONTEXT.md
@.planning/phases/23-ingestion-reliability-precursor/23-RESEARCH.md

<interfaces>
The base contract every connector must satisfy (backend/app/connectors/base.py):
```python
class BaseConnector(abc.ABC):
    source_name: str
    @abc.abstractmethod
    async def authenticate(self, credentials: dict, config: dict) -> bool: ...   # "Returns True on success"
    @abc.abstractmethod
    async def fetch_vulnerabilities(self) -> list[NormalizedVulnerability]: ...
    async def fetch_misconfigurations(self) -> list: ...   # default no-op
```
The sync harness (backend/app/connectors/sync.py:91-100) constructs every connector no-arg and treats a falsy `authenticate` return as failure:
```python
connector = connector_cls()                                   # no-arg — REL-02 must satisfy this
authed = await connector.authenticate(credentials, connector_config.config or {})
if not authed:                                                # None/False/0/"" => "Authentication failed"
    log.status = "FAILED"; ...
```
NormalizedVulnerability dataclass (mapping target for REL-03 field-for-field assertions) is defined in backend/app/connectors/base.py — see 23-RESEARCH.md "Code Examples".

Backend pytest invocation (mandatory form — per-file, with env vars):
```bash
cd backend && export ENCRYPTION_KEY=$(.venv/bin/python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=$(.venv/bin/python -c "import secrets;print(secrets.token_urlsafe(48))") ENVIRONMENT=development && .venv/bin/pytest tests/test_connectors/test_wiz_connector.py -v
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix Wiz authenticate() return-type wiring + Wiz MockTransport test (REL-01, REL-03)</name>
  <files>backend/app/connectors/wiz.py, backend/tests/test_connectors/test_wiz_connector.py</files>
  <read_first>
    - backend/app/connectors/wiz.py — the whole file, especially `authenticate()` (~line 155, currently `-> None`, no return) and `_paginate` GraphQL cursor loop (~line 228: `after`/`endCursor`/`hasNextPage`).
    - backend/app/connectors/base.py — `authenticate -> bool` contract + `NormalizedVulnerability` dataclass fields.
    - backend/app/connectors/sync.py:91-100 — the `if not authed:` truthiness check that the None return breaks.
    - backend/tests/test_provider_stubs.py AND backend/tests/test_okta_sync.py — the `httpx.MockTransport` conventions to mirror (Variant B `_patch_transport` and the `pages=[...]; calls={"n":0}` multi-page counter idiom), quoted verbatim in 23-RESEARCH.md "Architecture Patterns".
  </read_first>
  <behavior>
    - RED: test asserts `await WizConnector().authenticate(creds, config) is True` against a MockTransport returning a valid OAuth token response.
    - Test asserts `_paginate`/`fetch_vulnerabilities` follows the GraphQL `endCursor`/`hasNextPage` across ≥2 pages to completion (assert the handler was called ≥2 times).
    - Test asserts `fetch_vulnerabilities` maps a fixture response field-for-field into `NormalizedVulnerability` (cve_id, severity, cvss_v3_score, hostname, source_vuln_id at minimum).
    - Test asserts auth FAILURE handling: a 401 token response raises (via `resp.raise_for_status()`), which the harness catches separately — assert the exception propagates from `authenticate`.
  </behavior>
  <action>
    In backend/app/connectors/wiz.py change the `authenticate` signature return annotation from `-> None` to `-> bool`, and add `return True` as the final statement of the success path (after `self._client.headers["Authorization"] = f"Bearer {self._token}"` and `logger.info("wiz.authenticated")`). Do NOT add a try/except that swallows failures — `resp.raise_for_status()` must still raise on a bad token response (the harness's outer `except Exception` handles that path). Grep the file and the rest of `app/connectors/` for any call site that inspects the result of `wiz.authenticate(...)`; the only consumer is `sync.py:95` which already does `if not authed:` — no other call-site change needed, but confirm via grep.
    Write backend/tests/test_connectors/test_wiz_connector.py using the MockTransport pattern from test_provider_stubs.py (fresh `httpx.AsyncClient(transport=httpx.MockTransport(handler))` assigned to the connector's `_client`). Use the `pages=[...]; calls={"n":0}` counter for the two-page GraphQL cursor fixture. Cover: (1) authenticate success returns True, (2) authenticate failure (401) raises, (3) pagination followed to completion (handler call count ≥2), (4) field-for-field `NormalizedVulnerability` mapping.
  </action>
  <verify>
    <automated>cd backend && export ENCRYPTION_KEY=$(.venv/bin/python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=$(.venv/bin/python -c "import secrets;print(secrets.token_urlsafe(48))") ENVIRONMENT=development && .venv/bin/pytest tests/test_connectors/test_wiz_connector.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def authenticate" backend/app/connectors/wiz.py` shows `-> bool`.
    - `grep -n "return True" backend/app/connectors/wiz.py` present inside `authenticate`.
    - test file asserts authenticate returns `True` (`is True`) and asserts handler call count `>= 2` for pagination.
    - the pytest command above exits 0 with ≥4 passing tests.
  </acceptance_criteria>
  <done>Wiz authenticate returns True on success; MockTransport test green covering auth success/failure + multi-page pagination + field-for-field mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rapid7 no-arg __init__ + authenticate() implementation + Rapid7 MockTransport test (REL-02, REL-03)</name>
  <files>backend/app/connectors/rapid7.py, backend/tests/test_connectors/test_rapid7_connector.py</files>
  <read_first>
    - backend/app/connectors/rapid7.py — whole file: `__init__(self, config)` (~line 22-33, captures base_url/username/password), `verify=False` (~line 44), `_paginate` REST `page`/`totalPages` loop (~line 55). No `authenticate()` exists today.
    - backend/app/connectors/base.py — the ABC contract (no `__init__` defined on base) and `NormalizedVulnerability`.
    - backend/app/connectors/qualys.py OR crowdstrike.py — a sibling connector that already implements the no-arg `__init__` + `authenticate(credentials, config) -> bool` pattern, to mirror exactly.
    - backend/tests/test_okta_sync.py — the multi-page `pages=[...]; calls={"n":0}` fake for a REST page/totalPages loop.
  </read_first>
  <behavior>
    - RED (reproduce the bug first): `Rapid7Connector()` currently raises `TypeError: Can't instantiate abstract class Rapid7Connector without an implementation for abstract method 'authenticate'`. Test after fix asserts `Rapid7Connector()` constructs and `await conn.authenticate(creds, config) is True`.
    - Test asserts REST pagination via `page`/`totalPages` followed to completion (handler call count == totalPages).
    - Test asserts field-for-field `NormalizedVulnerability` mapping from a fixture asset/vuln response.
    - Test asserts auth failure (401) returns False or raises consistently with the sibling connector's convention.
  </behavior>
  <action>
    In backend/app/connectors/rapid7.py: change `__init__` to `def __init__(self) -> None: super().__init__()` (no config arg) OR match the exact sibling pattern from qualys.py — set instance attrs to None/defaults. Add `async def authenticate(self, credentials: dict, config: dict) -> bool:` that captures `self.base_url = config["base_url"].rstrip("/")`, `self.username = credentials["username"]`, `self.password = credentials["password"]` (move today's `__init__` capture logic here — note the harness passes decrypted secrets in `credentials`, non-secret settings in `config`; put username/password on the credentials side, base_url on config), constructs `self._client = httpx.AsyncClient(base_url=self.base_url, verify=self.verify_tls, ...)` where `self.verify_tls = config.get("verify_tls", True)`, performs the existing auth probe, and returns `True` on success. Keep the existing `_paginate`/`fetch_vulnerabilities` bodies; only their reliance on `__init__`-set attrs changes to authenticate-set attrs.
    Write backend/tests/test_connectors/test_rapid7_connector.py mirroring Task 1's structure but with Rapid7's `page`/`totalPages` REST pagination fixture. First assert `Rapid7Connector()` constructs without error (the regression for the live TypeError), then auth/pagination/mapping.
  </action>
  <verify>
    <automated>cd backend && export ENCRYPTION_KEY=$(.venv/bin/python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=$(.venv/bin/python -c "import secrets;print(secrets.token_urlsafe(48))") ENVIRONMENT=development && .venv/bin/pytest tests/test_connectors/test_rapid7_connector.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def __init__" backend/app/connectors/rapid7.py` shows a no-arg signature (`def __init__(self)`).
    - `grep -n "async def authenticate" backend/app/connectors/rapid7.py` present, `-> bool`, with `return True`.
    - `python -c "from app.connectors.rapid7 import Rapid7Connector; Rapid7Connector()"` no longer raises (validated inside the test).
    - test asserts handler call count equals the fixture's `totalPages` (>1).
    - the pytest command exits 0.
  </acceptance_criteria>
  <done>Rapid7 constructs no-arg, authenticates, and syncs through paginated fetch; MockTransport test green.</done>
</task>

<task type="auto">
  <name>Task 3: verify_tls config field threaded through all four verify=False sites (D-21)</name>
  <files>backend/app/connectors/rapid7.py, backend/app/connectors/nessus.py, backend/app/connectors/tester.py, backend/app/connectors/schemas.py</files>
  <read_first>
    - backend/app/connectors/nessus.py — `verify=False` (~line 70) in its httpx client construction.
    - backend/app/connectors/tester.py — `test_nessus` `verify=False` (~line 77) and `test_rapid7` `verify=False` (~line 348) "Test Connection" functions.
    - backend/app/connectors/schemas.py — `CONNECTOR_TYPES` dict entries for `"NESSUS"` (~line 59) and `"RAPID7"` (~line 288); `ConnectorTypeInfo` config-field shape used by the add-connector wizard.
    - 23-RESEARCH.md "Pitfall 3" — the four-site enumeration and the Test-Connection-vs-sync divergence warning.
  </read_first>
  <action>
    Replace every hardcoded `verify=False` with `verify=<config>.get("verify_tls", True)` (default ON) in exactly these four places: rapid7.py sync client, nessus.py sync client, tester.py::test_nessus, tester.py::test_rapid7. In tester.py the value comes from the submitted test payload's config dict; read `config.get("verify_tls", True)` there too so the wizard "Test Connection" and the real sync agree. In backend/app/connectors/schemas.py, advertise `verify_tls` (boolean, default True, label "Verify TLS certificate", help "Disable only for on-prem scanners with self-signed/internal-CA certs") as a config field on the `"NESSUS"` and `"RAPID7"` `CONNECTOR_TYPES` entries so the wizard renders the opt-out. Do NOT change any other connector. Extend backend/tests/test_connectors/test_rapid7_connector.py (from Task 2) with one assertion that the client is constructed with `verify=True` when `verify_tls` is absent from config, and `verify=False` only when `config={"verify_tls": False}`.
  </action>
  <verify>
    <automated>cd backend && grep -rn "verify=False" app/connectors/rapid7.py app/connectors/nessus.py app/connectors/tester.py; test $(grep -rc "verify=False" app/connectors/rapid7.py app/connectors/nessus.py app/connectors/tester.py | awk -F: '{s+=$2} END{print s}') -eq 0 && export ENCRYPTION_KEY=$(.venv/bin/python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=$(.venv/bin/python -c "import secrets;print(secrets.token_urlsafe(48))") ENVIRONMENT=development && .venv/bin/pytest tests/test_connectors/test_rapid7_connector.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "verify=False" backend/app/connectors/rapid7.py backend/app/connectors/nessus.py backend/app/connectors/tester.py` returns zero hits.
    - `grep -n "verify_tls" backend/app/connectors/schemas.py` shows it added to NESSUS and RAPID7 entries.
    - test asserts default verify_tls resolves to True (client `verify` is True unless config explicitly sets false).
  </acceptance_criteria>
  <done>All four verify=False sites read config verify_tls (default True); wizard advertises the opt-out for Nessus + Rapid7; regression test pins the default-ON behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GetVul backend → external scanner API (Wiz/Rapid7/Nessus over TLS) | Untrusted network path; scanner responses are untrusted data |
| Connector config (Fernet-encrypted credentials) → connector runtime | Decrypted secrets in memory during authenticate() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering / Information Disclosure | rapid7.py / nessus.py / tester.py `verify=False` | mitigate | Replace hardcoded `verify=False` with `config.get("verify_tls", True)` at all four sites; TLS validation defaults ON, opt-out is explicit per-connector (D-21) — closes silent MITM |
| T-23-02 | Information Disclosure | Wiz/Rapid7 auth exceptions | accept | `raise_for_status()` exceptions propagate to the harness; httpx exception `__str__` does not embed request headers/tokens by default (per 23-RESEARCH Pitfall 4). Secret redaction of the persisted string is owned by Plan 07 (D-19) |
| T-23-03 | Spoofing | Decrypted scanner credentials in authenticate() | accept | Reuses the existing Fernet `get_decrypted_credentials` path unchanged; credentials never logged (only `logger.info("wiz.authenticated")` with no secret payload) |
</threat_model>

<verification>
- `pytest tests/test_connectors/test_wiz_connector.py -v` and `tests/test_connectors/test_rapid7_connector.py -v` both green (env-var + per-file form).
- Zero `verify=False` literals remain in rapid7.py, nessus.py, tester.py.
- `ruff check app/connectors/ && ruff format --check app/connectors/` clean; mypy baseline not regressed for the touched files.
</verification>

<success_criteria>
Wiz and Rapid7 each complete a full simulated sync (authenticate → paginate to completion → fetch_vulnerabilities → NormalizedVulnerability) under MockTransport with no live credentials, and TLS verification is on-by-default everywhere it was silently off. Maps ROADMAP SC#1 (REL-01, REL-02) and the Wiz/Rapid7 slice of SC#2 (REL-03).
</success_criteria>

<output>
After completion, create `.planning/phases/23-ingestion-reliability-precursor/23-01-SUMMARY.md`
</output>
