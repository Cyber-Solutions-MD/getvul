---
phase: 23-ingestion-reliability-precursor
plan: 02
type: execute
wave: 3
depends_on: [01]
files_modified:
  - backend/tests/test_connectors/test_crowdstrike_connector.py
  - backend/tests/test_connectors/test_defender_connector.py
  - backend/tests/test_connectors/test_nessus_connector.py
  - backend/tests/test_connectors/test_qualys_connector.py
autonomous: true
requirements: [REL-03]
must_haves:
  truths:
    - "CrowdStrike, Defender, Nessus, and Qualys each have an HTTP-layer MockTransport test asserting auth success AND failure, multi-page pagination followed to completion, and field-for-field fetch_vulnerabilities -> NormalizedVulnerability mapping"
    - "Each connector's existing retry/rate-limit behavior is pinned (asserted as-is, not changed) per D-22 — including CrowdStrike's documented main-loop-vs-enrichment-batch inconsistency"
    - "The Nessus test asserts verify_tls defaults to True (Plan 01's D-21 change)"
  artifacts:
    - path: "backend/tests/test_connectors/test_crowdstrike_connector.py"
      provides: "CrowdStrike HTTP-layer integration test"
    - path: "backend/tests/test_connectors/test_defender_connector.py"
      provides: "Defender HTTP-layer integration test"
    - path: "backend/tests/test_connectors/test_nessus_connector.py"
      provides: "Nessus HTTP-layer integration test"
    - path: "backend/tests/test_connectors/test_qualys_connector.py"
      provides: "Qualys HTTP-layer integration test"
  key_links:
    - from: "each test file"
      to: "NormalizedVulnerability (backend/app/connectors/base.py)"
      via: "field-for-field assertion on fetch_vulnerabilities output"
      pattern: "NormalizedVulnerability|cve_id"
---

<objective>
Author the four remaining scanner HTTP-layer integration tests (CrowdStrike, Defender, Nessus, Qualys) so all six connectors have MockTransport coverage of auth + pagination + `fetch_vulnerabilities` mapping (REL-03; Wiz and Rapid7 are covered by Plan 01).

Purpose: `backend/tests/test_connector_normalization.py` already tests pure in-memory normalization for all six scanners, but the empty `backend/tests/test_connectors/` directory has NO HTTP-layer coverage — auth, real pagination loops, and end-to-end transport mapping are untested. This is genuinely new test authoring, not augmenting the existing normalization file.
Output: Four new MockTransport test files pinning each connector's current transport behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-ingestion-reliability-precursor/23-CONTEXT.md
@.planning/phases/23-ingestion-reliability-precursor/23-RESEARCH.md

<interfaces>
Each connector's pagination shape (23-RESEARCH.md Architecture Patterns + Open Question 10):
- CrowdStrike: `after` cursor; fetch_vulnerabilities main loop retries 429 UNBOUNDED (`sleep(5); continue`); enrichment batches (`_resolve_devices_batch` etc.) `sleep(3)` on 429 but DO NOT retry (silently drop the batch) — pin BOTH behaviors with a code comment.
- Defender: `nextLink` pagination; `MAX_RETRIES = 3` 429 loop.
- Nessus: scan-export polling loop (not a page cursor — poll until export ready, then download); no 429 handling.
- Qualys: XML pagination; `_retries=3`, retries on `httpx.TimeoutException` AND HTTP 409 (Qualys rate-limit code, not 429), proactive throttle at `X-RateLimit-Remaining <= 2`.

MockTransport conventions to mirror verbatim (23-RESEARCH.md): Variant A (`_mock_client(handler)` → fresh `httpx.AsyncClient(transport=httpx.MockTransport(handler))`) from test_directory_connectors.py; multi-page `pages=[...]; calls={"n":0}` counter from test_okta_sync.py.

Backend pytest form (per-file, env vars):
```bash
cd backend && export ENCRYPTION_KEY=$(.venv/bin/python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=$(.venv/bin/python -c "import secrets;print(secrets.token_urlsafe(48))") ENVIRONMENT=development && .venv/bin/pytest tests/test_connectors/test_crowdstrike_connector.py -v
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CrowdStrike HTTP-layer integration test (REL-03)</name>
  <files>backend/tests/test_connectors/test_crowdstrike_connector.py</files>
  <read_first>
    - backend/app/connectors/crowdstrike.py — `authenticate`, `fetch_vulnerabilities` main `after`-cursor loop, and the enrichment batch helpers (`_resolve_devices_batch`, `_resolve_remediations_batch`, `_resolve_vuln_metadata_batch`, `_resolve_eval_logic_batch`).
    - backend/app/connectors/base.py — `NormalizedVulnerability` fields.
    - backend/tests/test_directory_connectors.py + backend/tests/test_okta_sync.py — MockTransport Variant A + the multi-page counter idiom.
    - backend/tests/test_connector_normalization.py — the existing `test_crowdstrike_normalize_vuln_*` cases, so this HTTP-layer test does not duplicate pure-normalization assertions.
  </read_first>
  <behavior>
    - authenticate success returns True (OAuth2 token endpoint mocked 200) and failure (401) handled per the connector's convention.
    - fetch_vulnerabilities follows the `after` cursor across ≥2 pages (assert handler call count).
    - field-for-field NormalizedVulnerability mapping from a realistic combined vuln+device fixture.
    - a pinned-behavior assertion documenting (with a code comment) that the main loop retries 429 unbounded while enrichment batches drop on 429 — assert current behavior, do NOT fix it (D-22).
  </behavior>
  <action>
    Create backend/tests/test_connectors/test_crowdstrike_connector.py using MockTransport Variant A. Build a two-page `after`-cursor fixture and assert the cursor is followed to completion. Assert the `fetch_vulnerabilities` output maps each fixture field into the exact `NormalizedVulnerability` attribute (cve_id, vulnerability_name, cvss_v3_score, severity, hostname, source_vuln_id, exploit_available, cisa_kev where present in the fixture). Add a test that a 429 on the main pagination loop is retried (mock a 429 then 200) and a comment `# D-22: pinned — main loop retries 429 unbounded; enrichment batches drop on 429 (see 23-RESEARCH Open Question 10)`.
  </action>
  <verify>
    <automated>cd backend && export ENCRYPTION_KEY=$(.venv/bin/python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=$(.venv/bin/python -c "import secrets;print(secrets.token_urlsafe(48))") ENVIRONMENT=development && .venv/bin/pytest tests/test_connectors/test_crowdstrike_connector.py -v</automated>
  </verify>
  <acceptance_criteria>
    - file asserts handler call count ≥2 (pagination to completion) and asserts ≥5 NormalizedVulnerability fields mapped field-for-field.
    - a `# D-22` pinned-behavior comment present.
    - pytest command exits 0.
  </acceptance_criteria>
  <done>CrowdStrike HTTP-layer test green covering auth (success+failure), cursor pagination, mapping, and pinned 429 behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Defender HTTP-layer integration test (REL-03)</name>
  <files>backend/tests/test_connectors/test_defender_connector.py</files>
  <read_first>
    - backend/app/connectors/defender.py — `authenticate`, `nextLink` pagination, `MAX_RETRIES = 3` 429 loop.
    - backend/app/connectors/base.py — NormalizedVulnerability.
    - backend/tests/test_directory_connectors.py — MockTransport Variant A.
    - backend/tests/test_connectors/test_crowdstrike_connector.py (Task 1) — keep structure consistent.
  </read_first>
  <behavior>
    - authenticate success/failure.
    - `nextLink` pagination followed across ≥2 pages to completion.
    - field-for-field NormalizedVulnerability mapping.
    - 429 retried up to MAX_RETRIES=3 (pin): mock two 429s then a 200 and assert success; assert exhausting 3 raises.
  </behavior>
  <action>
    Create backend/tests/test_connectors/test_defender_connector.py using MockTransport Variant A with a two-page `@odata.nextLink` fixture. Assert pagination to completion, field-for-field mapping, and the `MAX_RETRIES=3` 429 behavior (pinned, D-22). Comment the retry-count assertion referencing D-22.
  </action>
  <verify>
    <automated>cd backend && export ENCRYPTION_KEY=$(.venv/bin/python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=$(.venv/bin/python -c "import secrets;print(secrets.token_urlsafe(48))") ENVIRONMENT=development && .venv/bin/pytest tests/test_connectors/test_defender_connector.py -v</automated>
  </verify>
  <acceptance_criteria>
    - asserts `nextLink` followed (handler call count ≥2) and ≥5 fields mapped.
    - asserts the 3-attempt 429 retry bound.
    - pytest exits 0.
  </acceptance_criteria>
  <done>Defender HTTP-layer test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Nessus + Qualys HTTP-layer integration tests (REL-03, incl. Nessus verify_tls default)</name>
  <files>backend/tests/test_connectors/test_nessus_connector.py, backend/tests/test_connectors/test_qualys_connector.py</files>
  <read_first>
    - backend/app/connectors/nessus.py — `authenticate`, the scan-export polling loop (request export → poll status → download), and the verify_tls change from Plan 01 (D-21).
    - backend/app/connectors/qualys.py — `authenticate`, XML pagination, `_retries=3` on TimeoutException + HTTP 409, proactive throttle at `X-RateLimit-Remaining <= 2`.
    - backend/app/connectors/base.py — NormalizedVulnerability.
    - backend/tests/test_okta_sync.py — the `pages=[...]; calls={"n":0}` counter (adapt for Nessus's poll loop and Qualys's XML pages).
  </read_first>
  <behavior>
    - Nessus: authenticate success/failure; the export poll loop iterates until "ready" then downloads (assert ≥2 poll calls before download); field-for-field mapping; assert the httpx client is built with `verify=True` when `verify_tls` absent (Plan 01 default-ON).
    - Qualys: authenticate success/failure; XML pagination to completion; field-for-field mapping; pin the 409 retry + `X-RateLimit-Remaining<=2` throttle behavior (D-22).
  </behavior>
  <action>
    Create both files with MockTransport. For Nessus, model the request-export → poll-status(×N) → download-file sequence with a `calls` counter and assert the poll loop runs to "ready"; add an assertion that `verify_tls` defaults True. For Qualys, build a two-page XML fixture (`httpx.Response(200, content=<xml bytes>)`), assert pagination to completion and field-for-field mapping, and pin the 409-retry behavior with a `# D-22` comment (mock a 409 then 200).
  </action>
  <verify>
    <automated>cd backend && export ENCRYPTION_KEY=$(.venv/bin/python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=$(.venv/bin/python -c "import secrets;print(secrets.token_urlsafe(48))") ENVIRONMENT=development && .venv/bin/pytest tests/test_connectors/test_nessus_connector.py tests/test_connectors/test_qualys_connector.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Nessus test asserts poll loop iterations (≥2) and verify_tls default True.
    - Qualys test asserts XML pagination to completion and pins 409 retry (`# D-22` comment).
    - both files assert ≥5 NormalizedVulnerability fields mapped; pytest exits 0.
  </acceptance_criteria>
  <done>Nessus + Qualys HTTP-layer tests green; all six connectors now have transport-level coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → mocked scanner transport | No real network; fixtures are the only "external" data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Tampering | Test fixtures pinning wrong behavior | accept | Tests pin CURRENT behavior (D-22) with explicit comments; any behavior change is a deliberate future decision, not silent drift. No production code changes in this plan |
| T-23-05 | Information Disclosure | Fixture data resembling real scanner tokens | accept | Fixtures use obviously-fake values (`"fake-token"`); no real credentials in CI (BYOK, no creds in CI per D-03) |
</threat_model>

<verification>
- All four new test files pass under the env-var + per-file pytest form.
- `ruff check tests/test_connectors/ && ruff format --check tests/test_connectors/` clean.
- Combined with Plan 01, `ls backend/tests/test_connectors/test_*_connector.py` lists exactly six files.
</verification>

<success_criteria>
All six scanner connectors (CrowdStrike, Nessus, Defender, Wiz, Qualys, Rapid7) have HTTP-layer integration tests covering auth, pagination, and fetch_vulnerabilities mapping against a mocked transport — ROADMAP SC#2 (REL-03).
</success_criteria>

<output>
After completion, create `.planning/phases/23-ingestion-reliability-precursor/23-02-SUMMARY.md`
</output>
