---
phase: 16-light-theme-visual-completion
plan: 03
type: execute
wave: 1
depends_on: []
gap_closure: true
closes_gaps:
  - "16-VERIFICATION.md truth #2: light-theme axe sweep never executed (SC#2 / UX-D-03-05)"
  - "16-VERIFICATION.md truth #3: --color-info not overridden in light (WR-01) + text-amber call sites not migrated to --color-amber-on-soft (WR-02) (SC#2/SC#3 / UX-D-03-02/03)"
files_modified:
  - frontend/src/app/globals.css
  - .claude/skills/sketch-findings-getvul/sources/themes/sunset.css
  - .claude/skills/sketch-findings-getvul/references/foundation.md
  - frontend/src/components/tickets/status-pill.tsx
  - frontend/src/components/settings/profile-pane.tsx
  - frontend/src/components/tickets/status-pill.test.tsx
  - frontend/e2e/a11y-routes.spec.ts
autonomous: false
requirements:
  - UX-D-03-02
  - UX-D-03-03
  - UX-D-03-04
  - UX-D-03-05
user_setup: []

must_haves:
  truths:
    - "The :root[data-theme=\"light\"] block in globals.css overrides --color-info to #2563EB (blue-600), and the design-system skill mirrors it (sunset.css light block + foundation.md annotation) — SourcePill google/azure/humaans text-info pills clear 4.5:1 on cream"
    - "No component in frontend/src renders text-amber on an -soft/amber fill — every amber-on-fill text site (status-pill in_progress/'in progress', profile-pane ANALYST badge) uses text-[var(--color-amber-on-soft)], and the status-pill unit test asserts it; the --color-amber-on-soft token is no longer dead"
    - "Dark mode is byte-identical after the amber migration: dark --color-amber-on-soft = #F59E0B = the old text-amber value, so the swap is a visual no-op in dark mode"
    - "The light-theme axe describe block has been EXECUTED against a live Next.js production build after the token/component fixes landed, and both the dark and light describe blocks report 0 critical/serious violations on every route, with recorded evidence dated after this plan's fix commits"
  artifacts:
    - path: "frontend/src/app/globals.css"
      provides: "--color-info light override inside the single :root[data-theme=\"light\"] block"
      contains: "--color-info: #2563EB"
    - path: ".claude/skills/sketch-findings-getvul/sources/themes/sunset.css"
      provides: "--color-info light override in the skill's :root[data-theme=\"light\"] block (design-system single source of truth)"
      contains: "--color-info: #2563EB"
    - path: "frontend/src/components/tickets/status-pill.tsx"
      provides: "in_progress + 'in progress' status pills consuming var(--color-amber-on-soft)"
      contains: "text-[var(--color-amber-on-soft)]"
    - path: "frontend/src/components/settings/profile-pane.tsx"
      provides: "ANALYST role badge consuming var(--color-amber-on-soft)"
      contains: "text-[var(--color-amber-on-soft)]"
    - path: "frontend/src/components/tickets/status-pill.test.tsx"
      provides: "in_progress assertion locking the amber-on-soft var() migration"
      contains: "text-[var(--color-amber-on-soft)]"
  key_links:
    - from: "frontend/src/components/users/source-pill.tsx (google/azure/humaans)"
      to: "frontend/src/app/globals.css :root[data-theme=\"light\"] --color-info"
      via: "Tailwind text-info utility resolving var(--color-info) through the CSS cascade under data-theme=light"
      pattern: "--color-info:\\s*#2563EB"
    - from: "frontend/src/components/tickets/status-pill.tsx (in_progress)"
      to: "frontend/src/app/globals.css :root[data-theme=\"light\"] --color-amber-on-soft"
      via: "Tailwind JIT var() reference resolving through the CSS variable cascade"
      pattern: "text-\\[var\\(--color-amber-on-soft\\)\\]"
    - from: "frontend/src/components/settings/profile-pane.tsx (ANALYST)"
      to: "frontend/src/app/globals.css :root[data-theme=\"light\"] --color-amber-on-soft"
      via: "Tailwind JIT var() reference resolving through the CSS variable cascade"
      pattern: "text-\\[var\\(--color-amber-on-soft\\)\\]"
    - from: "frontend/e2e/a11y-routes.spec.ts light describe"
      to: "globals.css light-mode token block (post-fix)"
      via: "axe color-contrast rule measuring computed values under data-theme=light against a live prod build"
      pattern: "data-theme.*light"
---

<objective>
Close the two Phase-16 verification gaps that block the phase goal ("every authenticated route is
visually correct and WCAG 2.1 AA in light mode — verified"). Plans 16-01 and 16-02 shipped the ~20
token overrides, the violet/pink on-soft migration, the skill reconciliation, and the enabled Light
toggle. Two contrast failures and one process gap remain:

- **WR-01** — `--color-info` was never overridden in the light block; it stays at the dark value
  `#60A5FA` (blue-400, ~2.5:1 on cream) and is consumed as small `text-xs font-mono` by SourcePill
  on `/dashboard/users` (a STATIC_ROUTE swept by the light axe describe).
- **WR-02** — the `--color-amber-on-soft` token (`#92400E`) is defined but DEAD; three code sites
  still use `text-amber` (base accent `#F59E0B`, ~1.9:1 on cream). The violet/pink migration was
  done in 16-01 but amber was missed.
- **SC#2 process gap** — the light-theme axe describe block was scaffolded (16-01) but NEVER
  executed against a live server; the most recent playwright-report predates Phase 16 by 15 days.

This plan is CSS-token + class-string surgery plus one executed axe run. It re-plans NOTHING from
16-01/16-02 — it only closes the confirmed gaps. Tasks are ordered so the axe sweep (Task 3) runs
LAST, after the info override (Task 1) and amber migrations (Task 2) land — running it earlier
produces the known SourcePill + amber failures.

Purpose: closes UX-D-03-02, UX-D-03-03, UX-D-03-04, UX-D-03-05 — flips the phase to verified.
Output: --color-info light override (app + skill), amber-on-soft consumed at all three sites,
and a recorded green light+dark axe run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-light-theme-visual-completion/16-VERIFICATION.md
@.planning/phases/16-light-theme-visual-completion/16-REVIEW.md
@.planning/phases/16-light-theme-visual-completion/16-01-SUMMARY.md
@.claude/skills/sketch-findings-getvul/references/foundation.md

<interfaces>
<!-- Exact current state the executor edits against. Line numbers verified 2026-07-16. -->

globals.css light block — `--color-info` is ABSENT. The severity block currently reads
(lines 27-37); insert the `--color-info` override alongside the semantic-state group, mirroring
`--color-severity-info: #2563EB` which is already present at line 32:
```css
  /* Severity — darker same-hue variants for AA on cream (UX-D-03-03) */
  --color-severity-critical: #DC2626;
  --color-severity-high:     #EA580C;
  --color-severity-medium:   #B45309;
  --color-severity-low:      #7C3AED;
  --color-severity-info:     #2563EB;  /* blue-600 */

  /* Semantic states (UX-D-03-02) */
  --color-danger:  #DC2626;
  --color-success: #15803D;
  --color-warning: #B45309;
  /* <-- add --color-info here (mirrors --color-severity-info) */
```

skill sunset.css light block (lines 145-176) — `--color-info` is ABSENT. Its semantic-state group
is lines 153-156. The dark base `--color-info: #60A5FA` lives in the :root dark block above and in
foundation.md line 48. Add the light override to the skill's light block to keep it the single
source of truth.

foundation.md line 48 currently: `--color-info:      #60A5FA;` — the ONLY semantic-state token
NOT carrying a `/* light: ... */` annotation (danger/success/warning at lines 45-47 all do). Add
the light annotation.

status-pill.tsx (lines 39-46) — the two amber entries still use base `text-amber`:
```tsx
  in_progress: {
    classes: 'border-amber/40 bg-amber/10 text-amber',
    label: 'In progress',
  },
  'in progress': {
    classes: 'border-amber/40 bg-amber/10 text-amber',
    label: 'In progress',
  },
```
(The `open` entry at line 36 already uses `text-[var(--color-violet-on-soft)]` — mirror that.)

profile-pane.tsx roleBadgeClass (lines 59-64) — ANALYST still uses base `text-amber`:
```tsx
  const map: Record<string, string> = {
    OWNER: 'bg-pink-soft text-[var(--color-pink-on-soft)]',
    ADMIN: 'bg-violet-soft text-[var(--color-violet-on-soft)]',
    ANALYST: 'bg-amber-soft text-amber',
    VIEWER: 'bg-surface-2 text-text-muted',
  };
```

status-pill.test.tsx line 35 (in_progress test, lines 30-36) still asserts base `text-amber`:
```tsx
    expect(pill.className).toContain('text-amber');
```

Token values (authoritative, from 16-01-SUMMARY.md — do NOT re-derive):
- `--color-info` light = `#2563EB` (blue-600) — mirrors severity-info, ~5.1:1 on #FAF7F2.
- `--color-amber-on-soft` dark = `#F59E0B` (= old text-amber, so dark is a no-op), light = `#92400E`
  (amber-800). Already defined in globals.css:54 and skill sunset.css:172 — this plan only wires
  consumers to it; the token declaration is unchanged.

Local axe-sweep invocation (from memory note "Local e2e + perf gate setup" — admin user, :3000
CORS, prod build, kill next-server children). Run from `frontend/`:
`npx playwright test e2e/a11y-routes.spec.ts --project=chromium-a11y`
Both describe blocks must report 0 critical/serious violations:
  - "WCAG 2.1 AA axe sweep — all routes (blocking)" (dark)
  - "WCAG 2.1 AA axe sweep — light theme (blocking)" (light)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Override --color-info for light mode (globals.css) + mirror into the skill (WR-01)</name>
  <read_first>
    - frontend/src/app/globals.css lines 8-60 (the single :root[data-theme="light"] block; --color-severity-info at line 32 is the mirror target; --color-info is absent)
    - .claude/skills/sketch-findings-getvul/sources/themes/sunset.css lines 145-176 (the skill's light block; semantic-state group lines 153-156; --color-info absent)
    - .claude/skills/sketch-findings-getvul/references/foundation.md lines 44-48 (semantic-state token list; line 48 --color-info is the only one without a /* light: */ annotation)
    - frontend/src/components/users/source-pill.tsx lines 20-26 (the text-info consumer — google/azure/humaans pills, text-xs font-mono)
  </read_first>
  <files>frontend/src/app/globals.css, .claude/skills/sketch-findings-getvul/sources/themes/sunset.css, .claude/skills/sketch-findings-getvul/references/foundation.md</files>
  <action>
    Three edits, all mechanical. Do NOT touch any other token.

    1. globals.css — INSIDE the existing `:root[data-theme="light"]` block, immediately after the
       `--color-warning: #B45309;` line (currently line 37), append (do NOT create a second selector
       block; keep `color-scheme: light;` last):
       ```css
         --color-info:    #2563EB;  /* blue-600 — was dark #60A5FA (blue-400, fails 4.5:1 on cream); mirrors --color-severity-info (WR-01) */
       ```

    2. skill sunset.css — INSIDE the `:root[data-theme="light"]` block (lines 145-176), immediately
       after `--color-warning: #B45309; ...` (line 156), append the mirrored override:
       ```css
         --color-info:     #2563EB;  /* was dark #60A5FA — blue-600, ~5.1:1 on cream; mirrors severity-info (WR-01) */
       ```

    3. foundation.md — on line 48, replace:
       - FROM: `--color-info:      #60A5FA;`
       - TO:   `--color-info:      #60A5FA;  /* light: #2563EB (blue-600, ~5.1:1 on cream — consumed by SourcePill text-info) */`

    Rationale: SourcePill renders `text-info` (text-xs font-mono) for google/azure/humaans source
    pills on `/dashboard/users`. Under data-theme=light the unoverridden `--color-info` stayed at
    the dark `#60A5FA` (~2.5:1 on #FAF7F2), failing AA. `#2563EB` is the same value already used for
    `--color-severity-info` in the light block (16-01), so it is design-system-consistent, not a new
    freehand hex.
  </action>
  <verify>
    <automated>cd frontend && grep -q -- '--color-info:.*#2563EB' src/app/globals.css && grep -q ':root\[data-theme="light"\]' src/app/globals.css && grep -q -- '--color-info:.*#2563EB' ../.claude/skills/sketch-findings-getvul/sources/themes/sunset.css && grep -qi 'light: #2563EB' ../.claude/skills/sketch-findings-getvul/references/foundation.md && node scripts/check-bundle-all.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ':root\[data-theme="light"\]' frontend/src/app/globals.css` returns 1 (single block — the override was appended, not put in a new selector).
    - `grep -n -- '--color-info:[[:space:]]*#2563EB' frontend/src/app/globals.css` matches exactly once, and the match is between the `:root[data-theme="light"]` line and its closing `color-scheme: light;`.
    - `grep -n -- '--color-info:[[:space:]]*#2563EB' .claude/skills/sketch-findings-getvul/sources/themes/sunset.css` matches inside the skill's `:root[data-theme="light"]` block.
    - `grep -n 'light: #2563EB' .claude/skills/sketch-findings-getvul/references/foundation.md` matches (line 48 annotated).
    - No freehand hex added outside a `--color-*` declaration.
    - `node scripts/check-bundle-all.mjs` (from frontend/) passes — 0 First-Load-JS delta (CSS-only).
  </acceptance_criteria>
  <done>--color-info overridden to #2563EB in the globals.css light block and mirrored in the skill's sunset.css light block; foundation.md line 48 annotated with the light value; single light block preserved; bundle budget unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the three text-amber call sites to text-[var(--color-amber-on-soft)] + update the unit test (WR-02)</name>
  <read_first>
    - frontend/src/components/tickets/status-pill.tsx lines 39-46 (in_progress + 'in progress' entries using text-amber) — the open entry (line 36) already uses the var() pattern to mirror
    - frontend/src/components/settings/profile-pane.tsx lines 49-66 (roleBadgeClass — ANALYST line 62 uses text-amber; OWNER/ADMIN lines 60-61 already migrated)
    - frontend/src/components/tickets/status-pill.test.tsx lines 30-36 (the in_progress test asserting text-amber on line 35)
    - .claude/skills/sketch-findings-getvul/references/visual-language.md lines 100-120 ("Text on -soft fills" — the rule this migration satisfies; amber-on-soft light = #92400E already documented at line 118)
  </read_first>
  <files>frontend/src/components/tickets/status-pill.tsx, frontend/src/components/settings/profile-pane.tsx, frontend/src/components/tickets/status-pill.test.tsx</files>
  <action>
    Four literal swaps. Keep `border-amber/40` and `bg-amber/10` (and `bg-amber-soft`) unchanged —
    only the TEXT token lifts (per the design-system "-on-soft" rule; border/dot/fill stay base
    accent). The `--color-amber-on-soft` token is already declared in globals.css:54
    (light #92400E) and skill sunset.css:172 — this task only wires consumers to it.

    1. status-pill.tsx — `STATUS_MAP.in_progress.classes` (line 40):
       - FROM: `classes: 'border-amber/40 bg-amber/10 text-amber',`
       - TO:   `classes: 'border-amber/40 bg-amber/10 text-[var(--color-amber-on-soft)]',`

    2. status-pill.tsx — `STATUS_MAP['in progress'].classes` (line 44):
       - FROM: `classes: 'border-amber/40 bg-amber/10 text-amber',`
       - TO:   `classes: 'border-amber/40 bg-amber/10 text-[var(--color-amber-on-soft)]',`

       Add/extend the comment above STATUS_MAP (or inline) noting In-progress now resolves through
       the token for both themes (dark #F59E0B — byte-identical to the old text-amber; light #92400E
       via the globals.css light override), mirroring the Open-pill treatment.

    3. profile-pane.tsx — ANALYST entry in roleBadgeClass (line 62):
       - FROM: `ANALYST: 'bg-amber-soft text-amber',`
       - TO:   `ANALYST: 'bg-amber-soft text-[var(--color-amber-on-soft)]',`

       Extend the comment block (lines 56-58) to note ANALYST now also resolves via the token
       cascade (dark #F59E0B no-op; light #92400E), completing the on-soft migration alongside
       OWNER/ADMIN.

    4. status-pill.test.tsx — in_progress assertion (line 35):
       - FROM: `expect(pill.className).toContain('text-amber');`
       - TO:   `expect(pill.className).toContain('text-[var(--color-amber-on-soft)]');`
       Update any adjacent inline comment referencing `text-amber` to reference `--color-amber-on-soft`.

    Dark-mode no-op guarantee: the dark `--color-amber-on-soft` is `#F59E0B` (= the old `text-amber`
    value), so these swaps render byte-identically in dark mode — call this out in the acceptance
    criteria / SUMMARY. Do NOT change border-amber/40, bg-amber/10, or bg-amber-soft anywhere.
    After this task, no `text-amber` (base accent) remains on any amber-fill text in src/.
  </action>
  <verify>
    <automated>cd frontend && ! grep -rn "text-amber'" src/components/tickets/status-pill.tsx && ! grep -rn "text-amber'" src/components/settings/profile-pane.tsx && grep -c "text-\[var(--color-amber-on-soft)\]" src/components/tickets/status-pill.tsx | grep -q 2 && grep -q "text-\[var(--color-amber-on-soft)\]" src/components/settings/profile-pane.tsx && grep -q "text-\[var(--color-amber-on-soft)\]" src/components/tickets/status-pill.test.tsx && npx vitest run src/components/tickets/status-pill.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "text-amber\b" frontend/src/components/tickets/status-pill.tsx frontend/src/components/settings/profile-pane.tsx` returns NOTHING (no base text-amber text token remains; `border-amber`/`bg-amber`/`bg-amber-soft` are still present and unchanged).
    - `grep -c "text-\[var(--color-amber-on-soft)\]" frontend/src/components/tickets/status-pill.tsx` returns 2 (both in_progress + 'in progress').
    - `grep -n "text-\[var(--color-amber-on-soft)\]" frontend/src/components/settings/profile-pane.tsx` matches (ANALYST badge).
    - `grep -n "text-\[var(--color-amber-on-soft)\]" frontend/src/components/tickets/status-pill.test.tsx` matches; `grep -n "toContain('text-amber')" frontend/src/components/tickets/status-pill.test.tsx` returns NOTHING.
    - `grep -n "border-amber/40" frontend/src/components/tickets/status-pill.tsx` still matches twice and `grep -n "bg-amber-soft" frontend/src/components/settings/profile-pane.tsx` still matches (fills/borders unchanged — only text lifted).
    - `npx vitest run src/components/tickets/status-pill.test.tsx` (from frontend/) passes all 7 assertions.
    - `cd frontend && npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>All three text-amber call sites consume text-[var(--color-amber-on-soft)]; the unit test asserts the var() class; --color-amber-on-soft is no longer dead; fills/borders unchanged; dark mode is a byte-identical no-op; unit + type-check green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Execute the light + dark axe sweep against a live prod build and record green evidence (SC#2 / UX-D-03-05)</name>
  <read_first>
    - frontend/e2e/a11y-routes.spec.ts lines 21-135 (both describe blocks — dark "all routes (blocking)" and light "light theme (blocking)"; already wired, do NOT modify)
    - MEMORY note "Local e2e + perf gate setup" (getvul-local-e2e-perf-gate.md — admin user, :3000 CORS, prod build, kill next-server children) for the exact local invocation
    - MEMORY note "Axe sweep not run during execution" (getvul-axe-sweep-not-run-during-exec.md — the exact process gap the verifier flagged; do NOT claim AA without a real run)
    - 16-VERIFICATION.md "Human Verification Required" section #1 (the run recipe and the AFTER-fix ordering note)
  </read_first>
  <files>frontend/playwright-report/index.html</files>
  <action>
    Human-verify checkpoint: this cannot be fully automated because the sweep needs a running
    Next.js production build PLUS a backend with seed data and an admin user. Claude automates
    everything it CAN — build the frontend prod bundle, start the server, run the spec if the
    backend is reachable — then presents the run output for human confirmation. If the
    backend/prod-build environment is unavailable in the sandbox, hand the human the exact command
    sequence in <how-to-verify> to run locally and paste the result. Do NOT modify
    e2e/a11y-routes.spec.ts (it was already correctly wired in 16-01), and do NOT claim SC#2
    verified without a real green run — that is the exact process gap the verifier flagged.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test e2e/a11y-routes.spec.ts --project=chromium-a11y</automated>
  </verify>
  <done>The light + dark axe sweep ran against a live prod build after Tasks 1+2; both describe blocks report 0 critical/serious violations on every route; fresh evidence (regenerated playwright-report or pasted console output) is recorded in 16-03-SUMMARY.md, dated after the fix commits.</done>
  <what-built>
    Tasks 1 and 2 landed the last two light-mode contrast fixes (--color-info override + amber-on-soft
    migration). The light-theme axe describe block in e2e/a11y-routes.spec.ts (added in 16-01) is
    structurally correct but was NEVER executed against a live server — the only playwright-report on
    disk predates Phase 16 by 15 days. This checkpoint executes the sweep so SC#2's "0 critical/serious
    violations on every route" is a verified fact, not a claim. It MUST run AFTER Tasks 1+2 (running
    before produces the known SourcePill text-info and amber text failures).
  </what-built>
  <how-to-verify>
    Run from `frontend/` (per the "Local e2e + perf gate setup" memory note — admin user seeded, :3000
    CORS, prod build, kill stray next-server children between runs):

    1. Build + serve the frontend production bundle and ensure the backend is up with seed data:
       ```bash
       # in getvul/ — backend + deps (Docker or local), seeded with the admin user
       # then, in frontend/:
       npm run build
       npm start &          # serves the prod build on :3000
       ```
    2. Run BOTH describe blocks (the project chromium-a11y project runs the whole spec):
       ```bash
       cd frontend
       npx playwright test e2e/a11y-routes.spec.ts --project=chromium-a11y
       ```
    3. Confirm BOTH describe blocks report 0 critical/serious violations on every route:
       - "WCAG 2.1 AA axe sweep — all routes (blocking)" (dark) — expect PASS
       - "WCAG 2.1 AA axe sweep — light theme (blocking)" (light) — expect PASS
       If any route fails, the console.error output names the failing rule + route + node count.
    4. Teardown: kill the `next-server` child processes (per the memory note) so a stale server
       doesn't poison the next run:
       ```bash
       pkill -f next-server || true
       ```
    5. Record the evidence: EITHER commit the fresh `frontend/playwright-report/` (its index.html
       dated after the Task-1/Task-2 fix commits) OR paste the terminal output showing both describe
       blocks green (0 violations on every route) into the SUMMARY.

    Expected outcome: all tests green; a playwright-report dated 2026-07-16 or later, OR pasted
    console output with 0 critical/serious violations on both describe blocks.
  </how-to-verify>
  <acceptance_criteria>
    - The command `npx playwright test e2e/a11y-routes.spec.ts --project=chromium-a11y` (from frontend/) was run against a LIVE prod build after Tasks 1+2 committed.
    - BOTH describe blocks report 0 critical/serious axe violations on every route (dark AND light).
    - Recorded evidence exists dated AFTER this plan's fix commits: either a regenerated frontend/playwright-report/index.html (mtime after the Task-1/Task-2 commits) OR pasted console output in 16-03-SUMMARY.md showing both describe blocks green.
    - The SUMMARY explicitly names the two describe blocks and states "0 critical/serious on every route" — no unproven AA claim.
    - `frontend/e2e/a11y-routes.spec.ts` was NOT modified by this plan (the spec was already correct in 16-01; `git diff --name-only` for this plan does not list it unless a genuine spec bug surfaced during the run — if so, document the fix in the SUMMARY).
  </acceptance_criteria>
  <resume-signal>Type "approved" with the pasted green run output (or confirm the fresh playwright-report is committed), or describe the violations the run surfaced so they can be fixed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary is crossed. This plan changes CSS custom-property values (one `--color-info`
override, mirrored into a documentation copy), three Tailwind class strings, one unit-test
assertion, and executes an existing e2e spec. There is no new input handling, endpoint, data flow,
authentication/authorization surface, or cryptographic operation. The `data-theme` attribute is a
pre-existing client-only presentation flag, unchanged by this plan.

## STRIDE Threat Register

Per 16-RESEARCH.md § Security Domain and the 16-01/16-02 threat-model disposition, ASVS is
appropriately skipped: CSS token + class-string migrations + a test assertion + an e2e run, with
zero new data handling, endpoints, auth surface, or crypto. No threats are invented to fill the
table.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering | `data-theme` attribute / `getvul_theme` localStorage | accept | Client-only presentation flag; worst case a user sees their own preferred palette. No integrity or confidentiality impact. Pre-existing mechanism, unchanged by this gap-closure plan. Mirrors the 16-01/16-02 disposition. |
</threat_model>

<verification>
- `grep -n -- '--color-info:[[:space:]]*#2563EB' frontend/src/app/globals.css` — light info override present (Task 1).
- `grep -n -- '--color-info:[[:space:]]*#2563EB' .claude/skills/sketch-findings-getvul/sources/themes/sunset.css` — skill mirror present (Task 1).
- `! grep -rnE "text-amber\b" frontend/src/components/tickets/status-pill.tsx frontend/src/components/settings/profile-pane.tsx` — no base text-amber text token remains (Task 2).
- `cd frontend && npx vitest run src/components/tickets/status-pill.test.tsx` — 7/7 green with the updated amber assertion (Task 2).
- `cd frontend && node scripts/check-bundle-all.mjs` — all routes ≤250 KB; 0 First-Load-JS delta.
- `cd frontend && npx playwright test e2e/a11y-routes.spec.ts --project=chromium-a11y` against a live prod build — BOTH the dark and light describe blocks green (0 critical/serious on every route), with recorded evidence dated after the fix commits (Task 3).
</verification>

<success_criteria>
- `--color-info` overridden to `#2563EB` in the light block (globals.css) and mirrored into the skill (sunset.css + foundation.md) — SourcePill text-info pills clear AA on cream (UX-D-03-02).
- All amber-on-fill text (status-pill in_progress/'in progress', profile-pane ANALYST) resolves through `--color-amber-on-soft`; the token is no longer dead; dark mode is a byte-identical no-op (UX-D-03-02/03).
- The light + dark axe sweep executed against a live prod build reports 0 critical/serious violations on every route, with recorded evidence dated after the fix commits (UX-D-03-05).
- text-faint/muted + on-soft tokens (now including info) pass AA on light; design-system skill carries the info light value (UX-D-03-04 reconciliation).
- Unit suite green; bundle budget unchanged (CSS + class-string + test only).
</success_criteria>

<output>
After completion, create `.planning/phases/16-light-theme-visual-completion/16-03-SUMMARY.md`.
CRITICAL: paste (or reference the committed report for) the axe-sweep run output showing BOTH the
dark and light describe blocks reporting 0 critical/serious violations on every route — this is the
evidence that closes the SC#2 process gap the verifier flagged. Record the run date and confirm it
is after the Task-1/Task-2 fix commits.
</output>
