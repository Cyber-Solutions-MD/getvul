---
phase: 16-light-theme-visual-completion
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - .claude/skills/sketch-findings-getvul/sources/themes/sunset.css
  - .claude/skills/sketch-findings-getvul/references/foundation.md
  - .claude/skills/sketch-findings-getvul/references/visual-language.md
  - frontend/src/components/shell/user-chip.tsx
autonomous: true
requirements:
  - UX-D-03-04
user_setup: []

must_haves:
  truths:
    - "The design-system skill carries the canonical light-mode token values alongside the dark values — a future contributor reading foundation.md/visual-language.md/sunset.css sees the correct light-mode behavior of severity, semantic-state, glow, shadow, and -on-soft tokens (BL-04 mirror for light mode)"
    - "The Theme: Light radio in the user chip is enabled (no disabled attr, no 'In progress' badge) — the user can actually switch to the now-complete light theme"
    - "No regression: the dark + light axe sweeps, reduced-motion, smoke, and unit suites stay green; 0 First-Load-JS delta"
  artifacts:
    - path: ".claude/skills/sketch-findings-getvul/sources/themes/sunset.css"
      provides: "A light-mode overrides section documenting the axe-confirmed light token values"
      contains: "data-theme=\"light\""
    - path: ".claude/skills/sketch-findings-getvul/references/foundation.md"
      provides: "Token tables annotated with the light-mode value alongside the dark value"
      contains: "light"
    - path: ".claude/skills/sketch-findings-getvul/references/visual-language.md"
      provides: "A Light-mode variants subsection under 'Text on -soft fills' + severity, analogous to BL-04"
      contains: "Light-mode"
    - path: "frontend/src/components/shell/user-chip.tsx"
      provides: "Enabled Light theme radio (disabled attr + In progress badge removed)"
      contains: "value=\"light\""
  key_links:
    - from: ".claude/skills/sketch-findings-getvul/references/foundation.md light-mode token values"
      to: "frontend/src/app/globals.css :root[data-theme=\"light\"] (Plan 16-01)"
      via: "the SUMMARY-recorded canonical hex values copied verbatim into the skill"
      pattern: "light"
    - from: "frontend/src/components/shell/user-chip.tsx Light radio"
      to: "useTheme setTheme('light')"
      via: "DropdownMenuRadioItem value=\"light\" with disabled removed"
      pattern: "value=\"light\""
---

<objective>
Close the loop on the light theme: reconcile the axe-confirmed light-mode token values into the
design-system skill (so the source palette carries light values, not just the app layer), and
enable the user-facing Theme: Light toggle that was held `disabled` until light mode was complete.

This is the BL-04 reconciliation pattern applied to light mode: Plan 16-01 fixed the tokens in
`globals.css` (the app layer) and proved them with axe; this plan writes those canonical values
back into `sketch-findings-getvul` (`sunset.css` + `foundation.md` + `visual-language.md`) so the
design system stays the single source of truth — and flips the toggle so users can use it.

Purpose: satisfies the reconciliation half of UX-D-03-04 (source-palette changes reconciled into
the design system per BL-04) and ships the theme as user-selectable.
Output: skill files carry light-mode token values; Light radio enabled; suites green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-light-theme-visual-completion/16-01-SUMMARY.md
@.claude/skills/sketch-findings-getvul/references/foundation.md
@.claude/skills/sketch-findings-getvul/references/visual-language.md

<interfaces>
<!-- The SOURCE of truth for the values this plan writes: 16-01-SUMMARY.md records the FINAL
     axe-confirmed light-mode hex for every overridden token. Copy those verbatim — do NOT
     re-derive or re-guess. If a token's final value in the SUMMARY differs from Plan 01's
     candidate starting points, the SUMMARY value wins. -->

Existing BL-04 dark-mode reconciliation precedent to mirror:
- sunset.css lines 18-20: `--color-text-faint: #8B84A8; /* AA-lifted (was #6B6488 ...) */`
- sunset.css lines 31-37: the `-on-soft` block with per-token comments explaining the AA rule.
- visual-language.md lines 86-96: the "### Text on -soft fills (AA — locked)" subsection with a
  fill→text-token→hex table. This plan adds a "Light-mode variants" analogue.
- foundation.md lines 24-25: `--color-text-faint: #8B84A8; /* AA-lifted; was #6B6488 (failed 4.5:1 on dark) */`
- foundation.md lines 33-35: the on-soft comment block.

user-chip.tsx current Light radio (lines 59-77) — the disabled + In-progress badge to remove:
```tsx
{/* Theme radio per D-38. Light is disabled until D-06 / UX-D-03 ships
    light-mode overrides for severity / accent / danger tokens. WR-03. */}
<DropdownMenuRadioItem
  value="light"
  disabled
  aria-description="Light theme is not ready yet — only surface tokens swap; severity, accent, and danger colors stay dark-tuned."
>
  <span className="flex w-full items-center justify-between gap-3">
    <span>{'Theme: Light'}</span>
    <span className="text-[10px] uppercase tracking-wide text-text-faint">
      {'In progress'}
    </span>
  </span>
</DropdownMenuRadioItem>
```
The Dark radio directly above it is the target shape: `<DropdownMenuRadioItem value="dark">{'Theme: Dark'}</DropdownMenuRadioItem>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the axe-confirmed light-mode token values into the design-system skill (BL-04 mirror)</name>
  <read_first>
    - .planning/phases/16-light-theme-visual-completion/16-01-SUMMARY.md (the FINAL axe-confirmed light hex values — the authoritative source for this task)
    - .claude/skills/sketch-findings-getvul/sources/themes/sunset.css lines 4-121 (the :root dark block + BL-04 on-soft comment style to mirror)
    - .claude/skills/sketch-findings-getvul/references/foundation.md lines 9-58 + 121-131 (color tokens, severity, shadows/glow tables)
    - .claude/skills/sketch-findings-getvul/references/visual-language.md lines 1-96 (severity table + "Text on -soft fills (AA — locked)" subsection)
    - 16-RESEARCH.md § BL-04 Reconciliation Pattern (steps 1-3)
  </read_first>
  <files>.claude/skills/sketch-findings-getvul/sources/themes/sunset.css, .claude/skills/sketch-findings-getvul/references/foundation.md, .claude/skills/sketch-findings-getvul/references/visual-language.md</files>
  <action>
    Use the EXACT final hex values recorded in 16-01-SUMMARY.md (they may differ from Plan 01's
    candidates after the token→axe→adjust loop). Do NOT edit any `frontend/` file here — this task
    is skill-only. The vendored `frontend/src/styles/sunset.css` is NEVER touched; only the skill's
    `sources/themes/sunset.css` reference copy is annotated.

    1. sunset.css — add a light-mode overrides section at the END of the file (after the reset
       rules, ~line 138), documenting the canonical light values. Shape (fill with SUMMARY values):
       ```css
       /* ─── Light-mode overrides (Phase 16, UX-D-03) ───
          The :root block above is the dark theme. Under data-theme="light" these tokens
          diverge to darker same-hue variants that clear WCAG 2.1 AA on the warm-cream light
          surfaces (bg #FAF7F2 / surface #FFFFFF). Values axe-confirmed in 16-01-SUMMARY.md;
          the live overrides live in frontend/src/app/globals.css (this vendored copy is not
          consumed at runtime). BL-04 pattern, mirrored for light mode. */
       :root[data-theme="light"] {
         --color-severity-critical: <SUMMARY>;  /* was dark #F87171 */
         ...all severity, semantic-state, shadow, glow, on-soft, text-faint tokens...
       }
       ```

    2. foundation.md — annotate the token tables so each overridden token shows its light value
       alongside the dark value. Under "### Severity colors", add a "Light-mode variants" note row
       or inline comment per token (e.g. `--color-severity-critical: #F87171 /* light: <SUMMARY> */`).
       Under "## Shadows & Glow", add the light-mode shadow/glow values. Under the on-soft comment
       block (lines 33-35), add the light-mode on-soft hexes. Keep the existing dark documentation
       intact — this is additive.

    3. visual-language.md — under "### Text on -soft fills (AA — locked)" (after the existing dark
       table at lines 90-94), add a "**Light-mode variants**" paragraph + table:
       ```
       | Fill | Text token (light) | Hex (light) |
       |---|---|---|
       | --color-violet-soft | --color-violet-on-soft | <SUMMARY> |
       | --color-pink-soft   | --color-pink-on-soft   | <SUMMARY> |
       | --color-amber-soft  | --color-amber-on-soft  | <SUMMARY> |
       ```
       Under the severity table (lines 9-15), add a "Light-mode severity colors" note listing the
       darker same-hue variants, explaining the three-axis encoding (color + glyph + text) keeps
       glyphs distinct even where hue shifts for AA.

    Every value MUST match 16-01-SUMMARY.md exactly (no drift between the app layer and the skill).
  </action>
  <verify>
    <automated>grep -q 'data-theme="light"' .claude/skills/sketch-findings-getvul/sources/themes/sunset.css && grep -qi "Light-mode variants" .claude/skills/sketch-findings-getvul/references/visual-language.md && grep -qi "light-mode" .claude/skills/sketch-findings-getvul/references/foundation.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'data-theme="light"' .claude/skills/sketch-findings-getvul/sources/themes/sunset.css` matches — a light-mode overrides block was added to the skill's sunset.css.
    - The light block in the skill's sunset.css contains all overridden token names present in globals.css's light block (severity ×5, danger/success/warning, shadow-card/elevated, glow-pink/violet/amber/cta/card-inner, the 3 on-soft, text-faint).
    - For every overridden token, the hex in the skill's sunset.css light block byte-matches the value in `.planning/phases/16-light-theme-visual-completion/16-01-SUMMARY.md` (no drift between app layer and skill).
    - `grep -niE "light-mode|light:" .claude/skills/sketch-findings-getvul/references/foundation.md` matches (foundation token tables annotated with light values).
    - `grep -ni "Light-mode variants" .claude/skills/sketch-findings-getvul/references/visual-language.md` matches (on-soft light table added).
    - No `frontend/` file is modified by this task (`git diff --name-only` for this task touches only the three skill files).
  </acceptance_criteria>
  <done>The skill's sunset.css, foundation.md, and visual-language.md all carry the axe-confirmed light-mode token values, byte-matching 16-01-SUMMARY.md; the design system is the single source of truth for both themes.</done>
</task>

<task type="auto">
  <name>Task 2: Enable the Theme: Light toggle in the user chip</name>
  <read_first>
    - frontend/src/components/shell/user-chip.tsx lines 59-78 (the disabled Light radio + In-progress badge + WR-03 comment)
    - 16-RESEARCH.md § Pitfall 5 (the disabled switch is a deliverable, not a nice-to-have) + Code Example Pattern 4
    - Confirm 16-01-SUMMARY.md shows the light axe sweep GREEN before enabling (do not enable a broken theme)
  </read_first>
  <files>frontend/src/components/shell/user-chip.tsx</files>
  <action>
    Only enable the toggle AFTER confirming (via 16-01-SUMMARY.md) that the light axe sweep is
    green — Plan 16-01 is a hard dependency. Then make the Light radio match the Dark radio shape:

    - Remove the `disabled` attribute from the `<DropdownMenuRadioItem value="light">`.
    - Remove the `aria-description="Light theme is not ready yet …"` attribute.
    - Remove the inner `<span className="flex w-full items-center justify-between gap-3">` wrapper
      and the `{'In progress'}` badge span; render just the plain label.
    - Result (mirror the Dark radio directly above):
      ```tsx
      <DropdownMenuRadioItem value="light">{'Theme: Light'}</DropdownMenuRadioItem>
      ```
    - Update the comment above the radio group (lines 59-60): replace the "Light is disabled until
      D-06 / UX-D-03 ships …" note with a note that light mode shipped in Phase 16 (UX-D-03) and
      both themes pass the axe gate. Remove the stale "WR-03" disabled reference.

    Do not change the Dark radio, the radio group value binding, or `setTheme`.
  </action>
  <verify>
    <automated>cd frontend && ! grep -A3 'value="light"' src/components/shell/user-chip.tsx | grep -q "disabled" && ! grep -q "In progress" src/components/shell/user-chip.tsx && npx tsc --noEmit && npx eslint src/components/shell/user-chip.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A3 'value="light"' frontend/src/components/shell/user-chip.tsx` shows NO `disabled` attribute on the Light radio.
    - `grep -n "In progress" frontend/src/components/shell/user-chip.tsx` returns NOTHING (badge removed).
    - `grep -n "aria-description" frontend/src/components/shell/user-chip.tsx` returns NOTHING (stale not-ready description removed).
    - `grep -n "not ready\|WR-03" frontend/src/components/shell/user-chip.tsx` returns NOTHING (stale comment updated).
    - `npx tsc --noEmit` (from frontend/) and `npx eslint src/components/shell/user-chip.tsx` both clean.
    - `npx playwright test e2e/smoke.spec.ts` (from frontend/) green — theme-toggle bootstrap tests unaffected.
  </acceptance_criteria>
  <done>Light radio is enabled and matches the Dark radio shape; the "In progress"/not-ready affordances are gone; lint + type-check clean; smoke green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary is crossed. This plan edits three design-system documentation/source files in
the skill directory and removes a `disabled` attribute + label span from one React component. No
new input handling, endpoint, data flow, auth surface, or crypto.

## STRIDE Threat Register

Per 16-RESEARCH.md § Security Domain, ASVS is appropriately skipped for this phase (CSS/docs +
one component attribute change; zero new data handling, endpoints, auth surface, or crypto). No
threats are invented to fill the table.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | (n/a) | design-system skill files + user-chip toggle | accept | Documentation reconciliation + enabling a client-only presentation toggle. No security-relevant surface. |
</threat_model>

<verification>
- `grep -n 'data-theme="light"' .claude/skills/sketch-findings-getvul/sources/themes/sunset.css` — light values reconciled into the skill.
- Skill light hex values byte-match `16-01-SUMMARY.md` — no app-layer/skill drift.
- `! grep -q "In progress" frontend/src/components/shell/user-chip.tsx` — toggle enabled.
- `cd frontend && npx tsc --noEmit && npx eslint src/components/shell/user-chip.tsx` — clean.
- `cd frontend && npx playwright test && npx vitest run` — full suite green (dark + light a11y, reduced-motion, smoke, unit); 0 First-Load-JS delta.
</verification>

<success_criteria>
- Canonical light-mode token values written into `sunset.css` + `foundation.md` + `visual-language.md`, byte-matching the app layer (UX-D-03-04 reconciliation half, BL-04 pattern).
- Theme: Light radio enabled and user-selectable; stale "In progress"/not-ready affordances removed.
- Full Playwright + unit suites green; 0 First-Load-JS delta.
</success_criteria>

<output>
After completion, create `.planning/phases/16-light-theme-visual-completion/16-02-SUMMARY.md`
recording which skill files were updated and confirming the light hex values match 16-01-SUMMARY.md.
</output>
