---
phase: 01-multi-replica-state
plan: 03
type: execute
wave: 2
depends_on:
  - "01-00"
  - "01-01"
  - "01-02"
files_modified:
  - backend/tests/test_multi_replica.py
autonomous: true
requirements:
  - PROD-01-03
must_haves:
  truths:
    - "An integration test boots TWO independent FastAPI app instances against ONE shared Redis db=1 and proves OIDC state set on app A can be consumed by app B"
    - "A second consume of the same OIDC state returns 400 — proving one-shot semantics survive across replicas"
    - "100 requests through app A plus 101 requests through app B → the 201st request returns 429 (shared rate-limit budget)"
    - "When Redis is killed mid-test, app A's limiter fails open + warns AND app B's OIDC callback returns 503"
    - "All four integration tests use the existing `two_apps` fixture from conftest.py"
  artifacts:
    - path: "backend/tests/test_multi_replica.py"
      provides: "Four PROD-01-03 cross-replica integration tests"
      min_lines: 150
  key_links:
    - from: "backend/tests/test_multi_replica.py"
      to: "two_apps fixture in backend/tests/conftest.py"
      via: "pytest fixture injection — yields (client_a, client_b)"
      pattern: "two_apps"
    - from: "test_oidc_cross_replica"
      to: "single shared Redis db=1"
      via: "app A's GET /auth/login then app B's GET /auth/callback"
      pattern: "client_a.get.*login.*client_b.get.*callback"
    - from: "test_redis_outage_failure_modes"
      to: "monkeypatched RedisError on both apps"
      via: "monkeypatch.setattr(app.state.redis, 'pipeline'/'set'/'getdel', boom_*)"
      pattern: "RedisConnectionError"
---

<objective>
Prove the implementations from 01-01 (Redis-backed OIDC state) and 01-02 (Redis-backed rate limiter) actually share state across replicas. This is the entire reason Phase 1 exists. Without this test the goal "two backend replicas behind a load balancer can complete an OIDC login and share rate-limit budget without race conditions or lost state" is unverified.

Purpose: PROD-01-03 closes the loop — it's the regression test that catches "in-memory state silently re-introduced" (e.g., a future contributor wires up a local cache "for performance"), and it's the proof artifact for the v1.0 production-readiness audit.
Output: New `backend/tests/test_multi_replica.py` with four named tests matching VALIDATION.md rows 01-03-01 through 01-03-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-multi-replica-state/01-CONTEXT.md
@.planning/phases/01-multi-replica-state/01-RESEARCH.md
@.planning/phases/01-multi-replica-state/01-VALIDATION.md
@.planning/phases/01-multi-replica-state/01-00-SUMMARY.md
@.planning/phases/01-multi-replica-state/01-01-SUMMARY.md
@.planning/phases/01-multi-replica-state/01-02-SUMMARY.md

@backend/tests/conftest.py
@backend/tests/test_oidc_state.py
@backend/tests/test_rate_limit.py
@backend/app/auth/router.py
@backend/app/main.py

<interfaces>
The `two_apps` fixture (from 01-00 Task 4):

```
@pytest_asyncio.fixture(scope="function")
async def two_apps(flushed_redis, app_factory):
    """Two independent app instances both pointing at the same db=1.

    Yields (client_a, client_b).
    """
```

It yields a tuple `(httpx.AsyncClient, httpx.AsyncClient)` where each client is bound to its own independent FastAPI instance whose lifespan has run, populating its own `app.state.redis` — but BOTH clients are pointing at the same `redis://localhost:6379/1`.

To get hold of the underlying `app.state.redis` object on each app for the outage test, the fixture as written exposes only the `client.transport` shape. RECOMMENDED: extend the fixture (in 01-00 Task 4 if not already) OR — better, since 01-00 is locked — derive the apps via the same pattern inside this test file:

```
async with LifespanManager(app_a) as mgr_a, LifespanManager(app_b) as mgr_b:
    async with AsyncClient(transport=ASGITransport(app=mgr_a.app), base_url="http://app-a") as client_a, \
               AsyncClient(transport=ASGITransport(app=mgr_b.app), base_url="http://app-b") as client_b:
        # access app instances via mgr_a.app, mgr_b.app
```

If the `two_apps` fixture in conftest.py also yields the app instances (e.g., yields `(client_a, app_a, client_b, app_b)`), prefer that. The 01-00 fixture as specified yields just `(client_a, client_b)`. Either approach is acceptable — the test author should pick whichever requires the minimum churn.

CONTEXT.md locked decisions:
  D-15: Two FastAPI app instances via app_factory(), both pointing at one real Redis.
  D-16: REDIS_URL=redis://localhost:6379/1 (db=1); fixture flushes db=1 before each test.
  D-17: Required test cases (a/b/c) — encoded into 4 named tests below.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-replica integration test suite</name>
  <files>backend/tests/test_multi_replica.py</files>
  <read_first>
    - .planning/phases/01-multi-replica-state/01-VALIDATION.md (rows 01-03-01 through 01-03-04 — exact pytest target names)
    - .planning/phases/01-multi-replica-state/01-CONTEXT.md (D-15, D-16, D-17)
    - .planning/phases/01-multi-replica-state/01-RESEARCH.md "Pattern 6: Test architecture"
    - backend/tests/conftest.py (to confirm fixture signatures)
    - backend/tests/test_oidc_state.py (reuse the same monkeypatch pattern for the outage test)
    - backend/tests/test_rate_limit.py (reuse the asyncio.gather pattern)
  </read_first>
  <behavior>
    Four tests, exact names per VALIDATION.md:

    1. `test_oidc_cross_replica` — App A's GET /auth/login/google → response includes `state`. Use a separate redis client (the `flushed_redis` fixture) to verify the key was set. App B's GET /auth/callback/google?state=...&code=fake → must consume the state (verifiable: the key is gone from Redis afterwards). The callback's HTTP status will be 4xx because the IdP mock fails token exchange — that's fine; the assertion is on the side effect, not the response.

    2. `test_oidc_one_shot` — App A's login mints state. App B's first /auth/callback consumes it. App B's SECOND /auth/callback with the same state returns 400 with detail `"Invalid or expired state parameter"`.

    3. `test_ratelimit_shared_budget` — App A issues 100 requests to `/api/v1/reports`, all allowed. App B issues 100 requests to the same endpoint, all allowed (total 200). App B's 101st request returns 429 (total 201 — exceeded). Use `asyncio.gather` for the burst phase to ensure interleaving across the two app instances.

    4. `test_redis_outage_failure_modes` — Reach into both `app_a.state.redis` and `app_b.state.redis` and monkeypatch `pipeline`, `set`, and `getdel` to raise `redis.exceptions.ConnectionError`. Issue a request to `/api/v1/reports` on app A → status NOT 429 (fail-open) AND captured structlog includes `event=redis_unavailable subsystem=rate_limiter`. Issue /auth/callback/google?state=anything&code=fake on app B → status 503 with detail `"Auth backend unavailable"`. Issue /auth/login/google on app A → status 503.
  </behavior>
  <action>
Create `backend/tests/test_multi_replica.py` with this exact content:

```
"""PROD-01-03: Cross-replica integration tests.

Two independent FastAPI app instances against one shared Redis db=1.
Targets each row in .planning/phases/01-multi-replica-state/01-VALIDATION.md
under "Per-Task Verification Map" tasks 01-03-01 through 01-03-04.

The two_apps fixture in conftest.py yields (client_a, client_b). For tests
that need to monkeypatch the underlying app.state.redis, we derive the apps
locally via LifespanManager so we have direct handles to mgr_a.app /
mgr_b.app.
"""

from __future__ import annotations

import asyncio

import pytest
import structlog
from asgi_lifespan import LifespanManager
from httpx import ASGITransport, AsyncClient
from redis.exceptions import ConnectionError as RedisConnectionError


TEST_API_ENDPOINT = "/api/v1/reports"


@pytest.mark.asyncio
async def test_oidc_cross_replica(two_apps, flushed_redis):
    client_a, client_b = two_apps

    # Replica A initiates login.
    login_resp = await client_a.get("/auth/login/google")
    assert login_resp.status_code == 200, login_resp.text
    state = login_resp.json()["state"]

    # The state lives in shared Redis db=1, visible to BOTH replicas.
    stored = await flushed_redis.get(f"oidc:state:{state}")
    assert stored == "google", f"state not visible in shared Redis; got {stored!r}"

    # Replica B (different process — simulated by a different app instance)
    # consumes the state. Token exchange will fail because the IdP is fake,
    # but the GETDEL must have removed the key.
    callback_resp = await client_b.get(
        f"/auth/callback/google?state={state}&code=fake"
    )
    assert callback_resp.status_code in (400, 401, 502, 503)

    # The state key MUST be gone (consumed atomically across replicas).
    assert await flushed_redis.exists(f"oidc:state:{state}") == 0


@pytest.mark.asyncio
async def test_oidc_one_shot(two_apps, flushed_redis):
    client_a, client_b = two_apps

    login_resp = await client_a.get("/auth/login/google")
    state = login_resp.json()["state"]

    # First consume on replica B — succeeds at the state-validation level.
    first = await client_b.get(f"/auth/callback/google?state={state}&code=fake")
    assert first.status_code in (400, 401, 502, 503)

    # Second consume of the same state — replay defense.
    second = await client_b.get(f"/auth/callback/google?state={state}&code=fake")
    assert second.status_code == 400
    assert second.json()["detail"] == "Invalid or expired state parameter"


@pytest.mark.asyncio
async def test_ratelimit_shared_budget(two_apps, flushed_redis):
    client_a, client_b = two_apps

    # 100 anonymous requests through replica A.
    for _ in range(100):
        await client_a.get(TEST_API_ENDPOINT)

    # 100 anonymous requests through replica B.
    for _ in range(100):
        await client_b.get(TEST_API_ENDPOINT)

    # The next request — anywhere — must be 429.
    blocked = await client_b.get(TEST_API_ENDPOINT)
    assert blocked.status_code == 429, (
        f"replica B should hit shared cap on the 201st request; got {blocked.status_code}"
    )
    assert blocked.headers.get("Retry-After") == "60"

    # And replica A is also out of budget.
    blocked_a = await client_a.get(TEST_API_ENDPOINT)
    assert blocked_a.status_code == 429


@pytest.mark.asyncio
async def test_redis_outage_failure_modes(flushed_redis, app_factory, monkeypatch):
    """Build the two apps locally so we can reach `app.state.redis` to mock it."""
    app_a = app_factory()
    app_b = app_factory()

    async with LifespanManager(app_a) as mgr_a, LifespanManager(app_b) as mgr_b:
        async with (
            AsyncClient(
                transport=ASGITransport(app=mgr_a.app), base_url="http://app-a"
            ) as client_a,
            AsyncClient(
                transport=ASGITransport(app=mgr_b.app), base_url="http://app-b"
            ) as client_b,
        ):
            # Sanity: both apps see the same Redis db.
            await mgr_a.app.state.redis.set("probe", "1")
            assert await mgr_b.app.state.redis.get("probe") == "1"

            # Kill both apps' Redis client surfaces.
            def boom_pipeline(*args, **kwargs):
                raise RedisConnectionError("simulated outage")

            async def boom_set(*args, **kwargs):
                raise RedisConnectionError("simulated outage")

            async def boom_getdel(*args, **kwargs):
                raise RedisConnectionError("simulated outage")

            monkeypatch.setattr(mgr_a.app.state.redis, "pipeline", boom_pipeline)
            monkeypatch.setattr(mgr_a.app.state.redis, "set", boom_set)
            monkeypatch.setattr(mgr_a.app.state.redis, "getdel", boom_getdel)
            monkeypatch.setattr(mgr_b.app.state.redis, "pipeline", boom_pipeline)
            monkeypatch.setattr(mgr_b.app.state.redis, "set", boom_set)
            monkeypatch.setattr(mgr_b.app.state.redis, "getdel", boom_getdel)

            # Limiter: fail OPEN with structured warning.
            with structlog.testing.capture_logs() as captured:
                resp = await client_a.get(TEST_API_ENDPOINT)
            assert resp.status_code != 429, (
                "limiter must fail open during Redis outage"
            )
            limiter_events = [
                e
                for e in captured
                if e.get("event") == "redis_unavailable"
                and e.get("subsystem") == "rate_limiter"
            ]
            assert limiter_events, (
                f"expected a rate_limiter redis_unavailable warning; got {captured}"
            )

            # OIDC: fail CLOSED with 503.
            login_resp = await client_a.get("/auth/login/google")
            assert login_resp.status_code == 503
            assert login_resp.json()["detail"] == "Auth backend unavailable"

            callback_resp = await client_b.get(
                "/auth/callback/google?state=anything&code=fake"
            )
            assert callback_resp.status_code == 503
            assert callback_resp.json()["detail"] == "Auth backend unavailable"
```

Notes:
- The 4 test names match VALIDATION.md exactly.
- `test_redis_outage_failure_modes` deliberately builds its own two apps instead of using the `two_apps` fixture, because the fixture as defined yields only the clients, not the underlying apps. The pattern is a 6-line copy of what conftest does, so this is acceptable per "Claude's discretion" in CONTEXT.md.
- `test_ratelimit_shared_budget` does NOT use `asyncio.gather` for the 200 warm-up requests — issuing them sequentially is sufficient to prove cross-replica accounting because we are NOT testing the sub-ms duplicate-member bug here (that's covered by `test_concurrent_burst_respects_limit` in plan 01-02). Sequential warm-up keeps the test fast (~200ms).
- `flushed_redis` fixture handles cleanup, so the test does not need to FLUSHDB.
- The "probe" sanity assertion in `test_redis_outage_failure_modes` is not strictly required but it catches a class of fixture bugs (e.g., apps accidentally pointing at db=0 vs db=1). Keep it.

Run:

```
cd backend && pytest tests/test_multi_replica.py -x
```

Then run the full Phase 1 suite as a final integration check:

```
cd backend && pytest tests/test_oidc_state.py tests/test_rate_limit.py tests/test_multi_replica.py -v
```
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_multi_replica.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/tests/test_multi_replica.py` exits 0
    - `grep -c "def test_oidc_cross_replica" backend/tests/test_multi_replica.py` returns 1
    - `grep -c "def test_oidc_one_shot" backend/tests/test_multi_replica.py` returns 1
    - `grep -c "def test_ratelimit_shared_budget" backend/tests/test_multi_replica.py` returns 1
    - `grep -c "def test_redis_outage_failure_modes" backend/tests/test_multi_replica.py` returns 1
    - `grep -F "two_apps" backend/tests/test_multi_replica.py` returns at least 3 hits (used by 3 of 4 tests)
    - `grep -F "LifespanManager" backend/tests/test_multi_replica.py` returns a hit (used by outage test)
    - `grep -F "structlog.testing.capture_logs" backend/tests/test_multi_replica.py` returns a hit
    - `grep -F "Auth backend unavailable" backend/tests/test_multi_replica.py` returns a hit
    - `cd backend && pytest tests/test_multi_replica.py::test_oidc_cross_replica -x` exits 0
    - `cd backend && pytest tests/test_multi_replica.py::test_oidc_one_shot -x` exits 0
    - `cd backend && pytest tests/test_multi_replica.py::test_ratelimit_shared_budget -x` exits 0
    - `cd backend && pytest tests/test_multi_replica.py::test_redis_outage_failure_modes -x` exits 0
    - `cd backend && pytest tests/test_multi_replica.py -x` exits 0 (all 4 green)
    - `cd backend && pytest tests/test_oidc_state.py tests/test_rate_limit.py tests/test_multi_replica.py -v` exits 0 (all 15 Phase 1 tests green)
    - `cd backend && pytest -x` exits 0 (full suite, no regressions)
  </acceptance_criteria>
  <done>All four PROD-01-03 cross-replica integration tests pass against a live Redis db=1 with two FastAPI app instances.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Replica A → shared Redis ← Replica B | Both replicas read/write to one Redis; Phase 1 correctness depends on Redis being the single source of truth. |
| Test runner → real Redis db=1 | Tests must not bleed state across runs (fixture FLUSHDBs db=1). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-replica-spoof | S (Spoofing) — replay or hijack across replicas | OIDC state cross-replica path | high | mitigate | `test_oidc_cross_replica` proves state set on A is visible on B; `test_oidc_one_shot` proves the second consume on B fails with 400. The shared Redis is the single source of truth. |
| T-replica-replay | S (Spoofing) — same state used twice across replicas | OIDC GETDEL atomicity | high | mitigate | `test_oidc_one_shot` — second consume on app B returns 400 "Invalid or expired state parameter". GETDEL is atomic regardless of which replica calls it. |
| T-replica-bypass | D (DoS) — current bug: per-replica rate-limit counters allow N×replicas tenants to exceed cap | TenantRateLimitMiddleware shared budget | high | mitigate | `test_ratelimit_shared_budget` — 100 reqs A + 100 reqs B + 1 = 429. Single Redis sorted set is shared. |
| T-redis-outage | A (Availability) | both subsystems under simulated Redis ConnectionError | medium | mitigate | `test_redis_outage_failure_modes` proves limiter fails OPEN with warning (D-05) and OIDC fails CLOSED with 503 (D-06). Both behaviors are tested on each app instance independently. |
| T-test-bleed | T (Tampering) — leftover keys from one test poison the next | flushed_redis fixture | low | mitigate | `flushed_redis` fixture FLUSHDBs db=1 before every test (db=1 isolation per D-16). The "probe" sanity check in test_redis_outage_failure_modes catches db-0 misrouting. |
| T-test-flake | reliability | sequential 100+100 vs concurrent | low | accept | We sequence the rate-limit warm-up to keep the test deterministic (~200ms wall clock). The Pitfall-1 concurrency case is already covered by 01-02's `test_concurrent_burst_respects_limit`. |

Block severity: high. All `high` threats are explicitly tested by the four cases.
</threat_model>

<verification>
After Task 1:

```
# File exists with all 4 named tests
test -f backend/tests/test_multi_replica.py
grep -c "def test_oidc_cross_replica" backend/tests/test_multi_replica.py        # → 1
grep -c "def test_oidc_one_shot" backend/tests/test_multi_replica.py             # → 1
grep -c "def test_ratelimit_shared_budget" backend/tests/test_multi_replica.py   # → 1
grep -c "def test_redis_outage_failure_modes" backend/tests/test_multi_replica.py # → 1

# Phase 1 suite green
cd backend && pytest tests/test_oidc_state.py tests/test_rate_limit.py tests/test_multi_replica.py -v
```

All 15 Phase 1 tests must pass (5 from 01-01, 6 from 01-02, 4 from 01-03). The full suite must remain green:

```
cd backend && pytest -x
```
</verification>

<success_criteria>
- `backend/tests/test_multi_replica.py` exists with 4 named tests matching VALIDATION.md rows 01-03-01 through 01-03-04
- All 4 tests pass against real Redis db=1 with two independent FastAPI app instances built via the `app_factory` fixture
- The combined Phase 1 test suite (15 tests across 3 files) passes
- The full backend test suite (`pytest -x`) is green — no regressions
- The `must_haves.truths` for the entire phase are observable: a developer can re-run `cd backend && pytest tests/test_multi_replica.py -v` and see the 4 cross-replica behaviors verified
</success_criteria>

<output>
After completion, create `.planning/phases/01-multi-replica-state/01-03-SUMMARY.md` documenting:
- The 4 test names and which D-17 subcase each maps to (a, b, c)
- Confirmation that the full Phase 1 test suite (15 tests) is green
- Any deviations from the plan (e.g., if the `two_apps` fixture in conftest had to be extended to also yield app instances — note that here)
- A pointer to the next phase (Phase 2: CI Gating, which will wire these tests into the push/PR workflow)
</output>
