---
phase: 01-multi-replica-state
plan: 02
type: execute
wave: 1
depends_on:
  - "01-00"
files_modified:
  - backend/app/main.py
  - backend/tests/test_rate_limit.py
  - doc/security.md
autonomous: true
requirements:
  - PROD-01-02
must_haves:
  truths:
    - "TenantRateLimitMiddleware uses a Redis sorted-set sliding window keyed `ratelimit:{tenant_id}` (or `ratelimit:anonymous`)"
    - "ZADD member is `f\"{now_ms}:{uuid.uuid4().hex[:8]}\"` (NOT `now_ms` alone) — defeats sub-millisecond duplicate-member undercount"
    - "Pipeline is `transaction=True` and runs ZREMRANGEBYSCORE then ZADD then ZCARD then EXPIRE in one round trip"
    - "When count exceeds 200 the middleware returns 429 with `Retry-After: 60`"
    - "On `RedisError` the middleware fails OPEN (calls `call_next`) and emits structured warning `event=redis_unavailable subsystem=rate_limiter error=...`"
    - "The in-memory `_rate_limit_store: defaultdict` declaration and the in-memory pruning logic are deleted"
    - "doc/security.md:20 reads `Per-tenant API rate limiting: 200 requests per 60 seconds (Redis-backed sliding window)`"
  artifacts:
    - path: "backend/app/main.py"
      provides: "Redis-backed TenantRateLimitMiddleware sliding window"
      contains: "zadd"
    - path: "backend/tests/test_rate_limit.py"
      provides: "Six PROD-01-02 tests covering enforcement, window slide, tenant isolation, fail-open, concurrency, and doc parity"
      min_lines: 200
    - path: "doc/security.md"
      provides: "accurate Redis-backed sliding window claim on line 20"
      contains: "Redis-backed sliding window"
  key_links:
    - from: "backend/app/main.py TenantRateLimitMiddleware.dispatch"
      to: "redis ZADD ratelimit:{tenant_key} {member: now_ms}"
      via: "pipe.zadd(key, {member: now_ms}) inside async with redis.pipeline(transaction=True)"
      pattern: "pipe\\.zadd\\(.*\\{.*now_ms"
    - from: "TenantRateLimitMiddleware.dispatch RedisError catch"
      to: "structlog warning + call_next (fail-open)"
      via: "logger.warning + return await call_next(request)"
      pattern: "subsystem=.rate_limiter"
    - from: "doc/security.md:20"
      to: "code in main.py middleware"
      via: "verbatim wording match"
      pattern: "Redis-backed sliding window"
---

<objective>
Replace the in-memory `_rate_limit_store: defaultdict(list)` (backend/app/main.py:103-105) and the manual list-pruning logic in `TenantRateLimitMiddleware.dispatch` with a Redis sorted-set sliding window using a MULTI/EXEC pipeline. Fail OPEN on Redis outage (limiter is a safety valve, not a security boundary — D-05). Update doc/security.md:20 so the long-standing "Redis-backed" claim becomes accurate.

Purpose: Two backend replicas must share the rate-limit budget. Today, a tenant can issue 199 requests against replica A and another 199 against replica B and never get throttled. After this plan the budget lives in Redis and is shared.
Output: Updated `main.py` middleware + new `tests/test_rate_limit.py` (6 cases) + 1-line edit to `doc/security.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-multi-replica-state/01-CONTEXT.md
@.planning/phases/01-multi-replica-state/01-RESEARCH.md
@.planning/phases/01-multi-replica-state/01-VALIDATION.md
@.planning/phases/01-multi-replica-state/01-00-SUMMARY.md

@backend/app/main.py
@backend/app/auth/jwt.py
@doc/security.md

<interfaces>
Tenant key extraction (preserve verbatim — D-04):

```
auth = request.headers.get("authorization", "")
tenant_key = "anonymous"
if auth.startswith("Bearer "):
    try:
        from app.auth.jwt import decode_token
        payload = decode_token(auth[7:])
        tenant_key = payload.tenant_id
    except Exception:
        pass
```

Redis sorted-set pipeline API (redis-py 5.x):

```
async with redis_client.pipeline(transaction=True) as pipe:
    pipe.zremrangebyscore(key, 0, window_start_ms)
    pipe.zadd(key, {member: now_ms})
    pipe.zcard(key)
    pipe.expire(key, RATE_LIMIT_WINDOW)
    results = await pipe.execute()
# results[0]=removed, results[1]=added (1 if unique), results[2]=ZCARD, results[3]=EXPIRE flag
```

Existing logger pattern across codebase:

```
import structlog
logger = structlog.get_logger()
logger.warning("redis_unavailable", subsystem="rate_limiter", error=str(e))
```

doc/security.md current line 20:

```
- Per-tenant API rate limiting: 200 requests per 60 seconds (Redis-backed)
```

CONTEXT.md locked decisions for this plan:

- D-01: Sliding window via Redis sorted set, key `ratelimit:{tenant_id}`.
- D-02: `pipeline(transaction=True)` — one round trip.
- D-03: 200 req / 60s; constants stay at module scope.
- D-04: Tenant key via JWT decode, fall back to "anonymous". UNCHANGED logic.
- D-05: Fail OPEN on RedisError; emit structlog warning.
- D-13: Delete `_rate_limit_store` entirely. No fallback.
- D-19: Log fields: event, subsystem="rate_limiter", error=str(e), tenant_id if known.
- D-20: Update doc/security.md:20.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace in-memory store with Redis sorted-set sliding window</name>
  <files>backend/app/main.py</files>
  <read_first>
    - backend/app/main.py (current full file — `TenantRateLimitMiddleware` lives at lines 99-147; create_app() factory from 01-00 must still wrap everything)
    - .planning/phases/01-multi-replica-state/01-RESEARCH.md section "Pattern 4: Sliding-window pipeline" (the exact 4-command sequence)
    - .planning/phases/01-multi-replica-state/01-RESEARCH.md section "Pitfall 1: ZADD with member==score is a no-op on duplicates" (the bug we are explicitly defeating)
    - .planning/phases/01-multi-replica-state/01-CONTEXT.md (D-01, D-02, D-05, D-13, D-19)
  </read_first>
  <behavior>
    - Test 1: 200 requests succeed (200 OK), 201st request returns 429 with `Retry-After: 60`.
    - Test 2: After waiting RATE_LIMIT_WINDOW + 1 seconds, the budget resets (entries pruned by ZREMRANGEBYSCORE).
    - Test 3: Tenant A and tenant B have independent budgets.
    - Test 4: When `app.state.redis.pipeline` raises `RedisError`, request goes through (fail-open) and structlog warning is emitted with `subsystem="rate_limiter"`.
    - Test 5: 200 requests issued via `asyncio.gather` (true sub-millisecond contention) all process; the 201st returns 429 — proves the unique-member uuid suffix defeats Pitfall 1.
    - Test 6: doc/security.md:20 contains the verbatim string `"Redis-backed sliding window"`.
  </behavior>
  <action>
Modify `backend/app/main.py`:

**1. Imports — add at the top of the file (with other stdlib/third-party imports):**

Add these three lines after the existing third-party imports (`from fastapi.middleware.cors import CORSMiddleware`):

```
import uuid
import structlog
from redis.exceptions import RedisError
```

DELETE the line `from collections import defaultdict` (currently around line 101). Keep `import time` — still used.

Add `logger = structlog.get_logger()` at module scope, immediately after the import block at the top of the file.

**2. Delete line 103 entirely** — the `_rate_limit_store: dict[str, list[float]] = defaultdict(list)` declaration. KEEP lines 104-105 (the `RATE_LIMIT_REQUESTS = 200` and `RATE_LIMIT_WINDOW = 60` constants — they remain module-scoped per D-03).

**3. Replace the entire body of `TenantRateLimitMiddleware.dispatch` (currently lines 111-144) with the following exact code:**

```
class TenantRateLimitMiddleware(BaseHTTPMiddleware):
    """Per-tenant rate limiting via Redis sorted-set sliding window."""

    async def dispatch(self, request: Request, call_next):
        if not request.url.path.startswith("/api/"):
            return await call_next(request)

        # Extract tenant from JWT (lightweight — just parse, don't validate DB)
        auth = request.headers.get("authorization", "")
        tenant_key = "anonymous"
        if auth.startswith("Bearer "):
            try:
                from app.auth.jwt import decode_token

                payload = decode_token(auth[7:])
                tenant_key = payload.tenant_id
            except Exception:
                pass

        redis_client = request.app.state.redis
        key = f"ratelimit:{tenant_key}"
        now_ms = int(time.time() * 1000)
        window_start_ms = now_ms - RATE_LIMIT_WINDOW * 1000
        # Unique member defeats sub-ms ZADD duplicate-member coalescing (Pitfall 1).
        member = f"{now_ms}:{uuid.uuid4().hex[:8]}"

        try:
            async with redis_client.pipeline(transaction=True) as pipe:
                pipe.zremrangebyscore(key, 0, window_start_ms)
                pipe.zadd(key, {member: now_ms})
                pipe.zcard(key)
                pipe.expire(key, RATE_LIMIT_WINDOW)
                results = await pipe.execute()
        except RedisError as e:
            logger.warning(
                "redis_unavailable",
                subsystem="rate_limiter",
                error=str(e),
                tenant_id=tenant_key if tenant_key != "anonymous" else None,
            )
            return await call_next(request)

        count = results[2]
        if count > RATE_LIMIT_REQUESTS:
            from starlette.responses import JSONResponse

            return JSONResponse(
                {"detail": "Rate limit exceeded. Try again later."},
                status_code=429,
                headers={"Retry-After": str(RATE_LIMIT_WINDOW)},
            )

        return await call_next(request)
```

**Critical invariants — do NOT skip these:**

- `member = f"{now_ms}:{uuid.uuid4().hex[:8]}"` — DO NOT use `now_ms` alone. Two requests in the same millisecond produce identical (member, score) and the second `ZADD` is a no-op. This is Pitfall 1; the concurrency test in Task 2 will fail without the uuid suffix.
- `pipeline(transaction=True)` — without `transaction=True` the four commands can interleave with another client's commands, defeating D-02.
- The check is `count > RATE_LIMIT_REQUESTS` (strict greater-than), NOT `>=`. Reasoning: the current request's `ZADD` happened BEFORE the `ZCARD`, so a count of 200 means "this is the 200th request, allow it". The 201st request sees count=201 and is rejected.
- `tenant_id=tenant_key if tenant_key != "anonymous" else None` — D-19 says "tenant_id if known"; do not clutter logs with `"anonymous"`.

**4. Verify `_rate_limit_store` does not appear anywhere in the file** — `grep -c "_rate_limit_store" backend/app/main.py` must return 0.

**5. Verify the create_app() factory from 01-00 still wraps everything** — `grep -c "def create_app" backend/app/main.py` must still return 1, and `grep -c "^app = create_app()" backend/app/main.py` must still return 1.
  </action>
  <verify>
    <automated>cd backend && grep -c "_rate_limit_store" app/main.py | grep -q "^0$" && grep -c "from collections import defaultdict" app/main.py | grep -q "^0$" && grep -F "pipe.zremrangebyscore" app/main.py && grep -F "pipe.zadd(" app/main.py && grep -F "pipe.zcard" app/main.py && grep -F "pipe.expire(key, RATE_LIMIT_WINDOW)" app/main.py && grep -F "uuid.uuid4().hex[:8]" app/main.py && grep -F "transaction=True" app/main.py && grep -F 'subsystem="rate_limiter"' app/main.py && python -m py_compile app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_rate_limit_store" backend/app/main.py` returns 0
    - `grep -c "from collections import defaultdict" backend/app/main.py` returns 0
    - `grep -c "defaultdict" backend/app/main.py` returns 0
    - `grep -F "pipe.zremrangebyscore" backend/app/main.py` returns a hit
    - `grep -F "pipe.zadd(" backend/app/main.py` returns a hit
    - `grep -F "pipe.zcard" backend/app/main.py` returns a hit
    - `grep -F "pipe.expire(key, RATE_LIMIT_WINDOW)" backend/app/main.py` returns a hit
    - `grep -F "uuid.uuid4().hex[:8]" backend/app/main.py` returns a hit (Pitfall 1 mitigation)
    - `grep -F "transaction=True" backend/app/main.py` returns a hit
    - `grep -F 'subsystem="rate_limiter"' backend/app/main.py` returns a hit (D-19)
    - `grep -F "except RedisError" backend/app/main.py` returns a hit
    - `grep -F "import structlog" backend/app/main.py` returns a hit
    - `grep -F "logger.warning(" backend/app/main.py` returns a hit
    - `grep -F "Retry-After" backend/app/main.py` returns a hit (preserved behavior)
    - `grep -F 'status_code=429' backend/app/main.py` returns a hit
    - `grep -F 'count > RATE_LIMIT_REQUESTS' backend/app/main.py` returns a hit (strict gt, not ge)
    - `grep -c "def create_app" backend/app/main.py` returns 1 (factory from 01-00 preserved)
    - `grep -c "^app = create_app()" backend/app/main.py` returns 1
    - `cd backend && python -m py_compile app/main.py` exits 0
    - `cd backend && python -c "from app.main import create_app; a = create_app(); print('ok')"` exits 0
  </acceptance_criteria>
  <done>The middleware uses a Redis sorted-set sliding window with a unique uuid-suffixed member, fails open with a structured warning on RedisError, and returns 429 + Retry-After: 60 when count exceeds 200. The in-memory store and defaultdict import are gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PROD-01-02 unit + concurrency tests</name>
  <files>backend/tests/test_rate_limit.py</files>
  <read_first>
    - .planning/phases/01-multi-replica-state/01-VALIDATION.md (Per-Task Verification Map rows 01-02-01 through 01-02-06 — exact pytest target names)
    - .planning/phases/01-multi-replica-state/01-RESEARCH.md "Pattern 4" + "Pitfall 1" (concurrency test must use asyncio.gather to actually exercise the duplicate-member bug)
    - backend/tests/conftest.py (fixtures `single_app`, `flushed_redis`, `redis_test_url`)
    - backend/app/main.py (after Task 1 — confirms middleware now consults Redis)
    - backend/app/auth/jwt.py (to mint test tokens for tenant isolation test)
  </read_first>
  <behavior>
    Six tests, exact names per VALIDATION.md:
    1. `test_limit_enforced` — 200 sequential requests against `/api/v1/...` succeed (or return any non-429); the 201st returns 429 with `Retry-After: 60`.
    2. `test_window_slides` — Issue ~5 requests, manually expire the sorted-set members by deleting the key, issue more requests — count restarts. (Faster than waiting 60s in test; we mock the time advance by deleting the key, OR by calling `redis.zremrangebyscore` to manually prune.)
    3. `test_per_tenant_isolation` — Issue 200 requests as tenant A (uses Bearer token), then 1 request as tenant B — tenant B's request succeeds, proving budgets are independent.
    4. `test_fail_open_on_redis_down` — monkeypatch `app.state.redis.pipeline` to raise `RedisConnectionError`; capture structlog with `structlog.testing.capture_logs`; issue request → status 200 (fail-open) and captured log contains `event=redis_unavailable subsystem=rate_limiter`.
    5. `test_concurrent_burst_respects_limit` — Issue 250 requests concurrently via `asyncio.gather`; count 429 responses; assert exactly 50 are 429 (250 issued, 200 allowed). This proves the uuid-suffix member defeats Pitfall 1.
    6. `test_doc_parity` — `read("doc/security.md")` and assert line 20 contains the substring `"Redis-backed sliding window"` (a grep-style check; once Task 3 lands, this is green).
  </behavior>
  <action>
Create `backend/tests/test_rate_limit.py` with this exact content (every test name matches VALIDATION.md):

```
"""PROD-01-02: Redis-backed per-tenant rate limiter unit tests.

Targets each row in .planning/phases/01-multi-replica-state/01-VALIDATION.md
under "Per-Task Verification Map" tasks 01-02-01 through 01-02-06.
"""

from __future__ import annotations

import asyncio
import time
from pathlib import Path

import pytest
import structlog
from redis.exceptions import ConnectionError as RedisConnectionError


# A real /api endpoint that exists in main.py is `/api/v1/reports` (GET); we
# expect 401 when unauthenticated, but the rate-limit middleware runs FIRST,
# so the limiter still increments. That makes 401 the "allowed" status here.
TEST_ENDPOINT = "/api/v1/reports"


def _is_allowed(status_code: int) -> bool:
    """The limiter allows the request if it is NOT 429."""
    return status_code != 429


@pytest.mark.asyncio
async def test_limit_enforced(single_app, flushed_redis):
    client, app = single_app
    allowed = 0
    for _ in range(200):
        resp = await client.get(TEST_ENDPOINT)
        if _is_allowed(resp.status_code):
            allowed += 1
    assert allowed == 200, f"expected all 200 to be allowed, got {allowed}"

    blocked = await client.get(TEST_ENDPOINT)
    assert blocked.status_code == 429
    assert blocked.headers.get("Retry-After") == "60"


@pytest.mark.asyncio
async def test_window_slides(single_app, flushed_redis):
    client, app = single_app
    for _ in range(5):
        await client.get(TEST_ENDPOINT)

    # Simulate window slide by deleting the sorted-set entries.
    await flushed_redis.delete("ratelimit:anonymous")

    # Budget is restored.
    for _ in range(200):
        resp = await client.get(TEST_ENDPOINT)
        assert _is_allowed(resp.status_code)
    blocked = await client.get(TEST_ENDPOINT)
    assert blocked.status_code == 429


@pytest.mark.asyncio
async def test_per_tenant_isolation(single_app, flushed_redis):
    from app.auth.jwt import create_access_token

    client, app = single_app
    # Burn tenant A's budget with 200 requests, then 429 the 201st.
    token_a = create_access_token(
        user_id="user-a",
        tenant_id="tenant-aaa",
        email="a@example.com",
        role="ADMIN",
    )
    headers_a = {"Authorization": f"Bearer {token_a}"}
    for _ in range(200):
        await client.get(TEST_ENDPOINT, headers=headers_a)
    blocked_a = await client.get(TEST_ENDPOINT, headers=headers_a)
    assert blocked_a.status_code == 429

    # Tenant B's budget is independent.
    token_b = create_access_token(
        user_id="user-b",
        tenant_id="tenant-bbb",
        email="b@example.com",
        role="ADMIN",
    )
    headers_b = {"Authorization": f"Bearer {token_b}"}
    resp_b = await client.get(TEST_ENDPOINT, headers=headers_b)
    assert _is_allowed(resp_b.status_code), (
        f"tenant B should be unaffected by tenant A's exhaustion; got {resp_b.status_code}"
    )


@pytest.mark.asyncio
async def test_fail_open_on_redis_down(single_app, monkeypatch):
    client, app = single_app

    def boom_pipeline(*args, **kwargs):
        raise RedisConnectionError("simulated outage")

    monkeypatch.setattr(app.state.redis, "pipeline", boom_pipeline)

    with structlog.testing.capture_logs() as captured:
        resp = await client.get(TEST_ENDPOINT)

    assert resp.status_code != 429, "limiter must fail open on Redis outage"
    events = [e for e in captured if e.get("event") == "redis_unavailable"]
    assert events, f"expected a redis_unavailable warning; captured: {captured}"
    assert events[0].get("subsystem") == "rate_limiter"
    assert "simulated outage" in events[0].get("error", "")


@pytest.mark.asyncio
async def test_concurrent_burst_respects_limit(single_app, flushed_redis):
    client, app = single_app

    async def one():
        return await client.get(TEST_ENDPOINT)

    responses = await asyncio.gather(*(one() for _ in range(250)))
    blocked = sum(1 for r in responses if r.status_code == 429)
    allowed = sum(1 for r in responses if _is_allowed(r.status_code))
    assert allowed == 200, (
        f"expected exactly 200 allowed (defeating ZADD duplicate-member bug); got allowed={allowed}, blocked={blocked}"
    )
    assert blocked == 50


def test_doc_parity():
    repo_root = Path(__file__).resolve().parents[2]
    sec_doc = (repo_root / "doc" / "security.md").read_text()
    # Line 20 in current file; we just need the substring anywhere on that line.
    line_20 = sec_doc.splitlines()[19]
    assert "Redis-backed sliding window" in line_20, (
        f"doc/security.md:20 must say 'Redis-backed sliding window'; got: {line_20!r}"
    )
```

Notes on the tests:
- The tests issue requests against `/api/v1/reports` because (a) it exists in `main.py` (added in 01-00 refactor), (b) the rate-limit middleware runs BEFORE auth so the 401-from-no-token still counts toward the budget, and (c) we don't need a working DB session for the limiter behavior to be observable.
- `_is_allowed` accepts ANY non-429 status (200, 401, etc.) because the test only cares about whether the limiter fired.
- `test_concurrent_burst_respects_limit` is the one that explicitly proves Pitfall 1 is mitigated. Without `uuid.uuid4().hex[:8]` in the member, this test will produce `allowed > 200` because sub-ms duplicates would silently undercount.
- `test_per_tenant_isolation` imports `create_access_token` from `app.auth.jwt` — this is the existing JWT helper; if its signature differs, adapt (the JWT module exposes a token-mint function used in `test_auth.py`).
- `test_fail_open_on_redis_down` uses `structlog.testing.capture_logs()` which is the canonical way to assert on structlog events. The code path under test is `except RedisError → logger.warning → return await call_next(request)`.
- `test_doc_parity` reads doc/security.md and asserts the substring; this test depends on Task 3 (doc edit). Task 2's automated verify therefore *deselects* test_doc_parity so the suite passes when Task 2 lands. Task 3's verify covers test_doc_parity explicitly, and any phase-level verification re-runs the full suite.

Run the tests (excluding the doc-parity test, which Task 3 will exercise):

```
cd backend && pytest tests/test_rate_limit.py -x --deselect tests/test_rate_limit.py::test_doc_parity
```
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_rate_limit.py -x --deselect tests/test_rate_limit.py::test_doc_parity</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/tests/test_rate_limit.py` exits 0
    - `grep -c "def test_limit_enforced" backend/tests/test_rate_limit.py` returns 1
    - `grep -c "def test_window_slides" backend/tests/test_rate_limit.py` returns 1
    - `grep -c "def test_per_tenant_isolation" backend/tests/test_rate_limit.py` returns 1
    - `grep -c "def test_fail_open_on_redis_down" backend/tests/test_rate_limit.py` returns 1
    - `grep -c "def test_concurrent_burst_respects_limit" backend/tests/test_rate_limit.py` returns 1
    - `grep -c "def test_doc_parity" backend/tests/test_rate_limit.py` returns 1
    - `grep -F "asyncio.gather" backend/tests/test_rate_limit.py` returns a hit (concurrency test)
    - `grep -F "structlog.testing.capture_logs" backend/tests/test_rate_limit.py` returns a hit (fail-open log assertion)
    - `cd backend && pytest tests/test_rate_limit.py::test_limit_enforced -x` exits 0
    - `cd backend && pytest tests/test_rate_limit.py::test_window_slides -x` exits 0
    - `cd backend && pytest tests/test_rate_limit.py::test_per_tenant_isolation -x` exits 0
    - `cd backend && pytest tests/test_rate_limit.py::test_fail_open_on_redis_down -x` exits 0
    - `cd backend && pytest tests/test_rate_limit.py::test_concurrent_burst_respects_limit -x` exits 0
    - `cd backend && pytest tests/test_rate_limit.py -x --deselect tests/test_rate_limit.py::test_doc_parity` exits 0 (5 of 6 green; test_doc_parity is exercised by Task 3)
  </acceptance_criteria>
  <done>Five of the six PROD-01-02 tests pass at this task boundary; test_doc_parity is deferred to Task 3 (which edits doc/security.md and runs that single test). The concurrency test (test_concurrent_burst_respects_limit) explicitly verifies the uuid-suffix mitigation for Pitfall 1.</done>
</task>

<task type="auto">
  <name>Task 3: doc/security.md parity</name>
  <files>doc/security.md</files>
  <read_first>
    - doc/security.md (current line 20: `- Per-tenant API rate limiting: 200 requests per 60 seconds (Redis-backed)`)
    - .planning/phases/01-multi-replica-state/01-CONTEXT.md (D-20 — exact wording mandated)
  </read_first>
  <action>
Edit `doc/security.md`. On line 20, change:

```
- Per-tenant API rate limiting: 200 requests per 60 seconds (Redis-backed)
```

to:

```
- Per-tenant API rate limiting: 200 requests per 60 seconds (Redis-backed sliding window)
```

The ONLY difference is appending ` sliding window` before the closing `)`. Do not modify any other line in `doc/security.md` — Phase 4 covers the rest of doc parity.

This wording is mandated verbatim by D-20 and is what `test_doc_parity` (Task 2 above) asserts.
  </action>
  <verify>
    <automated>grep -F "Per-tenant API rate limiting: 200 requests per 60 seconds (Redis-backed sliding window)" doc/security.md && cd backend && pytest tests/test_rate_limit.py::test_doc_parity -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Per-tenant API rate limiting: 200 requests per 60 seconds (Redis-backed sliding window)" doc/security.md` returns a hit
    - `grep -c "Redis-backed sliding window" doc/security.md` returns 1
    - `grep -c "Redis-backed)" doc/security.md` returns 0 (the old wording is gone)
    - The change is on line 20: `sed -n '20p' doc/security.md` outputs the new wording
    - No other lines in `doc/security.md` changed (use `git diff doc/security.md` and confirm only line 20 differs)
    - `cd backend && pytest tests/test_rate_limit.py::test_doc_parity -x` exits 0 (the test from Task 2 now passes against the edited doc)
    - `cd backend && pytest tests/test_rate_limit.py -x` exits 0 (all 6 PROD-01-02 tests now green)
  </acceptance_criteria>
  <done>doc/security.md:20 reads `- Per-tenant API rate limiting: 200 requests per 60 seconds (Redis-backed sliding window)`, and the full test_rate_limit.py suite (including test_doc_parity, deferred from Task 2) passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/client → /api/* | Untrusted client requests; rate limit is a coarse availability boundary per tenant. |
| Backend → Redis | Internal trust; outages are availability events. Per D-05 the limiter is a safety valve, not authz. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-RL-zadd-race | D (DoS) — silent undercount via duplicate-member coalescing | TenantRateLimitMiddleware ZADD | high | mitigate | `member = f"{now_ms}:{uuid.uuid4().hex[:8]}"` — guarantees uniqueness even under sub-ms contention (RESEARCH Pitfall 1). Verified by `test_concurrent_burst_respects_limit`. |
| T-RL-toctou | D (DoS) — interleaved ops between replicas | pipeline atomicity | medium | mitigate | `redis_client.pipeline(transaction=True)` wraps the four commands as MULTI/EXEC; no other client can interleave. (RESEARCH Pattern 4 + D-02). |
| T-RL-redis-down | A (Availability) — outage blocks all traffic if fail-closed | RedisError catch | medium | accept (fail-OPEN by design) | Per D-05, the limiter is NOT a security boundary; outage allows traffic with a structured warning so operators see the gap. Verified by `test_fail_open_on_redis_down`. |
| T-RL-bypass-replica | D (DoS) — current bug: per-replica counters allow 2× cap | shared Redis sorted set | high | mitigate | A single Redis sorted set keyed `ratelimit:{tenant}` is shared across all replicas; adding the entry happens BEFORE the count is read (add-then-count algorithm). Verified by integration test in 01-03. |
| T-RL-tenant-spoof | S (Spoofing) — bad JWT lets attacker impersonate a tenant key | tenant key derivation | low | accept | Existing `decode_token` validates HS256 signature; on failure we fall to `"anonymous"`. The limit is global at 200, so spoofing only buys access to the anonymous bucket. Inherited unchanged from current code. |
| T-RL-anon-flood | D (DoS) — all anonymous traffic shares one bucket | `ratelimit:anonymous` key | low | accept | Acceptable for v1; Phase-future could add per-IP if abuse observed. CONTEXT.md "Deferred Ideas". |

Block severity: high. All `high` threats are mitigated above.
</threat_model>

<verification>
After all three tasks:

```
# Source code shape
grep -c "_rate_limit_store" backend/app/main.py            # → 0
grep -c "from collections import defaultdict" backend/app/main.py  # → 0
grep -F "uuid.uuid4().hex[:8]" backend/app/main.py
grep -F "transaction=True" backend/app/main.py
grep -F "pipe.zremrangebyscore" backend/app/main.py
grep -F "pipe.zadd(" backend/app/main.py
grep -F "pipe.zcard" backend/app/main.py
grep -F "pipe.expire(key, RATE_LIMIT_WINDOW)" backend/app/main.py

# Doc parity
grep -F "Redis-backed sliding window" doc/security.md

# Tests green
cd backend && pytest tests/test_rate_limit.py -v
```

All 6 tests must pass. The full suite must remain green:

```
cd backend && pytest -x
```
</verification>

<success_criteria>
- `_rate_limit_store` and `from collections import defaultdict` are gone from `backend/app/main.py`
- `TenantRateLimitMiddleware.dispatch` uses a 4-command pipeline (`zremrangebyscore`, `zadd`, `zcard`, `expire`) inside `pipeline(transaction=True)` with a uuid-suffixed member
- On `RedisError` the middleware fails open via `return await call_next(request)` after `logger.warning("redis_unavailable", subsystem="rate_limiter", error=str(e), tenant_id=...)`
- `count > RATE_LIMIT_REQUESTS` triggers 429 with `Retry-After: 60`
- `doc/security.md:20` reads `Per-tenant API rate limiting: 200 requests per 60 seconds (Redis-backed sliding window)` verbatim
- All 6 PROD-01-02 tests in `backend/tests/test_rate_limit.py` pass
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-multi-replica-state/01-02-SUMMARY.md` documenting:
- The exact diff to `backend/app/main.py` middleware body
- The list of 6 tests and their VALIDATION.md row IDs (01-02-01 through 01-02-06)
- The doc/security.md:20 change
- Confirmation that count > N (not >=) is the correct strict-greater-than choice for the add-then-count algorithm
</output>
