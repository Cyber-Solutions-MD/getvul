---
phase: 27-ticket-auto-drafting
plan: 03
type: execute
wave: 3
depends_on: [2]
files_modified:
  - frontend/src/components/ai/ai-explanation-section.tsx
  - frontend/src/components/vulnerabilities/drill-content.tsx
  - frontend/src/components/vulnerabilities/drill-panel-mobile.tsx
  - frontend/src/components/vulnerabilities/drill-panel.test.tsx
  - frontend/src/components/vulnerabilities/drill-panel-mobile.test.tsx
autonomous: true
requirements: [AID-01]

must_haves:
  truths:
    - "When Description and/or Remediation is not in the composed body, a compact 'Draft description with AI' / 'Draft remediation with AI' text-button renders — ONLY when an AI key is configured AND role is Analyst-or-above (Viewer / no-key → no button, D-17/D-23)"
    - "Clicking a gap-fill button triggers the existing useExplainStream(resourceType, id).start() (no new endpoint) and, on grounded success, APPENDS the labeled section to the current Description content — never overwriting an analyst's edits"
    - "Gap-fill degradation renders the exact locked caption per kind: insufficient-evidence (terminal), unsafe/danger (terminal, remediation only), busy (retryable amber), budget-exceeded (amber; Admin/Owner-only 'Raise the cap' link) — never a generic error"
    - "The in-flight state reuses the exact exported AnalyzingIndicator pulsing-dot — not a second spinner (D-12)"
    - "The mobile Drawer.NestedRoot renderConfirm renders an IDENTICAL Title Input + gap-fill row + composed Description, threading title into the mutation (Phase 25 divergence lesson, D-05)"
    - "Create is still never gated on draft state; no gap-fill path auto-submits (SC3)"
  artifacts:
    - path: "frontend/src/components/ai/ai-explanation-section.tsx"
      provides: "exported AnalyzingIndicator"
      contains: "export function AnalyzingIndicator"
    - path: "frontend/src/components/vulnerabilities/drill-content.tsx"
      provides: "gap-fill row (2 useExplainStream triggers, role/key gate, 6 typed states, append-on-success) threaded through renderConfirm args"
      contains: "useExplainStream"
    - path: "frontend/src/components/vulnerabilities/drill-panel-mobile.tsx"
      provides: "mirrored Title Input + gap-fill row in the mobile renderConfirm markup"
      contains: "ticket-title-input-mobile"
  key_links:
    - from: "gap-fill button onClick"
      to: "useExplainStream('vuln' | 'remediation-guidance', id).start()"
      via: "existing per-resource SSE trigger, require_analyst-gated, append-on-done"
      pattern: "useExplainStream\\("
    - from: "drill-panel-mobile.tsx renderConfirm"
      to: "threaded title/onTitleChange + gap-fill args from drill-content.tsx"
      via: "renderConfirm args object (same seam description uses)"
      pattern: "onTitleChange"
---

<objective>
Expand the desktop tracer to full coverage: export the private `AnalyzingIndicator`, add the compact "Draft with AI" gap-fill row (D-02) to the desktop dialog — two role/key-gated text-buttons that trigger the existing `useExplainStream` engine and append a labeled section on success with the full typed degradation matrix — and mirror the complete new surface (Title Input + gap-fill row + composed Description) into the divergent mobile `Drawer.NestedRoot` `renderConfirm` path, closing the Phase 25 divergence lesson (D-05).

Purpose: Finish AID-01 — analysts can fill gaps on-demand from inside the create dialog and get an identical experience on mobile, with graceful degradation and no new submit path.
Output: exported AnalyzingIndicator; gap-fill row in drill-content.tsx; mobile mirror in drill-panel-mobile.tsx; extended tests on both.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-ticket-auto-drafting/27-CONTEXT.md
@.planning/phases/27-ticket-auto-drafting/27-RESEARCH.md
@.planning/phases/27-ticket-auto-drafting/27-PATTERNS.md
@.planning/phases/27-ticket-auto-drafting/27-UI-SPEC.md

<mandatory_ui_read>
UI-touching plan — the executor MUST read `.claude/skills/sketch-findings-getvul/` before editing components (CLAUDE.md): `references/foundation.md` (tokens), `references/state-patterns.md`, `references/visual-language.md`, `references/copy-voice.md`. Every caption, color token, and the pulsing-dot are LOCKED in 27-UI-SPEC.md §4 + Copywriting/Color contract — reuse the exact `--color-amber`/`--color-danger`/`--color-violet` tokens and locked copy already established by DegradedCard; do NOT introduce a new token, a new spinner, or a button-shaped CTA for gap-fill (text-buttons only).
</mandatory_ui_read>

<interfaces>
<!-- Verified 2026-08-01. Do NOT re-explore. -->

frontend/src/components/ai/ai-explanation-section.tsx:
- line 104: function AnalyzingIndicator() { ... }   ← ADD `export` (no props, no module-private closure — safe verbatim); its internal call site (line 227) is unaffected.
- lines 92-102: CopyToDescriptionButton chrome — the exact text-button className to reuse for each gap-fill button: "mt-3 block text-xs font-medium text-text-muted underline-offset-2 hover:text-text hover:underline focus-visible:outline focus-visible:outline-2 focus-visible:outline-violet"
- lines 148-163: role/key gating source — `const role = user?.role ?? 'VIEWER'`; `isAdminOrOwner = role==='OWNER'||role==='ADMIN'`; `isAnalystOrAbove = isAdminOrOwner || role==='ANALYST'`; `keyConfigured = Boolean(useAiStatus().data?.configured)`. Reuse useAuth() + useAiStatus() identically.

frontend/src/lib/ai/use-explain-stream.ts — useExplainStream(resourceType, resourceId) returns { state, start }:
- state.phase: 'idle' | 'analyzing' | 'done' | 'error'
- phase 'done' → state.data: ExplainVulnResponse (has .summary, .grounded)
- phase 'error' → state.kind: 'busy' | 'grounded_false' | 'budget_exceeded' | 'unknown' | 'unsafe'

frontend/src/components/vulnerabilities/drill-content.tsx (post-Plan-02): has title/setTitle state, composedForId guard, Title Input, renderConfirm args with title/onTitleChange, the three useExplainCache reads. The gap-fill hooks/logic live HERE (hooks run in the component body) and are threaded to mobile via renderConfirm args.

frontend/src/components/vulnerabilities/drill-panel-mobile.tsx:
- lines 103-112: renderConfirm destructures { open, onConfirm, onCancel, cveLabel, ticketProvider, onProviderChange, description, onDescriptionChange }   ← ADD title, onTitleChange + gap-fill args
- lines 142-168: the mobile TicketProviderPicker div + the description Textarea block (id="ticket-description-textarea-mobile"). Mirror the Title Input (id="ticket-title-input-mobile") between them, and the gap-fill row above the Textarea — never imports ConfirmModal (separate code path).

LOCKED gap-fill copy (27-UI-SPEC.md Copywriting Contract):
- triggers: "Draft description with AI" / "Draft remediation with AI"
- in-flight: AnalyzingIndicator ("Analyzing this finding…")
- busy/unknown (amber, retryable): "AI busy — try again in a moment"
- budget_exceeded (amber; Admin/Owner see "Raise the cap" → /dashboard/connectors): "AI budget exceeded"
- grounded_false description (terminal): "Not enough finding data to explain this reliably"
- grounded_false remediation (terminal): "Not enough vendor guidance to recommend a fix"
- unsafe, remediation only (danger, terminal): "This guidance was withheld for safety"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: export AnalyzingIndicator + desktop gap-fill row (triggers, gating, typed states, append-on-success)</name>
  <files>frontend/src/components/ai/ai-explanation-section.tsx, frontend/src/components/vulnerabilities/drill-content.tsx, frontend/src/components/vulnerabilities/drill-panel.test.tsx</files>
  <read_first>
    - 27-UI-SPEC.md §4 (the full gap-fill interaction contract: at-most-two buttons via ['description','remediation'].filter(missing), 8px gap, gating, append-on-success preceded by a blank line, the 6 typed states) + §5 (never-auto-submit) + "UI Considerations" gap-fill rows.
    - 27-PATTERNS.md §"drill-content.tsx" Analog 4 (the CopyToDescriptionButton chrome + the isAnalystOrAbove trigger wiring to reuse; the useExplainStream direct-call architecture, bypassing AiExplanationSection) + §"ai-explanation-section.tsx — export AnalyzingIndicator" + "No Analog Found" (the 2-button row layout is genuinely new).
    - 27-RESEARCH.md Pitfall 4 (AnalyzingIndicator export) + "Don't Hand-Roll" (reuse useAiStatus + useAuth role gate; do not re-derive).
    - ai-explanation-section.tsx lines 92-113, 148-163; drill-content.tsx renderConfirm args + ConfirmModal children (post-Plan-02).
  </read_first>
  <behavior>
    - "missing" set = ['description','remediation'].filter(section not present in the composed body i.e. its cache is not a grounded hit). Zero missing → no buttons render.
    - No AI key configured OR role Viewer → no gap-fill buttons at all (composed body just lacks those sections; dialog fully usable).
    - Analyst+ with key, description missing → "Draft description with AI" button; remediation missing → "Draft remediation with AI"; both missing → both, 8px gap.
    - Click → useExplainStream('vuln' | 'remediation-guidance', id).start(); while phase==='analyzing' the button is replaced by AnalyzingIndicator.
    - phase 'done' grounded true → button disappears; "Description:\n{summary}" / "Remediation:\n{summary}" appended to CURRENT description value (blank line before it if non-empty); never overwrites existing content.
    - error.kind busy/unknown → button stays clickable + amber "AI busy — try again in a moment"; budget_exceeded → amber "AI budget exceeded" (Admin/Owner also "Raise the cap" link); grounded_false → terminal locked caption per resourceType; unsafe (remediation only) → danger "This guidance was withheld for safety", terminal, no partial content.
    - No gap-fill path calls createTicket.mutateAsync.
  </behavior>
  <action>
    In ai-explanation-section.tsx, add `export` to `function AnalyzingIndicator()` at line 104 (no other change; its line-227 call site is unaffected).

    In drill-content.tsx, compute the composed-body "missing" sections from the same CacheSection mapping used in Plan 02 (a section is missing when its useExplainCache data is not a grounded cache hit). Instantiate `useExplainStream('vuln', v.id ?? idOrCve)` and `useExplainStream('remediation-guidance', v.id ?? idOrCve)` at the component top level. Build a gap-fill row descriptor (which buttons to show, each button's state derived from its stream state, and an append handler that on a grounded 'done' does setDescription(prev => prev ? prev + "\n\n" + labeledSection : labeledSection)). Gate the whole row on keyConfigured && isAnalystOrAbove (reuse useAuth() role + useAiStatus() exactly as ai-explanation-section.tsx does; import AnalyzingIndicator from '@/components/ai/ai-explanation-section'). Render the row in the desktop ConfirmModal children ABOVE the Description block and BELOW the Title block, using the CopyToDescriptionButton text-button chrome (8px `gap-2` between two buttons), swapping in AnalyzingIndicator while analyzing and the LOCKED typed captions (amber/danger/muted tokens) per error.kind. Thread the entire gap-fill descriptor through the renderConfirm args (extend the args type) so mobile consumes it. Do NOT create a new endpoint, a new spinner, or a button-shaped CTA. Do NOT add any submit path.

    Extend drill-panel.test.tsx: gap-fill row renders the correct buttons for a missing-section cache state (Analyst+ & key), renders NOTHING for Viewer and for no-key; a click calls the stream start and, on a mocked grounded 'done', the appended labeled section is present in the textarea AND prior typed content is preserved; a mocked error.kind renders the exact locked caption (assert at least the busy/amber and grounded_false/terminal cases); and a negative assertion that gap-fill interactions never call mockMutateAsync.
  </action>
  <acceptance_criteria>
    - `grep -c 'export function AnalyzingIndicator' frontend/src/components/ai/ai-explanation-section.tsx` == 1; its internal usage at the analyzing branch still resolves (no duplicate component).
    - `grep -n 'useExplainStream' frontend/src/components/vulnerabilities/drill-content.tsx` shows two instances ('vuln', 'remediation-guidance'); `grep -n "from '@/components/ai/ai-explanation-section'" drill-content.tsx` imports AnalyzingIndicator (no copy-pasted second pulsing-dot).
    - Gap-fill row is gated on `keyConfigured` && `isAnalystOrAbove` (grep shows both in the row's render condition); the append handler uses a functional setDescription(prev => ...) that prepends a blank line only when prev is non-empty.
    - The locked captions "AI busy — try again in a moment", "AI budget exceeded", "Not enough finding data to explain this reliably", "Not enough vendor guidance to recommend a fix", "This guidance was withheld for safety", and triggers "Draft description with AI"/"Draft remediation with AI" appear verbatim in drill-content.tsx.
    - drill-panel.test.tsx: Viewer/no-key → no gap-fill button; Analyst+key → button; grounded-done append preserves prior content; a typed error caption asserted; mockMutateAsync NOT called by gap-fill.
    - `npx vitest run drill-panel ai-explanation-section` green; `tsc --noEmit` + `lint` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run drill-panel ai-explanation-section</automated>
  </verify>
  <done>AnalyzingIndicator exported; desktop gap-fill row renders/gates correctly, triggers the existing stream, appends-without-overwrite on success, shows the full typed degradation matrix with locked copy, never submits; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: mirror Title Input + gap-fill row + composed Description into the mobile renderConfirm path</name>
  <files>frontend/src/components/vulnerabilities/drill-panel-mobile.tsx, frontend/src/components/vulnerabilities/drill-panel-mobile.test.tsx</files>
  <read_first>
    - 27-UI-SPEC.md §5 (both desktop ConfirmModal and mobile Drawer.NestedRoot MUST render an identical Title + gap-fill + Description with identical copy/gating/append behavior — the divergence lesson) + §2/§4 for placement.
    - 27-PATTERNS.md §"drill-panel-mobile.tsx" (the description Textarea block lines 142-168; the renderConfirm destructuring lines 103-112; -mobile id suffix convention) and §"drill-panel-mobile.test.tsx" (the 3 description tests to mirror; existing mocks cover the new field).
    - 25-07-SUMMARY.md (how the mobile path mirrored the description Textarea — same seam, same relative position, never imports ConfirmModal).
    - drill-panel-mobile.tsx lines 100-168.
  </read_first>
  <behavior>
    - Mobile renderConfirm renders, between TicketProviderPicker and the description Textarea: the shared "AI-drafted — review before creating." caption, a "Title" label + Input id="ticket-title-input-mobile" bound to the threaded title/onTitleChange, then the gap-fill row (same buttons/gating/states as desktop, consuming the threaded descriptor), then the Description Textarea (label "Description", updated placeholder).
    - Typing into the mobile Title Input + Create threads title into mutateAsync (proven at the mutation boundary, mirroring the description test).
    - Blank title threads title: undefined.
    - Gap-fill on mobile behaves identically (append-on-success, typed captions) — no separate logic, just the threaded descriptor rendered.
  </behavior>
  <action>
    In drill-panel-mobile.tsx, add `title, onTitleChange` (and the gap-fill descriptor args) to the renderConfirm destructuring (lines 103-112). Between the TicketProviderPicker `<div className="mt-4">` and the description Textarea block, render a new `<div className="mt-4">` containing the shared LOCKED caption, a "Title" label (same 12px `mb-1 block text-xs font-medium text-text-muted` chrome), and `<Input id="ticket-title-input-mobile" value={title} onChange={(e)=>onTitleChange(e.target.value)} />` (import Input the same way the file imports Textarea), followed by the gap-fill row rendered from the threaded descriptor (identical chrome/copy/AnalyzingIndicator as desktop). Update the description block's label to "Description" and placeholder to "No AI draft available yet — add a description or leave blank." to match the desktop change. Keep the `-mobile` id suffix convention. Do not import ConfirmModal; do not add a submit path.

    Extend drill-panel-mobile.test.tsx: mirror the 3 description tests for the Title Input (renders between picker and action row with the locked caption + deterministic composed value; typing + confirm threads title into mockMutateAsync; blank threads title: undefined). Reuse the existing useProvidersMock/mockMutateAsync/mockMatchMedia fixtures unchanged.
  </action>
  <acceptance_criteria>
    - `grep -n 'ticket-title-input-mobile' frontend/src/components/vulnerabilities/drill-panel-mobile.tsx` shows the mobile Title Input; `grep -n 'onTitleChange' drill-panel-mobile.tsx` shows it in both the destructured args and the Input onChange.
    - The mobile file renders the gap-fill row from the threaded descriptor (grep shows the gap-fill trigger copy) and does NOT import ConfirmModal (`grep -c 'ConfirmModal' drill-panel-mobile.tsx` == 0).
    - The shared caption "AI-drafted — review before creating." and placeholder "No AI draft available yet — add a description or leave blank." appear verbatim in the mobile file.
    - drill-panel-mobile.test.tsx: Title renders with locked copy; typed title threads into mutateAsync; blank threads title: undefined.
    - `npx vitest run drill-panel-mobile` green; then full suite `npx vitest run` green; `tsc --noEmit` + `lint` clean on touched files.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run drill-panel-mobile</automated>
  </verify>
  <done>Mobile renderConfirm renders an identical Title Input + gap-fill row + composed Description with identical copy/gating/append behavior, threads title into the mutation, never imports ConfirmModal, never auto-submits; mobile tests + full frontend suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gap-fill trigger → on-demand AI generation | A paid model call; must be RBAC + key gated |
| stream result → local Description state | Appended AI text stays local until the human Create click |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Elevation of Privilege | gap-fill on-demand generation (D-17) | mitigate | Client-side row gated on `keyConfigured && isAnalystOrAbove` (reused verbatim from AiExplanationSection); the underlying `useExplainStream` routes are already require_analyst-gated server-side (defense in depth). Test: Viewer/no-key render no button. |
| T-27-03 | (Integrity / SC3) | gap-fill append + mobile mirror | mitigate | Gap-fill only calls `useExplainStream(...).start()` and appends to local state via functional setDescription; no path calls createTicket.mutateAsync. Negative test asserts mockMutateAsync uncalled by gap-fill. No `<form>` on mobile either. |
| T-27-05 | Information Disclosure | appended sections + mobile body | mitigate | Appended sections use only the AI `summary` (grounded output); the always-present Asset context still excludes owner PII (Pitfall 5); danger/unsafe kind renders NO partial content. |
| T-27-02 | (Reliability) | busy/budget/refusal degradation | mitigate | Each error.kind maps to its exact locked caption (never a generic error), so a degraded gap-fill is a visible, correctly-typed state — not a silent no-op. |
</threat_model>

<artifacts_this_phase_produces>
Phase 27 total artifact set (this plan delivers ✅):
- (Plan 01) title field + validator; create_tickets() fallback; CreateTicketRequest.title?
- (Plan 02) compose-ticket-draft.ts; drill-content.tsx title state + resourceId guard + Title Input + compose-on-open
- ✅ exported `AnalyzingIndicator` (ai-explanation-section.tsx)
- ✅ desktop gap-fill "Draft with AI" row in drill-content.tsx (2 useExplainStream triggers, role/key gate, 6 typed states, append-on-success)
- ✅ mobile mirror in drill-panel-mobile.tsx: Title Input + gap-fill row + composed Description, title threaded to the mutation
</artifacts_this_phase_produces>

<verification>
- `npx vitest run drill-panel drill-panel-mobile ai-explanation-section compose-ticket-draft` green; full `npx vitest run` green.
- `npx tsc --noEmit` + `npm run lint` clean.
- Manual (waived-class per 27-VALIDATION.md): live gap-fill generation + append; mobile parity; degradation cards — automated coverage proves gating/append/typed-states/never-submit in isolation.
</verification>

<success_criteria>
- AID-01 complete: on-demand gap-fill from inside the dialog (Analyst+/key-gated), append-without-overwrite, full typed degradation matrix, and identical desktop/mobile experience.
- SC3 held across every new gap-fill and mobile path — the human Create click remains the only submit.
- D-05 divergence lesson closed — desktop and mobile render identical Title + gap-fill + Description.
</success_criteria>

<output>
After completion, create `.planning/phases/27-ticket-auto-drafting/27-03-SUMMARY.md`
</output>
