---
phase: 27-ticket-auto-drafting
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - frontend/src/lib/tickets/compose-ticket-draft.ts
  - frontend/src/lib/tickets/compose-ticket-draft.test.ts
  - frontend/src/components/vulnerabilities/drill-content.tsx
  - frontend/src/components/vulnerabilities/drill-panel.test.tsx
autonomous: true
requirements: [AID-01]

must_haves:
  truths:
    - "Opening the create dialog for a vuln pre-fills the Title field deterministically as '[{sevLabel}] {cveLabel} on {hostsLine}', regardless of AI key configuration (SC1 title)"
    - "Opening the create dialog pre-fills the Description with a labeled multi-section plain-text body (Description → Remediation → Asset context → Prioritization), each AI section present only on a grounded cache hit (SC1 description/remediation/asset-context)"
    - "The 'Asset context:' section is ALWAYS present — it needs no AI call and renders even with no AI key configured (D-04)"
    - "The analyst can edit or clear the Title and Description; re-opening the SAME vuln's dialog never re-composes over those edits (composed-once guard)"
    - "Switching to a DIFFERENT vuln recomposes the draft — vuln A's title/description never carry onto a ticket created for vuln B (Pitfall 3 fixed)"
    - "Clicking Create threads the edited title into createTicket.mutateAsync; no effect/timer/keyboard shortcut ever submits (SC3)"
  artifacts:
    - path: "frontend/src/lib/tickets/compose-ticket-draft.ts"
      provides: "composeTicketTitle + composeTicketDescription pure functions"
      contains: "composeTicketDescription"
    - path: "frontend/src/lib/tickets/compose-ticket-draft.test.ts"
      provides: "cache-state-permutation unit tests (both missing / one missing / all present / prioritization on-off)"
      contains: "composeTicketTitle"
    - path: "frontend/src/components/vulnerabilities/drill-content.tsx"
      provides: "title/setTitle state, resourceId-keyed composedForId ref guard + compose-on-open effect, Title Input in ConfirmModal, renderConfirm args extended with title/onTitleChange, title threaded into fireTicket"
      contains: "composedForId"
  key_links:
    - from: "drill-content.tsx compose-on-open effect"
      to: "composeTicketTitle / composeTicketDescription"
      via: "useRef<string|null> compared against v.id ?? idOrCve, gated on confirmOpen"
      pattern: "composedForId\\.current"
    - from: "drill-content.tsx fireTicket()"
      to: "createTicket.mutateAsync body"
      via: "title: title || undefined alongside description"
      pattern: "title: title \\|\\| undefined"
---

<objective>
Ship the desktop tracer: a shared, unit-tested pure composition module (`compose-ticket-draft.ts`) and the desktop ConfirmModal wiring in `drill-content.tsx` — a deterministic editable Title field, a composed multi-section Description body, and a `resourceId`-keyed composed-once guard that solves three discovered edge cases (analyst edits preserved on re-open, the pre-existing "Copy into ticket description" interaction, and cross-vuln carryover) with one mechanism (RESEARCH Pattern 4). Proven end-to-end desktop: open → compose title+body → edit → Create threads the edited title into the mutation.

Purpose: Close SC1 (pre-fill) + SC2 (edit-everything) + SC3 (never auto-submit) on the desktop path before expanding to mobile + gap-fill (Plan 03).
Output: `compose-ticket-draft.ts` (+ test), and desktop Title/compose wiring in `drill-content.tsx` (+ `drill-panel.test.tsx`).
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-ticket-auto-drafting/27-CONTEXT.md
@.planning/phases/27-ticket-auto-drafting/27-RESEARCH.md
@.planning/phases/27-ticket-auto-drafting/27-PATTERNS.md
@.planning/phases/27-ticket-auto-drafting/27-UI-SPEC.md
@.planning/phases/25-asset-aware-remediation-guidance/25-07-SUMMARY.md

<mandatory_ui_read>
UI-touching plan — the executor MUST read `.claude/skills/sketch-findings-getvul/` before editing any component, per CLAUDE.md. Specifically: `references/foundation.md` (tokens), `references/state-patterns.md` (empty/loading/error), `references/copy-voice.md`. All new copy/tokens are already LOCKED in 27-UI-SPEC.md's Copywriting/Color/Typography contract — reuse verbatim, do not restyle the inherited dialog chrome or the Phase 25 Textarea.
</mandatory_ui_read>

<interfaces>
<!-- Verified 2026-08-01. Do NOT re-explore. -->

frontend/src/components/vulnerabilities/drill-content.tsx (the file this plan edits):
- line 89: const [ticketProvider, setTicketProvider] = useState<TicketProvider | null>(null);
- line 94: const [description, setDescription] = useState('');   ← the ticket-description state (already threaded)
- lines 60-74: renderConfirm?: (args: { open; onConfirm; onCancel; cveLabel; ticketProvider; onProviderChange; description; onDescriptionChange }) => ReactNode   ← EXTEND with title + onTitleChange
- lines 104-105: useEffect(() => { closeBtnRef.current?.focus(); }, [idOrCve]);   ← structural analog for an idOrCve-keyed effect (different purpose)
- line 147: const cveLabel = v.cve_id ?? v.id ?? idOrCve;
- line 155: const hostsLine = <joined affected-hosts string>;
- line 160: const sevLabel = <severity label>;
- v.cisa_kev (line 228 usage), v.exploit_available (233), v.affected_product available on FlexibleDetail; NO owner/department field exists (Pitfall 5 + D-06 owner-PII discipline — asset-context composes from host/product/severity ONLY; do not invent or include a department/owner field).
- fireTicket() lines 165-180: mutateAsync({ vulnerability_ids:[v.id ?? idOrCve], provider, description: description || undefined })   ← ADD title: title || undefined
- lines 384-393: renderConfirm({... description, onDescriptionChange: setDescription})   ← ADD title, onTitleChange: setTitle
- lines 398-417: desktop ConfirmModal children — TicketProviderPicker + the description <div className="mt-4"> block with <Textarea id="ticket-description-textarea">. Import Textarea is at line 13; import Input from '@/components/ui/input' the same way.

frontend/src/lib/queries/use-explain-cache.ts — useExplainCache(resourceType, resourceId): TanStack query. .data is `{ cached:false; groundable?; queued? } | ({ cached:true } & ExplainVulnResponse)`. On a cache hit, .data.summary (string) and .data.grounded (boolean) are present. resourceTypes: 'vuln', 'remediation-guidance', 'prioritization'.

CacheSection contract for the composer: map each useExplainCache().data to `{ grounded, summary }` when data?.cached === true, else null.

backend server title convention to mirror EXACTLY (so an unedited draft == server fallback): service.py:202 → `[{sev}] {cve} on {host}`. UI-SPEC §2 title format: `[{sevLabel}] {cveLabel} on {hostsLine}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compose-ticket-draft.ts pure module + exhaustive cache-state tests</name>
  <files>frontend/src/lib/tickets/compose-ticket-draft.ts, frontend/src/lib/tickets/compose-ticket-draft.test.ts</files>
  <read_first>
    - 27-RESEARCH.md Pattern 1 (the fully-worked composeTicketTitle/composeTicketDescription functions — use as the starting point) + the CacheSection type.
    - 27-UI-SPEC.md §3 "Composed Description body" (exact section order, labels, the always-present Asset context rules, the CISA-KEV/Exploit-available conditional lines, prioritization-only-when-cached, summary-not-business_risk).
    - 27-PATTERNS.md §"frontend/src/lib/tickets/compose-ticket-draft.ts" (module shape analog `lib/ticketing/providers.ts`; the `_build_task_description` Title-Case-with-colon voice) and §"compose-ticket-draft.test.ts" (the no-DOM `keys.test.ts` test shape).
  </read_first>
  <behavior>
    - composeTicketTitle({sevLabel:'Critical', cveLabel:'CVE-2026-1234', hostsLine:'web-01.internal'}) → "[Critical] CVE-2026-1234 on web-01.internal".
    - composeTicketDescription with all three caches null (no AI) → only the "Asset context:" section (Host/Product/Severity, plus CISA KEV / Exploit available lines only when true).
    - explain grounded → "Description:\n{summary}" section present; remediationGuidance grounded → "Remediation:\n{summary}"; a non-grounded or null cache → that section OMITTED entirely (never a labeled-but-empty stub).
    - prioritization grounded → "Prioritization:\n{summary}" appended last; null/non-grounded → omitted.
    - affectedProduct null → "Product: —"; cisaKev true → "CISA KEV: yes" line; exploitAvailable true → "Exploit available: yes" line.
    - Sections joined by exactly one blank line; only `summary` ever used, never business_risk.
  </behavior>
  <action>
    Create frontend/src/lib/tickets/compose-ticket-draft.ts exporting two pure functions (zero React/network imports): composeTicketTitle(params:{sevLabel;cveLabel;hostsLine}) returning the D-01 deterministic title, and composeTicketDescription(params:{explain;remediationGuidance;prioritization: CacheSection; hostsLine; affectedProduct; sevLabel; cisaKev; exploitAvailable}) building the labeled multi-section body per UI-SPEC §3. Define `type CacheSection = { grounded: boolean; summary: string } | null`. Include Description (explain) and Remediation only when that CacheSection is non-null AND grounded; Asset context always; Prioritization only when grounded. Use Title-Case-with-colon section labels matching the house `_build_task_description` voice. Do NOT read or reference any owner/department/assignee field (Pitfall 5 — it does not exist on VulnerabilityDetail/FlexibleDetail).

    Create frontend/src/lib/tickets/compose-ticket-draft.test.ts (plain Vitest, no @testing-library/react) covering every permutation the UI-SPEC "partial" row enumerates: all three null; only explain grounded; only remediation grounded; explain+remediation grounded; a cached-but-not-grounded section omitted; prioritization included vs excluded; cisaKev/exploitAvailable on/off; affectedProduct null → "—". Assert exact strings and blank-line separation.
  </action>
  <acceptance_criteria>
    - `frontend/src/lib/tickets/compose-ticket-draft.ts` exports composeTicketTitle and composeTicketDescription; `grep -c 'business_risk' frontend/src/lib/tickets/compose-ticket-draft.ts` == 0 (only summary is used).
    - A cached-but-grounded:false section is provably omitted (a test asserts the label string is absent for that case).
    - Asset context is present in the all-null case (a test asserts "Asset context:" appears with no AI cached at all).
    - `npx vitest run compose-ticket-draft` all green; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run compose-ticket-draft</automated>
  </verify>
  <done>Pure composer module + exhaustive permutation tests green; no owner/department reference; summary-only; Asset context always present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: desktop Title field + resourceId-keyed compose-on-open guard + mutation threading</name>
  <files>frontend/src/components/vulnerabilities/drill-content.tsx, frontend/src/components/vulnerabilities/drill-panel.test.tsx</files>
  <read_first>
    - 27-RESEARCH.md Pattern 4 (the `useRef<string|null>` composed-once guard keyed to resourceId, gated on confirmOpen; why a blank-string check is wrong) + Pitfalls 2 & 3.
    - 27-UI-SPEC.md §1 (compose-once pristine rule), §2 (Title Input placement/copy/deterministic format/Body-not-Mono), §5 (never-auto-submit; Create disabled only by !ticketProvider).
    - 27-PATTERNS.md §"drill-content.tsx" Analogs 1-3 (description state + renderConfirm threading + fireTicket; the ConfirmModal children block; the focus-effect idiom) and §"drill-panel.test.tsx" (the description tests to mirror + the Pitfall-2 copy-in test at lines 324-339 that MUST be updated).
    - drill-content.tsx lines 60-74, 89-105, 147-180, 384-417; drill-panel.test.tsx description-textarea describe block (lines 280-339).
  </read_first>
  <behavior>
    - First open of vuln A's confirm dialog: Title Input value === "[{sevLabel}] {cveLabel} on {hostsLine}"; Description contains the composed sections. True even with no AI key (title deterministic, Asset context always present).
    - Analyst edits Title, cancels, re-opens the SAME vuln: Title retains the edit (no recompose).
    - Analyst opens vuln A, cancels, row-switches to vuln B (idOrCve changes, panel not remounted), opens Create: Title/Description recompose for B (not A's).
    - Pitfall 2: analyst clicks main-panel "Copy into ticket description" (sets description) THEN opens the dialog for the first time → compose runs unconditionally on first open (Title populated, Asset context present) — the resourceId guard, not a blank-string check, governs.
    - Create click threads title: title || undefined into mutateAsync alongside description. Create button stays enabled/disabled solely by !ticketProvider — no new disable condition; no effect/timer submits.
  </behavior>
  <action>
    In drill-content.tsx: add `const [title, setTitle] = useState('')` (sibling of description at line 94) and `const composedForId = useRef<string | null>(null)`. Add a compose-on-open useEffect gated on confirmOpen: when confirmOpen is true, compute id = v.id ?? idOrCve; if composedForId.current === id return; set composedForId.current = id; call setTitle(composeTicketTitle({sevLabel, cveLabel, hostsLine})) and setDescription(composeTicketDescription({... three useExplainCache reads mapped to CacheSection + hostsLine/affectedProduct/sevLabel/cisaKev/exploitAvailable})). Add the three `useExplainCache('vuln'|'remediation-guidance'|'prioritization', v.id ?? idOrCve)` reads at the top-level of the component body (they are cheap cache reads, safe to call unconditionally). Map each .data to `{ grounded, summary }` when data?.cached === true else null.

    Extend the renderConfirm args type (lines 60-74) with `title: string` and `onTitleChange: (v: string) => void`; pass `title, onTitleChange: setTitle` in the renderConfirm(...) call (lines 384-393). In the desktop ConfirmModal children, add a new `<div className="mt-4">` Title block directly beneath TicketProviderPicker and above the description block: the shared caption "AI-drafted — review before creating." (12px label chrome, verbatim from UI-SPEC), a "Title" label, and `<Input id="ticket-title-input" value={title} onChange={(e)=>setTitle(e.target.value)} />` (import Input from '@/components/ui/input'). Update the description block's label to "Description" and its placeholder to "No AI draft available yet — add a description or leave blank." (UI-SPEC Copywriting Contract supersedes the stale Phase 25 caption/placeholder). In fireTicket(), add `title: title || undefined` to the mutateAsync body. Do NOT gate the Create button on title/description — leave the existing !ticketProvider condition untouched. Do NOT add any new useEffect/submit path.

    Update drill-panel.test.tsx: mirror the description tests for the Title Input (renders with locked caption + deterministic value on open; typing + Create threads title into mutateAsync). Update the existing Pitfall-2 test (lines 324-339, "copying remediation guidance in ... pre-fills the dialog textarea") to the new first-open-composes behavior (the dialog's first open composes the full body; assert the composed body per the chosen guard, not the bare copied summary). Add a cross-vuln reset test: open+cancel for one cve, re-render with a different mocked cve_id, open Create, assert Title recomposed to the new cve. Add a negative never-auto-submit assertion: rendering/opening/composing does NOT call mockMutateAsync until the Create button is clicked. Reuse the file's existing vi.fn()-backed useExplainCache/useAiStatus mocks (Phase 25 upgraded them) and the getAllByRole('dialog').slice(-1)[0] idiom.
  </action>
  <acceptance_criteria>
    - `grep -n 'composedForId' frontend/src/components/vulnerabilities/drill-content.tsx` shows a useRef guard compared against `v.id ?? idOrCve` inside a confirmOpen-gated effect (NOT a `title === ''` blank-string check).
    - `grep -c 'title: title || undefined' frontend/src/components/vulnerabilities/drill-content.tsx` == 1 (fireTicket threading).
    - `grep -n 'ticket-title-input' frontend/src/components/vulnerabilities/drill-content.tsx` shows the desktop Title Input; the shared caption "AI-drafted — review before creating." and placeholder "No AI draft available yet — add a description or leave blank." appear verbatim.
    - Create's disabled condition is still exactly `!ticketProvider` — `grep -n 'confirmDisabled\|disabled=' ...` shows no new title/description-based gate.
    - drill-panel.test.tsx contains: a title-threads-into-mutateAsync test, a cross-vuln recompose test, an updated Pitfall-2 test, and a never-auto-submit negative assertion (mockMutateAsync not called until Create clicked).
    - `npx vitest run drill-panel` green; `npx tsc --noEmit` + `npm run lint` clean on touched files.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run drill-panel compose-ticket-draft</automated>
  </verify>
  <done>Desktop dialog auto-composes Title + multi-section Description via a resourceId-keyed guard, preserves edits on same-vuln re-open, recomposes on vuln switch, threads title into the mutation, never auto-submits; drill-panel.test.tsx (incl. updated Pitfall-2 + cross-vuln + never-submit) green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cached AI outputs → composed ticket body (client) | AI-sourced summaries flow into the draft; must exclude owner PII |
| local component state → createTicket.mutateAsync | The only path a draft becomes a real ticket; must remain human-click-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | (Integrity / SC3 hard constraint) | compose-on-open effect + fireTicket | mitigate | Compose/setTitle/setDescription only touch local React state; the sole mutateAsync caller is the human Create onClick. A negative test asserts mockMutateAsync is NOT called by open/compose. No `<form>` wraps the inputs (Enter-to-submit structurally impossible). |
| T-27-05 | Information Disclosure | composeTicketDescription asset-context | mitigate | Asset context composes from host/product/severity/cisa_kev/exploit_available ONLY; VulnerabilityDetail/FlexibleDetail carry no owner/department/assignee field (Pitfall 5). `grep business_risk` in the composer == 0 — only `summary` is used. |
| T-27-06 | Tampering | Cross-vuln draft carryover (Pitfall 3) | mitigate | The composed-once guard stores the resourceId it composed for and recomposes when `v.id ?? idOrCve` changes, so vuln A's draft can never ship on vuln B's ticket. Test: cross-vuln recompose. |
</threat_model>

<artifacts_this_phase_produces>
Phase 27 total artifact set (this plan delivers ✅):
- (Plan 01) title field + validator; create_tickets() fallback; CreateTicketRequest.title?
- ✅ `frontend/src/lib/tickets/compose-ticket-draft.ts` — composeTicketTitle + composeTicketDescription
- ✅ drill-content.tsx: title state, resourceId-keyed composedForId guard + compose-on-open effect, Title Input, renderConfirm args extended, title threaded into fireTicket
- (Plan 03) exported AnalyzingIndicator; gap-fill row (desktop + mobile); mobile Title Input mirror
</artifacts_this_phase_produces>

<verification>
- `npx vitest run drill-panel compose-ticket-draft` green.
- `npx tsc --noEmit` + `npm run lint` clean.
- Manual (waived-class, per 27-VALIDATION.md): live browser pre-fill/edit/Create + no-key manual flow + vuln-switch — automated coverage proves the guard/threading/never-submit in isolation.
</verification>

<success_criteria>
- SC1 (title + description + remediation + asset-context pre-filled on open) proven on desktop.
- SC2 (edit every field) proven — Title and Description freely editable, edits survive re-open.
- SC3 (never auto-submit) proven — negative assertion + structural no-form guarantee.
- Pitfalls 2 & 3 closed by the single resourceId-keyed guard.
</success_criteria>

<output>
After completion, create `.planning/phases/27-ticket-auto-drafting/27-02-SUMMARY.md`
</output>
