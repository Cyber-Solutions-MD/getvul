---
phase: 14-remaining-screens
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/globals.css
  - frontend/src/components/connectors/types.ts
  - frontend/src/components/connectors/connector-mark.tsx
  - frontend/src/components/connectors/connector-mark.test.tsx
  - frontend/src/components/connectors/sync-status-pill.tsx
  - frontend/src/components/connectors/sync-status-pill.test.tsx
  - frontend/src/components/ui/ConfirmModal.tsx
  - frontend/src/components/ui/ExportButton.tsx
  - frontend/src/lib/queries/keys.ts
  - frontend/src/lib/queries/keys.test.ts
autonomous: true
requirements: [UX-06-01, UX-06-02, UX-06-03, UX-06-04]
must_haves:
  truths:
    - "All 14 connector providers render a brand-tinted gradient mark from a CSS variable (no inline hex)"
    - "A connector's sync health renders as a 4-state pill (ok/failed/never-synced/running) driven by last_sync_status"
    - "ConfirmModal and ExportButton render in sunset tokens (no raw gray-*/indigo-* palette)"
    - "queryKeys exposes cspm, settings, and directoryUsers namespaces for downstream hooks"
  artifacts:
    - path: "frontend/src/app/globals.css"
      provides: "11 new --gradient-provider-* tokens in the :root block"
      contains: "--gradient-provider-crowdstrike"
    - path: "frontend/src/components/connectors/connector-mark.tsx"
      provides: "ConnectorMark with 14-provider literal gradient lookup"
      exports: ["ConnectorMark"]
    - path: "frontend/src/components/connectors/sync-status-pill.tsx"
      provides: "4-state sync health pill"
      exports: ["SyncStatusPill"]
    - path: "frontend/src/lib/queries/keys.ts"
      provides: "cspm/settings/directoryUsers query-key namespaces"
      contains: "cspm:"
  key_links:
    - from: "frontend/src/components/connectors/connector-mark.tsx"
      to: "frontend/src/app/globals.css"
      via: "var(--gradient-provider-{type}) literal lookup"
      pattern: "var\\(--gradient-provider-"
    - from: "frontend/src/components/connectors/sync-status-pill.tsx"
      to: "last_sync_status field"
      via: "status → class mapping"
      pattern: "last_sync_status|ok|failed|running"
---

<objective>
Wave 0 — connector-domain foundation. Land the shared tokens, primitives, and cache-key
namespaces the four Phase 14 screens consume so the screen plans can run in parallel.

Purpose: D-SEQ-01 mandates foundation-before-screens. This plan delivers the connector/global
half: 11 new provider gradient tokens, the 14-provider `ConnectorMark`, the 4-state
`SyncStatusPill`, the sunset restyle of two carried-over v1 primitives (`ConfirmModal`,
`ExportButton`), and the `queryKeys` namespace extension every new TanStack hook imports.
Output: tokens + 2 new connector primitives + 2 restyled primitives + extended keys factory.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-remaining-screens/14-CONTEXT.md
@.planning/phases/14-remaining-screens/14-RESEARCH.md
@.claude/skills/sketch-findings-getvul/references/foundation.md
@.claude/skills/sketch-findings-getvul/references/visual-language.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

Existing provider-mark pattern (frontend/src/components/tickets/provider-mark.tsx) — MIRROR this for ConnectorMark:
```typescript
const PROVIDER_GRADIENTS: Record<TicketProvider, string> = {
  jira:   'var(--gradient-provider-jira)',
  asana:  'var(--gradient-provider-asana)',
  github: 'var(--gradient-provider-github)',
};
// literal lookup (NOT string interpolation — T-13-14 CSS-injection mitigation)
// span: 'inline-grid size-3.5 shrink-0 place-items-center rounded-[3px] text-[8px] font-bold leading-none text-white'
// style={{ background: gradient }} aria-label={provider}
```

Existing globals.css :root tokens (frontend/src/app/globals.css:56-62):
```css
:root {
  --gradient-provider-jira:   linear-gradient(135deg, #5C9CFF, #2684FF);
  --gradient-provider-asana:  linear-gradient(135deg, #FF8AA0, #F1506E);
  --gradient-provider-github: linear-gradient(135deg, #C7BAFF, #A78BFA);
}
```

ConnectorConfigResponse.last_sync_status field (backend/app/connectors/schemas.py):
  "ok" | "failed" | "syncing" | null (null = never synced)

Phase 13 status-pill visual contract (D-P-04) — reuse the color family:
  ok → border-severity-low/40 bg-severity-low/10 text-severity-low (green)
  failed → border-severity-critical/40 bg-severity-critical/10 text-severity-critical (red)
  running → border-amber/40 bg-amber/10 text-amber (amber, animated dot)
  never-synced → border-border-subtle bg-surface-2 text-text-faint (gray)

queryKeys factory (frontend/src/lib/queries/keys.ts) — append new namespaces to the existing object.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 11 provider gradient tokens + 14-provider ConnectorMark</name>
  <files>frontend/src/app/globals.css, frontend/src/components/connectors/types.ts, frontend/src/components/connectors/connector-mark.tsx, frontend/src/components/connectors/connector-mark.test.tsx</files>
  <read_first>
    - frontend/src/app/globals.css (lines 50-70 — the existing :root provider-token block to extend)
    - frontend/src/components/tickets/provider-mark.tsx (the exact pattern to mirror: literal lookup, span classes, aria-label)
    - frontend/src/components/tickets/types.ts (TicketProvider union — do NOT modify; create a separate ConnectorProvider union)
    - .planning/phases/14-remaining-screens/14-RESEARCH.md (## Token Discipline section — the exact 11 hex gradients + token-naming rule)
    - .claude/skills/sketch-findings-getvul/references/foundation.md (color token conventions; 135deg gradient convention)
  </read_first>
  <behavior>
    - Test 1: ConnectorMark for each of the 14 providers renders a span with aria-label equal to the provider and a non-empty background style.
    - Test 2: An unknown provider string (e.g. 'evilcorp') renders no gradient (background falls through to undefined) — proves the literal-lookup injection guard.
    - Test 3: globals.css contains all 11 new --gradient-provider-* token names.
  </behavior>
  <action>
    Append a new block to the EXISTING `:root` selector in frontend/src/app/globals.css (directly after the Phase 13 jira/asana/github tokens at lines 56-62), commented `/* Phase 14 — Additional provider gradient tokens (D-CONN-01). Hex lives ONCE here. */`. Add these 11 tokens VERBATIM (135deg to match Phase 13 convention):
      --gradient-provider-crowdstrike:      linear-gradient(135deg, #E04020, #C0301A);
      --gradient-provider-nessus:           linear-gradient(135deg, #00C176, #008F55);
      --gradient-provider-defender:         linear-gradient(135deg, #5C9CFF, #2563EB);
      --gradient-provider-wiz:              linear-gradient(135deg, #6ECCAF, #2E8B57);
      --gradient-provider-qualys:           linear-gradient(135deg, #E84E4E, #C23B3B);
      --gradient-provider-rapid7:           linear-gradient(135deg, #E87A2A, #C05C18);
      --gradient-provider-google_workspace: linear-gradient(135deg, #60A5FA, #2563EB);
      --gradient-provider-azure_entra_id:   linear-gradient(135deg, #818CF8, #4F46E5);
      --gradient-provider-okta:             linear-gradient(135deg, #818CF8, #4F46E5);
      --gradient-provider-jamf:             linear-gradient(135deg, #6EE7B7, #059669);
      --gradient-provider-intune:           linear-gradient(135deg, #818CF8, #4338CA);
      --gradient-provider-humaans:          linear-gradient(135deg, #67E8F9, #0E7490);
    (Note: jamf/intune/humaans cover the "enrichment" category; that is 12 lines but 11 NEW providers + the existing 3 = 14 total — google_workspace, azure_entra_id, okta are the 3 identity providers. Count the NEW token lines: crowdstrike, nessus, defender, wiz, qualys, rapid7, google_workspace, azure_entra_id, okta, jamf, intune, humaans = 12 NEW tokens covering the 11 non-ticket providers plus humaans; ship all 12 listed above.)

    Create frontend/src/components/connectors/types.ts exporting:
      export type ConnectorProvider =
        | 'crowdstrike' | 'nessus' | 'defender' | 'wiz' | 'qualys' | 'rapid7'
        | 'google_workspace' | 'azure_entra_id' | 'okta' | 'jamf' | 'intune' | 'humaans'
        | 'jira' | 'asana' | 'github';

    Create frontend/src/components/connectors/connector-mark.tsx EXACTLY mirroring tickets/provider-mark.tsx:
      - PROVIDER_GRADIENTS: Record<ConnectorProvider, string> mapping each provider to `var(--gradient-provider-{provider})` (literal entries, never `var(--gradient-provider-${p})` interpolation — T-13-14 guard).
      - PROVIDER_GLYPH: Record<ConnectorProvider, string> single-uppercase-letter glyphs (e.g. crowdstrike→'C', nessus→'N', wiz→'W', okta→'O', jira→'J', etc.).
      - ConnectorMark({ provider, className }) renders the same span as ProviderMark: `inline-grid size-3.5 shrink-0 place-items-center rounded-[3px] text-[8px] font-bold leading-none text-white`, `style={{ background: PROVIDER_GRADIENTS[provider] }}`, `aria-label={provider}`, text glyph child.
      - The page-layer caller lowercases the backend connector_type (e.g. "CROWDSTRIKE" → "crowdstrike") before passing; document this in a comment.

    Write connector-mark.test.tsx covering all 3 behaviors above.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/connectors/connector-mark.test.tsx && grep -c '^[^/]*--gradient-provider-' src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "--gradient-provider-(crowdstrike|nessus|defender|wiz|qualys|rapid7|google_workspace|azure_entra_id|okta|jamf|intune|humaans)" frontend/src/app/globals.css` returns 12 matches
    - `grep -c "var(--gradient-provider-\${" frontend/src/components/connectors/connector-mark.tsx` returns 0 (no string interpolation — injection guard intact)
    - `grep -c "ConnectorProvider" frontend/src/components/connectors/types.ts` returns ≥ 1
    - `cd frontend && npx vitest run src/components/connectors/connector-mark.test.tsx` passes all tests
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 14 providers map to a CSS-var gradient via literal lookup; unknown providers render no gradient; 12 new tokens live in globals.css; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SyncStatusPill (4-state) + queryKeys namespace extension</name>
  <files>frontend/src/components/connectors/sync-status-pill.tsx, frontend/src/components/connectors/sync-status-pill.test.tsx, frontend/src/lib/queries/keys.ts, frontend/src/lib/queries/keys.test.ts</files>
  <read_first>
    - frontend/src/lib/queries/keys.ts (the full existing factory — append, do not rewrite)
    - frontend/src/lib/queries/keys.test.ts (existing key tests — extend)
    - .planning/phases/13-tickets-list-detail/13-CONTEXT.md (§D-P-04 — the status-pill color family to reuse: leading dot + border/bg/text triad)
    - .claude/skills/sketch-findings-getvul/references/visual-language.md (status pill leading-dot convention)
  </read_first>
  <behavior>
    - Test 1: SyncStatusPill status="ok" renders label "Synced" with class text-severity-low.
    - Test 2: status="failed" renders "Failed" with text-severity-critical.
    - Test 3: status="running" renders "Syncing" with text-amber and a motion-safe animated dot.
    - Test 4: status=null renders "Never synced" with text-text-faint.
    - Test 5: queryKeys.cspm.list(opts) / .detail(id) / .stats() / .compliance() and queryKeys.settings.* and queryKeys.directoryUsers.* return stable arrays.
  </behavior>
  <action>
    Create frontend/src/components/connectors/sync-status-pill.tsx:
      - Props: `{ status: 'ok' | 'failed' | 'syncing' | null; className?: string }` (matches backend last_sync_status raw values; note backend uses "syncing" — map it to the "running" visual).
      - A leading dot `<span className="size-1.5 rounded-full bg-current" />` then the label, matching the Phase 13 pill shape.
      - Mapping (reuse Phase 13 D-P-04 token family, NOT raw palette):
          ok      → 'border-severity-low/40 bg-severity-low/10 text-severity-low', label "Synced"
          failed  → 'border-severity-critical/40 bg-severity-critical/10 text-severity-critical', label "Failed"
          syncing → 'border-amber/40 bg-amber/10 text-amber', label "Syncing", dot gets `motion-safe:animate-pulse`
          null    → 'border-border-subtle bg-surface-2 text-text-faint', label "Never synced"
      - Pill container classes: `inline-flex items-center gap-1.5 rounded-full border px-2 py-0.5 text-xs font-medium`.
      - Add `data-sync-status={status ?? 'never'}` for the test hook.

    Extend frontend/src/lib/queries/keys.ts — append THREE new namespaces inside the existing `queryKeys` object (after `tickets`), following the Phase 12/13 nesting style:
      cspm: {
        all: ['cspm'] as const,
        list: (opts: { filters: object; page: number }) => ['cspm', 'list', opts] as const,
        detail: (id: string) => ['cspm', 'detail', id] as const,
        stats: () => ['cspm', 'stats'] as const,
        compliance: () => ['cspm', 'compliance'] as const,
      },
      settings: {
        all: ['settings'] as const,
        tenant: () => ['settings', 'tenant'] as const,
        users: () => ['settings', 'users'] as const,
        auditLog: (opts: { action?: string; resource_type?: string; user_email?: string; page: number }) => ['settings', 'audit-log', opts] as const,
        groups: () => ['settings', 'groups'] as const,
      },
      directoryUsers: {
        all: ['directory-users'] as const,
        list: (opts: { filters: object; page: number; sort: string; order: string }) => ['directory-users', 'list', opts] as const,
        stats: () => ['directory-users', 'stats'] as const,
      },

    Extend keys.test.ts with assertions for the new namespaces (shape + stability).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/connectors/sync-status-pill.test.tsx src/lib/queries/keys.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "ok|failed|syncing" frontend/src/components/connectors/sync-status-pill.tsx` returns matches; `grep -c "gray-\|indigo-\|emerald-" frontend/src/components/connectors/sync-status-pill.tsx` returns 0 (no raw palette)
    - `grep -E "cspm:|settings:|directoryUsers:" frontend/src/lib/queries/keys.ts` returns all 3
    - `cd frontend && npx vitest run src/components/connectors/sync-status-pill.test.tsx src/lib/queries/keys.test.ts` passes
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SyncStatusPill renders all 4 states with sunset tokens + leading dot; queryKeys has cspm/settings/directoryUsers namespaces; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Sunset-restyle ConfirmModal + ExportButton</name>
  <files>frontend/src/components/ui/ConfirmModal.tsx, frontend/src/components/ui/ExportButton.tsx</files>
  <read_first>
    - frontend/src/components/ui/ConfirmModal.tsx (full file — only the className strings change; props/behavior stay identical)
    - frontend/src/components/ui/ExportButton.tsx (full file — restyle only; do NOT migrate auth to TanStack per RESEARCH anti-pattern)
    - frontend/src/styles/sunset.css (available tokens — note --color-blue and --color-coral do NOT exist; --color-info is the blue analog)
    - .planning/phases/14-remaining-screens/14-RESEARCH.md (Pitfall 2 + Pitfall 3 — exact token swaps)
  </read_first>
  <action>
    ConfirmModal.tsx — swap raw palette utilities for sunset tokens, keeping ALL props and behavior (open/title/message/confirmLabel/cancelLabel/variant/onConfirm/onCancel) and focus management unchanged:
      - backdrop: `bg-gray-900/*` or `bg-black/*` → `bg-surface/80 backdrop-blur-sm`
      - modal panel: `bg-gray-800`/`bg-gray-900` → `bg-surface-2 border border-border-subtle`
      - title text → `text-text`; message text → `text-text-muted`
      - cancel button → `border-border-subtle text-text-muted hover:bg-surface`
      - confirm button by variant: danger → `bg-severity-critical text-white hover:bg-severity-critical/90`; warning → `bg-amber text-surface hover:bg-amber/90`; info → `bg-violet text-white hover:bg-violet/90`
      - Remove every `gray-*`, `indigo-*`, `red-600` literal.

    ExportButton.tsx — restyle ONLY (do not touch the localStorage auth-token fetch logic):
      - `border-gray-700` → `border-border-subtle`
      - `text-gray-300` → `text-text-muted`
      - `hover:bg-gray-800` → `hover:bg-surface-2`
      - keep the resource/label/filters props and the blob-download behavior intact.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -rc "gray-\|indigo-\|red-600\|emerald-" src/components/ui/ConfirmModal.tsx src/components/ui/ExportButton.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "gray-[0-9]|indigo-[0-9]|red-600|emerald-[0-9]" frontend/src/components/ui/ConfirmModal.tsx frontend/src/components/ui/ExportButton.tsx` returns 0 matches
    - `grep -c "ConfirmModalProps" frontend/src/components/ui/ConfirmModal.tsx` returns ≥ 1 (props unchanged)
    - `grep -E "border-border-subtle|text-text-muted" frontend/src/components/ui/ExportButton.tsx` returns matches
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both primitives render entirely in sunset tokens; ConfirmModal props/behavior and ExportButton auth/download logic unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend connector_type → ConnectorMark CSS var | connector_type string flows into a CSS variable name lookup |
| user-selected resource → ExportButton URL | resource/filters values become a CSV export request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | ConnectorMark CSS-var lookup | mitigate | Literal `Record<ConnectorProvider,string>` lookup — arbitrary connector_type falls through to undefined, no `var(--gradient-provider-${x})` interpolation (acceptance criterion grep=0) |
| T-14-02 | Information disclosure | ExportButton auth token | accept | Pre-existing localStorage token mechanism unchanged; restyle-only per RESEARCH anti-pattern; export is tenant-scoped server-side |
| T-14-03 | Tampering | ConfirmModal variant prop | accept | variant is a typed union ('danger'|'warning'|'info'); no user-controlled string reaches a class name |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0
- `cd frontend && npx vitest run src/components/connectors/ src/lib/queries/keys.test.ts` green
- `grep -E "--gradient-provider-(crowdstrike|wiz|okta|humaans)" frontend/src/app/globals.css` returns matches
- No raw palette utilities in the two restyled primitives
</verification>

<success_criteria>
- 12 new provider gradient tokens in globals.css; ConnectorMark covers all 14 providers via literal lookup
- SyncStatusPill renders 4 states with the Phase 13 color family
- ConfirmModal + ExportButton fully sunset-tokenized
- queryKeys exposes cspm/settings/directoryUsers namespaces
</success_criteria>

<output>
After completion, create `.planning/phases/14-remaining-screens/14-00-SUMMARY.md`
</output>
