---
phase: 14-remaining-screens
plan: 03
type: execute
wave: 2
depends_on: [00]
files_modified:
  - frontend/src/lib/queries/use-cspm-findings.ts
  - frontend/src/lib/queries/use-cspm-findings.test.ts
  - frontend/src/lib/queries/use-cspm-detail.ts
  - frontend/src/components/cspm/finding-card.tsx
  - frontend/src/components/cspm/finding-card.test.tsx
  - frontend/src/components/cspm/finding-drill-content.tsx
  - frontend/src/components/cspm/finding-drill-content.test.tsx
  - frontend/src/components/cspm/compliance-framework-strip.tsx
  - frontend/src/components/cspm/compliance-framework-strip.test.tsx
  - frontend/src/components/cspm/cspm-bulk-bar.tsx
  - frontend/src/components/cspm/cspm-status-pill.tsx
  - frontend/src/components/cspm/microcopy.ts
  - frontend/src/app/(authed)/dashboard/cspm/page.tsx
  - frontend/src/app/(authed)/dashboard/cspm/page.test.tsx
autonomous: true
requirements: [UX-06-01]
must_haves:
  truths:
    - "CSPM renders a chip-bar above a finding list, a cloud segmented control (All/AWS/Azure/GCP), and a compliance frameworks rail"
    - "Clicking a finding opens the DrillPanel at ?finding=<id>&open=drill with cloud resource + framework mappings + remediation + status"
    - "Each finding card shows cloud provider gradient mark + severity glyph + resource_id (mono) + title + framework tags + CSPM status pill"
    - "Bulk Resolve/Ignore/Reopen calls POST /cspm/bulk-status with the right status mapping"
    - "Loading shows SkeletonTable, empty shows EmptyState, errors show PartialFailureBanner, status changes toast"
  artifacts:
    - path: "frontend/src/components/cspm/finding-drill-content.tsx"
      provides: "DrillPanel finding content slot (idKey='finding')"
      exports: ["FindingDrillContent"]
    - path: "frontend/src/components/cspm/compliance-framework-strip.tsx"
      provides: "compliance frameworks pass-rate rail"
      exports: ["ComplianceFrameworkStrip"]
    - path: "frontend/src/app/(authed)/dashboard/cspm/page.tsx"
      provides: "CSPM page with chip-bar + drill + cloud control + frameworks + cards"
      contains: "DrillPanel"
  key_links:
    - from: "frontend/src/app/(authed)/dashboard/cspm/page.tsx"
      to: "DrillPanel idKey='finding'"
      via: "?finding=<id>&open=drill"
      pattern: "idKey=\"finding\"|finding"
    - from: "frontend/src/components/cspm/cspm-bulk-bar.tsx"
      to: "POST /api/v1/cspm/bulk-status"
      via: "Resolve→REMEDIATED / Ignore→SUPPRESSED / Reopen→OPEN"
      pattern: "REMEDIATED|SUPPRESSED|bulk-status"
---

<objective>
Rebuild `/dashboard/cspm` (UX-06-01) — chip-bar filtered finding cards with a cloud segmented
control, a compliance frameworks rail, a DrillPanel `finding` content slot, and bulk
Resolve/Ignore/Reopen, on the sunset design system.

Purpose: maximum primitive reuse (DrillPanel idKey='finding' per D-CSPM-01, mirroring the
Phase 13 ticket slot); defer trend charts (D-CSPM-04 / UX-D-05).
Output: CSPM TanStack hooks, FindingCard, FindingDrillContent, ComplianceFrameworkStrip,
CspmBulkBar, CspmStatusPill, cspm page rewrite.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-remaining-screens/14-CONTEXT.md
@.planning/phases/14-remaining-screens/14-RESEARCH.md
@.planning/phases/14-remaining-screens/14-00-SUMMARY.md
@.claude/skills/sketch-findings-getvul/references/page-layouts.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md
@.claude/skills/sketch-findings-getvul/references/visual-language.md
@.claude/skills/sketch-findings-getvul/references/interaction-patterns.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md
@frontend/src/components/vulnerabilities/drill-panel.tsx
@frontend/src/components/ui/ChipBar.tsx
@frontend/src/components/connectors/connector-mark.tsx

<interfaces>
MisconfigSummary (backend/app/cspm/schemas.py — snake_case, no transform):
  { id, rule_id, rule_name, category, severity, source, status, resource_id, resource_name,
    resource_type, cloud_provider, first_detected_at, last_seen_at }
MisconfigResponse (drill detail) adds: rule_description, frameworks[], resource_region,
  cloud_account_id, cloud_account_name, remediation_info, remediation_url, remediated_at, details.

Endpoints:
  GET  /api/v1/cspm            (Viewer) params: page,page_size,severity[],source[],status[],category[],cloud_provider,resource_type,search → PaginatedResponse[MisconfigSummary]
  GET  /api/v1/cspm/stats      → { ..., by_cloud_provider: [{cloud_provider,count}], ... }
  GET  /api/v1/cspm/compliance → [{ name, total_controls, passed, failed, suppressed, pass_rate }]
  GET  /api/v1/cspm/{id}       → MisconfigResponse
  POST /api/v1/cspm/bulk-status (Analyst) body { ids: UUID[], status }

CSPM statuses: OPEN | IN_PROGRESS | REMEDIATED | SUPPRESSED | FALSE_POSITIVE
Bulk mapping (D-CSPM-03): Resolve→REMEDIATED, Ignore→SUPPRESSED, Reopen→OPEN.

CSPM status pill mapping (Pitfall 4 — distinct from ticket statuses):
  OPEN→violet, IN_PROGRESS→amber, REMEDIATED→severity-low(green), SUPPRESSED→text-muted gray, FALSE_POSITIVE→text-muted gray italic.

DrillPanel (frontend/src/components/vulnerabilities/drill-panel.tsx): props { id, idKey, ariaLabel, renderContent({id,onClose}), originRowRef }. URL: ?{idKey}=<id>&open=drill.
ChipBar: axes: ChipAxis[] (key, allowList REQUIRED, chips[]/derivedFromCounts).
queryKeys.cspm (keys.ts, added in 14-00): list/detail/stats/compliance.
Primitives: ChipBar, DrillPanel, SkeletonTable, EmptyState, PartialFailureBanner, BulkActionBar pattern (ticket-bulk-bar.tsx), useToast, ConnectorMark (cloud provider mark).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CSPM hooks + CspmStatusPill + FindingCard + ComplianceFrameworkStrip</name>
  <files>frontend/src/lib/queries/use-cspm-findings.ts, frontend/src/lib/queries/use-cspm-findings.test.ts, frontend/src/components/cspm/cspm-status-pill.tsx, frontend/src/components/cspm/finding-card.tsx, frontend/src/components/cspm/finding-card.test.tsx, frontend/src/components/cspm/compliance-framework-strip.tsx, frontend/src/components/cspm/compliance-framework-strip.test.tsx, frontend/src/components/cspm/microcopy.ts</files>
  <read_first>
    - frontend/src/lib/queries/use-vulnerabilities.ts (Phase 11 list-hook pattern with URL filters → query key)
    - frontend/src/lib/queries/keys.ts (queryKeys.cspm)
    - frontend/src/components/connectors/connector-mark.tsx (used as the cloud-provider gradient mark)
    - .planning/phases/13-tickets-list-detail/13-CONTEXT.md (§D-P-04 pill shape — but use the CSPM-specific status mapping from Pitfall 4, NOT ticket statuses)
    - .planning/phases/14-remaining-screens/14-RESEARCH.md (CSPM endpoint shapes + D-CSPM-02/03 + Pitfall 4)
    - .claude/skills/sketch-findings-getvul/references/visual-language.md (severity glyphs ■▲◆○, status pill leading dot)
  </read_first>
  <behavior>
    - Test 1: useCspmFindings(filters) GETs /api/v1/cspm with the filter params and returns the paginated items.
    - Test 2: useComplianceFrameworks GETs /api/v1/cspm/compliance returning the framework array.
    - Test 3: CspmStatusPill renders OPEN→violet, REMEDIATED→severity-low, SUPPRESSED→text-muted (distinct from ticket colors).
    - Test 4: FindingCard renders the cloud-provider gradient mark, severity glyph, resource_id in font-mono, title, framework tags, and a CspmStatusPill.
    - Test 5: ComplianceFrameworkStrip renders one cell per framework with its pass_rate as a percentage.
  </behavior>
  <action>
    Create frontend/src/lib/queries/use-cspm-findings.ts:
      - useCspmFindings(opts: { filters; page }) — useQuery(queryKeys.cspm.list({filters,page}), GET /api/v1/cspm with severity[]/source[]/status[]/cloud_provider/search params), staleTime 60_000, retry 1.
      - useCspmStats() — useQuery(queryKeys.cspm.stats(), GET /api/v1/cspm/stats).
      - useComplianceFrameworks() — useQuery(queryKeys.cspm.compliance(), GET /api/v1/cspm/compliance).
      - useBulkCspmStatus() — mutation POST /api/v1/cspm/bulk-status { ids, status }; onSuccess invalidate queryKeys.cspm.all + toast "Resolved {n} findings." / etc.
      snake_case fields; no transform.

    Create frontend/src/components/cspm/cspm-status-pill.tsx:
      Props { status: string; className? }. Leading dot + label. Mapping (Pitfall 4):
        OPEN → 'border-violet/40 bg-violet-soft text-violet'
        IN_PROGRESS → 'border-amber/40 bg-amber/10 text-amber'
        REMEDIATED → 'border-severity-low/40 bg-severity-low/10 text-severity-low'
        SUPPRESSED → 'border-border-subtle bg-surface-2 text-text-muted'
        FALSE_POSITIVE → 'border-border-subtle bg-surface-2 text-text-muted italic'
      Pill container same shape as SyncStatusPill (`inline-flex items-center gap-1.5 rounded-full border px-2 py-0.5 text-xs font-medium`). data-cspm-status={status}.

    Create frontend/src/components/cspm/microcopy.ts: empty-state copy ("Nothing matches these filters" + explained-why + clear-all/broaden CTAs + lightbulb), bulk action labels (Resolve/Ignore/Reopen), severity glyph map (CRITICAL ■ / HIGH ▲ / MEDIUM ◆ / LOW ○).

    Create frontend/src/components/cspm/finding-card.tsx:
      Props { finding: MisconfigSummary; selected: boolean; onSelect; onOpen }.
      - <ConnectorMark provider={finding.cloud_provider?.toLowerCase()} /> (AWS/AZURE/GCP → map to a sensible gradient; if no token, fall back to a neutral mark — comment this) + severity glyph (colored) + resource_id in `font-mono text-sm` + rule_name title + framework tags (small chips) + <CspmStatusPill status={finding.status} />.
      - A selection checkbox bound to selected/onSelect (for bulk). Click on the card body → onOpen(finding.id).
      - Card container sunset tokens; `data-finding-card`.

    Create frontend/src/components/cspm/compliance-framework-strip.tsx:
      Props { frameworks: Array<{ name; total_controls; passed; failed; suppressed; pass_rate }> }.
      - Horizontal strip of compact cells, each: framework name + pass_rate as "{n}%" + a thin progress bar (`bg-severity-low` for the passed portion). Sunset tokens. data-framework-strip.
    Write the three test files covering behaviors 1-5.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/queries/use-cspm-findings.test.ts src/components/cspm/finding-card.test.tsx src/components/cspm/compliance-framework-strip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "OPEN|REMEDIATED|SUPPRESSED" frontend/src/components/cspm/cspm-status-pill.tsx` returns matches; `grep -c "Completed\|Blocked" frontend/src/components/cspm/cspm-status-pill.tsx` returns 0 (not ticket statuses)
    - `grep -E "ConnectorMark|resource_id|CspmStatusPill|font-mono" frontend/src/components/cspm/finding-card.tsx` returns all
    - `grep -c "pass_rate" frontend/src/components/cspm/compliance-framework-strip.tsx` returns ≥ 1
    - `grep -rc "gray-[0-9]\|indigo-[0-9]" frontend/src/components/cspm/` returns 0
    - `cd frontend && npx vitest run src/lib/queries/use-cspm-findings.test.ts src/components/cspm/finding-card.test.tsx src/components/cspm/compliance-framework-strip.test.tsx` passes
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>CSPM hooks fetch findings/stats/compliance; FindingCard shows mark+glyph+resource_id+frameworks+status; framework strip shows pass-rates; status pill uses CSPM-specific colors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FindingDrillContent slot + CspmBulkBar</name>
  <files>frontend/src/components/cspm/finding-drill-content.tsx, frontend/src/components/cspm/finding-drill-content.test.tsx, frontend/src/lib/queries/use-cspm-detail.ts, frontend/src/components/cspm/cspm-bulk-bar.tsx</files>
  <read_first>
    - frontend/src/components/tickets/ticket-drill-content.tsx (Phase 13 ticket slot — MIRROR this exact structure for the finding slot)
    - frontend/src/components/tickets/ticket-bulk-bar.tsx (BulkActionBar pattern: fixed bottom, selectedCount, onBulkAction, onClearSelection)
    - frontend/src/components/cspm/cspm-status-pill.tsx + finding-card.tsx (Task 1 outputs)
    - .planning/phases/14-remaining-screens/14-RESEARCH.md (Pattern 1: DrillPanel content slot + D-CSPM-01/03)
    - .claude/skills/sketch-findings-getvul/references/interaction-patterns.md (drill panel body conventions)
  </read_first>
  <behavior>
    - Test 1: useCspmDetail(id) GETs /api/v1/cspm/{id} returning MisconfigResponse.
    - Test 2: FindingDrillContent renders rule_name, resource_id (mono), framework mappings, remediation_info, and a status row while data is present.
    - Test 3: FindingDrillContent renders SkeletonTable/skeleton while pending and PartialFailureBanner on error.
    - Test 4: CspmBulkBar with selectedCount>0 renders Resolve/Ignore/Reopen; clicking Resolve calls onBulkAction('REMEDIATED').
  </behavior>
  <action>
    Create frontend/src/lib/queries/use-cspm-detail.ts: useCspmDetail(id) — useQuery(queryKeys.cspm.detail(id), GET /api/v1/cspm/{id}), enabled: !!id.

    Create frontend/src/components/cspm/finding-drill-content.tsx mirroring ticket-drill-content.tsx:
      Props { findingId: string; onClose: () => void }.
      - useCspmDetail(findingId).
      - Header: <ConnectorMark provider={data.cloud_provider?.toLowerCase()} /> + rule_id (font-mono) + close button (aria-label "Close").
      - Body (scrollable): severity glyph + rule_name; resource block (resource_id mono + resource_name + resource_region + cloud_account_name); framework mappings (data.frameworks tags); rule_description; remediation_info + remediation_url ("View remediation →" external link); <CspmStatusPill status={data.status} />.
      - Loading → skeleton; error/!data → <PartialFailureBanner errors={[{code:'ERR',requestId:''}]} />.
      - Sunset tokens only.

    Create frontend/src/components/cspm/cspm-bulk-bar.tsx (mirror ticket-bulk-bar shape):
      Props { selectedCount: number; onBulkAction: (status: 'REMEDIATED'|'SUPPRESSED'|'OPEN') => void; onClearSelection: () => void; isPending?: boolean }.
      - Returns null when selectedCount===0.
      - `fixed inset-x-0 bottom-0 z-30 ... animate-in slide-in-from-bottom-2` bottom bar; "{n} selected" + buttons "Resolve" (→REMEDIATED), "Ignore" (→SUPPRESSED), "Reopen" (→OPEN), and a clear-selection ×.
      - Sunset tokens; data-cspm-bulk-bar.
    Write finding-drill-content.test.tsx covering behaviors 1-4 (include a CspmBulkBar assertion).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/cspm/finding-drill-content.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "useCspmDetail|resource_id|remediation_info|CspmStatusPill" frontend/src/components/cspm/finding-drill-content.tsx` returns all
    - `grep -E "REMEDIATED|SUPPRESSED|OPEN" frontend/src/components/cspm/cspm-bulk-bar.tsx` returns all 3
    - `grep -rc "gray-[0-9]\|indigo-[0-9]" frontend/src/components/cspm/finding-drill-content.tsx frontend/src/components/cspm/cspm-bulk-bar.tsx` returns 0
    - `cd frontend && npx vitest run src/components/cspm/finding-drill-content.test.tsx` passes
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Finding slot renders full detail with loading/error states; bulk bar exposes Resolve/Ignore/Reopen with correct status mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CSPM page rewrite (chip-bar + cloud control + frameworks + drill + states)</name>
  <files>frontend/src/app/(authed)/dashboard/cspm/page.tsx, frontend/src/app/(authed)/dashboard/cspm/page.test.tsx</files>
  <read_first>
    - frontend/src/app/(authed)/dashboard/cspm/page.tsx (full v1 — replace; delete all 4 tab subcomponents)
    - frontend/src/app/(authed)/dashboard/vulnerabilities/page.tsx (Phase 11 page composition — ChipBar + DrillPanel + URL-state template to mirror)
    - frontend/src/components/cspm/* (Tasks 1-2 outputs)
    - frontend/src/components/ui/ChipBar.tsx (axes config)
    - frontend/src/components/vulnerabilities/drill-panel.tsx (DrillPanel idKey/renderContent)
    - .planning/phases/14-remaining-screens/14-RESEARCH.md (CSPM axes example + D-CSPM-02 cloud control + D-X-01 states + URL pattern ?finding=...&open=drill)
    - .claude/skills/sketch-findings-getvul/references/state-patterns.md
  </read_first>
  <behavior>
    - Test 1: Page renders a ChipBar (severity/status/source axes), the cloud segmented control (All/AWS/Azure/GCP from by_cloud_provider), and a ComplianceFrameworkStrip above the finding cards.
    - Test 2: Clicking a finding card sets URL ?finding=<id>&open=drill and DrillPanel (idKey="finding") opens.
    - Test 3: isPending → SkeletonTable; zero findings → EmptyState; query error → PartialFailureBanner.
    - Test 4: Selecting findings shows CspmBulkBar; Resolve fires useBulkCspmStatus.
  </behavior>
  <action>
    Rewrite frontend/src/app/(authed)/dashboard/cspm/page.tsx as a 'use client' TanStack page (delete v1 tabs Findings/Compliance/Resources/Trends — Trends stays deferred per D-CSPM-04):
      - URL filter state via useUrlStateList (severity/status/source) + useUrlState (cloud_provider, search).
      - <ChipBar axes={CSPM_AXES}> with severity (■▲◆○ glyphs), status (OPEN/IN_PROGRESS/REMEDIATED/SUPPRESSED), source axes — each with the REQUIRED allowList.
      - Cloud segmented control (All / AWS / Azure / GCP) from useCspmStats().by_cloud_provider, bound to the cloud_provider URL param (D-CSPM-02).
      - <ComplianceFrameworkStrip frameworks={useComplianceFrameworks().data}> above the finding list.
      - Finding list: map useCspmFindings(filters).items → <FindingCard ... onOpen={(id)=> set URL ?finding=id&open=drill} selected/onSelect for bulk />.
      - <DrillPanel id={findingId} idKey="finding" ariaLabel="Finding detail" renderContent={({id,onClose})=><FindingDrillContent findingId={id} onClose={onClose} />} /> (+ DrillPanelMobile <900px).
      - <CspmBulkBar selectedCount onBulkAction={status=>useBulkCspmStatus} onClearSelection /> when selection non-empty.
      - State patterns (D-X-01): isPending → SkeletonTable (columns describing the finding row); empty → EmptyState (explained-why + clear-all/broaden CTAs + lightbulb); error → PartialFailureBanner(watchKeys=[queryKeys.cspm.list(...), queryKeys.cspm.stats()], onRetry); status changes → toast.
      - No `gray-*`/`indigo-*`; no useEffect-fetch.
    Write page.test.tsx covering behaviors 1-4.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/(authed)/dashboard/cspm/page.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "ChipBar|DrillPanel|ComplianceFrameworkStrip" "frontend/src/app/(authed)/dashboard/cspm/page.tsx"` returns all 3
    - `grep -E "idKey=\"finding\"|cloud_provider" "frontend/src/app/(authed)/dashboard/cspm/page.tsx"` returns matches
    - `grep -E "SkeletonTable|EmptyState|PartialFailureBanner" "frontend/src/app/(authed)/dashboard/cspm/page.tsx"` returns all 3
    - `grep -rcE "gray-[0-9]|indigo-[0-9]|useEffect.*fetch|<Trend" "frontend/src/app/(authed)/dashboard/cspm/page.tsx"` returns 0 (no v1 palette, no inline fetch, no trend chart — D-CSPM-04 deferral)
    - `cd frontend && npx vitest run "src/app/(authed)/dashboard/cspm/page.test.tsx"` passes
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>CSPM page renders chip-bar + cloud control + frameworks rail + finding cards on TanStack data; finding drill opens at ?finding=...&open=drill; bulk status works; state-pattern complete; trends deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL filter values → /cspm query | reflected severity/status/cloud_provider values become query params |
| client → POST /cspm/bulk-status | bulk status transitions cross to the backend (Analyst-gated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10 | Tampering | ChipBar URL filter values | mitigate | Every ChipAxis declares a REQUIRED allowList; useUrlStateList clamps reflected values to the allowList (T-12-05 XSS guard) — arbitrary URL values are dropped |
| T-14-11 | Elevation of privilege | bulk-status mutation | accept (backend-enforced) | POST /cspm/bulk-status requires Analyst+ server-side; a Viewer's bulk action returns 403 and surfaces as an error toast — backend is the authority |
| T-14-12 | Tampering | cloud_provider gradient mark lookup | mitigate | ConnectorMark literal lookup — unknown cloud_provider falls through to no gradient (no var injection) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0
- `cd frontend && npx vitest run src/components/cspm/ src/lib/queries/use-cspm-findings.test.ts "src/app/(authed)/dashboard/cspm/"` green
- `grep -rE "gray-[0-9]|indigo-[0-9]" "frontend/src/app/(authed)/dashboard/cspm/page.tsx"` returns 0
</verification>

<success_criteria>
- Chip-bar + side-panel (idKey='finding') + cloud segmented control + compliance frameworks rail + finding cards (UX-06-01)
- Bulk Resolve/Ignore/Reopen via /cspm/bulk-status with correct status mapping
- CSPM-specific status pill (not ticket colors); trends deferred (D-CSPM-04)
- Loading/empty/error/toast present (D-X-01)
</success_criteria>

<output>
After completion, create `.planning/phases/14-remaining-screens/14-03-SUMMARY.md`
</output>
