---
phase: 14-remaining-screens
plan: 04
type: execute
wave: 2
depends_on: [00]
files_modified:
  - frontend/src/lib/queries/use-directory-users.ts
  - frontend/src/lib/queries/use-directory-users.test.ts
  - frontend/src/lib/queries/use-tenant-groups.ts
  - frontend/src/components/users/source-pill.tsx
  - frontend/src/components/users/source-pill.test.tsx
  - frontend/src/components/users/directory-table.tsx
  - frontend/src/components/users/directory-table.test.tsx
  - frontend/src/components/users/users-export-bar.tsx
  - frontend/src/components/users/microcopy.ts
  - frontend/src/app/(authed)/dashboard/users/page.tsx
  - frontend/src/app/(authed)/dashboard/users/page.test.tsx
autonomous: true
requirements: [UX-06-03]
must_haves:
  truths:
    - "The users directory renders a row per person with an enrichment-source pill (idp_source) and a job-title/department chip"
    - "A Directory / Groups segmented toggle (not horizontal tabs) switches views and persists in the URL"
    - "Row selection shows an export-only bulk bar whose only action is Export selected (CSV via ExportButton)"
    - "Chip-bar filters (status/department/source) + search drive URL-synced TanStack queries"
    - "Loading shows SkeletonTable, empty shows EmptyState, errors show PartialFailureBanner"
  artifacts:
    - path: "frontend/src/components/users/source-pill.tsx"
      provides: "idp_source enrichment-source pill"
      exports: ["SourcePill"]
    - path: "frontend/src/components/users/directory-table.tsx"
      provides: "directory table with source pill + title/department chips + selection"
      exports: ["DirectoryTable"]
    - path: "frontend/src/app/(authed)/dashboard/users/page.tsx"
      provides: "directory + groups page with segmented toggle"
      contains: "DirectoryTable"
  key_links:
    - from: "frontend/src/components/users/directory-table.tsx"
      to: "idp_source + job_title + department"
      via: "SourcePill + title/department chip"
      pattern: "idp_source|job_title|department"
    - from: "frontend/src/components/users/users-export-bar.tsx"
      to: "ExportButton resource=users"
      via: "Export selected with selected IDs"
      pattern: "ExportButton|Export selected"
---

<objective>
Rebuild `/dashboard/users` (UX-06-03) — the people DIRECTORY with enrichment-source pills,
job-title/department chips, a Directory/Groups segmented toggle, and an export-only bulk bar,
on the sunset design system.

Purpose: D-USR-01 reinterprets UX-06-03's "role pills + IdP-source pills" for the directory data
model — source pill = idp_source enrichment source; "role" context = job_title/department (NOT
RBAC role). Writable RBAC actions live in Workspace settings (Plan 14-05), not here (D-USR-02).
Output: directory + groups TanStack hooks, SourcePill, DirectoryTable, UsersExportBar, page rewrite.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-remaining-screens/14-CONTEXT.md
@.planning/phases/14-remaining-screens/14-RESEARCH.md
@.planning/phases/14-remaining-screens/14-00-SUMMARY.md
@.claude/skills/sketch-findings-getvul/references/page-layouts.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md
@.claude/skills/sketch-findings-getvul/references/visual-language.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md
@frontend/src/components/ui/ChipBar.tsx
@frontend/src/components/ui/ExportButton.tsx
@frontend/src/components/states/index.ts

<interfaces>
GET /api/v1/users/directory (authenticated; envelope { items, total, page, page_size, pages }) item shape (snake_case):
  { id, email, display_name, role, department, job_title, idp_source, is_active, groups[],
    avatar_url, last_login_at, device_count, devices[], max_risk_score, total_vulns, critical_vulns, high_vulns, exploitable_vulns }
  params: page,page_size,search,status(active|suspended|all),department,source,sort_by,sort_dir
GET /api/v1/users/stats → { total_users, active, suspended, by_source, departments[{name,count}], ... }
GET /api/v1/tenant/groups (Admin) → [{ name, member_count, members[{id,email,display_name,role,department}] }]
GET /api/v1/tenant/groups/export → CSV stream (ExportButton resource="groups")

D-USR-01 mapping: SourcePill uses idp_source (google→blue, azure→blue, okta→indigo, humaans→cyan, local→gray). "role" context = job_title + department chip — NOT the RBAC role field (Pitfall 7).
D-USR-02: bulk bar action = Export selected ONLY (ExportButton resource="users" filters={ids}).
D-USR-03: Directory / Groups = sunset SEGMENTED TOGGLE (not tabs), URL ?view=directory|groups.

queryKeys.directoryUsers (keys.ts, added 14-00): list/stats. queryKeys.settings.groups for groups.
Primitives: ChipBar, ExportButton (restyled 14-00), SkeletonTable, EmptyState, PartialFailureBanner, Avatar (2-char initials), useToast.
ExportButton: <ExportButton resource="users" label="Export selected" filters={{ ids: selectedIds }} /> — do NOT refactor its auth (RESEARCH anti-pattern).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Directory/groups hooks + SourcePill + DirectoryTable</name>
  <files>frontend/src/lib/queries/use-directory-users.ts, frontend/src/lib/queries/use-directory-users.test.ts, frontend/src/lib/queries/use-tenant-groups.ts, frontend/src/components/users/source-pill.tsx, frontend/src/components/users/source-pill.test.tsx, frontend/src/components/users/directory-table.tsx, frontend/src/components/users/directory-table.test.tsx, frontend/src/components/users/microcopy.ts</files>
  <read_first>
    - frontend/src/app/(authed)/dashboard/users/page.tsx (v1 — note the SOURCE_COLORS map + ExportButton usage to port and restyle)
    - frontend/src/lib/queries/use-assets.ts (Phase 12 list-hook with URL filters → key, paginated envelope handling)
    - frontend/src/lib/queries/keys.ts (queryKeys.directoryUsers + queryKeys.settings.groups)
    - frontend/src/components/ui/Avatar.tsx (2-char initials for the person row)
    - .planning/phases/14-remaining-screens/14-RESEARCH.md (/directory response shape + D-USR-01 mapping + Pitfall 7 role-vs-RBAC)
    - .claude/skills/sketch-findings-getvul/references/visual-language.md (pill chrome, mono for identifiers)
  </read_first>
  <behavior>
    - Test 1: useDirectoryUsers(filters) GETs /api/v1/users/directory with the params and returns the items envelope.
    - Test 2: SourcePill renders idp_source 'google'→blue, 'okta'→indigo, 'humaans'→cyan, 'local'→gray with the source label.
    - Test 3: DirectoryTable renders a row per person with Avatar + display_name + email + a SourcePill (idp_source) + a job_title/department chip.
    - Test 4: DirectoryTable does NOT render the RBAC role (OWNER/ADMIN/etc.) as a chip (Pitfall 7) — only job_title/department.
    - Test 5: Toggling a row's selection checkbox calls onSelect with the row id.
  </behavior>
  <action>
    Create frontend/src/lib/queries/use-directory-users.ts:
      - useDirectoryUsers(opts: { filters; page; sort; order }) — useQuery(queryKeys.directoryUsers.list(opts), GET /api/v1/users/directory with search/status/department/source/sort_by/sort_dir), staleTime 60_000.
      - useDirectoryStats() — useQuery(queryKeys.directoryUsers.stats(), GET /api/v1/users/stats).
    Create frontend/src/lib/queries/use-tenant-groups.ts:
      - useTenantGroups() — useQuery(queryKeys.settings.groups(), GET /api/v1/tenant/groups).

    Create frontend/src/components/users/source-pill.tsx:
      Props { source: string; className? }. idp_source → sunset token mapping:
        google → 'text-info border-info/40 bg-info/10'
        azure  → 'text-info border-info/40 bg-info/10'
        okta   → 'text-violet border-violet/40 bg-violet-soft'
        humaans→ 'text-info border-info/40 bg-info/10'  (cyan analog; --color-info is the blue/cyan token)
        local  → 'text-text-faint border-border-subtle bg-surface-2'
      Pill: `inline-flex items-center rounded-full border px-2 py-0.5 text-xs font-mono`. Label = source. data-source-pill={source}.

    Create frontend/src/components/users/microcopy.ts: empty-state copy ("No people match these filters" + explained-why + clear-all CTA + lightbulb), export label "Export selected".

    Create frontend/src/components/users/directory-table.tsx:
      Props { users: DirectoryUser[]; selectedIds: string[]; onSelect: (id: string) => void; onSelectAll?: () => void }.
      - Columns: selection checkbox · person (Avatar 2-char + display_name + email mono) · Source (<SourcePill source={u.idp_source} />) · Title/Dept (job_title + department chip — NOT u.role) · Devices (device_count) · Risk (max_risk_score).
      - Do NOT render u.role anywhere as a displayed chip (Pitfall 7).
      - Sunset tokens; `data-directory-table`; row `data-user-row`.
    Write the three test files covering behaviors 1-5.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/queries/use-directory-users.test.ts src/components/users/source-pill.test.tsx src/components/users/directory-table.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "idp_source|job_title|department" frontend/src/components/users/directory-table.tsx` returns all 3
    - `grep -c "u.role\|user.role\|\.role" frontend/src/components/users/directory-table.tsx` returns 0 (RBAC role not displayed — Pitfall 7)
    - `grep -E "google|okta|humaans|local" frontend/src/components/users/source-pill.tsx` returns matches; `grep -c "gray-[0-9]\|indigo-[0-9]" frontend/src/components/users/source-pill.tsx` returns 0
    - `cd frontend && npx vitest run src/lib/queries/use-directory-users.test.ts src/components/users/source-pill.test.tsx src/components/users/directory-table.test.tsx` passes
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Directory hooks fetch people + groups; SourcePill maps idp_source to sunset tokens; DirectoryTable shows person + source pill + title/department (never RBAC role); selection works.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UsersExportBar + users page rewrite (segmented toggle + chip-bar + states)</name>
  <files>frontend/src/components/users/users-export-bar.tsx, frontend/src/app/(authed)/dashboard/users/page.tsx, frontend/src/app/(authed)/dashboard/users/page.test.tsx</files>
  <read_first>
    - frontend/src/app/(authed)/dashboard/users/page.tsx (full v1 — replace; port ExportButton + directory/groups toggle, restyle, convert tabs→segmented toggle)
    - frontend/src/components/users/directory-table.tsx + source-pill.tsx (Task 1 outputs)
    - frontend/src/components/tickets/view-toggle.tsx (Phase 13 List/Board segmented toggle — MIRROR for Directory/Groups, NOT horizontal tabs)
    - frontend/src/components/ui/ChipBar.tsx (axes config) + frontend/src/components/ui/ExportButton.tsx (resource/label/filters)
    - .planning/phases/14-remaining-screens/14-RESEARCH.md (D-USR-02 export-only bulk bar + D-USR-03 segmented toggle + D-X-01 states + URL ?view=directory|groups)
    - .claude/skills/sketch-findings-getvul/references/state-patterns.md
  </read_first>
  <behavior>
    - Test 1: UsersExportBar with selectedIds.length>0 renders an ExportButton (resource="users", label "Export selected") wired with the selected ids; returns null when none selected.
    - Test 2: Page renders a Directory/Groups segmented toggle (not horizontal tabs) bound to ?view; default view=directory.
    - Test 3: Page renders a ChipBar with status/department/source axes above the directory table.
    - Test 4: isPending → SkeletonTable; zero results → EmptyState; query error → PartialFailureBanner.
    - Test 5: Switching to view=groups renders the groups list (useTenantGroups) with a "Export groups" ExportButton.
  </behavior>
  <action>
    Create frontend/src/components/users/users-export-bar.tsx:
      Props { selectedIds: string[]; onClearSelection: () => void }.
      - Returns null when selectedIds.length===0.
      - `fixed inset-x-0 bottom-0 z-30 ... animate-in slide-in-from-bottom-2` bar: "{n} selected" + <ExportButton resource="users" label="Export selected" filters={{ ids: selectedIds }} /> + clear × . No write actions (D-USR-02). data-users-export-bar.

    Rewrite frontend/src/app/(authed)/dashboard/users/page.tsx as a 'use client' TanStack page:
      - useUrlState('view','directory') for the Directory/Groups segmented toggle (MIRROR Phase 13 view-toggle component shape; NO border-b tabs).
      - Directory view: URL filter state (status/department/source via useUrlStateList/useUrlState + search). <ChipBar axes={USERS_AXES}> (status: active/suspended/all; department: derived from stats.departments; source: derived from stats.by_source — each axis with REQUIRED allowList). <DirectoryTable users selectedIds onSelect /> + selection state → <UsersExportBar selectedIds onClearSelection />.
      - Groups view: <list of useTenantGroups().data> with member_count + an <ExportButton resource="groups" label="Export groups" />.
      - State patterns (D-X-01): isPending → SkeletonTable; empty → EmptyState (explained-why + clear-all CTA + lightbulb); error → PartialFailureBanner(watchKeys=[queryKeys.directoryUsers.list(...)], onRetry).
      - Delete v1 horizontal-tab markup; no `gray-*`/`indigo-*`; no useEffect-fetch.
    Write page.test.tsx covering behaviors 1-5.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/(authed)/dashboard/users/page.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "ExportButton|Export selected" frontend/src/components/users/users-export-bar.tsx` returns matches
    - `grep -E "DirectoryTable|ChipBar|SkeletonTable|EmptyState|PartialFailureBanner" "frontend/src/app/(authed)/dashboard/users/page.tsx"` returns all
    - `grep -rcE "border-b-2|border-b border-indigo|gray-[0-9]|indigo-[0-9]|useEffect.*fetch" "frontend/src/app/(authed)/dashboard/users/page.tsx"` returns 0 (no tabs, no v1 palette, no inline fetch)
    - `cd frontend && npx vitest run "src/app/(authed)/dashboard/users/page.test.tsx"` passes
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Users page renders directory with chip-bar + source/title-dept chips + export-only bulk bar, a Directory/Groups segmented toggle (no tabs), and groups export; state-pattern complete; v1 surface deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL filter values → /directory query | reflected status/department/source values become query params |
| selected ids → ExportButton CSV request | selected row ids become an export filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-13 | Tampering | ChipBar URL filter values | mitigate | Every ChipAxis declares a REQUIRED allowList; useUrlStateList clamps reflected values (T-12-05) |
| T-14-14 | Information disclosure | directory/groups export | accept (backend tenant-scoped) | ExportButton hits /api/v1/export/{resource}; backend scopes to the caller's tenant — no cross-tenant rows; export uses the existing (unchanged) auth token |
| T-14-15 | Information disclosure | RBAC role on directory | mitigate | Directory deliberately does NOT render the RBAC role field (Pitfall 7); writable RBAC actions live only in Workspace settings (Plan 14-05) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0
- `cd frontend && npx vitest run src/components/users/ src/lib/queries/use-directory-users.test.ts "src/app/(authed)/dashboard/users/"` green
- `grep -rE "border-b-2|gray-[0-9]|indigo-[0-9]" "frontend/src/app/(authed)/dashboard/users/page.tsx"` returns 0
</verification>

<success_criteria>
- Directory rows: enrichment-source pill (idp_source) + job-title/department chip (not RBAC role)
- Directory/Groups segmented toggle (no tabs), URL-persisted
- Export-only bulk bar (Export selected via ExportButton); groups export
- Loading/empty/error present (D-X-01)
</success_criteria>

<output>
After completion, create `.planning/phases/14-remaining-screens/14-04-SUMMARY.md`
</output>
