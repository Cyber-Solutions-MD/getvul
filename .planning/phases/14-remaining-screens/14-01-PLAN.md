---
phase: 14-remaining-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/settings/settings-sidebar-shell.tsx
  - frontend/src/components/settings/settings-sidebar-shell.test.tsx
  - frontend/src/components/settings/save-bar.tsx
  - frontend/src/components/settings/save-bar.test.tsx
  - frontend/src/components/settings/use-dirty-state.ts
  - frontend/src/components/settings/microcopy.ts
autonomous: true
requirements: [UX-06-04]
must_haves:
  truths:
    - "Settings renders a 220px category sidebar + a right pane; selecting a category swaps the pane"
    - "The sidebar lists only the categories the current role can access (Profile + API tokens always; Workspace/SAML/Notifications/Audit gated to Admin+)"
    - "A sticky per-category save bar appears only when a pane is dirty and offers Save changes / Discard"
    - "There is no horizontal-tab pattern anywhere in the shell (no border-b-2 active indicator)"
  artifacts:
    - path: "frontend/src/components/settings/settings-sidebar-shell.tsx"
      provides: "sidebar-of-categories layout shell with RBAC gating"
      exports: ["SettingsSidebarShell"]
    - path: "frontend/src/components/settings/save-bar.tsx"
      provides: "per-category sticky dirty-state save bar"
      exports: ["SaveBar"]
    - path: "frontend/src/components/settings/use-dirty-state.ts"
      provides: "dirty-tracking hook for panes"
      exports: ["useDirtyState"]
  key_links:
    - from: "frontend/src/components/settings/settings-sidebar-shell.tsx"
      to: "useAuth().user.role"
      via: "visibleCategories role filter"
      pattern: "role|isAdmin|visibleCategories"
    - from: "frontend/src/components/settings/save-bar.tsx"
      to: "useDirtyState"
      via: "isDirty controls visibility"
      pattern: "isDirty"
---

<objective>
Wave 0 — settings shell + dirty-state foundation, plus the CSPM finding-drill content slot.

Purpose: D-SEQ-01 foundation half two. The settings screen plan (14-05) consumes the sidebar
shell + save bar + dirty-state hook verbatim; the CSPM plan (14-03) consumes the finding-drill
content slot. Extracting these as Wave 0 primitives lets all four screen plans run in parallel.
Output: SettingsSidebarShell (RBAC-gated, no tabs), SaveBar (sticky, dirty-driven), useDirtyState
hook, settings microcopy.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-remaining-screens/14-CONTEXT.md
@.planning/phases/14-remaining-screens/14-RESEARCH.md
@.claude/skills/sketch-findings-getvul/references/app-shell.md
@.claude/skills/sketch-findings-getvul/references/page-layouts.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md

<interfaces>
<!-- Extracted from codebase. -->

useAuth role check (frontend/src/lib/auth.tsx):
```typescript
const { user } = useAuth();
const isOwner = user?.role === 'OWNER';
const isAdmin = user?.role === 'OWNER' || user?.role === 'ADMIN';
```

Inline-edit dirty/cancel pattern to mirror (frontend/src/components/assets/reassign-combobox.tsx):
  Esc → cancel; click-outside → cancel; document mousedown listener for outside detection.

Settings category set (D-SET-01): 'profile' | 'workspace' | 'saml' | 'notifications' | 'api-tokens' | 'audit'.
RBAC gating (D-SET-05): profile + api-tokens always visible; workspace/saml/notifications/audit require isAdmin.
URL contract (A3): ?category=<name>.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SettingsSidebarShell (RBAC-gated, tab-free) + microcopy</name>
  <files>frontend/src/components/settings/settings-sidebar-shell.tsx, frontend/src/components/settings/settings-sidebar-shell.test.tsx, frontend/src/components/settings/microcopy.ts</files>
  <read_first>
    - frontend/src/lib/auth.tsx (useAuth + User.role + the isOwner/isAdmin idiom)
    - .claude/skills/sketch-findings-getvul/references/app-shell.md (the persistent sidebar visual the settings sidebar echoes — gradient-strip active indicator, NOT a border-b tab)
    - .claude/skills/sketch-findings-getvul/references/copy-voice.md (sentence-case category labels)
    - .planning/phases/14-remaining-screens/14-RESEARCH.md (D-SET-05 sidebar gating pattern + Pitfall 1 grep gate + RBAC mechanics)
  </read_first>
  <behavior>
    - Test 1: With user.role='VIEWER', the sidebar renders exactly 'Profile' and 'API tokens' (no Workspace/SAML/Notifications/Audit).
    - Test 2: With user.role='ADMIN', all 6 categories render.
    - Test 3: Clicking a category calls onCategoryChange with that category key.
    - Test 4: The active category item uses a left gradient-strip indicator (data-active="true"), NOT a bottom border.
  </behavior>
  <action>
    Create frontend/src/components/settings/settings-sidebar-shell.tsx:
      type Category = 'profile' | 'workspace' | 'saml' | 'notifications' | 'api-tokens' | 'audit';
      Props: { children: React.ReactNode; activeCategory: Category; onCategoryChange: (c: Category) => void; }
      - Internally read role via useAuth(): const isAdmin = user?.role === 'OWNER' || user?.role === 'ADMIN'.
      - Build visibleCategories = ['profile', ...(isAdmin ? ['workspace','saml','notifications','audit'] : []), 'api-tokens'].
      - Layout: `flex` with a 220px sticky left `<nav>` (`w-[220px] shrink-0 border-r border-border-subtle`) and a `flex-1 overflow-y-auto` right pane wrapping {children}.
      - Each nav item is a `<button>` (NOT an `<a>` with border-b). Active state uses a LEFT gradient strip: `<span className="absolute left-0 top-1 bottom-1 w-0.5 rounded-full" style={{ background: 'var(--gradient-brand)' }} />` rendered only when active; item text active → `text-text`, inactive → `text-text-muted hover:text-text`. Item container `relative`. Add `data-category={cat}` and `data-active={cat === activeCategory}`.
      - Category labels from microcopy.ts (sentence case): Profile / Workspace / SAML & OIDC / Notifications / API tokens / Audit log.
      - DO NOT use any `border-b`, `border-b-2`, `role="tab"`, or the word "tab" in className/markup (Pitfall 1).
      - Below 900px (D-SET-10): the shell stacks the nav full-width above the pane (`max-[900px]:flex-col max-[900px]:w-full`); full master-detail drill is the screen plan's job — the shell just must not horizontal-scroll. Add a comment noting the screen plan wires the drill.

    Create frontend/src/components/settings/microcopy.ts exporting CATEGORY_LABELS: Record<Category, string> + the unsaved-changes guard copy: UNSAVED_GUARD = "You have unsaved changes. Discard them and switch?" (copy-voice: no "Are you sure?", names the action).

    Write the test covering all 4 behaviors (mock useAuth).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/settings/settings-sidebar-shell.test.tsx && grep -rcE "border-b-2|border-b border-indigo|role=\"tab\"" src/components/settings/settings-sidebar-shell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "border-b-2|border-b border-indigo|role=\"tab\"" frontend/src/components/settings/settings-sidebar-shell.tsx` returns 0 matches
    - `grep -c "isAdmin\|visibleCategories" frontend/src/components/settings/settings-sidebar-shell.tsx` returns ≥ 1
    - `grep -c "gray-[0-9]\|indigo-[0-9]" frontend/src/components/settings/settings-sidebar-shell.tsx` returns 0
    - `cd frontend && npx vitest run src/components/settings/settings-sidebar-shell.test.tsx` passes (VIEWER=2 items, ADMIN=6 items)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Shell renders RBAC-gated category sidebar with a gradient-strip active indicator (no tabs); pane swaps via onCategoryChange; VIEWER sees 2 categories, ADMIN sees 6.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SaveBar + useDirtyState hook</name>
  <files>frontend/src/components/settings/save-bar.tsx, frontend/src/components/settings/save-bar.test.tsx, frontend/src/components/settings/use-dirty-state.ts</files>
  <read_first>
    - frontend/src/components/assets/reassign-combobox.tsx (the Phase 12 inline-edit dirty/cancel pattern to mirror)
    - frontend/src/components/tickets/ticket-bulk-bar.tsx (bottom-anchored `fixed inset-x-0 bottom-0` + `animate-in slide-in-from-bottom-2` pattern to reuse)
    - .planning/phases/14-remaining-screens/14-RESEARCH.md (Pattern 3: Per-Category Save Bar + D-SET-04)
    - .claude/skills/sketch-findings-getvul/references/copy-voice.md (button verbs: "Save changes" / "Discard")
  </read_first>
  <behavior>
    - Test 1: SaveBar with isDirty=false renders nothing.
    - Test 2: SaveBar with isDirty=true renders "Save changes" + "Discard" buttons.
    - Test 3: Clicking Save calls onSave; clicking Discard calls onDiscard.
    - Test 4: SaveBar with isSaving=true disables the Save button and shows "Saving…".
    - Test 5: useDirtyState({a:1}) returns isDirty=false; after setField('a',2) returns isDirty=true; reset() returns isDirty=false.
  </behavior>
  <action>
    Create frontend/src/components/settings/use-dirty-state.ts:
      export function useDirtyState<T extends Record<string, unknown>>(initial: T) — returns
      { values: T; setField: (key: keyof T, val: unknown) => void; isDirty: boolean; reset: (next?: T) => void }.
      - isDirty is computed by shallow-comparing current values against a baseline ref (JSON.stringify equality is acceptable for the flat settings payloads).
      - reset(next) sets both baseline and values to next (used after a successful PATCH to clear dirty).

    Create frontend/src/components/settings/save-bar.tsx:
      Props: { isDirty: boolean; isSaving: boolean; onSave: () => void; onDiscard: () => void; }
      - Returns null when !isDirty.
      - Sticky bottom bar: `fixed inset-x-0 bottom-0 z-30 border-t border-border-subtle bg-surface-2/95 backdrop-blur px-6 py-3 animate-in slide-in-from-bottom-2`.
      - Left: a `text-sm text-text-muted` note "Unsaved changes".
      - Right: a "Discard" ghost button (`text-text-muted hover:text-text`) + a "Save changes" gradient CTA button. When isSaving: CTA disabled + label "Saving…".
      - Buttons use verbs per copy-voice ("Save changes", "Discard" — never "Submit"/"OK").
      - Add `data-save-bar` on the container for the test/grep hook.

    Write save-bar.test.tsx covering behaviors 1-4 and a use-dirty-state.test path for behavior 5 (or fold into the same file).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/settings/save-bar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isDirty" frontend/src/components/settings/save-bar.tsx` returns ≥ 1 and SaveBar returns null when !isDirty (covered by Test 1)
    - `grep -E "Save changes|Discard" frontend/src/components/settings/save-bar.tsx` returns matches
    - `grep -c "isDirty\|reset" frontend/src/components/settings/use-dirty-state.ts` returns ≥ 1
    - `grep -c "gray-[0-9]\|indigo-[0-9]" frontend/src/components/settings/save-bar.tsx` returns 0
    - `cd frontend && npx vitest run src/components/settings/save-bar.test.tsx` passes
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SaveBar is invisible until dirty, then shows Save changes/Discard with a Saving… disabled state; useDirtyState tracks dirty + reset clears it; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client role → visible settings categories | useAuth().role drives which category panes are reachable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Elevation of privilege | SettingsSidebarShell RBAC gating | mitigate (UX layer) + backend-enforced | Sidebar hides Admin-only categories for non-admins, but this is UX only — the backend `require_admin`/`require_owner` guards on GET/PATCH /tenant/settings remain the authority. A VIEWER who hand-crafts ?category=workspace still gets 403 from the API; the pane shows the PartialFailureBanner. Document this in the shell comment. |
| T-14-05 | Tampering | useDirtyState baseline | accept | Pure client state; no security impact — server validates the PATCH body via Pydantic regardless of client dirty tracking |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0
- `cd frontend && npx vitest run src/components/settings/` green
- `grep -rE "border-b-2|role=\"tab\"" frontend/src/components/settings/settings-sidebar-shell.tsx` returns 0
</verification>

<success_criteria>
- SettingsSidebarShell: 220px sidebar, RBAC-gated categories, gradient-strip active indicator, zero tab patterns
- SaveBar + useDirtyState: dirty-driven sticky save bar with Save changes/Discard + Saving state
</success_criteria>

<output>
After completion, create `.planning/phases/14-remaining-screens/14-01-SUMMARY.md`
</output>
