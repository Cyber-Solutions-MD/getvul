---
phase: 07-health-and-observability
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_health_observability.py
  - backend/app/logging.py
autonomous: true
requirements: [PROD-07-01, PROD-07-02, PROD-07-04]
must_haves:
  truths:
    - "The Nyquist test file exists and imports cleanly (collection succeeds)"
    - "Every D-21 test case has a named RED test function that fails for the right reason (not ImportError)"
    - "backend/app/logging.py is importable so main.py's future import resolves"
  artifacts:
    - path: "backend/tests/test_health_observability.py"
      provides: "RED test scaffold covering all D-21 cases + D-13/D-14/D-17"
      contains: "def test_health_always_200"
      min_lines: 120
    - path: "backend/app/logging.py"
      provides: "configure_logging() + redact_sensitive_keys() stub module (importable)"
      contains: "def configure_logging"
  key_links:
    - from: "backend/tests/test_health_observability.py"
      to: "backend/app/logging.py"
      via: "import configure_logging, redact_sensitive_keys"
      pattern: "from app.logging import"
    - from: "backend/tests/test_health_observability.py"
      to: "conftest.single_app fixture"
      via: "fixture reuse for /ready integration tests"
      pattern: "single_app"
---

<objective>
Create the Wave 0 Nyquist scaffold for Phase 7: a RED test file covering the full D-21 test matrix plus the security tests (D-13/D-14 request-id, D-17 redaction), and a minimal importable `backend/app/logging.py` stub so downstream plans (07-01 wiring, 07-02 structlog config) have concrete RED targets to turn GREEN.

Purpose: Every downstream task's `<verify>` runs against a real named test. Without this scaffold, verification is untestable. This plan intentionally produces FAILING tests — that is the correct RED state.
Output: `backend/tests/test_health_observability.py` (RED), `backend/app/logging.py` (stub that imports cleanly).
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-health-and-observability/07-CONTEXT.md
@.planning/phases/07-health-and-observability/07-RESEARCH.md
@.planning/phases/07-health-and-observability/07-VALIDATION.md

<interfaces>
<!-- Contracts these RED tests assert against. Downstream plans implement them. -->

D-05 /ready 200 body shape (top-level, NOT nested under "detail"):
  {"status":"ready","checks":{"postgres":{"ok":true,"latency_ms":4},"redis":{"ok":true,"latency_ms":1}}}
D-05 /ready 503 body shape:
  {"status":"not_ready","checks":{"postgres":{"ok":true,"latency_ms":5},"redis":{"ok":false,"error":"timeout"}}}
D-02 /health body (verbatim): {"status": "ok", "service": "getvul-api"}

logging.py public surface (implemented in 07-02):
  def configure_logging() -> None
  def redact_sensitive_keys(logger, method, event_dict) -> dict
  SENSITIVE_KEYS: frozenset = {"authorization","cookie","password","token","secret","credentials","api_key"}

Existing test fixtures (backend/tests/conftest.py):
  single_app -> yields (client, app); lifespan HAS run so app.state.redis is set
  Mock-after-lifespan pattern (from test_rate_limit.py):
    client, app = single_app
    def boom_ping(*a, **k): raise RedisConnectionError("simulated outage")
    monkeypatch.setattr(app.state.redis, "ping", boom_ping)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create importable logging.py stub</name>
  <read_first>
    - backend/app/config.py (settings.environment / settings.debug pattern)
    - .planning/phases/07-health-and-observability/07-RESEARCH.md §"Pattern 1" (target shape)
  </read_first>
  <files>backend/app/logging.py</files>
  <action>
Create `backend/app/logging.py` as a MINIMAL importable stub — NOT the full implementation (that is 07-02). The stub must let `from app.logging import configure_logging, redact_sensitive_keys, SENSITIVE_KEYS` succeed so the test file collects and so a future `main.py` import resolves. Write exactly:

- Module docstring noting this is the operational-logging config module (distinct from the audit CEF/syslog pipeline in app/audit.py — D-18, keep independent).
- `SENSITIVE_KEYS = frozenset({"authorization", "cookie", "password", "token", "secret", "credentials", "api_key"})` (verbatim key list from D-17).
- `def redact_sensitive_keys(logger, method, event_dict):` — stub body `raise NotImplementedError("implemented in 07-02")`. Keep the 3-arg structlog processor signature. (This is a pure function called DIRECTLY in `test_redact_sensitive_keys`, so `NotImplementedError` is the correct RED signal.)
- `def configure_logging() -> None:` — stub body is a **no-op `return`** (NOT `raise NotImplementedError`). Rationale: 07-01 wires `configure_logging()` as the first statement in `lifespan()`; if the stub raised, every `single_app`-based test (including 07-01's own verify) would crash at app startup before any assertion runs, and there is no `depends_on` forcing 07-02 to complete first. A no-op still leaves the logging tests RED for the right reason — `test_logging_json_in_production` / `test_logging_console_in_dev` assert a `ProcessorFormatter`/`JSONRenderer` is configured (or that output is JSON), which a no-op does not do, so those assertions still fail until 07-02 lands the real implementation.

Do NOT import structlog or orjson in the stub (avoid side effects); the real imports land in 07-02. The stub only needs to be import-clean and expose the three names.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.logging import configure_logging, redact_sensitive_keys, SENSITIVE_KEYS; assert SENSITIVE_KEYS == frozenset({'authorization','cookie','password','token','secret','credentials','api_key'})"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/logging.py` exists
    - grep: `backend/app/logging.py` contains `def configure_logging`
    - grep: `backend/app/logging.py` contains `def redact_sensitive_keys`
    - grep: `backend/app/logging.py` contains `SENSITIVE_KEYS = frozenset`
    - The python import one-liner above exits 0
  </acceptance_criteria>
  <done>logging.py imports cleanly and exposes configure_logging, redact_sensitive_keys, SENSITIVE_KEYS with the exact D-17 key set.</done>
</task>

<task type="auto">
  <name>Task 2: Write the RED D-21 test matrix + security tests</name>
  <read_first>
    - backend/tests/conftest.py (single_app fixture, redis_test_url, mock-after-lifespan pattern)
    - backend/tests/test_rate_limit.py (established monkeypatch.setattr(app.state.redis, ...) pattern)
    - .planning/phases/07-health-and-observability/07-VALIDATION.md §"Per-Task Verification Map" (exact test function names)
    - .planning/phases/07-health-and-observability/07-RESEARCH.md §"Validation Architecture" + §"Mock Strategy"
  </read_first>
  <files>backend/tests/test_health_observability.py</files>
  <action>
Create `backend/tests/test_health_observability.py` with the EXACT test function names from the VALIDATION.md map so downstream `<verify>` commands match. Each test asserts real behavior (RED now, GREEN after 07-01/07-02). Use `asyncio_mode = "auto"` (already configured) — write `async def` tests without `@pytest.mark.asyncio`. Reuse the `single_app` fixture (yields `(client, app)`, lifespan run). Follow the test_rate_limit.py mock-after-lifespan pattern.

Write these test functions:

1. `async def test_health_always_200(single_app)` — GET /health returns 200 and body EXACTLY `{"status": "ok", "service": "getvul-api"}` (D-02 verbatim). Uses `client, app = single_app`.

2. `async def test_ready_200_both_up(single_app)` — GET /ready returns 200; `body["status"] == "ready"`; `body["checks"]["postgres"]["ok"] is True` and `"latency_ms" in body["checks"]["postgres"]`; same for redis. Assert the body is TOP-LEVEL shape (NOT nested under "detail") — assert `"detail" not in body`. (Requires a reachable Postgres; if the DB is unreachable in the sandbox, `pytest.skip("Postgres not reachable")` inside the test — mirror conftest `_db_reachable` guard.)

3. `async def test_ready_503_postgres_down(single_app, monkeypatch)` — monkeypatch `app.db.session.async_session_factory` to a callable whose context-manager `execute` raises `ConnectionRefusedError` (or use an AsyncMock session whose `.execute` raises). GET /ready returns 503; `body["status"] == "not_ready"`; `body["checks"]["postgres"]["ok"] is False`. Assert `"detail" not in body`.

4. `async def test_ready_503_redis_down(single_app, monkeypatch)` — `client, app = single_app`; define `async def boom_ping(*a, **k): raise Exception("simulated outage")` and `monkeypatch.setattr(app.state.redis, "ping", boom_ping)`. GET /ready returns 503; `body["checks"]["redis"]["ok"] is False`.

5. `async def test_ready_503_timeout_path(single_app, monkeypatch)` — replace `app.state.redis.ping` with `async def slow_ping(*a, **k): await asyncio.sleep(10)`. Because the probe wraps each check in `asyncio.wait_for(coro, timeout=0.5)`, GET /ready returns 503 and `body["checks"]["redis"] == {"ok": False, "error": "timeout"}` (assert `error == "timeout"`). Test itself must complete in ~<1s (the 0.5s bound), proving the timeout fired rather than the 10s sleep.

6. `async def test_logging_json_in_production(monkeypatch)` — monkeypatch `app.config.settings.environment` to `"production"` and `settings.debug` to `False`, then call `configure_logging()`. Assert the root logger's handler formatter routes through a JSONRenderer — inspect `logging.getLogger().handlers[0].formatter` is a `structlog.stdlib.ProcessorFormatter`, and that its configured renderer is a `structlog.processors.JSONRenderer`. (Exact introspection assertion: the test may check that emitting a log line via `logging.getLogger("x").info("evt")` yields a line that `json.loads()` parses to a dict containing key `"event"`.)

7. `async def test_logging_console_in_dev(monkeypatch)` — monkeypatch `settings.environment` to `"development"` and `settings.debug` to `True`, call `configure_logging()`. Assert the dev path selects `structlog.dev.ConsoleRenderer` (a log line is NOT valid JSON — `json.loads()` raises, OR introspect the renderer type). Reset structlog state between tests: call `structlog.reset_defaults()` in a fixture or at test top so tests 6/7 do not leak config into each other.

8. `async def test_request_id_middleware(single_app)` — three assertions (D-13/D-14): (a) GET /health with NO inbound X-Request-ID → response has an `X-Request-ID` header matching regex `^[A-Za-z0-9._-]{1,128}$` (a UUID4 matches). (b) GET /health with a VALID inbound `X-Request-ID: abc-123.ok` → response echoes the SAME value. (c) GET /health with an INVALID inbound value (e.g. 200-char string OR one containing a space/`;`) → response `X-Request-ID` differs from the inbound value and matches the valid charset regex (proves the invalid value was rejected and a UUID4 minted).

9. `def test_redact_sensitive_keys()` — direct call to the processor (D-17). Build `event_dict = {"authorization": "Bearer xyz", "password": "hunter2", "user": "alice", "api_key": "sk-123"}`, call `redact_sensitive_keys(None, "info", event_dict)`, assert result `["authorization"] == "[REDACTED]"`, `["password"] == "[REDACTED]"`, `["api_key"] == "[REDACTED]"`, and non-sensitive `["user"] == "alice"` (untouched). Also assert calling with an empty dict does not raise.

Add `import asyncio`, `import json`, `import logging`, `import re`, `import structlog`, `import pytest` at top. Reference `from app.logging import configure_logging, redact_sensitive_keys, SENSITIVE_KEYS`.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY="3iPwTM32LuijVi-uwCGt448cCCPHGVvUoriwi0A3FkY=" JWT_SECRET_KEY="ci-test-secret-not-for-prod" python -m pytest tests/test_health_observability.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_health_observability.py` exists
    - `--collect-only` lists all 9 test functions (no collection ImportError)
    - grep: file contains `def test_health_always_200`
    - grep: file contains `def test_ready_503_timeout_path`
    - grep: file contains `def test_logging_json_in_production`
    - grep: file contains `def test_request_id_middleware`
    - grep: file contains `def test_redact_sensitive_keys`
    - grep: file contains `from app.logging import`
    - Running the suite RED is EXPECTED: `python -m pytest tests/test_health_observability.py -x` fails on assertion / NotImplementedError, NOT on collection/ImportError
  </acceptance_criteria>
  <done>All 9 D-21/security tests collect cleanly and fail for the right reason (missing /ready route, stub configure_logging, no request-id middleware) — a valid RED baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app under test | Tests assert the security invariants that 07-01/07-02 must satisfy; the scaffold itself introduces no runtime surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-00-01 | Tampering | X-Request-ID handling | mitigate | `test_request_id_middleware` asserts oversized/invalid inbound header is replaced with a UUID4 (charset `[A-Za-z0-9._-]{1,128}` enforced) — locks in D-14 defense |
| T-07-00-02 | Information Disclosure | log key-material leakage | mitigate | `test_redact_sensitive_keys` asserts the D-17 key set scrubs to `[REDACTED]` before render |
| T-07-00-03 | Information Disclosure | /ready body | accept→verify | `test_ready_503_*` assert body carries only `status`/`ok`/`latency_ms`/`error` and `"detail" not in body` — no connection strings / stack traces |
</threat_model>

<verification>
- `cd backend && ENCRYPTION_KEY=... JWT_SECRET_KEY=... python -m pytest tests/test_health_observability.py --collect-only -q` lists 9 tests, zero collection errors.
- `python -c "from app.logging import configure_logging, redact_sensitive_keys, SENSITIVE_KEYS"` exits 0.
- Running the suite fails on assertions/NotImplementedError (correct RED), not ImportError.
</verification>

<success_criteria>
- 9 named test functions matching VALIDATION.md exist and collect.
- logging.py stub is import-clean and exposes the three public names with the exact D-17 key set.
- The suite is RED for the right reasons — downstream plans have concrete GREEN targets.
</success_criteria>

<output>
After completion, create `.planning/phases/07-health-and-observability/07-00-SUMMARY.md`
</output>
