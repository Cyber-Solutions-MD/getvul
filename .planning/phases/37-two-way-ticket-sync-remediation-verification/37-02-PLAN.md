---
phase: 37-two-way-ticket-sync-remediation-verification
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - backend/app/vulnerabilities/service.py
  - backend/app/connectors/sync.py
  - backend/tests/test_finding_reopen.py
autonomous: true
requirements: [SYNC-03]
must_haves:
  truths:
    - "A scan re-detecting a REMEDIATED finding reopens the SAME row (no duplicate finding created)"
    - "Reopen preserves first_detected_at so MTTR/RemediationEvent lineage is intact"
    - "Reopen resets clean_scan_streak to 0 and clears remediated_at"
    - "The linked Ticket row (Ticket.vulnerability_id FK) still points at the reopened finding — no new Ticket row"
    - "Every reopen writes a tenant-scoped AuditLog with user_email='system:rescan-reopen'"
  artifacts:
    - path: "backend/app/vulnerabilities/service.py"
      provides: "reopen_vulnerability soft-close resurrection helper + audit"
      contains: "def reopen_vulnerability"
    - path: "backend/app/connectors/sync.py"
      provides: "_upsert_vulnerability existing-row reopen branch"
      contains: "existing.status == \"REMEDIATED\""
  key_links:
    - from: "backend/app/connectors/sync.py _upsert_vulnerability existing branch"
      to: "reopen_vulnerability"
      via: "recurrence-after-auto-close resurrection"
      pattern: "reopen_vulnerability\\("
    - from: "reopen_vulnerability"
      to: "AuditLog(user_email='system:rescan-reopen')"
      via: "direct AuditLog construction"
      pattern: "system:rescan-reopen"
---

<objective>
Deliver SYNC-03 (reopen-on-recurrence, D-04): when a later scan re-detects a finding that was
auto-closed (soft REMEDIATED), resurrect the ORIGINAL row rather than silently creating a
duplicate. The dedup identity key `uq_vuln_dedup(tenant_id, cve_id, asset_id, source)` already
routes a re-detected finding to the same row in `_upsert_vulnerability`'s existing branch — this
plan makes that branch reopen a REMEDIATED row, preserving MTTR lineage and the existing ticket link.

Purpose: guarantees "a recurrence reopens rather than duplicates" — the invariant that keeps
finding identity and MTTR history stable across the close→recur→reopen lifecycle.
Output: `reopen_vulnerability` helper + the `_upsert_vulnerability` reopen branch + `tests/test_finding_reopen.py`.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/37-two-way-ticket-sync-remediation-verification/37-CONTEXT.md
@.planning/phases/37-two-way-ticket-sync-remediation-verification/37-RESEARCH.md
@.planning/phases/37-two-way-ticket-sync-remediation-verification/37-PATTERNS.md
@.planning/phases/37-two-way-ticket-sync-remediation-verification/37-01-PLAN.md

<interfaces>
<!-- Depends on Plan 01: Vulnerability.clean_scan_streak column now exists. -->

From backend/app/connectors/sync.py::_upsert_vulnerability (line 360-393) — the existing-row
branch to EXTEND. It already locates the row by the dedup identity key and refreshes
`last_seen_at`; it does NOT touch `status`:
```python
result = await db.execute(select(Vulnerability).where(
    Vulnerability.tenant_id == tenant_id, Vulnerability.cve_id == v.cve_id,
    Vulnerability.asset_id == asset_id, Vulnerability.source == source))
existing = result.scalar_one_or_none()
if existing:
    existing.last_seen_at = now
    # ... field refresh (severity, epss, etc.) ...
    return False           # <-- add reopen logic BEFORE this return
```

From backend/app/vulnerabilities/models.py — reopen target fields:
`status` (String, set to "OPEN"), `remediated_at` (set to None), `clean_scan_streak` (reset 0),
`first_detected_at` (PRESERVE — never touch). RemediationEvent rows are historical and must
NOT be deleted (MTTR lineage). `Ticket.vulnerability_id` (ticketing/models.py:84) is a SINGULAR
FK already pointing at this row — relink is automatic, do not create a new Ticket.

From backend/app/audit.py + sla_tier_service.py:332 — system-actor audit precedent (see Plan 01).
Use `user_email="system:rescan-reopen"`, `action="vuln.reopen_recurrence"`.
</interfaces>
</context>

<resolved_open_questions>
- **Q3 (findings already prematurely-closed by the current ticket-done path): leave as-is.**
  Rationale: they are `REMEDIATED` rows that were never rescan-verified, but this plan's reopen
  branch fires for ANY re-detected REMEDIATED row — so any that recur will be reopened naturally
  by SYNC-03. No proactive backfill task; recorded here as a data note only. (Plan 01 already
  removes the ability to create new premature closures via the rescan-only path; Plan 03 removes
  the remaining ticket-done→REMEDIATED writes.)
</resolved_open_questions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reopen_vulnerability soft-close resurrection helper (SYNC-03)</name>
  <files>backend/app/vulnerabilities/service.py, backend/tests/test_finding_reopen.py</files>
  <behavior>
    - `reopen_vulnerability(db, vuln)` on a REMEDIATED row sets status="OPEN", remediated_at=None,
      clean_scan_streak=0, PRESERVES first_detected_at, and writes an AuditLog
      (user_email="system:rescan-reopen", action="vuln.reopen_recurrence", real tenant_id).
    - Any existing RemediationEvent rows for that vuln remain in the DB (not deleted) — MTTR lineage intact.
    - Called on a non-REMEDIATED row it is a no-op returning False (idempotent guard).
  </behavior>
  <action>
    Add `async def reopen_vulnerability(db: AsyncSession, vuln: Vulnerability) -> bool` next to
    `mark_vulnerability_remediated` in `service.py`. Guard: if `vuln.status != "REMEDIATED"` return
    False. Otherwise set `vuln.status = "OPEN"`, `vuln.remediated_at = None`,
    `vuln.clean_scan_streak = 0` (leave `first_detected_at` untouched), `db.add(AuditLog(...))`
    (import from `app.audit`; `user_email="system:rescan-reopen"`,
    `action="vuln.reopen_recurrence"`, `resource_type="vulnerability"`,
    `resource_id=str(vuln.id)`, `details={"source": vuln.source}`, `ip_address=None`,
    `created_at=datetime.now(UTC)`), return True. No flush/commit (caller owns the txn boundary).
    Write `tests/test_finding_reopen.py` FIRST (RED) with the behaviors above using the
    `db_session` fixture + `_seed_vuln(status="REMEDIATED")` pattern; assert the prior
    RemediationEvent row survives reopen. Then implement to GREEN.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret uv run pytest tests/test_finding_reopen.py -x -k "reopen_helper or preserves or noop"</automated>
  </verify>
  <done>reopen_vulnerability resurrects a REMEDIATED row (OPEN, streak 0, remediated_at cleared, first_detected_at preserved), audits it, no-ops on non-REMEDIATED; targeted tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: wire reopen into _upsert_vulnerability recurrence branch (SYNC-03)</name>
  <files>backend/app/connectors/sync.py, backend/tests/test_finding_reopen.py</files>
  <behavior>
    - A scan re-detecting a finding whose existing row is REMEDIATED reopens that SAME row via
      reopen_vulnerability — no second Vulnerability row is inserted (row count unchanged).
    - `first_detected_at` is unchanged after reopen; a subsequent 2-clean-scan cycle can re-close it,
      writing a SECOND RemediationEvent (RemediationEvent has no unique constraint on vuln).
    - The pre-existing Ticket row (vulnerability_id FK) still resolves to the reopened finding.
  </behavior>
  <action>
    In `_upsert_vulnerability`'s `if existing:` branch (sync.py line 375), AFTER the field-refresh
    block and BEFORE `return False`, add: `if existing.status == "REMEDIATED":
    await reopen_vulnerability(db, existing)` (import `reopen_vulnerability` from
    `app.vulnerabilities.service` locally, matching the existing local-import convention in
    daily_sync). `existing.last_seen_at = now` is already set at the top of the branch — leave it.
    Do not add dedup logic (the identity-key select already routes to the same row). Do not touch
    the `else:` (new-row) branch.
    Extend `tests/test_finding_reopen.py` (RED first) to drive a real re-detection through
    `_upsert_vulnerability`: seed a REMEDIATED finding + a linked Ticket, run an upsert of the same
    (tenant, cve, asset, source), assert the row reopened, the finding COUNT for that key is still 1,
    first_detected_at preserved, and the Ticket still points at it. Then implement to GREEN.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret uv run pytest tests/test_finding_reopen.py -x</automated>
  </verify>
  <done>Re-detecting an auto-closed finding reopens the same row (no duplicate finding, no duplicate ticket), preserves MTTR lineage, and audits the reopen; all tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner connector → _upsert_vulnerability | Re-detected finding payload maps to an existing row via the dedup identity key |
| scheduler → AuditLog | System-actor reopen audit must carry the real tenant_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05 | Tampering | reopen branch | mitigate | Reopen resolves via `uq_vuln_dedup` — a recurrence can only land on the caller's own (tenant, cve, asset, source) row; no cross-tenant resurrection is expressible |
| T-37-06 | Information Disclosure | reopen AuditLog | mitigate | Direct AuditLog with `tenant_id=vuln.tenant_id` (never nil UUID) |
| T-37-07 | Repudiation | recurrence decision | accept | Reopen writes an audit row; the historical RemediationEvent is retained so the full close→reopen timeline is reconstructable — no further control needed |
</threat_model>

<verification>
- `cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret uv run pytest tests/test_finding_reopen.py -x` green.
- Regression: `... uv run pytest tests/test_github_sync.py -x` green (upsert path unbroken).
</verification>

<success_criteria>
- A recurrence of an auto-closed finding reopens the SAME finding row and never creates a duplicate finding or ticket (SYNC-03, D-04).
- MTTR lineage (first_detected_at + RemediationEvent history) is preserved across reopen.
- Every reopen is audited as a tenant-scoped system-actor event.
</success_criteria>

<output>
After completion, create `.planning/phases/37-two-way-ticket-sync-remediation-verification/37-02-SUMMARY.md`
</output>
