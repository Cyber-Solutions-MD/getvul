---
phase: 37-two-way-ticket-sync-remediation-verification
plan: 04
type: execute
wave: 1
gap_closure: true
depends_on: ["37-03"]
files_modified:
  - backend/app/ticketing/service.py
  - backend/app/ticketing/daily_sync.py
  - backend/tests/test_mttr.py
autonomous: true
requirements: [SYNC-01]
must_haves:
  truths:
    - "No `mark_vulnerability_remediated` call remains anywhere under `backend/app/ticketing/` (grep gate = 0); a done/closed ticket via ANY ticketing entry point (scheduled sync `daily_sync.py`, manual `sync_ticket_status`, manual `close_ticket`) drives the linked finding to IN_PROGRESS + an awaiting-rescan comment + a `system:ticket-sync` AuditLog, NEVER to REMEDIATED"
    - "`sync_ticket_status()`'s ticket-done arm sets the finding OPEN/IN_PROGRESS → IN_PROGRESS (via the `map_ticket_status` intent taxonomy, not `_is_ticket_completed`), posts the awaiting-rescan comment, writes the audit — and NEVER sets ticket.resolved_at on that arm (the finding is not resolved until rescan)"
    - "`close_ticket()` still closes the ticket on its provider and resolves the ticket rows, but drives each linked OPEN/IN_PROGRESS finding to IN_PROGRESS (+ awaiting-rescan comment + audit) instead of REMEDIATED — no RemediationEvent is written from a manual close"
    - "The two `test_mttr.py` regression tests (`test_sync_ticket_status_remediated_routes_through_helper`, `test_close_ticket_remediated_routes_through_helper`) assert the IN_PROGRESS-only outcome and zero RemediationEvent rows — the suite no longer green-lights the old force-close semantics"
    - "sync_ticket_status's D-03-SAFE second pass (auto-close the TICKET when all linked findings are ALREADY REMEDIATED-by-rescan, + progress comments) is preserved unchanged; close_ticket still closes the ticket on its provider unchanged"
    - "The IN_PROGRESS + comment + audit side effect fires exactly ONCE per real transition and is idempotent under repeated invocation: `sync_ticket_status` gates it on `not _was_previously_done(...)` (fresh done-transition only) so re-clicking 'Sync now' never re-comments the live ticket or duplicates the audit row, and its `in_progress` intent branch is a plain side-effect-free status set (no comment/audit, matching daily_sync.py); `close_ticket` gates it on the ticket row not already being resolved. A double-call test proves the fire-count does not grow"
  artifacts:
    - path: "backend/app/ticketing/service.py"
      provides: "D-03-safe sync_ticket_status + close_ticket (no mark_vulnerability_remediated); shared _AWAITING_RESCAN_COMMENT constant now owned here"
      contains: "map_ticket_status"
    - path: "backend/app/ticketing/daily_sync.py"
      provides: "imports the promoted _AWAITING_RESCAN_COMMENT from service (single source, no duplicate copy)"
      contains: "from app.ticketing.service import"
    - path: "backend/tests/test_mttr.py"
      provides: "two rewritten regression tests asserting IN_PROGRESS-only, zero RemediationEvent"
      contains: "IN_PROGRESS"
  key_links:
    - from: "backend/app/ticketing/service.py::sync_ticket_status ticket-done arm"
      to: "finding.status = IN_PROGRESS (+ awaiting-rescan comment + system:ticket-sync AuditLog)"
      via: "map_ticket_status intent 'done_awaiting_rescan' replacing the deleted mark_vulnerability_remediated call"
      pattern: "IN_PROGRESS"
    - from: "backend/app/ticketing/service.py::close_ticket per-ticket loop"
      to: "finding.status = IN_PROGRESS (+ awaiting-rescan comment + audit)"
      via: "removal of the mark_vulnerability_remediated call on manual close"
      pattern: "IN_PROGRESS"
---

<objective>
Close the single BLOCKER gap from `37-VERIFICATION.md`: the D-03 fix landed in Plan 37-03
was scoped to `daily_sync.py` (the scheduled poll pass) only, but `backend/app/ticketing/service.py`
holds a SEPARATE, live, router-wired implementation of inbound ticket-status sync
(`sync_ticket_status`) and manual close (`close_ticket`) that STILL call
`mark_vulnerability_remediated(db, vuln)` directly the instant a ticket reads done — with no
rescan gate. Both are reachable through real authenticated endpoints (`POST /tickets/sync-status`,
`POST /tickets/bulk-action` action=sync-update / action=close). Two `test_mttr.py` tests still
green-light the old behavior.

This plan applies the exact IN_PROGRESS-only pattern already proven in `daily_sync.py` to both
`service.py` functions, per the locked **D-03 addendum** (37-CONTEXT.md): D-03 applies to BOTH —
neither an inbound status sync NOR an analyst's explicit "Close Ticket" click may close a finding;
the scanner re-scanning clean (SYNC-02, `connectors/sync.py`) is the ONLY closure path.

Purpose: makes "closure only ever comes from the scanner, never from a ticket" TRUE everywhere it
is reachable in the codebase — restoring SYNC-01 to full satisfaction.
Output: rewritten `sync_ticket_status` + `close_ticket` (D-03-safe), the `_AWAITING_RESCAN_COMMENT`
constant promoted to a single owner, and two rewritten `test_mttr.py` regression tests.

Scope discipline (gap closure): this is a surgical, single-file-plus-tests closure. Do NOT re-plan
the phase, do NOT touch the already-verified `daily_sync.py` sync logic (only its import line for the
shared constant), `connectors/sync.py`, `github_client.py`, or the migration.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/37-two-way-ticket-sync-remediation-verification/37-CONTEXT.md
@.planning/phases/37-two-way-ticket-sync-remediation-verification/37-VERIFICATION.md
@.planning/phases/37-two-way-ticket-sync-remediation-verification/37-03-PLAN.md
@.planning/phases/37-two-way-ticket-sync-remediation-verification/37-03-SUMMARY.md

<interfaces>
<!-- Everything the executor needs is ALREADY in this codebase from Plan 37-03.
     Reuse it verbatim — do not build a second copy of any of it. -->

ALREADY EXISTS in backend/app/ticketing/service.py (reuse, do not duplicate):
- `map_ticket_status(provider: str, payload: dict) -> str` (line 1127) — D-03-safe mapper.
  Returns one of {"in_progress", "done_awaiting_rescan", "open", "unknown"} — NEVER "remediated".
  This is the REPLACEMENT for `_is_ticket_completed` in the finding-status decision (the intent
  taxonomy matches daily_sync.py). `_is_ticket_completed` (line 1092) has NO remaining callers after
  this change — `grep -rn "_is_ticket_completed" backend/` confirms its ONLY call site is the
  line 1234 block Task 1 replaces — so DELETE it in Task 1 rather than leaving dead code.
- `sync_ticket_status(db, tenant_id, client_resolver)` (line 1176) — the D-03 violation to fix.
  Its ticket-done arm is lines 1234-1248; the `mark_vulnerability_remediated` call is line 1246.
  Its SECOND pass (lines 1250-1347: auto-close the TICKET when all linked vulns are ALREADY
  REMEDIATED-by-rescan + progress comments) is D-03-SAFE and MUST be preserved untouched.
- `close_ticket(db, tenant_id, external_ticket_url, client_resolver)` (line 1352) — the second
  D-03 violation. `client.close(ref)` (line 1383) + the ticket-row resolution loop stay; the
  `mark_vulnerability_remediated` call (line 1399) is what changes.
- The generic dispatch client (`ClientResolver = Callable[[str], Awaitable[TicketingClient|None]]`)
  exposes verbs `create` / `get(ref)` / `comment(ref, body)` / `close(ref)` ONLY (dispatch.py).
  There is NO generic `reopen` verb — see the recurrence note below.

ALREADY EXISTS in backend/app/ticketing/daily_sync.py (the PROVEN reference to mirror):
- `_AWAITING_RESCAN_COMMENT` (line 43), `_DONE_EXTERNAL_STATUSES` (line 37), and
  `_was_previously_done(provider, external_status)` (line 57) — the awaiting-rescan comment string,
  the per-provider done-status map, and the fresh-transition guard predicate. All THREE must become
  SHARED between the two files (see Task 1: promote them to service.py; daily_sync imports them back).
  daily_sync already does `from app.ticketing.service import map_ticket_status` (line 18), so
  service→daily_sync would be circular — service.py MUST be the owner. `_was_previously_done` is what
  gives `sync_ticket_status` its idempotent, safe-to-re-poll behavior — it is NOT unused here.
- The proven done-transition block (`_sync_asana_tickets` lines 359-385): capture
  `was_done_before = _was_previously_done(provider, ticket.external_status)` BEFORE overwriting
  `ticket.external_status`, then on intent "done_awaiting_rescan" with a linked vuln in
  ("OPEN","IN_PROGRESS") AND `not was_done_before` (fresh transition only):
    ```python
    was_done_before = _was_previously_done(provider, ticket.external_status)
    # ... after intent == "done_awaiting_rescan":
    if vuln and not was_done_before and vuln.status in ("OPEN", "IN_PROGRESS"):
        vuln.status = "IN_PROGRESS"
        await client.comment(ref, _AWAITING_RESCAN_COMMENT)
        db.add(AuditLog(
            tenant_id=vuln.tenant_id, user_id=None,
            user_email="system:ticket-sync", action="vuln.ticket_status_sync",
            resource_type="vulnerability", resource_id=str(vuln.id),
            details={"provider": provider, "ticket_id": str(ticket.id), "new_status": "IN_PROGRESS"},
            ip_address=None, created_at=datetime.now(UTC),
        ))
    ```
  Mirror THIS exactly (via the generic `client.comment` verb, since service.py uses the dispatch
  client, not the provider-specific clients). The `not was_done_before` clause is mandatory — without
  it, every re-poll re-fires the comment + audit (the BLOCKER this revision fixes).

AuditLog import: service.py does NOT currently import AuditLog. Add `from app.audit import AuditLog`
(daily_sync.py line 14 is the reference). AuditLog fields as constructed above.

test_mttr.py (backend/tests/test_mttr.py):
- `_FakeTicketingClient` (line 206) already has `get` / `comment` (no-op → None) / `close`
  (records `closed_refs`). No fake change needed.
- `_events_for(db, vuln_id)` returns the RemediationEvent rows for a vuln (used to assert none
  are written). `_vuln(...)` / `_ticket(...)` helpers already exist in-file.
- Rewrite `test_sync_ticket_status_remediated_routes_through_helper` (line 227) and
  `test_close_ticket_remediated_routes_through_helper` (line 253) — see Tasks 1 & 2.
</interfaces>

<recurrence_reopen_scope>
daily_sync.py ALSO reopens the SAME external ticket on recurrence (`was_done_before and
vuln.status == "OPEN"` → provider `update_task(completed=False)` / `transition(..., "To Do")` /
`reopen_issue(...)`). This branch is DELIBERATELY OUT OF SCOPE for `sync_ticket_status` /
`close_ticket`, because the generic dispatch `TicketingClient` Protocol exposes only
create/get/comment/close — it has NO reopen verb. Reaching around the Protocol for a
provider-specific reopen here would duplicate provider-dispatch logic that daily_sync.py
(which uses the provider-specific clients) already owns and that VERIFICATION marked VERIFIED
(truth #6). The `missing:` list in 37-VERIFICATION.md requires ONLY the IN_PROGRESS split + the
two test rewrites; recurrence reopen for the router path is intentionally left to the scheduled
poll pass. State this explicitly in the SUMMARY — do not silently drop it, and do not invent a
second reopen path.
</recurrence_reopen_scope>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-03 split in sync_ticket_status — ticket-done drives IN_PROGRESS, never REMEDIATED (SYNC-01)</name>
  <files>backend/app/ticketing/service.py, backend/app/ticketing/daily_sync.py, backend/tests/test_mttr.py</files>
  <behavior>
    - A done/completed/closed ticket read (`map_ticket_status(provider, payload) == "done_awaiting_rescan"`)
      that is a FRESH transition — the ticket's STORED `external_status` was NOT already done, i.e.
      `not _was_previously_done(provider, ticket.external_status)` captured BEFORE this cycle overwrites
      it — drives the linked finding OPEN/IN_PROGRESS → IN_PROGRESS, posts `_AWAITING_RESCAN_COMMENT`
      via `client.comment(ref, ...)`, and writes a `system:ticket-sync` AuditLog, exactly ONCE. A
      steady-state re-poll (ticket already done last cycle, finding still awaiting rescan) is a no-op on
      the finding: NO repeat comment on the live ticket, NO duplicate AuditLog row. This mirrors
      daily_sync.py's `not was_done_before` guard — the entire reason the "Sync now" button is safe to
      click repeatedly. It NEVER calls `mark_vulnerability_remediated` and NEVER sets `ticket.resolved_at`
      (the finding is not resolved until rescan; the ticket stays poll-able).
    - `intent == "in_progress"` → PLAIN, side-effect-free `vuln.status = "IN_PROGRESS"` ONLY — NO comment,
      NO AuditLog (mirrors daily_sync.py's Jira `in_progress` branch, lines 494-497, which has zero side
      effects). "In progress" is the everyday, long-lived steady state for most open tickets; adding a
      comment/audit here would spam nearly every ticket on nearly every sync click. `intent == "open"`
      → `ticket.external_status = "open"`, no finding write. `intent == "unknown"` → no-op + `logger.warning`.
    - The D-03-SAFE SECOND pass (auto-close the TICKET when ALL linked findings are already
      REMEDIATED, + progress comments) is byte-for-byte preserved — it closes tickets based on
      findings ALREADY remediated by rescan; it does not remediate findings.
  </behavior>
  <action>
    1. Promote the shared helpers to a single owner: MOVE `_AWAITING_RESCAN_COMMENT` (daily_sync.py
       line 43), `_DONE_EXTERNAL_STATUSES` (line 37), and `_was_previously_done` (line 57) into
       `service.py` (define them near `map_ticket_status`, ~line 1125). In `daily_sync.py`, DELETE the
       three local definitions and add them to the existing import:
       `from app.ticketing.service import _AWAITING_RESCAN_COMMENT, _DONE_EXTERNAL_STATUSES, _was_previously_done, map_ticket_status`.
       (service.py must be the owner — daily_sync imports service, so the reverse would be circular.)
       Leave daily_sync's `_RECURRENCE_COMMENT`, `_JIRA_OPEN_TRANSITION_TARGET` where they are
       (recurrence-only, still daily_sync-local).
    2. Add `from app.audit import AuditLog` to service.py's imports.
    3. Rewrite ONLY the first-pass done arm of `sync_ticket_status` (lines 1234-1248). Replace the
       `if _is_ticket_completed(provider, payload): ...` block with an intent branch on
       `map_ticket_status(provider, payload)`. FIRST, at the top of the per-ticket loop body (before
       any `ticket.external_status` overwrite), capture the fresh-transition guard:
       `was_done_before = _was_previously_done(provider, ticket.external_status)`. Then:
         - `"done_awaiting_rescan"`: set `ticket.external_status = "completed"`; load the linked vuln
           (`ticket.vulnerability_id`); if `vuln and not was_done_before and vuln.status in ("OPEN", "IN_PROGRESS")`:
           `vuln.status = "IN_PROGRESS"`, `await client.comment(ref, _AWAITING_RESCAN_COMMENT)`,
           `db.add(AuditLog(... user_email="system:ticket-sync", action="vuln.ticket_status_sync",
           resource_type="vulnerability", resource_id=str(vuln.id),
           details={"provider": provider, "ticket_id": str(ticket.id), "new_status": "IN_PROGRESS"} ...))`.
           The `not was_done_before` clause makes a steady-state re-poll a no-op (no repeat comment/audit).
           DELETE the `mark_vulnerability_remediated` import+call. Do NOT set `ticket.resolved_at`;
           do NOT increment `resolved`.
         - `"in_progress"`: PLAIN status set ONLY — `if vuln and vuln.status == "OPEN": vuln.status = "IN_PROGRESS"`.
           NO comment, NO AuditLog (matches daily_sync.py's side-effect-free Jira in_progress branch).
           Set `ticket.external_status` to a non-done value (e.g. "open") so it stays in the second pass.
         - `"open"`: `ticket.external_status = "open"` (existing else behavior), no finding write.
         - `"unknown"`: `logger.warning("ticket_status_unknown", provider=provider, ticket_id=str(ticket.id))`,
           no-op.
       Leave the SECOND pass (lines 1250-1347) and the final `return` shape unchanged.
    4. DELETE `_is_ticket_completed` (service.py line 1092) — after step 3 it has no remaining callers
       (confirm with `grep -rn "_is_ticket_completed" backend/`; the only hit is the line 1234 block just replaced).
    5. Rewrite `test_mttr.py::test_sync_ticket_status_remediated_routes_through_helper` (line 227),
       renaming it to `test_sync_ticket_status_done_drives_in_progress_never_remediated`. Keep the
       Asana `{"completed": True}` fixture; the seeded ticket must start with a NON-done
       `external_status` (e.g. `"open"`/`None`) so the first sync is a fresh transition. After
       `sync_ticket_status(...)` assert `vuln.status == "IN_PROGRESS"` and
       `await _events_for(db_session, vuln.id) == []` (no RemediationEvent). Then ADD an idempotency
       assertion: call `sync_ticket_status(...)` a SECOND time on the now-steady state (ticket already
       "completed", vuln still IN_PROGRESS) and assert the `_FakeTicketingClient`'s comment-call count
       and the vuln's AuditLog row count did NOT grow (the fresh-transition guard holds). If
       `_FakeTicketingClient` does not already count `comment` calls, extend it to record them. Update
       the docstring to describe the D-03-safe, re-poll-idempotent intent.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret uv run pytest tests/test_mttr.py -x -k "sync_ticket_status"</automated>
  </verify>
  <done>sync_ticket_status no longer calls mark_vulnerability_remediated; a done ticket drives IN_PROGRESS + comment + audit and never REMEDIATED; resolved_at not set on the done arm; second pass preserved; the rewritten test asserts IN_PROGRESS + zero events and is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-03 split in close_ticket — manual close drives IN_PROGRESS, never REMEDIATED (SYNC-01)</name>
  <files>backend/app/ticketing/service.py, backend/tests/test_mttr.py</files>
  <behavior>
    - A manual `close_ticket` STILL closes the ticket on its provider (`client.close(ref)`) and
      resolves the local ticket rows (`external_status = "completed"`, `resolved_at = now`) — a
      manual close is a terminal action on the TICKET.
    - On a FRESH close (the ticket row was NOT already resolved before this call), each linked finding
      in ("OPEN","IN_PROGRESS") is driven to IN_PROGRESS (+ a single `_AWAITING_RESCAN_COMMENT` on the
      ref + a `system:ticket-sync` AuditLog per finding) instead of REMEDIATED. No
      `mark_vulnerability_remediated` call; no RemediationEvent from this path.
    - A repeat `close_ticket` on an already-resolved ticket URL is a no-op on findings: no repeat
      comment, no duplicate audit (the `was_already_resolved` guard — the close_ticket analog of
      sync_ticket_status's `not was_done_before`).
  </behavior>
  <action>
    1. In `close_ticket` (service.py:1352), keep the `client.close(ref)` call and the per-ticket-row
       `external_status = "completed"` / `resolved_at = now` writes. In the loop, capture the guard
       BEFORE overwriting: `row_was_resolved = ticket.resolved_at is not None`. Then REPLACE the
       `if vuln and vuln.status not in ("REMEDIATED", "SUPPRESSED"): ...mark_vulnerability_remediated(...)`
       block (lines 1393-1400) with: if `not row_was_resolved and vuln and vuln.status in ("OPEN", "IN_PROGRESS")`:
       set `vuln.status = "IN_PROGRESS"` and `db.add(AuditLog(... user_email="system:ticket-sync",
       action="vuln.ticket_status_sync", resource_type="vulnerability", resource_id=str(vuln.id),
       details={"provider": provider, "ticket_id": str(ticket.id), "new_status": "IN_PROGRESS",
       "trigger": "manual_close"} ...))`; count these into a local `findings_awaiting_rescan` counter.
       DELETE the `mark_vulnerability_remediated` import+call.
    2. After the loop, post ONE `_AWAITING_RESCAN_COMMENT` for the ref via `client.comment(ref, ...)`
       IF `findings_awaiting_rescan > 0` (single comment, not per-row, to avoid spam).
    3. Change the return dict: replace `"vulns_remediated": resolved_vulns` with
       `"findings_awaiting_rescan": findings_awaiting_rescan` (keep `"closed": True`,
       `"tickets_resolved": len(tickets)`). NOTE: `router.py` bulk-action `close` only checks
       `if "error" in result` (confirmed) — this return-shape change breaks no caller.
    4. Rewrite `test_mttr.py::test_close_ticket_remediated_routes_through_helper` (line 253),
       renaming it to `test_close_ticket_done_drives_in_progress_never_remediated`. After
       `close_ticket(...)` assert: `fake_client.closed_refs == ["GV-1"]` (ticket still closes),
       `result["findings_awaiting_rescan"] == 1`, `vuln.status == "IN_PROGRESS"`, and
       `await _events_for(db_session, vuln.id) == []`. Then ADD the idempotency assertion: call
       `close_ticket(...)` a SECOND time on the same (now-resolved) URL and assert the comment-call
       count did NOT grow and no second AuditLog row was written (`row_was_resolved` guard holds).
       Update the docstring.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret uv run pytest tests/test_mttr.py -x -k "close_ticket"</automated>
  </verify>
  <done>close_ticket still closes the ticket on its provider + resolves ticket rows, but drives linked findings to IN_PROGRESS (+ single awaiting-rescan comment + audit) and never REMEDIATED; return exposes findings_awaiting_rescan; the rewritten test asserts closed_refs + IN_PROGRESS + zero events and is green.</done>
</task>

<task type="auto">
  <name>Task 3: whole-surface D-03 grep gate + regression verification</name>
  <files>backend/tests/test_mttr.py</files>
  <action>
    Prove the closure holds across the whole ticketing surface and nothing regressed:
    1. Run the ticketing-surface grep gate (POSIX class, macOS/BSD grep — not `\s`):
       `grep -rvE '^[[:space:]]*#' backend/app/ticketing/*.py | grep -c 'mark_vulnerability_remediated('`
       must return `0` (no call remains anywhere under `backend/app/ticketing/`).
    2. Sanity-check the two legitimate, OUT-OF-SCOPE call sites are UNTOUCHED and still present:
       `connectors/sync.py:258` (rescan-gated, `verified_by="rescan"`) and the two manual/bulk
       vuln-status-management calls in `vulnerabilities/service.py` (analyst directly setting a vuln
       REMEDIATED via the vuln API — NOT the ticketing surface; leave them alone).
    3. Run the full `test_mttr.py` and the ticketing regression suites green.
  </action>
  <verify>
    <automated>cd backend && test "$(grep -rvE '^[[:space:]]*#' app/ticketing/*.py | grep -c 'mark_vulnerability_remediated(')" = "0" && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret uv run pytest tests/test_mttr.py tests/test_ticket_status_sync.py tests/test_ticketing_dispatch.py tests/test_ticketing_clients.py -x</automated>
  </verify>
  <done>Ticketing-surface grep gate returns 0; connectors/sync.py + vulnerabilities/service.py sites untouched; test_mttr.py + test_ticket_status_sync.py + test_ticketing_dispatch.py + test_ticketing_clients.py all green.</done>
</task>

</tasks>

<artifacts>
## Artifacts this closure produces / changes

| Symbol / file | Change | Notes |
|---------------|--------|-------|
| `service.py::_AWAITING_RESCAN_COMMENT`, `_DONE_EXTERNAL_STATUSES`, `_was_previously_done` | NEW (promoted from daily_sync) | single owner; daily_sync now imports all three back |
| `daily_sync.py` import line | CHANGED | `from app.ticketing.service import _AWAITING_RESCAN_COMMENT, _DONE_EXTERNAL_STATUSES, _was_previously_done, map_ticket_status` + the three local definitions deleted |
| `service.py::_is_ticket_completed` | DELETED | no remaining callers after the `map_ticket_status` swap (dead code) |
| `service.py` imports | CHANGED | add `from app.audit import AuditLog` |
| `service.py::sync_ticket_status` | CHANGED | done arm now IN_PROGRESS + comment + audit via `map_ticket_status`, gated on `not was_done_before` (idempotent re-poll); `in_progress` intent = plain status set, no side effects; no REMEDIATED write; no resolved_at on done arm; second pass preserved |
| `service.py::close_ticket` | CHANGED | ticket still closes; findings driven IN_PROGRESS + comment + audit, gated on `not row_was_resolved` (idempotent repeat-close); returns `findings_awaiting_rescan` instead of `vulns_remediated` |
| `test_mttr.py` (2 tests) | REWRITTEN | assert IN_PROGRESS-only + zero RemediationEvent + double-call idempotency (fire-count does not grow) |
| `test_mttr.py::_FakeTicketingClient` | CHANGED (if needed) | count `comment` calls so the idempotency assertion can check fire-count |

No new migration, no new endpoint, no schema change, no frontend change.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider API → sync_ticket_status | Untrusted ticket status payloads (Jira/Asana/GitHub) cross into finding workflow writes via the router-invoked path |
| analyst request → close_ticket | An authenticated analyst's explicit "Close Ticket" click must not be able to force-close a scanner-detected finding |
| scheduler/router → AuditLog / status writes | System-actor status write; must carry the real `tenant_id` from the loaded finding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-14 | Elevation / integrity | sync_ticket_status ticket-done arm | mitigate | Remove the direct `mark_vulnerability_remediated` call; a done ticket read can at most set IN_PROGRESS via `map_ticket_status` (which structurally cannot return "remediated") — closure stays rescan-only (D-03) |
| T-37-15 | Elevation / integrity | close_ticket manual close | mitigate | Per the locked D-03 addendum, a manual analyst close drives the finding to IN_PROGRESS + awaiting-rescan audit, never REMEDIATED — no human action closes a finding the scanner still detects |
| T-37-16 | Tampering | map_ticket_status payload handling | mitigate | Reuse the existing whitelist mapper (`.get`-defaulted, V5 input validation); a malformed/garbage/unknown payload is a logged no-op, never a status write |
| T-37-17 | Repudiation | system-actor status write | mitigate | Every IN_PROGRESS transition writes a direct `AuditLog(user_email="system:ticket-sync", tenant_id=vuln.tenant_id)` — the who/when/why of the workflow move is auditable and tenant-scoped |
</threat_model>

<verification>
- `cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret uv run pytest tests/test_mttr.py tests/test_ticket_status_sync.py tests/test_ticket_sync_resilience.py tests/test_github_sync.py tests/test_ticketing_dispatch.py tests/test_ticketing_clients.py -x` green.
- D-03 ticketing-surface grep gate: `grep -rvE '^[[:space:]]*#' backend/app/ticketing/*.py | grep -c 'mark_vulnerability_remediated('` returns `0`.
- Whole-app sanity: `grep -rn 'mark_vulnerability_remediated(' backend/app/` shows the def in `vulnerabilities/service.py`, the rescan-gated `connectors/sync.py:258`, and the two manual/bulk vuln-status-management calls in `vulnerabilities/service.py` — and NOTHING under `backend/app/ticketing/`.
- Per project memory: run pytest per-file with `ENCRYPTION_KEY`/`JWT_SECRET_KEY` set, never the whole `tests/` dir.
</verification>

<success_criteria>
- SYNC-01 restored to full satisfaction: a ticket status change writes back onto the linked finding as a WORKFLOW transition (IN_PROGRESS) — via the scheduled poll AND the manual router endpoints AND manual close — and NEVER force-closes a still-detected finding (D-03, D-03 addendum).
- No `mark_vulnerability_remediated` call remains anywhere under `backend/app/ticketing/`.
- The `test_mttr.py` suite no longer green-lights REMEDIATED-on-ticket-done; both rewritten tests assert the IN_PROGRESS-only outcome with zero RemediationEvent rows.
- Preserved: sync_ticket_status's D-03-safe ticket auto-close/progress-comment second pass; close_ticket's provider close + ticket-row resolution; connectors/sync.py rescan closure; vulnerabilities/service.py manual/bulk status management.
</success_criteria>

<output>
After completion, create `.planning/phases/37-two-way-ticket-sync-remediation-verification/37-04-SUMMARY.md`.
In the SUMMARY, explicitly record that recurrence-reopen was left out of scope for the router path
(generic dispatch client has no reopen verb; daily_sync.py already owns it, VERIFIED) — this is an
intentional, documented boundary, not a dropped requirement.
</output>
