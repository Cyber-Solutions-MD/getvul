---
phase: 28-eval-cost-observability-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/v1/ai/usage.py
  - backend/app/api/v1/ai/__init__.py
  - backend/tests/test_ai_usage.py
autonomous: true
requirements: [AIE-03, AIE-04]
must_haves:
  truths:
    - "A tenant admin can GET /api/v1/ai/usage and receive month-to-date cost vs budget, a per-capability usage breakdown (6 fixed rows), the derived breaker status, and degraded-call count — all from existing ai.* AuditLog rows, no new telemetry"
    - "The batch vs on-demand prioritization split keys on user_email == 'system:scheduler', NEVER on status (a successful batch call audits status='ok')"
    - "breaker_tripped is derived with the IDENTICAL comparison check_tenant_budget() uses (monthly_cap_usd is not None and spent >= monthly_cap_usd) — never a second, independently-authored comparison"
    - "The endpoint is require_admin-gated and tenant-scoped (no cross-tenant usage leakage)"
  artifacts:
    - path: "backend/app/api/v1/ai/usage.py"
      provides: "GET /usage require_admin aggregation over ai.* AuditLog"
      contains: "require_admin"
    - path: "backend/tests/test_ai_usage.py"
      provides: "admin-only + tenant-scoped + user_email-split + breaker-derivation tests"
  key_links:
    - from: "backend/app/api/v1/ai/usage.py"
      to: "backend/app/ai/budget.py"
      via: "reuse get_month_to_date_spend + get_model_and_budget; breaker_tripped uses check_tenant_budget's exact comparison"
      pattern: "get_month_to_date_spend"
    - from: "backend/app/api/v1/ai/__init__.py"
      to: "usage.router"
      via: "ai_router.include_router(usage.router) mirroring status.router"
      pattern: "usage.router"
---

<objective>
Build the AIE-04 read-only usage/cost aggregation endpoint `GET /api/v1/ai/usage` (require_admin, tenant-scoped), aggregating the EXISTING `ai.*` AuditLog rows (D-08 — no new telemetry pipeline). It returns the data the admin pane (Plan 04) renders: model, monthly_budget_usd, spent_this_month_usd, the derived `breaker_tripped` boolean (AIE-03's frontend exposure), a 6-row per-capability breakdown, and degraded_calls_count.

Purpose: The observability half of the gate + the single source of the derived breaker status the pane reads (D-09). SC4, plus AIE-03's "expose the same derived boolean to the frontend".
Output: `backend/app/api/v1/ai/usage.py`, registration in `__init__.py`, `backend/tests/test_ai_usage.py`.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28-eval-cost-observability-gate/28-CONTEXT.md
@.planning/phases/28-eval-cost-observability-gate/28-RESEARCH.md
@.planning/phases/28-eval-cost-observability-gate/28-PATTERNS.md
@.planning/phases/28-eval-cost-observability-gate/28-UI-SPEC.md

<interfaces>
<!-- Extracted from PATTERNS.md AIE-04 section (all direct-read verified). -->

Analog route to mirror (backend/app/api/v1/ai/status.py, full file):
  router = APIRouter()
  @router.get("/status")
  async def get_ai_status(db: DBSession, user: Annotated[CurrentUser, Depends(require_viewer)]) -> dict[str, bool]:
      configured = await get_tenant_anthropic_key(db, user.tenant_id) is not None
      return {"configured": configured}
  # usage.py swaps require_viewer -> require_admin and returns a richer dict.

RBAC (backend/app/auth/rbac.py:52): require_admin = RequireRole(UserRole.ADMIN.value)

Reuse (READ-ONLY):
  backend/app/ai/budget.py: async def get_month_to_date_spend(db, tenant_id) -> float   # SUMs AuditLog.details["cost_estimate_usd"].as_float() where action LIKE 'ai.%', created_at >= month_start
  backend/app/ai/budget.py: check_tenant_budget comparison = (monthly_cap_usd is not None and spent >= monthly_cap_usd)  # invert of "under budget"
  backend/app/ai/explain.py: async def get_model_and_budget(db, tenant_id) -> (model, monthly_cap_usd)
  backend/app/ai/tenant_keys.py: async def get_tenant_anthropic_key(db, tenant_id) -> key | None
  backend/app/audit.py AuditLog columns: tenant_id, user_email(String 320), action(String 50), resource_type(String 50), details(JSONB: cost_estimate_usd, input_tokens, output_tokens, status), created_at

The 6 fixed capability rows (28-UI-SPEC Meter/Table Contract) — (resource_type, discriminator):
  1 vuln                 | none
  2 host                 | none
  3 remediation          | none
  4 remediation-guidance | none
  5 prioritization       | user_email != 'system:scheduler'   (on demand)
  6 prioritization       | user_email == 'system:scheduler'   (batch)
  # user_email='system:scheduler' set at batch.py:267,318,511. NEVER split on status (successful batch audits status='ok').

Registration analog (backend/app/api/v1/ai/__init__.py:37-54): mirror status.router — add usage to the import tuple + ai_router.include_router(usage.router) as the last line.

RESEARCH Code Example "AIE-04 Usage Aggregation Endpoint" (lines ~484-585) is the reference implementation — follow it, with the user_email discriminator and the get_month_to_date_spend-based breaker_tripped exactly as written.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET /api/v1/ai/usage aggregation endpoint + registration</name>
  <files>backend/app/api/v1/ai/usage.py, backend/app/api/v1/ai/__init__.py</files>
  <read_first>
    - backend/app/api/v1/ai/status.py (full) — the thin DBSession + role-Depends shape to copy.
    - backend/app/tenants/router.py:378-391 (get_audit_log) — the require_admin + tenant-scoped-AuditLog precedent.
    - backend/app/ai/budget.py:41-80 (get_month_to_date_spend + check_tenant_budget) — reuse both; the breaker comparison MUST match check_tenant_budget's.
    - backend/app/audit.py:36-50 (AuditLog columns) + the JSONB indexing DSL (budget.py:47-57).
    - 28-RESEARCH.md Code Example "AIE-04 Usage Aggregation Endpoint" + 28-UI-SPEC Meter/Table Contract (the 6 rows + degraded footnote definition).
  </read_first>
  <action>
    Create backend/app/api/v1/ai/usage.py with an APIRouter and `@router.get("/usage")` handler gated by `Depends(require_admin)`, returning a dict. Resolve model + monthly_cap_usd via get_model_and_budget(db, user.tenant_id); spent via get_month_to_date_spend(db, user.tenant_id); configured via get_tenant_anthropic_key. Compute `breaker_tripped = monthly_cap_usd is not None and spent >= monthly_cap_usd` (the exact comparison check_tenant_budget uses — never a second one). Build a _CAPABILITY_ROWS list of (resource_type, is_batch) with is_batch None/False/True per the 6-row table; for each run one AuditLog aggregation (count, SUM cost_estimate_usd, SUM input_tokens+output_tokens) scoped by tenant_id + action LIKE 'ai.%' + resource_type + created_at >= month_start, adding the user_email discriminator only for the two prioritization rows (is_batch True -> == 'system:scheduler', False -> != 'system:scheduler'). Compute degraded_calls_count = COUNT of ai.* rows this month whose details['status'] != 'ok'. Return {configured, model, monthly_budget_usd, spent_this_month_usd, breaker_tripped, capability_breakdown[], degraded_calls_count}. Reuse budget.py's JSONB indexing operators verbatim (AuditLog.details[...].as_float()/.as_integer()/.as_string()).

    Register in backend/app/api/v1/ai/__init__.py: add `usage` to the existing `from app.api.v1.ai import (...)` tuple and add `ai_router.include_router(usage.router)` as the last include, mirroring status.router exactly.
  </action>
  <acceptance_criteria>
    - `grep -c 'Depends(require_admin)' backend/app/api/v1/ai/usage.py` >= 1 (admin-gated, not require_viewer).
    - The endpoint contains no `status LIKE 'batch_%'` / `status = 'batch` split logic; the prioritization split uses `user_email` == / != 'system:scheduler' (grep confirms both the == and != forms).
    - breaker_tripped is computed as `monthly_cap_usd is not None and spent >= monthly_cap_usd` (grep the `>=` comparison; no independent TS-style re-derivation).
    - Every aggregation query is filtered by `AuditLog.tenant_id == user.tenant_id` and `AuditLog.action.like("ai.%")` (tenant-scoped; D-08 no-new-telemetry).
    - `__init__.py` imports `usage` and calls `ai_router.include_router(usage.router)`.
    - App imports cleanly and the route is mounted (present in the OpenAPI paths).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c 'from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())') JWT_SECRET_KEY=test python -c "from app.main import create_app; app=create_app(); paths=[r.path for r in app.routes]; assert '/api/v1/ai/usage' in paths, paths; print('usage route mounted')"</automated>
  </verify>
  <done>GET /api/v1/ai/usage mounted, admin-gated, tenant-scoped, 6-row user_email-split breakdown, derived breaker_tripped matching the backend guard, degraded footnote count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: usage endpoint tests (RBAC, tenant scope, batch split, breaker derivation)</name>
  <files>backend/tests/test_ai_usage.py</files>
  <behavior>
    - A VIEWER/ANALYST caller gets 403 (require_admin); an ADMIN/OWNER gets 200.
    - Two tenants' ai.* rows do not bleed: tenant A's usage totals exclude tenant B's rows.
    - A successful batch prioritization row (status='ok', user_email='system:scheduler') lands in row 6 (batch), NOT row 5 (on demand) — proving the user_email split, not a status split.
    - breaker_tripped is true when seeded spend >= cap and false when under; null cap -> false.
    - degraded_calls_count counts ai.* rows with status != 'ok'.
  </behavior>
  <read_first>
    - backend/tests/test_ai_budget.py::_seed_ai_spend (27-43) — reuse/adapt for seeding ai.* rows with a given resource_type/user_email/status/cost.
    - backend/tests/conftest.py — `client` (admin? confirm role) + `client_factory` (role-switching) fixtures; use client_factory to exercise the 403 (viewer/analyst) vs 200 (admin) cases.
    - backend/app/ai/batch.py:511 — the success-path audit sets user_email='system:scheduler' with status='ok' (the exact shape row 6 must catch).
  </read_first>
  <action>
    Create backend/tests/test_ai_usage.py. Seed ai.* AuditLog rows (adapting _seed_ai_spend to set resource_type, user_email, status, cost_estimate_usd/input_tokens/output_tokens) covering: on-demand prioritization (analyst email), batch prioritization (user_email='system:scheduler', status='ok'), a couple vuln/host rows, and a degraded row (status != 'ok'). Write tests: (1) RBAC — viewer and analyst -> 403, admin -> 200 (via client_factory identities); (2) tenant isolation — seed tenant B rows, assert tenant A's totals exclude them; (3) batch split — assert the prioritization batch row's calls/cost come from the system:scheduler rows and the on-demand row excludes them (a status-based split would misattribute — assert the correct attribution); (4) breaker derivation — over-cap seed -> breaker_tripped true, under-cap -> false, null cap -> false; (5) degraded_calls_count matches the seeded non-ok rows.
  </action>
  <acceptance_criteria>
    - Explicit assertions for 403 (viewer AND analyst) and 200 (admin).
    - A test named for the batch/on-demand split seeds a status='ok' + user_email='system:scheduler' row and asserts it is counted in the batch row (row 6), not the on-demand row (row 5).
    - A cross-tenant test asserts tenant B's seeded spend is absent from tenant A's spent_this_month_usd and breakdown.
    - breaker_tripped asserted true/false/false for over-cap/under-cap/null-cap.
    - Suite green against Postgres+Redis.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c 'from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())') JWT_SECRET_KEY=test python -m pytest tests/test_ai_usage.py -q</automated>
  </verify>
  <done>Tests prove admin-only access, tenant isolation, the user_email batch split, and the derived breaker — all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/v1/ai/usage | admin-authenticated request crosses into tenant-scoped aggregation |
| aggregation query → AuditLog | must never read another tenant's ai.* rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | Information Disclosure | GET /api/v1/ai/usage aggregation | mitigate | Every query filtered by AuditLog.tenant_id == user.tenant_id (mirrors get_month_to_date_spend); tested via a cross-tenant isolation case |
| T-28-04 | Elevation of Privilege | usage endpoint RBAC | mitigate | Depends(require_admin) is the authoritative gate (backend, not UI); tested with viewer/analyst -> 403, admin -> 200 |
| T-28-09 | Tampering / mis-observability | breaker_tripped derivation | mitigate | Derived with check_tenant_budget's exact comparison — a divergent comparison could hide real overspend from the admin; grep + test enforce parity |
</threat_model>

## Artifacts this phase produces (Plan 03)
- `backend/app/api/v1/ai/usage.py`: read-only require_admin usage/cost aggregation endpoint (AIE-04) exposing the derived breaker status (AIE-03).
- `backend/tests/test_ai_usage.py`: RBAC + tenant-isolation + user_email-split + breaker-derivation tests.
- `usage.router` registered in the ai router.

<verification>
- Route mounted at /api/v1/ai/usage, admin-gated.
- Batch/on-demand split keys on user_email, not status.
- breaker_tripped matches the backend guard comparison exactly.
- No new telemetry table/pipeline — pure AuditLog aggregation (D-08).
</verification>

<success_criteria>
The admin usage/cost data source exists, is safe (admin-only, tenant-scoped), and surfaces the single derived breaker boolean the pane (Plan 04) consumes. Feeds SC4 and AIE-03's frontend-exposure delta.
</success_criteria>

<output>
After completion, create `.planning/phases/28-eval-cost-observability-gate/28-03-SUMMARY.md`. Document the response shape (field names) so Plan 04's hook types match exactly.
</output>
