---
phase: 28-eval-cost-observability-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/scripts/capture_ai_goldens.py
  - backend/tests/evals/__init__.py
  - backend/tests/evals/metrics.py
  - backend/tests/evals/test_golden_evals.py
  - backend/tests/evals/goldens/vuln/grounded.json
  - backend/tests/evals/goldens/vuln/insufficient_evidence.json
  - backend/tests/evals/goldens/host/grounded.json
  - backend/tests/evals/goldens/host/insufficient_evidence.json
  - backend/tests/evals/goldens/remediation/grounded.json
  - backend/tests/evals/goldens/remediation/insufficient_evidence.json
  - backend/tests/evals/goldens/remediation_guidance/grounded.json
  - backend/tests/evals/goldens/remediation_guidance/insufficient_evidence.json
  - backend/tests/evals/goldens/prioritization/grounded.json
  - backend/tests/evals/goldens/prioritization/insufficient_evidence.json
autonomous: true
requirements: [AIE-01]
must_haves:
  truths:
    - "A DeepEval structural eval suite runs KEYLESS (zero model calls) against 10 committed golden fixtures and fails when schema/grounding/citation/no-rank/no-PII/cite-or-refuse regresses"
    - "The eval metrics call the PRODUCTION gates (recheck_business_rules + model_validate_json), never re-implement them, so the eval can never drift from what production enforces"
    - "A documented one-time dev-key capture script exists to (re)generate golden fixtures, redacted to a synthetic tenant"
  artifacts:
    - path: "backend/tests/evals/metrics.py"
      provides: "5 non-LLM BaseMetric subclasses: SchemaValidMetric, GroundingTraceabilityMetric, NoRankInvariantMetric, NoOwnerPiiMetric, CiteOrRefuseMetric"
      contains: "recheck_business_rules"
    - path: "backend/tests/evals/test_golden_evals.py"
      provides: "parametrized assert_test over goldens/ x 5 metrics"
      contains: "assert_test"
    - path: "backend/scripts/capture_ai_goldens.py"
      provides: "one-time dev-key fixture capture (D-07), synthetic records by construction"
    - path: "backend/tests/evals/goldens/prioritization/grounded.json"
      provides: "a golden fixture that validates against ExplainPrioritizationResponse with zero numeric rank field"
  key_links:
    - from: "backend/tests/evals/metrics.py"
      to: "backend/app/ai/schemas.py"
      via: "import recheck_business_rules + ExplainResponseBase.model_validate_json"
      pattern: "recheck_business_rules"
    - from: "backend/tests/evals/test_golden_evals.py"
      to: "backend/app/ai/prompt_builder.py"
      via: "import VULN_ALLOWLIST/HOST_ALLOWLIST/REMEDIATION_ALLOWLIST/REMEDIATION_GUIDANCE_ALLOWLIST/PRIORITIZATION_ALLOWLIST"
      pattern: "ALLOWLIST"
---

<objective>
Build the AIE-01 CI-blocking eval harness: a DeepEval pytest-native suite that asserts STRUCTURAL properties (schema validity, grounding-traceability, no-rank, no-owner-PII, cite-or-refuse) over 10 committed golden fixtures — with ZERO model calls (D-01/D-06 keyless-CI, BYOK privacy). Also ship the one-time dev-key capture script (D-07) and the committed golden fixtures (D-02).

Purpose: This is the mechanism that enforces Pitfall #8 ("shipping without evals"): the eval, not a prose snapshot, is the arbiter. SC1.
Output: `backend/tests/evals/` package (metrics + golden test + fixtures), `backend/scripts/capture_ai_goldens.py`, `deepeval==4.1.5` pinned dev dep.

NOTE on fixtures (honest scope): `GETVUL_DEV_ANTHROPIC_KEY` is confirmed absent in this environment (STATE Phase 24-01 blocker). The capture script IS the reproducible D-07 mechanism and is a required deliverable. For the committed fixtures: if a dev key is present, run the script; otherwise hand-author each fixture's `model_response` so it passes the SAME production validation gates the script would enforce (`model_validate_json` + `recheck_business_rules`), using synthetic records by construction, and add a re-capture note in the capture script docstring. Either way the harness is fully CI-blocking and meaningful — do NOT reduce the metric set or skip a capability to work around the missing key.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/28-eval-cost-observability-gate/28-CONTEXT.md
@.planning/phases/28-eval-cost-observability-gate/28-RESEARCH.md
@.planning/phases/28-eval-cost-observability-gate/28-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase via PATTERNS.md. No exploration required. -->

From backend/app/ai/schemas.py (READ-ONLY ground truth — never modified this phase):
  class ExplainResponseBase  # fields: summary, business_risk, citations, grounded
  class ExplainVulnResponse(ExplainResponseBase)          # zero added fields
  class ExplainHostResponse(ExplainResponseBase)
  class ExplainRemediationResponse(ExplainResponseBase)
  class ExplainRemediationGuidanceResponse(ExplainResponseBase)
  class ExplainPrioritizationResponse(ExplainResponseBase)  # no-rank contract (docstring lines 101-105)
  class BusinessRuleError(...)
  def recheck_business_rules(resp: ExplainResponseBase, *, allowed_source_fields: frozenset[str] | None = None) -> None
  # raises BusinessRuleError when: summary/business_risk over char budget, OR a citation.source_field is not None and not in allowed_source_fields
  MAX_SUMMARY_CHARS, MAX_BUSINESS_RISK_CHARS

From backend/app/ai/prompt_builder.py (READ-ONLY):
  VULN_ALLOWLIST (line 54), HOST_ALLOWLIST (358), REMEDIATION_ALLOWLIST (599),
  REMEDIATION_GUIDANCE_ALLOWLIST (800), PRIORITIZATION_ALLOWLIST (1032)  # all frozenset[str]
  build_explain_vuln_prompt / _host / _remediation / _remediation_guidance / _prioritization
  # each returns (SYSTEM_PROMPT, [{"type":"text","text": user_block}])

From backend/app/ai/explain.py (READ-ONLY):
  def _build_output_config(response_model, model)  # line 138-144 — structured-output config shape for a raw AsyncAnthropic call

From deepeval (new dep 4.1.5):
  from deepeval.metrics import BaseMetric          # implement measure()/a_measure()/is_successful()/__name__
  from deepeval.test_case import LLMTestCase       # input=, actual_output=
  from deepeval import assert_test                 # assert_test(test_case, [metrics...]) -> raises AssertionError

Existing no-rank precedent (backend/tests/test_ai_schemas.py:198-206):
  field_names == {"summary","business_risk","citations","grounded"}
  forbidden = {"priority","rank","score","ai_priority","ai_rank"}; assert not (field_names & forbidden)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: deepeval dep + capture script + 10 committed golden fixtures</name>
  <files>backend/pyproject.toml, backend/scripts/capture_ai_goldens.py, backend/tests/evals/goldens/{vuln,host,remediation,remediation_guidance,prioritization}/{grounded,insufficient_evidence}.json</files>
  <read_first>
    - backend/pyproject.toml lines 29-39 (the pinned dev-dep list: ruff==0.15.21, mypy==2.1.0 — the exact-pin-with-comment convention to mirror). PATTERNS "backend/pyproject.toml" analog.
    - backend/app/ai/prompt_builder.py build_explain_vuln_prompt (294-315) + the 4 sibling builders (562, 762, 986, 1214) — the exact prompt-build call shape. PATTERNS "capture_ai_goldens.py" analog.
    - backend/app/ai/explain.py:138-144 (_build_output_config) + 392-394 (model_validate_json + recheck_business_rules validation chain the script must reuse).
    - 28-RESEARCH.md "Golden-Fixture Capture" section (the 10-fixture table + the per-capability grounded/insufficient_evidence cases + the redaction-by-construction design).
    - The 5 allowlist constants + their line numbers (interfaces block above) — each fixture's grounding_record must contain ONLY that capability's allowlisted field names.
  </read_first>
  <action>
    Add `"deepeval==4.1.5",` to backend/pyproject.toml's [project.optional-dependencies].dev list, following the exact-pin-with-comment convention already used for ruff/mypy (comment: pinned — DeepEval metric/CLI behavior changes across versions, keeps the gate deterministic). Then `pip install -e ".[dev]"` in backend/.venv so the dep is importable.

    Create backend/scripts/capture_ai_goldens.py: a one-time, manually-invoked, dev-key-only script (reads GETVUL_DEV_ANTHROPIC_KEY). Its docstring must state it is NOT run in CI (D-07), that records are hand-authored synthetic by construction (never real tenant data — D-07 redaction is structural, not post-hoc), and note the re-capture procedure. For each of the 10 capability x case rows in RESEARCH's Golden-Fixture table it: (1) builds a hand-authored synthetic grounding_record containing only that capability's allowlisted fields (synthetic hostnames like acme-web-01, synthetic CVE IDs); (2) calls the real build_explain_*_prompt(record) + a raw AsyncAnthropic call using _build_output_config's shape; (3) validates via response_model.model_validate_json() + recheck_business_rules() — only a genuinely valid capture is written; (4) dumps {grounding_record, schema_name, model_response, model_used, captured_at} JSON to backend/tests/evals/goldens/<capability>/<case>.json.

    Create the 10 committed golden fixtures. If GETVUL_DEV_ANTHROPIC_KEY is set, run the script to generate them. Otherwise hand-author each fixture's model_response so it passes response_model.model_validate_json() + recheck_business_rules(allowed_source_fields=<that allowlist>): grounded cases have grounded=true with >=1 citation whose source_field is in the allowlist; insufficient_evidence cases have grounded=false. schema_name must name the matching Explain*Response class. grounding_record must contain ONLY allowlisted field names for that capability (synthetic values). The prioritization fixtures must have model_response keys == exactly {summary, business_risk, citations, grounded} — no numeric rank/priority/score field anywhere.
  </action>
  <acceptance_criteria>
    - `grep -c 'deepeval==4.1.5' backend/pyproject.toml` returns 1; `python -c "import deepeval"` in backend/.venv succeeds.
    - backend/scripts/capture_ai_goldens.py exists; its docstring contains the literal substrings "one-time", "not" + "CI" (documents it is not a CI dependency), and "GETVUL_DEV_ANTHROPIC_KEY".
    - Exactly 10 fixture JSON files exist under backend/tests/evals/goldens/ (2 per capability x 5 capabilities).
    - For every fixture: `ExplainResponseBase.model_validate_json(json.dumps(fixture["model_response"]))` succeeds AND `recheck_business_rules(candidate, allowed_source_fields=<capability allowlist>)` raises nothing.
    - Each grounding_record's keys are a subset of that capability's allowlist frozenset.
    - Both prioritization fixtures' model_response keys == {"summary","business_risk","citations","grounded"} with no key in {priority,rank,score,ai_priority,ai_rank}.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c 'from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())') JWT_SECRET_KEY=test python -c "import json,glob,pathlib; from app.ai.schemas import ExplainResponseBase, recheck_business_rules; from app.ai.prompt_builder import VULN_ALLOWLIST,HOST_ALLOWLIST,REMEDIATION_ALLOWLIST,REMEDIATION_GUIDANCE_ALLOWLIST,PRIORITIZATION_ALLOWLIST; A={'vuln':VULN_ALLOWLIST,'host':HOST_ALLOWLIST,'remediation':REMEDIATION_ALLOWLIST,'remediation_guidance':REMEDIATION_GUIDANCE_ALLOWLIST,'prioritization':PRIORITIZATION_ALLOWLIST}; fs=sorted(glob.glob('tests/evals/goldens/*/*.json')); assert len(fs)==10,len(fs); [recheck_business_rules(ExplainResponseBase.model_validate_json(json.dumps(json.loads(pathlib.Path(f).read_text())['model_response'])), allowed_source_fields=A[pathlib.Path(f).parent.name]) for f in fs]; print('10 fixtures valid')"</automated>
  </verify>
  <done>deepeval pinned + importable; capture script committed with the required docstring; 10 synthetic golden fixtures committed, each passing the production schema + business-rule gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DeepEval non-LLM structural metrics + golden eval test</name>
  <files>backend/tests/evals/__init__.py, backend/tests/evals/metrics.py, backend/tests/evals/test_golden_evals.py</files>
  <behavior>
    - SchemaValidMetric: fixture whose model_response validates against its named Explain*Response -> score 1.0; a malformed one -> 0.0.
    - GroundingTraceabilityMetric: a citation.source_field outside the capability allowlist -> 0.0 (via recheck_business_rules raising); all-allowlisted -> 1.0.
    - NoRankInvariantMetric: a model_response carrying a numeric priority/rank/score key -> 0.0; the clean {summary,business_risk,citations,grounded} shape -> 1.0.
    - NoOwnerPiiMetric: a model_response containing a synthetic owner-PII sentinel value -> 0.0; clean -> 1.0.
    - CiteOrRefuseMetric: grounded=true with zero citations -> 0.0; grounded=true with >=1 citation OR grounded=false -> 1.0.
    - test_golden_evals: all 10 committed fixtures pass all 5 metrics via assert_test.
  </behavior>
  <read_first>
    - backend/tests/test_connectors/__init__.py (empty package-marker precedent — evals/ needs its own __init__.py because test_golden_evals does `from .metrics import ...`). PATTERNS AIE-01 directory-shape.
    - backend/app/ai/schemas.py recheck_business_rules (129-162) — the production gate metrics.py MUST call, never re-implement (PATTERNS anti-pattern warning).
    - backend/tests/test_ai_schemas.py:198-206 (no-rank precedent) + 217-240 (citation-substring-of-source-field precedent).
    - 28-RESEARCH.md Pattern 1 (Non-LLM BaseMetric) + Pattern 2 (golden-fixture-driven test) code examples.
  </read_first>
  <action>
    Create backend/tests/evals/__init__.py (empty, 0 bytes) so the relative import works.

    Create backend/tests/evals/metrics.py with 5 BaseMetric subclasses, each pure-Python (no evaluation_model, no network — this is the keyless guarantee). Each implements measure()/a_measure()/is_successful()/__name__ and sets self.score/self.success. GroundingTraceabilityMetric and SchemaValidMetric MUST import and call ExplainResponseBase.model_validate_json + recheck_business_rules from app.ai.schemas (never a re-implementation). NoRankInvariantMetric asserts the parsed response keys == {summary,business_risk,citations,grounded} and shares nothing with {priority,rank,score,ai_priority,ai_rank}. NoOwnerPiiMetric does a defense-in-depth keyword scan of the serialized model_response for a small OWNER_PII_SENTINELS set (the synthetic owner-PII values the allowlists exclude at input; mirror safety.py's "small known set" reasoning in a comment). CiteOrRefuseMetric passes when (grounded and len(citations)>=1) or (not grounded). If deepeval's untyped API trips mypy strict, add a narrow `# type: ignore[import-untyped]` scoped to metrics.py only (Pitfall 7 — do NOT loosen strict project-wide).

    Create backend/tests/evals/test_golden_evals.py: build the _ALLOWLISTS map (5 capability -> allowlist), a _load_goldens() generator over goldens/*/*.json, and a @pytest.mark.parametrize("capability,fixture_path", ...) test that loads each fixture into an LLMTestCase(input=json.dumps(grounding_record), actual_output=json.dumps(model_response)) and runs assert_test(test_case, [all 5 metrics]). Add a trailing comment documenting the CI invocation: `deepeval test run tests/evals/test_golden_evals.py`.
  </action>
  <acceptance_criteria>
    - backend/tests/evals/__init__.py exists and is empty (0 bytes).
    - metrics.py defines all 5 named classes; `grep -v '^#' backend/tests/evals/metrics.py | grep -c 'recheck_business_rules'` >= 1 (production gate is actually called, not commented).
    - metrics.py contains no `evaluation_model=`/`GEval`/`FaithfulnessMetric`/`AnthropicModel`/`api_key` reference (keyless proof).
    - test_golden_evals.py runs `deepeval test run` green over all 10 fixtures with zero model calls (no ANTHROPIC_API_KEY in the verify env).
    - A deliberately-corrupted fixture (extra "priority": 5 key injected in a temp copy) makes NoRankInvariantMetric fail — proving the gate bites (executor demonstrates once, does not commit the corrupted copy).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && DEEPEVAL_TELEMETRY_OPT_OUT=1 ENCRYPTION_KEY=$(python -c 'from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())') JWT_SECRET_KEY=test deepeval test run tests/evals/test_golden_evals.py</automated>
  </verify>
  <done>5 non-LLM metrics calling production gates; golden eval suite green keyless over 10 fixtures via `deepeval test run`; keyless properties grep-provable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture data → git repo | golden JSON is checked in; must never carry real tenant data |
| eval metric → production schema code | metrics assert against production gates; drift here silently weakens the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | Information Disclosure | backend/tests/evals/goldens/*.json + capture_ai_goldens.py | mitigate | Hand-authored synthetic records by construction (never captured-then-redacted real data), per D-07; acceptance asserts grounding_record keys ⊆ allowlist (no owner-PII fields) |
| T-28-07 | Tampering | backend/tests/evals/metrics.py | mitigate | Metrics call production recheck_business_rules/model_validate_json directly (grep-enforced), so an eval can never assert a weaker contract than production enforces |
| T-28-08 | Information Disclosure | deepeval telemetry in CI | mitigate | DEEPEVAL_TELEMETRY_OPT_OUT=1 in every eval invocation (Pitfall 2); no Confident API key -> no data leaves the runner |
</threat_model>

## Artifacts this phase produces (Plan 01)
- `backend/tests/evals/` package: 5 keyless structural metrics + parametrized golden eval suite.
- `backend/tests/evals/goldens/**/*.json`: 10 committed synthetic golden fixtures (2 per capability).
- `backend/scripts/capture_ai_goldens.py`: documented one-time dev-key capture mechanism (D-07).
- `deepeval==4.1.5` pinned dev dependency.

<verification>
- `deepeval test run tests/evals/test_golden_evals.py` green, keyless, over 10 fixtures.
- All 10 fixtures pass production schema + business-rule gates.
- No LLM-judge/model-call reference anywhere in metrics.py or the blocking suite.
</verification>

<success_criteria>
AIE-01 harness exists and is CI-blocking-ready: structural (never prose) assertions over real-shaped golden fixtures, keyless, calling production gates. Feeds Plan 05's `ai-evals` CI job.
</success_criteria>

<output>
After completion, create `.planning/phases/28-eval-cost-observability-gate/28-01-SUMMARY.md`. Record whether fixtures were dev-key-captured or hand-authored (and the re-capture note), plus any mypy-baseline handling for deepeval.
</output>
