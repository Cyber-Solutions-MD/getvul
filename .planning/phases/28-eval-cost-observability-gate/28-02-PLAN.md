---
phase: 28-eval-cost-observability-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_ai_injection_redteam.py
  - backend/tests/test_ai_budget_coverage.py
  - backend/tests/test_ai_prompt_builder.py
  - backend/tests/test_ai_prompt_builder_host.py
  - backend/tests/test_ai_prompt_builder_remediation_guidance.py
  - backend/tests/test_ai_prompt_builder_prioritization.py
autonomous: true
requirements: [AIE-02, AIE-03]
must_haves:
  truths:
    - "A single consolidated KEYLESS pytest suite proves an adversarial payload (across ~15-25 payloads x 5 categories) lands ONLY inside the <scanner_data> block and never in the system prompt, for every one of the 5 AI capabilities"
    - "A no-bypass coverage test proves an over-budget tenant NEVER reaches a BILLED Anthropic dispatch on any of the 5 explain routes or the batch prewarm path"
    - "Both suites run with zero model calls and are the AIE-02 + AIE-03 CI-blocking gates"
  artifacts:
    - path: "backend/tests/test_ai_injection_redteam.py"
      provides: "parametrized adversarial-corpus x 5-capability injection-isolation contract"
      contains: "scanner_data"
    - path: "backend/tests/test_ai_budget_coverage.py"
      provides: "module-local patch coverage test across 5 routes + DI-seam batch path"
      contains: "app.ai.explain.AsyncAnthropic"
  key_links:
    - from: "backend/tests/test_ai_injection_redteam.py"
      to: "backend/app/ai/prompt_builder.py"
      via: "calls build_explain_*_prompt directly (pure fn, in-process)"
      pattern: "build_explain_.*_prompt"
    - from: "backend/tests/test_ai_budget_coverage.py"
      to: "app.ai.explain.AsyncAnthropic (+ run_batch_prewarm anthropic_client_factory= seam for batch)"
      via: "patch the module-local bound name for the 5 explain routes; inject a fake via the DI seam for the batch path (NOT the top-level anthropic.AsyncAnthropic)"
      pattern: "app.ai.explain.AsyncAnthropic|anthropic_client_factory="
---

<objective>
Ship the two KEYLESS CI-blocking backend gates that share the `ai-redteam-injection` CI job (Plan 05):
- AIE-02: consolidate the 5 scattered single-payload `test_injection_isolation` functions into ONE parametrized adversarial corpus (~15-25 payloads, 5 categories) x 5 capabilities, asserting the injection-as-data contract (payload appears only inside `<scanner_data>`, never as an instruction). Real `promptfoo redteam` cannot run keylessly (verified — it needs a remote/configured LLM), so the CI-blocking tier is plain pytest against the real prompt builders; real promptfoo is reserved for the opt-in key-gated tier (Plan 05). This is a deliberate, reasoned divergence from AIE-02's literal "promptfoo" wording, flagged in RESEARCH Open Question 1 and honored here.
- AIE-03: a no-bypass coverage test proving no AI call path bills before the budget guard trips. The `check_tenant_budget()` guard already exists and is already effectively persistent (monotonic month-to-date AuditLog SUM) — this plan does NOT build a new breaker state machine; it proves the sweep covers every call site.

Purpose: Pitfall #1 (prompt injection) release-gate + Pitfall #5 (cost blowup) release-gate. SC2, SC3.
Output: `backend/tests/test_ai_injection_redteam.py`, `backend/tests/test_ai_budget_coverage.py`.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28-eval-cost-observability-gate/28-CONTEXT.md
@.planning/phases/28-eval-cost-observability-gate/28-RESEARCH.md
@.planning/phases/28-eval-cost-observability-gate/28-PATTERNS.md

<interfaces>
<!-- Extracted from PATTERNS.md direct reads. -->

The 5 existing single-payload injection tests to CONSOLIDATE (PATTERNS AIE-02 table — note: 5 functions across 4 files; the host file has BOTH):
  vuln                  | tests/test_ai_prompt_builder.py::test_injection_isolation (line 98)
  host                  | tests/test_ai_prompt_builder_host.py::test_host_injection_isolation_stays_inside_scanner_data (line 246)
  remediation           | tests/test_ai_prompt_builder_host.py::test_remediation_injection_isolation (line 362)
  remediation_guidance  | tests/test_ai_prompt_builder_remediation_guidance.py::test_injection_isolation (line 177)
  prioritization        | tests/test_ai_prompt_builder_prioritization.py::test_injection_isolation (line 185)

Prompt builders (READ-ONLY, backend/app/ai/prompt_builder.py) — each returns (SYSTEM_PROMPT*, [{"type":"text","text": user_block}]):
  build_explain_vuln_prompt / _host / _remediation / _remediation_guidance / _prioritization
  # user block shape: f'<scanner_data source="...">{json.dumps(model.model_dump())}</scanner_data>'
  # SYSTEM_PROMPT* each carry an <untrusted_content_policy> block

Budget guard (READ-ONLY, backend/app/ai/budget.py):
  async def check_tenant_budget(db, tenant_id, monthly_cap_usd) -> bool   # fail-closed; None cap -> True; else spent < cap
  async def would_exceed_budget_for_batch(...)   # batch equivalent

5 explain routes (all dispatch through _run_explain_stream; check_tenant_budget at explain.py:308 runs BEFORE client construction at :339):
  POST /api/v1/ai/explain-vuln/{finding_id}                 (explain_vuln.py:52)
  POST /api/v1/ai/explain-host/{asset_id}                   (explain_host.py:51)
  POST /api/v1/ai/explain-remediation/{cve_id}              (explain_remediation.py:56)  # CVE-ID string keyed
  POST /api/v1/ai/explain-remediation-guidance/{finding_id} (explain_remediation_guidance.py:114)
  POST /api/v1/ai/explain-prioritization/{finding_id}       (explain_prioritization.py:107)

Batch path (backend/app/ai/batch.py, run_batch_prewarm):
  :210 client = (anthropic_client_factory or _default_client_factory)(key)   # constructed UNCONDITIONALLY; _default_client_factory (imported from app.ai.explain) builds app.ai.explain.AsyncAnthropic — Test B injects a fake via anthropic_client_factory= instead (test_ai_batch.py precedent), never a real client
  :259 est = await estimate_batch_cost_usd(client, model, requests)          # real count_tokens() call, pre-gate BY DESIGN
  :261 if await would_exceed_budget_for_batch(...): ... continue             # the fail-closed gate
  :278 batch = await client.messages.batches.create(requests=requests)       # the BILLED dispatch — gated

Test helpers (backend/tests/test_ai_budget.py):
  async def _seed_ai_spend(db_session, tenant_id, cost_estimate_usd, action="ai.explain.vuln")  # reuse, don't re-derive
Fixture (backend/tests/conftest.py): use `client` (analyst-auth AsyncClient, :345-362) — NOT `async_client` (does not exist).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Consolidated keyless prompt-injection red-team suite (AIE-02)</name>
  <files>backend/tests/test_ai_injection_redteam.py, backend/tests/test_ai_prompt_builder.py, backend/tests/test_ai_prompt_builder_host.py, backend/tests/test_ai_prompt_builder_remediation_guidance.py, backend/tests/test_ai_prompt_builder_prioritization.py</files>
  <behavior>
    - For every (payload, capability) pair: payload is absent from the system prompt AND present in the user/scanner_data block.
    - A literal `</scanner_data>` embedded in a poisoned field does NOT break out of the block (JSON-encoding neutralizes it) — the inner tag count equals the payload's own tag count.
    - All 5 capabilities exercised via their real record factories against every payload.
  </behavior>
  <read_first>
    - The 5 existing test functions (interfaces block) — copy each one's exact record-factory shape (_finding(remediation_info=...), _host_record(hostname=...), _record(department=...), etc.) so the consolidated cases mirror what already passes.
    - backend/app/ai/prompt_builder.py SYSTEM_PROMPT (277-291) + SYSTEM_PROMPT_PRIORITIZATION (1190-1211) — confirm the <untrusted_content_policy> uniformity. PATTERNS AIE-02 "System prompt source".
    - backend/app/ai/explain.py:147-163 (_extract_scanner_data — the rightmost `</scanner_data>` close-tag convention the breakout check mirrors).
    - 28-RESEARCH.md Pattern 3 + Open Question 2 (the recommended corpus composition).
  </read_first>
  <action>
    Create backend/tests/test_ai_injection_redteam.py — self-contained (inline record factories per RESEARCH Pattern 3; do NOT cross-import test helpers between test files). Module docstring states: keyless (zero model calls), consolidates the 5 single-payload isolation tests, and that real promptfoo is the opt-in key-gated tier (RESEARCH Open Question 1 divergence).

    Define ADVERSARIAL_PAYLOADS with ~15-25 entries across 5 categories per RESEARCH Open Question 2: 3-4 "ignore previous instructions" variants, 2-3 system-prompt-extraction attempts, 2-3 tag/delimiter-breakout attempts (INCLUDING a literal `</scanner_data>` substring), 2-3 role-play/jailbreak framings, 1-2 obfuscation/unicode attempts.

    Define CAPABILITY_CASES: one row per capability = (builder_fn, poisoned_field, record_factory) covering all 5 builders, poisoning a free-text allowlisted field each (vuln:remediation_info, host:hostname, remediation:fix, remediation_guidance:remediation_info, prioritization:department). Double-parametrize over (payload) x (capability). Each test: build (system, blocks); assert payload not in system; assert payload in user_text; and a tag-boundary breakout check using the rightmost `</scanner_data>` index so an embedded literal close-tag stays counted only inside the JSON string.

    Then remove the 5 now-superseded single-payload test FUNCTIONS from the 4 old files (test_injection_isolation in test_ai_prompt_builder.py:98; test_host_injection_isolation_stays_inside_scanner_data:246 AND test_remediation_injection_isolation:362 in test_ai_prompt_builder_host.py; test_injection_isolation in test_ai_prompt_builder_remediation_guidance.py:177 and test_ai_prompt_builder_prioritization.py:185). Delete ONLY those functions — keep every per-capability helper factory and every other test in those files intact.
  </action>
  <acceptance_criteria>
    - `len(ADVERSARIAL_PAYLOADS) >= 15`; the corpus includes an entry containing the literal substring `</scanner_data>`.
    - CAPABILITY_CASES has exactly 5 rows, one per builder (all 5 build_explain_*_prompt functions referenced).
    - The new suite runs KEYLESS green (no ANTHROPIC_API_KEY in env) with >= 75 parametrized cases (>=15 payloads x 5 capabilities).
    - `grep -c 'def test_injection_isolation' backend/tests/test_ai_prompt_builder.py` == 0 and the two host-file functions + the remediation_guidance/prioritization functions are removed (grep each name == 0), while `grep -c 'def ' ` on each file stays > 0 (other tests + factories preserved).
    - The full `tests/test_ai_prompt_builder*.py` suite still passes after removals (no orphaned reference to a deleted function).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c 'from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())') JWT_SECRET_KEY=test python -m pytest tests/test_ai_injection_redteam.py tests/test_ai_prompt_builder.py tests/test_ai_prompt_builder_host.py tests/test_ai_prompt_builder_remediation_guidance.py tests/test_ai_prompt_builder_prioritization.py -q</automated>
  </verify>
  <done>One consolidated keyless red-team suite (>=15 payloads x 5 capabilities) green; the 5 superseded single-payload functions removed; sibling suites still green.</done>
</task>

<task type="auto">
  <name>Task 2: No-bypass budget coverage test (AIE-03)</name>
  <files>backend/tests/test_ai_budget_coverage.py</files>
  <read_first>
    - backend/tests/test_ai_budget.py — reuse _seed_ai_spend (27-43) + its fixtures. PATTERNS AIE-03 analog.
    - backend/tests/conftest.py:345-362 (`client` fixture) — the correct fixture name (RESEARCH Code Example's `async_client` does NOT exist).
    - backend/app/ai/batch.py:200-278 (run_batch_prewarm) — the CRITICAL nuance: client constructed at :210 and count_tokens called at :259 BOTH run before the budget gate; only `.batches.create()` at :278 is the billed, gated dispatch. PATTERNS "CRITICAL CORRECTION" + Verification Correction #3.
    - backend/tests/test_ai_batch.py — the `_FakeBatchAnthropic`/`_FakeBatches`/`_FakeTokensCount` fakes injected via `run_batch_prewarm(anthropic_client_factory=...)`; mirror this DI seam for Test B (no global monkeypatch, no real network call).
    - backend/tests/test_ai_explain_prioritization.py:146 (`patch("app.ai.explain.AsyncAnthropic")`) — the correct precedent (6 existing repo call sites patch the module-local bound name). Both explain.py:54 and batch.py:71 do `from anthropic import AsyncAnthropic`, binding the name in their OWN namespace at import time, so a top-level `anthropic.AsyncAnthropic` patch (Pitfall 6) intercepts NEITHER — the resulting `call_count == 0` assertion is tautologically true. `_default_client_factory` (explain.py:121) constructs `app.ai.explain.AsyncAnthropic`, so a single patch there also covers the batch path's default factory.
    - 28-RESEARCH.md Pattern 4 + the Anti-Patterns list.
  </read_first>
  <action>
    Create backend/tests/test_ai_budget_coverage.py. Module docstring: D-04 "no AI call path bypasses the guard"; Test A patches the module-local bound name `app.ai.explain.AsyncAnthropic` (the target of 6 existing repo precedents — a top-level `anthropic.AsyncAnthropic` patch would NOT intercept either module's import-time binding, making the assertion tautological); Test B injects a fake via the `anthropic_client_factory=` DI seam on run_batch_prewarm so the batch path makes ZERO real Anthropic network calls (Pitfall 6).

    Test A (5 explain routes, parametrized over ALL_EXPLAIN_ROUTES): seed the tenant over budget via _seed_ai_spend (e.g. cost_estimate_usd=999.0) and seed an ANTHROPIC ConnectorConfig with monthly_budget_usd=1.0. `with patch("app.ai.explain.AsyncAnthropic") as mock_cls:` invoke each POST route via the `client` fixture (format finding_id/cve_id per route). Assert `mock_cls.call_count == 0` — the budget_exceeded short-circuit at explain.py:308 runs before the client is constructed at explain.py:339 (`_default_client_factory` builds app.ai.explain.AsyncAnthropic), so patching the module-local bound name genuinely observes every construction on the 5 explain routes. To prove the gate is REAL (not tautological), add one under-budget control assertion: with the tenant UNDER budget the same patched mock_cls IS constructed (call_count >= 1) — so removing the budget guard would flip the over-budget call_count to >= 1 and FAIL the over-budget assertion.

    Test B (batch path): seed the same tenant over budget. Inject a fake client via the documented `anthropic_client_factory=` DI seam on run_batch_prewarm (mirror test_ai_batch.py's `_FakeBatchAnthropic`/`_FakeBatches`/`_FakeTokensCount` — a recording factory, NOT a global monkeypatch, so no real network call is ever possible). The fake's `messages.count_tokens` returns a fixed integer input-token count (so estimate_batch_cost_usd computes a real pre-estimate without raising) and its `messages.batches.create` records each call. Call `run_batch_prewarm(anthropic_client_factory=fake_factory)`. Assert the budget-gate ordering holds against the fake: the client IS constructed and the free count_tokens pre-estimate IS called, but the BILLED `.batches.create` is NEVER called (`fake.batches_created == 0` / zero recorded create_calls) — a budget-exceeded tenant reaches ZERO billed dispatches. Document this construct-then-count-then-gate asymmetry in a comment.
  </action>
  <acceptance_criteria>
    - Uses the `client` fixture (grep shows no `async_client`).
    - Test A patches `app.ai.explain.AsyncAnthropic` (the module-local bound name) and asserts `mock_cls.call_count == 0` across all 5 routes in ALL_EXPLAIN_ROUTES (5 parametrized cases; explain-remediation keyed by cve_id string). `grep -c 'app.ai.explain.AsyncAnthropic' backend/tests/test_ai_budget_coverage.py` >= 1; the file does NOT patch the top-level `anthropic.AsyncAnthropic` (a bare `patch("anthropic.AsyncAnthropic"` / `patch('anthropic.AsyncAnthropic'` grep == 0) and does NOT patch `_default_client_factory`.
    - REGRESSION GATE (the coverage test must be genuinely able to FAIL): Test A includes an under-budget control asserting the same patched mock IS constructed (call_count >= 1). If a budget-guard call is removed from _run_explain_stream, the over-budget assertion flips to call_count >= 1 and the test FAILS — the assertion is NOT tautological.
    - Test B injects the fake via `run_batch_prewarm(anthropic_client_factory=...)` (grep shows `anthropic_client_factory=` usage), asserts zero calls on `...batches.create` (the billed dispatch), and does NOT assert zero client constructions for the batch path; a comment explains the batch nuance (client + count_tokens legitimately pre-gate). No test makes a real Anthropic network call (KEYLESS batch path via the DI seam).
    - Suite green against Postgres+Redis up.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c 'from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())') JWT_SECRET_KEY=test python -m pytest tests/test_ai_budget_coverage.py -q</automated>
  </verify>
  <done>Coverage test proves the billed dispatch is unreachable over budget across all 5 explain routes + the batch path; Test A patches the module-local app.ai.explain.AsyncAnthropic (with an under-budget control proving the assertion can fail), Test B injects a fake via the anthropic_client_factory= DI seam (keyless, zero real network calls); honors the batch construct-then-count-then-gate nuance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner-sourced free text → prompt | untrusted third-party data must stay data, never instructions |
| any AI call site → billed Anthropic dispatch | every path must pass the fail-closed budget gate first |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering / Elevation of Privilege | build_explain_*_prompt (injection-as-data) | mitigate | Consolidated keyless red-team proves an adversarial payload lands only inside <scanner_data> across all 5 capabilities x ~15-25 payloads (verifies the existing JSON-encoding contract; does not rebuild it) |
| T-28-02 | Denial of Service (financial) | 5 explain routes + batch prewarm | mitigate | No-bypass coverage test proves the billed dispatch is unreachable over budget on every path; Test A patches the module-local app.ai.explain.AsyncAnthropic (asserts zero construction on the 5 explain routes, with an under-budget control proving the gate is real), Test B injects a fake via the anthropic_client_factory= DI seam (asserts zero billed .batches.create, keyless — no real network call) (Pitfall 6) |
</threat_model>

## Artifacts this phase produces (Plan 02)
- `backend/tests/test_ai_injection_redteam.py`: consolidated keyless injection-isolation gate (AIE-02).
- `backend/tests/test_ai_budget_coverage.py`: no-bypass budget coverage gate (AIE-03).
- 5 superseded single-payload injection functions removed from 4 prompt-builder test files.

<verification>
- Both suites green keyless (injection) / against DB (coverage).
- No `promptfoo`/`redteam` binary invoked in the CI-blocking path (that is the opt-in tier, Plan 05).
- Batch coverage asserts on the billed `.batches.create`, not on client construction.
</verification>

<success_criteria>
AIE-02 injection-isolation and AIE-03 no-bypass gates exist, run keyless/DB-only with zero model calls, and are ready for Plan 05's `ai-redteam-injection` CI job.
</success_criteria>

<output>
After completion, create `.planning/phases/28-eval-cost-observability-gate/28-02-SUMMARY.md`. Record the final payload-corpus size, which functions were removed, and the batch-path mock nuance handling.
</output>
