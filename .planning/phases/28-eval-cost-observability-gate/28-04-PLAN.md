---
phase: 28-eval-cost-observability-gate
plan: 04
type: execute
wave: 2
depends_on: [28-03]
files_modified:
  - frontend/src/lib/queries/keys.ts
  - frontend/src/lib/queries/use-ai-usage.ts
  - frontend/src/components/ui/progress.tsx
  - frontend/src/components/settings/microcopy.ts
  - frontend/src/components/settings/settings-sidebar-shell.tsx
  - frontend/src/app/(authed)/dashboard/settings/page.tsx
  - frontend/src/components/settings/ai-usage-pane.tsx
  - frontend/src/components/settings/ai-usage-pane.test.tsx
autonomous: true
requirements: [AIE-04]
must_haves:
  truths:
    - "A tenant admin sees a new 'AI usage & settings' settings category showing month-to-date cost vs budget, a 6-row per-capability usage breakdown, the breaker status, and a read-only key & model summary linking out to Connectors"
    - "When the breaker is tripped, an amber 'AI paused — budget exceeded' banner is the pane's primary visual anchor, rendered above everything else"
    - "The pane is admin-only via the existing ADMIN_ONLY sidebar mechanism + the require_admin backend gate (no bespoke gating code)"
    - "The pane composes existing primitives (Stat, audit-log-pane table recipe, SyncStatusPill recipe, DegradedCard amber recipe) and adds exactly one new shadcn primitive (progress); no new fonts/hex/foundation tokens"
  artifacts:
    - path: "frontend/src/components/settings/ai-usage-pane.tsx"
      provides: "the AIE-04 admin pane (4 cards: breaker banner, status/budget+meter, usage table, key & model)"
      contains: "data-pane=\"ai\""
    - path: "frontend/src/lib/queries/use-ai-usage.ts"
      provides: "useAiUsage() single admin-gated GET hook"
      contains: "/api/v1/ai/usage"
    - path: "frontend/src/components/ui/progress.tsx"
      provides: "shadcn progress primitive restyled with sunset tokens (budget meter)"
  key_links:
    - from: "frontend/src/components/settings/ai-usage-pane.tsx"
      to: "/api/v1/ai/usage"
      via: "useAiUsage() hook"
      pattern: "useAiUsage"
    - from: "frontend/src/app/(authed)/dashboard/settings/page.tsx"
      to: "AiUsagePane"
      via: "renderPane() switch case 'ai'"
      pattern: "case 'ai'"
    - from: "frontend/src/components/settings/settings-sidebar-shell.tsx"
      to: "ADMIN_ONLY set"
      via: "append 'ai' to ALL_CATEGORIES + ADMIN_ONLY"
      pattern: "'ai'"
---

<objective>
Ship the AIE-04 admin surface: a new admin-only "AI usage & settings" category in `/dashboard/settings`, rendered by a new `ai-usage-pane.tsx` that reads `GET /api/v1/ai/usage` (Plan 03) and shows month-to-date cost vs budget (with a restyled shadcn `progress` meter), a 6-row per-capability usage breakdown, the derived breaker status (amber banner + pill), and a read-only key & model summary that links out to the existing Connectors wizard (D-05 — this phase does NOT rebuild key/model/budget edit UI).

Purpose: The one user-facing deliverable of the gate — one admin home for AI usage/cost/settings. SC4.
Output: pane + hook + query-key + progress primitive + 3 registration edits + pane test.

MANDATORY: Before writing ANY frontend code, read the `sketch-findings-getvul` skill (CLAUDE.md routing) — especially `references/foundation.md`, `references/state-patterns.md`, `references/visual-language.md`, `references/copy-voice.md`. Every token/card/pill/table chrome is INHERITED per 28-UI-SPEC.md; do NOT restyle existing primitives or pick freehand hex. The ONLY new primitive is `progress` (add + restyle with sunset tokens, exactly like the `tooltip`/`textarea` precedent).
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-eval-cost-observability-gate/28-UI-SPEC.md
@.planning/phases/28-eval-cost-observability-gate/28-PATTERNS.md
@.planning/phases/28-eval-cost-observability-gate/28-CONTEXT.md
@.claude/skills/sketch-findings-getvul/SKILL.md

<interfaces>
<!-- Extracted from PATTERNS.md AIE-04 frontend section + RESEARCH hook shape. -->

Response type (from Plan 03's GET /api/v1/ai/usage — match field names EXACTLY):
  AiUsageCapabilityRow = { resource_type: string; is_batch: boolean | null; calls: number; cost_usd: number; tokens: number }
  AiUsageResult = { configured: boolean; model: string; monthly_budget_usd: number | null;
                    spent_this_month_usd: number; breaker_tripped: boolean;
                    capability_breakdown: AiUsageCapabilityRow[]; degraded_calls_count: number }

Hook analog (frontend/src/lib/queries/use-ai-status.ts, full) — copy shape, staleTime: 30_000, retry: 1:
  useQuery({ queryKey: queryKeys.ai.usage(), queryFn: ({signal}) => api<AiUsageResult>('/api/v1/ai/usage',{signal}), staleTime: 30_000, retry: 1 })

Query key (frontend/src/lib/queries/keys.ts:96-100) — add to the EXISTING ai block:
  ai: { explain: (...)=>..., status: () => ['ai','status'] as const, usage: () => ['ai','usage'] as const }

Pane analog (frontend/src/components/settings/audit-log-pane.tsx, full 262 lines) — copy structure:
  'use client'; imports SkeletonTable/EmptyState/PartialFailureBanner from '@/components/states'; queryKeys.
  root: <div data-pane="ai" className="p-6 space-y-6"> (24px gap — 4 cards; vs audit's space-y-4)
  error: <PartialFailureBanner watchKeys={[queryKeys.ai.usage()]} onRetry={refetch} />
  table chrome VERBATIM: overflow-x-auto rounded-lg border border-border-subtle; thead border-b border-border-subtle bg-surface;
    th text-xs font-medium uppercase tracking-wide text-text-faint px-4 py-3; tbody divide-y divide-border-subtle;
    tr bg-surface hover:bg-surface-2; td px-4 py-2.

Stat (frontend/src/components/ui/stat.tsx) — import unchanged; number treatment font-mono text-4xl font-bold leading-none tabular-nums. Pass label/value/optional hint; omit delta/deltaIsGood.

SyncStatusPill RECIPE (frontend/src/components/connectors/sync-status-pill.tsx:30-51) — recreate as a new instance (do not import):
  Active            -> border-severity-low/40 bg-severity-low/10 text-severity-low   (ok recipe)
  Paused — budget exceeded -> border-amber/40 bg-amber/10 text-[var(--color-amber-on-soft)]  (syncing recipe)
  Not configured    -> border-border-subtle bg-surface-2 text-text-faint             (__never recipe)

DegradedCard amber chrome (frontend/src/components/ai/ai-explanation-section.tsx:47-71) is MODULE-PRIVATE — RECREATE inline (do not import):
  <div role="status" className="rounded-lg border border-border-subtle bg-surface-2 p-5"> + h-8 w-8 rounded-full bg-amber-soft chip + <AlertTriangle className="h-4 w-4"/> + heading text-sm font-medium + body mt-1 text-sm text-text-muted + action.

progress primitive: no analog file — `npx shadcn add progress`, then hand-edit generated bg-primary/track classes to sunset tokens exactly like tooltip.tsx (frontend/src/components/ui/tooltip.tsx:14-34) did.

Registration edits (all confirmed via direct read):
  microcopy.ts: add | 'ai' to Category union; ai: 'AI usage & settings' to CATEGORY_LABELS.
  settings-sidebar-shell.tsx: append 'ai' to ALL_CATEGORIES (:40-47, last) AND ADMIN_ONLY set (:53-58).
  page.tsx: append 'ai' to CATEGORY_ALLOW_LIST (:51-58, after 'audit'); add `case 'ai': return <AiUsagePane />;` to renderPane() (:114-131) + import AiUsagePane (alongside line 45 AuditLogPane import).
</interfaces>

<ui_considerations>
<!-- LIFTED from 28-UI-SPEC ## UI Considerations — every row is a must-have for the pane. -->
- loading: single admin-gated query, <300ms expected — a lightweight pulse placeholder (like AiExplanationSection's prereqsPending), NOT a heavy SkeletonTable.
- empty (no key configured): "AI isn't set up yet" card + "Configure AI" -> /dashboard/connectors (Copywriting Contract; role is always Admin/Owner here so the CTA branch always renders).
- empty (key configured, zero usage): "No AI usage yet" — the 4 cards STILL render with zero-value shapes (Status='Active', Budget '$0.00 of $Y.YY (0%)' or 'No cap set', all-zero table rows, Key & model populated) — never replace the whole pane.
- error: PartialFailureBanner verbatim.
- populated / zero-one-many: the usage table renders EXACTLY 6 fixed rows always (literal 0 / $0.00 / 0 shown, never hidden); "many" is structurally impossible.
- partial (monthly_budget_usd null): no meter bar rendered; "No monthly cap set — spend is unlimited for this tenant." caption + plain $ figure only. Never a 0%- or full-bar for the no-cap case.
- partial (ticket-draft attribution) — 🧪 BACKSTOP: ticket-draft calls fold into rows 1 (Explain — vulnerability) and 4 (Remediation guidance); do NOT fabricate a 7th row or a new resource_type. A held-out check confirms no 7th capability row/resource_type was introduced.
- overflow (key & model values): model is a fixed short enum ("Sonnet 5"/"Opus 5"/"Haiku"); connector name not shown (avoids truncation).
- long-text (breaker banner body): fixed locked copy, no dynamic text.
</ui_considerations>
</context>

<tasks>

<task type="auto">
  <name>Task 1: query key + useAiUsage hook + progress primitive + 3 registration edits</name>
  <files>frontend/src/lib/queries/keys.ts, frontend/src/lib/queries/use-ai-usage.ts, frontend/src/components/ui/progress.tsx, frontend/src/components/settings/microcopy.ts, frontend/src/components/settings/settings-sidebar-shell.tsx, frontend/src/app/(authed)/dashboard/settings/page.tsx</files>
  <read_first>
    - frontend/src/lib/queries/use-ai-status.ts (full) + keys.ts:96-100 — the hook + key shape.
    - frontend/src/components/ui/tooltip.tsx:14-34 — the "add shadcn primitive + restyle sunset tokens" precedent for progress.
    - frontend/src/components/settings/microcopy.ts (full) + settings-sidebar-shell.tsx:40-58 + settings/page.tsx:45,51-58,114-131 — the exact registration edit sites (PATTERNS AIE-04 frontend).
    - sketch-findings-getvul references/foundation.md — the sunset token names for the progress track/fill.
  </read_first>
  <action>
    Add `usage: () => ['ai', 'usage'] as const,` inside the existing queryKeys.ai block in keys.ts (do not create a new namespace).

    Create use-ai-usage.ts mirroring use-ai-status.ts: export AiUsageCapabilityRow + AiUsageResult types (field names exactly matching Plan 03's response), and useAiUsage() using queryKeys.ai.usage(), api<AiUsageResult>('/api/v1/ai/usage',{signal}), staleTime: 30_000, retry: 1.

    Run `npx shadcn add progress` (use --legacy-peer-deps if the lucide/React19 peer conflict blocks install, per the established package.json override precedent). Hand-edit the generated Progress component: replace bg-primary/default track+indicator classes with sunset tokens — track bg-surface-2 rounded-full; the indicator color will be driven by a caller-passed className (the SLA 3-tier fill), so keep the indicator class overridable via props.

    Registration: microcopy.ts add `| 'ai'` to Category and `ai: 'AI usage & settings'` to CATEGORY_LABELS. settings-sidebar-shell.tsx append 'ai' to ALL_CATEGORIES (last) and to the ADMIN_ONLY set. page.tsx append 'ai' to CATEGORY_ALLOW_LIST after 'audit', add the AiUsagePane import, and add `case 'ai': return <AiUsagePane />;` to renderPane().
  </action>
  <acceptance_criteria>
    - `grep -c "usage: () =>" frontend/src/lib/queries/keys.ts` >= 1 inside the ai block.
    - use-ai-usage.ts exports useAiUsage + AiUsageResult with field names {configured, model, monthly_budget_usd, spent_this_month_usd, breaker_tripped, capability_breakdown, degraded_calls_count}.
    - frontend/src/components/ui/progress.tsx exists; contains no `bg-primary` (restyled to sunset tokens); track uses bg-surface-2.
    - `grep -c "'ai'" frontend/src/components/settings/settings-sidebar-shell.tsx` shows 'ai' in BOTH ALL_CATEGORIES and ADMIN_ONLY.
    - page.tsx has `case 'ai'` in renderPane and the AiUsagePane import; microcopy.ts has `ai: 'AI usage & settings'`.
    - tsc + eslint clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx eslint src/lib/queries/use-ai-usage.ts src/lib/queries/keys.ts src/components/ui/progress.tsx src/components/settings/microcopy.ts src/components/settings/settings-sidebar-shell.tsx "src/app/(authed)/dashboard/settings/page.tsx"</automated>
  </verify>
  <done>Hook + query key + restyled progress primitive + all 3 registration edits land; the 'ai' category is admin-only and wired to render AiUsagePane; tsc/eslint clean.</done>
</task>

<task type="auto">
  <name>Task 2: ai-usage-pane.tsx (4 cards) + pane test</name>
  <files>frontend/src/components/settings/ai-usage-pane.tsx, frontend/src/components/settings/ai-usage-pane.test.tsx</files>
  <read_first>
    - frontend/src/components/settings/audit-log-pane.tsx (full) — root container, error banner, table chrome to reuse verbatim.
    - frontend/src/components/ui/stat.tsx (Stat) + frontend/src/components/connectors/sync-status-pill.tsx:30-51 (pill recipe) + frontend/src/components/ai/ai-explanation-section.tsx:47-71,258-266 (DegradedCard amber chrome + the locked budget-exceeded copy/destination).
    - 28-UI-SPEC.md ALL sections — Copywriting Contract (LOCKED copy verbatim), Meter/Table Contract (6 rows + SLA 3-tier fill + no-cap rule + degraded footnote), Color, Typography, Spacing, the <ui_considerations> block above.
    - frontend/src/components/settings/audit-log-pane.test.tsx — the vitest QueryClientProvider + vi.mock('@/lib/api') + role-mocked useAuth pattern.
    - sketch-findings-getvul references/state-patterns.md + copy-voice.md.
  </read_first>
  <action>
    Create ai-usage-pane.tsx ('use client', data-pane="ai", root <div className="p-6 space-y-6">). Consume useAiUsage(). Render, in order:
    1. Breaker-tripped banner — ONLY when breaker_tripped: the recreated DegradedCard amber chrome (rounded-lg border border-border-subtle bg-surface-2 p-5 + amber-soft chip + AlertTriangle), heading "AI paused — budget exceeded", the locked banner body verbatim from the Copywriting Contract, action "Raise the cap" -> /dashboard/connectors. This is the primary visual anchor, above everything.
    2. Status + Budget row: the 3-state SyncStatusPill recipe (Active/Paused — budget exceeded/Not configured) + the month-to-date spend Stat (font-mono text-4xl). The budget meter uses the restyled progress primitive with the SLA 3-tier fill (<75% success/green, 75-99% amber, >=100% danger). When monthly_budget_usd is null: render NO bar, only the $ figure + "No monthly cap set — spend is unlimited for this tenant." caption. Captions verbatim from the Copywriting Contract ("$X.XX of $Y.YY used this month (Z%)" / "— cap reached").
    3. Usage-by-capability table (verbatim audit-log-pane chrome): exactly 6 fixed rows always, labels locked ("Explain — vulnerability" / "Explain — host posture" / "Explain — remediation impact" / "Remediation guidance" / "Prioritization — on demand" / "Prioritization — batch"), columns Capability/Calls/Cost/Tokens, zero shown as literal 0/$0.00/0, tokens with thousands separators, cost as $X.XX. Below the table, the degraded-calls footnote rendered ONLY when degraded_calls_count > 0, copy verbatim.
    4. Key & model summary card: read-only metadata grid (Model as the enum label "Sonnet 5"/"Opus 5"/"Haiku" — never the raw model ID; Monthly budget "$Y.YY" or "No cap set"; Connector Enabled/Disabled) + single "Manage in Connectors" btn-secondary -> /dashboard/connectors.
    States: loading -> lightweight pulse (not SkeletonTable); error -> PartialFailureBanner watchKeys={[queryKeys.ai.usage()]}; no-key (configured=false) -> "AI isn't set up yet" + "Configure AI" -> /dashboard/connectors; zero-usage (configured, all-zero) -> the 4 cards still render with zero shapes + "No AI usage yet". All copy verbatim from the Copywriting Contract. Do NOT fabricate a 7th ticket-draft row.

    Create ai-usage-pane.test.tsx (audit-log-pane.test.tsx pattern): QueryClientProvider + vi.mock('@/lib/api') + role-mocked useAuth (ADMIN). Cover: breaker-tripped banner renders + is first/anchor when breaker_tripped; no-cap path renders no meter bar + the no-cap caption; the table renders exactly 6 rows with locked labels; a held-out backstop assertion that NO 7th row / no fabricated resource_type exists (row count == 6); zero-usage renders all-zero rows not an empty screen; error renders PartialFailureBanner.
  </action>
  <acceptance_criteria>
    - Pane renders exactly 6 capability rows (test asserts row count == 6 and the 6 locked labels) — the ticket-draft backstop.
    - breaker-tripped banner: appears only when breaker_tripped, uses amber chrome (bg-surface-2 + amber-soft chip + AlertTriangle), heading "AI paused — budget exceeded", action -> /dashboard/connectors. Test asserts it is present-when-tripped / absent-when-not.
    - monthly_budget_usd null -> no progress bar + the "No monthly cap set…" caption (test asserts no meter role/element).
    - No freehand hex and no new font: `grep -cE '#[0-9A-Fa-f]{6}' frontend/src/components/settings/ai-usage-pane.tsx` == 0; DegradedCard is recreated (not imported) and SyncStatusPill is recreated (not imported).
    - Model shown as enum label, never a raw `claude-` id (grep the file has no `claude-` literal).
    - Pane test suite + tsc + eslint green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run ai-usage-pane && npx tsc --noEmit && npx eslint src/components/settings/ai-usage-pane.tsx</automated>
  </verify>
  <done>The AIE-04 admin pane renders all 4 cards with every UI-SPEC state (loading/empty×2/error/populated/no-cap/breaker-tripped), 6 fixed rows, locked copy, composed from inherited primitives + the one new progress meter; pane test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-admin user → settings sidebar | must not see or reach the AI pane's data |
| browser pane → /api/v1/ai/usage | display-only; backend is the authoritative RBAC + tenant gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Elevation of Privilege | ai-usage-pane visibility | mitigate | 'ai' added to ADMIN_ONLY (sidebar hide is UX-only); a hand-crafted ?category=ai still hits the require_admin backend gate -> 403 -> PartialFailureBanner (existing T-14-04/T-14-16 precedent, zero new gating code) |
| T-28-10 | Information Disclosure | breaker/budget display | accept | The pane only renders what the tenant-scoped admin endpoint returns; no independent client-side data access — display fidelity guaranteed by consuming the server's derived breaker_tripped, never recomputing it |
</threat_model>

## Artifacts this phase produces (Plan 04)
- `frontend/src/components/settings/ai-usage-pane.tsx` + `.test.tsx`: the AIE-04 admin pane.
- `frontend/src/lib/queries/use-ai-usage.ts` + `queryKeys.ai.usage()`.
- `frontend/src/components/ui/progress.tsx`: restyled shadcn budget meter (the only new primitive).
- 3 registration edits (microcopy, sidebar-shell, settings page) wiring the admin-only 'ai' category.

<verification>
- New admin-only "AI usage & settings" category renders the 4-card pane.
- Every UI-SPEC state covered; 6 fixed rows; no fabricated ticket-draft row.
- Inherited chrome only (no freehand hex, no new font, one new primitive).
- Breaker banner is the anchor when tripped; no-cap renders no meter.
</verification>

<success_criteria>
SC4: a tenant admin can view AI usage + cost and reach key/model/budget management (via the Connectors link) in the UI, on the sunset design system, admin-only, composed from inherited primitives.
</success_criteria>

<output>
After completion, create `.planning/phases/28-eval-cost-observability-gate/28-04-SUMMARY.md`. Note the progress-primitive restyle, any --legacy-peer-deps use, and the ticket-draft-attribution backstop result.
</output>
