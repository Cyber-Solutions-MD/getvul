---
phase: 28-eval-cost-observability-gate
plan: 05
type: execute
wave: 2
depends_on: [28-01, 28-02]
files_modified:
  - .github/workflows/ci.yml
  - .github/branch-protection.json
autonomous: true
requirements: [AIE-01, AIE-02, AIE-03]
must_haves:
  truths:
    - "Two new KEYLESS CI jobs (AI Golden-Set Evals + AI Prompt-Injection Red-Team) run on every push/PR alongside semgrep, with NO continue-on-error, and are added to branch-protection.json's required checks so a regression actually blocks merge"
    - "The eval job runs `deepeval test run` with DEEPEVAL_TELEMETRY_OPT_OUT=1 (never bare pytest); the red-team job runs the consolidated injection suite AND the budget coverage test — all with zero model calls"
    - "An opt-in, key-gated, non-blocking live tier runs the real LLM-judge eval + real promptfoo redteam ONLY when a dev key secret is present and NEVER on fork PRs"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "ai-evals + ai-redteam-injection (blocking) + ai-live-eval-optin (non-blocking) jobs"
      contains: "deepeval test run"
    - path: ".github/branch-protection.json"
      provides: "the 2 new blocking checks added to required_status_checks.checks[]"
      contains: "AI Golden-Set Evals"
  key_links:
    - from: ".github/workflows/ci.yml (ai-evals job)"
      to: "backend/tests/evals/test_golden_evals.py"
      via: "deepeval test run"
      pattern: "deepeval test run tests/evals"
    - from: ".github/branch-protection.json"
      to: "ci.yml job name strings"
      via: "required_status_checks.checks[].context matches the job name:"
      pattern: "AI Golden-Set Evals"
---

<objective>
Wire the keyless CI-blocking gates into CI and make them ACTUALLY merge-blocking. Adding a job to ci.yml alone does NOT block merges on this repo (confirmed: the `docs` job runs but is not a required check) — the job `name:` strings must ALSO be added to `.github/branch-protection.json`'s `required_status_checks.checks[]`. Add three jobs: two blocking keyless jobs (`ai-evals`, `ai-redteam-injection`) sibling to `semgrep`, and one opt-in, key-gated, non-blocking live tier (`ai-live-eval-optin`) that never runs on fork PRs.

Purpose: D-01's "CI-BLOCKING" contract + Pitfall #8 enforcement (the eval is the arbiter). Closes SC1/SC2/SC3 at the CI level.
Output: `.github/workflows/ci.yml` (+3 jobs), `.github/branch-protection.json` (+2 required checks).

NAMING NOTE: Plan 02 created the red-team file as `backend/tests/test_ai_injection_redteam.py` (matches 28-VALIDATION.md). Use that exact path in the CI job (RESEARCH's structure sketch used `test_ai_redteam_injection.py` — the committed file name wins; keep them consistent).
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-eval-cost-observability-gate/28-RESEARCH.md
@.planning/phases/28-eval-cost-observability-gate/28-PATTERNS.md
@.planning/phases/28-eval-cost-observability-gate/28-CONTEXT.md

<interfaces>
<!-- Extracted from PATTERNS.md CI section (all direct-read verified). -->

ci.yml env (lines 12-14): PYTHON_VERSION "3.12", NODE_VERSION "20".
Blocking job shape to mirror = the `backend` job (defaults.run.working-directory: backend + actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6 + `pip install -e ".[dev]"`) — the eval/red-team jobs need app.ai.* importable, so use this shape, NOT semgrep's container shape. No continue-on-error (matches semgrep/backend blocking convention).
Non-blocking secrets-gated shape to mirror = the `dast` job (:160-213): needs:/if:/continue-on-error: true. Pitfall 4 wrinkle: gate on a secret via env-indirection — `env: HAS_DEV_KEY: ${{ secrets.DEV_ANTHROPIC_API_KEY != '' }}` then `if: env.HAS_DEV_KEY == 'true'` on steps; also `if: github.event.repository.fork == false` so it never runs on untrusted fork PRs.
checkout pin: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5. setup-node pin: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.

RESEARCH "CI Integration" section (lines ~709-789) is the reference YAML for all three jobs — follow it, correcting the red-team file path to test_ai_injection_redteam.py.

branch-protection.json current (full file) required_status_checks.checks[] = [Backend, Frontend, Semgrep SAST, Terraform Validate] (each {"context": ..., "app_id": -1}). `docs` is deliberately NOT in this list — proving a job must be named here to block. Add the 2 new blocking job names; do NOT add the opt-in job.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3 AI CI jobs + register the 2 blocking checks in branch-protection</name>
  <files>.github/workflows/ci.yml, .github/branch-protection.json</files>
  <read_first>
    - .github/workflows/ci.yml: the `backend` job (:31-69, working-directory + setup-python + pip install -e ".[dev]"), the `semgrep` job (:148-158, blocking), the `dast` job (:160-213, non-blocking + needs/if), and env (:12-14). PATTERNS CI section.
    - .github/branch-protection.json (full) — the checks[] array + app_id: -1 convention.
    - 28-RESEARCH.md "CI Integration" reference YAML (all 3 jobs) + Pitfall 4 (secrets-in-if env indirection) + Pitfall 2 (DEEPEVAL_TELEMETRY_OPT_OUT=1) + Pitfall 1 (deepeval test run, not bare pytest).
    - Confirm the committed test file names from Plans 01/02: tests/evals/test_golden_evals.py, tests/test_ai_injection_redteam.py, tests/test_ai_budget_coverage.py.
  </read_first>
  <action>
    Add job `ai-evals` (name: "AI Golden-Set Evals (DeepEval)"): backend-shaped (working-directory backend, setup-python v6, pip install -e ".[dev]"), no continue-on-error, step runs `deepeval test run tests/evals/test_golden_evals.py` with env DEEPEVAL_TELEMETRY_OPT_OUT: "1" and no ANTHROPIC_API_KEY (keyless proof). Do NOT add Postgres/Redis services (golden fixtures are static JSON — RESEARCH Assumption A4).

    Add job `ai-redteam-injection` (name: "AI Prompt-Injection Red-Team (static)"): backend-shaped, no continue-on-error, step runs `pytest tests/test_ai_injection_redteam.py tests/test_ai_budget_coverage.py -v`. The budget-coverage test uses the `client` fixture + DB — include the SAME Postgres/Redis service block + env the existing `backend` job uses so those fixtures work (copy the backend job's services + DB/Redis env verbatim). No ANTHROPIC_API_KEY.

    Add job `ai-live-eval-optin` (name: "AI Live Eval + Red-Team (opt-in, non-blocking)"): needs: [backend], continue-on-error: true, `if: ${{ github.event.repository.fork == false }}`, env HAS_DEV_KEY: ${{ secrets.DEV_ANTHROPIC_API_KEY != '' }}. First step echoes a skip + exits 0 when env.HAS_DEV_KEY != 'true'; the remaining steps (setup-python/install/deepeval LLM-judge suite with ANTHROPIC_API_KEY + DEEPEVAL_TELEMETRY_OPT_OUT=1; setup-node + `npx promptfoo@0.121.20 redteam run ...`) are each `if: env.HAS_DEV_KEY == 'true'`. This job MUST NOT be added to branch-protection (non-blocking by design). (The LLM-judge suite file and promptfoo config are opt-in-tier only; if they don't exist yet the job no-ops via the skip step — do not create them here unless trivially scaffolding an empty config, and never make them blocking.)

    In .github/branch-protection.json, append to required_status_checks.checks[]: {"context": "AI Golden-Set Evals (DeepEval)", "app_id": -1} and {"context": "AI Prompt-Injection Red-Team (static)", "app_id": -1}. The context strings MUST match the ci.yml `name:` values byte-for-byte. Do NOT add the opt-in job.
  </action>
  <acceptance_criteria>
    - ci.yml is valid YAML and defines all 3 job keys (ai-evals, ai-redteam-injection, ai-live-eval-optin).
    - ai-evals step uses `deepeval test run tests/evals/test_golden_evals.py` (NOT bare pytest) with DEEPEVAL_TELEMETRY_OPT_OUT: "1"; neither blocking job references ANTHROPIC_API_KEY.
    - ai-redteam-injection runs BOTH tests/test_ai_injection_redteam.py AND tests/test_ai_budget_coverage.py, with the Postgres/Redis service block present (matches the backend job).
    - Neither ai-evals nor ai-redteam-injection has continue-on-error; ai-live-eval-optin HAS continue-on-error: true AND `github.event.repository.fork == false` AND the HAS_DEV_KEY env-indirection gate.
    - branch-protection.json is valid JSON and its checks[] contains exactly the 2 new context strings matching the job names byte-for-byte; the opt-in job name is absent from checks[].
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/chemencedji/Desktop/getvul && python -c "import yaml,json; y=yaml.safe_load(open('.github/workflows/ci.yml')); jobs=y['jobs']; assert {'ai-evals','ai-redteam-injection','ai-live-eval-optin'} <= set(jobs), set(jobs); assert 'deepeval test run' in yaml.dump(jobs['ai-evals']); assert jobs['ai-evals'].get('steps'); assert 'continue-on-error' not in yaml.dump(jobs['ai-evals']); assert jobs['ai-live-eval-optin'].get('continue-on-error') is True; bp=json.load(open('.github/branch-protection.json')); ctx=[c['context'] for c in bp['required_status_checks']['checks']]; assert 'AI Golden-Set Evals (DeepEval)' in ctx and 'AI Prompt-Injection Red-Team (static)' in ctx, ctx; assert not any('opt-in' in c for c in ctx); print('CI wiring OK:', ctx)"</automated>
  </verify>
  <done>3 jobs added (2 blocking keyless + 1 opt-in non-blocking); both blocking job names registered in branch-protection.json; opt-in tier fork-guarded + secret-gated + never required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fork-originated PR → CI secrets | untrusted PRs must never see the dev Anthropic key |
| CI job → merge gate | a job must be a REQUIRED check to actually block merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06 | Information Disclosure | ai-live-eval-optin secret handling | mitigate | `if: github.event.repository.fork == false` + HAS_DEV_KEY env-indirection gate (Pitfall 4); secret only referenced in fork==false, key-present steps; never logged |
| T-28-11 | Tampering (gate evasion) | CI-blocking enforcement | mitigate | Job names added to branch-protection.json required checks (byte-for-byte) so a regression genuinely blocks merge — not merely visible-red (the `docs`-not-required proof) |
| T-28-12 | Denial of Service (CI cost) | keyless blocking jobs | accept | ai-evals needs no DB/Redis (static JSON); ai-redteam-injection reuses the backend service block — bounded, deterministic, no external egress |
</threat_model>

## Artifacts this phase produces (Plan 05)
- `.github/workflows/ci.yml`: `ai-evals` + `ai-redteam-injection` (blocking, keyless) + `ai-live-eval-optin` (non-blocking, key-gated, fork-guarded).
- `.github/branch-protection.json`: 2 new required checks making the keyless gates merge-blocking.

<verification>
- ci.yml valid; 3 jobs; keyless blocking jobs have no ANTHROPIC_API_KEY and no continue-on-error.
- deepeval invoked via `deepeval test run` with telemetry opt-out; red-team job runs injection + coverage suites.
- branch-protection.json valid; 2 new required checks match job names; opt-in job not required.
</verification>

<success_criteria>
The AIE-01/02/03 keyless gates are genuinely merge-blocking in CI; the live LLM-judge + real promptfoo tier is opt-in, non-blocking, and safe against fork PRs. SC1/SC2/SC3 enforced at the CI boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/28-eval-cost-observability-gate/28-05-SUMMARY.md`. Note the final job names, the branch-protection additions, and whether the opt-in LLM-judge/promptfoo config files were scaffolded or left as skip-no-op.
</output>
