---
phase: 41-coverage-blind-spot-detection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/connectors/intune_sync.py
  - backend/tests/test_intune_sync.py
autonomous: true
requirements: [COV-01]
reversibility: reversible (contained defect fix in one sync file; independently revertable; touches no coverage code and no schema)
planner_scoping_note: >
  41-RESEARCH.md Open Question 6 / Pitfall 1 flagged this as a planner scoping fork ("raise
  explicitly, either choice defensible"). DECISION: INCLUDE in Phase 41. Rationale: (1) D-01 (LOCKED)
  names INTUNE as one of exactly three authoritative sources — the blind-spot baseline is silently
  empty for any Intune-only tenant until this is fixed, so leaving it broken would make COV-01
  untruthful for a whole class of tenants; (2) the omitted tenant_id on the Asset lookups/constructor
  is a latent cross-tenant matching bug (a security defect), not merely cosmetic; (3) it is small,
  contained, independently testable, and shares zero files with the coverage module (safe Wave-1
  parallel). This does NOT expand the phase's capability surface beyond COV-01..03 — it makes an
  existing ingestion path actually deliver the data D-01 depends on. Reversible; no schema change.

must_haves:
  truths:
    - "run_intune_sync constructs a valid SyncLog and can persist Intune-discovered devices to the assets table (Pitfall 1 defect resolved) so an Intune-configured tenant's assets carry INTUNE in seen_by_sources and become part of the D-01 authoritative baseline"
    - "The Intune SyncLog is constructed with connector_id (not the non-existent connector_config_id) and tenant_id, with an uppercase status recognized by _normalize_sync_status (RUNNING/SUCCESS/FAILED)"
    - "Every Asset lookup and the Asset(...) constructor in run_intune_sync is tenant-scoped (Asset.tenant_id == connector_config.tenant_id) so Intune can never match or create an asset in another tenant"
  artifacts:
    - path: "backend/app/connectors/intune_sync.py"
      provides: "corrected SyncLog construction + tenant-scoped asset upsert"
      contains: "connector_id=connector_config.id"
    - path: "backend/tests/test_intune_sync.py"
      provides: "integration test asserting a SyncLog row is created with status SUCCESS + tenant_id"
      contains: "run_intune_sync"
  key_links:
    - from: "backend/app/connectors/intune_sync.py"
      to: "backend/app/ticketing/models.py::SyncLog"
      via: "SyncLog(connector_id=..., tenant_id=..., status='RUNNING')"
      pattern: "SyncLog\\("
  prohibitions:
    - "Never fix only the SyncLog kwargs while leaving the Asset lookups/constructor un-tenant-scoped — that is the cross-tenant matching bug (fix both together)"
    - "Never write a lowercase sync status — _normalize_sync_status only maps SUCCESS/FAILED/SYNCING; anything else degrades SyncStatusPill to 'Never synced'"
    - "Never change the SyncLog model or add a migration — the model is correct; only the caller is wrong"
    - "Never broaden this plan into a general Intune-connector feature — scope is exactly the Pitfall-1 defect fix"
---

<objective>
Fix the verified `run_intune_sync` defect (41-RESEARCH.md Pitfall 1) so Intune-discovered devices actually land in the `assets` table with `INTUNE` in `seen_by_sources`. Without this, D-01's authoritative baseline is silently empty for any Intune-only tenant and COV-01's Coverage view shows the D-11 "no inventory" state despite a correctly-configured connector.

Purpose: Make the D-01 authoritative baseline truthful end-to-end for all three enrichment sources, and close a latent cross-tenant asset-matching bug in the same edit.

Output: A corrected `intune_sync.py` and an integration test proving a `SyncLog` row is created with `status="SUCCESS"` and the right `tenant_id`.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-coverage-blind-spot-detection/41-RESEARCH.md
@.planning/phases/41-coverage-blind-spot-detection/41-PATTERNS.md

<interfaces>
SyncLog (backend/app/ticketing/models.py:60-76) — the ground-truth fields:
  connector_id: UUID (FK connector_configs.id, NOT NULL)   ← the correct field name
  tenant_id: UUID (NOT NULL, indexed)                        ← currently omitted (bug)
  status: str(20) (NOT NULL)                                 ← must be uppercase enum value
  started_at, completed_at, records_fetched, records_created, records_updated
  (there is NO connector_config_id field — the current code's TypeError source)

SyncStatus enum (ticketing/models.py:32-36) + _normalize_sync_status map (connectors/service.py:33-44):
  recognizes only "SUCCESS" / "FAILED" / "SYNCING" (→ 'ok'/'failed'/'syncing'); else falls through.

Correct construction pattern (every sibling: jamf_sync.py / humaans_sync.py / connectors/sync.py):
  SyncLog(connector_id=connector_config.id, tenant_id=connector_config.tenant_id, status="RUNNING", started_at=...)
  select(Asset).where(Asset.tenant_id == connector_config.tenant_id, Asset.hostname == device_name)
  select(Asset).where(Asset.tenant_id == connector_config.tenant_id, Asset.serial_number == serial)
  Asset(tenant_id=connector_config.tenant_id, hostname=device_name, ...)
  on completion: sync_log.status = "SUCCESS"  (error path: "FAILED")
</interfaces>
</context>

<artifacts_produced>
Modified: `backend/app/connectors/intune_sync.py` (SyncLog construction + tenant-scoped Asset upsert + uppercase statuses)
New test: integration test in `backend/tests/test_intune_sync.py` for `run_intune_sync`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Correct SyncLog construction + tenant-scope the Asset upsert in run_intune_sync</name>
  <files>backend/app/connectors/intune_sync.py</files>
  <read_first>
    - backend/app/connectors/intune_sync.py (the full file — the defect is lines ~122-129 SyncLog, ~153/158 Asset lookups, ~165 Asset constructor, ~170/181 status assignments)
    - backend/app/connectors/jamf_sync.py (the correct sibling SyncLog + tenant-scoped Asset pattern to mirror)
    - backend/app/ticketing/models.py lines 32-76 (SyncStatus enum + SyncLog model fields)
    - backend/app/connectors/service.py lines 29-44 (_normalize_sync_status map)
    - 41-PATTERNS.md §backend/app/connectors/intune_sync.py (the exact fix shape)
  </read_first>
  <action>
    In `run_intune_sync`, replace the `SyncLog(connector_config_id=connector_config.id, status="running", ...)` construction with `SyncLog(connector_id=connector_config.id, tenant_id=connector_config.tenant_id, status="RUNNING", started_at=datetime.now(UTC), records_fetched=0, records_created=0, records_updated=0)`.
    Add `Asset.tenant_id == connector_config.tenant_id` to BOTH `select(Asset).where(...)` lookups (the hostname lookup and the serial_number lookup) as the first condition.
    Add `tenant_id=connector_config.tenant_id` to the `Asset(hostname=device_name, ...)` constructor for newly-created rows.
    Change the completion status assignment from `"success"` to `"SUCCESS"` and the error-path assignment from `"error"`/`"failed"` to `"FAILED"` to match the SyncStatus enum and the _normalize_sync_status map.
    Do NOT alter the SyncLog model, add a migration, or change any other file.
  </action>
  <verify>
    <automated>cd backend && python -c "import ast,sys; src=open('app/connectors/intune_sync.py').read(); assert 'connector_config_id' not in src, 'stale bad field'; assert 'connector_id=connector_config.id' in src; assert 'tenant_id=connector_config.tenant_id' in src; assert 'status=\"RUNNING\"' in src; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "connector_config_id" backend/app/connectors/intune_sync.py` returns 0.
    - `grep -q "connector_id=connector_config.id" backend/app/connectors/intune_sync.py` and `grep -q "tenant_id=connector_config.tenant_id" backend/app/connectors/intune_sync.py`.
    - Both `select(Asset)` lookups include `Asset.tenant_id == connector_config.tenant_id`.
    - No lowercase `"running"`/`"success"`/`"error"` status literals remain on the SyncLog in this file (grep for them returns only uppercase forms).
  </acceptance_criteria>
  <done>run_intune_sync constructs a valid, tenant-scoped SyncLog and upserts tenant-scoped assets with uppercase statuses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test — run_intune_sync persists a SyncLog + asset</name>
  <files>backend/tests/test_intune_sync.py</files>
  <read_first>
    - backend/tests/test_intune_sync.py (existing pure-helper tests + docstring noting run_intune_sync had no DB coverage)
    - backend/tests/conftest.py (db_session, tenant fixtures, any connector-config factory)
    - backend/tests/test_jamf_sync.py or test_humaans_sync.py if present (closest DB-integration sync-test pattern to mirror; pick whichever exists)
  </read_first>
  <behavior>
    - Given an enabled Intune ConnectorConfig for tenant A and a stubbed/mocked managed-devices fetch returning one device, run_intune_sync creates exactly one SyncLog row with connector_id set, tenant_id == tenant A, and status == "SUCCESS".
    - The discovered device is persisted as an Asset in tenant A with "INTUNE" in seen_by_sources (or is enriched onto an existing tenant-A asset), and never in any other tenant.
  </behavior>
  <action>
    Extend `backend/tests/test_intune_sync.py` with an async integration test for `run_intune_sync`, mocking the Graph/`_fetch_managed_devices` fetch layer (mirror the sibling sync test's mocking approach) so no real network call occurs. Assert: one SyncLog row exists for the connector with `status == "SUCCESS"` and correct `tenant_id`; the resulting Asset carries `"INTUNE"` in `seen_by_sources` and `tenant_id` == the connector's tenant. Keep the existing pure-helper tests untouched.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret python -m pytest tests/test_intune_sync.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The new test asserts `SyncLog.status == "SUCCESS"` and `SyncLog.tenant_id` equals the connector's tenant.
    - The new test asserts the persisted Asset has `"INTUNE" in seen_by_sources` and the correct tenant_id.
    - Full `tests/test_intune_sync.py` passes (existing pure-helper tests still green).
  </acceptance_criteria>
  <done>An integration test proves run_intune_sync now writes a valid SyncLog and a tenant-scoped INTUNE-tagged asset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Intune Graph API → sync worker → Postgres | Devices from an external tenant-scoped connector become asset rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-05 | Tampering / Information Disclosure | run_intune_sync Asset lookup/create | mitigate | tenant_id in both Asset selects and the Asset constructor — closes the latent cross-tenant matching bug (a device from connector X's tenant can never bind to another tenant's asset row) |
| T-41-06 | Repudiation | SyncLog persistence | mitigate | corrected SyncLog now actually records the sync attempt/outcome per tenant (previously never written due to the TypeError) |
</threat_model>

<verification>
- `pytest tests/test_intune_sync.py -x` green with the env-var prelude.
- `grep -c connector_config_id backend/app/connectors/intune_sync.py` == 0.
</verification>

<success_criteria>
- An Intune-configured tenant's devices reach `seen_by_sources` with `INTUNE`, making the D-01 baseline truthful for Intune-only tenants.
- The cross-tenant asset-matching bug in the Intune upsert path is closed.
- No schema change, no migration, no coverage-module coupling.
</success_criteria>

<output>
After completion, create `.planning/phases/41-coverage-blind-spot-detection/41-02-SUMMARY.md`.
</output>
