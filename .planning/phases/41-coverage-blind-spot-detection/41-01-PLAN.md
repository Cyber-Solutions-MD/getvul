---
phase: 41-coverage-blind-spot-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/coverage/__init__.py
  - backend/app/coverage/router.py
  - backend/app/coverage/service.py
  - backend/app/coverage/schemas.py
  - backend/app/main.py
  - backend/tests/test_coverage.py
  - frontend/src/app/(authed)/dashboard/coverage/page.tsx
  - frontend/src/app/(authed)/dashboard/coverage/page.test.tsx
  - frontend/src/lib/queries/use-blind-spot-assets.ts
  - frontend/src/lib/queries/keys.ts
  - frontend/src/components/coverage/microcopy.ts
  - frontend/src/components/shell/nav-items.ts
  - frontend/e2e/routes.ts
autonomous: true
requirements: [COV-01]
reversibility: reversible (pure read-side service + new route; no schema/table/column change per D-10; can be deleted or materialized later without migration)

must_haves:
  truths:
    - "A viewer can open /dashboard/coverage from the sidebar and see the list of authoritative-inventory assets that no scanner has ever touched (COV-01, D-01/D-02)"
    - "The blind-spot list includes an asset iff its seen_by_sources contains at least one ENRICHMENT source (JAMF/HUMAANS/INTUNE) AND contains no SCANNER source (the 6 VulnSource values) — authoritative AND never-scanned (D-01/D-02)"
    - "An asset seen by BOTH an enrichment source and a scanner is NOT a blind spot — provenance is already collapsed onto one asset row per host at sync time, so coverage is intra-row set logic over seen_by_sources, never a cross-table join or a runtime dedupe (probe COV-01 adjacency)"
    - "When the tenant has zero authoritative assets, the page shows the D-11 no-inventory EmptyState ('No inventory source connected' → link to /connectors) driven by has_authoritative_inventory=false, never a misleading 0%/100% or a total-assets fallback (probe COV-01 empty)"
    - "When the tenant has authoritative assets but zero blind spots, the page shows the 'Every device is covered' quiet-win EmptyState with no CTA (probe COV-01 empty)"
    - "The blind-spot list has a deterministic, stable default order (hostname ascending, asset id ascending as tiebreak) so pagination is repeatable across requests (probe COV-01 ordering)"
    - "Ignored assets (is_ignored=true) are excluded from the blind-spot list, mirroring the /assets list default"
    - "{ statement: 'Blind-spot list reuses the /assets page/page_size pagination envelope (AssetsResponse shape: {items,total,page,page_size,pages}); a tenant with a very large blind-spot count paginates rather than rendering all rows client-side', verification: backstop }"
  artifacts:
    - path: "backend/app/coverage/service.py"
      provides: "list_blind_spot_assets(db, tenant_id, page, page_size) reconciliation query (D-01/D-02) + has_authoritative_inventory signal"
      contains: "def list_blind_spot_assets"
    - path: "backend/app/coverage/router.py"
      provides: "GET /blind-spots (require_viewer) + _get_asset_or_404 tenant-scoped helper"
      contains: "require_viewer"
    - path: "backend/app/coverage/schemas.py"
      provides: "BlindSpotAssetResponse + BlindSpotAssetListResponse (with has_authoritative_inventory)"
      contains: "has_authoritative_inventory"
    - path: "frontend/src/app/(authed)/dashboard/coverage/page.tsx"
      provides: "Coverage page: blind-spot list with loading/error/populated + no-inventory + all-covered empty branches"
      min_lines: 60
    - path: "frontend/src/lib/queries/use-blind-spot-assets.ts"
      provides: "useBlindSpotAssets({page}) useQuery hook"
      contains: "queryKeys.coverage.blindSpots"
    - path: "frontend/src/components/shell/nav-items.ts"
      provides: "Coverage nav entry (Radar icon) in WORKFLOW_ITEMS"
      contains: "/dashboard/coverage"
  key_links:
    - from: "frontend/src/app/(authed)/dashboard/coverage/page.tsx"
      to: "/api/v1/coverage/blind-spots"
      via: "useBlindSpotAssets -> api() fetch"
      pattern: "coverage/blind-spots"
    - from: "backend/app/coverage/service.py"
      to: "app.assets.constants.SCANNER_SOURCES/ENRICHMENT_SOURCES"
      via: "import + .contains() filter (never re-derive the 9 literals)"
      pattern: "from app.assets.constants import"
    - from: "backend/app/main.py"
      to: "app.coverage.router"
      via: "include_router prefix=/api/v1/coverage"
      pattern: "coverage_router"
  prohibitions:
    - "Never build a second scanner-inventory-vs-authoritative-inventory join or a runtime hostname/serial matcher — reconciliation is a .contains() filter over the single Asset.seen_by_sources column"
    - "Never drop tenant_id scoping from the blind-spot query or the by-id lookup (every WHERE clause carries Asset.tenant_id == tenant_id)"
    - "Never return 403 or 500 for a cross-tenant asset id on the by-id helper — return 404 (IDOR: existence stays private)"
    - "Never fall back to a total-assets denominator or imply full coverage when authoritative inventory is unknown (D-11)"
    - "Never place the page at /coverage — it must live under (authed)/dashboard/coverage or it loses the auth guard + shell (Pitfall 2)"
    - "Never re-derive or re-compute the v4.0 risk-exposure score or scanner findings here — consume seen_by_sources only"
    - "Never add a new table, column, or Alembic migration for coverage (D-10 compute-on-read)"
---

<objective>
Ship the Phase 41 TRACER slice: the end-to-end blind-spot list (COV-01). An analyst opens a new "Coverage" sidebar page and sees exactly the authoritative-inventory devices (MDM+HR, per D-01) that no scanner has ever touched (D-02) — proving the whole browser → route → API → Postgres → render stack before any COV-02/COV-03 expansion.

Purpose: Establish the new `backend/app/coverage/` module and the `/dashboard/coverage` route as a single verified vertical slice. Every later plan (COV-02 strip, COV-03 route-to-owner) extends these files rather than inventing them.

Output: New backend coverage module (read side), a GET /blind-spots endpoint, the Coverage page rendering all its empty/loading/error/populated states, the sidebar nav entry, and the e2e route registration.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-coverage-blind-spot-detection/41-CONTEXT.md
@.planning/phases/41-coverage-blind-spot-detection/41-RESEARCH.md
@.planning/phases/41-coverage-blind-spot-detection/41-PATTERNS.md
@.planning/phases/41-coverage-blind-spot-detection/41-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs — from 41-PATTERNS.md/41-RESEARCH.md. Use directly, no codebase spelunking. -->

Source partition (backend/app/assets/constants.py — import, never re-derive):
  SCANNER_SOURCES: frozenset = {"CROWDSTRIKE","NESSUS","DEFENDER","WIZ","QUALYS","RAPID7"}  (== VulnSource values)
  ENRICHMENT_SOURCES: frozenset = {"JAMF","HUMAANS","INTUNE"}

Asset (backend/app/assets/models.py): id: UUID; tenant_id: UUID; hostname: str;
  seen_by_sources: JSONB list[str]; is_ignored: bool; last_seen_at: datetime|None;
  category: str|None; os: str|None; serial_number: str|None; assigned_user: str|None.
  Asset.seen_by_sources.contains([X]) is the GIN-indexed idiom (alembic 045).

RBAC (backend/app/auth/rbac.py — NOT app.auth.dependencies.require_role, Pitfall 4):
  require_viewer, require_analyst  (FastAPI deps yielding CurrentUser; CurrentUser has .tenant_id, .id, .email)

Reconciliation composition (D-01/D-02):
  authoritative = or_(*[Asset.seen_by_sources.contains([e]) for e in ENRICHMENT_SOURCES])
  never_scanned = not_(or_(*[Asset.seen_by_sources.contains([s]) for s in SCANNER_SOURCES]))

Existing pagination envelope to mirror verbatim (AssetsResponse, backend/app/assets):
  { items: [...], total: int, page: int, page_size: int, pages: int }

Router analog (backend/app/exceptions/router.py): module docstring naming COV IDs;
  _get_exception_or_404 tenant-scoped helper (copy shape → _get_asset_or_404, message "Asset not found");
  main.py registration mirrors: app.include_router(coverage_router, prefix="/api/v1/coverage", tags=["Coverage"]).

Frontend primitives (all already shipped, compose verbatim):
  EmptyState compound (components/states/empty-state.tsx): EmptyState.Title/.Body/.Actions
  PartialFailureBanner (components/states): errors=[{code,requestId}] onRetry
  SkeletonTable (components/states): columns, rows
  useAuth (@/lib/auth): user?.role in 'OWNER'|'ADMIN'|'ANALYST'|'VIEWER'
  api (@/lib/api): api<T>(path, {signal}) fetch wrapper
  queryKeys (frontend/src/lib/queries/keys.ts) per-domain nesting convention
  nav-items.ts WORKFLOW_ITEMS entry shape: { label, href, icon }; import Radar from 'lucide-react'

State-branch ORDER (WR-13, from assets/page.tsx): check error FIRST, then loading, then
  empty-variant branches, then populated — never rely on items.length alone (defaults to [] on error).
</interfaces>
</context>

<artifacts_produced>
New endpoints: `GET /api/v1/coverage/blind-spots?page=&page_size=`
New route: `/dashboard/coverage`
New backend module: `app/coverage/{__init__,router,service,schemas}.py`
New service fn: `list_blind_spot_assets(db, tenant_id, page, page_size)`; router helper `_get_asset_or_404`
New Pydantic schemas: `BlindSpotAssetResponse`, `BlindSpotAssetListResponse` (adds `has_authoritative_inventory: bool`)
New frontend page: `coverage/page.tsx` (+ `page.test.tsx`); hook `use-blind-spot-assets.ts`; `microcopy.ts`
Modified: `main.py` (include_router), `keys.ts` (full `coverage` key group), `nav-items.ts` (Radar entry), `e2e/routes.ts` (`/dashboard/coverage`)
</artifacts_produced>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Backend coverage module + GET /blind-spots reconciliation (COV-01)</name>
  <files>backend/app/coverage/__init__.py, backend/app/coverage/router.py, backend/app/coverage/service.py, backend/app/coverage/schemas.py, backend/app/main.py, backend/tests/test_coverage.py</files>
  <read_first>
    - backend/app/exceptions/router.py (router docstring, _get_exception_or_404, require_viewer GET, main.py registration shape)
    - backend/app/assets/router.py lines 82-170 (existing .contains() facet-filter idiom + is_ignored default + AssetsResponse envelope)
    - backend/app/assets/constants.py (SCANNER_SOURCES / ENRICHMENT_SOURCES — import these)
    - backend/app/assets/models.py (Asset fields)
    - backend/tests/test_exceptions.py lines 375-403 (cross-tenant 404 test) + backend/tests/conftest.py (client_factory, tenant fixtures)
    - 41-PATTERNS.md §backend/app/coverage/service.py Half A + §backend/app/coverage/router.py
  </read_first>
  <behavior>
    - test_blind_spot_list: an asset with seen_by_sources=["JAMF"] appears; ["JAMF","QUALYS"] does NOT; ["QUALYS"] does NOT (not authoritative); is_ignored=true JAMF asset does NOT.
    - test_blind_spot_empty_inventory: tenant with zero authoritative assets → items=[], has_authoritative_inventory=false.
    - test_blind_spot_all_covered: tenant with only authoritative+scanner assets → items=[], has_authoritative_inventory=true.
    - test_blind_spot_ordering: results ordered by hostname asc then id asc (stable across two identical requests).
    - test_blind_spot_cross_tenant_isolation: tenant B never sees tenant A's blind-spot assets.
  </behavior>
  <action>
    Create `backend/app/coverage/__init__.py` (bare package marker, mirror `app/exceptions/__init__.py`).
    Create `backend/app/coverage/schemas.py` with `BlindSpotAssetResponse` (model_config from_attributes=True; fields: id: uuid.UUID, hostname: str, category: str | None, os: str | None, last_seen_at: datetime | None, seen_by_sources: list[str]) and `BlindSpotAssetListResponse` (items: list[BlindSpotAssetResponse], total: int, page: int, page_size: int, pages: int, has_authoritative_inventory: bool).
    Create `backend/app/coverage/service.py`: import SCANNER_SOURCES, ENRICHMENT_SOURCES from app.assets.constants; define `authoritative = or_(*[Asset.seen_by_sources.contains([e]) for e in ENRICHMENT_SOURCES])` and `never_scanned = not_(or_(*[Asset.seen_by_sources.contains([s]) for s in SCANNER_SOURCES]))`. Implement `async def list_blind_spot_assets(db, tenant_id, page: int = 1, page_size: int = 50)` selecting Asset where tenant_id==tenant_id, Asset.is_ignored.is_(False), authoritative, never_scanned, ordered by Asset.hostname.asc(), Asset.id.asc(); paginate with limit/offset; also compute total via func.count and has_authoritative_inventory via a `SELECT EXISTS` over (tenant_id + authoritative + is_ignored False); return the shape matching BlindSpotAssetListResponse (pages = ceil(total/page_size)).
    Create `backend/app/coverage/router.py`: module docstring naming COV-01 and the tenant-scoped-404/IDOR intent; `from app.auth.rbac import require_viewer` (NEVER app.auth.dependencies.require_role — Pitfall 4); define `_get_asset_or_404(db, tenant_id, asset_id) -> Asset` (tenant_id IN the WHERE clause, 404 detail "Asset not found") for later plans; `@router.get("/blind-spots", response_model=BlindSpotAssetListResponse)` gated on require_viewer, delegating to list_blind_spot_assets with page/page_size query params (defaults page=1, page_size=50).
    Register in `backend/app/main.py`: `from app.coverage.router import router as coverage_router` and `app.include_router(coverage_router, prefix="/api/v1/coverage", tags=["Coverage"])`, placed alphabetically near the campaigns/exceptions include_router lines.
    Create `backend/tests/test_coverage.py` implementing the five behavior tests above using conftest's client_factory + tenant/user fixtures; seed assets by inserting Asset rows with explicit seen_by_sources lists. Include a cross-tenant isolation assertion for the list endpoint.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret python -m pytest tests/test_coverage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_coverage.py::test_blind_spot_list` passes: JAMF-only asset present, JAMF+QUALYS absent, QUALYS-only absent, ignored-JAMF absent.
    - `has_authoritative_inventory` is false for a zero-authoritative tenant and true for an all-covered tenant.
    - Two identical GET /blind-spots calls return items in identical (hostname asc, id asc) order.
    - `grep -q "from app.assets.constants import" backend/app/coverage/service.py` and the file does NOT re-declare the 9 source literals.
    - `grep -q "app.auth.rbac import" backend/app/coverage/router.py` and NO occurrence of `app.auth.dependencies` in that file.
    - `grep -q "coverage_router" backend/app/main.py`.
  </acceptance_criteria>
  <done>GET /api/v1/coverage/blind-spots returns tenant-scoped authoritative-never-scanned assets in stable order with has_authoritative_inventory, all five tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Coverage page (blind-spot list) + nav entry + query wiring (frontend tracer half)</name>
  <files>frontend/src/app/(authed)/dashboard/coverage/page.tsx, frontend/src/app/(authed)/dashboard/coverage/page.test.tsx, frontend/src/lib/queries/use-blind-spot-assets.ts, frontend/src/lib/queries/keys.ts, frontend/src/components/coverage/microcopy.ts, frontend/src/components/shell/nav-items.ts, frontend/e2e/routes.ts</files>
  <read_first>
    - frontend/src/app/(authed)/dashboard/tickets/page.tsx lines 340-409 (list + DrillPanel wiring precedent — NOT /assets which navigates full-page, Pitfall 8)
    - frontend/src/app/(authed)/dashboard/assets/page.tsx (state-branch order WR-13, SkeletonTable/EmptyState/PartialFailureBanner scaffolding, AssetsResponse envelope consumption)
    - frontend/src/app/(authed)/dashboard/assets/page.test.tsx (mock+renderWithClient branch-coverage convention)
    - frontend/src/lib/queries/use-assets.ts + use-exceptions.ts (useQuery shape, staleTime)
    - frontend/src/lib/queries/keys.ts lines 118-136 (per-domain key group)
    - frontend/src/components/shell/nav-items.ts (WORKFLOW_ITEMS entry shape)
    - frontend/e2e/routes.ts (STATIC_ROUTES)
    - 41-UI-SPEC.md §Copywriting Contract + §UI Considerations E2/E4/E5
    - .claude/skills/sketch-findings-getvul/references/state-patterns.md + copy-voice.md
  </read_first>
  <action>
    Add the `coverage` key group to `frontend/src/lib/queries/keys.ts`: `coverage: { all: ['coverage'] as const, summary: () => ['coverage','summary'] as const, blindSpots: (opts: { page: number }) => ['coverage','blind-spots', opts] as const }` (summary key added now so Plan 03 needs no re-touch).
    Create `frontend/src/components/coverage/microcopy.ts` (mirror components/assets/microcopy.ts) holding the UI-SPEC Copywriting Contract strings: page title "Coverage", subtitle template "{N} devices in inventory have never been touched by a scanner", no-inventory empty (title "No inventory source connected", body "Connect an inventory source (Jamf / Intune / Humaans) to detect coverage gaps.", action "Connect an inventory source" → /connectors), all-covered quiet-win empty (title "Every device is covered", body "All {N} devices in your inventory have been touched by at least one scanner. Nothing to route right now.", no CTA), blind-spot row badge "No scanner coverage", never-scanned column header "Never scanned".
    Create `frontend/src/lib/queries/use-blind-spot-assets.ts`: `useBlindSpotAssets({ page }: { page: number })` useQuery, queryKey queryKeys.coverage.blindSpots({page}), queryFn `({signal}) => api<BlindSpotAssetListResponse>(\`/api/v1/coverage/blind-spots?page=${page}&page_size=50\`, {signal})`, staleTime: 0 (D-10 compute-on-read), retry: 1. Declare the response TS type inline/exported to match the backend schema (items, total, page, page_size, pages, has_authoritative_inventory).
    Create `frontend/src/app/(authed)/dashboard/coverage/page.tsx` composing: page header (title + dynamic subtitle from total) and the blind-spot list section. State-branch order EXACTLY: error → PartialFailureBanner(onRetry=refetch); else loading → SkeletonTable; else `!has_authoritative_inventory` → no-inventory EmptyState (D-11, links to /connectors); else `total === 0` → all-covered quiet-win EmptyState; else → a blind-spot table (hostname mono, category, os, last-seen, "No scanner coverage" amber badge) with row rendering reusing the assets-table primitive shape, plus Pagination when pages > 1. Do NOT wire a DrillPanel or Route-to-owner action yet (Plan 03) — leave the row as read-only for this tracer. Amber badge chrome only (never red / never --color-severity-critical) per UI-SPEC Color.
    Add `{ label: 'Coverage', href: '/dashboard/coverage', icon: Radar }` after the Exceptions entry in WORKFLOW_ITEMS in `nav-items.ts`; add `Radar` to the lucide-react import line.
    Add `'/dashboard/coverage'` to `STATIC_ROUTES` in `frontend/e2e/routes.ts`.
    Create `frontend/src/app/(authed)/dashboard/coverage/page.test.tsx` mirroring assets/page.test.tsx: mock useBlindSpotAssets; one `it()` per branch — loading, error, no-inventory empty (has_authoritative_inventory=false), all-covered empty (has_authoritative_inventory=true,total=0), populated (asserts a rendered hostname + "No scanner coverage" badge).
  </action>
  <verify>
    <automated>cd frontend && npm test -- coverage/page && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `coverage/page.test.tsx` has distinct passing tests for loading, error, no-inventory-empty, all-covered-empty, and populated branches.
    - Error branch is evaluated before the loading and empty branches (grep the page: the `error` check precedes the `isLoading`/`total === 0` checks).
    - `grep -q "/dashboard/coverage" frontend/src/components/shell/nav-items.ts` and `grep -q "Radar" frontend/src/components/shell/nav-items.ts`.
    - `grep -q "/dashboard/coverage" frontend/e2e/routes.ts`.
    - No hardcoded hex colors in page.tsx / microcopy.ts (grep shows only `var(--color-*)` / token classNames); badge uses amber token, never severity-critical.
    - `npm run lint` passes clean.
  </acceptance_criteria>
  <done>Navigating to /dashboard/coverage renders the blind-spot list with all five states correctly branched; the sidebar shows a Radar "Coverage" entry; the e2e route array includes the new path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/v1/coverage | Authenticated analyst request; asset UUIDs and tenant scope cross here |
| API → Postgres | Reconciliation query must be tenant-bounded in the WHERE clause |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Information Disclosure | GET /blind-spots list query | mitigate | `Asset.tenant_id == user.tenant_id` in the WHERE clause of every select (list + count + exists); never fetch-then-filter |
| T-41-02 | Information Disclosure | _get_asset_or_404 (by-id, used by later plans) | mitigate | tenant_id IN the WHERE clause; cross-tenant id returns 404 not 403/500 (IDOR, mirrors exceptions T-39-01) |
| T-41-03 | Elevation of Privilege | GET endpoints | accept | read endpoints require_viewer (lowest authed role); no write in this plan — no analyst gate needed yet |
| T-41-04 | Tampering | route placement | mitigate | page lives under (authed)/dashboard so the existing auth guard + shell apply (Pitfall 2) |
</threat_model>

<verification>
- Backend: `pytest tests/test_coverage.py -x` green (5 tests) with the env-var prelude.
- Frontend: `npm test -- coverage/page` green (5 branch tests) + `npm run lint` clean.
- Manual smoke (optional): start the dev stack, log in as a viewer, click the "Coverage" sidebar entry, confirm the blind-spot list (or the correct empty state) renders.
</verification>

<success_criteria>
- COV-01 end-to-end: browser → GET /api/v1/coverage/blind-spots → Postgres reconciliation → rendered list, proven by passing backend + frontend tests.
- Zero new tables/columns/migrations; reconciliation is a `.contains()` filter over the existing GIN-indexed column.
- All three empty/loading/error state patterns present (project mandatory-states rule).
</success_criteria>

<output>
After completion, create `.planning/phases/41-coverage-blind-spot-detection/41-01-SUMMARY.md`.
</output>
