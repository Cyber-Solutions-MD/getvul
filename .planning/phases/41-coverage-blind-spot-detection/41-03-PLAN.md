---
phase: 41-coverage-blind-spot-detection
plan: 03
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - backend/app/coverage/service.py
  - backend/app/coverage/router.py
  - backend/app/coverage/schemas.py
  - backend/tests/test_coverage.py
  - frontend/src/app/(authed)/dashboard/coverage/page.tsx
  - frontend/src/components/coverage/coverage-connector-card.tsx
  - frontend/src/components/coverage/coverage-connector-card.test.tsx
  - frontend/src/lib/queries/use-coverage-summary.ts
  - frontend/src/components/coverage/microcopy.ts
autonomous: true
requirements: [COV-02]
reversibility: reversible (D-06 stale threshold is a single constant; per-connector coverage % is pure read-side compute; can be materialized later without migration per D-10)

must_haves:
  truths:
    - "The Coverage page shows a per-connector coverage strip above the blind-spot list: for each scanner connector, coverage% = (authoritative assets whose seen_by_sources includes that scanner) / (total authoritative assets), per D-05 (COV-02)"
    - "A connector whose ConnectorConfig.last_sync_at is older than 7 days is flagged stale with a 'stale · {N}d' amber pill (D-06)"
    - "The stale flag fires strictly for age > 7 days, NOT at exactly 7 days (D-06 boundary: now - last_sync_at > timedelta(days=7))"
    - "{ statement: 'coverage_pct is null (never 0 or 100) when the authoritative-asset denominator is zero, and the card renders a non-crashing state rather than dividing by zero', verification: backstop }"
    - "last_sync_status is wire-normalized through _normalize_sync_status ('ok'|'failed'|'syncing'|null) before reaching CoverageConnectorCard, so SyncStatusPill renders the true state instead of silently degrading to 'Never synced' (Pitfall 3)"
    - "Only enabled scanner connectors (ConnectorConfig.is_enabled == True, connector_type in SCANNER_SOURCES) appear as coverage cards"
    - "The coverage % headline uses the existing SLA 3-tier color family (≥90% success/green, 50-89% warning/amber, <50% danger/red) and JetBrains Mono tabular-nums, inventing no new palette (UI-SPEC Color/Typography)"
    - "{ statement: 'When ≥1 authoritative (MDM/HR) connector is configured but zero scanner connectors exist, the page shows a scanner-specific empty variant (heading ~\"No scanner connected\", link to /connectors) distinct from the D-11 no-inventory copy', verification: backstop }"
  artifacts:
    - path: "backend/app/coverage/service.py"
      provides: "get_coverage_summary(db, tenant_id) — per-connector coverage % + staleness + normalized status"
      contains: "def get_coverage_summary"
    - path: "backend/app/coverage/schemas.py"
      provides: "CoverageConnectorCardResponse + CoverageSummaryResponse (has_authoritative_inventory + has_scanner_connector)"
      contains: "coverage_pct"
    - path: "frontend/src/components/coverage/coverage-connector-card.tsx"
      provides: "per-connector card: % headline + SyncStatusPill + stale pill"
      min_lines: 30
    - path: "frontend/src/lib/queries/use-coverage-summary.ts"
      provides: "useCoverageSummary() useQuery hook (staleTime 0)"
      contains: "queryKeys.coverage.summary"
  key_links:
    - from: "frontend/src/app/(authed)/dashboard/coverage/page.tsx"
      to: "/api/v1/coverage/summary"
      via: "useCoverageSummary -> api() fetch, rendered in StatStrip above the list"
      pattern: "coverage/summary"
    - from: "backend/app/coverage/service.py"
      to: "app.connectors.service._normalize_sync_status"
      via: "import + apply per card (Pitfall 3)"
      pattern: "_normalize_sync_status"
  prohibitions:
    - "Never pass a raw uppercase last_sync_status to the frontend — always normalize (Pitfall 3)"
    - "Never emit coverage_pct as 0 or 100 when the denominator is zero — emit null (D-11 misleading-number guard)"
    - "Never special-case vendor names like Tenable/AWS Inspector — iterate ConnectorConfig where connector_type in SCANNER_SOURCES (the 6 real values only, Pitfall 6)"
    - "Never color a stale badge red — stale is amber ('hasn't reported'), distinct from SyncStatusPill's red 'failed'"
    - "Never re-derive the risk score or scanner findings — coverage is a count over seen_by_sources"
    - "Never add a new StatStrip/SyncStatusPill/ConnectorMark component — compose the existing primitives"
---

<objective>
Extend the Coverage page (from Plan 01) with the COV-02 per-connector coverage strip: for each enabled scanner connector, what fraction of the authoritative MDM+HR baseline it actually covers (D-05), plus a stale-source badge when it hasn't reported in >7 days (D-06). This is the "see the gap %" half that the blind-spot list ("see the assets behind it") sits beneath, per D-04's top-to-bottom read order.

Purpose: Deliver COV-02 as an extension of the already-verified tracer stack — a second read endpoint and a new card composition, no new subsystem.

Output: GET /summary endpoint + get_coverage_summary service, the CoverageConnectorCard, the useCoverageSummary hook, the strip wired above the blind-spot list, and the scanner-absent empty variant.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-coverage-blind-spot-detection/41-CONTEXT.md
@.planning/phases/41-coverage-blind-spot-detection/41-RESEARCH.md
@.planning/phases/41-coverage-blind-spot-detection/41-PATTERNS.md
@.planning/phases/41-coverage-blind-spot-detection/41-UI-SPEC.md
@.planning/phases/41-coverage-blind-spot-detection/41-01-SUMMARY.md

<interfaces>
Built in Plan 01 (extend, do not recreate): app/coverage/{router,service,schemas}.py;
  keys.ts already has coverage.summary(); coverage/page.tsx already renders the blind-spot list;
  coverage/microcopy.ts exists.

ConnectorConfig (backend/app/ticketing/models.py:39-56):
  tenant_id, connector_type: str(30), is_enabled: bool, last_sync_at: datetime|None,
  last_sync_status: str(20)|None, sync_interval_minutes: int, consecutive_failure_count: int

_normalize_sync_status (backend/app/connectors/service.py:33-44) — IMPORT, do not re-derive:
  {"SUCCESS":"ok","FAILED":"failed","SYNCING":"syncing"}.get(raw, raw); None → None

Coverage % + staleness recipe (41-RESEARCH.md Code Example B):
  authoritative_filter = or_(*[Asset.seen_by_sources.contains([e]) for e in ENRICHMENT_SOURCES])
  total = count(Asset where tenant_id + authoritative_filter)
  per connector: covered = count(Asset where tenant_id + authoritative_filter + contains([conn.connector_type]))
  coverage_pct = round(100*covered/total) if total else None      # D-11: null, never 0/100
  is_stale = bool(conn.last_sync_at and (now - conn.last_sync_at) > timedelta(days=7))   # D-06 strict >

Frontend primitives:
  StatStrip (components/ui/stat-strip.tsx): responsive grid, wrap children, no data logic
  ConnectorMark (components/connectors/connector-mark.tsx): provider prop; covers all 6 scanners
  SyncStatusPill (components/connectors/sync-status-pill.tsx): status 'ok'|'failed'|'syncing'|null
  Day-diff helper (components/exceptions/exceptions-table.tsx:92-97): Math.floor day math, no date lib

Coverage % 3-tier colors (UI-SPEC Color): ≥90% var(--color-success); 50-89% var(--color-warning)/--color-amber;
  <50% var(--color-danger). Stale pill: existing .stale-pill amber token. Mono tabular-nums for the % headline.
</interfaces>
</context>

<artifacts_produced>
New endpoint: `GET /api/v1/coverage/summary`
New service fn: `get_coverage_summary(db, tenant_id)`
New schemas: `CoverageConnectorCardResponse` (connector_type, coverage_pct: int|None, is_stale, stale_days: int|None, last_sync_status normalized, last_sync_at), `CoverageSummaryResponse` (cards: list, total_authoritative_assets, has_authoritative_inventory, has_scanner_connector)
New component: `coverage-connector-card.tsx` (+ test); hook `use-coverage-summary.ts`
Modified: `coverage/page.tsx` (strip above list + scanner-absent empty variant), `coverage/microcopy.ts` (scanner-absent copy)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /summary — per-connector coverage % + staleness (COV-02)</name>
  <files>backend/app/coverage/service.py, backend/app/coverage/router.py, backend/app/coverage/schemas.py, backend/tests/test_coverage.py</files>
  <read_first>
    - backend/app/coverage/service.py + router.py + schemas.py (Plan 01 output to extend)
    - backend/app/ticketing/models.py lines 39-56 (ConnectorConfig sync-health fields)
    - backend/app/connectors/service.py lines 29-44 (_normalize_sync_status — import it)
    - backend/app/assets/constants.py (SCANNER_SOURCES / ENRICHMENT_SOURCES)
    - 41-RESEARCH.md §Code Examples "Per-connector coverage %" + 41-PATTERNS.md §service.py Half A
    - backend/tests/test_coverage.py (Plan 01 tests to extend)
  </read_first>
  <behavior>
    - test_coverage_percentage: 4 authoritative assets, 3 of them include "QUALYS" → the QUALYS card coverage_pct == 75.
    - test_coverage_zero_denominator: tenant with zero authoritative assets → each card coverage_pct is None (never 0/100), no ZeroDivisionError.
    - test_stale_threshold_boundary: connector last_sync_at exactly 7 days ago → is_stale False; 7 days + 1 minute ago → is_stale True (strict >).
    - test_status_normalized: a connector with last_sync_status "SUCCESS" surfaces as "ok" in the card payload; None stays None.
    - test_summary_flags: has_scanner_connector reflects whether ≥1 enabled scanner connector exists; disabled connectors are excluded from cards.
  </behavior>
  <action>
    Add `CoverageConnectorCardResponse` and `CoverageSummaryResponse` to `backend/app/coverage/schemas.py`. Card fields: connector_type: str, coverage_pct: int | None, is_stale: bool, stale_days: int | None, last_sync_status: str | None (normalized), last_sync_at: datetime | None. Summary fields: cards: list[CoverageConnectorCardResponse], total_authoritative_assets: int, has_authoritative_inventory: bool, has_scanner_connector: bool.
    In `backend/app/coverage/service.py` add `async def get_coverage_summary(db, tenant_id)`: reuse the `authoritative` filter; compute total; select ConnectorConfig where tenant_id==tenant_id, is_enabled.is_(True), connector_type.in_(SCANNER_SOURCES); for each, count covered = authoritative ∧ contains([conn.connector_type]); coverage_pct = round(100*covered/total) if total else None; is_stale = bool(conn.last_sync_at and (now - conn.last_sync_at) > timedelta(days=7)); stale_days = day-diff when stale else None; last_sync_status via imported `_normalize_sync_status(conn.last_sync_status)`. Set has_authoritative_inventory = total > 0; has_scanner_connector = len(cards) > 0. Import `_normalize_sync_status` from `app.connectors.service` (do NOT re-derive the map, Pitfall 3).
    In `backend/app/coverage/router.py` add `@router.get("/summary", response_model=CoverageSummaryResponse)` gated on require_viewer, delegating to get_coverage_summary(db, user.tenant_id).
    Extend `backend/tests/test_coverage.py` with the five behavior tests above.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret python -m pytest tests/test_coverage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_coverage_percentage` asserts a 75 coverage_pct for the 3-of-4 case.
    - `test_coverage_zero_denominator` asserts coverage_pct is None (not 0, not 100) and no exception.
    - `test_stale_threshold_boundary` asserts is_stale is False at exactly 7d and True just past 7d.
    - `test_status_normalized` asserts "SUCCESS" → "ok" in the card payload.
    - `grep -q "_normalize_sync_status" backend/app/coverage/service.py` with an import from `app.connectors.service`.
    - Disabled connectors produce no card (asserted in test_summary_flags).
  </acceptance_criteria>
  <done>GET /api/v1/coverage/summary returns per-connector coverage %, staleness (strict >7d), and wire-normalized status; all COV-02 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: CoverageConnectorCard + strip on the page + scanner-absent empty variant</name>
  <files>frontend/src/lib/queries/use-coverage-summary.ts, frontend/src/components/coverage/coverage-connector-card.tsx, frontend/src/components/coverage/coverage-connector-card.test.tsx, frontend/src/app/(authed)/dashboard/coverage/page.tsx, frontend/src/components/coverage/microcopy.ts</files>
  <read_first>
    - frontend/src/components/connectors/connector-card.tsx (ConnectorMark + SyncStatusPill composition to adapt)
    - frontend/src/components/ui/stat-strip.tsx (responsive grid container)
    - frontend/src/components/connectors/connector-card.test.tsx (test convention to mirror)
    - frontend/src/components/exceptions/exceptions-table.tsx lines 92-97 (day-diff helper, no date lib)
    - frontend/src/app/(authed)/dashboard/coverage/page.tsx + coverage/microcopy.ts (Plan 01 output to extend)
    - frontend/src/lib/queries/use-exceptions.ts (no-arg useQuery shape) + keys.ts (coverage.summary already present)
    - 41-UI-SPEC.md §Color (3-tier %) §Typography §Copywriting Contract §UI Considerations E1/E4
    - .claude/skills/sketch-findings-getvul/references/visual-language.md + copy-voice.md
  </read_first>
  <action>
    Create `frontend/src/lib/queries/use-coverage-summary.ts`: `useCoverageSummary()` useQuery, queryKey queryKeys.coverage.summary(), queryFn `({signal}) => api<CoverageSummaryResponse>('/api/v1/coverage/summary', {signal})`, staleTime: 0, retry: 1. Export the response TS type matching the backend schema.
    Create `frontend/src/components/coverage/coverage-connector-card.tsx`: props = one CoverageConnectorCardResponse card. Compose ConnectorMark(provider=connector_type lowercased) + connector display label + SyncStatusPill(status=card.last_sync_status). Render the coverage-% headline in 40px Display JetBrains Mono tabular-nums with the 3-tier color class (≥90% success, 50-89% warning/amber, <50% danger); when coverage_pct is null render an em-dash "—" (never 0%). When is_stale, render a `stale · {stale_days}d` amber pill (reuse .stale-pill token; never red). Use only var(--color-*) tokens / existing classNames — no freehand hex.
    Add the `scannerAbsent` empty-state copy to `coverage/microcopy.ts` (title "No scanner connected", body "You have inventory sources connected, but no vulnerability scanner. Connect one to measure coverage.", action "Connect a scanner" → /connectors) — E4 backstop, confirm wording in review.
    Wire into `coverage/page.tsx`: call useCoverageSummary; render the strip ABOVE the blind-spot list as `<StatStrip>` wrapping one CoverageConnectorCard per summary.cards entry, with an xl gap between strip and list (UI-SPEC Spacing). Skeleton cards while summary loads; PartialFailureBanner on summary error. Add the third empty branch: when `has_authoritative_inventory && !has_scanner_connector`, show the scanner-absent EmptyState variant. Preserve the WR-13 branch order (error → loading → empty variants → populated).
    Create `frontend/src/components/coverage/coverage-connector-card.test.tsx`: assert the 3-tier color class switches at the ≥90 / 50-89 / <50 boundaries; assert coverage_pct=null renders "—" not "0%"; assert a card given last_sync_status="ok" renders SyncStatusPill in its ok state (Pitfall 3 regression guard — the payload it receives is already normalized); assert the stale pill shows `stale · {N}d` and is not red when is_stale.
  </action>
  <verify>
    <automated>cd frontend && npm test -- coverage && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `coverage-connector-card.test.tsx` asserts the color-tier boundaries (≥90/50-89/<50) and the null→"—" rendering.
    - Stale pill renders `stale · {N}d` with an amber (not danger/severity-critical) token.
    - page.tsx renders the StatStrip of CoverageConnectorCards above the blind-spot list, and adds the scanner-absent empty branch.
    - `grep -q "coverage/summary" frontend/src/lib/queries/use-coverage-summary.ts`.
    - No freehand hex in the new card/microcopy (grep shows only tokens/classNames).
    - `npm run lint` clean.
  </acceptance_criteria>
  <done>The Coverage page shows the per-connector coverage strip above the blind-spot list, with correct % tiers, stale badges, normalized sync status, and a scanner-absent empty variant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/v1/coverage/summary | Authenticated read of tenant connector + asset aggregates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07 | Information Disclosure | get_coverage_summary connector + asset counts | mitigate | tenant_id in every ConnectorConfig and Asset WHERE clause; a tenant sees only its own connectors and coverage numbers |
| T-41-08 | Elevation of Privilege | GET /summary | accept | require_viewer only (read); no write introduced here |
| T-41-09 | Tampering (data integrity of the displayed metric) | last_sync_status wire mapping | mitigate | normalize via _normalize_sync_status so the pill reflects true state rather than silently degrading (Pitfall 3) |
</threat_model>

<verification>
- Backend: `pytest tests/test_coverage.py -x` green (Plan 01 + Plan 03 tests) with the env-var prelude.
- Frontend: `npm test -- coverage` green + `npm run lint` clean.
</verification>

<success_criteria>
- COV-02 delivered: per-connector coverage % (D-05), stale-source badges (D-06, strict >7d), null-safe denominator, normalized status.
- The two read-together halves (strip + list, D-04) render top-to-bottom on one page.
- Zero new palette, zero new primitives, zero schema change.
</success_criteria>

<output>
After completion, create `.planning/phases/41-coverage-blind-spot-detection/41-03-SUMMARY.md`.
</output>
