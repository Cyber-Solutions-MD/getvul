---
phase: 41-coverage-blind-spot-detection
plan: 05
type: execute
wave: 4
depends_on: [41-03, 41-04]
files_modified:
  - frontend/src/app/(authed)/dashboard/coverage/page.tsx
  - frontend/src/components/coverage/coverage-asset-drill-content.tsx
  - frontend/src/components/coverage/route-to-owner-dialog.tsx
  - frontend/src/components/coverage/route-to-owner-dialog.test.tsx
  - frontend/src/lib/queries/use-route-to-owner.ts
  - frontend/src/lib/queries/use-route-to-owner.test.tsx
  - frontend/src/components/coverage/microcopy.ts
  - frontend/src/app/(authed)/dashboard/coverage/page.test.tsx
autonomous: true
requirements: [COV-03]
reversibility: reversible (pure frontend composition of existing primitives + one mutation hook; no schema/contract change beyond consuming the Plan 04 endpoint)

must_haves:
  truths:
    - "From a blind-spot row (and from the asset DrillPanel), an analyst can trigger 'Route to owner', which opens a confirm dialog and on confirm POSTs to /api/v1/coverage/assets/{id}/route-to-owner, then shows a success toast '{hostname} routed to {routed_to}' (COV-03, D-07)"
    - "Clicking a blind-spot row opens a DrillPanel with idKey='asset' (copying the tickets list-page precedent, NOT the /assets full-page navigation — Pitfall 8), rendering CoverageAssetDrillContent with the Route-to-owner action in its footer"
    - "The confirm dialog has two copy branches — owner-resolved (D-07: 'Notify {owner} about this device?' / 'Notify owner') and unresolvable-owner (D-09: 'No owner found for this device' / 'Notify admins') — per the UI-SPEC Copywriting Contract"
    - "A viewer sees the Route-to-owner action disabled/hidden (never a raw 403 to react to) — canRouteToOwner = role in OWNER/ADMIN/ANALYST, computed once at page level and passed down (D-08 asymmetric RBAC, mirrors the connectors-page useAuth convention)"
    - "The mutation never retries (retry: 0 — audit/notification side effects), invalidates queryKeys.coverage.all on success, and on error shows the UI-SPEC error copy 'Couldn't send the notification. Try again, or check the device's owner directly in your directory connector.'"
    - "The Route-to-owner button uses secondary/violet-focus-ring chrome, NOT the pink gradient CTA — this reconciliation page has no page-level primary CTA (UI-SPEC Design System / Color)"
  artifacts:
    - path: "frontend/src/components/coverage/route-to-owner-dialog.tsx"
      provides: "2-branch confirm dialog (resolved vs unresolvable), no form fields"
      min_lines: 30
    - path: "frontend/src/lib/queries/use-route-to-owner.ts"
      provides: "useRouteToOwner(assetId) mutation (toast + invalidate coverage.all, retry 0)"
      contains: "route-to-owner"
    - path: "frontend/src/components/coverage/coverage-asset-drill-content.tsx"
      provides: "DrillPanel renderContent for idKey='asset' with Route-to-owner footer action"
      min_lines: 30
  key_links:
    - from: "frontend/src/lib/queries/use-route-to-owner.ts"
      to: "/api/v1/coverage/assets/{id}/route-to-owner"
      via: "api() POST inside useMutation"
      pattern: "route-to-owner"
    - from: "frontend/src/app/(authed)/dashboard/coverage/page.tsx"
      to: "frontend/src/components/coverage/coverage-asset-drill-content.tsx"
      via: "DrillPanel idKey='asset' renderContent"
      pattern: "idKey=[\"']asset[\"']"
    - from: "frontend/src/app/(authed)/dashboard/coverage/page.tsx"
      to: "canRouteToOwner (useAuth role check)"
      via: "prop passed to row action + drill content"
      pattern: "canRouteToOwner"
  prohibitions:
    - "Never copy the /assets list page's row-click handler (full-page router.push) — copy the tickets DrillPanel idKey pattern (Pitfall 8)"
    - "Never use the pink bg-gradient-sunset CTA class for the Route-to-owner button — it is a secondary-weight action with a violet focus ring (UI-SPEC Color)"
    - "Never retry the mutation (retry: 0) — it has audit + notification side effects"
    - "Never show a viewer the Route-to-owner action enabled — disable/hide it (backend 403 is defense-in-depth only)"
    - "Never build a manual owner-entry field — D-09 rejected typed-owner entry; the dialog is confirm-only, no free-text"
    - "Never introduce freehand hex colors — only var(--color-*) tokens / existing classNames"
---

<objective>
Deliver COV-03's frontend: a per-row (and per-DrillPanel-footer) "Route to owner" action that opens a two-branch confirm dialog and fires the Plan 04 endpoint, with a success/error toast and analyst-only gating. Clicking a blind-spot row opens an asset DrillPanel (idKey='asset', tickets-page precedent). This completes the shadow-IT loop from the Coverage view.

Purpose: Wire the write path end-to-end on the client, composing the shipped ConfirmModal/ResponsiveDialog/DrillPanel/useToast primitives — zero new primitives.

Output: CoverageAssetDrillContent, the RouteToOwnerDialog (2-branch), useRouteToOwner mutation hook, DrillPanel + row-action wiring on the page, RBAC gating, microcopy, and tests.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-coverage-blind-spot-detection/41-CONTEXT.md
@.planning/phases/41-coverage-blind-spot-detection/41-PATTERNS.md
@.planning/phases/41-coverage-blind-spot-detection/41-UI-SPEC.md
@.planning/phases/41-coverage-blind-spot-detection/41-01-SUMMARY.md
@.planning/phases/41-coverage-blind-spot-detection/41-03-SUMMARY.md
@.planning/phases/41-coverage-blind-spot-detection/41-04-SUMMARY.md

<interfaces>
Plan 04 endpoint: POST /api/v1/coverage/assets/{id}/route-to-owner → { hostname: string, routed_to: string }.
  No request body. 403 for viewers, 404 cross-tenant.

DrillPanel (components/vulnerabilities/drill-panel.tsx) — generalized, no changes needed:
  props idKey, id, ariaLabel, renderContent({ id, onClose }). Tickets precedent: tickets/page.tsx:349-394
  (idKey="ticket"). Copy that shape with idKey="asset". (Do NOT copy assets/page.tsx which navigates.)

TicketDrillContent (components/tickets/ticket-drill-content.tsx) — 3-region shape to mirror:
  Header (mono id + label + close), Body (labeled <section> blocks), sticky Footer (action buttons);
  !data branch renders aria-busy "Loading…".

ConfirmModal (components/ui/ConfirmModal.tsx): open,title,message,confirmLabel,cancelLabel,variant,
  onConfirm,onCancel,children,confirmDisabled. Real call-site: exceptions-table.tsx:363-375.
  NOTE its variant→color map is danger/warning/info — NONE is the required violet-focus secondary chrome.
  Either minimally extend ConfirmModal or build a thin ResponsiveDialog wrapper (exception-grant-dialog.tsx
  shows the ResponsiveDialog + role="alert" error-banner + "…ing" pending-label conventions to reuse —
  but NOT its pink bg-gradient-sunset button color).

useReassignAsset (lib/queries/use-reassign-asset.ts) — mutation+toast+invalidate shape to mirror; retry: 0.
useToast (components/ui/ToastProvider.tsx): toast({variant:'success'|'error', message}).
useAuth (@/lib/auth): user?.role. connectors/page.tsx:98-99 shows the role-check convention.
queryKeys.coverage.all exists (Plan 01).

Copy (UI-SPEC Copywriting Contract — use verbatim):
  Row action "Route to owner"; success toast "{hostname} routed to {routed_to}";
  error "Couldn't send the notification. Try again, or check the device's owner directly in your directory connector.";
  resolved dialog title "Notify {owner_first_name} about this device?" body "{hostname} is in your inventory but no
  scanner covers it. We'll email {owner_first_name} to onboard it — this doesn't create a ticket or a finding."
  confirm "Notify owner"; unresolvable dialog title "No owner found for this device" body "We couldn't resolve an
  owner from your directory. We'll notify your admins and the configured alert channel instead so this isn't silently
  dropped." confirm "Notify admins". Loading: confirm button disabled + "Notifying…".
</interfaces>
</context>

<artifacts_produced>
New components: `coverage-asset-drill-content.tsx`, `route-to-owner-dialog.tsx` (+ test)
New hook: `use-route-to-owner.ts` (+ test)
Modified: `coverage/page.tsx` (DrillPanel idKey='asset' + row action + canRouteToOwner gating), `microcopy.ts` (dialog/toast/error copy), `coverage/page.test.tsx` (drill + RBAC-gating branches)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: useRouteToOwner mutation hook + route-to-owner-dialog (2-branch) + microcopy</name>
  <files>frontend/src/lib/queries/use-route-to-owner.ts, frontend/src/lib/queries/use-route-to-owner.test.tsx, frontend/src/components/coverage/route-to-owner-dialog.tsx, frontend/src/components/coverage/route-to-owner-dialog.test.tsx, frontend/src/components/coverage/microcopy.ts</files>
  <read_first>
    - frontend/src/lib/queries/use-reassign-asset.ts + use-reassign-asset.test.tsx (mutation+toast+invalidate+retry:0 shape)
    - frontend/src/components/ui/ConfirmModal.tsx (confirm-dialog props + variant color map)
    - frontend/src/components/ui/responsive-dialog.tsx + components/exceptions/exception-grant-dialog.tsx (ResponsiveDialog chrome, role="alert" error banner, "…ing" pending label — NOT the pink CTA color)
    - frontend/src/components/exceptions/exceptions-table.tsx lines 355-377 (ConfirmModal call-site + test convention)
    - frontend/src/components/coverage/microcopy.ts (Plan 01/03 output to extend)
    - frontend/src/lib/queries/keys.ts (coverage.all)
    - 41-UI-SPEC.md §Copywriting Contract + §Color (violet focus ring, no pink gradient)
    - .claude/skills/sketch-findings-getvul/references/copy-voice.md
  </read_first>
  <action>
    Create `frontend/src/lib/queries/use-route-to-owner.ts`: `useRouteToOwner(assetId: string)` useMutation<RouteToOwnerResponse, Error, void>; mutationFn `() => api<RouteToOwnerResponse>(\`/api/v1/coverage/assets/${assetId}/route-to-owner\`, { method: 'POST' })` (no body); onSuccess → `qc.invalidateQueries({ queryKey: queryKeys.coverage.all })` + success toast "{data.hostname} routed to {data.routed_to}"; onError → error toast with the UI-SPEC error copy; `retry: 0`.
    Add the dialog/toast/error copy strings to `frontend/src/components/coverage/microcopy.ts` (all verbatim from the Copywriting Contract, both resolved and unresolvable branches, the "Notifying…" pending label, and the "Route to owner" action label).
    Create `frontend/src/components/coverage/route-to-owner-dialog.tsx`: a confirm-only dialog (no form fields) with two copy branches selected by an `ownerResolved: boolean` / `ownerName?: string` prop (or by whether a resolved-owner name is known for the row). Reuse ResponsiveDialog + the role="alert" error-banner pattern; the confirm button uses secondary/`.btn-secondary` chrome with a violet focus ring (NEVER bg-gradient-sunset); confirm shows "Notifying…" and is disabled while the mutation isPending. Wire onConfirm to trigger the passed mutation and close on success.
    Create `frontend/src/components/coverage/route-to-owner-dialog.test.tsx`: assert the resolved branch renders "Notify owner" + the resolved title/body; the unresolvable branch renders "Notify admins" + the D-09 title/body; the confirm button is disabled and shows "Notifying…" while isPending; the button does NOT carry the pink gradient class.
    Create `frontend/src/lib/queries/use-route-to-owner.test.tsx` mirroring use-reassign-asset.test.tsx: assert success toast text, error toast text, retry:0, and that coverage.all is invalidated on success.
  </action>
  <verify>
    <automated>cd frontend && npm test -- route-to-owner && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - use-route-to-owner.test.tsx asserts POST to the correct path (no body), retry:0, success toast "{hostname} routed to {routed_to}", error toast with the exact UI-SPEC error copy, and coverage.all invalidation.
    - route-to-owner-dialog.test.tsx asserts both copy branches (Notify owner / Notify admins) and the "Notifying…" disabled pending state.
    - `grep -Lq "bg-gradient-sunset" frontend/src/components/coverage/route-to-owner-dialog.tsx` (the pink gradient class is absent).
    - No freehand hex in the new files.
    - `npm run lint` clean.
  </acceptance_criteria>
  <done>The mutation hook and 2-branch confirm dialog work with correct copy, toasts, retry:0, invalidation, and secondary (non-pink) button chrome.</done>
</task>

<task type="auto">
  <name>Task 2: Asset DrillPanel content + row action + RBAC gating on the page</name>
  <files>frontend/src/components/coverage/coverage-asset-drill-content.tsx, frontend/src/app/(authed)/dashboard/coverage/page.tsx, frontend/src/app/(authed)/dashboard/coverage/page.test.tsx</files>
  <read_first>
    - frontend/src/app/(authed)/dashboard/tickets/page.tsx lines 340-409 (DrillPanel idKey wiring — the precedent)
    - frontend/src/components/tickets/ticket-drill-content.tsx (3-region drill-content shape + !data loading branch)
    - frontend/src/components/vulnerabilities/drill-panel.tsx (idKey/renderContent contract — no changes needed)
    - frontend/src/app/(authed)/dashboard/connectors/page.tsx lines 98-99 (useAuth role-check convention)
    - frontend/src/app/(authed)/dashboard/coverage/page.tsx + page.test.tsx (Plan 01/03 output to extend)
    - 41-PATTERNS.md §coverage-asset-drill-content.tsx + §page.tsx (RBAC gating section)
    - 41-UI-SPEC.md §UI Considerations E2 (partial/RBAC row) + §Copywriting Contract
  </read_first>
  <action>
    Create `frontend/src/components/coverage/coverage-asset-drill-content.tsx` mirroring ticket-drill-content.tsx's 3-region shape: Header (hostname in JetBrains Mono + device-category label + close button), Body (labeled <section> blocks: a "No scanner coverage" amber badge, which authoritative source(s) placed the asset in the baseline from seen_by_sources, and last-seen-by-authoritative-source), sticky Footer (a single "Route to owner" secondary button — NOT bg-gradient-sunset). Accept an `asset` prop (pre-fetched summary from the row, presentational only) and a `canRouteToOwner` prop; when the button is clicked, open the RouteToOwnerDialog for that asset. Render the aria-busy "Loading…" branch when `asset` is undefined. When `!canRouteToOwner`, render the button disabled (title explaining analyst+ required) rather than omitting it silently.
    Wire `frontend/src/app/(authed)/dashboard/coverage/page.tsx`: compute `const { user } = useAuth(); const canRouteToOwner = user?.role === 'OWNER' || user?.role === 'ADMIN' || user?.role === 'ANALYST';` once. Make each blind-spot row open the DrillPanel via a URL param (mirror tickets page), rendering `<DrillPanel idKey="asset" id={assetIdFromUrl} ariaLabel="Device detail" renderContent={({id, onClose}) => <CoverageAssetDrillContent asset={selectedAsset} canRouteToOwner={canRouteToOwner} onClose={onClose} />} />`. Add the per-row "Route to owner" action (also gated by canRouteToOwner) that opens the same RouteToOwnerDialog. Instantiate useRouteToOwner(assetId) for the active asset and pass into the dialog. Do NOT convert the row click into a full-page navigation (Pitfall 8).
    Extend `frontend/src/app/(authed)/dashboard/coverage/page.test.tsx`: add a test that a row click opens the DrillPanel (idKey='asset' content visible); a test that with a viewer role the Route-to-owner action is disabled/hidden; a test that with an analyst role it is enabled.
  </action>
  <verify>
    <automated>cd frontend && npm test -- coverage/page && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "idKey=\"asset\"" frontend/src/app/(authed)/dashboard/coverage/page.tsx` (DrillPanel wired, not full-page nav).
    - `grep -q "canRouteToOwner" frontend/src/app/(authed)/dashboard/coverage/page.tsx` and the flag gates both the row action and the drill footer button.
    - page.test.tsx asserts: row-click opens the asset DrillPanel; viewer → action disabled/hidden; analyst → action enabled.
    - The drill footer "Route to owner" button does not use bg-gradient-sunset (grep absent in coverage-asset-drill-content.tsx).
    - `npm run lint` clean.
  </acceptance_criteria>
  <done>Clicking a blind-spot row opens the asset DrillPanel; Route-to-owner is available to analysts (disabled for viewers) from both the row and the drill footer, driving the confirm dialog + mutation end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser UI → coverage write endpoint | The client gates the action by role, but the server remains authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-15 | Elevation of Privilege | client-side Route-to-owner gating | mitigate | canRouteToOwner disables/hides the action for viewers; the Plan 04 require_analyst gate is the authoritative defense-in-depth (client gate is UX only, never the security boundary) |
| T-41-16 | Tampering (retry duplicating notifications) | useRouteToOwner | mitigate | retry: 0 — a mutation with audit/notification side effects is never auto-retried |
| T-41-17 | Information Disclosure | DrillPanel content | accept | renders only the tenant's own already-fetched blind-spot asset summary; no new data fetch beyond the tenant-scoped list |
</threat_model>

<verification>
- Frontend: `npm test -- coverage` and `npm test -- route-to-owner` green + `npm run lint` clean.
- Phase gate (run after this plan, per 41-RESEARCH.md Validation Architecture): `make test` (backend) + `npm test` (frontend) + `npm run test:e2e` (the /dashboard/coverage route now in e2e/routes.ts gets an axe + smoke pass).
</verification>

<success_criteria>
- COV-03 delivered end-to-end: analyst routes a blind-spot asset's owner from the Coverage view (row or drill), viewer cannot; success/error toasts and both dialog branches behave per the UI-SPEC.
- DrillPanel uses idKey='asset' (tickets precedent), no full-page navigation.
- No pink CTA, no manual owner-entry field, no client-side retry.
</success_criteria>

<output>
After completion, create `.planning/phases/41-coverage-blind-spot-detection/41-05-SUMMARY.md`.
</output>
