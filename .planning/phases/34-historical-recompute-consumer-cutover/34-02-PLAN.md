---
phase: 34-historical-recompute-consumer-cutover
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - backend/app/vulnerabilities/service.py
  - backend/tests/test_risk_cutover.py
autonomous: true
requirements: [RISK-08]
must_haves:
  truths:
    - "With cutover_risk_exposure_scoring OFF (default), sort=\"triage\" ordering is BYTE-IDENTICAL to pre-Phase-34 (KEV desc → CVSS desc → SLA-due asc) — the OFF path proves zero live behavior change (34-CONTEXT locked: flag stays OFF in this env)"
    - "With the flag ON, sort=\"triage\" leads with nulls_last(desc(Vulnerability.risk_exposure_score)) as the PRIMARY key, keeping the existing 3 as tiebreakers (a superset of today's intent — risk_exposure_score already folds in CVSS/KEV)"
    - "With the flag OFF, get_top_findings_for_ai_batch ranks by nulls_last(desc(Asset.risk_score)) exactly as today; with it ON, it ranks by per-finding nulls_last(desc(Vulnerability.risk_exposure_score)) — a genuine improvement (per-finding vs asset-level) plus a cutover"
    - "The flag is read ONCE per call via a scalar Tenant fetch (mirroring get_sla_days' Tenant fetch), tenant_id-scoped; no other consumer path changes"
    - "SLA breach detection / sla_service.py due-date math is UNTOUCHED (documented: SLA stays severity-keyed, 34-CONTEXT RESOLVED A1) — no risk_exposure_score/Asset.risk_score read is added to any SLA due-date computation"
    - "The stale docstring in get_top_findings_for_ai_batch (\"Vulnerability has no risk_score field at all\") is corrected — Phase 33 added risk_exposure_score"
  artifacts:
    - path: "backend/app/vulnerabilities/service.py"
      provides: "flag-gated branch in sort=\"triage\" (list_vulnerabilities) and in get_top_findings_for_ai_batch, each with a once-per-call scalar Tenant fetch; corrected docstring"
      contains: "cutover_risk_exposure_scoring"
    - path: "backend/tests/test_risk_cutover.py"
      provides: "flag-OFF byte-identical + flag-ON new-score ordering tests for both consumers; an explicit SLA-untouched assertion"
      contains: "def test_triage_sort_cutover_flag"
  key_links:
    - from: "backend/app/vulnerabilities/service.py::list_vulnerabilities"
      to: "tenants.cutover_risk_exposure_scoring"
      via: "scalar Tenant fetch, branch the sort=\"triage\" order_by on it"
      pattern: "cutover_risk_exposure_scoring"
    - from: "backend/app/vulnerabilities/service.py::get_top_findings_for_ai_batch"
      to: "Vulnerability.risk_exposure_score"
      via: "ON-path primary order key replaces Asset.risk_score"
      pattern: "risk_exposure_score"
---

<objective>
RISK-08 flag-gated consumer cutover (build + fixture-test both branches; flag stays OFF in this env per
34-CONTEXT). Wire the two GENUINE cutover consumers — list sort=\"triage\" and the v3.0 AI batch selector
get_top_findings_for_ai_batch — to read the new per-finding risk_exposure_score ONLY when
Tenant.cutover_risk_exposure_scoring is ON, while keeping the OFF path byte-identical to today. SLA breach
detection is deliberately NOT a cutover target (34-CONTEXT RESOLVED A1: SLA stays severity-keyed) and the
trend chart's continuity is Plan 04's concern — this plan documents both boundaries and touches neither.

Purpose: Prove the OFF path is provably safe (byte-identical) and the ON path is provably wired
(fixture-tested) for the two consumers that actually have a risk score to cut over. The severity-tier
centralization clause of RISK-08 was already completed in Phase 33 (RISK_SCORE_TIER_* in
app/assets/risk_score.py:59-61) — nothing to do here beyond a verification citation.

Output: two flag-gated branches in service.py + the RISK-08 fixture suite.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-historical-recompute-consumer-cutover/34-CONTEXT.md
@.planning/phases/34-historical-recompute-consumer-cutover/34-RESEARCH.md
@.planning/phases/34-historical-recompute-consumer-cutover/34-PATTERNS.md

<interfaces>
<!-- Plan 01 landed Tenant.cutover_risk_exposure_scoring (default False). This plan READS it. -->

Tenant-fetch precedent to copy (once per call) — from sla_service.py:43 / exposure.py:424-426:
```python
from app.tenants.models import Tenant
tenant = (await db.execute(select(Tenant).where(Tenant.id == tenant_id))).scalar_one_or_none()
cutover_enabled = tenant.cutover_risk_exposure_scoring if tenant is not None else False
```

sort="triage" (list_vulnerabilities, service.py:92-99) — OFF path is BYTE-IDENTICAL to current:
```python
if filters.sort == "triage":
    if cutover_enabled:
        data_q = data_q.order_by(
            nulls_last(desc(Vulnerability.risk_exposure_score)),   # NEW primary
            desc(Vulnerability.cisa_kev),
            nulls_last(desc(Vulnerability.cvss_v3_score)),
            nulls_last(asc(Vulnerability.sla_due_at)),
        )
    else:
        data_q = data_q.order_by(                                  # unchanged
            desc(Vulnerability.cisa_kev),
            nulls_last(desc(Vulnerability.cvss_v3_score)),
            nulls_last(asc(Vulnerability.sla_due_at)),
        )
```

get_top_findings_for_ai_batch (service.py:534-579) — OFF path byte-identical (nulls_last(desc(Asset.risk_score)) primary);
ON path swaps the primary order key to nulls_last(desc(Vulnerability.risk_exposure_score)). Keep the Asset
outerjoin on both paths (simplest; dropping it on the ON path is an OPTIONAL simplification — do NOT unless
it keeps the OFF path identical). Correct the stale docstring lines ("Vulnerability has no risk_score field
at all") to reflect Phase 33's risk_exposure_score.

list_vulnerabilities receives (db, tenant_id, filters, pagination) — add the Tenant fetch inside it before
the sort branch. get_top_findings_for_ai_batch receives (db, tenant_id, limit) — add the fetch before the
order_by. Both are already tenant_id-scoped.

DO NOT TOUCH: sla_service.py, notifications/alerts.py's _check_sla_breaches (SLA severity-keyed, A1);
trends.py (Plan 04); rule_engine.py / saved_filters.py min_risk_score (Plan 03 diff-only, Pitfall 4).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — flag-OFF-identical + flag-ON-new-score tests for both consumers + SLA-untouched assertion</name>
  <read_first>
    - backend/tests/test_risk_exposure_service.py:149-210 (_seed_asset/_seed_vuln helpers + persisting risk_exposure_score for fixtures)
    - backend/tests/conftest.py (db_session, tenant_a, admin_user, commit-before-call convention; how a Tenant's boolean column is set in a test)
    - backend/app/vulnerabilities/service.py:69-155 (list_vulnerabilities signature + full sort chain) and :534-579 (get_top_findings_for_ai_batch)
    - MEMORY getvul-backend-pytest-env
  </read_first>
  <behavior>
    - test_triage_sort_flag_off_is_identical: seed 3 vulns with mixed cisa_kev/cvss/sla_due and DELIBERATELY inverted risk_exposure_score (so a new-score sort would reorder); with the flag OFF (default), list_vulnerabilities(sort="triage") returns the exact KEV→CVSS→SLA order — assert the id order equals a hand-computed old-order list (proves byte-identical).
    - test_triage_sort_cutover_flag: same fixture, set tenant.cutover_risk_exposure_scoring=True; assert the top result is the highest-risk_exposure_score finding and the overall order leads by risk_exposure_score (differs from the OFF order).
    - test_ai_batch_selector_flag_off_is_identical: seed 3 findings across assets with Asset.risk_score set and inverted per-finding risk_exposure_score; flag OFF → get_top_findings_for_ai_batch returns the Asset.risk_score order (hand-computed).
    - test_ai_batch_selector_cutover_flag: flag ON → the same call returns the per-finding risk_exposure_score order (differs from OFF); an asset-less finding (asset_id NULL) still sorts last via nulls_last.
    - test_sla_breach_stays_severity_keyed (guard, RESOLVED A1): a grep-style structural assertion that sla_service.py contains NO reference to risk_exposure_score/Asset.risk_score (read the file text in the test and assert absence), documenting SLA is intentionally untouched.
  </behavior>
  <action>
    Author backend/tests/test_risk_cutover.py with the tests above. Module docstring names Phase 34 Plan 02 + RISK-08 and states the OFF path must be byte-identical to pre-Phase-34. Seed fixtures with risk_exposure_score deliberately INVERTED vs the KEV/CVSS/Asset.risk_score ordering so an OFF-vs-ON difference is unambiguous. Set the tenant flag via a direct column update + commit. For the SLA-untouched guard, read sla_service.py's source text and assert "risk_exposure_score" not in it. RED: the flag branch does not exist yet → the ON tests get the OFF ordering (assertion fails) and/or the Tenant column read is absent. Env prefix per MEMORY.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret .venv/bin/python -m pytest tests/test_risk_cutover.py -x 2>&1 | grep -Eiq "assert|failed|error" && echo RED-OK</automated>
  </verify>
  <done>test_risk_cutover.py exists; the flag-ON ordering tests fail for real (ON currently behaves like OFF), proving the cutover branch is not yet wired; the OFF-identical + SLA-untouched guards are authored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — flag-gated branch in sort="triage" and get_top_findings_for_ai_batch</name>
  <read_first>
    - backend/app/vulnerabilities/service.py:1-40 (imports — desc/asc/nulls_last/select already imported; add Tenant import if absent)
    - backend/app/vulnerabilities/sla_service.py:43 (the exact scalar Tenant fetch idiom to copy)
    - 34-PATTERNS.md §"service.py:92-99" and §"service.py:534-579" (the exact branch shapes + docstring-correction note)
  </read_first>
  <behavior>
    - All test_risk_cutover.py tests GREEN: OFF byte-identical for both consumers; ON reads risk_exposure_score for both; SLA-untouched guard passes.
  </behavior>
  <action>
    In list_vulnerabilities: add the scalar Tenant fetch (cutover_enabled) before the sort branch; wrap the sort="triage" branch with the OFF/ON split from the interfaces block (OFF byte-identical). Touch no other sort branch. In get_top_findings_for_ai_batch: add the same Tenant fetch; branch the primary order_by key (Asset.risk_score OFF vs Vulnerability.risk_exposure_score ON), keeping the tiebreakers and the Asset outerjoin identical on both paths; correct the stale docstring. Add NO new query beyond the single scalar Tenant fetch per call. Do NOT touch sla_service.py, alerts.py, trends.py, rule_engine.py, saved_filters.py.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret .venv/bin/python -m pytest tests/test_risk_cutover.py -x</automated>
  </verify>
  <done>Both consumers branch on cutover_risk_exposure_scoring (OFF byte-identical, ON reads the new score); the AI-batch docstring is corrected; sla_service.py untouched; all RISK-08 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request tenant_id → per-call flag read | the flag fetch must be tenant-scoped; a wrong-tenant flag read would cut over the wrong tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05 | Information Disclosure | scalar Tenant fetch in list_vulnerabilities / get_top_findings_for_ai_batch | mitigate | The fetch is `select(Tenant).where(Tenant.id == tenant_id)` (the request's own tenant); ordering only, no new rows exposed. Mirrors the already-audited sla_service.py:43 / exposure.py:426 fetch. |
| T-34-06 | Tampering | accidental live behavior change with the flag OFF | mitigate | The OFF path is authored byte-identical and pinned by test_triage_sort_flag_off_is_identical + test_ai_batch_selector_flag_off_is_identical (inverted-score fixtures make any drift fail the test). |
| T-34-07 | Scope creep | SLA / min_risk_score silently cut over | accept (guarded) | sla_service.py + rule_engine.py + saved_filters.py explicitly untouched; test_sla_breach_stays_severity_keyed asserts absence of risk_exposure_score in sla_service.py. |
</threat_model>

<verification>
- `cd backend && ENCRYPTION_KEY=... JWT_SECRET_KEY=... .venv/bin/python -m pytest tests/test_risk_cutover.py -x` green.
- Flag-OFF gate: with the flag OFF, `... pytest tests/test_vulnerabilities.py tests/test_sla_service.py -x` (existing suites) stay green unmodified — no live behavior change.
- SLA-untouched gate: `grep -n "risk_exposure_score\|risk_score" backend/app/vulnerabilities/sla_service.py` returns nothing.
- min_risk_score-untouched gate (Pitfall 4): `git diff` shows NO change to rule_engine.py or saved_filters.py.
- Severity-tier centralization (RISK-08 clause): cite Phase 33 — `grep -n "RISK_SCORE_TIER_" backend/app/assets/risk_score.py` confirms the single-constant source already exists; no new work.
</verification>

<success_criteria>
- SC#2 (RISK-08): sort="triage" and get_top_findings_for_ai_batch read the new per-finding score WHEN the flag is ON, byte-identical when OFF; SLA windows stay severity-keyed (untouched, documented); severity-tier boundaries already centralized in Phase 33.
- The flag flip itself is NOT performed in this env (34-CONTEXT locked) — only the branches are built + fixture-proven.
</success_criteria>

<output>
After completion, create `.planning/phases/34-historical-recompute-consumer-cutover/34-02-SUMMARY.md`.
Record: the exact OFF/ON order_by for both consumers; confirmation the OFF path is byte-identical (which
existing suites stayed green); whether the Asset outerjoin was kept on the ON path or simplified; the
corrected AI-batch docstring; and that SLA + min_risk_score were left untouched (with the grep evidence).
</output>
