---
phase: 33-risk-exposure-model-definition
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - frontend/src/lib/queries/use-vulnerability-detail.ts
  - frontend/src/components/vulnerabilities/microcopy.ts
  - frontend/src/components/vulnerabilities/drill-content.tsx
  - frontend/src/components/vulnerabilities/drill-panel.test.tsx
  - frontend/src/components/vulnerabilities/drill-panel-mobile.test.tsx
autonomous: false
requirements: [RISK-05]
must_haves:
  truths:
    - "The DrillPanel renders a 'Risk Exposure' section between CVSS and Affected hosts showing the overall risk_exposure_score plus one row per breakdown component (label + raw_value + points/max_points) — the analyst can see 'why is this an 82' (RISK-05)"
    - "The section is clearly labeled shadow/preview ('not yet used for sorting or alerts') so analysts do not mistake it for a live triage driver (CONTEXT shadow/preview lock)"
    - "A '★ KEV floor applied' chip renders (reusing the existing CISA-KEV pink-chip styling) when the breakdown contains a kev_floor component (from Plan 02) — no frontend re-derivation of business logic"
    - "When risk_exposure_score / risk_exposure_breakdown is null (finding never shadow-computed yet), the section degrades gracefully (empty/absent state, no crash) per the mandatory state-patterns rule"
    - "All copy is sourced from microcopy.ts (no hardcoded strings), sunset tokens + RiskRing reused (sketch-findings-getvul); no new gauge component built"
    - "The new section is exercised by RTL tests in drill-panel.test.tsx AND drill-panel-mobile.test.tsx (DrillContent has no standalone test file — it is tested via both wrappers)"
  artifacts:
    - path: "frontend/src/lib/queries/use-vulnerability-detail.ts"
      provides: "RiskBreakdownComponent type + 3 new fields on VulnerabilityDetail"
      contains: "risk_exposure_breakdown"
    - path: "frontend/src/components/vulnerabilities/drill-content.tsx"
      provides: "the Risk Exposure section (RiskRing badge + data-driven .map breakdown rows + KEV-floor chip + preview caption)"
      contains: "drill-risk-exposure-h"
    - path: "frontend/src/components/vulnerabilities/microcopy.ts"
      provides: "drill.sections.riskExposure heading + preview caption + KEV-floor chip copy"
      contains: "riskExposure"
    - path: "frontend/src/components/vulnerabilities/drill-panel.test.tsx"
      provides: "RTL cases: breakdown renders, preview label present, KEV chip conditional, null-safe"
      contains: "risk exposure"
  key_links:
    - from: "drill-content.tsx Risk Exposure section"
      to: "v.risk_exposure_breakdown (data-driven .map)"
      via: "render one BreakdownRow-style row per component; RiskRing for the overall score"
      pattern: "risk_exposure_breakdown"
    - from: "KEV-floor chip"
      to: "breakdown component key === 'kev_floor'"
      via: "conditional render reusing the CISA-KEV pink-chip class list"
      pattern: "kev_floor"
---

<objective>
Surface the shadow per-finding risk-exposure score in the DrillPanel as a read-only, clearly
preview-labeled "Risk Exposure" section — the RISK-05 explainability requirement ("an analyst can see
the per-input breakdown, 'why is this an 82'"). Reuse the existing RiskRing gauge and RiskCard breakdown-row
shape (data-driven over the API's risk_exposure_breakdown array), the existing CISA-KEV chip styling for a
"★ KEV floor applied" indicator, and route every string through microcopy.ts (copy-voice).

Purpose: Close RISK-05 while honoring the RISK-06 zero-consumer gate — this is a human-only display, the
sole allowed reader of the shadow score this phase (CONTEXT RESOLVED A3). It is explicitly labeled "not yet
used for sorting or alerts" so no analyst mistakes it for a live triage driver before Phase 34's cutover.
Output: the VulnerabilityDetail type + RiskBreakdownComponent type extension, new microcopy keys, the Risk
Exposure section in drill-content.tsx (one edit covers desktop + mobile), and RTL coverage in both wrapper
test files, ending in a human-verify checkpoint on the live stack.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-risk-exposure-model-definition/33-CONTEXT.md
@.planning/phases/33-risk-exposure-model-definition/33-PATTERNS.md
@.planning/phases/33-risk-exposure-model-definition/33-02-SUMMARY.md
@.claude/skills/sketch-findings-getvul/references/visual-language.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md

<interfaces>
<!-- Backend contract landed by Plans 01+02. The frontend mirrors it 1:1. -->
Response fields on GET /vulnerabilities/{id} (schemas.py, Plan 01/02):
```
risk_exposure_score: int | null
risk_exposure_breakdown: RiskBreakdownComponent[] | null   # includes a key==="kev_floor" row when the floor applied
risk_model_version: string | null
```
RiskBreakdownComponent (mirror backend 1:1 in use-vulnerability-detail.ts):
```typescript
export type RiskBreakdownComponent = {
  key: string; label: string; raw_value: string; points: number; max_points: number;
};
```
Reuse (do NOT rebuild): RiskRing (frontend/src/components/ui/RiskRing.tsx — pass a small size), the
BreakdownRow row shape (risk-card.tsx:20-40: label left, `font-mono tabular-nums` tinted value right),
the CISA-KEV pink chip class list (drill-content.tsx:583-586), and RiskRing's `text-xs text-text-faint`
caption styling for the preview caption. Insert AFTER the CVSS section (drill-content.tsx:611-622),
BEFORE "Affected hosts". Do NOT use the ai-explanation-citations tooltip pattern (wrong shape).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend VulnerabilityDetail type + microcopy keys</name>
  <read_first>
    - frontend/src/lib/queries/use-vulnerability-detail.ts:12-29 (flat VulnerabilityDetail type)
    - frontend/src/components/vulnerabilities/microcopy.ts:41-53 (drill.sections structure)
    - .claude/skills/sketch-findings-getvul/references/copy-voice.md (no "Coming soon!"/generic-SaaS boilerplate)
  </read_first>
  <action>
    In use-vulnerability-detail.ts: add the exported RiskBreakdownComponent type (interfaces block) and the 3 new fields (risk_exposure_score/risk_exposure_breakdown/risk_model_version) to VulnerabilityDetail, matching the file's flat style. No change to the useQuery/api call itself.
    In microcopy.ts: add under drill.sections a `riskExposure` heading (e.g. "Risk exposure"), a preview caption keyed nearby (copy-voice compliant, conveying "shadow score — not yet used for sorting or alerts", NO "Coming soon!"), and a "KEV floor applied" chip label. Also add a short overall-score aria/label if needed. All copy voice-compliant per copy-voice.md.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -Ei "use-vulnerability-detail|microcopy" ; echo "typecheck-scan-done"</automated>
  </verify>
  <done>RiskBreakdownComponent type + 3 fields exist on VulnerabilityDetail; microcopy.ts has the riskExposure heading, preview caption, and KEV-floor chip label; no type errors introduced in the edited files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Risk Exposure section in drill-content.tsx + RTL tests (desktop + mobile)</name>
  <read_first>
    - frontend/src/components/vulnerabilities/drill-content.tsx:583-586 (KEV chip class list), :611-624 (CVSS section = insertion point + section/h4 conventions)
    - frontend/src/components/assets/risk-card.tsx:20-40 (BreakdownRow shape) + frontend/src/components/ui/RiskRing.tsx (gauge + size + caption props)
    - frontend/src/components/vulnerabilities/drill-panel.test.tsx:1-70 (vi.mock('@/lib/queries/use-vulnerability-detail') + inline detail object + query style) and drill-panel-mobile.test.tsx (mobile wrapper)
    - .claude/skills/sketch-findings-getvul/references/state-patterns.md (mandatory empty/absent handling)
  </read_first>
  <behavior>
    - RTL (drill-panel.test.tsx): given a mocked detail with risk_exposure_score=82 and a breakdown array (severity_cvss, epss, native_exploitability, exposure_business_criticality, corroboration, kev_floor), the panel renders the "Risk exposure" heading, the score 82 (RiskRing or badge), one row per component with its raw_value + points, the preview caption, and the "★ KEV floor applied" chip (kev_floor present).
    - RTL: with risk_exposure_breakdown=null / risk_exposure_score=null, the section renders nothing (or a graceful absent state) and the panel does not crash.
    - RTL: with a breakdown lacking a kev_floor component, the KEV-floor chip is NOT rendered.
    - RTL (drill-panel-mobile.test.tsx): the same section renders via the mobile wrapper (DrillContent is shared) — at minimum assert the heading + score appear.
  </behavior>
  <action>
    Write the RTL cases FIRST (extend both existing test files' inline mock detail objects with the 3 new fields; assert per the behavior block using the file's existing query style). Then implement the section in drill-content.tsx: a `<section aria-labelledby="drill-risk-exposure-h">` with the h4 heading convention (microcopy.drill.sections.riskExposure), a small RiskRing (or badge) showing risk_exposure_score, a data-driven `.map()` over v.risk_exposure_breakdown rendering one BreakdownRow-style row per component (label left; `${raw_value} · ${points}/${max_points} pts` right, font-mono tabular-nums), the preview caption (text-xs text-text-faint), and the "★ KEV floor applied" chip (reuse the CISA-KEV pink-chip class list) conditioned on `breakdown.some(c => c.key === 'kev_floor')`. Guard the entire section on `v.risk_exposure_score != null && v.risk_exposure_breakdown` (absent state otherwise — state-patterns rule). Insert after CVSS, before Affected hosts. Sunset tokens only; reuse RiskRing (no second gauge). One edit to drill-content.tsx covers both desktop + mobile.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/vulnerabilities/drill-panel.test.tsx src/components/vulnerabilities/drill-panel-mobile.test.tsx</automated>
  </verify>
  <done>Both wrapper test files green including the new Risk Exposure cases; the section renders score + data-driven breakdown rows + preview caption + conditional KEV-floor chip; null-safe absent state; RiskRing reused; all copy from microcopy.ts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A read-only "Risk Exposure" section in the vulnerability DrillPanel (desktop + mobile) showing the shadow
    per-finding score, a per-input breakdown ("why is this an 82"), a "★ KEV floor applied" chip when the
    floor escalated the score, and a clearly-worded shadow/preview caption. Backend shadow-computes the score
    at sync; the frontend reads the persisted breakdown. No sort/SLA/trend/AI consumer reads the score.
  </what-built>
  <how-to-verify>
    1. Bring up the local stack per MEMORY getvul-local-e2e-perf-gate (prod build, admin user, :3000 CORS).
    2. Trigger a connector sync (or run the dev seed + recompute path) so compute_finding_risk_scores populates
       risk_exposure_score on findings.
    3. Open a finding in the DrillPanel (desktop). Confirm: the "Risk exposure" section appears between CVSS and
       Affected hosts; the overall score renders (RiskRing/badge); each breakdown row shows a label + raw_value +
       points/max_points; the preview caption clearly says the score is shadow/not-yet-used-for-sorting-or-alerts.
    4. Open a KEV finding — confirm the "★ KEV floor applied" chip shows (styling matches the existing ★ CISA KEV chip).
    5. Open the same finding on a narrow viewport (mobile drill panel) — confirm the section renders there too.
    6. Confirm NO finding list / dashboard / SLA view changed its sort or counts (score is shadow only).
    7. Visual: sunset tokens, readable contrast, no generic-SaaS copy (copy-voice).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (missing rows, wrong ordering, chip not showing, contrast, copy).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-computed breakdown JSON → DrillPanel render | display-only data crossing into the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-10 | Information Disclosure | Risk Exposure section reading the shadow score | accept | Read-only display of the tenant's own already-authorized finding detail (require_viewer-gated endpoint, unchanged); the only permitted reader of the shadow score this phase (CONTEXT RESOLVED A3). |
| T-33-11 | Tampering | breakdown array rendered client-side | mitigate | Rendered from the server-computed, fixed-key component list; React escapes text; frontend does not re-derive scoring business logic (KEV-floor chip driven by the backend kev_floor component, not a client recomputation). |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/vulnerabilities/drill-panel.test.tsx src/components/vulnerabilities/drill-panel-mobile.test.tsx` green.
- `cd frontend && npm run build` (or tsc --noEmit) clean.
- Human-verify checkpoint approved (live DrillPanel, desktop + mobile, KEV chip, preview label, no consumer changed).
- Full frontend suite `cd frontend && npm test` green as the wave gate.
</verification>

<success_criteria>
- RISK-05: an analyst sees the per-input breakdown for a finding in the DrillPanel, shadow/preview-labeled, with a KEV-floor indicator — no automated consumer reads the score (RISK-06 intact).
</success_criteria>

<output>
After completion, create `.planning/phases/33-risk-exposure-model-definition/33-04-SUMMARY.md`.
Record: the exact microcopy keys added, how the KEV-floor chip is driven (backend kev_floor component), the
absent/null state behavior, confirmation one drill-content.tsx edit covered both desktop + mobile, and the
human-verify outcome.
</output>
