---
phase: 11
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/tailwind.config.ts
  - frontend/src/components/states/skeleton-table.test.tsx
  - frontend/src/components/states/empty-state.test.tsx
  - frontend/src/components/states/partial-failure-banner.test.tsx
  - frontend/src/components/states/per-source-status-strip.test.tsx
  - frontend/src/hooks/use-url-state-list.test.ts
  - frontend/src/lib/queries/use-query-errors.test.tsx
  - frontend/src/lib/queries/use-vulnerabilities.test.tsx
  - frontend/src/lib/mutations/use-create-ticket.test.tsx
  - frontend/src/components/vulnerabilities/chip-bar.test.tsx
  - frontend/src/components/vulnerabilities/vuln-table.test.tsx
  - frontend/src/components/vulnerabilities/drill-panel.test.tsx
  - frontend/src/components/vulnerabilities/drill-panel-mobile.test.tsx
  - frontend/src/components/vulnerabilities/view-toggle.test.tsx
  - frontend/src/app/(authed)/dashboard/vulnerabilities/page.test.tsx
autonomous: true
requirements:
  - UX-03-01
  - UX-03-02
  - UX-03-03
  - UX-03-04
  - UX-03-05
  - UX-03-06
  - UX-S-01
  - UX-S-02
  - UX-S-03
  - UX-S-04
tags:
  - frontend
  - testing
  - tdd

must_haves:
  truths:
    - "`vaul@1.1.2` is installed and pinned exactly (no caret) in frontend/package.json"
    - "Tailwind `animate-shimmer` keyframe is wired in `tailwind.config.ts` (or matches existing `animate-skeleton-shimmer` and is aliased)"
    - "`vitest-setup.ts` (or `vitest.setup.ts`) is wired with `vitest-axe/matchers` (already true — verify only)"
    - "14 RED frontend test files exist in their canonical locations, each describing observable behavior, no implementations yet"
    - "Running `npx vitest run` against any of the 14 test files exits non-zero (RED)"
  artifacts:
    - path: "frontend/package.json"
      provides: "vaul@1.1.2 (exact) pinned dependency"
      contains: '"vaul": "1.1.2"'
    - path: "frontend/tailwind.config.ts"
      provides: "animate-shimmer keyframe (or alias to existing skeleton-shimmer)"
    - path: "frontend/src/components/states/skeleton-table.test.tsx"
      provides: "RED unit + axe coverage for D-S-01 column-aware skeleton"
    - path: "frontend/src/components/states/empty-state.test.tsx"
      provides: "RED unit + axe coverage for D-S-02 compound EmptyState"
    - path: "frontend/src/components/states/partial-failure-banner.test.tsx"
      provides: "RED unit + axe coverage for D-S-03 hybrid hook+props mode"
    - path: "frontend/src/components/states/per-source-status-strip.test.tsx"
      provides: "RED unit + axe coverage for D-V-02 + aria-live"
    - path: "frontend/src/hooks/use-url-state-list.test.ts"
      provides: "RED coverage for D-F-05 multi-value URL state + XSS clamp"
    - path: "frontend/src/lib/queries/use-query-errors.test.tsx"
      provides: "RED coverage for D-S-03 QueryCache subscription bridge"
    - path: "frontend/src/lib/queries/use-vulnerabilities.test.tsx"
      provides: "RED coverage for query-key shape + filter composition"
    - path: "frontend/src/lib/mutations/use-create-ticket.test.tsx"
      provides: "RED coverage for 401 surface (Phase 10 BL-06 carryover)"
    - path: "frontend/src/components/vulnerabilities/chip-bar.test.tsx"
      provides: "RED coverage for UX-03-01 chip-bar + 250ms debounce + immediate chip toggle"
    - path: "frontend/src/components/vulnerabilities/vuln-table.test.tsx"
      provides: "RED coverage for UX-03-02 + UX-07-03 keyboard + D-V-04 stale tinting"
    - path: "frontend/src/components/vulnerabilities/drill-panel.test.tsx"
      provides: "RED coverage for UX-03-03 + D-P-01/02/05/06"
    - path: "frontend/src/components/vulnerabilities/drill-panel-mobile.test.tsx"
      provides: "RED coverage for UX-03-06 + vaul bottom-sheet"
    - path: "frontend/src/components/vulnerabilities/view-toggle.test.tsx"
      provides: "RED coverage for UX-03-05"
    - path: "frontend/src/app/(authed)/dashboard/vulnerabilities/page.test.tsx"
      provides: "RED page-level integration test"
  key_links:
    - from: "frontend/package.json"
      to: "node_modules/vaul"
      via: "exact-pin install"
      pattern: '"vaul":\s*"1\\.1\\.2"'
    - from: "frontend/tailwind.config.ts"
      to: "SkeletonTable shimmer animation"
      via: "animate-shimmer keyframe"
      pattern: 'shimmer'
---

<objective>
Lay the test scaffolding (14 RED test files) and infrastructure (vaul pinned, Tailwind shimmer wired) that all of Wave 1 + Wave 2 will then turn GREEN. Per RESEARCH §Validation Architecture and VALIDATION.md, every test file matches a Wave 1/Wave 2 implementation target.

Purpose: Wave 0 enforces the Nyquist sampling contract — every downstream implementation task has a concrete `<automated>` test command that already exists. Tests describe the locked CONTEXT.md behaviors as executable specs.

Output: 14 RED test files, vaul installed, Tailwind shimmer wired.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-vulnerabilities-state-patterns/11-CONTEXT.md
@.planning/phases/11-vulnerabilities-state-patterns/11-RESEARCH.md
@.planning/phases/11-vulnerabilities-state-patterns/11-VALIDATION.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md
@.claude/skills/sketch-findings-getvul/references/interaction-patterns.md
@frontend/vitest.config.mts
@frontend/vitest.setup.ts
@frontend/src/hooks/use-url-state.test.ts
@frontend/src/components/ui/card.test.tsx

<interfaces>
<!-- The 14 test files target implementations that ship in Wave 1 + Wave 2. -->
<!-- These signatures are LOCKED via CONTEXT.md — tests assert them as specs. -->

Wave 1 hooks/queries (Plan 11-03):
```typescript
// frontend/src/hooks/use-url-state-list.ts (NEW — D-F-05)
export function useUrlStateList<T extends string>(
  key: string,
  allowed: readonly T[],
  defaultValue?: readonly T[]
): [readonly T[], (next: readonly T[]) => void, (item: T) => void];

// frontend/src/lib/queries/use-query-errors.ts (NEW — D-S-03)
export type QueryError = { queryKey: QueryKey; error: Error; code: number | string; requestId: string };
export function useQueryErrors(keys: readonly QueryKey[]): QueryError[];

// frontend/src/lib/queries/use-vulnerabilities.ts (NEW)
export function useVulnerabilities(opts: {
  filters: VulnerabilitiesFilters;
  group: 'cve' | 'host';
  page: number;
  sort: string;        // e.g. 'cve_id:asc' OR include order separately
  order: 'asc' | 'desc';
}): UseQueryResult<VulnerabilitiesResponse>;

// frontend/src/lib/mutations/use-create-ticket.ts (NEW)
export function useCreateTicketMutation(): UseMutationResult<
  { created: number; tickets: unknown[] },
  Error,
  { vulnerability_ids: string[]; provider: 'ASANA'|'JIRA'|'GITHUB'; project_key?: string; assignee?: string; due_days?: number }
>;
```

Wave 1 state primitives (Plan 11-04):
```typescript
// frontend/src/components/states/skeleton-table.tsx (NEW — D-S-01)
export type SkeletonColumnKind = 'pill' | 'mono' | 'text' | 'badge';
export type SkeletonColumn = { kind: SkeletonColumnKind; width: number };
export function SkeletonTable(props: { rows?: number; columns: SkeletonColumn[]; className?: string }): JSX.Element;

// frontend/src/components/states/empty-state.tsx (NEW — D-S-02 compound)
export const EmptyState: ForwardRefExoticComponent<...> & {
  Title: ...; Body: ...; Actions: ...; Suggestion: ...;
};

// frontend/src/components/states/partial-failure-banner.tsx (NEW — D-S-03)
type PartialFailureBannerProps = {
  watchKeys?: readonly QueryKey[];               // default-mode (uses useQueryErrors)
  errors?: { code: number|string; requestId: string; message?: string }[];  // override
  onRetry?: () => void;
  source?: string;       // e.g., "Tenable" — names the connector in copy
};
export function PartialFailureBanner(props: PartialFailureBannerProps): JSX.Element | null;

// frontend/src/components/states/per-source-status-strip.tsx (NEW — D-V-02)
export function PerSourceStatusStrip(props: { facets: Record<string, number> }): JSX.Element | null;
```

Wave 2 vuln-page components (Plan 11-05):
```typescript
// frontend/src/components/vulnerabilities/chip-bar.tsx (NEW)
// frontend/src/components/vulnerabilities/vuln-table.tsx (NEW)
// frontend/src/components/vulnerabilities/drill-panel.tsx (NEW)
// frontend/src/components/vulnerabilities/drill-panel-mobile.tsx (NEW)
// frontend/src/components/vulnerabilities/view-toggle.tsx (NEW)
```

From `frontend/src/hooks/use-url-state.test.ts` (mock pattern to mirror in all hook tests):
```typescript
const mockReplace = vi.fn();
let mockParams = new URLSearchParams();
vi.mock('next/navigation', () => ({
  useRouter: () => ({ replace: mockReplace }),
  usePathname: () => '/dashboard',
  useSearchParams: () => ({
    get: (k: string) => mockParams.get(k),
    getAll: (k: string) => mockParams.getAll(k),  // add for useUrlStateList
    toString: () => mockParams.toString(),
  }),
}));
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev environment → npm registry | `npm install vaul@1.1.2` pulls third-party code; supply-chain risk |
| Test runtime → DOM | jsdom env executes test fixtures; vitest-axe runs DOM assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Tampering (supply chain) | vaul dependency | mitigate | Pin EXACTLY to 1.1.2 (no caret) via `npm install vaul@1.1.2 --save-exact`. Verify with `grep '"vaul": "1\\.1\\.2"' frontend/package.json` (no `^`). RESEARCH Pitfall 8 — vaul is unmaintained; pinning prevents Dependabot surprise. |
| T-11-10 | Information Disclosure | test fixtures leaking real CVE / asset data | accept | Test fixtures use placeholder strings (`CVE-2024-0001`, `host-a`) — no real customer data. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 11-02-01: Install vaul@1.1.2 (exact pin) and verify Tailwind shimmer wiring</name>
  <files>frontend/package.json, frontend/tailwind.config.ts</files>
  <read_first>
    - frontend/package.json (existing dependencies block — no other "vaul" entry should pre-exist)
    - frontend/tailwind.config.ts lines 84-100 (existing `keyframes` and `animation` blocks — `skeleton-shimmer` already wired)
    - .planning/phases/11-vulnerabilities-state-patterns/11-RESEARCH.md §Pitfall 8 — "Pin EXACTLY to 1.1.2 (no caret) in package.json to prevent surprise"
    - .planning/phases/11-vulnerabilities-state-patterns/11-RESEARCH.md §Code Examples "Sunset-tokenized SkeletonTable" — shimmer keyframe shape
  </read_first>
  <action>
    1. From `/Users/chemencedji/Desktop/getvul/frontend`, run:
       ```bash
       npm install vaul@1.1.2 --save-exact
       ```
       This adds `"vaul": "1.1.2"` (NO leading caret) to the `dependencies` block.

    2. Inspect `frontend/tailwind.config.ts`. Existing keyframe `skeleton-shimmer` already exists at lines 87-89 with the correct `from/to backgroundPosition` shape. The animation name is `animate-skeleton-shimmer`. RESEARCH §Code Examples uses `animate-shimmer`. To keep RESEARCH-quoted code working verbatim without breaking the existing animation:
       - Add an alias entry in the `animation` block:
         ```typescript
         animation: {
           'pulse-urgency':    'pulse-urgency 2s cubic-bezier(0.4, 0, 0.6, 1) infinite',
           'gradient-drift':   'gradient-drift 24s ease-in-out infinite',
           'skeleton-shimmer': 'skeleton-shimmer 2s linear infinite',
           'shimmer':          'skeleton-shimmer 1.6s linear infinite',   // ← NEW alias for Phase 11 SkeletonTable
           'cta-shine-sweep':  'cta-shine-sweep 3s ease-in-out infinite',
         },
         ```
       - This lets `motion-safe:animate-shimmer` resolve in Phase 11 components while preserving Phase 10's `animate-skeleton-shimmer` consumers.

    3. Verify `frontend/vitest.setup.ts` (not `vitest-setup.ts`) already imports `vitest-axe/matchers` and extends `expect` — it does (verified). NO change needed unless missing.

    Commit: `chore(11-02): pin vaul@1.1.2 + alias animate-shimmer in tailwind config`.
  </action>
  <verify>
    <automated>cd /Users/chemencedji/Desktop/getvul/frontend && grep '"vaul":\s*"1\.1\.2"' package.json && grep "'shimmer':" tailwind.config.ts && test -f node_modules/vaul/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"vaul": "1\\.1\\.2"' frontend/package.json` returns a match (EXACT, no caret prefix)
    - `grep '"\\^vaul"\\|"vaul": "\\^' frontend/package.json` returns no matches (no caret on vaul)
    - `cat frontend/node_modules/vaul/package.json` exists and `version` field is exactly `1.1.2`
    - `grep "'shimmer':" frontend/tailwind.config.ts` returns a match (the alias)
    - `grep "skeleton-shimmer" frontend/tailwind.config.ts` still returns matches (Phase 10 keyframe preserved)
    - `cat frontend/vitest.setup.ts | grep vitest-axe` returns a match (setup already wired)
  </acceptance_criteria>
  <done>vaul installed pinned; Tailwind shimmer alias wired; vitest-axe setup confirmed already present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 11-02-02: Scaffold 6 RED test files for hooks + queries + mutations</name>
  <files>frontend/src/hooks/use-url-state-list.test.ts, frontend/src/lib/queries/use-query-errors.test.tsx, frontend/src/lib/queries/use-vulnerabilities.test.tsx, frontend/src/lib/mutations/use-create-ticket.test.tsx</files>
  <read_first>
    - frontend/src/hooks/use-url-state.test.ts (canonical hook-test mock shape — copy `vi.mock('next/navigation', ...)` block + the XSS-clamp test as the spec for `useUrlStateList`)
    - frontend/src/lib/queries/use-stats.test.tsx (canonical TanStack hook-test shape with QueryClientProvider wrapper)
    - frontend/src/lib/mutations/use-snooze.test.tsx (canonical mutation test pattern — `useMutation` + `mockMutationFn`)
    - .planning/phases/11-vulnerabilities-state-patterns/11-RESEARCH.md §Pattern 1 useUrlStateList (lines ~225-275)
    - .planning/phases/11-vulnerabilities-state-patterns/11-RESEARCH.md §Pattern 3 useQueryErrors (lines ~382-477)
    - .planning/phases/11-vulnerabilities-state-patterns/11-RESEARCH.md §Pattern 2 useVulnerabilities (lines ~286-360)
    - .planning/phases/11-vulnerabilities-state-patterns/11-CONTEXT.md §D-F-05, §D-S-03, §D-P-04
    - frontend/src/lib/api.ts lines 71-95 (BL-06 — 401 retry restriction; `useCreateTicketMutation` MUST throw `Session expired during mutation. Please retry.` on 401 with no silent refresh)
  </read_first>
  <behavior>
    **`use-url-state-list.test.ts`** — D-F-05 + XSS clamp (mirror Phase 10 WR-04):
    - Test 1: `getAll` returns allow-listed values when URL has multiple `?severity=critical&severity=high`
    - Test 2: garbage URL values (`severity=<script>alert(1)</script>`) are filtered out → returned array contains ONLY allow-listed values; assertion `expect((allowed as readonly string[]).includes(...))` for every returned element
    - Test 3: `setValue(['critical','high'])` appends two `severity` params + `replace` called with `{ scroll: false }`
    - Test 4: `setValue([])` removes the key entirely (clean URL — no trailing `?severity=` empty)
    - Test 5: `toggle('critical')` adds when absent, removes when present (idempotent in-out)
    - Test 6: Write-path also filters via allow-list (defense in depth — calling `setValue(['evil' as any])` produces no URL update for that value)
    - Test 7: Coexists with `useUrlState('search', ...)` — using different keys on the same URL doesn't interfere

    **`use-query-errors.test.tsx`** — D-S-03:
    - Test 1: returns `[]` when no queries match the keys
    - Test 2: returns errors for queries that partially-match the supplied keys (e.g. supply `[['vulnerabilities']]`, expect to capture an errored query whose key is `['vulnerabilities','list',{...}]`)
    - Test 3: re-renders consuming component when a query transitions success → error (verify via a `useRenderCount` counter that increments only on snapshot fingerprint change)
    - Test 4: re-renders when error → success (banner disappears — returned array goes from length 1 to 0)
    - Test 5: SSR returns `[]` — the third arg of `useSyncExternalStore` is exercised by calling the hook in a setup that has no window (or by spying on the `getServerSnapshot` path)
    - Test 6: snapshot fingerprint stability — re-firing `queryCache.subscribe` with the same error set returns the SAME array reference (no churn)

    **`use-vulnerabilities.test.tsx`** — query-key + filter composition:
    - Test 1: `queryKey` includes filters, group, page, sort, order in the third positional object (matches D-D-03 domain-first convention; assertion via the QueryCache `.find()` shape)
    - Test 2: `buildSearchParams` produces `?severity=critical&severity=high&source=QUALYS&facets=severity,source,status&page=1` when given those filters (helper function exported from use-vulnerabilities.ts for testability)
    - Test 3: `?group=host` flows through to the request URL when `opts.group === 'host'`
    - Test 4: `?facets=severity,source,status` is ALWAYS appended (per D-F-02, every list call includes all facets so chip counts stay synced)
    - Test 5: 401 on the underlying api() call surfaces as an Error (api.ts redirects; mutation hook tested separately surfaces it; query hook's 401 is the SAFE-METHOD path so retry+refresh happens silently per BL-06)

    **`use-create-ticket.test.tsx`** — D-P-04 + Phase 10 BL-06 + T-11-06/07:
    - Test 1: happy path — calling `mutate({ vulnerability_ids: [...], provider: 'ASANA', project_key: 'GID-123' })` issues `POST /api/v1/tickets` and resolves with the response `{ created, tickets }`
    - Test 2: on 401, mutation throws `Error('Session expired during mutation. Please retry.')` — NO silent token-refresh retry (this is api.ts BL-06's contract; verify by mocking fetch to return 401 once and asserting the rejection message)
    - Test 3: on 403 (viewer role), mutation throws and the error message includes `403` or a generic surfacing — UI consumer (drill-panel) handles
    - Test 4: on success, the mutation calls `queryClient.invalidateQueries({ queryKey: ['notifications'] })` so the activity feed picks up the new `ticket.create` audit event (per RESEARCH Open Question 3 recommendation)
    - Test 5: mutationFn aborts cleanly on component unmount (signal pass-through verified)
  </behavior>
  <action>
    Create all 4 files. Each test file must:

    - Use `@vitest-environment jsdom` directive at the top (vitest-setup wires jsdom but the directive keeps tests self-documenting).
    - For hook tests with TanStack: wrap in a fresh `new QueryClient({ defaultOptions: { queries: { retry: false }, mutations: { retry: false } } })` per test (canonical from `use-stats.test.tsx`); render via `renderHook(() => useFoo(args), { wrapper: ({ children }) => <QueryClientProvider client={qc}>{children}</QueryClientProvider> })`.
    - For URL state mocks: copy the `vi.mock('next/navigation', ...)` block from `use-url-state.test.ts` and EXTEND it with `getAll: (k: string) => mockParams.getAll(k)` and `append: (k, v) => mockParams.append(k, v)`.
    - Mock `fetch` (`vi.spyOn(global, 'fetch').mockImplementation(...)`) for 401/403/200 paths in the mutation test.

    DO NOT IMPLEMENT the hooks or queries themselves. Each test should import its target file — the import will fail because the file doesn't exist yet. That import failure is the RED signal.

    Commit: `test(11-02): RED frontend hook + query + mutation tests for Phase 11`.
  </action>
  <verify>
    <automated>cd /Users/chemencedji/Desktop/getvul/frontend && npx vitest run --reporter=dot src/hooks/use-url-state-list.test.ts src/lib/queries/use-query-errors.test.tsx src/lib/queries/use-vulnerabilities.test.tsx src/lib/mutations/use-create-ticket.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - 4 files exist at the exact paths listed in `<files>`
    - `grep -c "describe\\|it(" frontend/src/hooks/use-url-state-list.test.ts` ≥ 7
    - `grep -c "describe\\|it(" frontend/src/lib/queries/use-query-errors.test.tsx` ≥ 6
    - `grep -c "describe\\|it(" frontend/src/lib/queries/use-vulnerabilities.test.tsx` ≥ 5
    - `grep -c "describe\\|it(" frontend/src/lib/mutations/use-create-ticket.test.tsx` ≥ 5
    - `grep 'Session expired during mutation' frontend/src/lib/mutations/use-create-ticket.test.tsx` returns a match (BL-06 verbatim)
    - `grep 'allowed.includes\\|XSS\\|clamp\\|garbage' frontend/src/hooks/use-url-state-list.test.ts` returns ≥ 1 match (XSS clamp test)
    - Running vitest on these 4 files exits non-zero (RED — implementations don't exist yet)
  </acceptance_criteria>
  <done>4 RED test files committed; vitest reports import errors / module-not-found for each target hook/query/mutation file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 11-02-03: Scaffold 4 RED test files for state primitives (with axe coverage)</name>
  <files>frontend/src/components/states/skeleton-table.test.tsx, frontend/src/components/states/empty-state.test.tsx, frontend/src/components/states/partial-failure-banner.test.tsx, frontend/src/components/states/per-source-status-strip.test.tsx</files>
  <read_first>
    - frontend/src/components/ui/card.test.tsx (canonical compound-component test pattern — assertions on `.Header`, `.Body`, `.Footer` subcomponents)
    - frontend/src/components/ui/toast.test.tsx (axe usage pattern)
    - .planning/phases/11-vulnerabilities-state-patterns/11-RESEARCH.md §Code Examples "Sunset-tokenized SkeletonTable" — column shapes
    - .planning/phases/11-vulnerabilities-state-patterns/11-RESEARCH.md §Pattern 4 "Compound component primitive (D-S-02 EmptyState)" — slot pattern
    - .planning/phases/11-vulnerabilities-state-patterns/11-RESEARCH.md §Pattern 7 "Per-source status strip composition" — aria-live="polite"
    - .planning/phases/11-vulnerabilities-state-patterns/11-CONTEXT.md §D-S-01..07
    - .claude/skills/sketch-findings-getvul/references/state-patterns.md (lines 14-87 loading + 89-152 empty + 154-238 error)
  </read_first>
  <behavior>
    **`skeleton-table.test.tsx`** — D-S-01 + axe:
    - Test 1: renders N rows when `rows={N}` (N=8 default; N=5 honored)
    - Test 2: renders one cell per column descriptor with width matching `columns[i].width`
    - Test 3: `kind="pill"` renders rounded-full pill chrome (className contains `rounded-full`); `kind="mono"` renders rounded rect (contains `rounded` but not `rounded-full`); `kind="badge"` and `kind="text"` likewise
    - Test 4: shimmer animation gated by `motion-safe:animate-shimmer` — verify the className string contains `motion-safe:animate-shimmer` literally (the CSS gate is project-wide in globals.css; test asserts the className, not computed style)
    - Test 5: axe — `expect(await axe(container)).toHaveNoViolations()` on a 4-column 5-row example
    - Test 6: `aria-busy="true"` attribute present on the wrapping element (loading state ARIA)
    - Test 7: `aria-label` includes "Loading" verbiage (screen-reader announces it's loading)

    **`empty-state.test.tsx`** — D-S-02 + axe + D-S-07:
    - Test 1: `<EmptyState>` renders with `role="status"` + `aria-live="polite"` on the root
    - Test 2: `<EmptyState.Title>` renders as a heading (h2 or h3 — assert by role)
    - Test 3: `<EmptyState.Body>` renders body text inside a `<p>`
    - Test 4: `<EmptyState.Actions>` renders an action group with flex layout (className contains `flex`)
    - Test 5: `<EmptyState.Suggestion>` renders the violet-accented hint chrome (className contains `bg-violet-soft` and `text-violet`; assert via container.querySelector)
    - Test 6: compound composition — rendering all 5 sub-pieces produces a single accessible structure with sr-friendly hierarchy
    - Test 7: axe — `toHaveNoViolations()` on the full filtered-zero variant from RESEARCH §Pattern 4
    - Test 8: optional Suggestion — omitting `<EmptyState.Suggestion>` still renders without errors
    - Test 9: copy-voice — when consumers pass the text `Nothing matches all 5 filters` etc., it renders verbatim (no `Welcome` / `Please` injected by the primitive)

    **`partial-failure-banner.test.tsx`** — D-S-03 + axe + hybrid mode:
    - Test 1: props mode — given `errors={[{code:503,requestId:'req_8f2a91c',message:'Tenable unreachable'}]}`, renders a banner with `role="alert"`, mono-font HTTP code, mono-font request ID
    - Test 2: props mode — `onRetry` button fires the callback on click
    - Test 3: default mode — given `watchKeys={[['vulnerabilities']]}` and a QueryClient with a failed vulnerabilities query, the banner appears
    - Test 4: default mode returns `null` (renders nothing) when no errors present (TanStack queryCache happy state)
    - Test 5: copy includes the partial-data phrasing per state-patterns.md ("Tenable connector is unreachable" pattern — copy is consumer-provided via `source` prop, banner format is fixed)
    - Test 6: amber not red — assert className contains `bg-amber-soft` or `border-amber` (not `bg-danger-soft`) — partial failure is degraded, not down (state-patterns.md "Amber, not red")
    - Test 7: axe — `toHaveNoViolations()` on the canonical 503-Tenable variant
    - Test 8: stale-row source surfaced — banner exposes `data-failed-sources` attribute or named export consumers can read to drive D-V-04 stale-row tinting (banner has the info; consumer surfaces it on rows)

    **`per-source-status-strip.test.tsx`** — D-V-02 + axe + aria-live:
    - Test 1: renders one chip per connector returned by `useConnectors()` (mock the hook)
    - Test 2: chip shows connector type (mono font, e.g., `QUALYS`) + per-source count from `facets` prop
    - Test 3: state-aware coloring — `last_sync_status === 'ok'` → `bg-success-soft text-success`; `'failed'` → `bg-danger-soft text-danger`; `'syncing'` → `bg-pink-soft text-pink`; other/null → `bg-surface-2 text-text-muted`
    - Test 4: `role="status"` + `aria-live="polite"` on the wrapping element (D-S-07)
    - Test 5: returns null while connectors query is pending (other components cover the loading state)
    - Test 6: returns null on connectors query error (PartialFailureBanner covers the error state)
    - Test 7: axe — `toHaveNoViolations()` with a mixed status set (ok/failed/syncing)
  </behavior>
  <action>
    Create all 4 files. Patterns:

    - `axe` import: `import { axe } from 'vitest-axe';`
    - Add typed extension to `expect`: handled by `vitest.setup.ts` which already calls `expect.extend(axeMatchers)`.
    - For `per-source-status-strip.test.tsx`, mock `useConnectors` via `vi.mock('@/lib/queries/use-connectors', ...)` — the hook lands in Wave 1 Plan 11-03.
    - For `partial-failure-banner.test.tsx`, the QueryClient wrapper approach mirrors `use-stats.test.tsx`.

    Each test file imports its target component file — the import will fail (RED). That's the contract for Wave 1.

    Commit: `test(11-02): RED state-primitive tests with axe coverage`.
  </action>
  <verify>
    <automated>cd /Users/chemencedji/Desktop/getvul/frontend && npx vitest run --reporter=dot src/components/states/*.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - 4 files exist under `frontend/src/components/states/`
    - Each file imports `axe` from `vitest-axe` (`grep "from 'vitest-axe'" frontend/src/components/states/*.test.tsx | wc -l` ≥ 4)
    - Each file has at least one `toHaveNoViolations()` assertion (`grep -c 'toHaveNoViolations' frontend/src/components/states/*.test.tsx | awk -F: '{s+=$2} END{print s}'` ≥ 4)
    - `grep -c "describe\\|it(" frontend/src/components/states/skeleton-table.test.tsx` ≥ 7
    - `grep -c "describe\\|it(" frontend/src/components/states/empty-state.test.tsx` ≥ 9
    - `grep -c "describe\\|it(" frontend/src/components/states/partial-failure-banner.test.tsx` ≥ 8
    - `grep -c "describe\\|it(" frontend/src/components/states/per-source-status-strip.test.tsx` ≥ 7
    - `grep 'role="status"\\|aria-live="polite"\\|role="alert"' frontend/src/components/states/*.test.tsx | wc -l` ≥ 4 (D-S-07 + D-S-03)
    - Running vitest on these 4 files exits non-zero (RED — no implementations exist yet)
  </acceptance_criteria>
  <done>4 RED state-primitive test files committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 11-02-04: Scaffold 5 RED test files for vuln-page components + page integration</name>
  <files>frontend/src/components/vulnerabilities/chip-bar.test.tsx, frontend/src/components/vulnerabilities/vuln-table.test.tsx, frontend/src/components/vulnerabilities/drill-panel.test.tsx, frontend/src/components/vulnerabilities/drill-panel-mobile.test.tsx, frontend/src/components/vulnerabilities/view-toggle.test.tsx, frontend/src/app/(authed)/dashboard/vulnerabilities/page.test.tsx</files>
  <read_first>
    - frontend/src/hooks/use-url-state.test.ts (URL mock pattern — reuse + extend with getAll)
    - frontend/src/components/dashboard/hero.test.tsx (canonical component-integration test pattern with TanStack QueryClient wrapper)
    - frontend/src/components/dashboard/top5-card.test.tsx (link assertion + microcopy pattern)
    - .planning/phases/11-vulnerabilities-state-patterns/11-RESEARCH.md §Pattern 6 "Keyboard-navigable table" — Enter/Space/↑/↓/Home/End behavior
    - .planning/phases/11-vulnerabilities-state-patterns/11-RESEARCH.md §Pattern 5 "vaul mobile bottom-sheet" — controlled `Drawer.Root` with URL-driven open
    - .planning/phases/11-vulnerabilities-state-patterns/11-CONTEXT.md §D-F-01 (250ms debounce), §D-T-01..04 (sort/keyboard/sticky/pagination), §D-P-01..06 (drill-panel close behaviors + focus mgmt), §D-V-01 (view toggle), §D-V-04 (stale-row)
    - .claude/skills/sketch-findings-getvul/sources/003-vulnerabilities-sunset/index.html (variant C — layout fidelity)
    - .claude/skills/sketch-findings-getvul/references/visual-language.md lines 1-100 (severity glyphs + pills)
  </read_first>
  <behavior>
    **`chip-bar.test.tsx`** — UX-03-01 + D-F-01/02/03/05:
    - Test 1: renders search input on the left, severity chips, source chips, optional violet `★ Today's triage` saved-filter pill, `Clear all` link
    - Test 2: chip click is synchronous — clicking `<chip>Critical</chip>` immediately calls the URL setter (no debounce)
    - Test 3: search input debounces 250ms — typing `log4j` and waiting <250ms before assertions shows no URL update; waiting ≥250ms then asserts the URL setter fired once
    - Test 4: chip counts surface from `facets` prop — `Critical · 12` rendered when facets.severity.CRITICAL === 12
    - Test 5: source chips list is rendered from `facets.source` (not hardcoded) — pass `facets.source = {QUALYS: 287}` and assert only one source chip
    - Test 6: `Clear all` clears both chips AND search (planner discretion → clear-all wipes both per CONTEXT.md "Default leaning")
    - Test 7: saved-filter pill renders ONLY when `savedFilters.length > 0` (D-F-04 read-only — pill applies first saved filter)

    **`vuln-table.test.tsx`** — UX-03-02 + UX-07-03 keyboard + D-V-04 stale-row:
    - Test 1: renders 7 columns with the correct headers (Severity, CVE, Title/Product, Asset, CVSS, Status, SLA)
    - Test 2: severity column renders both the colored glyph (■▲◆○) AND the pill label
    - Test 3: CVE column uses mono font (className contains `font-mono`)
    - Test 4: KEV badge rendered next to status when `row.cisa_kev === true`
    - Test 5: SLA column right-aligned mono with color band (overdue → danger; <24h → amber; >24h → success)
    - Test 6: ArrowDown moves focus row-to-row (canonical from RESEARCH Pattern 6 — verify last test from research is in here)
    - Test 7: ArrowUp moves focus up
    - Test 8: Home jumps to first row; End jumps to last row
    - Test 9: Enter/Space on focused row fires `onRowOpen(row.cve_id ?? row.id)`
    - Test 10: row click also fires `onRowOpen`
    - Test 11: stale-row tinting — when `failedSources={['TENABLE']}` and a row has `source === 'TENABLE'`, the `<tr>` carries `data-stale="true"` and className includes `data-[stale=true]:bg-amber-soft` (or computed amber class)
    - Test 12: sticky header — `<thead>` className includes `sticky top-0 bg-surface`
    - Test 13: sortable column headers — clicking the Severity / CVE / CVSS / SLA header cycles asc → desc → clear; assert the prop callback fires with `('cve_id','asc')` then `('cve_id','desc')` then `(null,null)` or whatever the locked contract is

    **`drill-panel.test.tsx`** — UX-03-03 + D-P-01/02/05/06:
    - Test 1: renders the 7 sections in order (drill header / CVSS / Affected hosts / Description / Remediation / Activity / Actions) — assert via getAllByRole('heading') or section labels
    - Test 2: close × button closes the panel (URL setter called with `open=null` / removed param)
    - Test 3: Esc key closes the panel
    - Test 4: clicking outside the panel closes (clickaway test)
    - Test 5: row-swap behavior — providing a new `cveId` prop swaps panel content (no close)
    - Test 6: focus on open → goes to the close button (D-P-06; assert via `document.activeElement === closeButton`)
    - Test 7: Tab cycles through panel interactives; Shift-Tab from close returns focus to originating row (verify via tabbable order assertion)
    - Test 8: actions section has Snooze (secondary) + Create ticket (primary `btn-cta` variant) buttons
    - Test 9: 420px width on desktop — assert `className` contains `w-[420px]` or computed style is `420px`
    - Test 10: URL-encoded panel state — render with `?cve=CVE-2024-3094&open=drill` query string, panel renders open with that CVE's content

    **`drill-panel-mobile.test.tsx`** — UX-03-06 + D-P-03:
    - Test 1: at viewport <900px (mocked via `vi.spyOn(window, 'matchMedia').mockReturnValue({ matches: true, ... })`), the component renders a vaul `Drawer.Root` (assert presence of `[role="dialog"]` or vaul's data attributes)
    - Test 2: at viewport ≥900px, returns `null` or renders nothing (desktop branch covers it)
    - Test 3: open state driven by URL — `?cve=...&open=drill` opens the drawer; removing `open=drill` closes
    - Test 4: swipe-to-close + Esc both close (vaul handles natively — assert by firing the close handler and verifying URL setter called)
    - Test 5: nested ConfirmModal — clicking "Create ticket" inside the drawer opens a `Drawer.NestedRoot` (D-P-04 mobile path)
    - Test 6: smoke test — at <900px the same `<DrillContent>` shared with desktop renders inside vaul without crashing

    **`view-toggle.test.tsx`** — UX-03-05 + D-V-01:
    - Test 1: renders a 2-segment pill (By CVE / By Host)
    - Test 2: active segment matches current `?group` URL state
    - Test 3: clicking inactive segment fires URL setter with the new group value
    - Test 4: switching does NOT clear filter chips — other URL params (severity, source, search, etc.) survive the toggle (assert the setter for `group` is called but other params remain in mockParams)
    - Test 5: keyboard — Tab focuses each segment; Enter/Space activates

    **`page.test.tsx`** — page-level integration:
    - Test 1: page renders chip-bar + view-toggle + table on initial load (no panel)
    - Test 2: URL `?cve=CVE-2024-3094&open=drill` pre-opens the drill panel with that CVE
    - Test 3: clicking a row opens the panel + updates URL to `?cve={row.cve}&open=drill`
    - Test 4: loading state — when `useVulnerabilities()` `isPending`, the page renders `<SkeletonTable rows=8 columns=[...]>` with 7-column shape matching the real table
    - Test 5: empty-filtered state — when `data.items.length === 0` AND active filters exist, page renders `<EmptyState>` with the 3-tier CTAs (Clear all / Include Medium / Search all sources) and the violet lightbulb suggestion
    - Test 6: partial-failure — when a query in the page's watchKeys errors, `<PartialFailureBanner>` renders at top of page; PerSourceStatusStrip renders the connector status row; stale-row tinting flows through to rows whose source matches failed sources
    - Test 7: total-failure (UX-S-04) — when the primary list query errors AND there are no rows to show, page renders the `<EmptyState>` shell with retry CTAs (uses S-02 shell per VALIDATION.md row)
    - Test 8: tab title — when `data.total > 0`, document.title is `(N) Vulnerabilities · GetVul` (Phase 10 `useDocumentTitle` reused; consumer follows Phase 10 D-Tab-01 convention)
  </behavior>
  <action>
    Create all 6 files. Patterns:

    - Mock `next/navigation` using the canonical block from `use-url-state.test.ts`, extended with `getAll`.
    - Mock TanStack hooks (`useVulnerabilities`, `useVulnerabilityDetail`, etc.) via `vi.mock('@/lib/queries/use-vulnerabilities', ...)` — these don't exist yet, so the vi.mock will succeed but the implementation file will fail to import.
    - For `drill-panel-mobile.test.tsx`, mock matchMedia BEFORE the test using `vi.spyOn(window, 'matchMedia').mockImplementation((q) => ({ matches: q.includes('900'), ... }))`.
    - For the page-level test, render the page component and provide a `QueryClientProvider` wrapper with seeded query data via `qc.setQueryData(['vulnerabilities','list',...], fixture)`.

    Each test imports its target component — those imports fail (RED).

    Commit: `test(11-02): RED component + page-level tests for /vulnerabilities`.
  </action>
  <verify>
    <automated>cd /Users/chemencedji/Desktop/getvul/frontend && npx vitest run --reporter=dot src/components/vulnerabilities/*.test.tsx 'src/app/(authed)/dashboard/vulnerabilities/page.test.tsx' 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - 6 files exist at the exact paths in `<files>`
    - `grep -c "describe\\|it(" frontend/src/components/vulnerabilities/chip-bar.test.tsx` ≥ 7
    - `grep -c "describe\\|it(" frontend/src/components/vulnerabilities/vuln-table.test.tsx` ≥ 13
    - `grep -c "describe\\|it(" frontend/src/components/vulnerabilities/drill-panel.test.tsx` ≥ 10
    - `grep -c "describe\\|it(" frontend/src/components/vulnerabilities/drill-panel-mobile.test.tsx` ≥ 6
    - `grep -c "describe\\|it(" frontend/src/components/vulnerabilities/view-toggle.test.tsx` ≥ 5
    - `grep -c "describe\\|it(" 'frontend/src/app/(authed)/dashboard/vulnerabilities/page.test.tsx'` ≥ 8
    - `grep 'ArrowDown\\|ArrowUp\\|Home\\|End\\|Enter\\|Space' frontend/src/components/vulnerabilities/vuln-table.test.tsx | wc -l` ≥ 4 (keyboard coverage)
    - `grep 'Drawer\\|vaul' frontend/src/components/vulnerabilities/drill-panel-mobile.test.tsx | wc -l` ≥ 2
    - `grep 'data-stale\\|failedSources' frontend/src/components/vulnerabilities/vuln-table.test.tsx | wc -l` ≥ 1 (D-V-04)
    - Running vitest on these 6 files exits non-zero (RED — no implementations exist)
  </acceptance_criteria>
  <done>6 RED component + page-level test files committed; the 14-file Wave 0 test inventory in 11-VALIDATION.md is complete.</done>
</task>

</tasks>

<verification>
- `grep -l '"vaul": "1\\.1\\.2"' frontend/package.json` returns the file path
- `find frontend/src -name "*.test.tsx" -o -name "*.test.ts" -newer .planning/phases/11-vulnerabilities-state-patterns/11-CONTEXT.md | wc -l` returns ≥ 14 NEW test files
- `npx vitest run --reporter=dot 2>&1 | grep -E 'FAIL|failed'` returns ≥ 14 failures (RED state)
- 4 axe assertions present (one per state primitive)
</verification>

<success_criteria>
14 RED test files match the VALIDATION.md inventory verbatim. vaul pinned. Tailwind shimmer aliased. Wave 1+ can read the test specs as the contract for implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/11-vulnerabilities-state-patterns/11-02-SUMMARY.md` listing all 14 test files + their behavior signatures + how Wave 1 plans consume them as GREEN targets.
</output>
