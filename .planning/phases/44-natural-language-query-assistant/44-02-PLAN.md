---
phase: 44-natural-language-query-assistant
plan: 02
type: execute
wave: 2
depends_on: [44-01]
autonomous: true
requirements: [NLQ-01]
files_modified:
  - backend/app/vulnerabilities/schemas.py
  - backend/app/vulnerabilities/service.py
  - backend/app/assets/schemas.py
  - backend/app/assets/service.py
  - backend/app/ticketing/schemas.py
  - backend/app/ai/schemas.py
  - backend/app/ai/prompt_builder.py
  - backend/app/ai/query_assistant.py
  - backend/tests/test_vulnerabilities_filters.py
  - backend/tests/test_ai_query_stream.py

must_haves:
  truths:
    - "VulnerabilityFilter gains asset_internet_facing and sla_breached; setting asset_internet_facing filters via a join to Asset.internet_facing WITHOUT double-joining Asset on the count OR data query path (Pitfall 1)."
    - "sla_breached maps directly to the stored Vulnerability.sla_breached derived-mirror column (refreshed ≤60s by run_sla_tier_pass); it is NOT recomputed live (Pitfall 6)."
    - "The north-star question 'which internet-facing hosts have an unremediated KEV older than 30 days?' resolves to a single vulnerabilities-entity filter (cisa_kev=true, status=[OPEN,IN_PROGRESS], age_days_min=30, asset_internet_facing=true) — no model-side multi-tool join (D-02)."
    - "The assets entity executes via list_assets and the tickets entity via list_tickets; 'Open tickets for asset prod-db-01' resolves the hostname to a UUID server-side via _resolve_hostname before calling list_tickets (never a model-emitted UUID; D-01/D-02)."
    - "The vulnerabilities translator never emits a hostname predicate (VulnFilterInput has no asset_hostname; extra=forbid rejects one), so the D-17 'Open in Vulnerabilities' deep-link can never silently drop a predicate (W3; D-04/D-17 no-silent-narrowing); host-scoped questions route to the tickets/assets entity."
    - { statement: "An unresolvable 'asset X' hostname yields a zero-results answer (well-formed, safe-schema-compliant, matched nothing), NOT a D-14 refusal (RESEARCH Pattern 3 / Open Question 2 — NLQ-01 empty edge probe).", verification: backstop }
    - "AssetFilter gains internet_facing (native column, no join); TicketQueryFilter is a NEW NLQ-only extra=forbid translation wrapper that maps onto list_tickets' existing loose kwargs — list_tickets' own signature is NOT changed."
  prohibitions:
    - "must never fabricate a count or a row not in the executed result set"
  artifacts:
    - path: backend/app/vulnerabilities/schemas.py
      provides: "VulnerabilityFilter.asset_internet_facing, VulnerabilityFilter.sla_breached"
      contains: "asset_internet_facing"
    - path: backend/app/ticketing/schemas.py
      provides: "TicketQueryFilter (NLQ-only translation wrapper, extra=forbid)"
      contains: "class TicketQueryFilter"
    - path: backend/app/ai/query_assistant.py
      provides: "assets + tickets entity execution branches; hostname resolution wired into ticket path"
      contains: "list_tickets"
  key_links:
    - from: backend/app/vulnerabilities/service.py
      to: backend/app/assets/models.py
      via: "_apply_filters join to Asset for asset_internet_facing (single join, no duplication)"
      pattern: "internet_facing"
    - from: backend/app/ai/query_assistant.py
      to: backend/app/ticketing/service.py
      via: "list_tickets(db, tenant_id, status=..., asset_id=resolved_uuid)"
      pattern: "list_tickets"
---

<objective>
EXPANSION 1 (backend query surface): add the D-03 additive predicates that the representative query set needs
but the existing filters don't express, and fill in the assets + tickets entity execution branches the tracer
left guarded. This makes the north-star multi-predicate question and all four UI-SPEC starter questions answerable.

Purpose: NLQ-01 — complete the core-three (D-05) query coverage on the proven D-01 contract.
Output: VulnerabilityFilter.asset_internet_facing + sla_breached, AssetFilter.internet_facing, TicketQueryFilter,
and real assets/tickets branches in _run_query_stream with server-side hostname resolution.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-natural-language-query-assistant/44-CONTEXT.md
@.planning/phases/44-natural-language-query-assistant/44-RESEARCH.md
@.planning/phases/44-natural-language-query-assistant/44-PATTERNS.md
@.planning/phases/44-natural-language-query-assistant/44-01-SUMMARY.md

<interfaces>
<!-- Verified this session. -->
D-03 PREDICATE INVENTORY (44-RESEARCH.md): most north-star predicates ALREADY exist on VulnerabilityFilter
(cisa_kev §schemas.py:143, status, age_days_min §146, exploit_available §142, severity §134). Only TWO are new:
  - asset_internet_facing → Asset.internet_facing exists on the model (assets/models.py:118) but no vuln-side predicate
  - sla_breached → stored Vulnerability.sla_breached derived-mirror column (sla_tier_service.py:194,230-233; refreshed ≤60s scheduler.py:422). Do NOT use the live resolve_state_for_vuln computation.

_apply_filters existing clause style (backend/app/vulnerabilities/service.py:44-113), e.g.:
  `if filters.cisa_kev is not None: query = query.where(Vulnerability.cisa_kev == filters.cisa_kev)`  (§L94-95)
Pitfall 1 (DOUBLE-JOIN): list_vulnerabilities ALREADY does .outerjoin(Asset, Vulnerability.asset_id==Asset.id)
for asset_hostname (§service.py:135, §301, §736, §938) AFTER _apply_filters runs; the COUNT-query path never
joins Asset today. Adding a filter-driven Asset join inside _apply_filters risks a duplicate join on the data path.
Resolution: verify SQLAlchemy 2.0 dedup empirically (a test asserting the query runs); the safe fallback is to
have _apply_filters own the Asset join unconditionally (move list_vulnerabilities' outerjoin into _apply_filters).

list_tickets loose kwargs (backend/app/ticketing/service.py:709-721): provider, status, page, page_size, asset_id,
severity, sla, search, source. Its severity/sla filters ARE SQL WHERE/HAVING (docstring is STALE — Pitfall 4);
exact count is safe. Do NOT refactor list_tickets — write TicketQueryFilter as a translation wrapper above it.
AssetFilter (assets/schemas.py:68-76): hostname, os_name, asset_type, cloud_provider, source, risk_score_min,
device_category, search — add internet_facing (native Asset column, one where-clause in assets/service.py:16-41).

From Plan 01 (query_assistant.py): _run_query_stream currently executes the vulnerabilities branch and yields a
guarded {"type":"refuse"} for assets/tickets. VulnFilterInput/AssetFilterInput/TicketFilterInput + NlqFilterResponse
exist in ai/schemas.py; FEW_SHOT_QUERY_TRANSLATE currently has vulnerabilities examples only.
TYPE FLOW (W4): the model emits NlqFilterResponse.ticket_filter as a TicketFilterInput (ai/schemas.py); this plan adds
TicketQueryFilter (ticketing/schemas.py) and converts TicketFilterInput -> TicketQueryFilter in the orchestrator before
list_tickets — the two are a producer/consumer pair, never interchangeable.
</interfaces>
</context>

<artifacts_this_phase_produces>
See 44-01-PLAN.md `<artifacts_this_phase_produces>` for the full phase-wide registry. THIS plan creates:
VulnerabilityFilter.asset_internet_facing, VulnerabilityFilter.sla_breached, AssetFilter.internet_facing,
ticketing/schemas.py TicketQueryFilter; adds asset_internet_facing + sla_breached fields to VulnFilterInput and
internet_facing to AssetFilterInput (ai/schemas.py); extends FEW_SHOT_QUERY_TRANSLATE with asset/ticket examples;
adds assets + tickets execution branches to _run_query_stream (converting TicketFilterInput -> TicketQueryFilter — W4).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-03 additive predicates (vuln join + sla_breached, asset native, ticket wrapper)</name>
  <read_first>
    - backend/app/vulnerabilities/schemas.py §L127-176 (VulnerabilityFilter existing fields)
    - backend/app/vulnerabilities/service.py §L44-113 (_apply_filters clauses), §L135/301/736/938 (existing Asset outerjoins — Pitfall 1)
    - backend/app/assets/schemas.py §L68-76, backend/app/assets/service.py §L16-41
    - backend/app/ticketing/schemas.py §L138 TicketRuleConditions (file-placement convention)
    - 44-RESEARCH.md §Pitfall 1 + §Pitfall 6 + D-03 Predicate Inventory
  </read_first>
  <behavior>
    - test_filter_asset_internet_facing: list_vulnerabilities with VulnerabilityFilter(asset_internet_facing=True) returns only vulns whose asset is internet-facing, exact count correct, query executes without a duplicate-Asset-join InvalidRequestError on BOTH the count and data paths.
    - test_filter_sla_breached: VulnerabilityFilter(sla_breached=True) filters on the stored Vulnerability.sla_breached column (no live recompute).
    - test_asset_internet_facing_filter: list_assets with AssetFilter(internet_facing=True) returns only internet-facing assets.
    - test_ticket_query_filter_forbids_extra: TicketQueryFilter rejects an unknown field (extra="forbid").
  </behavior>
  <action>
    Add asset_internet_facing: bool|None = None and sla_breached: bool|None = None to VulnerabilityFilter. In
    _apply_filters add `if filters.sla_breached is not None: query = query.where(Vulnerability.sla_breached == filters.sla_breached)`
    and the asset_internet_facing predicate via a join to Asset — FIRST verify empirically whether SQLAlchemy 2.0 dedups
    the join against list_vulnerabilities' existing outerjoin on both the count and data paths; if it raises/duplicates,
    apply the safe fallback (move the Asset join into _apply_filters unconditionally and drop the redundant outerjoin in
    list_vulnerabilities). Add internet_facing: bool|None = None to AssetFilter + the matching where-clause in
    assets/service.py _apply_filters (native column, no join). Add class TicketQueryFilter(extra="forbid") to
    ticketing/schemas.py (status: str|None, asset_hostname: str|None — resolved server-side, never a UUID) — do NOT
    change list_tickets' signature. Add/extend backend/tests/test_vulnerabilities_filters.py covering the four behaviors.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=test-key-please-ignore-000000000000000000000000000= JWT_SECRET_KEY=test pytest tests/test_vulnerabilities_filters.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "asset_internet_facing" backend/app/vulnerabilities/schemas.py backend/app/vulnerabilities/service.py returns matches in both
    - grep -n "sla_breached" backend/app/vulnerabilities/service.py shows a where-clause on Vulnerability.sla_breached (not a resolve_state_for_vuln call)
    - backend/app/ticketing/schemas.py contains `class TicketQueryFilter` with model_config extra="forbid"
    - test_filter_asset_internet_facing passes on BOTH count and data paths (no InvalidRequestError)
    - pytest tests/test_vulnerabilities_filters.py exits 0
  </acceptance_criteria>
  <done>Both new vuln predicates work without a double-join; asset internet_facing and TicketQueryFilter added; list_tickets untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: assets + tickets entity branches + hostname resolution in the orchestrator</name>
  <read_first>
    - backend/app/ai/query_assistant.py (Plan 01 — _run_query_stream vuln branch + guarded refuse; _resolve_hostname)
    - backend/app/ai/schemas.py (VulnFilterInput / AssetFilterInput / TicketFilterInput)
    - backend/app/ai/prompt_builder.py FEW_SHOT_QUERY_TRANSLATE (extend with asset/ticket examples)
    - backend/app/ticketing/service.py §L709-861 (list_tickets kwargs + real SQL severity/sla), backend/app/assets/service.py §L44 list_assets
    - 44-RESEARCH.md §Architecture Patterns Pattern 3 (hostname resolution) + 44-PATTERNS.md TicketQueryFilter section
  </read_first>
  <behavior>
    - test_assets_entity_branch: an assets question executes list_assets with the session tenant_id and streams interpreted→results→narrate.
    - test_tickets_entity_branch: "Open tickets for asset prod-db-01" resolves the hostname to a UUID via _resolve_hostname, then calls list_tickets(status=..., asset_id=<uuid>); the model never supplies the UUID.
    - test_unresolvable_hostname_is_zero_results: an unknown hostname yields a results event with total=0 (zero-results), NOT a refuse event.
  </behavior>
  <action>
    Add asset_internet_facing and sla_breached to VulnFilterInput (W2 — the vuln-side exposure field is asset_internet_facing,
    matching VulnerabilityFilter.asset_internet_facing and the buildNlqDeepLink param contract; internet_facing is the
    AssetFilterInput/AssetFilter name only) and internet_facing to AssetFilterInput in ai/schemas.py so the translator can
    emit them. Extend FEW_SHOT_QUERY_TRANSLATE with representative assets and tickets examples (incl. the hostname-named
    ticket question) and extend SYSTEM_PROMPT_QUERY_TRANSLATE's allowed-field catalog for all three entities. W3 — the vuln
    entity's allowed-field catalog and few-shots MUST NOT include or ever emit a hostname field (VulnFilterInput has no
    asset_hostname; extra=forbid rejects one); host-scoped questions map to the tickets/assets entity, keeping the D-17
    vuln deep-link param set tight and free of any droppable predicate.
    In _run_query_stream replace the guarded assets/tickets placeholder with real branches: entity=="assets" → map
    AssetFilterInput → AssetFilter, execute list_assets(db, tenant_id, filter, PaginationParams(page=1,page_size=10));
    entity=="tickets" → CONVERT the model-emitted NlqFilterResponse.ticket_filter (TicketFilterInput, from ai/schemas.py —
    the ai-side contract) into a TicketQueryFilter (ticketing/schemas.py — the ticketing-side extra=forbid validated
    wrapper that decouples ai/ from ticketing internals), then if its asset_hostname is present call _resolve_hostname
    first (None → short-circuit to a total=0 results event, NOT a refuse), then list_tickets(db, tenant_id,
    status=query_filter.status, asset_id=resolved). This TicketFilterInput→TicketQueryFilter conversion is the single
    documented type flow (W4); TicketFilterInput is what the model emits, TicketQueryFilter is what the ticketing layer
    validates/consumes — they are NOT used interchangeably. Emit interpreted→results→narrate identically to the vuln
    branch; tenant_id always from session. Extend test_ai_query_stream.py with the three behaviors.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=test-key-please-ignore-000000000000000000000000000= JWT_SECRET_KEY=test pytest tests/test_ai_query_stream.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "list_assets\|list_tickets" backend/app/ai/query_assistant.py returns matches (real branches, not placeholders)
    - test_tickets_entity_branch proves _resolve_hostname runs before list_tickets and the model output never supplies asset_id
    - test_unresolvable_hostname_is_zero_results proves total=0 rather than a refuse event
    - pytest tests/test_ai_query_stream.py exits 0
  </acceptance_criteria>
  <done>All three entities answer end-to-end; hostname→UUID is deterministic and server-side; unresolvable hosts are zero-results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| model output → query execution | Emitted filter (incl. new predicates + hostname) untrusted until validated and resolved server-side |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02 | Information Disclosure | assets/tickets branches (cross-tenant) | mitigate | list_assets/list_tickets called with the session tenant_id; _resolve_hostname is tenant-scoped; no tenant_id in any *FilterInput schema |
| T-44-07 | Tampering | model-emitted hostname/asset_id | mitigate | Model emits a hostname string only; _resolve_hostname deterministically maps it within the tenant; a model-supplied UUID is never trusted |
| T-44-08 | Info Disclosure | stale sla_breached mirror | accept | ≤60s staleness window on an ad-hoc analyst query is acceptable — same tradeoff Phase 40 alerting already accepts (Pitfall 6) |
| T-44-04 | Tampering | free-form SQL | accept (structurally impossible) | Only Pydantic filter objects executed via parameterized ORM list_* funcs |
ASVS L1; block on high. No high-severity threat left unmitigated.
</threat_model>

<verification>
- `cd backend && ENCRYPTION_KEY=... JWT_SECRET_KEY=test pytest tests/test_vulnerabilities_filters.py tests/test_ai_query_stream.py -q` green
- Existing vuln/asset/ticket list suites unregressed: `pytest tests/test_vulnerabilities_service.py tests/test_assets_service.py tests/test_ticketing_service.py -q` green (additive optional filter fields)
- No Alembic migration produced (reuses existing Asset join + stored sla_breached column — schema-gate: no new columns)
</verification>

<success_criteria>
The north-star question and all four UI-SPEC starter questions resolve to a single-entity filter and return grounded results across vulnerabilities/assets/tickets, with server-side hostname resolution and no double-join regression.
</success_criteria>

<output>
After completion, create `.planning/phases/44-natural-language-query-assistant/44-02-SUMMARY.md`.
</output>
