---
phase: 44-natural-language-query-assistant
plan: 03
type: execute
wave: 3
depends_on: [44-01, 44-02]
autonomous: true
requirements: [NLQ-01]
files_modified:
  - frontend/src/lib/ai/use-query-stream.ts
  - frontend/src/components/ai/ai-explanation-section.tsx
  - frontend/src/components/ai/ask/query-box.tsx
  - frontend/src/components/ai/ask/starter-questions.tsx
  - frontend/src/components/ai/ask/interpreted-filter.tsx
  - frontend/src/components/ai/ask/result-table.tsx
  - frontend/src/lib/ai/use-query-stream.test.ts
  - frontend/src/components/ai/ask/result-table.test.tsx

must_haves:
  truths:
    - "use-query-stream.ts POSTs {question} as a JSON body to /api/v1/ai/query with a Bearer token (the existing useExplainStream sends NO body and interpolates a resourceId — Pitfall 7, do not reuse it unchanged)."
    - "The hook's state machine surfaces interpreted and results phases BEFORE any narrative summary_delta, matching the backend D-15 results-first ordering."
    - "result-table.tsx is a THIN wrapper that selects the existing vuln/asset/ticket list-row primitive by entity — it is NOT a new/second table pattern (D-08; sketch-findings warns against a second table)."
    - "The result region shows the '{topN} of {total} total' caption in JetBrains Mono numerals (D-07/UI-SPEC)."
    - "starter-questions renders exactly the 4 curated UI-SPEC chips, click-to-fill, using the EmptyState.Suggestion violet-soft chip chrome verbatim (D-11)."
    - "interpreted-filter renders the 'Interpreted as:' label + mono predicate tokens (e.g. severity=Critical · KEV=true · first_seen>30d · exposure=internet-facing) so a misread is always visible (D-04)."
    - "DegradedCard is exported from ai-explanation-section.tsx so the ask/ components can reuse it verbatim for refusal/budget/safety/configure states (Pitfall 8) — a one-line, zero-behavior-change diff."
    - { statement: "query-box enforces a ~500-char cap with a live counter mirroring CommentInput's char-count-warning; text never clips (UI-SPEC E1 long-text backstop; bounds the NLQ-02 injection/cost surface).", verification: backstop }
    - { statement: "query-box input soft-wraps and grows to a bounded height then scrolls internally rather than clipping (UI-SPEC E1 overflow backstop).", verification: backstop }
    - { statement: "interpreted-filter predicate tokens flex-wrap onto multiple lines rather than truncate or scroll (UI-SPEC E4 overflow backstop).", verification: backstop }
  prohibitions:
    - "must never silently widen or narrow the interpreted filter without showing it"
  artifacts:
    - path: frontend/src/lib/ai/use-query-stream.ts
      provides: "useQueryStream hook (POST body, interpreted/results/summary/done state machine)"
      contains: "export function useQueryStream"
    - path: frontend/src/components/ai/ask/result-table.tsx
      provides: "entity-dispatch thin wrapper over vuln/asset/ticket row primitives"
      contains: "export function"
    - path: frontend/src/components/ai/ask/interpreted-filter.tsx
      provides: "D-04 interpreted-filter summary card"
    - path: frontend/src/components/ai/ask/starter-questions.tsx
      provides: "D-11 curated starter-question chips (exactly 4)"
    - path: frontend/src/components/ai/ask/query-box.tsx
      provides: "bounded free-text question input + Ask CTA"
  key_links:
    - from: frontend/src/lib/ai/use-query-stream.ts
      to: backend/app/api/v1/ai/query.py
      via: "fetch(POST /api/v1/ai/query, body: JSON.stringify({question}))"
      pattern: "api/v1/ai/query"
    - from: frontend/src/components/ai/ask/result-table.tsx
      to: frontend/src/components/vulnerabilities
      via: "entity-dispatch import of existing list-row primitives"
      pattern: "entity"
---

<objective>
EXPANSION 2 (frontend data + presentational layer): the streaming hook and the reusable Ask-page building blocks
that consume the proven SSE contract — a body-carrying use-query-stream hook, the DegradedCard export, and the
four ask/ components (query-box, starter-questions, interpreted-filter, result-table). No page composition yet
(Plan 04) — this plan delivers testable, reuse-heavy pieces.

Purpose: NLQ-01 — render the grounded result set + interpretation with the existing sunset primitives (D-04/D-07/D-08/D-11/D-15).
Output: useQueryStream + exported DegradedCard + 4 ask/ components, each unit-tested.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-natural-language-query-assistant/44-CONTEXT.md
@.planning/phases/44-natural-language-query-assistant/44-UI-SPEC.md
@.planning/phases/44-natural-language-query-assistant/44-PATTERNS.md
@.claude/skills/sketch-findings-getvul/SKILL.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md
@.claude/skills/sketch-findings-getvul/references/visual-language.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md

<interfaces>
<!-- Verified this session. -->
ANALOG use-explain-stream.ts (frontend/src/lib/ai/use-explain-stream.ts, full 142 lines):
  - export type ExplainStreamState §L35 (idle/analyzing/done/error) — structurally reusable; ADD interpreted/results phases
  - type RawSseEvent §L49 (DoneEvent|ErrorEvent|SummaryDeltaEvent|NoKeyEvent) — extend in the NEW file only
  - fetch(`${API_URL}/api/v1/ai/explain-${resourceType}/${resourceId}`, {method:'POST', headers:{Authorization}}) §L70 — NO body param (Pitfall 7)
  - res.body.getReader() SSE frame-parsing loop §L84-138 — reuse essentially verbatim (the valuable ~50 lines)
NEW SSE kinds to handle (Plan 01/02 backend): {type:'interpreted', entity, filter}, {type:'results', rows, total},
  plus existing summary_delta/done/error/no_key/refuse.
ANALOG DegradedCard (ai-explanation-section.tsx §L47, currently module-private — ADD export): props
  {variant:'neutral'|'amber'|'danger', heading, body, action?, icon='sparkles'}. AnalyzingIndicator §L108 ALREADY exported.
ANALOG starter chips: EmptyState.Suggestion (frontend/src/components/states/empty-state.tsx) violet-soft chip chrome.
ANALOG result rows: existing VulnTable / asset / ticket list-row primitives (frontend/src/components/{vulnerabilities,assets,tickets}/).
ANALOG query-box: CommentInput char-cap + counter convention (no exact free-text analog — UI-SPEC backstop).
COPY (UI-SPEC Copywriting Contract — use VERBATIM):
  - 4 starter questions (exact strings), "Interpreted as:" label, "{topN} of {total} total", "Ask" CTA.
Design tokens: foundation.md CSS variables only (no freehand hex/fonts); Inter + JetBrains Mono; --gradient-sunset
for the Ask CTA (once/twice per screen), --color-violet-soft focus/chip; 44px min touch targets.
</interfaces>
</context>

<artifacts_this_phase_produces>
See 44-01-PLAN.md `<artifacts_this_phase_produces>` for the full phase-wide registry. THIS plan creates:
frontend/src/lib/ai/use-query-stream.ts (useQueryStream); the DegradedCard export; and
frontend/src/components/ai/ask/{query-box,starter-questions,interpreted-filter,result-table}.tsx.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useQueryStream hook (POST body + results-first state machine) + export DegradedCard</name>
  <read_first>
    - frontend/src/lib/ai/use-explain-stream.ts (full — frame-parsing loop L84-138, fetch L70, state type L35, RawSseEvent L49)
    - frontend/src/components/ai/ai-explanation-section.tsx §L47 DegradedCard, §L108 AnalyzingIndicator
    - 44-RESEARCH.md §Pitfall 7 + §Pitfall 8 + 44-PATTERNS.md use-query-stream section
  </read_first>
  <behavior>
    - useQueryStream POSTs {question} as a JSON body with Content-Type application/json + Bearer token (asserted against a mocked fetch/ReadableStream).
    - Emitting interpreted then results then summary_delta drives the state through interpreted → results → streaming → done, with interpreted/results state available before any narrative text.
    - A {type:'no_key'} frame drives a no_key terminal state; {type:'refuse'} drives a refuse terminal state; {type:'error',kind} drives an error state carrying kind.
  </behavior>
  <action>
    Create frontend/src/lib/ai/use-query-stream.ts: copy the SSE frame-parsing loop from use-explain-stream.ts, change
    the fetch to `fetch(`${API_URL}/api/v1/ai/query`, {method:'POST', headers:{Authorization:`Bearer ${token}`,
    'Content-Type':'application/json'}, body: JSON.stringify({question})})`. Extend the state type with interpreted
    ({entity, filter}) and results ({rows, total}) phases held BEFORE the narrative streams (D-15), plus no_key/refuse
    terminal states. Add the InterpretedEvent/ResultsEvent types to the NEW file's RawSseEvent union only (do not touch
    the shared explain union). In ai-explanation-section.tsx add `export` to the DegradedCard declaration (§L47) — zero
    behavior change, existing internal call sites unaffected. Add use-query-stream.test.ts covering the three behaviors
    with a mocked ReadableStream.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/ai/use-query-stream.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/lib/ai/use-query-stream.ts contains `export function useQueryStream` and `body: JSON.stringify({ question })`
    - grep -n "export function DegradedCard" frontend/src/components/ai/ai-explanation-section.tsx returns a match
    - The hook exposes interpreted + results state before narrative summary_delta (asserted by test)
    - vitest run src/lib/ai/use-query-stream.test.ts exits 0
  </acceptance_criteria>
  <done>The hook streams the NLQ SSE contract with a POST body and results-first phases; DegradedCard is importable.</done>
</task>

<task type="auto">
  <name>Task 2: ask/ components — query-box, starter-questions, interpreted-filter</name>
  <read_first>
    - frontend/src/components/states/empty-state.tsx (EmptyState.Suggestion chip chrome)
    - frontend/src/components/ai/ai-explanation-section.tsx (DegradedCard/AnalyzingIndicator reuse)
    - CommentInput (char-cap + counter convention — UI-SPEC E1 backstop)
    - 44-UI-SPEC.md §Copywriting Contract (verbatim copy) + §Color + §Typography + §Spacing
    - .claude/skills/sketch-findings-getvul/references/{state-patterns,copy-voice,visual-language}.md
  </read_first>
  <action>
    Create query-box.tsx: a bounded (~500-char) free-text input with a live char-count warning mirroring CommentInput,
    soft-wrap to a bounded height then internal scroll (E1 backstops), a gradient --gradient-sunset "Ask" CTA (44px touch
    target), disabled/pending state while a request is in flight, violet-soft focus ring (foundation.md tokens only). Create
    starter-questions.tsx: exactly the 4 curated UI-SPEC starter questions as click-to-fill chips using EmptyState.Suggestion
    chrome verbatim, flex-wrap onto multiple lines (E3). Create interpreted-filter.tsx: the D-04 "Interpreted as:" card
    rendering mono predicate tokens (JetBrains Mono, · separators per UI-SPEC), flex-wrap not truncate (E4 backstop). All
    copy verbatim from the UI-SPEC Copywriting Contract; no generic SaaS voice; empty/loading/error handled by the parent
    (Plan 04) via these components' props.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json && npx eslint src/components/ai/ask/query-box.tsx src/components/ai/ask/starter-questions.tsx src/components/ai/ask/interpreted-filter.tsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ai/ask/query-box.tsx, starter-questions.tsx, interpreted-filter.tsx all exist and type-check
    - starter-questions renders exactly 4 chips with the verbatim UI-SPEC strings
    - interpreted-filter renders the literal "Interpreted as:" label and mono predicate tokens
    - query-box enforces a maxLength cap with a visible counter
    - tsc --noEmit + eslint exit 0
  </acceptance_criteria>
  <done>The three input/interpretation components render with sunset tokens + verbatim copy and honor the E1/E3/E4 backstops.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ask/result-table.tsx — entity-dispatch thin wrapper over existing list-row primitives</name>
  <read_first>
    - frontend/src/components/vulnerabilities/ (VulnTable + row primitive, imported in vulnerabilities/page.tsx:10), frontend/src/components/assets/, frontend/src/components/tickets/
    - frontend/src/components/ai/ai-explanation-citations.tsx (Citation types imported from use-explain-stream.ts §L5 — reuse or small type-import adjust for narrative)
    - 44-UI-SPEC.md §UI Considerations E5 (result table) + §Copywriting ("{topN} of {total} total")
    - .claude/skills/sketch-findings-getvul/references/visual-language.md (severity/SLA/status data colors — unchanged)
  </read_first>
  <behavior>
    - result-table renders the vulnerabilities row primitive when entity="vulnerabilities", the asset primitive when "assets", the ticket primitive when "tickets" — never a bespoke second table.
    - It renders the "{topN} of {total} total" caption in mono numerals and a zero-rows state slot.
    - Each row links to its real drill panel / detail (per D-08).
  </behavior>
  <action>
    Create result-table.tsx as a thin wrapper: switch on entity → render the existing vuln/asset/ticket list-row primitive
    with the streamed rows; show the "{topN} of {total} total" caption (JetBrains Mono numerals); the wrapper scrolls, rows
    never clip the page (E5 overflow); expose a zero-rows slot the parent fills with the "Nothing matches that" empty state.
    Do NOT introduce a new table component or a second row style (D-08). Add result-table.test.tsx asserting entity dispatch
    + caption rendering + zero-rows slot.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ai/ask/result-table.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ai/ask/result-table.tsx dispatches on entity to the three EXISTING row primitives (no new table)
    - The caption renders "{topN} of {total} total" (asserted by test)
    - vitest run src/components/ai/ask/result-table.test.tsx exits 0
  </acceptance_criteria>
  <done>Results render via the existing sunset list-row primitives per entity, with the top-N/total caption and a zero-rows slot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| SSE frames → DOM | Model-narrated prose + result rows rendered in the browser |
| question input → network | Free-text question leaves the browser as a JSON body |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-09 | Tampering (XSS) | narrative prose / interpreted tokens render | mitigate | React escapes by default; render as text, never dangerouslySetInnerHTML; reuse the existing AiExplanationCitations render path |
| T-44-05 | DoS (financial) | oversized question | mitigate | query-box ~500-char client cap (defense-in-depth; the API Field(max_length=500) is authoritative, Plan 01) |
| T-44-10 | Info Disclosure | Bearer token handling | mitigate | Token sent only in the Authorization header to the same-origin API (mirrors useExplainStream), never logged or placed in a URL |
ASVS L1; block on high. No high-severity threat left unmitigated.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/ai/use-query-stream.test.ts src/components/ai/ask/result-table.test.tsx` green
- `npx tsc --noEmit` + `npx eslint src/components/ai/ask src/lib/ai/use-query-stream.ts` clean
- Existing explain views unregressed (shared RawSseEvent union untouched): `npx vitest run src/lib/ai/use-explain-stream.test.ts` green if present
</verification>

<success_criteria>
The Ask page's data + presentational layer exists and is tested: a body-carrying streaming hook with results-first phases, exported DegradedCard, and four sunset-styled components that render the interpretation + grounded result set via existing primitives with verbatim UI-SPEC copy.
</success_criteria>

<output>
After completion, create `.planning/phases/44-natural-language-query-assistant/44-03-SUMMARY.md`.
</output>
