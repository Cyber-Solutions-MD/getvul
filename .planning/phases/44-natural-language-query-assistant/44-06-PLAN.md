---
phase: 44-natural-language-query-assistant
plan: 06
type: execute
wave: 3
depends_on: [44-02]
autonomous: true
requirements: [NLQ-02]
files_modified:
  - backend/tests/evals/goldens/nlq_translate/
  - backend/tests/evals/goldens/nlq_narrate/
  - backend/tests/evals/test_nlq_golden_evals.py
  - backend/tests/test_ai_injection_redteam.py
  - .github/workflows/ci.yml
  - redteam/promptfooconfig.yaml

must_haves:
  truths:
    - "The keyless CI-blocking gates cover NLQ: test_nlq_golden_evals.py runs in the existing ai-evals job and the extended test_ai_injection_redteam.py runs in the existing ai-redteam-injection job (D-16 — evals are the arbiter, NLQ-02 provability)."
    - "The red-team suite's CAPABILITY_CASES gains a 6th entry (build_query_translate_prompt, poisoned 'question' field) so all 17 existing adversarial payloads run against the NLQ translate prompt automatically — no new payload authoring."
    - "A golden translate case asserts the north-star question maps to the EXPECTED filter (cisa_kev=true, status unremediated, age_days_min=30, asset_internet_facing=true); a hallucinated-field / cross-tenant / out-of-scope case must be rejected or refused (D-16 injection/hallucination coverage)."
    - "nlq_narrate goldens reuse the 5 EXISTING structural metrics verbatim (NlqAnswerResponse mirrors ExplainResponseBase); nlq_translate goldens use one new filter-correctness metric."
    - { statement: "safe-schema/injection boundary: the extended red-team proves question text stays isolated to <user_question>, tenant_id is never taken from model output, and extra=forbid rejects hallucinated fields (NLQ-02 unclassified edge probe — resolved against D-01/D-13 + the Security Domain threat table).", verification: backstop }
    - { statement: "goldens are hand-authored (same accepted Phase 28 fallback — DEV_ANTHROPIC_API_KEY absent); the live promptfoo tier remains non-blocking/inert (UI-SPEC/RESEARCH Environment Availability).", verification: backstop }
  prohibitions:
    - "must never fabricate a count or a row not in the executed result set"
    - "must never fall back to a shared/GetVul Anthropic key"
  artifacts:
    - path: backend/tests/evals/test_nlq_golden_evals.py
      provides: "NLQ golden eval file (5 reused narrate metrics + 1 new translate filter-correctness metric)"
      contains: "nlq"
    - path: backend/tests/evals/goldens/nlq_translate/
      provides: "hand-authored question→filter golden fixtures (auto-discovered by _load_goldens)"
    - path: backend/tests/test_ai_injection_redteam.py
      provides: "6th CAPABILITY_CASES entry for build_query_translate_prompt"
      contains: "query_translate"
  key_links:
    - from: .github/workflows/ci.yml
      to: backend/tests/evals/test_nlq_golden_evals.py
      via: "ai-evals job step extended to run the NLQ eval file"
      pattern: "test_nlq_golden_evals"
---

<objective>
EXPANSION 4 (the arbiter gate): extend the two existing keyless CI-blocking gates with NLQ cases so NLQ-02 is
provable in CI. Golden-set filter-correctness for the representative questions + the red-team injection suite gaining
a 6th capability (the NLQ translate prompt) — additively, reusing the auto-discovery + shared payload machinery
Phase 28 built, never a parallel pipeline.

Purpose: NLQ-02 — "evals are the arbiter" (D-16); this gate is what proves the safe-schema constraint holds.
Output: nlq_translate + nlq_narrate goldens, test_nlq_golden_evals.py, a 6th red-team CAPABILITY_CASES entry,
ci.yml wiring, and non-blocking promptfoo NLQ scenarios.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-natural-language-query-assistant/44-CONTEXT.md
@.planning/phases/44-natural-language-query-assistant/44-RESEARCH.md
@.planning/phases/44-natural-language-query-assistant/44-VALIDATION.md

<interfaces>
<!-- Verified this session (RESEARCH §Validation Architecture). -->
EXISTING keyless CI-blocking gates (Phase 28, AIE-01..04):
  - ai-evals job → deepeval test run over backend/tests/evals/test_golden_evals.py; _load_goldens() AUTO-DISCOVERS new
    goldens/<capability>/ dirs; the 5 structural metrics (metrics.py) all assume an ExplainResponseBase-shaped model_response
  - ai-redteam-injection job → pytest backend/tests/test_ai_injection_redteam.py; CAPABILITY_CASES is a plain list;
    17 adversarial payloads run per capability × a _<capability>_record factory
  - .github/workflows/ci.yml jobs (RESEARCH §Sources: lines 160-296); .github/branch-protection.json required-check names
FROM Plan 01/02: build_query_translate_prompt (poisoned field = "question"), NlqFilterResponse, NlqAnswerResponse,
  the final VulnFilterInput field set (incl. sla_breached, asset_internet_facing).
DEV_ANTHROPIC_API_KEY: absent (accepted Phase 28 override) → hand-author goldens (capture_ai_goldens.py documents the
  fallback); the keyless gates are fully exercisable without it; the live promptfoo tier stays inert/non-blocking.
</interfaces>
</context>

<artifacts_this_phase_produces>
See 44-01-PLAN.md `<artifacts_this_phase_produces>` for the full phase-wide registry. THIS plan creates:
backend/tests/evals/test_nlq_golden_evals.py, backend/tests/evals/goldens/nlq_translate/*.json + nlq_narrate/*.json,
a 6th test_ai_injection_redteam.py CAPABILITY_CASES entry, ci.yml ai-evals step extension, and non-blocking
redteam/promptfooconfig.yaml NLQ scenarios.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: NLQ golden evals — translate filter-correctness + narrate structural reuse</name>
  <read_first>
    - backend/tests/evals/test_golden_evals.py (full — _load_goldens auto-discovery), backend/tests/evals/metrics.py (the 5 structural metrics), backend/tests/evals/goldens/vuln/grounded.json (fixture shape)
    - backend/app/ai/schemas.py NlqFilterResponse / NlqAnswerResponse (Plan 01/02)
    - 44-RESEARCH.md §Validation Architecture (Phase Requirements → Test Map) + §Wave 0 Gaps
  </read_first>
  <behavior>
    - nlq_translate goldens (hand-authored): the north-star question + the 4 UI-SPEC starter questions each map to their EXPECTED filter; a new FilterCorrectnessMetric asserts the emitted filter equals the golden filter.
    - An out-of-scope / hallucinated-field golden asserts groundable=false (refuse) or schema rejection.
    - nlq_narrate goldens: the 5 existing structural metrics pass verbatim on NlqAnswerResponse-shaped responses (grounded, no fabricated numbers).
  </behavior>
  <action>
    Create backend/tests/evals/goldens/nlq_translate/*.json (question → expected NlqFilterResponse) and
    backend/tests/evals/goldens/nlq_narrate/*.json (grounding record → expected NlqAnswerResponse), hand-authored per the
    Phase 28 fallback. Create backend/tests/evals/test_nlq_golden_evals.py that reuses the 5 existing narrate metrics verbatim
    for nlq_narrate and adds ONE FilterCorrectnessMetric (emitted filter == golden filter) for nlq_translate. Rely on
    _load_goldens() auto-discovery for the new dirs. Include at least one refuse (out-of-scope) and one hallucinated-field case.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=test-key-please-ignore-000000000000000000000000000= JWT_SECRET_KEY=test deepeval test run tests/evals/test_nlq_golden_evals.py</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/evals/test_nlq_golden_evals.py exists and references both nlq_translate and nlq_narrate goldens
    - backend/tests/evals/goldens/nlq_translate/ and nlq_narrate/ contain ≥1 fixture each, incl. a refuse + a hallucinated-field case
    - deepeval test run tests/evals/test_nlq_golden_evals.py exits 0
  </acceptance_criteria>
  <done>NLQ golden evals pass keyless: translate filter-correctness + narrate structural metrics, incl. refuse/hallucination cases.</done>
</task>

<task type="auto">
  <name>Task 2: extend the red-team injection suite + CI wiring (+ inert promptfoo scenarios)</name>
  <read_first>
    - backend/tests/test_ai_injection_redteam.py (full — CAPABILITY_CASES list + per-capability _record factory + 17 payloads)
    - .github/workflows/ci.yml (ai-evals + ai-redteam-injection job steps), .github/branch-protection.json (required-check names)
    - redteam/promptfooconfig.yaml (existing scenarios) — 44-RESEARCH.md §Validation Architecture NLQ-02 rows
  </read_first>
  <behavior>
    - The 6th CAPABILITY_CASES entry (build_query_translate_prompt, poisoned field "question") runs all 17 existing adversarial payloads; each asserts the question text stays inside <user_question> and never leaks into the system prompt, and no payload steers a forbidden filter/enum.
    - The ai-evals CI step also runs tests/evals/test_nlq_golden_evals.py; ai-redteam-injection picks up the extended file automatically (same pytest invocation).
  </behavior>
  <action>
    Add a 6th entry to CAPABILITY_CASES in test_ai_injection_redteam.py: capability build_query_translate_prompt, poisoned
    field "question", a one-line _query_record factory. Extend the .github/workflows/ci.yml ai-evals step to include
    tests/evals/test_nlq_golden_evals.py (ai-redteam-injection already runs the whole test_ai_injection_redteam.py file —
    confirm no change needed there). Add NLQ red-team scenarios to redteam/promptfooconfig.yaml (question-phrasing injection,
    cross-tenant reach, hallucinated field/enum) — this tier stays non-blocking/inert without DEV_ANTHROPIC_API_KEY (same
    accepted Phase 28 gap). Do NOT invent a parallel eval/red-team pipeline (D-16 — extend, don't fork).
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=test-key-please-ignore-000000000000000000000000000= JWT_SECRET_KEY=test pytest tests/test_ai_injection_redteam.py -k query_translate -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "build_query_translate_prompt\|query_translate" backend/tests/test_ai_injection_redteam.py returns a match (6th capability)
    - grep -n "test_nlq_golden_evals" .github/workflows/ci.yml returns a match (ai-evals step extended)
    - redteam/promptfooconfig.yaml contains NLQ scenarios
    - pytest tests/test_ai_injection_redteam.py -k query_translate exits 0
  </acceptance_criteria>
  <done>The NLQ translate prompt is red-teamed by all existing payloads in the keyless CI-blocking gate; ci.yml runs the NLQ evals; promptfoo NLQ scenarios exist (inert).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| adversarial question → translate prompt | Red-team payloads attempt injection / cross-tenant reach / hallucination via question text |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering / EoP | translate prompt (injection) | mitigate | 6th CAPABILITY_CASES entry runs all 17 adversarial payloads against build_query_translate_prompt in the CI-blocking gate (Task 2) |
| T-44-02 | Info Disclosure | cross-tenant reach attempt | mitigate | Red-team + golden cases assert the model cannot emit a tenant_id (schema has none) and cross-tenant phrasing is refused |
| T-44-03 | Tampering | hallucinated field/enum | mitigate | Golden hallucinated-field case + extra=forbid asserted in the eval gate (Task 1) |
| T-44-13 | Repudiation | undetected eval regression | mitigate | Both gates are CI-blocking (branch-protection required checks) — an NLQ eval/red-team failure blocks merge |
ASVS L1; block on high. No high-severity threat left unmitigated.
</threat_model>

<verification>
- `cd backend && ENCRYPTION_KEY=... JWT_SECRET_KEY=test deepeval test run tests/evals/test_nlq_golden_evals.py` green
- `pytest tests/test_ai_injection_redteam.py -q` green (all capabilities incl. the new 6th)
- `grep -n test_nlq_golden_evals .github/workflows/ci.yml` returns a match; existing ai-evals/ai-redteam-injection jobs still green
</verification>

<success_criteria>
NLQ-02 is provable in CI: the safe-schema constraint (isolation, no cross-tenant reach, no hallucinated field) is enforced by the two existing keyless CI-blocking gates, extended additively, plus hand-authored golden filter-correctness — no parallel pipeline, live promptfoo tier remains an accepted inert override.
</success_criteria>

<output>
After completion, create `.planning/phases/44-natural-language-query-assistant/44-06-SUMMARY.md`.
</output>
