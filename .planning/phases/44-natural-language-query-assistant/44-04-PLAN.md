---
phase: 44-natural-language-query-assistant
plan: 04
type: execute
wave: 4
depends_on: [44-03, 44-05]
autonomous: false
requirements: [NLQ-01, NLQ-03]
files_modified:
  - frontend/src/app/(authed)/dashboard/ask/page.tsx
  - frontend/src/components/shell/nav-items.ts
  - frontend/src/app/(authed)/dashboard/ask/page.test.tsx

must_haves:
  truths:
    - "A new top-level 'Ask' nav entry (label 'Ask', href /dashboard/ask, Sparkles icon, no chip) is added to WORKFLOW_ITEMS, mirroring the Coverage/Analytics/Compliance precedent (D-09)."
    - "The Ask nav item is ALWAYS visible; when useAiStatus() returns configured:false the page renders the 'AI isn't set up yet' Configure-AI DegradedCard with a Configure AI CTA to /dashboard/connectors, and role-specific body copy (Admin/Owner vs Analyst/Viewer) (D-12/NLQ-03 inert boundary — resolves the NLQ-03 inert-state edge probe)."
    - "First-run empty state shows the D-11 heading/body + 4 clickable starter-question chips (mandatory empty state per state-patterns.md)."
    - "On submit the page shows the interpretation + result table AS SOON AS the query executes, then streams the narrative after (D-15 results-first), driven by useQueryStream's interpreted→results→streaming→done phases."
    - "An out-of-scope/unmappable question ({type:'refuse'}) renders the 'Can't answer that one' neutral DegradedCard that points back to what Ask CAN answer (D-14); a valid-but-empty query renders 'Nothing matches that' with the interpretation still shown above (zero-results, distinct from refuse)."
    - "budget-exceeded renders the amber DegradedCard ('This tenant's monthly AI budget is used up'); a safety-flagged response renders the danger variant; a transient error renders the error banner with HTTP code + request ID + Retry now, retaining the typed question (E7/E8 + state-patterns.md)."
    - "Each answer offers a read-only 'Open in {Vulnerabilities|Assets|Tickets}' secondary button built from buildNlqDeepLink(entity, filter) — the same translated filter, zero mutation surface (D-17)."
    - "The interpreted filter is ALWAYS shown alongside any answer, so a misread is visible before the analyst trusts it (D-04)."
    - "The narrative renders as streamed prose with an 'Answering…' indicator then validated prose; a rejected buffer falls back to the error banner (D-15/E6 streaming)."
    - { statement: "the transient-error DegradedCard body (request ID + HTTP code + View trace/Retry now) wraps and its buttons stay reachable at mobile width (UI-SPEC E8 overflow backstop).", verification: backstop }
  prohibitions:
    - "must never silently widen or narrow the interpreted filter without showing it"
    - "must never fall back to a shared/GetVul Anthropic key"
  artifacts:
    - path: frontend/src/app/(authed)/dashboard/ask/page.tsx
      provides: "The /dashboard/ask page composing all states (inert/empty/loading/interpreted/results/streaming/refuse/zero/budget/safety/error) + Open-in deep-link"
      contains: "useAiStatus"
      min_lines: 120
    - path: frontend/src/components/shell/nav-items.ts
      provides: "'Ask' WORKFLOW_ITEMS entry (Sparkles, no chip)"
      contains: "/dashboard/ask"
  key_links:
    - from: frontend/src/app/(authed)/dashboard/ask/page.tsx
      to: frontend/src/lib/ai/use-query-stream.ts
      via: "useQueryStream drives the submit→interpreted→results→streaming→done flow"
      pattern: "useQueryStream"
    - from: frontend/src/app/(authed)/dashboard/ask/page.tsx
      to: frontend/src/lib/ai/nlq-deep-link.ts
      via: "buildNlqDeepLink(entity, filter) for the Open-in button"
      pattern: "buildNlqDeepLink"
    - from: frontend/src/app/(authed)/dashboard/ask/page.tsx
      to: frontend/src/lib/queries/use-ai-status.ts
      via: "useAiStatus() page-load gate for the D-12 inert state"
      pattern: "useAiStatus"
---

<objective>
EXPANSION 5 (the page): compose the proven pieces into the /dashboard/ask page and add the always-visible 'Ask'
nav entry — the full D-09 workflow with every mandatory state (inert, empty, loading, interpreted, results,
streaming, refuse, zero-results, budget, safety, error). This is the user-facing deliverable that makes NLQ real.

Contains a `checkpoint:human-verify` (autonomous:false) because the full live flow — configure-AI gate, submit →
results-first → streaming narrative, refuse/zero/budget/error states, and the Open-in deep-link into a filtered list —
is visual/interactive and cannot be fully proven by component tests alone.

Purpose: NLQ-01 (grounded answer + result set through the query interface) + NLQ-03 (inert Configure-AI state).
Output: /dashboard/ask/page.tsx, the 'Ask' nav item, page.test.tsx, and a verified end-to-end flow.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-natural-language-query-assistant/44-CONTEXT.md
@.planning/phases/44-natural-language-query-assistant/44-UI-SPEC.md
@.planning/phases/44-natural-language-query-assistant/44-PATTERNS.md
@.claude/skills/sketch-findings-getvul/references/page-layouts.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md

<interfaces>
<!-- Verified this session. -->
ANALOG page (frontend/src/app/(authed)/dashboard/compliance/page.tsx, full 275 lines):
  - composition ErrorBoundary(pageErrorFallback) > Suspense(PAGE_FALLBACK) > PageInner
  - useDocumentTitle(PAGE_TITLE) §L143 ; sr-only <h1> §L193 ; pageErrorFallback + PartialFailureBanner §L64-74
  - EmptyState / .Title / .Body / .Actions composition §L213-224
  - WATCH-OUT: compliance fetches on load; Ask is SUBMIT-triggered — the ONLY page-load query is useAiStatus() (D-12 gate);
    everything else is driven by useQueryStream's local state (44-PATTERNS.md).
FROM Plan 03: useQueryStream (interpreted/results/streaming/done/no_key/refuse/error states), exported DegradedCard,
  ask/{query-box, starter-questions, interpreted-filter, result-table}, AnalyzingIndicator (already exported).
FROM Plan 05: buildNlqDeepLink(entity, filter) for the Open-in button.
REUSE: frontend/src/lib/queries/use-ai-status.ts useAiStatus() (configured check, byte-identical to how
  ai-explanation-section.tsx:163,300-313 gates its trigger); DegradedCard variants neutral/amber/danger.
ANALOG nav (frontend/src/components/shell/nav-items.ts §L35 WORKFLOW_ITEMS, §L56-63 Coverage/Analytics/Compliance):
  `{ label: 'Ask', href: '/dashboard/ask', icon: Sparkles }` — import Sparkles from lucide-react (already project-wide).
COPY (UI-SPEC Copywriting Contract — VERBATIM): empty heading/body, 4 starters, "Interpreted as:", "{topN} of {total} total",
  Configure-AI heading/body (role-specific)/CTA, refusal "Can't answer that one"/body, budget copy, zero-results
  "Nothing matches that"/body, error "Couldn't translate that question"/View trace + Retry now, "Open in {list}".
ANALOG test: frontend/src/app/(authed)/dashboard/campaigns/page.test.tsx (co-located convention).
</interfaces>
</context>

<artifacts_this_phase_produces>
See 44-01-PLAN.md `<artifacts_this_phase_produces>` for the full phase-wide registry. THIS plan creates:
frontend/src/app/(authed)/dashboard/ask/page.tsx, the 'Ask' WORKFLOW_ITEMS nav entry, and ask/page.test.tsx.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /dashboard/ask/page.tsx composition + 'Ask' nav entry + page.test.tsx</name>
  <read_first>
    - frontend/src/app/(authed)/dashboard/compliance/page.tsx (full — ErrorBoundary>Suspense>PageInner, useDocumentTitle, sr-only h1, EmptyState composition)
    - frontend/src/components/shell/nav-items.ts §L35/56-63
    - frontend/src/lib/queries/use-ai-status.ts; Plan 03 ask/ components + useQueryStream; Plan 05 buildNlqDeepLink
    - frontend/src/app/(authed)/dashboard/campaigns/page.test.tsx (co-located test convention)
    - 44-UI-SPEC.md (full Copywriting Contract + UI Considerations E1-E8) + state-patterns.md
  </read_first>
  <behavior>
    - configured:false → renders the "AI isn't set up yet" Configure-AI DegradedCard + Configure AI CTA to /dashboard/connectors, with role-specific body (Admin/Owner vs Analyst/Viewer).
    - configured:true + no submission → renders the D-11 empty state (heading/body + 4 starter chips); clicking a chip fills the query box.
    - submit → interpreted + result table shown before narrative (D-15); then streamed prose.
    - {type:'refuse'} → "Can't answer that one" neutral card; zero rows → "Nothing matches that" with interpretation still shown; {kind:'budget_exceeded'} → amber card; error → banner with Retry now retaining the question.
    - a rendered answer shows the "Open in {entity}" button whose href equals buildNlqDeepLink(entity, filter).
  </behavior>
  <action>
    Create frontend/src/app/(authed)/dashboard/ask/page.tsx mirroring compliance/page.tsx's ErrorBoundary>Suspense>PageInner
    shell + useDocumentTitle + sr-only h1. The ONLY page-load query is useAiStatus(); gate on configured (D-12 inert card).
    When configured, compose query-box + starter-questions (empty state) and drive submission through useQueryStream, rendering
    each phase: AnalyzingIndicator ("Interpreting your question…") → interpreted-filter (D-04, always shown) → result-table
    (D-15 results-first) with the "{topN} of {total} total" caption → streamed narrative ("Answering…" then validated prose via
    the AiExplanationCitations render path). Map terminal states to DegradedCard variants: refuse→neutral, budget→amber,
    safety→danger, transient error→error banner (HTTP code + request ID + View trace/Retry now, wraps at mobile — E8 backstop),
    zero-results→"Nothing matches that" EmptyState with the interpretation retained above. Add the read-only "Open in {entity}"
    .btn-secondary using buildNlqDeepLink(entity, filter) (D-17). Add the 'Ask' WORKFLOW_ITEMS entry (Sparkles, no chip) to
    nav-items.ts. All copy verbatim from the UI-SPEC. Add ask/page.test.tsx mirroring campaigns/page.test.tsx covering the
    Configure-AI, empty-state, refuse, and deep-link behaviors (mock useAiStatus + useQueryStream).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/(authed)/dashboard/ask/page.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/app/(authed)/dashboard/ask/page.tsx contains `useAiStatus`, `useQueryStream`, and `buildNlqDeepLink`
    - grep -n "/dashboard/ask" frontend/src/components/shell/nav-items.ts returns a match with a Sparkles icon and no chip
    - page.test.tsx asserts: Configure-AI card when configured:false, 4 starter chips when configured + no submission, "Can't answer that one" on refuse, and an Open-in href equal to buildNlqDeepLink output
    - vitest run ask/page.test.tsx + tsc --noEmit exit 0
  </acceptance_criteria>
  <done>The Ask page composes every mandatory state with verbatim copy, the nav entry is always visible, and component tests cover inert/empty/refuse/deep-link.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The /dashboard/ask page end-to-end: always-visible 'Ask' nav entry; Configure-AI inert state when no key; first-run
    empty state with 4 starter chips; submit → interpretation + result table first, then a streamed grounded narrative;
    refuse / zero-results / budget / safety / transient-error states; and the read-only "Open in {list}" deep-link that
    applies the same translated filter to the real list view.
  </what-built>
  <how-to-verify>
    1. Run the local stack (per project memory: admin user, :3000 CORS, prod build). Visit http://localhost:3000/dashboard/ask.
    2. With NO Anthropic key configured for the tenant: confirm the "AI isn't set up yet" card + Configure AI CTA to /dashboard/connectors, and that the 'Ask' nav item is still visible.
    3. Configure the tenant's own Anthropic key (BYOK). Reload /dashboard/ask; confirm the 4 starter chips render; click one to fill the box.
    4. Submit "which internet-facing hosts have an unremediated KEV older than 30 days?": confirm the "Interpreted as:" summary + result table appear BEFORE the narrative streams, the "{topN} of {total} total" caption is correct, and the prose narrates only the shown rows.
    5. Submit an out-of-scope question (e.g. "what's the weather?"): confirm the "Can't answer that one" refusal card. Submit a valid-but-empty query: confirm "Nothing matches that" with the interpretation still shown.
    6. Click "Open in Vulnerabilities": confirm it lands on /dashboard/vulnerabilities with the same filter applied (severity/KEV/age/exposure chips active).
    7. Confirm no shared/fallback key is ever used (removing the key returns the inert state, never an answer).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. results-first ordering, a state's copy, or the deep-link filter mismatch).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| useAiStatus gate → page render | The inert/configured decision gates whether the model-calling path is reachable |
| SSE narrative/rows → DOM | Model output rendered in the browser |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-14 | EoP | reaching the ask flow without a key | mitigate | useAiStatus() gate renders the inert Configure-AI state; the backend also fail-closes with {type:"no_key"} (Plan 01) — no shared/fallback key path exists (NLQ-03) |
| T-44-09 | Tampering (XSS) | narrative/interpretation/rows render | mitigate | React escaping; reuse AiExplanationCitations text render path; no dangerouslySetInnerHTML |
| T-44-04 | Info Disclosure | interpreted filter honesty | mitigate | D-04 interpreted-filter always shown so a misread/silent-widening is visible before the analyst acts |
| T-44-11 | Tampering | Open-in deep-link params | mitigate | buildNlqDeepLink emits only clamped param names; list pages clamp on read (Plan 05); tenant scope stays server-side |
ASVS L1; block on high. No high-severity threat left unmitigated.
</threat_model>

<verification>
- `cd frontend && npx vitest run "src/app/(authed)/dashboard/ask/page.test.tsx"` green; `npx tsc --noEmit` clean
- Human-verify checkpoint approved (live flow: inert → configure → submit → results-first → streaming → deep-link)
- Nav renders the 'Ask' item on every authed screen without breaking existing WORKFLOW_ITEMS
</verification>

<success_criteria>
An analyst opens /dashboard/ask, sees the inert state until they configure their own key, then asks a plain-English question and gets a grounded tenant-scoped answer with the result set shown first and the narrative streamed after, can see exactly how it was interpreted, and can open the same filter in the real list view — all NLQ-01/NLQ-03 success criteria met, verified live.
</success_criteria>

<output>
After completion, create `.planning/phases/44-natural-language-query-assistant/44-04-SUMMARY.md`.
</output>
