---
phase: 44
slug: natural-language-query-assistant
status: approved
shadcn_initialized: false
preset: none
created: 2026-08-24
---

# Phase 44 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (bespoke sunset design system — CSS variables + Tailwind utility classes; shadcn was never introduced across any of the prior 43 phases, so it is not introduced here either) |
| Preset | not applicable — no `components.json` in the repo; confirmed via `ls components.json tailwind.config.*` returning no match |
| Component library | none (hand-rolled compound components — e.g. `EmptyState` in `frontend/src/components/states/empty-state.tsx`, `DegradedCard`/`AnalyzingIndicator` in `frontend/src/components/ai/ai-explanation-section.tsx` — reused verbatim below, not rebuilt) |
| Icon library | `lucide-react` (confirmed: `nav-items.ts`, `ai-explanation-section.tsx` imports) |
| Font | Inter (prose/UI/headings) + JetBrains Mono (CVE IDs, hostnames, scores, durations, request IDs, and anything "terminal-pasteable" — including the interpreted-filter summary and result counts on this page) |

**shadcn gate note:** components.json absent, but this is a mature 43-phase-deep custom design system (sketch-findings-getvul, `sunset.css`), not a shadcn-eligible blank slate — introducing shadcn now would fork the design system mid-milestone. Per project CLAUDE.md ("Don't pick hex colors freehand — use the CSS variables from `foundation.md`"), Tool is set to `none` and the shadcn gate is treated as not-applicable rather than re-asked. Registry Safety section below is therefore not applicable.

---

## Spacing Scale

Declared values (multiples of 4, from `foundation.md` — reused verbatim, not reinvented):

| Token | Value | Usage on this phase's new surface |
|-------|-------|-------|
| xs | 4px | Gap between severity glyph and count in reused result rows; gap inside SLA/status pills |
| sm | 8px | Gap between starter-question chips; gap between interpretation-summary predicate tokens |
| md | 16px | Padding inside the query-box card; gap between query box and interpretation card |
| lg | 24px | Padding inside page container (matches Coverage/Analytics/Compliance precedent); gap between interpretation card and result table |
| xl | 32px | Gap between page header and query box on desktop |
| 2xl | 48px | Vertical margin above/below the first-run empty state card |
| 3xl | 64px | Not used on this page (no hero-scale section) |

Exceptions: **44px minimum touch target** on the "Ask" submit button, each starter-question chip, and each result row's tap target on mobile — mirrors the existing list-row/chip touch-target convention already established on Vulnerabilities/Assets/Tickets. No other exceptions.

---

## Typography

Only the sizes/weights this phase's **new** elements introduce are declared below (reused components — `EmptyState`, `DegradedCard`, list-row primitives — keep their own already-shipped type styles unchanged; not redeclared here).

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Page heading (`<h1>`, sr-only — mirrors Coverage/Analytics/Compliance `page.tsx` precedent where the visible page title lives in the topbar breadcrumb, not a visible `<h1>`) | 32px (`text-3xl`) | 600 (semibold) | 1.1 (`leading-tight`) |
| Question input + "Ask" button label | 16px (`text-base`) | 400 regular (input text) / 600 semibold (button label) | 1.5 (`leading-base`) |
| Interpretation summary, narrative answer prose, starter-question chip labels, result-table row text | 14px (`text-sm`) | 400 regular (prose) / 600 semibold (mono predicate tokens, e.g. `severity=Critical`) | 1.5 (`leading-base`) |
| Meta labels ("Interpreted as:", "N of M total", section eyebrows) | 12px (`text-xs`) | 600 semibold, uppercase, `tracking-wide` | 1.3 (`leading-snug`) |

**Declared weights for this phase: 400 (regular) and 600 (semibold) — 2 total.** Reused components (e.g. `SECONDARY_BTN_CLASS`'s existing `font-medium`/500 on the "Configure AI" / "Open in {list}" buttons) keep their own already-shipped weight; that is inherited chrome, not a new declaration.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--color-bg` `#0E0B1A` | Page background |
| Secondary (30%) | `--color-surface` `#1A1430` / `--color-surface-2` `#241B40` | Query-box card, interpretation-summary card, result-table wrapper, starter-question-chip container, `DegradedCard` shells |
| Accent (10%) | `--gradient-sunset` (pink→violet→amber) for the "Ask" submit button; `--color-violet` `#A78BFA` elsewhere | See "Accent reserved for" below |
| Destructive | `--color-danger` `#F87171` | Reserved for the safety-refusal `DegradedCard` variant only (injection-flagged / unsafe-denylisted responses) — never for the ordinary "can't map that question" refusal, which is a neutral honesty state, not a security event |

**Accent reserved for (explicit list — nothing else on this page gets accent color):**
- The **"Ask" submit button** — the single gradient CTA on this page (`.btn-cta`, `--gradient-sunset`, `--glow-cta`). Per `foundation.md`, a gradient CTA appears once or twice per screen; this is the one primary action.
- **Focus rings** on the question input and starter-question chips — violet (`--color-violet-soft` halo), matching the established `.field-input:focus` token (violet, never pink, to avoid competing with the CTA).
- **Starter-question chip active/hover state** — violet-soft fill + `--color-violet-on-soft` text (same token pair as `EmptyState.Suggestion`), reused verbatim, not a new hue.
- **"Configure AI" inert-state icon chip** — `bg-violet-soft` + `Sparkles` icon, reused verbatim from `DegradedCard variant="neutral"` (the exact "AI isn't set up yet" pattern already shipped in `ai-usage-pane.tsx` / `ai-explanation-section.tsx`).
- **Rate-limited / budget-exceeded state** — amber (`DegradedCard variant="amber"`), matching the existing degraded/partial-failure amber convention (never red — this is "works, but capped," not "broken").

No other element on this page uses accent color. Severity/SLA/status colors on the reused result rows follow their own locked visual-language tokens (unchanged, not part of this page's 10% accent budget — those are semantic data colors, not chrome).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (submit) | `Ask` — single verb, icon-prefixed (arrow-right or `Sparkles`, matching the AI-feature icon family), gradient CTA button next to the question input |
| Secondary CTA (D-17 deep-link) | `Open in Vulnerabilities` / `Open in Assets` / `Open in Tickets` — verb + noun, dynamic on the resolved entity, `.btn-secondary` chrome (never a second gradient) |
| Nav item label | `Ask` (mirrors the terse single-word nav labels already used: `Coverage`, `Analytics`, `Compliance`) |
| Page heading (sr-only) | `Ask` |
| First-run empty state heading | `Ask a question about your vulnerabilities, assets, or tickets` |
| First-run empty state body | `Answers stay scoped to your own tenant data and a fixed set of filters — no free-form queries, no guesswork on counts. Try one of these:` |
| Starter questions (D-11, exactly 4, clickable chips) | 1. `Which internet-facing hosts have an unremediated KEV older than 30 days?` 2. `Show critical vulns breaching SLA` 3. `Open tickets for asset prod-db-01` 4. `Vulnerabilities on internet-facing assets with an active exploit` |
| Interpreted-filter summary label (D-04) | `Interpreted as:` followed by mono predicate tokens, e.g. `severity=Critical · KEV=true · first_seen>30d · exposure=internet-facing` |
| Result count caption (D-07) | `{topN} of {total} total` — e.g. `10 of 47 total` (mono numerals) |
| Configure-AI inert state heading | `AI isn't set up yet` (verbatim reuse of the existing `ai-usage-pane.tsx` / `ai-explanation-section.tsx` copy — no new phrasing for the same state) |
| Configure-AI inert state body (Admin/Owner) | `Turn on AI with your own Anthropic key to ask plain-English questions over your vulnerabilities, assets, and tickets.` |
| Configure-AI inert state body (Analyst/Viewer) | `Ask needs AI turned on — ask an admin to configure GetVul's AI connector.` |
| Configure-AI CTA | `Configure AI` → `/dashboard/connectors` (verbatim reuse of the established href + label) |
| Refusal (out-of-scope question, D-14) heading | `Can't answer that one` |
| Refusal (out-of-scope question, D-14) body | `Ask covers vulnerabilities, assets, and tickets — filtered by things like severity, exposure, SLA status, or KEV. Try a starter question below, or rephrase using those terms.` |
| Rate-limited / budget-exceeded state | `This tenant's monthly AI budget is used up` / body: `Ask an admin to raise the budget in AI settings, or wait until next month.` — mirrors the existing fail-closed budget-breaker messaging pattern |
| Zero-results state (valid query, no matching rows) heading | `Nothing matches that` |
| Zero-results state body | `Interpreted as: {predicate summary}. Try broadening a term — for example, dropping the age or exposure predicate.` |
| Error state (translation/model call failed, not a refusal) | `Couldn't translate that question` / body includes HTTP code + request ID, `View trace` + `Retry now` buttons — mirrors `state-patterns.md`'s error-banner convention verbatim |
| Destructive confirmation | **None in this phase.** D-17 is read-only (deep-link only); there is no mutation surface on the Ask page, so no destructive-action confirmation copy applies. |

---

## UI Considerations

Post-verification UI-consideration probe (ui-consideration-probe.cjs) over 8 described surfaces
(question input `E1`, Ask button `E2`, starter chips `E3`, interpretation summary `E4`, result table
`E5`, narrative answer prose `E6`, Configure-AI inert card `E7`, degraded/refusal card family `E8`).
**43 applicable considerations resolved: 23 covered · 4 backstop · 0 unresolved · 16 dismissed (N/A).**
E6 came back `unclassified` (streaming prose has no cue the heuristic matches) and was manually
classified to `covered` under the D-15 streaming contract. No genuinely-open design decision
surfaced — every category maps to a locked CONTEXT.md decision or an already-approved contract row
above; the two backstops (input char-cap, predicate wrap) remain planner-discretion visual checks.

### Covered (concrete truth)

| Element | Category | Truth |
|---------|----------|-------|
| E1 input | empty | Unfilled field shows the placeholder; the page's first-run empty state renders the D-11 starter chips (Copywriting Contract) — the mandatory empty state per `state-patterns.md` |
| E1 input | loading | On submit the field is disabled while translation is in-flight, paired with the "Interpreting your question…" `AnalyzingIndicator` |
| E1 input | error | Translation failure shows "Couldn't translate that question" banner with `Retry now`; the typed question is retained |
| E2 Ask button | loading | Button enters a disabled/pending state during the in-flight translation request |
| E2 Ask button | error | On failure the error banner + `Retry now` restores the actionable state (Copywriting Contract error row) |
| E3 chips | long-text | The 4 chip labels are fixed curated copy (longest ~70 chars); chips `flex-wrap` onto multiple lines rather than truncate (reuses `EmptyState.Suggestion` behavior) |
| E4 interp. summary | loading | Card appears when translation lands; before that the "Interpreting…" indicator holds its place |
| E4 interp. summary | error | A failed translation shows the error banner in place of the summary |
| E4 interp. summary | populated | Mono predicate tokens, e.g. `severity=Critical · KEV=true · first_seen>30d` (Copywriting Contract) |
| E4 interp. summary | zero-one-many | One predicate vs many handled by the same `flex-wrap`; a translated query always carries ≥1 predicate |
| E5 result table | empty | "Nothing matches that" `EmptyState`, interpretation summary still shown above so the analyst sees exactly what was searched |
| E5 result table | loading | D-15 results-first: table region shows a loading state until the deterministic query returns |
| E5 result table | error | Error banner replaces the table on failure |
| E5 result table | populated | Reused list-row primitives, top-N rows, `{topN} of {total} total` caption |
| E5 result table | partial | The top-N truncation IS the partial-data affordance — the caption states how many of the total are shown (D-07) |
| E5 result table | overflow | The result-table wrapper scrolls (reused list layout); rows never clip the page |
| E5 result table | zero-one-many | Zero → empty state; the `{topN} of {total} total` caption carries singular/plural framing at one vs many |
| E6 narrative prose | streaming (manual) | D-15 streaming answer: "Answering…" indicator while the buffer-then-validate SSE streams; validated prose renders on success; a rejected buffer falls back to the error banner |
| E7 Configure-AI card | error | The AI-unconfigured inert state itself (D-12), with role-specific body copy (Admin/Owner vs Analyst/Viewer) |
| E7 Configure-AI card | populated | "AI isn't set up yet" heading + role body + `Configure AI` CTA (Copywriting Contract) |
| E8 degraded family | error | Full family enumerated — refusal (neutral), safety-flagged (danger), budget (amber), transient (amber banner) — each with distinct copy + `DegradedCard` variant per the Color + Copywriting contracts |
| E8 degraded family | populated | Each variant's heading/body/CTA copy is defined in the Copywriting Contract |
| E8 degraded family | long-text | Request ID and HTTP code are bounded mono strings; body copy is fixed and wraps within the card |

### Backstop (visual-state check at implementation/verification)

| Element | Category | Statement | Verification |
|---------|----------|-----------|--------------|
| E1 input | long-text | Bounded length (recommend ~500 chars) with a live counter mirroring `CommentInput`'s char-count-warning, to bound the injection/cost surface per NLQ-02; exact cap is planner discretion per CONTEXT.md | backstop — counter renders and text never clips |
| E1 input | overflow | Input soft-wraps and grows to a bounded height, then scrolls internally rather than clipping | backstop — long question never clips the field |
| E4 interp. summary | overflow | Predicate tokens `flex-wrap` onto multiple lines rather than truncate or scroll (no prior sketch covers a multi-predicate wrap) | backstop — many-predicate summary wraps, no truncation |
| E8 degraded family | overflow | The transient-error body carries a request ID + HTTP code + `View trace`/`Retry now` — these must wrap and the buttons must not clip on narrow viewports | backstop — error card wraps, buttons reachable at mobile width |

### Dismissed — not applicable (16)

- **E1 input** partial — a single free-text field has one atomic value, no partial-record state.
- **E2 Ask button** empty / partial / long-text — a CTA with a fixed "Ask" label has no data payload or dynamic text.
- **E4 interp. summary** empty — the card is not rendered until translation returns predicates (the page empty state is E3). partial — predicates are emitted atomically by the translator; no partially-mapped state.
- **E7 Configure-AI card** empty / loading / partial / overflow / zero-one-many / long-text — a static singleton inert card with fixed short copy; it *is* the empty-of-AI state, nothing loads or overflows inside it.
- **E8 degraded family** empty / loading / partial / zero-one-many — terminal error cards, shown one at a time after a failure resolves; no nested data/loading/collection state.

---

## Registry Safety

Not applicable — no shadcn (`Tool: none`, see Design System section). No third-party registries are in play.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | none | not applicable |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: FLAG (non-blocking) — CTA `Ask` is a bare verb; consider `Ask GetVul`/`Run Query`. Contextually unambiguous next to the question input.
- [x] Dimension 2 Visuals: FLAG (non-blocking) — no explicit focal-point declaration; recommend one sentence naming the gradient "Ask" button as the primary anchor and the interpretation summary as the secondary anchor once results land.
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS (not applicable — bespoke design system)

**Approval:** APPROVED — 6/6 dimensions clear, 2 non-blocking FLAGs (recommendations recorded above).
