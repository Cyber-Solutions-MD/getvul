---
phase: 24-ai-foundation-explain-this-vuln
plan: 08
type: tdd
wave: 7
depends_on: [06, 07]
files_modified:
  - backend/app/ai/schemas.py
  - backend/app/ai/prompt_builder.py
  - backend/app/ai/grounding.py
  - backend/app/api/v1/ai/explain_host.py
  - backend/app/api/v1/ai/explain_remediation.py
  - backend/app/api/v1/ai/__init__.py
  - backend/tests/test_ai_prompt_builder_host.py
  - backend/tests/test_ai_explain_host_remediation.py
autonomous: true
requirements: [AI-04]

must_haves:
  truths:
    - "ExplainHostResponse and ExplainRemediationResponse are schema variants sharing ExplainResponseBase — each is its own grounding shape + schema (D-16), reusing the validation gate unchanged"
    - "build_explain_host_prompt() constructs a narrow allowlist object field-by-field and EXCLUDES all PII: directory_user, assigned_user, managed_by, building, serial_number are never serialized into the prompt (PATTERNS PII allowlist; Critical Failure Mode #3)"
    - "The per-host allowlist includes ONLY: hostname, os_name, os_version, device_category, risk_score, vuln_counts, tags, sla_breach, last_checkin_at — a posture-summary shape, not a concatenation of per-CVE blurbs (D-16)"
    - "The per-remediation grounding record matches the shape DECIDED in Plan 06 (cross-asset-cve OR per-ticket-blast-radius) — implemented per that recorded decision, not re-litigated"
    - "explain_host + explain_remediation routes reuse _run_explain_stream() unchanged — only build_prompt + response_model + grounding fetch differ (the per-view wrappers are thin)"
    - "Both new routes are gated require_analyst (POST) / require_viewer (GET cache-check), tenant-scoped, cache-namespaced by resource_type so a vuln/asset/remediation id can never collide (AI-05)"
    - "Host prompt-injection isolation holds identically to the vuln path — adversarial text in an aggregated finding field stays inside <scanner_data> as data (AI-02)"
    - "An asset with zero findings / a remediation group with no vendor solution text routes to grounded=false, not fabrication (AI-04 empty; AIR-01 cite-or-refuse precursor)"
  artifacts:
    - path: "backend/app/ai/schemas.py"
      provides: "ExplainHostResponse, ExplainRemediationResponse (variants of ExplainResponseBase)"
      contains: "class ExplainHostResponse"
    - path: "backend/app/ai/prompt_builder.py"
      provides: "build_explain_host_prompt (PII-excluding allowlist), build_explain_remediation_prompt"
      contains: "build_explain_host_prompt"
    - path: "backend/app/ai/grounding.py"
      provides: "get_asset_posture(), get_remediation_group() grounding-record assemblers"
    - path: "backend/app/api/v1/ai/explain_host.py"
      provides: "POST/GET /explain-host/{asset_id}"
    - path: "backend/app/api/v1/ai/explain_remediation.py"
      provides: "POST/GET /explain-remediation/{id}"
  key_links:
    - from: "backend/app/api/v1/ai/explain_host.py"
      to: "_run_explain_stream (shared core)"
      via: "build_prompt=build_explain_host_prompt, response_model=ExplainHostResponse"
      pattern: "_run_explain_stream"
    - from: "backend/app/ai/prompt_builder.py build_explain_host_prompt"
      to: "PII exclusion"
      via: "explicit HOST_ALLOWLIST frozenset excluding directory_user/assigned_user/managed_by/building/serial_number"
      pattern: "HOST_ALLOWLIST"
  prohibitions:
    - "The raw AssetDetail dict/row must never be passed through even partially — the per-host builder constructs its own explicitly-named allowlist object (PATTERNS Pitfall 5)"
    - "AI output must never be executed or trusted as instructions — host/remediation paths reuse the same data-only prompt contract and schema gate"
    - "No tenant's cached host/remediation output may reach another tenant — resource_type-namespaced tenant-scoped cache keys"
---

<objective>
Expand the proven per-vuln scaffold to the host and remediation drill views (D-15), reusing the Plan-04 `_run_explain_stream()` core unchanged. Only the grounding-record assembly and the Pydantic response schema vary per view — exactly the reconciliation RESEARCH prescribes for D-15's three-view widening.

Purpose: Ship all three explain affordances (AI-04) with per-view grounding shapes (D-16), while re-auditing the PII allowlist field-by-field for the host view (its endpoint bundles owner PII the vuln view lacks — the concrete evidence-based finding). The per-remediation grounding shape follows the Plan-06 decision.
Output: host + remediation schema variants, PII-excluding prompt builders, grounding assemblers, and thin per-view SSE routes.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-ai-foundation-explain-this-vuln/24-RESEARCH.md
@.planning/phases/24-ai-foundation-explain-this-vuln/24-PATTERNS.md
@.planning/phases/24-ai-foundation-explain-this-vuln/24-CONTEXT.md
@.planning/phases/24-ai-foundation-explain-this-vuln/24-06-SUMMARY.md

<interfaces>
<!-- Reuse Plan 02/04 unchanged -->
_run_explain_stream(db,*,tenant_id,user_email,resource_type,resource_id,record,build_prompt,response_model) (Plan 04).
ExplainResponseBase (Plan 02): summary, business_risk, citations (non-empty), grounded.
build_explain_vuln_prompt + prompt_version + VULN_ALLOWLIST (Plan 02) — the pattern to mirror per view.

Per-host allowlist (PATTERNS §prompt_builder + use-asset-detail.ts:10-52):
  ALLOW: hostname, os_name, os_version, device_category, risk_score, vuln_counts{total,critical,high,medium,low,
    exploitable,kev,sla_breach}, tags, sla_breach, last_checkin_at
  EXCLUDE (PII / low-value): directory_user{email,display_name,department,job_title,...}, assigned_user, managed_by,
    building, serial_number, ip_addresses/mac_addresses (internal), model
Per-remediation: grounding shape per the Plan-06 checkpoint:decision (cross-asset-cve OR per-ticket-blast-radius).
  RemediationTicket shape reference: frontend/src/lib/queries/use-asset-remediations.ts (vuln_count/critical_count/
  high_count/max_severity aggregated within one ticket group).
RBAC + route shape: identical to Plan 04 explain_vuln.py (require_analyst POST, require_viewer GET).
Cache: resource_type in {"host","remediation"} namespaces the Plan-03 cache key (already parameterized).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Host + remediation schema variants + PII-excluding prompt builders</name>
  <read_first>
    - backend/app/ai/schemas.py, backend/app/ai/prompt_builder.py (Plan 02 — the base + vuln pattern to mirror)
    - frontend/src/lib/queries/use-asset-detail.ts (AssetDetail field list 10-52 — the PII fields to EXCLUDE)
    - frontend/src/lib/queries/use-asset-remediations.ts (RemediationTicket aggregate shape)
    - .planning/phases/24-ai-foundation-explain-this-vuln/24-06-SUMMARY.md (the recorded per-remediation grounding decision)
    - .planning/phases/24-ai-foundation-explain-this-vuln/24-PATTERNS.md (Pitfall 5 PII bundling; §prompt_builder host allowlist)
  </read_first>
  <behavior>
    - Test (host schema): ExplainHostResponse validates a posture-summary payload; shares ExplainResponseBase; rejects malformed like the vuln variant
    - Test (host PII exclusion): build_explain_host_prompt on an AssetDetail-shaped input carrying directory_user/assigned_user/managed_by/building/serial_number produces a <scanner_data> block containing NONE of those keys/values
    - Test (host allowlist positive): the block DOES contain hostname/os_name/risk_score/vuln_counts
    - Test (host injection isolation): adversarial text in an aggregated field stays inside <scanner_data>, never in system
    - Test (remediation schema+prompt): ExplainRemediationResponse validates; build_explain_remediation_prompt matches the Plan-06-decided grounding shape and excludes any PII the source carries
    - Test (empty): an asset with zero findings -> the builder still produces a valid prompt whose data signals sparsity (routes to grounded=false downstream)
  </behavior>
  <action>
    Extend `backend/app/ai/schemas.py` with `ExplainHostResponse(ExplainResponseBase)` and `ExplainRemediationResponse(ExplainResponseBase)` (D-16 variants; each may add view-specific optional fields but keeps the shared base). Extend `backend/app/ai/prompt_builder.py` with `HOST_ALLOWLIST: frozenset` (the exact positive list above), `build_explain_host_prompt(record)` constructing the narrow object field-by-field (NEVER passing a raw AssetDetail through — mirror _to_response discipline; explicit exclusion of the PII set), and `build_explain_remediation_prompt(record)` per the Plan-06 grounding decision with its own REMEDIATION_ALLOWLIST. prompt_version() now also folds the new prompts' text (auto-invalidation stays correct). Write `tests/test_ai_prompt_builder_host.py` including host PII-exclusion + injection isolation.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=test JWT_SECRET_KEY=test pytest tests/test_ai_prompt_builder_host.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "HOST_ALLOWLIST\|REMEDIATION_ALLOWLIST" backend/app/ai/prompt_builder.py` >= 2
    - Host PII-exclusion test proves directory_user/assigned_user/managed_by/building/serial_number are absent from the built prompt
    - `grep -c "class ExplainHostResponse\|class ExplainRemediationResponse" backend/app/ai/schemas.py` == 2
    - The remediation builder matches the Plan-06-decided shape (SUMMARY citation in the test comment)
  </acceptance_criteria>
  <done>Host + remediation grounding shapes exist with per-view schema variants and a re-audited, PII-excluding host allowlist.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Grounding assemblers + thin host/remediation SSE routes</name>
  <read_first>
    - backend/app/api/v1/ai/explain_vuln.py (Plan 04 — the route shape to mirror thinly)
    - backend/app/ai/explain.py (_run_explain_stream signature)
    - backend/app/api/v1/ai/__init__.py (ai_router)
    - existing asset detail + remediation query services (get asset posture / remediation group grounding sources; scoped by tenant_id)
    - .planning/phases/24-ai-foundation-explain-this-vuln/24-06-SUMMARY.md (remediation grounding decision)
  </read_first>
  <behavior>
    - Test: POST /api/v1/ai/explain-host/{asset_id} as analyst returns text/event-stream with a validated ExplainHostResponse done frame (mocked engine); Viewer POST -> 403; GET cache-check works
    - Test: POST /api/v1/ai/explain-remediation/{id} analogous
    - Test: a foreign-tenant asset_id/remediation id is not resolvable (tenant-scoped grounding) -> no cross-tenant data
    - Test: cache key for host vs vuln with the same underlying id do NOT collide (resource_type namespacing)
    - Test: an asset with no findings -> grounded=false path, no fabrication
  </behavior>
  <action>
    Create `backend/app/ai/grounding.py` with `async def get_asset_posture(db, tenant_id, asset_id)` (assemble the allowlisted posture record from the existing asset-detail + vuln-counts queries) and `async def get_remediation_group(db, tenant_id, id)` (assemble per the Plan-06 decision). Create `backend/app/api/v1/ai/explain_host.py` and `explain_remediation.py` — thin wrappers mirroring explain_vuln.py, each calling `_run_explain_stream(..., resource_type="host"|"remediation", record=<grounding>, build_prompt=<view builder>, response_model=<view schema>)` for POST and a cache-check GET; register both into ai_router. Write `tests/test_ai_explain_host_remediation.py`.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=test JWT_SECRET_KEY=test pytest tests/test_ai_explain_host_remediation.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Both routes return text/event-stream for analysts and 403 for viewers on POST (RBAC matrix asserted)
    - resource_type-namespacing test proves host vs vuln cache keys never collide
    - foreign-tenant id -> not resolvable (asserted)
    - `grep -c "_run_explain_stream" backend/app/api/v1/ai/explain_host.py backend/app/api/v1/ai/explain_remediation.py` each >= 1 (core reused, not reimplemented)
  </acceptance_criteria>
  <done>Host + remediation explain endpoints stream validated, grounded, tenant-scoped output reusing the shared engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AssetDetail (with owner PII) -> host prompt_builder | the highest-PII-risk boundary in the phase — owner PII bundled inline with grounding fields |
| aggregated finding/remediation fields -> model | untrusted aggregated scanner text crosses as data |
| host/remediation SSE -> browser | validated, tenant-owned aggregate content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-32 | Information Disclosure | owner PII in the host prompt | mitigate | HOST_ALLOWLIST explicit positive list; directory_user/assigned_user/managed_by/building/serial_number excluded at object-construction; raw AssetDetail never passed through; PII-exclusion unit test is the proof (Critical Failure Mode #3) |
| T-24-33 | Tampering / Elevation | injection via aggregated fields | mitigate | Same data-only prompt contract + schema gate reused unchanged from the vuln path |
| T-24-34 | Information Disclosure | cross-tenant aggregate cache collision | mitigate | resource_type-namespaced tenant-scoped cache keys; foreign-tenant ids not resolvable via tenant-scoped grounding |
| T-24-35 | Ungrounded fabrication | sparse asset / no-solution remediation | mitigate | Empty grounding routes to grounded=false (cite-or-refuse precursor for AIR-01), never invention |
</threat_model>

<verification>
- `pytest tests/test_ai_prompt_builder_host.py tests/test_ai_explain_host_remediation.py -q` (per-file) green
- Wave merge: re-run the full `tests/test_ai_*.py` set green
</verification>

<success_criteria>
- All three explain views (vuln/host/remediation) ship with per-view grounding shapes (D-15/D-16)
- Host PII allowlist re-audited and enforced
- Shared engine reused unchanged (thin per-view wrappers)
</success_criteria>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan:
- `ExplainHostResponse`, `ExplainRemediationResponse` in `backend/app/ai/schemas.py`
- `HOST_ALLOWLIST`, `REMEDIATION_ALLOWLIST`, `build_explain_host_prompt()`, `build_explain_remediation_prompt()` in `backend/app/ai/prompt_builder.py`
- `backend/app/ai/grounding.py` — `get_asset_posture()`, `get_remediation_group()`
- `backend/app/api/v1/ai/explain_host.py`, `explain_remediation.py` + ai_router registration
- tests: `test_ai_prompt_builder_host.py`, `test_ai_explain_host_remediation.py`
</artifacts_this_phase_produces>

<output>
After completion, create `.planning/phases/24-ai-foundation-explain-this-vuln/24-08-SUMMARY.md`
</output>
