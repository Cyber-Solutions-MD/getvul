---
phase: 24-ai-foundation-explain-this-vuln
plan: 07
type: tdd
wave: 6
depends_on: [06]
files_modified:
  - backend/alembic/versions/032_add_ai_feedback.py
  - backend/app/ai/models.py
  - backend/app/api/v1/ai/feedback.py
  - backend/app/api/v1/ai/__init__.py
  - backend/tests/test_ai_feedback.py
  - frontend/src/components/vulnerabilities/ai-feedback-control.tsx
  - frontend/src/lib/queries/use-ai-feedback.ts
  - frontend/src/components/vulnerabilities/ai-explanation-section.tsx
  - frontend/src/components/vulnerabilities/ai-feedback-control.test.tsx
autonomous: true
requirements: [AI-04]

must_haves:
  truths:
    - "A new ai_feedback table exists with an explicit tenant_id column (FK->tenants CASCADE), resource_type/resource_id (D-15 three views), user_id (FK->users CASCADE), verdict, nullable note (<=500 chars), created_at, updated_at, and a composite uniqueness on (resource_type, resource_id, user_id) (D-21/D-22)"
    - "Submitting feedback is an idempotent per-user UPSERT: a second submission for the same (resource, user) UPDATES the verdict/note (on_conflict_do_update), so an analyst can change their own verdict (D-22)"
    - "Feedback is capture-only this phase — no feedback data is surfaced back in any UI beyond the analyst's own current thumb/note state (D-21; dashboards are Phase 28)"
    - "The feedback control is a thumbs up/down + optional note beneath the explanation; a thumb alone is a valid submission and the note never blocks (partial submit is fine)"
    - "On a submit failure the thumb state silently reverts with NO blocking error toast (low-stakes accuracy signal — UI-SPEC user decision)"
    - "The note is capped at 500 chars with plain wrap and no char-count warning UI"
    - "Feedback writes an audit row (reusing the existing audit() helper is acceptable here since feedback is a real interactive user action with a real tenant — distinct from the AI-call audit path)"
    - "A feedback verdict for one user/tenant is never readable by another tenant (tenant_id scoping on every query) (AI-06 concurrency/ordering: concurrent per-user upserts each resolve to that user's single row)"
  artifacts:
    - path: "backend/alembic/versions/032_add_ai_feedback.py"
      provides: "ai_feedback table + composite unique index"
      contains: "ai_feedback"
    - path: "backend/app/ai/models.py"
      provides: "AiFeedback SQLAlchemy model"
      contains: "class AiFeedback"
    - path: "backend/app/api/v1/ai/feedback.py"
      provides: "POST /api/v1/ai/feedback/{resource_type}/{resource_id} (upsert)"
      contains: "on_conflict_do_update"
    - path: "frontend/src/components/vulnerabilities/ai-feedback-control.tsx"
      provides: "thumbs + optional note control with optimistic silent-revert"
  key_links:
    - from: "backend/app/api/v1/ai/feedback.py"
      to: "AiFeedback upsert"
      via: "pg_insert(...).on_conflict_do_update(index_elements=['resource_type','resource_id','user_id'])"
      pattern: "on_conflict_do_update"
    - from: "frontend/src/components/vulnerabilities/ai-explanation-section.tsx"
      to: "ai-feedback-control"
      via: "rendered beneath the validated explanation (state 1)"
      pattern: "ai-feedback-control|AiFeedbackControl"
  prohibitions:
    - "No tenant's feedback may reach another tenant — every ai_feedback read/write is tenant_id-scoped"
    - "Feedback failure must not surface a blocking error card/toast (silent optimistic revert only)"
---

<objective>
Capture the highest-signal monitoring metric (analyst thumbs + optional correction note) into a dedicated `ai_feedback` table — capture-only this phase (D-21), the flywheel/dashboards are Phase 28. This is the one genuinely-new table this phase adds.

Purpose: Ship the feedback signal now so Phase 28 has real data to seed golden sets from, without building any surfacing UI yet. The control attaches beneath the validated explanation (Plan 05's section state 1).
Output: migration 032 (ai_feedback), AiFeedback model, feedback upsert endpoint, feedback control + mutation hook wired into the section.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-ai-foundation-explain-this-vuln/24-PATTERNS.md
@.planning/phases/24-ai-foundation-explain-this-vuln/24-CONTEXT.md
@.planning/phases/24-ai-foundation-explain-this-vuln/24-UI-SPEC.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md

<interfaces>
<!-- Analogs (PATTERNS.md) -->
Migration template: alembic/versions/026_add_ticket_comments.py (new table + FK + composite index). HEAD after Plan 03 = 031.
Upsert analog: ticketing/router.py::watch_ticket (681-734) uses pg_insert(...).on_conflict_do_nothing(
  index_elements=["ticket_id","user_id"]); THIS plan needs on_conflict_do_update (D-22 editable) with
  set_={"verdict":...,"note":...,"updated_at":...}. Composite-PK model analog: TicketWatcher (models.py:139-156).
RBAC: gate feedback at require_analyst (mirror the watch/unwatch analog; RESEARCH Assumption A4 flags require_viewer
  as an alternative — use require_analyst for consistency with the watch analog and D-17's actor model).
Frontend mutation analog: use-connectors-admin.ts useCreateConnector (132-151) for the useMutation shape, BUT
  override error handling to SILENT revert (no toast) per UI-SPEC; optimistic-revert precedent: read
  frontend/src/lib/queries/use-mark-blocked.ts onMutate/onError optimistic cache patch (do not invent a new mechanism).
audit() helper (backend/app/audit.py) — acceptable here (feedback is a real interactive user action w/ real tenant).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: ai_feedback migration + model + idempotent upsert endpoint</name>
  <read_first>
    - backend/alembic/versions/026_add_ticket_comments.py (new-table migration template)
    - backend/app/ticketing/models.py (TicketWatcher composite-PK model 139-156)
    - backend/app/ticketing/router.py (watch_ticket pg_insert upsert 681-734)
    - backend/app/api/v1/ai/__init__.py (ai_router)
    - backend/tests/conftest.py (tenant_a/tenant_b/analyst_user/analyst_user_b fixtures)
    - .planning/phases/24-ai-foundation-explain-this-vuln/24-CONTEXT.md (D-21, D-22)
  </read_first>
  <behavior>
    - Test (create): POST feedback (verdict=up, note="off on CVSS") writes one ai_feedback row scoped to the analyst's tenant+user
    - Test (edit / idempotent D-22): a second POST for the same (resource_type,resource_id,user) UPDATES verdict/note to a single row (not a duplicate)
    - Test (thumb-only): POST with verdict and null note succeeds
    - Test (note cap): a note > 500 chars is rejected (422) or truncated per the schema decision (choose reject-422, matching request-validation convention)
    - Test (tenant isolation): analyst_user_b (tenant_b) cannot read/overwrite tenant_a's feedback row for the same resource
    - Test (RBAC): a viewer... (decide per interfaces — gate require_analyst; assert a viewer is 403 OR allowed per the chosen gate, documented)
  </behavior>
  <action>
    Create migration `032_add_ai_feedback.py` (down_revision="031_add_audit_logs_tenant_created_index"; confirm HEAD is 031 at execute time) creating `ai_feedback`: id UUID PK, tenant_id UUID FK->tenants CASCADE NOT NULL, resource_type String(20) NOT NULL, resource_id String(200) NOT NULL, user_id UUID FK->users CASCADE NOT NULL, verdict String(8) NOT NULL (up/down), note Text nullable, created_at + updated_at timestamptz; add a composite UNIQUE index on (resource_type, resource_id, user_id). Create `backend/app/ai/models.py` with the `AiFeedback` SQLAlchemy model matching it. Create `backend/app/api/v1/ai/feedback.py`: `POST /feedback/{resource_type}/{resource_id}` gated require_analyst, body {verdict, note}, doing `pg_insert(AiFeedback).values(...).on_conflict_do_update(index_elements=["resource_type","resource_id","user_id"], set_={"verdict":..,"note":..,"updated_at":..})`, scoped to user.tenant_id + user.id, then `audit(db, user, "ai.feedback", resource_type, resource_id, {"verdict":..})` + commit. Register into ai_router. Write `tests/test_ai_feedback.py`.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=test JWT_SECRET_KEY=test alembic upgrade head && ENCRYPTION_KEY=test JWT_SECRET_KEY=test pytest tests/test_ai_feedback.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - [BLOCKING] `alembic upgrade head` applied; `alembic heads` reports 032 BEFORE the pytest assertions run (the model would import-pass without the table, false-positive)
    - Edit test proves a second submission yields ONE row with updated values (on_conflict_do_update)
    - Cross-tenant test proves tenant_b cannot touch tenant_a's row
    - `grep -c "on_conflict_do_update" backend/app/api/v1/ai/feedback.py` >= 1
  </acceptance_criteria>
  <done>The ai_feedback table + editable per-user upsert endpoint exist, tenant-scoped, migration applied.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Feedback control (thumbs + optional note) with silent optimistic revert</name>
  <read_first>
    - frontend/src/components/vulnerabilities/ai-explanation-section.tsx (Plan 05 — attach beneath state 1)
    - frontend/src/lib/queries/use-connectors-admin.ts (useMutation shape 132-151)
    - frontend/src/lib/queries/use-mark-blocked.ts (optimistic onMutate/onError revert pattern — reuse, don't invent)
    - .planning/phases/24-ai-foundation-explain-this-vuln/24-UI-SPEC.md (Copywriting Contract feedback prompt; UI Considerations ai-feedback-control rows)
    - .claude/skills/sketch-findings-getvul/references/copy-voice.md
  </read_first>
  <behavior>
    - Test: renders "Was this explanation accurate?" with thumbs-up (aria-label "Accurate") + thumbs-down (aria-label "Not accurate") + an optional note field (placeholder "What was off? (optional)")
    - Test: clicking a thumb optimistically marks it active and fires the mutation
    - Test (silent revert): a mutation failure reverts the thumb to its prior state with NO error toast/card rendered
    - Test: a thumb without a note submits successfully (note optional)
    - Test: the note field caps input at 500 chars with no char-count warning element
  </behavior>
  <action>
    Create `frontend/src/lib/queries/use-ai-feedback.ts` — a useMutation posting to /api/v1/ai/feedback/{rt}/{id}, with onMutate optimistic thumb update and onError SILENT revert (no toast), modeled on use-mark-blocked.ts (NOT useCreateConnector's toast-on-error). Create `frontend/src/components/vulnerabilities/ai-feedback-control.tsx` — the thumbs + optional 500-char note row, background strip on --color-surface-2, verbatim Copywriting Contract copy, sunset tokens only. Wire it into ai-explanation-section.tsx beneath the validated explanation (section state 1 only — capture-only, per-user current state shown). Write `ai-feedback-control.test.tsx` covering the behaviors.
  </action>
  <verify>
    <automated>cd frontend && npm test -- ai-feedback-control</automated>
  </verify>
  <acceptance_criteria>
    - Silent-revert test: on mutation error, no element with role="alert" or a toast renders; the thumb reverts
    - `grep -c "toast" frontend/src/lib/queries/use-ai-feedback.ts` == 0 (no error toast on the feedback path)
    - aria-labels "Accurate"/"Not accurate" present; note optional; 500-char cap with no warning UI
    - control renders only beneath a validated explanation (section state 1)
  </acceptance_criteria>
  <done>Analysts can thumb + optionally note an explanation; failures revert silently; capture-only, no surfacing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyst browser -> POST /feedback | authenticated interactive write; tenant+user scoped |
| ai_feedback table | per-user, per-tenant verdict/note at rest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-28 | Information Disclosure | cross-tenant feedback read/overwrite | mitigate | Every ai_feedback query scoped by tenant_id + user_id; composite unique on (resource_type,resource_id,user_id); cross-tenant test proves isolation |
| T-24-29 | Tampering (stored XSS) | free-form note field | mitigate | Note stored as text, rendered (later, Phase 28) as React text nodes; capped at 500 chars; server-side length validation |
| T-24-30 | Repudiation | who submitted/edited feedback | mitigate | Feedback writes an audit row (ai.feedback) via the standard audit() helper with the real interactive user + tenant |
| T-24-31 | Denial of Service | feedback spam | accept | Idempotent per-user upsert caps rows to one-per-(resource,user); rate-limited by the existing /api/ zone — low residual risk |
</threat_model>

<verification>
- [BLOCKING] `alembic upgrade head` == 032
- `pytest tests/test_ai_feedback.py -q` green (per-file)
- `npm test -- ai-feedback-control` green
</verification>

<success_criteria>
- ai_feedback table + editable per-user upsert endpoint (tenant-scoped)
- Silent-optimistic feedback control wired beneath the explanation
- Capture-only (no surfacing this phase)
</success_criteria>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan:
- migration `032_add_ai_feedback.py`
- `backend/app/ai/models.py` — `AiFeedback`
- `backend/app/api/v1/ai/feedback.py` — POST `/feedback/{resource_type}/{resource_id}` + ai_router registration
- `frontend/src/lib/queries/use-ai-feedback.ts` — `useAiFeedback()`
- `frontend/src/components/vulnerabilities/ai-feedback-control.tsx`
- tests: `test_ai_feedback.py`, `ai-feedback-control.test.tsx`
</artifacts_this_phase_produces>

<output>
After completion, create `.planning/phases/24-ai-foundation-explain-this-vuln/24-07-SUMMARY.md`
</output>
