---
phase: 24-ai-foundation-explain-this-vuln
plan: 09
type: tdd
wave: 8
depends_on: [08]
files_modified:
  - frontend/src/components/ai/ai-explanation-section.tsx
  - frontend/src/components/vulnerabilities/drill-content.tsx
  - frontend/src/app/(authed)/dashboard/assets/[id]/page.tsx
  - frontend/src/components/assets/remediation-timeline.tsx
  - frontend/src/components/ai/ai-explanation-section.test.tsx
autonomous: true
requirements: [AI-04]

must_haves:
  truths:
    - "The AI Explanation section component is generalized over resourceType ('vuln'|'host'|'remediation') and resourceId and reused verbatim across all three views — no per-view copy of the state machine (D-15)"
    - "The asset-detail (host) page renders an AI Explanation section (resourceType='host') in its main column with the same 8 states, inline citations, and role-gating as the vuln view"
    - "The remediation surface renders an AI Explanation section (resourceType='remediation') with the same states"
    - "Each view calls its matching backend route: host->/explain-host/{asset_id}, remediation->/explain-remediation/{id} (the hook builds the path from resourceType)"
    - "All three views share the identical section chrome, color families, copy, reduced-motion behavior, and RBAC visibility rules established in Plan 05"
    - "The host and remediation cache-checks are cheap GETs on mount (D-09) and never collide with the vuln cache (resource_type namespacing carried through the query key)"
  artifacts:
    - path: "frontend/src/components/ai/ai-explanation-section.tsx"
      provides: "the view-agnostic AI Explanation section (moved from components/vulnerabilities, generalized)"
      contains: "resourceType"
  key_links:
    - from: "frontend/src/app/(authed)/dashboard/assets/[id]/page.tsx"
      to: "ai-explanation-section resourceType='host'"
      via: "component mount in the asset detail main column"
      pattern: "resourceType=.host."
    - from: "frontend/src/components/assets/remediation-timeline.tsx"
      to: "ai-explanation-section resourceType='remediation'"
      via: "component mount"
      pattern: "resourceType=.remediation."
  prohibitions:
    - "No per-view duplicate of the section state machine — one generalized component drives all three views (D-15 sequencing: the per-vuln foundation is reused, not reimplemented)"
    - "No new spinner, no red states, no second gradient CTA, no freehand hex, no font substitution — the Plan-05 design constraints carry to all views"
---

<objective>
Complete D-15's three-view widening on the frontend: mount the (now generalized) AI Explanation section on the host (asset-detail) and remediation surfaces, reusing the exact component built and verified for the vuln view. This is the last expansion step — after it, "Explain this" ships on all three drill views.

Purpose: Deliver AI-04's "all three drill views" affordance without reimplementing the state machine per view. The section component from Plan 05 already takes (resourceType, resourceId); this plan promotes it to a shared location and mounts it on two more surfaces.
Output: generalized `components/ai/ai-explanation-section.tsx`; host page + remediation timeline wired.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-ai-foundation-explain-this-vuln/24-UI-SPEC.md
@.planning/phases/24-ai-foundation-explain-this-vuln/24-PATTERNS.md
@.claude/skills/sketch-findings-getvul/references/page-layouts.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md

<interfaces>
<!-- From Plan 05 (already built + verified in Plan 06) -->
ai-explanation-section.tsx: takes {resourceType, resourceId, role, keyConfigured}; renders the 8 states;
  uses useExplainCache(resourceType, resourceId) + useExplainStream(resourceType, resourceId).
useExplainStream builds the path `/api/v1/ai/explain-${resourceType}/${resourceId}` (resourceType maps 1:1 to the
  backend route suffix: vuln->explain-vuln, host->explain-host, remediation->explain-remediation).
ai-explanation-citations.tsx (Plan 05) — reused unchanged (renders any ExplainResponseBase variant).
Host surface: frontend/src/app/(authed)/dashboard/assets/[id]/page.tsx (two-column detail + metadata rail — the
  section goes in the MAIN column per the detail-page pattern, page-layouts.md). asset_id is the resourceId.
Remediation surface: frontend/src/components/assets/remediation-timeline.tsx — the section goes near the remediation
  content; the remediation group id (per the Plan-08 grounding decision) is the resourceId.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Generalize the AI Explanation section to a shared, view-agnostic component</name>
  <read_first>
    - frontend/src/components/vulnerabilities/ai-explanation-section.tsx (Plan 05 — the component to move/generalize)
    - frontend/src/components/vulnerabilities/ai-explanation-citations.tsx (Plan 05 — reused unchanged)
    - frontend/src/components/vulnerabilities/drill-content.tsx (update the import path after the move)
    - .planning/phases/24-ai-foundation-explain-this-vuln/24-UI-SPEC.md (Section body states — must be view-agnostic)
  </read_first>
  <behavior>
    - Test: the section renders identically for resourceType='vuln', 'host', 'remediation' given equivalent state (same chrome, same states)
    - Test: the hook path resolves to explain-host for resourceType='host' and explain-remediation for 'remediation'
    - Test: role-gating (Viewer no-button, no-key role-gated CTA) behaves identically across resourceTypes
    - Test: the vuln drill still renders the section after the import-path change (no regression)
  </behavior>
  <action>
    Move `ai-explanation-section.tsx` and `ai-explanation-citations.tsx` from `components/vulnerabilities/` to a shared `components/ai/` directory (view-agnostic home), keeping them fully generalized over `resourceType`/`resourceId` (they already are per Plan 05). Update the import in `drill-content.tsx` to the new path. Confirm the section takes no vuln-specific props. Write/relocate `components/ai/ai-explanation-section.test.tsx` covering the three-resourceType parity + path-resolution behaviors.
  </action>
  <verify>
    <automated>cd frontend && npm test -- ai-explanation-section</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ai/ai-explanation-section.tsx` exists and takes a `resourceType` prop; no vuln-only prop remains
    - `grep -c "components/ai/ai-explanation-section\|components/vulnerabilities/ai-explanation-section" frontend/src/components/vulnerabilities/drill-content.tsx` shows the import points at the new shared path
    - Parity test passes for all three resourceType values
    - `npm test -- use-explain-stream ai-explanation-citations` still green (no regression from the move)
  </acceptance_criteria>
  <done>One shared, view-agnostic AI Explanation section; the vuln view consumes it from the new location with no regression.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Mount the section on the host (asset-detail) and remediation surfaces</name>
  <read_first>
    - frontend/src/app/(authed)/dashboard/assets/[id]/page.tsx (asset detail two-column layout; main column)
    - frontend/src/components/assets/remediation-timeline.tsx (remediation surface)
    - .claude/skills/sketch-findings-getvul/references/page-layouts.md (detail two-column + rail pattern — section goes in main column)
    - frontend/src/components/ai/ai-explanation-section.tsx (Task 1 output)
  </read_first>
  <behavior>
    - Test: the asset-detail page renders an AI Explanation section with resourceType='host' and the asset id as resourceId
    - Test: the remediation surface renders an AI Explanation section with resourceType='remediation'
    - Test: host cache-check hits /api/v1/ai/explain-host/{asset_id} (mocked) and vuln/host query keys do not collide
    - Test: role-gating renders correctly on the host page (Viewer sees no button)
  </behavior>
  <action>
    Mount `<AiExplanationSection resourceType="host" resourceId={assetId} .../>` in the main column of the asset-detail page (`assets/[id]/page.tsx`), following the two-column detail pattern (main column, not the metadata rail). Mount `<AiExplanationSection resourceType="remediation" resourceId={remediationGroupId} .../>` on the remediation surface (`remediation-timeline.tsx`) using the group id per the Plan-08 grounding decision. Pass the current user role + key-configured signal exactly as the vuln view does. Extend `ai-explanation-section.test.tsx` (or add host/remediation mount tests) covering the two new surfaces.
  </action>
  <verify>
    <automated>cd frontend && npm test -- ai-explanation-section && npm test -- "assets/[id]"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "resourceType=\"host\"\|resourceType={'host'}\|resourceType='host'" frontend/src/app/(authed)/dashboard/assets/[id]/page.tsx` >= 1
    - `grep -c "resourceType=\"remediation\"\|resourceType='remediation'" frontend/src/components/assets/remediation-timeline.tsx` >= 1
    - Host cache-check path test hits explain-host; query-key namespacing prevents vuln/host collision
    - Existing asset-detail page tests still green (no layout regression)
  </acceptance_criteria>
  <done>"Explain this" ships on all three views (vuln/host/remediation) driven by one shared component (D-15 complete).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host/remediation browser -> SSE endpoints | Bearer-authed fetch to server-validated aggregate explanations |
| validated aggregate payload -> DOM | only schema-valid fields render as text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-36 | Tampering (XSS) | rendering host/remediation model output | mitigate | Reuses the Plan-05 citation renderer (React text nodes, no dangerouslySetInnerHTML) unchanged for all variants |
| T-24-37 | Elevation of Privilege | Viewer triggering paid host/remediation calls | mitigate | Same role-gating component (no button for Viewer) + backend require_analyst on the new routes (Plan 08) |
| T-24-38 | Information Disclosure | owner PII appearing in the host explanation UI | mitigate | The host prompt-builder allowlist (Plan 08) already excludes PII server-side; the UI renders only the validated, PII-free payload |
</threat_model>

<verification>
- `npm test -- ai-explanation-section` green (three-view parity + host/remediation mounts)
- Existing asset-detail + vuln drill tests still green (no regression)
- Full phase gate (Plan-06 human-verify already covered vuln; host/remediation spot-checked in the phase verification)
</verification>

<success_criteria>
- All three explain views live on the frontend, driven by one shared component
- No regression to the vuln view or asset-detail page
- Design constraints (no spinner/red/second-gradient) hold across views
</success_criteria>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan:
- `frontend/src/components/ai/ai-explanation-section.tsx` (moved + generalized) and `components/ai/ai-explanation-citations.tsx`
- host section mount in `assets/[id]/page.tsx`; remediation section mount in `remediation-timeline.tsx`
- test: `components/ai/ai-explanation-section.test.tsx`
</artifacts_this_phase_produces>

<output>
After completion, create `.planning/phases/24-ai-foundation-explain-this-vuln/24-09-SUMMARY.md`
</output>
