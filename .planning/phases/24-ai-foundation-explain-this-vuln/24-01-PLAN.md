---
phase: 24-ai-foundation-explain-this-vuln
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/api/v1/ai/__init__.py
  - backend/app/api/v1/ai/spike.py
  - backend/app/main.py
  - nginx/nginx.conf
  - backend/app/connectors/schemas.py
  - backend/app/connectors/tester.py
  - backend/app/connectors/router.py
  - frontend/src/app/(authed)/dashboard/connectors/page.tsx
  - frontend/src/components/connectors/microcopy.ts
  - frontend/src/components/connectors/wizard/add-connector-wizard.test.tsx
autonomous: true
requirements: [AI-01, AI-03]
user_setup:
  - service: anthropic
    why: "BYOK — a real (dev/personal) Anthropic API key is needed only to run the Wave-0 Haiku effort smoke-test (RESEARCH Pitfall 1). Production keys are tenant-supplied via the wizard; GetVul stores no key of its own."
    env_vars:
      - name: GETVUL_DEV_ANTHROPIC_KEY
        source: "console.anthropic.com -> Settings -> API keys (a personal/dev key, NEVER a tenant key)"

must_haves:
  truths:
    - "A minimal text/event-stream endpoint yields multiple chunks that arrive at the browser/curl incrementally (first chunk before the last) through the full nginx -> Docker path — proving true incremental SSE works before the real engine is built"
    - "effort:'low' on claude-haiku-4-5 is confirmed working OR the request builder branches to omit effort for Haiku (RESEARCH Pitfall 1 resolved, not assumed)"
    - "A tenant admin sees an 'Anthropic' connector card in /dashboard/connectors and can open the existing Phase-19 add-connector wizard for it (api_key password field + model select field)"
    - "test_anthropic() returns success for a valid key and failure for an invalid key via a free count_tokens call — no inference billed (D-04)"
    - "Only one ANTHROPIC connector_config row per tenant is the resolution target for AI key lookup (AI-01 adjacency: no ambiguous multi-row merge)"
    - "The model dropdown renders its options in a fixed, stable order with per-option guidance copy (D-05) (AI-01 ordering)"
  artifacts:
    - path: "backend/app/api/v1/ai/spike.py"
      provides: "Throwaway incremental-SSE proof endpoint GET /api/v1/ai/_spike"
    - path: "nginx/nginx.conf"
      provides: "location /api/v1/ai/ block with proxy_buffering off in BOTH server blocks"
      contains: "proxy_buffering off"
    - path: "backend/app/connectors/tester.py"
      provides: "test_anthropic() + TESTERS['ANTHROPIC'] entry"
      contains: "test_anthropic"
    - path: "backend/app/connectors/schemas.py"
      provides: "CONNECTOR_TYPES['ANTHROPIC'] ConnectorTypeInfo entry"
      contains: "ANTHROPIC"
  key_links:
    - from: "frontend/src/app/(authed)/dashboard/connectors/page.tsx"
      to: "backend CONNECTOR_TYPES['ANTHROPIC']"
      via: "useConnectorTypes() -> AddConnectorWizard connectorType='ANTHROPIC'"
      pattern: "ANTHROPIC"
    - from: "backend/app/connectors/tester.py TESTERS"
      to: "test_anthropic"
      via: "test_connector() dispatch"
      pattern: "TESTERS\\[.ANTHROPIC.\\]"
  prohibitions:
    - "No shared/fallback Anthropic key may be introduced under any code path — the spike endpoint and tester must never read a GetVul-owned key from env for tenant-facing behavior (the dev key is smoke-test-only, never a runtime fallback)"
---

<objective>
Establish the two non-AI-call foundations the tracer needs, and de-risk the phase's highest-risk item first.

1. Prove true incremental SSE reaches the browser through nginx (RESEARCH Pitfall 2 — the backend has no multi-yield StreamingResponse precedent; every existing use is one-shot `iter([bytes])`). A throwaway spike endpoint yields delayed chunks; verify they arrive incrementally, not buffered-then-dumped.
2. Register the `ANTHROPIC` connector type end-to-end, reusing the Phase-19 add-connector wizard verbatim (D-03) — zero migration (RESEARCH Pattern 1: `connector_type` is `String(30)`, validated by the `CONNECTOR_TYPES` dict, swept by the existing `rotate_credentials()`).
3. Resolve the Haiku `effort` open question with a live smoke-test (RESEARCH Pitfall 1) so D-01's dropdown is honest.

Purpose: The SSE spike removes the phase's biggest unknown before the real engine (Plan 04) is built on top. The connector type is "admin sets key" — the front half of the tracer — reusing generic infra.
Output: `anthropic` dependency added; incremental-SSE proven; `ANTHROPIC` connector card live; `test_anthropic()` tester wired.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-ai-foundation-explain-this-vuln/24-RESEARCH.md
@.planning/phases/24-ai-foundation-explain-this-vuln/24-PATTERNS.md
@.planning/phases/24-ai-foundation-explain-this-vuln/24-CONTEXT.md
@.planning/phases/24-ai-foundation-explain-this-vuln/COVERAGE.md

<interfaces>
<!-- Extracted from PATTERNS.md — executor uses these directly, no exploration needed -->

CONNECTOR_TYPES["OKTA"] shape (backend/app/connectors/schemas.py:261-275) — the entry template:
  ConnectorTypeInfo(id, name, description, fields=[{name,label,type,required}], permissions=[...], setup_url, notes)
  A "select" field type precedent exists in CROWDSTRIKE.base_url (lines 38, 51-56); get_connector_types()
  (router.py:60-69) maps base_urls -> a defaults map. The model dropdown's OPTION LABELS + D-05 guidance copy
  are a FRONTEND concern (not base_urls) — the backend only declares a {name:"model", type:"select", required:true} field.

test_okta() shape (backend/app/connectors/tester.py:424-455): async (credentials: dict, config: dict) -> ConnectorTestResult;
  guard-clause on missing fields, try/except Exception, returns ConnectorTestResult(success, message, details).
  TESTERS dict registration at lines 496-512; test_connector() dispatcher (515-519) needs NO change (generic over TESTERS.get()).

RESEARCH Pattern 3 supplies the full test_anthropic() body (AsyncAnthropic(...).messages.count_tokens()).

Existing nginx `location /api/` block (nginx/nginx.conf HTTP lines 72-79, HTTPS lines 142-149) is the base to extend.

frontend microcopy.ts (lines 13-32): ConnectorCategory union + CATEGORY_LABELS + CATEGORY_ORDER + CATEGORY_EMPTY.
frontend page.tsx (lines 51-67): CONNECTOR_CATEGORIES dict mirroring backend router.py CONNECTOR_CATEGORIES (lines 33-49).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SSE spike endpoint + nginx AI location block + Haiku effort smoke-test</name>
  <read_first>
    - backend/app/main.py (StreamingResponse usage at export_resource lines 460-493; include_router calls lines 315-325)
    - nginx/nginx.conf (existing `location /api/` blocks, HTTP 72-79 + HTTPS 142-149)
    - backend/app/pyproject.toml equivalent: backend/pyproject.toml [project.dependencies]
    - .planning/phases/24-ai-foundation-explain-this-vuln/24-RESEARCH.md (Pattern 7 nginx diff; Pitfall 1 Haiku effort; Pitfall 2 incremental-SSE spike)
  </read_first>
  <action>
    Add `"anthropic>=0.120.0"` to backend/pyproject.toml [project.dependencies] (Docker build runs `pip install -e ".[dev]"`, no requirements.txt to sync). Create `backend/app/api/v1/ai/__init__.py` exposing an `ai_router = APIRouter(prefix="/api/v1/ai")` that later plans include sub-routers into; register it once in backend/app/main.py via `app.include_router(ai_router)` alongside the existing include_router calls. Create `backend/app/api/v1/ai/spike.py`: a THROWAWAY `GET /_spike` route on `ai_router` returning `StreamingResponse(generator(), media_type="text/event-stream", headers={"Cache-Control":"no-cache","X-Accel-Buffering":"no"})` where `generator()` is an async generator that `yield`s 4 `data: {...}\n\n` frames with `await asyncio.sleep(0.5)` between each. Gate the spike route behind `require_analyst` (never anonymous). Add a `location /api/v1/ai/` block to BOTH the HTTP and HTTPS server blocks in nginx/nginx.conf per RESEARCH Pattern 7: `limit_req zone=api burst=50 nodelay; proxy_pass http://backend/api/v1/ai/; proxy_http_version 1.1; proxy_set_header Host $host / X-Real-IP / X-Forwarded-For / X-Forwarded-Proto; proxy_buffering off; proxy_cache off; chunked_transfer_encoding on; proxy_read_timeout 90s;` (matching the HTTP-vs-HTTPS X-Forwarded-Proto convention already in each block). Then run a live one-call smoke-test with the dev key: `messages.count_tokens` and a tiny `messages.create` on `model="claude-haiku-4-5"` with `output_config={"effort":"low","format":{...}}` — record in the SUMMARY whether effort:'low' succeeds on Haiku; if it 400s, note that the Plan 04 request builder must omit `effort` when `model=="claude-haiku-4-5"`.
  </action>
  <verify>
    <automated>cd backend && docker compose up -d backend nginx && sleep 5 && curl -N -s -H "Authorization: Bearer $DEV_ANALYST_TOKEN" http://localhost/api/v1/ai/_spike | head -c 200 & sleep 1 && echo "PARTIAL-BEFORE-COMPLETE proves incremental delivery"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "anthropic>=0.120" backend/pyproject.toml` returns 1
    - `grep -v '^#' nginx/nginx.conf | grep -c "location /api/v1/ai/"` returns 2 (both server blocks)
    - `grep -v '^#' nginx/nginx.conf | grep -c "proxy_buffering off"` returns >= 2
    - A `curl -N` against `/api/v1/ai/_spike` through nginx prints the first frame visibly BEFORE the stream completes (~2s total, first byte < 0.8s) — NOT all four frames at once after 2s (the classic buffering symptom)
    - SUMMARY records the Haiku effort:'low' smoke-test result (works / 400s -> branch) as a concrete finding
  </acceptance_criteria>
  <done>Incremental SSE proven through the full nginx -> Docker path; anthropic dep added; Haiku effort question resolved and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Register the ANTHROPIC connector type end-to-end (backend + frontend + tester)</name>
  <read_first>
    - backend/app/connectors/schemas.py (CONNECTOR_TYPES["OKTA"] lines 261-275; ConnectorTypeInfo def lines 19-28; ConnectorCreate lines 346-357)
    - backend/app/connectors/tester.py (test_okta lines 424-455; TESTERS dict 496-512; test_connector dispatcher 515-519)
    - backend/app/connectors/router.py (CONNECTOR_CATEGORIES dict lines 33-49; get_connector_types base_urls->defaults lines 60-69)
    - frontend/src/app/(authed)/dashboard/connectors/page.tsx (CONNECTOR_CATEGORIES dict lines 51-67)
    - frontend/src/components/connectors/microcopy.ts (ConnectorCategory union + CATEGORY_LABELS/ORDER/EMPTY lines 13-68)
    - frontend/src/components/connectors/wizard/add-connector-wizard.test.tsx (existing test to extend)
    - .planning/phases/24-ai-foundation-explain-this-vuln/24-RESEARCH.md (Pattern 3 test_anthropic full body)
    - .planning/phases/24-ai-foundation-explain-this-vuln/24-UI-SPEC.md (Copywriting Contract: model dropdown guidance copy D-05)
  </read_first>
  <action>
    Add `CONNECTOR_TYPES["ANTHROPIC"] = ConnectorTypeInfo(id="ANTHROPIC", name="Anthropic", description=..., fields=[{"name":"api_key","label":"Anthropic API Key","type":"password","required":True}, {"name":"model","label":"Model","type":"select","required":True}], permissions=[...], setup_url="https://console.anthropic.com/settings/keys", notes=...)` in connectors/schemas.py. Add `test_anthropic(credentials, config) -> ConnectorTestResult` to connectors/tester.py using `AsyncAnthropic(api_key=...).messages.count_tokens(model=config.get("model","claude-sonnet-5"), messages=[{"role":"user","content":"test"}])` inside try/except catching `AuthenticationError` (invalid key) and generic Exception (connection error) — copy the guard-clause + try/except shape of `test_okta`; register `TESTERS["ANTHROPIC"] = test_anthropic`. Add the connector to categorization: pick a category KEY (decide: a new `"ai_assistant"` category vs. folding into existing — PATTERNS flags this as a plan-time decision; use a new `"ai_assistant"` category so AI is visually distinct from scanners/ticketing). Wire the key in THREE lockstep places: backend `CONNECTOR_CATEGORIES["ANTHROPIC"]="ai_assistant"` (router.py), frontend page.tsx duplicate dict, and microcopy.ts (`ConnectorCategory` union + `CATEGORY_LABELS` + `CATEGORY_ORDER` + `CATEGORY_EMPTY` new entry, sentence-case copy-voice). The `model` select's option labels + D-05 guidance strings ("Sonnet 5 — recommended balance" / "Opus 5 — higher cost" / "Haiku — cheapest, lower grounding fidelity") are rendered frontend-side. Extend add-connector-wizard.test.tsx with a data-driven ANTHROPIC case asserting the card renders + the wizard completes provider->credentials->test->confirm for connectorType="ANTHROPIC".
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=test JWT_SECRET_KEY=test pytest tests/test_connectors/test_ai_tester.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_connectors/test_ai_tester.py` exists and passes: test_anthropic returns success for a mocked valid count_tokens response and `ConnectorTestResult(success=False, message="Invalid API key")` on AuthenticationError, using `httpx.MockTransport` via `AsyncAnthropic(http_client=...)` (mirror backend/tests/test_okta_sync.py)
    - `grep -c ANTHROPIC backend/app/connectors/schemas.py backend/app/connectors/tester.py backend/app/connectors/router.py` each return >= 1
    - `npm test -- add-connector-wizard` passes with the new ANTHROPIC case
    - `grep -c ai_assistant frontend/src/components/connectors/microcopy.ts` returns >= 1 (union + label + order + empty)
    - test_connector() dispatcher is UNCHANGED (generic over TESTERS)
  </acceptance_criteria>
  <done>An admin can open the Anthropic connector card and the wizard's step-3 test calls test_anthropic; no migration was written (ConnectorConfig reuse).</done>
</task>

</tasks>

<assumption_delta_decision>
Run `gsd_run query assumption-delta scan "24" --json`. Expected: `detected: true` (singular "scanner connector" -> plural generalized "connector" identity, now including a non-scanner AI connector).
- Noun now primary: **connector** (the generalized encrypted-config identity) — "scanner connector" is one specialization alongside ticketing, identity, MDM/enrichment, and now AI.
- Decision: **promote** (add-alongside with zero cost). Rationale: RESEARCH confirms `ConnectorConfig.connector_type` is already a generic `String(30)` validated by the `CONNECTOR_TYPES` dict (15 existing entries across scanner/ticketing/identity/MDM), so `"ANTHROPIC"` is a zero-migration promote-style reuse of the already-generic abstraction — no new table, no accepted debt.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /api/v1/ai/_spike | authenticated (require_analyst) request; spike carries no tenant data |
| admin browser -> connector wizard -> ConnectorConfig | tenant Anthropic API key (secret) crosses here; encrypted at rest via existing Fernet |
| test_anthropic -> api.anthropic.com | tenant key leaves to the tenant's own Anthropic account (BYOK); count_tokens is free/non-inference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | ANTHROPIC key at rest in ConnectorConfig.credentials_secret_arn | mitigate | Reuse existing Fernet `encrypt_value` via the standard connector CRUD (`create_connector` json.dumps({field: encrypt_value})) — never store plaintext; `_to_response` returns only `has_credentials: bool`, never the arn |
| T-24-02 | Information Disclosure | test_anthropic error/log output | mitigate | ConnectorTestResult message must never echo the key; catch AuthenticationError -> generic "Invalid API key" (no key material), mirroring encryption.py's redaction discipline |
| T-24-03 | Spoofing / Elevation | /api/v1/ai/_spike unauthenticated access | mitigate | Spike route gated behind `require_analyst`; it is throwaway and MUST be removed or left inert before phase seal (note in SUMMARY) |
| T-24-04 | Elevation of Privilege | non-admin configuring the AI key | mitigate | Connector CRUD already gates at `require_admin` (unchanged) — only Admin/Owner can create the ANTHROPIC row |
| T-24-05 | Denial of Service | /api/v1/ai/ location bypassing rate limits | accept | The new location keeps the existing `limit_req zone=api burst=50 nodelay` — same DoS posture as `/api/`; long-lived-stream concurrency guard is deferred to Plan 03 (D-25) |
</threat_model>

<verification>
- `docker compose up -d backend nginx` then `curl -N /api/v1/ai/_spike` shows incremental frame delivery
- `pytest tests/test_connectors/test_ai_tester.py -x -q` green (per-file, ENCRYPTION_KEY/JWT_SECRET_KEY set)
- `npm test -- add-connector-wizard` green
- No new Alembic migration exists for the connector type (ConnectorConfig reuse — assert `alembic heads` still == 030)
</verification>

<success_criteria>
- Incremental SSE proven through nginx (spike)
- Haiku effort:'low' question resolved and recorded
- ANTHROPIC connector card live and testable via the existing wizard
- Zero migration added for the connector type
</success_criteria>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (excluded from downstream drift verification):
- `backend/app/api/v1/ai/__init__.py` — `ai_router` (APIRouter prefix=/api/v1/ai)
- `backend/app/api/v1/ai/spike.py` — throwaway `_spike` GET route (removed/inert before seal)
- `test_anthropic()` + `TESTERS["ANTHROPIC"]` in `backend/app/connectors/tester.py`
- `CONNECTOR_TYPES["ANTHROPIC"]` in `backend/app/connectors/schemas.py`
- `CONNECTOR_CATEGORIES["ANTHROPIC"]="ai_assistant"` (backend router.py + frontend page.tsx)
- `"ai_assistant"` category in `frontend/src/components/connectors/microcopy.ts`
- nginx `location /api/v1/ai/` block (both server blocks)
- `anthropic>=0.120.0` dependency
</artifacts_this_phase_produces>

<output>
After completion, create `.planning/phases/24-ai-foundation-explain-this-vuln/24-01-SUMMARY.md`
</output>
