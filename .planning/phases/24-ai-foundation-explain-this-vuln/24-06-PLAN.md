---
phase: 24-ai-foundation-explain-this-vuln
plan: 06
type: execute
wave: 5
depends_on: [05]
files_modified: []
autonomous: false
requirements: [AI-01, AI-02, AI-03, AI-04, AI-05, AI-06]

must_haves:
  truths:
    - "The full per-vuln tracer works through the real Docker/nginx stack: admin sets an Anthropic key via the wizard -> analyst opens a vuln drill panel -> clicks 'Explain this vuln' -> a schema-validated, two-tier-cited summary streams in incrementally (not buffered-then-dumped) -> an audit row appears in the audit-log pane"
    - "SSE is NOT buffered by nginx — the 'Analyzing...' state resolves progressively, first content before the stream completes (AI-03 manual verify)"
    - "A persistent 429 surfaces the typed amber 'AI busy — try again in a moment' card (D-25), not a generic error"
    - "The per-remediation grounding shape (D-16) is decided and recorded before any expansion work begins"
    - "D-27: AI-call audit rows (ai.explain.*) surface in the EXISTING audit-log-pane this phase (verify step 9); the dedicated AI usage/cost dashboard stays Phase 28 — do not build a new AI dashboard here"
  artifacts: []
  key_links: []
---

<objective>
The TRACER gate. Before expansion multiplies the scaffold across host + remediation views (Plans 08-09) and feedback (Plan 07), a human verifies the per-vuln end-to-end slice works through the real stack, and the one genuinely-open one-way decision (per-remediation grounding shape) is settled.

Purpose: TRACER_MODE requires the tracer verified before expansion. This is also where the AI-03 nginx-no-buffering check (justified manual per RESEARCH test map) and the axe/contrast + reduced-motion visual checks land. REVERSIBILITY_GATES: the per-remediation grounding shape is a costly one-way door (schema/prompt contract Phases 25-28 build on) whose design is genuinely unresolved (RESEARCH Open Question #2 / Assumption A1) — it earns a checkpoint:decision.
Output: human sign-off (or gap list) + a recorded remediation-grounding decision that Plan 08 implements.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-ai-foundation-explain-this-vuln/24-RESEARCH.md
@.planning/phases/24-ai-foundation-explain-this-vuln/24-UI-SPEC.md
@.planning/phases/24-ai-foundation-explain-this-vuln/24-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify the per-vuln tracer end-to-end through the real stack</name>
  <what-built>
    The complete per-vuln "Explain this vuln" path: ANTHROPIC connector type + wizard (Plan 01), the schema/prompt/audit contracts (Plan 02), BYOK key + tenant-scoped cache + fail-closed budget (Plan 03), the buffer-validate-replay SSE engine + endpoint (Plan 04), and the drill-panel AI Explanation section with inline two-tier citations (Plan 05) — proven in isolation by automated tests, now verified as one live flow.
  </what-built>
  <how-to-verify>
    Follow the local e2e stack setup from project memory (admin user, :3000 CORS, prod build, Firefox dark pref). Then:
    1. `docker compose up -d` (Postgres, Redis, backend, nginx, frontend).
    2. As an ADMIN: go to /dashboard/connectors, open the new "Anthropic" card, run the add-connector wizard with a real dev Anthropic key, confirm step-3 test-before-save passes, and save. (Use GETVUL_DEV_ANTHROPIC_KEY — a dev key, never a tenant/prod key.)
    3. As an ANALYST: open /vulnerabilities, click a finding row to open the drill panel. Confirm the "AI Explanation" section appears between Description and Remediation with sibling-identical chrome.
    4. Click "Explain this vuln". Confirm: an "Analyzing this finding..." pulsing-dot state appears (NOT a generic spinner), then a plain-English summary + business-risk framing renders with scanner_verbatim claims visibly tinted (violet) and ai_interpreted claims carrying a small "AI" tag; hover a tinted claim to see "Scanner-verbatim · from {source_field}".
    5. SSE buffering check (AI-03): run `curl -N -H "Authorization: Bearer <analyst-token>" -X POST http://localhost/api/v1/ai/explain-vuln/<finding_id>` and confirm frames arrive progressively, not all at once after full latency (the classic proxy-buffering symptom).
    6. Re-open the same finding: confirm it renders instantly from cache (no new spend, D-09).
    6b. Rate-limit / busy state (D-25): force a persistent Anthropic 429 (use a key at/over its rate limit, or temporarily point the client at a mock returning 429), click Explain on an UNcached finding, and confirm the amber 'AI busy — try again in a moment' card with a 'Try again' button appears — NOT a generic error, blank panel, or partial text.
    7. As a VIEWER: confirm the section shows muted "No AI explanation generated yet." with NO Explain button.
    8. Remove the key (or use a keyless tenant): confirm the role-gated "AI isn't set up yet" state (Admin sees Configure AI CTA; Analyst sees the ask-an-admin nudge) — never a raw error.
    9. Open the audit-log pane (settings): confirm an `ai.explain.vuln` row with model/tokens/cost/status is present for the call, attributed to the analyst + tenant.
    10. Accessibility: toggle prefers-reduced-motion and confirm the result renders instantly (no token-by-token replay); spot-check that the violet-on-soft citation tint is legible in both dark and light themes (axe contrast — treat AA claims as unproven per project memory; reason about the on-soft token contrast manually).
  </how-to-verify>
  <resume-signal>Type "approved" if the tracer works end-to-end, or describe the issues (they become gap-closure tasks before expansion proceeds).</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide the per-remediation grounding shape (D-16, one-way door)</name>
  <decision>
    What grounding record shape does the per-remediation "Explain this fix" view (D-15/D-16) use? This is a costly one-way decision — the schema variant + grounding query it fixes are a contract Phases 25 (remediation guidance) through 28 build on. RESEARCH flagged it genuinely unresolved (Open Question #2 / Assumption A1); the per-vuln tracer is now proven, so it's the right time to resolve it.
  </decision>
  <context>
    D-16 frames per-remediation as "what applying this one fix accomplishes ACROSS the affected assets + its priority." The existing codebase "remediation" concept (RemediationTicket / Ticket) is per-asset with a single vulnerability_id FK — NOT a cross-asset aggregate. There is no existing cross-asset-by-CVE query. Per-host is separately decided as a "posture summary" aggregate (D-16) and is NOT part of this decision.
  </context>
  <options>
    <option id="cross-asset-cve">
      <name>A — Cross-asset CVE grouping (faithful to D-16's literal framing)</name>
      <pros>Matches "across the affected assets"; most useful for a fleet-wide fix decision; sets a clean aggregate contract for Phase 25</pros>
      <cons>Requires a NEW aggregate query grouping a tenant's affected assets by CVE/fix — more implementation cost; a new grounding shape with its own faithfulness surface</cons>
    </option>
    <option id="per-ticket-blast-radius">
      <name>B — Per-ticket blast radius (reuse existing RemediationTicket shape)</name>
      <pros>Reuses the existing RemediationTicket aggregate (vuln_count/critical_count/max_severity already computed within a ticket group); lower cost; less new query surface</pros>
      <cons>Less faithful to a literal multi-asset reading of D-16; "across the affected assets" interpreted as "scoped to the ticket's actual blast radius"</cons>
    </option>
  </options>
  <resume-signal>Select: cross-asset-cve or per-ticket-blast-radius (Plan 08 implements the chosen grounding shape + schema variant; record the choice + one-line rationale in the SUMMARY).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live analyst/admin -> full stack | this gate exercises every prior boundary at once through real nginx/Docker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-26 | Information Disclosure | dev Anthropic key used in the manual verify | mitigate | The verify uses a dedicated dev/personal key (GETVUL_DEV_ANTHROPIC_KEY), never a tenant or production key; it is a tenant's-own BYOK slot in a dev tenant |
| T-24-27 | Tampering | nginx buffering masking the streaming requirement | mitigate | Step 5's curl -N is the explicit anti-buffering assertion — the one manual check the automated suite cannot cover through the real proxy |
</threat_model>

<verification>
- Human confirms the 10-step end-to-end flow (or files gaps)
- The per-remediation grounding shape is recorded for Plan 08
</verification>

<success_criteria>
- Tracer verified live (streaming incremental, cited, cached, audited, RBAC-gated, inert-when-keyless)
- Per-remediation grounding decision recorded
</success_criteria>

<artifacts_this_phase_produces>
No new symbols — this is a verification + decision gate. Output is human sign-off + a recorded design decision consumed by Plan 08.
</artifacts_this_phase_produces>

<output>
After completion, create `.planning/phases/24-ai-foundation-explain-this-vuln/24-06-SUMMARY.md` recording the sign-off and the remediation-grounding decision.
</output>
