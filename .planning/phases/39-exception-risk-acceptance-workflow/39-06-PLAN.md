---
phase: 39-exception-risk-acceptance-workflow
plan: 06
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - frontend/src/app/(authed)/dashboard/exceptions/page.tsx
  - frontend/src/components/exceptions/exceptions-table.tsx
  - frontend/src/components/exceptions/exceptions-chip-bar.tsx
  - frontend/src/lib/queries/use-exceptions.ts
  - frontend/src/lib/queries/keys.ts
  - frontend/src/components/shell/nav-items.ts
autonomous: true
requirements: [EXC-02, EXC-03]

must_haves:
  truths:
    - "The /dashboard/exceptions list page renders a paginated table (Type pill · CVE/target mono · Scope label · Approver avatar+name · Granted relative · Expires sla-pill · Revoke), reachable from a new 'Exceptions' sidebar entry (icon ShieldOff, after Campaigns, no live-count chip) — EXC-02/EXC-03 visibility."
    - "The Expires column is the visual focal point: the table sorts ascending by expires_at by default (soonest-expiring first, D-19) so amber sla-pill.soon (≤7d) rows cluster at the top without a filter; the header toggles sort direction; rows with equal expires_at render in a stable order — EXC-01 ordering edge."
    - "Expiry countdown reuses the SLA 3-tier pill restricted to its two non-overdue tiers: sla-pill.soon (amber, ≤7d 'Nd left') / sla-pill.ok (green, >7d) — an active exception is never 'overdue'; revoked/expired historical rows use a neutral muted chip, never sla-pill.overdue red (UI-SPEC Color)."
    - "State branches are mutually exclusive error > loading > empty > data (WR-13); loading = skeleton table rows (6-8 shimmer); error = PartialFailureBanner verbatim (HTTP code + request ID + Retry now); populated = the column set above."
    - "Empty (never granted) shows 'No exceptions granted yet' + explanatory body + 'View vulnerabilities' secondary action; filtered-to-zero shows 'Nothing matches this filter' + body naming the active chip conjunction + gradient 'Clear all filters' action (UI-SPEC Copywriting Contract, verbatim)."
    - "zero-one-many: zero → empty state; one → single-row table with pagination footer suppressed; many → standard paginated table sorted ascending by Expires (UI-SPEC)."
    - "Row click toggles an inline accordion expand (full justification + who-granted/when audit metadata) — NOT a drill panel and NOT a router navigation; the table never imports useRouter (UI-SPEC Layout §3)."
    - "The chip-bar exposes two allow-list-clamped axes: Type (False positive / Accept risk) and Scope (Finding / Asset / Asset group); reflected URL values outside the allow-list are dropped (T-38-09 precedent)."
    - "useExceptions() uses staleTime:0 (compute-on-read backend field — a stale cache would show an out-of-date expiring-soon pill / active state), retry:1; queryKeys gains an exceptions:{all,list} block."
    - { statement: "Long scope-target names (asset groups, hostnames) and approver display names truncate with ellipsis + native title tooltip; justification appears only in the inline-expand, never the compact cell; row height stays constant under a long synthetic name.", verification: backstop }
    - "nav-entry overflow: the fixed literal 'Exceptions' label reuses the existing nav-item layout in the fixed-width sidebar — no new overflow surface (UI-SPEC UI Considerations)."
  prohibitions:
    - "No screen ships without empty/loading/error states (CLAUDE.md v1-audit top pain point); no generic SaaS copy ('Something went wrong'); fonts stay Inter + JetBrains Mono; colors via CSS variables only."
  artifacts:
    - path: "frontend/src/app/(authed)/dashboard/exceptions/page.tsx"
      provides: "exceptions list route with WR-13 state branches"
    - path: "frontend/src/components/exceptions/exceptions-table.tsx"
      provides: "sortable table + inline-expand accordion, no useRouter"
    - path: "frontend/src/components/exceptions/exceptions-chip-bar.tsx"
      provides: "Type + Scope filter axes"
    - path: "frontend/src/lib/queries/use-exceptions.ts"
      provides: "useExceptions() query hook (staleTime 0)"
  key_links:
    - from: "frontend/src/lib/queries/use-exceptions.ts"
      to: "GET /api/v1/exceptions"
      via: "api<ExceptionResponse[]>('/api/v1/exceptions')"
      pattern: "/api/v1/exceptions"
    - from: "frontend/src/components/shell/nav-items.ts"
      to: "/dashboard/exceptions"
      via: "WORKFLOW_ITEMS entry"
      pattern: "/dashboard/exceptions"
---

<objective>
Build the manage-only exceptions list surface: a new /dashboard/exceptions page (table +
two-axis chip-bar + all state branches), the query hook + query keys, and the sidebar nav
entry — following the campaigns list as the direct analog and the sketch-findings design
system + 39-UI-SPEC as the locked visual contract.

Purpose: give analysts the "what needs re-review soon" view (D-19) and the per-row Revoke
affordance, satisfying EXC-02/EXC-03 visibility. Granting is contextual to a finding (Plan
07), so this list has no create CTA.
Output: exceptions list page + table + chip-bar + hook + keys + nav entry.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-exception-risk-acceptance-workflow/39-UI-SPEC.md
@.planning/phases/39-exception-risk-acceptance-workflow/39-PATTERNS.md
@.claude/skills/sketch-findings-getvul/SKILL.md
@.planning/phases/39-exception-risk-acceptance-workflow/39-01-SUMMARY.md

<interfaces>
Backend contract (from 39-01 ExceptionResponse): id, type, scope_type, cve_id, target label,
vulnerability_id/asset_id/asset_group_id, justification, approver (display_name), granted_by,
expires_at, revoked_at, created_at (granted-at).
Analogs (PATTERNS §F — copy structure verbatim, swap copy from UI-SPEC):
- frontend/src/app/(authed)/dashboard/campaigns/page.tsx:1-162 — ErrorBoundary>Suspense>Inner + WR-13 branch order + EmptyState compound
- frontend/src/components/campaigns/campaigns-table.tsx:56-121 — tabIndex/Arrow/Home/End keyboard nav + mono truncate-with-title cells
- frontend/src/components/campaigns/campaigns-chip-bar.tsx — ChipBar axes + hardcoded *_ALLOW clamp
- frontend/src/lib/queries/use-campaigns.ts — staleTime:0 query hook
- frontend/src/lib/queries/keys.ts:122-125 — campaigns block to mirror
- frontend/src/components/shell/nav-items.ts:34-50 — WORKFLOW_ITEMS
Reused primitives: ChipBar, EmptyState (Title/Body/Actions), PartialFailureBanner, Pagination,
Avatar, SkeletonTable, the sla-pill + status-pill families.
UI-SPEC Copywriting Contract — use the exact strings for empty/filtered-empty/error copy.
</interfaces>

<read_first>
- frontend/src/app/(authed)/dashboard/campaigns/page.tsx (full)
- frontend/src/components/campaigns/campaigns-table.tsx, campaigns-chip-bar.tsx
- frontend/src/lib/queries/use-campaigns.ts, keys.ts:118-132
- frontend/src/components/shell/nav-items.ts
- .planning/phases/39-exception-risk-acceptance-workflow/39-UI-SPEC.md (Layout §3, Color, Copywriting Contract, UI Considerations)
- .claude/skills/sketch-findings-getvul/references/state-patterns.md, visual-language.md
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: query hook + keys + nav entry</name>
  <files>frontend/src/lib/queries/use-exceptions.ts, frontend/src/lib/queries/keys.ts, frontend/src/components/shell/nav-items.ts</files>
  <read_first>frontend/src/lib/queries/use-campaigns.ts, frontend/src/lib/queries/keys.ts:118-132, frontend/src/components/shell/nav-items.ts:34-50</read_first>
  <action>Add an `exceptions: { all: ['exceptions'] as const, list: () => ['exceptions','list'] as const }` block to `keys.ts` mirroring the campaigns block (no detail key — the inline-expand reads the fetched array). Create `use-exceptions.ts` with `useExceptions()` calling `api<ExceptionResponse[]>('/api/v1/exceptions', { signal })`, `staleTime: 0`, `retry: 1` (mirror use-campaigns.ts; the ExceptionResponse type comes from a shared types file — define it alongside the hook or in the existing api-types location). In `nav-items.ts`, import `ShieldOff` from lucide-react and add `{ label: 'Exceptions', href: '/dashboard/exceptions', icon: ShieldOff }` to WORKFLOW_ITEMS right after the Campaigns entry — no `chip` key (D-N-01).</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "use-exceptions|keys.ts|nav-items" || echo "no type errors in touched files"</automated>
  </verify>
  <done>useExceptions() hook typed against ExceptionResponse[]; keys.exceptions block added; sidebar shows an Exceptions entry after Campaigns with the ShieldOff icon and no chip.</done>
</task>

<task type="auto">
  <name>Task 2: exceptions table (sortable, inline-expand) + two-axis chip-bar</name>
  <files>frontend/src/components/exceptions/exceptions-table.tsx, frontend/src/components/exceptions/exceptions-chip-bar.tsx</files>
  <read_first>frontend/src/components/campaigns/campaigns-table.tsx:56-121, frontend/src/components/campaigns/campaigns-chip-bar.tsx, .planning/phases/39-exception-risk-acceptance-workflow/39-UI-SPEC.md (Layout §3, Color)</read_first>
  <action>Create `exceptions-table.tsx` copying the campaigns-table keyboard-nav shape (tabIndex=0 rows, Arrow/Home/End) but replacing the row-click/Enter handler with a LOCAL inline-accordion expand toggle (full justification + who-granted/when) — do NOT import useRouter, do NOT open a drill panel. Columns: Type (status-pill family: FALSE_POSITIVE→violet 'False positive', ACCEPTED_RISK→amber 'Accept risk'); CVE/target (mono, truncate+title); Scope label; Approver (Avatar+name, truncate+title); Granted (relative 'Nd ago', tabular-nums); Expires (sla-pill.soon amber ≤7d 'Nd left' / sla-pill.ok green >7d; revoked/expired historical rows → muted neutral 'Revoked'/'Expired' chip, never sla-pill.overdue); Revoke (per-row ghost/secondary button, 34×34 icon-btn min hit area — wires to Plan 07's revoke ConfirmModal, render a disabled placeholder if the mutation hook is not yet present). Sort ascending by expires_at by default; make the Expires header toggle sort direction; keep equal-expiry ordering stable. Use font-semibold (600) on labels (2-weight cap). Create `exceptions-chip-bar.tsx` copying campaigns-chip-bar with TWO axes: type (FALSE_POSITIVE/ACCEPTED_RISK → 'False positive'/'Accept risk') and scope_type (FINDING/ASSET/ASSET_GROUP → 'Finding'/'Asset'/'Asset group'), each with a hardcoded *_ALLOW clamp array; searchPlaceholder 'Search CVE / target…'.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "exceptions-table|exceptions-chip-bar" || echo "no type errors in touched files"</automated>
  </verify>
  <done>Table renders the UI-SPEC column set with the sla-pill expiry treatment, default-ascending Expires sort, and inline-expand on row click (no navigation); chip-bar exposes the two allow-list-clamped axes.</done>
</task>

<task type="auto">
  <name>Task 3: exceptions list page with all state branches</name>
  <files>frontend/src/app/(authed)/dashboard/exceptions/page.tsx</files>
  <read_first>frontend/src/app/(authed)/dashboard/campaigns/page.tsx, .planning/phases/39-exception-risk-acceptance-workflow/39-UI-SPEC.md (Copywriting Contract, UI Considerations), .claude/skills/sketch-findings-getvul/references/state-patterns.md</read_first>
  <action>Create `page.tsx` copying the campaigns page's ErrorBoundary>Suspense>Inner composition and the mutually-exclusive WR-13 branch order error > loading > empty > data. Header: Display 32px 'Exceptions' title + count subline. Render `<ExceptionsChipBar />` then the branches: error→PartialFailureBanner (HTTP code + request id + Retry now, exact UI-SPEC copy 'Exceptions failed to load'); loading→SkeletonTable (6-8 rows); empty (never granted)→EmptyState with 'No exceptions granted yet' + the exact body + 'View vulnerabilities' secondary action linking /dashboard/vulnerabilities; filtered-to-zero→EmptyState 'Nothing matches this filter' + body naming the active chip conjunction + gradient 'Clear all filters' action; data→`<ExceptionsTable rows={...} />` + Pagination (footer suppressed for zero/one row). Apply chip-bar filters client-side over the fetched array (mirror campaigns). Use only CSS-variable colors and Inter/JetBrains Mono.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "dashboard/exceptions/page" || echo "no type errors in touched files"</automated>
  </verify>
  <done>Page renders all four mutually-exclusive states with the exact UI-SPEC copy; default view is the ascending-by-Expires table; no useRouter navigation on row click.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reflected URL chip params → filter state | Query-string chip values are untrusted |
| Client role → Revoke button | Client hides buttons for UX only; server RBAC is the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-22 | Tampering | exceptions-chip-bar reflected params | mitigate | hardcoded *_ALLOW clamp arrays — URL values outside the allow-list are dropped, not passed through (T-38-09 precedent) |
| T-39-23 | Elevation of Privilege | Revoke button visibility | accept | client visibility is UX only; the POST /{id}/revoke require_analyst gate (39-01) is the real boundary — no client enforcement claimed |
| T-39-24 | Information Disclosure | list rendering | mitigate | reads GET /api/v1/exceptions which is tenant-scoped server-side (39-01); no client-side cross-tenant merge |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (added by this plan)
- Frontend: exceptions list page.tsx, exceptions-table.tsx, exceptions-chip-bar.tsx, use-exceptions.ts, queryKeys.exceptions block, WORKFLOW_ITEMS 'Exceptions' nav entry
- Type: ExceptionResponse (frontend) mirroring the backend schema
</artifacts_produced>

<verification>
- `npx tsc --noEmit` clean for touched files.
- Grep gate: `grep -v '^#' frontend/src/components/exceptions/exceptions-table.tsx | grep -c useRouter` == 0 (table must not navigate).
- Grep gate: `grep -c "/dashboard/exceptions" frontend/src/components/shell/nav-items.ts` ≥ 1.
</verification>

<success_criteria>
- Exceptions list reachable from the sidebar, renders all state branches with UI-SPEC copy, sorts ascending by Expires, and offers per-row Revoke.
</success_criteria>

<output>
After completion, create `.planning/phases/39-exception-risk-acceptance-workflow/39-06-SUMMARY.md`.
</output>
