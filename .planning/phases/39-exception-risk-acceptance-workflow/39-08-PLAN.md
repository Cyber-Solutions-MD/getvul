---
phase: 39-exception-risk-acceptance-workflow
plan: 08
type: execute
wave: 5
depends_on: [39-03, 39-05, 39-06, 39-07]
files_modified: []
autonomous: false
requirements: [EXC-01, EXC-02, EXC-03, EXC-04]

must_haves:
  truths:
    - "A human confirms the full loop against the running dev stack: grant from a finding's drill panel → the finding disappears from the vuln list AND the dashboard tiles/badges → it appears on /dashboard/exceptions with the correct sla-pill → revoke → it immediately returns to the queue (EXC-01..04 end-to-end)."
    - "A human confirms the exceptions list state branches (loading skeleton, never-granted empty, filtered-to-zero empty, error banner) render with the UI-SPEC copy and the ascending-by-Expires default sort."
  artifacts: []
  key_links: []
---

<objective>
Final human verification of the integrated exception workflow against the running dev stack.
All implementation (backend module, consumer sweep, SLA subtraction, dashboards, frontend
grant + list) is complete before this checkpoint — this task verifies the visible,
functional loop that automated tests cannot fully assert (visual state, dashboard exclusion,
the grant→resurface UX).

Purpose: close EXC-01..04 with a real user confirming the governed exception behaves as the
phase goal promises ("nothing silently ignored forever; auto-resurfaces").
Output: user approval or a defect list feeding a --gaps replan.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-exception-risk-acceptance-workflow/39-UI-SPEC.md
@.planning/phases/39-exception-risk-acceptance-workflow/39-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: verify grant → exclude everywhere → list → revoke → resurface</name>
  <what-built>The complete exception workflow: backend exceptions module + grant/list/revoke endpoints, the compute-on-read exclusion across the vuln list / SLA / risk score / remediation / campaigns / rule engine / dashboards / export, D-16 SLA subtraction, and the frontend grant dialog + exceptions list + drill-panel entry points.</what-built>
  <read_first>.planning/phases/39-exception-risk-acceptance-workflow/39-UI-SPEC.md, /Users/chemencedji/.claude/projects/-Users-chemencedji-Desktop-getvul/memory/getvul-local-e2e-perf-gate.md</read_first>
  <how-to-verify>
    Bring up the local dev stack (admin user, :3000 CORS, prod build per the local-e2e-perf-gate memory) and run `alembic upgrade head` so the exceptions table exists. Then:
    1. Open /dashboard/vulnerabilities, pick an OPEN finding, open its drill panel → click 'Accept risk'. Confirm the dialog opens with type=Accept risk, scope='This finding', a mono CVE/asset header, Expires pre-filled (~90d) with the helper sentence, and 'Grant exception' disabled until Approver + Justification are filled.
    2. Grant it. Confirm the finding disappears from the vuln list, and its asset's badge counts + the /dashboard tiles drop by one (dashboard exclusion).
    3. Open /dashboard/exceptions (new sidebar 'Exceptions' entry). Confirm the row shows the Accept-risk pill, mono CVE/target, approver avatar+name, 'Nd ago' granted, and a green/amber Expires sla-pill; the list is sorted soonest-expiring first.
    4. Click the row → inline accordion expands showing the full justification + who-granted/when. Click Revoke → ConfirmModal 'Revoke this exception?' → confirm 'Revoke exception'.
    5. Confirm the finding immediately reappears in the vuln list and its dashboard counts restore, and is NOT instantly SLA-breached (D-16 subtraction).
    6. Grant an ASSET_GROUP-scoped exception for a CVE with zero current matches → confirm it is accepted (forward-looking, D-11) and audited.
    7. Check the exceptions list state branches: loading skeleton, never-granted empty copy, a filtered-to-zero chip combination, and (if reproducible) the error banner.
    8. Confirm a past/over-cap (2099) expiry date is rejected with the field-level D-14 copy.
  </how-to-verify>
  <acceptance_criteria>
    User confirms all 8 steps behave as described (grant excludes everywhere, list renders with UI-SPEC copy + ascending Expires sort, revoke resurfaces immediately without instant breach, forward-looking group grant accepted, expiry cap enforced). Any deviation is captured as a defect list.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the issues found (routed to a /gsd-plan-phase --gaps replan).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new code in this plan (verification only). The trust boundaries and STRIDE register for
the surfaces being verified are defined in 39-01 (endpoints), 39-03 (SLA/escalation),
39-04/39-05 (consumers/dashboards), and 39-06/39-07 (frontend). This checkpoint confirms
those mitigations hold in the running system — notably T-39-01 (tenant scoping), T-39-05/09
(expiry cap), and T-39-11/15 (no escalation/auto-ticket for a governed accepted finding).
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)
None — verification-only checkpoint. See 39-01..39-07 for the full symbol inventory.
</artifacts_produced>

<verification>
- Human sign-off recorded in the SUMMARY; defects (if any) enumerated for a --gaps replan.
</verification>

<success_criteria>
- The grant → exclude-everywhere → list → revoke → resurface loop is confirmed live, closing EXC-01..04.
</success_criteria>

<output>
After completion, create `.planning/phases/39-exception-risk-acceptance-workflow/39-08-SUMMARY.md`.
</output>
