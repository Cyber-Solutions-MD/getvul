---
phase: 39-exception-risk-acceptance-workflow
plan: 05
type: execute
wave: 4
depends_on: [39-01, 39-03]
files_modified:
  - backend/app/assets/router.py
  - backend/app/users/router.py
  - backend/app/vulnerabilities/dashboard.py
  - backend/app/vulnerabilities/service.py
  - backend/app/export.py
  - backend/app/vulnerabilities/risk_exposure_service.py
  - backend/tests/test_exceptions_dashboards.py
autonomous: true
requirements: [EXC-02]
reversibility:
  costly: [D-15]

must_haves:
  truths:
    - "Actively-excepted findings are excluded from the asset list/detail vuln-count/critical/high/exploitable/kev badges AND the sla_breach badge count in assets/router.py (EXC-02 'dashboards', Tier 2 #11) — the sla_breach badge relies on 39-03's run_sla_tier_pass fix having stopped updating the persisted mirror for excepted rows."
    - "Actively-excepted findings are excluded from the per-owner risk aggregate badges in users/router.py (Tier 2 #12) — EXC-02 'dashboards'."
    - "Actively-excepted findings are excluded from the /dashboard tiles / top-vuln spotlight / nav counts (dashboard.py compute_dashboard_tiles_v10/compute_top_vuln_v10/compute_nav_counts_v10 + service.py get_dashboard_stats) — Tier 2 #13, EXC-02 'dashboards'."
    - "Actively-excepted findings are excluded from CSV / exec-summary export (export.py) — Tier 2 #14, EXC-02 (research emphasis 'exports')."
    - "Actively-excepted findings are excluded from the Asset.risk_exposure_score MAX rollup subquery (risk_exposure_service.py:392-399) — NOT the per-finding score-write loop (347-385, untouched) — Tier 2 #15, D-15 names risk_exposure_score explicitly."
  prohibitions:
    - "Exclusion must never be applied to Tier 3 out-of-scope surfaces (connectors/sync.py ingestion bookkeeping, ticketing status write-back, search.py, ai/grounding.py, trends.py, cspm) — the finding stays OPEN under the hood (D-01); over-application is a defect."
  artifacts:
    - path: "backend/tests/test_exceptions_dashboards.py"
      provides: "dashboard/export/rollup exclusion coverage"
  key_links:
    - from: "backend/app/assets/router.py (sla_breach badge)"
      to: "backend/app/vulnerabilities/sla_tier_service.py::run_sla_tier_pass (39-03)"
      via: "reads persisted sla_due_at/sla_breached that 39-03 stopped updating for excepted rows"
      pattern: "sla_breach"
---

<objective>
Complete EXC-02's literal "dashboards" (and the research emphasis "exports") by threading
the shared exclusion seam into every remaining Tier 2 read surface: asset badges (incl.
sla_breach), per-owner risk aggregates, the /dashboard tiles + nav counts, CSV/exec-summary
export, and the risk_exposure_score MAX rollup — while explicitly NOT touching the Tier 3
out-of-scope surfaces.

Purpose: EXC-02 says "dashboards" without qualification; this plan makes that literal.
Output: 1-3 line WHERE additions per surface + a dashboards test file. Each is identical in
shape to Plan 04's consumer edits (same shared helper).
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-exception-risk-acceptance-workflow/39-RESEARCH.md
@.planning/phases/39-exception-risk-acceptance-workflow/39-PATTERNS.md
@.planning/phases/39-exception-risk-acceptance-workflow/39-01-SUMMARY.md
@.planning/phases/39-exception-risk-acceptance-workflow/39-03-SUMMARY.md

<interfaces>
From 39-01: active_exception_subquery(tenant_id, now) — apply `~active_exception_subquery(...)`.
Tier 2 insertion points (RESEARCH Consumer Sweep #11-16, grep-verified line ranges):
- assets/router.py:261-274 (asset list badges) + 388-403 (asset detail badges incl. sla_breach — reads persisted Vulnerability.sla_due_at directly; 39-03's run_sla_tier_pass fix is the prerequisite)
- users/router.py:130-149 + 358-381 (owner-risk aggregate badges)
- vulnerabilities/dashboard.py:43,189-208,311 (compute_dashboard_tiles_v10/compute_top_vuln_v10/compute_nav_counts_v10) + vulnerabilities/service.py:750 (get_dashboard_stats open_q)
- export.py:266,318 (CSV/exec-summary export)
- risk_exposure_service.py:392-399 (asset-level MAX rollup subquery ONLY — NOT 347-385 per-finding write loop)
Tier 3 DO-NOT-TOUCH: connectors/sync.py, ticketing/service.py, ticketing/daily_sync.py, search.py, ai/grounding.py, trends.py, cspm/*.
</interfaces>

<read_first>
- backend/app/assets/router.py:255-310,385-425
- backend/app/users/router.py:115-175,355-395
- backend/app/vulnerabilities/dashboard.py:43,189-208,311 + service.py:750 (get_dashboard_stats)
- backend/app/export.py:250-330
- backend/app/vulnerabilities/risk_exposure_service.py:347-399
- .planning/phases/39-exception-risk-acceptance-workflow/39-PATTERNS.md §D (Tier 2 #11-16 + "Explicitly out of scope")
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: asset + owner + dashboard + nav badge/count exclusion</name>
  <files>backend/app/assets/router.py, backend/app/users/router.py, backend/app/vulnerabilities/dashboard.py, backend/app/vulnerabilities/service.py</files>
  <read_first>backend/app/assets/router.py:255-310,385-425, backend/app/users/router.py:115-175,355-395, backend/app/vulnerabilities/dashboard.py:43,189-208,311, backend/app/vulnerabilities/service.py:750</read_first>
  <action>Import `active_exception_subquery` from app.exceptions.service into each file. Add `~active_exception_subquery(tenant_id, datetime.now(UTC))` to: the asset list + asset detail vuln-count/critical/high/exploitable/kev/sla_breach badge WHERE clauses (assets/router.py — the sla_breach count relies on 39-03 having stopped the persisted mirror for excepted rows, so this is exclusion-consistent); the owner-risk aggregate WHERE clauses (users/router.py); the compute_dashboard_tiles_v10 / compute_top_vuln_v10 / compute_nav_counts_v10 status-filtered queries (dashboard.py) and get_dashboard_stats' open_q (service.py:750). Do not touch any Tier 3 surface.</action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret pytest tests/test_exceptions_dashboards.py -x -k "asset or owner or dashboard or nav"</automated>
  </verify>
  <done>Asset/owner/dashboard/nav badges and counts all exclude actively-excepted findings; sla_breach badge count reflects the exclusion.</done>
</task>

<task type="auto">
  <name>Task 2: export + risk_exposure_score rollup exclusion + Tier-3-untouched assertion + test file</name>
  <files>backend/app/export.py, backend/app/vulnerabilities/risk_exposure_service.py, backend/tests/test_exceptions_dashboards.py</files>
  <read_first>backend/app/export.py:250-330, backend/app/vulnerabilities/risk_exposure_service.py:347-399, backend/tests/test_campaigns.py (harness), .planning/phases/39-exception-risk-acceptance-workflow/39-PATTERNS.md §D ("Explicitly out of scope")</read_first>
  <action>Add `~active_exception_subquery(...)` to export.py's CSV + exec-summary status-filtered queries (266, 318), and to risk_exposure_service.py's asset-level MAX rollup subquery (392-399) ONLY — leave the per-finding score-write loop (347-385) untouched. Create `backend/tests/test_exceptions_dashboards.py` (test_campaigns harness) with: test_excluded_from_asset_badges, test_excluded_from_sla_breach_badge, test_excluded_from_owner_aggregate, test_excluded_from_dashboard_tiles_and_nav, test_excluded_from_export, test_excluded_from_risk_exposure_rollup, and test_tier3_search_still_returns_excepted (a search for an excepted CVE still returns it — proves Tier 3 search.py was NOT changed, guarding against over-application).</action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret pytest tests/test_exceptions_dashboards.py -x</automated>
  </verify>
  <done>Export + risk_exposure rollup exclude excepted findings; the per-finding write loop and Tier 3 search remain unchanged (asserted); dashboards test file green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Read consumers → leadership dashboards/exports | Excepted findings must not inflate reported open/critical counts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-19 | Information Disclosure (misleading report) | dashboards / export / rollup | mitigate | ~active_exception_subquery on each Tier 2 surface; tenant_id filtered inside the helper |
| T-39-20 | Tampering (over-application) | Tier 3 surfaces | accept→guard | Tier 3 (search/sync/ai/trends/cspm) explicitly NOT modified; test asserts search still returns an excepted CVE (D-01 — finding stays OPEN under the hood) |
| T-39-21 | Tampering (stale mirror leak) | assets sla_breach badge | mitigate | depends on 39-03's run_sla_tier_pass exclusion; ordering enforced via wave 4 depends_on [39-03] |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (added by this plan)
- ~active_exception_subquery exclusion applied to: asset list/detail badges (incl. sla_breach), owner-risk aggregate, dashboard tiles/top-vuln/nav counts, get_dashboard_stats, CSV/exec-summary export, risk_exposure_score MAX rollup
- Test file: backend/tests/test_exceptions_dashboards.py
</artifacts_produced>

<verification>
- `pytest tests/test_exceptions_dashboards.py -x` green (per-file, keys set).
- Grep gate (Tier 3 untouched): `grep -c active_exception_subquery backend/app/search.py backend/app/connectors/sync.py backend/app/vulnerabilities/trends.py 2>/dev/null | grep -v ':0' | wc -l` == 0.
</verification>

<success_criteria>
- EXC-02's literal "dashboards" + exports fully satisfied.
- No Tier 3 over-application (search/sync/ai/trends/cspm unchanged).
</success_criteria>

<output>
After completion, create `.planning/phases/39-exception-risk-acceptance-workflow/39-05-SUMMARY.md`.
</output>
