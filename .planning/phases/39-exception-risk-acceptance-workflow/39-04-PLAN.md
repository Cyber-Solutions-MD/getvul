---
phase: 39-exception-risk-acceptance-workflow
plan: 04
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - backend/app/assets/risk_score.py
  - backend/app/vulnerabilities/remediation_service.py
  - backend/app/vulnerabilities/router.py
  - backend/app/campaigns/service.py
  - backend/app/ticketing/rule_engine.py
  - backend/tests/test_exceptions_consumers.py
autonomous: true
requirements: [EXC-02]
reversibility:
  costly: [D-15]   # comprehensive exclusion surface; recorded per REVERSIBILITY_GATES (`costly`, not `one-way`)

must_haves:
  truths:
    - "An actively-excepted finding is excluded from the risk-scored dashboards via compute_risk_scores' raw-score subquery (EXC-02, D-15)."
    - "An actively-excepted finding is excluded from the remediation grouped view via _base_open_vulns' 'active' branch ONLY (get_remediations_grouped / get_hosts_for_remediation / get_remediations_for_host), while the 'ignored'/'all' branches stay untouched (D-15)."
    - "The hand-rolled remediations_for_host endpoint (vulnerabilities/router.py:936-999), which bypasses the shared helpers, gets its own ~active_exception_subquery so an excepted finding never resurfaces under /hosts/{asset_id}/remediations (Pitfall 5)."
    - "An actively-excepted campaign member is excluded from get_campaign_progress' member count and from bulk_create_campaign_tickets' live-members query (an excepted member is never ticketed) — D-15."
    - "An actively-excepted finding is excluded from the automated ticket-creation rule engine (find_matching_assets / run_rule) so a scheduler tick never auto-opens a ticket for an asset whose only qualifying finding is under an active accept-risk exception (Tier 2 #8, governance-critical) — D-15."
  prohibitions:
    - "An accept-risk decision must never still auto-open a ticket or count toward a risk-scored dashboard while active — leaving rule_engine or compute_risk_scores unpatched would make behavior worse than pre-Phase-39; both must be covered."
  artifacts:
    - path: "backend/tests/test_exceptions_consumers.py"
      provides: "consumer-exclusion coverage incl. governance-critical rule engine + hand-rolled bypass"
  key_links:
    - from: "backend/app/ticketing/rule_engine.py::find_matching_assets"
      to: "backend/app/exceptions/service.py::active_exception_subquery"
      via: "~active_exception_subquery on the counts WHERE"
      pattern: "active_exception_subquery"
    - from: "backend/app/vulnerabilities/remediation_service.py::_base_open_vulns"
      to: "backend/app/exceptions/service.py::active_exception_subquery"
      via: "and_(~active_exception_subquery(...)) on the 'active' branch"
      pattern: "active_exception_subquery"
---

<objective>
Thread the shared active_exception_subquery seam into the core "active work" consumers and
the two governance-critical schedulers named by RESEARCH's sweep: the risk-score subquery,
the remediation grouped view (shared helper + the one hand-rolled bypass), the campaign
denominator + bulk ticket creation, and the automated ticket-creation rule engine.

Purpose: satisfy EXC-02's exclusion across active queues + risk-scored dashboards, and
close the governance gap where an accept-risk decision would otherwise still auto-open a
ticket (worse than the pre-phase status quo).
Output: 1-3 line WHERE-clause additions per consumer, all routed through the one shared
helper, plus a dedicated consumer test file.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-exception-risk-acceptance-workflow/39-RESEARCH.md
@.planning/phases/39-exception-risk-acceptance-workflow/39-PATTERNS.md
@.planning/phases/39-exception-risk-acceptance-workflow/39-01-SUMMARY.md

<interfaces>
From 39-01: active_exception_subquery(tenant_id, now) — apply `~active_exception_subquery(...)`
to EXCLUDE. Import from app.exceptions.service.
Consumer insertion points (RESEARCH Consumer Sweep, PATTERNS §D — grep-verified line ranges):
- assets/risk_score.py:119-131 — raw_score_sub WHERE (status.in_(["OPEN","IN_PROGRESS"]))
- vulnerabilities/remediation_service.py:14-32 — _base_open_vulns; add ONLY to the final "active" branch's and_(...)
- vulnerabilities/router.py:936-999 (remediations_for_host, WHERE ~966-970) — hand-rolled, bypasses shared helpers
- campaigns/service.py:88-118 (get_campaign_progress WHERE) + :249-384 (bulk_create_campaign_tickets WHERE ~275-286)
- ticketing/rule_engine.py:102-111 (find_matching_assets counts_q WHERE) + run_rule (208-234)
</interfaces>

<read_first>
- backend/app/assets/risk_score.py:91-154
- backend/app/vulnerabilities/remediation_service.py (full, 14-32 + the 3 callers)
- backend/app/vulnerabilities/router.py:936-999
- backend/app/campaigns/service.py:88-118, 249-384
- backend/app/ticketing/rule_engine.py:85-234
- backend/tests/test_campaigns.py (inline-seed harness)
- .planning/phases/39-exception-risk-acceptance-workflow/39-PATTERNS.md §D (Consumers 6,7,8,9,10,11)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: risk-score + remediation view (shared helper + hand-rolled bypass) + campaigns exclusion</name>
  <files>backend/app/assets/risk_score.py, backend/app/vulnerabilities/remediation_service.py, backend/app/vulnerabilities/router.py, backend/app/campaigns/service.py, backend/tests/test_exceptions_consumers.py</files>
  <read_first>backend/app/assets/risk_score.py:119-131, backend/app/vulnerabilities/remediation_service.py:14-32, backend/app/vulnerabilities/router.py:936-999, backend/app/campaigns/service.py:88-118,249-384, backend/tests/test_campaigns.py (inline-seed harness)</read_first>
  <action>Import `active_exception_subquery` from app.exceptions.service into each file. In `risk_score.py`, add `~active_exception_subquery(tenant_id, datetime.now(UTC))` to the raw_score_sub `.where(...)` (Consumer 6). In `remediation_service.py::_base_open_vulns`, add `~active_exception_subquery(tenant_id, datetime.now(UTC))` to the final ("active", default) branch's `and_(...)` ONLY — leave the "ignored" and "all" branches unchanged (Consumer 7). In `vulnerabilities/router.py::remediations_for_host`, add the same predicate to its hand-rolled WHERE (Consumer 11 / Pitfall 5 — this endpoint never calls the shared helpers). In `campaigns/service.py`, add the predicate to `get_campaign_progress`'s member-count `.where(...)` and to `bulk_create_campaign_tickets`' live-members `.where(...)` so an excepted member is not counted or ticketed (Consumers 8, 9). CREATE `backend/tests/test_exceptions_consumers.py` here (copy the test_campaigns inline-seed harness) and author THIS task's slice — named to match this task's -k filter: test_excluded_from_risk_scores, test_excluded_from_remediations_grouped, test_ignored_all_branches_still_show (proves the non-active branches are untouched), test_excluded_from_remediations_for_host_bypass, test_excluded_from_campaign_progress, test_excepted_member_not_ticketed. Task 2 appends the rule-engine test to this same file.</action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret pytest tests/test_exceptions_consumers.py -x -k "risk or remediation or campaign or host"</automated>
  </verify>
  <done>test_exceptions_consumers.py exists; risk score, remediation grouped view (incl. remediations_for_host bypass), campaign progress + bulk-ticket all exclude actively-excepted findings; the "ignored"/"all" remediation branches remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: governance-critical — automated ticket rule engine exclusion + rule-engine test</name>
  <files>backend/app/ticketing/rule_engine.py, backend/tests/test_exceptions_consumers.py</files>
  <read_first>backend/app/ticketing/rule_engine.py:85-234, backend/tests/test_exceptions_consumers.py (harness authored by Task 1), .planning/phases/39-exception-risk-acceptance-workflow/39-RESEARCH.md (Tier 2 #8)</read_first>
  <action>Import `active_exception_subquery` into `ticketing/rule_engine.py` and add `~active_exception_subquery(...)` to `find_matching_assets`' counts_q WHERE (and any sibling status-filtered query in run_rule's asset/remediation matcher) so an asset whose only qualifying finding is under an active accept-risk exception no longer matches a rule (Consumer 10). APPEND to `backend/tests/test_exceptions_consumers.py` (created by Task 1): test_excluded_from_rule_engine (an accept-risk exception → the rule engine does NOT auto-open a ticket for that asset).</action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret pytest tests/test_exceptions_consumers.py -x</automated>
  </verify>
  <done>The rule engine excludes actively-excepted findings; consumer test file green including the governance-critical no-auto-ticket case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scheduler tick → ticket provider | The rule engine auto-creates tickets; an unexcluded excepted finding would leak an auto-ticket |
| Read consumers → dashboards | Risk score / remediation / campaign counts must not count excepted findings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-15 | Denial / governance bypass | ticketing/rule_engine.py | mitigate | ~active_exception_subquery on find_matching_assets/run_rule — no auto-ticket for a governed accept-risk finding (Tier 2 #8) |
| T-39-16 | Tampering (incomplete exclusion) | remediations_for_host hand-rolled query | mitigate | its own predicate added (Pitfall 5) — shared-helper coverage is necessary but not sufficient |
| T-39-17 | Information Disclosure (cross-tenant) | every added subquery | mitigate | active_exception_subquery filters tenant_id internally; callers pass user.tenant_id |
| T-39-18 | Tampering (over-exclusion) | _base_open_vulns branches | mitigate | predicate added to the "active" branch only; "ignored"/"all" intentionally still show suppressed/excepted items — test asserts this |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (added by this plan)
- ~active_exception_subquery exclusion applied to: compute_risk_scores, _base_open_vulns (active branch), remediations_for_host, get_campaign_progress, bulk_create_campaign_tickets, find_matching_assets/run_rule
- Test file: backend/tests/test_exceptions_consumers.py (created in Task 1, extended in Task 2)
</artifacts_produced>

<verification>
- `pytest tests/test_exceptions_consumers.py -x` green (per-file, keys set).
- Grep gate: `grep -rl active_exception_subquery backend/app/assets/risk_score.py backend/app/vulnerabilities/remediation_service.py backend/app/vulnerabilities/router.py backend/app/campaigns/service.py backend/app/ticketing/rule_engine.py | wc -l` == 5.
</verification>

<success_criteria>
- Every Tier 1 core consumer + the two governance-critical schedulers exclude actively-excepted findings.
- The intentional non-exclusion of the remediation "ignored"/"all" branches is preserved and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/39-exception-risk-acceptance-workflow/39-04-SUMMARY.md`.
</output>
