---
phase: 39-exception-risk-acceptance-workflow
plan: 03
type: execute
wave: 3
depends_on: [39-01, 39-02]
files_modified:
  - backend/app/vulnerabilities/sla_tier_service.py
  - backend/app/exceptions/service.py
  - backend/app/vulnerabilities/service.py
  - backend/tests/test_exceptions_sla.py
autonomous: true
requirements: [EXC-02, EXC-04]
reversibility:
  costly: [D-15, D-16]   # SLA-engine exclusion + SLA-clock subtraction touch the Phase 36 read-time engine; recorded per REVERSIBILITY_GATES (`costly`, not `one-way`)

must_haves:
  truths:
    - "An actively-excepted finding is excluded from the read-time SLA path (resolve_state_for_vuln via list_vulnerabilities/get_vulnerability) AND the persisted mirror (run_sla_tier_pass) AND the escalation firing (detect_and_escalate) — an accepted-risk finding never fires a Slack/Teams/email/PagerDuty breach alert or an in-app sla_escalation notification (EXC-02, D-15, Tier 2 #9)."
    - "On resurface, a finding's SLA due date is shifted later by the summed duration it spent under lapsed (expired OR revoked) exceptions, so it re-enters as on_track/approaching for re-evaluation rather than instantly breaching (EXC-04, D-16)."
    - "Lapsed-exception windows are interval-MERGED before summing (overlapping [created_at, COALESCE(revoked_at, expires_at)] windows counted once, not double-counted) — Pitfall 4, D-12×D-16 interaction — EXC-04 SLA-subtraction boundary."
    - "The lapsed-exception lookup is batched page-scoped (list_vulnerabilities) / tenant-scoped-once-per-tick (run_sla_tier_pass, detect_and_escalate), never per-row N+1 (mirrors the corr_by_key batching precedent)."
    - "The persisted Vulnerability.sla_due_at/sla_breached mirror and the live sla_state agree for a recently-resurfaced finding — run_sla_tier_pass applies the SAME excepted_seconds subtraction, not only the two read-time callers (Pitfall 1)."
  prohibitions:
    - "An accepted-risk or false-positive finding must never trigger an SLA escalation while its exception is active — detect_and_escalate must exclude it (no breach-alert on a governed-accepted finding)."
  artifacts:
    - path: "backend/app/exceptions/service.py"
      provides: "lapsed_exception_seconds batched lookup + interval-merge helper for D-16"
      contains: "def "
    - path: "backend/app/vulnerabilities/sla_tier_service.py"
      provides: "compute_sla_state excepted_seconds param + exclusion in run_sla_tier_pass/detect_and_escalate"
      contains: "excepted_seconds"
    - path: "backend/tests/test_exceptions_sla.py"
      provides: "interval-merge units + SLA subtraction + escalation-exclusion coverage"
  key_links:
    - from: "backend/app/vulnerabilities/sla_tier_service.py::compute_sla_state"
      to: "backend/app/exceptions/service.py (lapsed-exception lookup)"
      via: "excepted_seconds parameter"
      pattern: "excepted_seconds"
---

<objective>
Wire the exclusion join into the Phase 36 SLA engine (D-15) and implement D-16's
SLA-clock subtraction so a resurfaced finding re-enters for re-evaluation rather than an
instant-breach escalation storm. Covers the two governance-critical SLA surfaces:
run_sla_tier_pass (persisted mirror) and detect_and_escalate (breach-alert firing).

Purpose: honor EXC-02's "SLA timers" exclusion and the phase's "not instantly breached on
resurface" promise (D-16) without re-deriving the risk score (lane discipline).
Output: excepted_seconds subtraction in the SLA state computation + exclusion on both the
persisted-mirror and escalation queries + a dedicated SLA test file.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-exception-risk-acceptance-workflow/39-CONTEXT.md
@.planning/phases/39-exception-risk-acceptance-workflow/39-RESEARCH.md
@.planning/phases/39-exception-risk-acceptance-workflow/39-PATTERNS.md
@.planning/phases/39-exception-risk-acceptance-workflow/39-01-SUMMARY.md

<interfaces>
From 39-01/02: active_exception_subquery(tenant_id, now); ExceptionRecord (created_at, expires_at, revoked_at, scope_type, cve_id, asset_id, asset_group_id, vulnerability_id).
compute_sla_state (sla_tier_service.py:111) current kwargs: first_detected_at, tier_days, approaching_pct, now → returns (sla_due_at, state). Recommended add: excepted_seconds:int=0 → effective_start = first_detected_at + timedelta(seconds=excepted_seconds).
resolve_state_for_vuln (sla_tier_service.py:131) — called from list_vulnerabilities, get_vulnerability, run_sla_tier_pass, detect_and_escalate; by construction only runs on findings that already passed the exclusion filter (so subtraction is over LAPSED windows only — RESEARCH Pattern 3).
run_sla_tier_pass WHERE (sla_tier_service.py:181-186): status.in_(["OPEN","IN_PROGRESS"]) — add ~active_exception_subquery.
detect_and_escalate WHERE (sla_tier_service.py:379-386): same status filter — add ~active_exception_subquery.
Batching precedent: corr_by_key (vulnerabilities/service.py:217-240) — collect page keys → one batched query → dict lookup.
</interfaces>

<read_first>
- backend/app/vulnerabilities/sla_tier_service.py:111-159 (compute_sla_state/resolve_state_for_vuln), :162-224 (run_sla_tier_pass), :353-534 (detect_and_escalate)
- backend/app/vulnerabilities/service.py:217-240 (corr_by_key batching to mirror), :130 (list_vulnerabilities), :276-347 (get_vulnerability SLA inheritance)
- backend/app/exceptions/service.py (add the lapsed-exception lookup here)
- backend/tests/test_campaigns.py (inline-seed harness)
- .planning/phases/39-exception-risk-acceptance-workflow/39-RESEARCH.md §"Pattern 3" + §"Pitfall 1" + §"Pitfall 4"
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lapsed-exception batched lookup + interval-merge, and excepted_seconds in compute_sla_state</name>
  <files>backend/app/exceptions/service.py, backend/app/vulnerabilities/sla_tier_service.py, backend/tests/test_exceptions_sla.py</files>
  <read_first>backend/app/vulnerabilities/sla_tier_service.py:111-159, backend/app/vulnerabilities/service.py:217-240, backend/tests/test_campaigns.py (inline-seed harness), .planning/phases/39-exception-risk-acceptance-workflow/39-RESEARCH.md (Pattern 3, Pitfall 4)</read_first>
  <behavior>
    - A pure interval-merge fn over sorted [start, end] tuples returns total covered seconds, counting overlap once (Pitfall 4). Cases: empty→0; single→its own duration; two disjoint→sum; two overlapping→union not sum; adjacent-touching→merged.
    - lapsed_exception_seconds(...) returns, per (cve_id, asset_id) key, the merged lapsed duration from all matching exceptions where (revoked_at IS NOT NULL OR expires_at <= now), scope-matched the same way active_exception_subquery matches (FINDING/ASSET/ASSET_GROUP), batched in ONE query for a set of keys.
    - compute_sla_state accepts excepted_seconds:int=0 and shifts effective_start = first_detected_at + timedelta(seconds=excepted_seconds); state formula otherwise unchanged.
  </behavior>
  <action>Add to `exceptions/service.py`: a private `_merge_intervals(intervals)` pure function (merge-adjacent-sorted-intervals, ~10 lines) returning summed covered seconds; and `lapsed_exception_seconds(db, tenant_id, keys, now)` that runs ONE batched query (mirroring corr_by_key's tuple_(...).in_(page_keys) shape) fetching all lapsed exceptions matching the page's (cve_id, asset_id) set + covering asset-groups, groups them per key, and returns a dict key→merged seconds. Extend `compute_sla_state` in `sla_tier_service.py` with a keyword-only `excepted_seconds: int = 0` and compute effective_start from it (RESEARCH Pattern 3 signature). Do NOT change the state-classification thresholds. CREATE `backend/tests/test_exceptions_sla.py` here (copy the test_campaigns inline-seed harness) and author THIS task's interval-merge + subtraction units — named to match this task's -k filter: test_merge_intervals_empty, test_merge_intervals_single, test_merge_intervals_disjoint, test_merge_intervals_overlap (union not sum), test_merge_intervals_touching (adjacent merged), and test_excepted_seconds_subtraction (compute_sla_state shifts the due date by excepted_seconds). Task 2 appends to this same file.</action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret pytest tests/test_exceptions_sla.py -x -k "interval or subtraction or merge"</automated>
  </verify>
  <done>test_exceptions_sla.py exists; _merge_intervals counts overlap once; excepted_seconds shifts the due date; the interval unit cases (empty/single/disjoint/overlap/touching) all pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: apply excepted_seconds at read-time + exclude excepted findings from the persisted mirror and escalation</name>
  <files>backend/app/vulnerabilities/sla_tier_service.py, backend/app/vulnerabilities/service.py, backend/tests/test_exceptions_sla.py</files>
  <read_first>backend/app/vulnerabilities/sla_tier_service.py:162-224,353-534, backend/app/vulnerabilities/service.py:130,217-347, .planning/phases/39-exception-risk-acceptance-workflow/39-RESEARCH.md (Pitfall 1)</read_first>
  <behavior>
    - list_vulnerabilities feeds a page-scoped excepted_seconds dict into resolve_state_for_vuln/compute_sla_state so each row's live sla_due_at/sla_state reflects the subtraction; get_vulnerability inherits the same.
    - run_sla_tier_pass excludes actively-excepted rows from its WHERE (persisted mirror stops updating for them) AND applies the same excepted_seconds subtraction when it writes sla_due_at/sla_breached (Pitfall 1 — mirror must not go stale/too-early after resurface).
    - detect_and_escalate excludes actively-excepted rows from its WHERE — an accepted-risk finding fires NO escalation while covered.
    - A resurfaced finding is on_track/approaching (not breached) immediately after resurface given the subtracted clock.
  </behavior>
  <action>In `vulnerabilities/service.py::list_vulnerabilities`, after building page_rows, call `lapsed_exception_seconds` once for the page keys and pass each row's value as `excepted_seconds` into `resolve_state_for_vuln` (extend resolve_state_for_vuln to accept and forward it). Do the same batched-once lookup inside `run_sla_tier_pass` (tenant-wide once per tick) before its per-row state write, and add `~active_exception_subquery(tenant.id, now)` to its WHERE. Add `~active_exception_subquery(tenant.id, now)` to `detect_and_escalate`'s WHERE (do not skip — Tier 2 #9 governance-critical). APPEND to `test_exceptions_sla.py` (created by Task 1): test_sla_subtraction_on_resurface (resurfaced finding is not instantly breached), test_persisted_mirror_matches_live (Pitfall 1 — run_sla_tier_pass mirror equals live after resurface), test_excluded_from_escalation (detect_and_escalate skips an actively-excepted breaching finding — assert no sla_escalation notification/webhook fired).</action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret pytest tests/test_exceptions_sla.py -x</automated>
  </verify>
  <done>Read-time + persisted-mirror SLA both reflect the subtraction and agree; detect_and_escalate excludes actively-excepted findings (no breach alert); SLA test file green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SLA scheduler tick → escalation channels | An unexcluded excepted finding would leak a breach alert to Slack/Teams/email/PagerDuty |
| Read-time SLA vs persisted mirror | Two representations must not diverge for a resurfaced finding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-11 | Repudiation / Denial (governance bypass) | detect_and_escalate | mitigate | ~active_exception_subquery on its WHERE — a governed accepted-risk finding fires no escalation (Tier 2 #9); test asserts no notification |
| T-39-12 | Tampering (over-credit SLA) | lapsed_exception_seconds | mitigate | interval-merge before summing (Pitfall 4) — overlapping windows counted once, cannot push the due date further than actually hidden |
| T-39-13 | Information Disclosure (cross-tenant) | lapsed_exception_seconds batched query | mitigate | tenant_id in the WHERE of the batched lookup, same as active_exception_subquery |
| T-39-14 | Tampering (stale mirror) | run_sla_tier_pass | mitigate | same exclusion + same subtraction applied to the persisted write, not only read-time callers (Pitfall 1) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (added by this plan)
- `_merge_intervals`, `lapsed_exception_seconds` (backend/app/exceptions/service.py)
- `excepted_seconds` param on `compute_sla_state` + forwarded through `resolve_state_for_vuln`
- Exclusion applied to `run_sla_tier_pass` and `detect_and_escalate`
- Test file: backend/tests/test_exceptions_sla.py (created in Task 1, extended in Task 2)
</artifacts_produced>

<verification>
- `pytest tests/test_exceptions_sla.py -x` green (per-file, keys set).
- Grep gates: `grep -v '^#' backend/app/vulnerabilities/sla_tier_service.py | grep -c active_exception_subquery` ≥ 2 (run_sla_tier_pass + detect_and_escalate); `grep -v '^#' backend/app/vulnerabilities/sla_tier_service.py | grep -c excepted_seconds` ≥ 1.
</verification>

<success_criteria>
- Excepted findings excluded from read-time SLA, persisted mirror, and escalation firing.
- Resurfaced findings re-enter for re-evaluation (not instant breach); overlap never double-credits the clock.
</success_criteria>

<output>
After completion, create `.planning/phases/39-exception-risk-acceptance-workflow/39-03-SUMMARY.md`.
</output>
