---
phase: 42-risk-trend-analytics-burndown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/analytics/__init__.py
  - backend/app/analytics/schemas.py
  - backend/app/analytics/service.py
  - backend/app/analytics/router.py
  - backend/app/main.py
  - backend/tests/test_analytics.py
  - frontend/src/components/shell/nav-items.ts
  - frontend/src/lib/queries/keys.ts
  - frontend/src/lib/queries/use-analytics.ts
  - frontend/src/components/analytics/microcopy.ts
  - frontend/src/components/analytics/risk-trend-chart.tsx
  - frontend/src/components/analytics/scope-window-controls.tsx
  - frontend/src/components/analytics/analytics-page-skeleton.tsx
  - frontend/src/app/(authed)/dashboard/analytics/page.tsx
  - frontend/src/app/(authed)/dashboard/analytics/page.test.tsx
autonomous: false
requirements: [TREND-01, TREND-03]
user_setup: []

must_haves:
  truths:
    - "GET /api/v1/analytics/overview returns a tenant-scoped risk-exposure trend series (the stored AVERAGE risk-exposure score per snapshot_date, ascending, per D-07) for a viewer+ user"
    - "The tenant trend series always reads metrics['avg_risk_exposure_score'] regardless of Tenant.cutover_risk_exposure_scoring (D-12), and is bounded by a date-range filter, not the legacy 90-row LIMIT (D-13)"
    - "Opening /dashboard/analytics renders a violet risk-exposure line for the selected window preset with working loading, empty (below min-history threshold), and error states"
    - "A new 'Analytics' sidebar nav entry (LineChart icon, no chip) routes to /dashboard/analytics"
    - "The empty state is gated on the count of snapshot rows in the window, never on whether the score value is falsy (a healthy tenant scoring 0 is not empty)"
    - "A window with exactly one snapshot point renders a single dot marker, never a flat line implying a trend (UI-SPEC E2 zero-one-many)"
    - "risk_model_version_snapshot boundaries are detected server-side and the line is segmented (no interpolation) across a boundary with a neutral labeled marker (D-11); a single-version tenant (today's real-world case) yields zero boundaries and one continuous segment"
    - "Series order is deterministic: strictly ascending by snapshot_date (one row per day, no timestamp ties)"
    - {statement: "Two adjacent version segments that meet at a boundary date are rendered as separate non-interpolated segments (no visual merge/collision across the boundary)", verification: backstop}
  artifacts:
    - path: "backend/app/analytics/service.py"
      provides: "get_scoped_trend_series (tenant scope), detect_version_boundaries, get_analytics_overview orchestrator — all plain async, no FastAPI Depends (D-16)"
      contains: "async def get_analytics_overview"
    - path: "backend/app/analytics/router.py"
      provides: "GET /overview, require_viewer, response_model set"
      contains: "require_viewer"
    - path: "backend/app/analytics/schemas.py"
      provides: "AnalyticsOverviewResponse + AnalyticsTrendPointResponse + VersionBoundaryResponse (Pydantic v2 ConfigDict(from_attributes=True))"
      contains: "ConfigDict(from_attributes=True)"
    - path: "frontend/src/app/(authed)/dashboard/analytics/page.tsx"
      provides: "ErrorBoundary > Suspense > AnalyticsPageInner, error>loading>empty>populated branch order"
      min_lines: 40
    - path: "frontend/src/components/analytics/risk-trend-chart.tsx"
      provides: "recharts LineChart, segmented Line (connectNulls=false) + ReferenceLine per boundary + sr-only ChartDataTable"
      contains: "ReferenceLine"
    - path: "backend/tests/test_analytics.py"
      provides: "tenant-trend + single-version-no-boundary + synthetic-multi-version fixture helper"
      contains: "def test_"
  key_links:
    - from: "frontend/src/lib/queries/use-analytics.ts"
      to: "/api/v1/analytics/overview"
      via: "api() fetch in useQuery"
      pattern: "api.*analytics/overview"
    - from: "backend/app/analytics/router.py"
      to: "backend/app/analytics/service.py"
      via: "await get_analytics_overview(db, user.tenant_id, ...)"
      pattern: "get_analytics_overview"
    - from: "backend/app/main.py"
      to: "backend/app/analytics/router.py"
      via: "app.include_router(analytics_router, prefix='/api/v1/analytics')"
      pattern: "analytics_router"
    - from: "frontend/src/components/shell/nav-items.ts"
      to: "/dashboard/analytics"
      via: "WORKFLOW_ITEMS entry"
      pattern: "/dashboard/analytics"
  prohibitions:
    - "Do NOT call get_risk_score_trend as-is, and do NOT copy its hardcoded .limit(90) or its Tenant.cutover_risk_exposure_scoring flag-branch"
    - "Do NOT touch the existing GET /trends endpoint or the dashboard TrendSection call site"
    - "Do NOT touch capture_daily_snapshot or the scheduler capture cadence"
    - "Do NOT gate the empty state on a falsy score value; gate on snapshot row count in the window"
    - "Do NOT color the version-boundary marker as violet/accent, severity, or success/danger — neutral var(--color-border-strong) only"
    - "Do NOT use pink accent or a gradient CTA on this page"
    - "Do NOT add any new charting or date-picker dependency; recharts 2.12 is already installed"
    - "Do NOT return raw SQLAlchemy Decimal aggregates without round(float(x), N) casting"
    - "Do NOT emit freehand hex; route every color through the foundation.md CSS variables"
    - "Do NOT ship the page without empty, loading, and error states"
    - "Do NOT bypass useUrlState's allow-list clamp for the window preset enum"
---

<objective>
Deliver the Phase 42 tracer: the thinnest production-quality end-to-end vertical slice — one tenant-scoped risk-exposure trend series flowing from `daily_snapshots` through a brand-new `/api/v1/analytics/overview` endpoint to a rendered, version-boundary-aware line on a new `/dashboard/analytics` page, with mandatory loading/empty/error states. This proves the whole spine (new backend module + new page + nav + query hook + chart primitive) before any horizontal expansion (aging/burndown in Plan 02, group scope + custom range + synthetic-boundary verification in Plan 03).

Purpose: Establish and verify the module skeleton, the response contract, and the novel chart primitive at minimum blast radius. Everything Plans 02/03 do is additive onto these files.
Output: `backend/app/analytics/` module (schemas/service/router/init), `main.py` registration, `backend/tests/test_analytics.py`, and the full `frontend/src/components/analytics/` + `/dashboard/analytics` page surface for the tenant trend line.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-risk-trend-analytics-burndown/42-CONTEXT.md
@.planning/phases/42-risk-trend-analytics-burndown/42-RESEARCH.md
@.planning/phases/42-risk-trend-analytics-burndown/42-PATTERNS.md
@.planning/phases/42-risk-trend-analytics-burndown/42-UI-SPEC.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md
@.claude/skills/sketch-findings-getvul/references/visual-language.md

<reversibility_ratings>
- New `/api/v1/analytics/overview` response contract: REVERSIBLE (two-way). Per D-15 it is a pure read-side service; Phase 43 reuses the service functions directly (D-16), not the HTTP contract. No checkpoint:decision gate required.
- New DB artifact: NONE. Read-only over existing tables; no Alembic migration. No one-way DB door.
- Metric-key selection (`avg_risk_exposure_score`, flag-decoupled, D-12): REVERSIBLE — a key choice in the new service, no data/contract change.
Conclusion: no one-way-door decision in this plan → no checkpoint:decision. The tracer end-to-end gate is a checkpoint:human-verify (Task 3), required by TRACER_MODE before Plans 02/03 expand.
</reversibility_ratings>
</context>

<artifacts_this_phase_produces>
This plan (tracer) creates:
- Backend module: `backend/app/analytics/__init__.py` (empty), `schemas.py`, `service.py`, `router.py`.
- Functions: `get_scoped_trend_series(db, tenant_id, *, start, end)` (tenant scope), `detect_version_boundaries(rows)`, `get_analytics_overview(db, tenant_id, *, days)` (orchestrator; Plans 02/03 extend its return).
- Endpoint: `GET /api/v1/analytics/overview` (`require_viewer`).
- `main.py`: `analytics_router` import + `app.include_router(analytics_router, prefix="/api/v1/analytics", tags=["Analytics"])`.
- Schemas: `AnalyticsTrendPointResponse`, `VersionBoundaryResponse`, `AnalyticsOverviewResponse`.
- Frontend: `/dashboard/analytics/page.tsx` (+ `page.test.tsx`), `components/analytics/{risk-trend-chart,scope-window-controls,analytics-page-skeleton,microcopy}.tsx/ts`, `lib/queries/use-analytics.ts`, `analytics` block in `lib/queries/keys.ts`, `Analytics` entry (+ `LineChart` icon import) in `components/shell/nav-items.ts`.
(Plan 02 adds aging/burndown; Plan 03 adds group scope, custom range, synthetic-boundary verification.)
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Backend analytics module + tenant trend series + version-boundary detection + Wave 0 tests</name>
  <read_first>
    - @backend/app/coverage/router.py (exact structural analog: brand-new top-level module, require_viewer thin handler, main.py registration idiom)
    - @backend/app/coverage/schemas.py (Pydantic v2 ConfigDict(from_attributes=True); nullable-vs-zero discipline at coverage/schemas.py:44-46)
    - @backend/app/coverage/service.py (tenant-scoped predicate-composition shape)
    - @backend/app/vulnerabilities/trends.py — DailySnapshot model (lines 23-32); get_risk_score_trend (166-215) shown ONLY as the function to supersede (do NOT copy its .limit(90) at 132 or the cutover flag-branch at 135); get_mttr_trend Decimal cast idiom (line 159); capture_daily_snapshot metrics shape (367-393)
    - @backend/app/main.py (router registration block 311-328; add after coverage_router line 323)
    - @backend/app/vulnerabilities/risk_exposure_service.py — RISK_MODEL_VERSION = "v1" constant (line 92; the value boundaries are detected on, has never changed)
    - @backend/tests/test_risk_boundary_guard.py — DailySnapshot fixture hand-construction (106-114, 200-220); _seed_asset helper (42-48); db_session/tenant_a fixtures
    - @.planning/phases/42-risk-trend-analytics-burndown/42-RESEARCH.md — Code Examples §2 (date-range replaces LIMIT), §3 (detect_version_boundaries sketch), §7 (schema convention); Pitfalls 1 & 6
  </read_first>
  <behavior>
    Author these tests FIRST in backend/tests/test_analytics.py (RED before GREEN); names match 42-VALIDATION.md test map:
    - test_tenant_trend_ignores_cutover_flag: seed 2+ DailySnapshot rows with avg_risk_exposure_score; assert get_scoped_trend_series returns those values ascending by date, identical result whether Tenant.cutover_risk_exposure_scoring is True or False.
    - test_tenant_trend_respects_date_range: seed snapshots older than the window; assert only in-window rows are returned (no 90-row-LIMIT reliance).
    - test_no_boundary_when_single_version: seed 3 rows all "v1"; assert detect_version_boundaries returns [] and the series is one continuous segment.
    - test_version_boundary_detected_and_segmented: SYNTHETIC fixture — seed rows varying risk_model_version_snapshot "v1"→"v2" (the never-before-varied axis, Pitfall 1); assert one boundary dict {date, old_version, new_version} is emitted. (This proves the detector; the visual segmentation is verified in Plan 03.)
    - test_overview_is_tenant_scoped: seed rows for tenant_a and tenant_b; assert get_analytics_overview(db, tenant_a) never returns tenant_b data.
  </behavior>
  <action>
    Create `backend/app/analytics/__init__.py` (empty, mirroring coverage/__init__.py).
    Create `backend/app/analytics/schemas.py` with Pydantic v2 models (`model_config = ConfigDict(from_attributes=True)`): `AnalyticsTrendPointResponse` {date: str, avg_risk_exposure_score: float | None, risk_model_version: str | None}; `VersionBoundaryResponse` {date: str, old_version: str, new_version: str}; `AnalyticsOverviewResponse` {trend: list[AnalyticsTrendPointResponse], boundaries: list[VersionBoundaryResponse]}. Note `avg_risk_exposure_score` is `float | None` where None = gap (reserved for Plan 03's zero-scored-members case); the tenant series in this plan uses the stored value directly.
    Create `backend/app/analytics/service.py` — plain async, no FastAPI Depends (D-16):
      - `async def get_scoped_trend_series(db, tenant_id, *, start, end) -> list[dict]`: `select(DailySnapshot.snapshot_date, DailySnapshot.metrics).where(DailySnapshot.tenant_id == tenant_id, DailySnapshot.snapshot_date.between(start, end)).order_by(DailySnapshot.snapshot_date.asc())`. For each row emit {date: iso, avg_risk_exposure_score: metrics.get("avg_risk_exposure_score"), risk_model_version: metrics.get("risk_model_version_snapshot")}. ALWAYS key on avg_risk_exposure_score; NEVER branch on the cutover flag; NEVER apply a 90-row LIMIT.
      - `def detect_version_boundaries(rows) -> list[dict]`: per RESEARCH §3 — compare each row's risk_model_version to the previous; emit {date, old_version, new_version} on a change.
      - `async def get_analytics_overview(db, tenant_id, *, days=30) -> dict`: compute start=today-days, end=today; call the two functions above; return {"trend": series, "boundaries": boundaries}. Structure this so Plans 02/03 add "aging"/"burndown" keys and scope/group params without reshaping.
    Create `backend/app/analytics/router.py`: `router = APIRouter()`; `GET /overview` with `db: DBSession`, `user: Annotated[CurrentUser, Depends(require_viewer)]`, `days: int = Query(30, ge=7, le=365)` (mirror /trends param cap), `response_model=AnalyticsOverviewResponse`; body `return await get_analytics_overview(db, user.tenant_id, days=days)`.
    Modify `backend/app/main.py`: add `from app.analytics.router import router as analytics_router` near the coverage import, and `app.include_router(analytics_router, prefix="/api/v1/analytics", tags=["Analytics"])` immediately after the coverage_router line.
    Create `backend/tests/test_analytics.py` with the <behavior> tests, importing DailySnapshot from app.vulnerabilities.trends and RISK_MODEL_VERSION from app.vulnerabilities.risk_exposure_service, reusing db_session/tenant_a fixtures (add a tenant_b where needed).
  </action>
  <acceptance_criteria>
    - `cd backend && ENCRYPTION_KEY=test-key-do-not-use-in-prod-000000000000 JWT_SECRET_KEY=test-jwt-secret uv run pytest tests/test_analytics.py -x` passes (all <behavior> tests green).
    - `grep -n "limit(90)" backend/app/analytics/service.py` returns nothing; `grep -n "cutover_risk_exposure_scoring" backend/app/analytics/service.py` returns nothing.
    - `grep -vn '^#' backend/app/analytics/service.py | grep -c "avg_risk_exposure_score"` ≥ 1 and the metric key is not conditional on any flag.
    - `grep -n "analytics_router" backend/app/main.py` shows both the import and the include_router registration with prefix `/api/v1/analytics`.
    - Every query in service.py has `tenant_id ==` inline in its `.where(...)` (grep confirms no query lacks it).
    - Any new numeric aggregate (none required this plan) that enters a response passes through `round(float(x), N)`.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=test-key-do-not-use-in-prod-000000000000 JWT_SECRET_KEY=test-jwt-secret uv run pytest tests/test_analytics.py -x</automated>
  </verify>
  <done>Tenant trend series + version-boundary detection + overview orchestrator exist as plain async functions, registered at GET /api/v1/analytics/overview under require_viewer, all Wave 0 backend tests (incl. the synthetic multi-version fixture) green.</done>
</task>

<task type="tracer">
  <name>Task 2: Analytics page + nav + query hook + segmented trend-line chart + states</name>
  <read_first>
    - @frontend/src/app/(authed)/dashboard/coverage/page.tsx (exact page analog: ErrorBoundary>Suspense>Inner at 426-441; pageErrorFallback 103-113; header 302-311; state-branch order error>loading>empty>populated 318-388; CoverageStripSkeleton 213-225)
    - @frontend/src/app/(authed)/dashboard/coverage/page.test.tsx (vi.spyOn hook-mock 31-35,68-80; renderWithClient 63-66 — one test per state branch)
    - @frontend/src/components/ui/trend-chart.tsx (scaffolding to copy: ResponsiveContainer+aria-hidden wrapper 230-236; usePrefersReducedMotion gating 220-222; sr-only ChartDataTable 188-216; SEVERITY_FILLS export 46-51 — NOT used here, single violet line)
    - @frontend/src/components/ui/trend-chart-skeleton.tsx (full file — chart shimmer shape)
    - @frontend/src/components/dashboard/trend-section.tsx (RangeToggle + useUrlState idiom 23-27)
    - @frontend/src/hooks/use-url-state.ts (enum-clamp hook 11-28 — reuse as-is, do NOT modify)
    - @frontend/src/lib/queries/use-coverage-summary.ts + @frontend/src/lib/queries/use-trends.ts (hook shapes to merge)
    - @frontend/src/lib/queries/keys.ts (coverage block 153-157 — template for the analytics block)
    - @frontend/src/components/shell/nav-items.ts (Coverage entry 53-55; import line 4-6 — append LineChart; ALL_ITEMS/MORE_ITEMS auto-derive)
    - @frontend/src/components/coverage/microcopy.ts (structure to mirror 30-63)
    - @frontend/src/components/states/index.ts (EmptyState / PartialFailureBanner compound contracts)
    - @.planning/phases/42-risk-trend-analytics-burndown/42-UI-SPEC.md — Copywriting Contract (all trend/window/empty strings locked), Color (violet line, neutral boundary marker), Typography, UI Considerations E1/E2/E5
    - @.planning/phases/42-risk-trend-analytics-burndown/42-RESEARCH.md — Architecture Patterns Pattern 4 (pivot-by-version segmented line + ReferenceLine — the ONLY source; no codebase precedent) and Pitfall 4
  </read_first>
  <action>
    Modify `frontend/src/components/shell/nav-items.ts`: append `LineChart` to the lucide-react import; add `{ label: 'Analytics', href: '/dashboard/analytics', icon: LineChart }` to WORKFLOW_ITEMS right after the Coverage entry (no chip).
    Modify `frontend/src/lib/queries/keys.ts`: add an `analytics` block per the PATTERNS.md template — `all: ['analytics']`, `overview: (opts: { scope: string; window: string; from?: string; to?: string }) => ['analytics','overview',opts]`. (scope/from/to are placeholders consumed fully in Plan 03; the hook passes scope='all' this plan.)
    Create `frontend/src/lib/queries/use-analytics.ts`: `useAnalytics(window: Window)` (window ∈ '7d'|'30d'|'90d'|'1y'), mapping window→days, `queryKey: queryKeys.analytics.overview({ scope: 'all', window })`, `queryFn: ({signal}) => api<AnalyticsOverviewResponse>(\`/api/v1/analytics/overview?days=${days}\`, {signal})`, `staleTime: 0`, `retry: 1`, `refetchOnWindowFocus: false`. Export the `AnalyticsOverviewResponse` TS type mirroring the backend schema (trend + boundaries).
    Create `frontend/src/components/analytics/microcopy.ts`: `const microcopy = {...} as const` transcribing UI-SPEC verbatim — page.h1 'Analytics'; window labels (7d/30d/90d/1y + sr-only expansions); trend.h2 'Risk-exposure trend'; trend.versionBoundaryLabel `(old,new) => \`${old} → ${new}\``; trend.versionBoundaryTooltip 'Model version changed here — scores before and after aren\'t directly comparable.'; empty.insufficientHistory {title:'Trends appear after a few days of history', body:(scope)=>`${scope} doesn't have enough snapshot history yet — check back in a few days.`}. (Aging/burndown/scope/caption sections are added in Plans 02/03.)
    Create `frontend/src/components/analytics/risk-trend-chart.tsx`: recharts `LineChart` inside an `aria-hidden` `ResponsiveContainer` wrapper. Implement RESEARCH Pattern 4 pivot-by-version: one `<Line>` per detected version with `dataKey=score_${v}`, `stroke="var(--color-violet)"`, `connectNulls={false}`, `isAnimationActive` gated by usePrefersReducedMotion; one `<ReferenceLine>` per boundary at the boundary date with `stroke="var(--color-border-strong)"`, `strokeDasharray="4 4"`, neutral mono label chip (NOT violet/severity/success). Render the canonical sr-only ChartDataTable with columns Date / Score / Model version (boundary readable in the table, not only visually). Exactly-one-point case: render a single dot (dot={{r:3}}), never a zero-length line.
    Create `frontend/src/components/analytics/scope-window-controls.tsx`: the window RangeToggle only (extend the trend-chart.tsx RangeToggle to the 4 presets 7d/30d/90d/1y; `role="group"`, `aria-pressed`), driven by `useUrlState<Window>('window', ['7d','30d','90d','1y'], '30d')`. (The scope dropdown and 'custom' range are added in Plan 03 — leave the allow-list at these 4 for now.)
    Create `frontend/src/components/analytics/analytics-page-skeleton.tsx`: `aria-busy="true" aria-live="polite"` wrapper composing a controls-row shimmer + an `h-[200px]` chart block (mirrors TrendChartSkeleton + CoverageStripSkeleton).
    Create `frontend/src/app/(authed)/dashboard/analytics/page.tsx`: `AnalyticsPage` = ErrorBoundary (boundaryName="AnalyticsPage", pageErrorFallback rendering PartialFailureBanner) > Suspense (fallback=<AnalyticsPageSkeleton/>) > `AnalyticsPageInner`. Inner: read window via useUrlState, call useAnalytics(window), render header (`<h1>` 32px/600 = microcopy.page.h1) + <ScopeWindowControls> then the single branch chain — error (PartialFailureBanner w/ onRetry) → loading (AnalyticsPageSkeleton) → below-min-history (EmptyState.Title/.Body, no actions) → populated (<RiskTrendChart trend boundaries/>). Gate the empty branch on trend.length < MIN_HISTORY_POINTS (a small const, e.g. 2), NEVER on a falsy score.
    Create `frontend/src/app/(authed)/dashboard/analytics/page.test.tsx`: vi.spyOn use-analytics; one test per branch — loading, error, empty (insufficient history), populated (line renders), single-data-point (renders a dot), version-boundary-marker present when boundaries non-empty.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run "src/app/(authed)/dashboard/analytics/page.test.tsx"` passes (all state-branch tests green).
    - `grep -rn "connectNulls" frontend/src/components/analytics/risk-trend-chart.tsx` shows `connectNulls={false}` (no interpolation across a null/boundary).
    - `grep -rn "ReferenceLine" frontend/src/components/analytics/risk-trend-chart.tsx` present; the boundary marker stroke is `var(--color-border-strong)` (grep) and is NOT `--color-violet` / any `--color-severity-*` / `--color-success`.
    - `grep -rniE "#[0-9a-f]{3,6}" frontend/src/components/analytics/` returns no freehand hex (all color via CSS vars).
    - `grep -n "/dashboard/analytics" frontend/src/components/shell/nav-items.ts` and `grep -n "LineChart" frontend/src/components/shell/nav-items.ts` both present.
    - The empty-branch condition greps to a `.length` / count check, not a score-value check.
    - `cd frontend && npx tsc --noEmit` clean for the new files.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/(authed)/dashboard/analytics/page.test.tsx"</automated>
  </verify>
  <done>/dashboard/analytics renders the tenant risk-exposure line (violet, segmented-capable, neutral boundary markers) with working loading/empty/error/single-point states; nav entry present; page.test.tsx green; no freehand hex; no new dependency.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Phase 42 tracer: a new Analytics sidebar entry opening /dashboard/analytics, which fetches GET /api/v1/analytics/overview and renders the tenant risk-exposure trend line for the selected window (7d/30d/90d/1y), with loading, empty (insufficient history), and error states, and neutral version-boundary markers.</what-built>
  <how-to-verify>
    1. Start the local dev stack (docker compose up) and log in as the seeded admin/viewer user.
    2. Confirm an "Analytics" entry with the line-chart icon appears in the sidebar; click it → URL is /dashboard/analytics.
    3. Confirm the risk-exposure trend line renders (violet). Switch window presets 7d/30d/90d/1y and confirm the series re-fetches and re-scopes to the window.
    4. Loading: throttle the network (or reload) — the skeleton shimmer shows in the chart footprint.
    5. Empty: point at a tenant/window with < 2 snapshot points — the guided "Trends appear after a few days of history" EmptyState renders (NOT a misleading 1-2-point line).
    6. Error: force a 500 (stop backend or block the request) — PartialFailureBanner with a working Retry shows.
    7. Boundary (optional today): real data is single-version ("v1") so expect zero markers and one continuous line — full multi-version rendering is verified in Plan 03 via a synthetic fixture.
  </how-to-verify>
  <resume-signal>Type "approved" to unblock Plans 02/03, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/v1/analytics/overview | Untrusted session + query params (`days`) cross into the new read endpoint |
| service query → Postgres | Tenant-scoped reads over `daily_snapshots` |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Information Disclosure | get_scoped_trend_series / get_analytics_overview | mitigate | Every query filters `DailySnapshot.tenant_id == tenant_id` inline in `.where(...)`; verified by `test_overview_is_tenant_scoped` (tenant_b never leaks) — acceptance criterion on Task 1. |
| T-42-02 | Elevation of Privilege | GET /overview | mitigate | `Depends(require_viewer)` on the route (D-14), mirroring GET /trends; no unauthenticated path. |
| T-42-03 | Denial of Service | `days` query param | mitigate | `days: int = Query(30, ge=7, le=365)` caps the window server-side (matches /trends). Custom unbounded ranges are not introduced until Plan 03, which adds its own span cap. |
| T-42-04 | Tampering | window param reflected into the page | accept | `useUrlState` clamps the window to a fixed allow-list before use; value never interpolated into a query string unescaped. Low risk, no PII. |
</threat_model>

<verification>
- Backend: `cd backend && ENCRYPTION_KEY=... JWT_SECRET_KEY=... uv run pytest tests/test_analytics.py -x` green.
- Frontend: `cd frontend && npx vitest run "src/app/(authed)/dashboard/analytics/page.test.tsx"` green; `npx tsc --noEmit` clean.
- Human tracer checkpoint approved (Task 3).
- No freehand hex, no new dependency, no touch to /trends or capture job (grep-confirmed).
</verification>

<success_criteria>
- A viewer+ user opens /dashboard/analytics and sees the tenant risk-exposure trend line over a selectable preset window (TREND-01, tenant scope), with loading/empty/error states.
- The series is flag-decoupled and date-range-bounded (D-12/D-13), never using the 90-row LIMIT.
- Version boundaries are detected server-side; the line never interpolates across one (TREND-03 rendering primitive), proven for the single-version case in real data and the multi-version case via a synthetic fixture test.
- The backend module is a plain-async service layer (D-16) reusable by Plans 02/03 and Phase 43.
</success_criteria>

<output>
After completion, create `.planning/phases/42-risk-trend-analytics-burndown/42-01-SUMMARY.md`
</output>
