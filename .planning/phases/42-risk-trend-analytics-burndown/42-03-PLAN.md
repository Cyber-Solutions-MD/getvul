---
phase: 42-risk-trend-analytics-burndown
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - backend/app/analytics/service.py
  - backend/app/analytics/router.py
  - backend/app/analytics/schemas.py
  - backend/tests/test_analytics.py
  - frontend/src/components/analytics/scope-window-controls.tsx
  - frontend/src/components/analytics/microcopy.ts
  - frontend/src/lib/queries/use-analytics.ts
  - frontend/src/app/(authed)/dashboard/analytics/page.tsx
  - frontend/src/app/(authed)/dashboard/analytics/page.test.tsx
autonomous: false
requirements: [TREND-01, TREND-03]

must_haves:
  truths:
    - "Group scope re-scopes EVERY chart (trend line, aging, burndown) via a single scope dropdown (D-02) — the asset_ids filter threads into the trend series, aging, and burndown"
    - "Group trend series is derived retroactively: each historical snapshot's metrics['asset_risk_exposure_scores'] dict is intersected with the group's CURRENT AssetGroupMember set, then AVERAGED per day (D-07 average aggregate, not sum) (D-05/D-06)"
    - "A day where the scoped group had zero scored assets yields avg_risk_exposure_score = None (a rendered gap), NEVER 0 (D-06 None-vs-zero discipline)"
    - "A cross-tenant group_id 404s via groups_service.list_members returning None (tenant filter in the WHERE clause), never a fetch-then-403 (IDOR guard)"
    - "The scope dropdown lists 'All (tenant)' + each AssetGroup; a mandatory group-scope caption renders whenever scope != All (tenant): 'Shows {group name}'s current members, applied retroactively across this window.'"
    - "The window control adds a 5th 'Custom range' preset revealing From/To native date inputs; an invalid range (To before From) is rejected client-side with 'End date must be after start date.' and fires no query"
    - "The custom range span is capped server-side (defensive max, e.g. ~1096 days) mirroring the /trends le=365 idiom; from/to are validated as ISO dates server-side, never interpolated unescaped"
    - "A synthetic multi-version DailySnapshot fixture ('v1'→'v2') proves the trend line segments at the boundary with a labeled neutral ReferenceLine and does NOT interpolate across it (TREND-03 SC3), verified against the fixture (never claimed against single-version production data)"
    - "A window spanning 3 distinct risk_model_version_snapshot values emits 2 boundaries/markers (every in-window boundary gets its own marker, not just the first/last — UI-SPEC E2 populated)"
    - "The scope dropdown supports many groups via a natively-scrollable list plus an inline search/filter input (UI-SPEC E1 overflow)"
    - "The scope trigger ellipsis-truncates a long AssetGroup.name (a truncate/text-ellipsis class on the trigger), never overflowing the control (UI-SPEC E1 long-text)"
    - {statement: "For a group whose current membership is empty, the trend renders the D-04 insufficient/empty affordance rather than an all-None misleading line, and aging/burndown render zero-scope correctly", verification: backstop}
  artifacts:
    - path: "backend/app/analytics/service.py"
      provides: "group-scope branch in get_scoped_trend_series (retroactive intersection); asset_ids threaded into aging+burndown"
      contains: "asset_risk_exposure_scores"
    - path: "backend/app/analytics/router.py"
      provides: "scope + group_id + from/to params, list_members 404 IDOR guard, custom-range span cap"
      contains: "list_members"
    - path: "frontend/src/components/analytics/scope-window-controls.tsx"
      provides: "scope dropdown (search-filterable) + 5th custom-range preset + From/To date fields + group caption"
      contains: "type=\"date\""
  key_links:
    - from: "backend/app/analytics/service.py"
      to: "app.assets.groups_service.list_members"
      via: "await list_members(db, tenant_id, group_id) → None means 404"
      pattern: "list_members"
    - from: "backend/app/analytics/service.py"
      to: "DailySnapshot.metrics['asset_risk_exposure_scores']"
      via: "intersect current member_ids against each snapshot's per-asset dict"
      pattern: "asset_risk_exposure_scores"
    - from: "frontend/src/components/analytics/scope-window-controls.tsx"
      to: "use-analytics"
      via: "scope + window + from/to feed the query key + request"
      pattern: "useAnalytics"
  prohibitions:
    - "Do NOT default a zero-scored-members group day to 0; it MUST be None (a gap)"
    - "Do NOT validate/authorize a group_id with a fetch-then-403; use list_members' None→404 (tenant filter in the WHERE clause)"
    - "Do NOT pass an open-ended allow-list into useUrlState for from/to; validate those as separate ISO-date params (Pitfall 3)"
    - "Do NOT leave the custom date range unbounded server-side; cap the span"
    - "Do NOT claim TREND-03 'verified against live version history' — real data is single-version 'v1'; verify only against the synthetic fixture"
    - "Do NOT build a point-in-time membership/history table; use CURRENT membership retroactively (D-06)"
    - "Do NOT add a new date-picker dependency; use native <input type=\"date\">"
    - "Do NOT color the version-boundary marker as accent/severity/success; neutral only"
    - "Do NOT emit freehand hex; route color through CSS variables"
---

<objective>
Horizontal expansion #2, completing the phase: add TREND-01's team/asset-group scoping (single scope dropdown re-scoping every chart via retroactive per-asset-dict intersection, D-02/D-05/D-06), the D-03 custom date-range window, and TREND-03's synthetic multi-version verification proving the boundary renders as a labeled neutral marker with no cross-boundary interpolation.

Purpose: Finish TREND-01 (group scope + full selectable window) and close TREND-03's success criterion against a synthetic fixture (the only way to prove it — real data has always been "v1").
Output: group-scope service branch + asset_ids threading + IDOR 404 + custom-range params/cap, the full scope-window controls with group caption, and the synthetic-boundary test.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-risk-trend-analytics-burndown/42-CONTEXT.md
@.planning/phases/42-risk-trend-analytics-burndown/42-RESEARCH.md
@.planning/phases/42-risk-trend-analytics-burndown/42-PATTERNS.md
@.planning/phases/42-risk-trend-analytics-burndown/42-UI-SPEC.md
@.planning/phases/42-risk-trend-analytics-burndown/42-01-SUMMARY.md
@.planning/phases/42-risk-trend-analytics-burndown/42-02-SUMMARY.md

<reversibility_ratings>
- Group-scope semantics = CURRENT membership retroactively (D-06): LOCKED DECISION, not re-opened here. Point-in-time membership is explicitly deferred. No checkpoint.
- Custom-range span cap value: REVERSIBLE — a defensive server-side constant. No checkpoint:decision.
- Scope param shape (`scope=all|group`, `group_id=<uuid>`): REVERSIBLE — read-side contract, Phase 43 reuses the service functions directly. No one-way door.
</reversibility_ratings>

<assumption_delta_decision>
- Primary noun (delta): scope (of a risk-exposure trend).
- Prior model: tenant-only trend (the existing dashboard /trends and Plan 01's tracer).
- Decision: ADD-ALONGSIDE — introduce group scope as a new selectable dimension alongside the existing tenant scope; the existing dashboard /trends and its tenant-only reads are left untouched (D-15). No promotion/renaming of the tenant model.
- Rationale: group series is derived on-read from data already banked in every snapshot (D-05); no schema change, no migration, and the tenant path stays authoritative. Advisory, non-blocking.
</assumption_delta_decision>
</context>

<artifacts_this_phase_produces>
- Backend: group-scope branch in `get_scoped_trend_series` (retroactive intersection, None-for-empty-day); `asset_ids` threaded through `get_aging_distribution` + `get_burndown_rate` + `get_analytics_overview`; `router.py` gains `scope` / `group_id` / `from` / `to` params, the `list_members` None→404 IDOR guard, and a custom-range span cap constant.
- Schemas: overview response gains a scope-echo / group-name field for the caption (as needed).
- Frontend: `scope-window-controls.tsx` gains the searchable scope dropdown, the 5th 'Custom range' preset with From/To native date inputs + client-side To>From validation, and the mandatory group-scope caption; `use-analytics.ts` carries scope + from/to; page renders the caption.
- Tests: retroactive-membership test, None-for-zero-scored-day test, cross-tenant group_id 404 test, and the synthetic multi-version boundary segmentation test.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Group scoping (retroactive intersection + asset_ids threading + IDOR 404) + custom-range params/cap + synthetic-boundary test</name>
  <read_first>
    - @backend/app/analytics/service.py (Plans 01/02 output — add the group branch + thread asset_ids)
    - @backend/app/analytics/router.py (Plan 01 output — add scope/group_id/from/to params)
    - @backend/app/assets/groups_service.py — list_members (123-136), tenant-scoped, None→404
    - @backend/app/coverage/router.py — _get_asset_or_404 IDOR shape (39-49): tenant filter in WHERE, None→404, never fetch-then-403
    - @backend/app/vulnerabilities/trends.py — DailySnapshot.metrics['asset_risk_exposure_scores'] shape ({str(asset_id): int}); the raw select shape from get_risk_score_trend (reuse the select, replace LIMIT with between())
    - @backend/tests/test_analytics.py (Plans 01/02 output — extend)
    - @backend/tests/test_risk_boundary_guard.py — DailySnapshot hand-construction with custom metrics (106-114, 200-220); this is the fixture pattern to extend by varying risk_model_version_snapshot
    - @.planning/phases/42-risk-trend-analytics-burndown/42-RESEARCH.md — Pattern 3 (retroactive intersection + None-vs-0), Code Examples §5 (synthetic multi-version fixture), Security Domain (V4 IDOR, V5 custom-range cap), Pitfalls 1, 5
  </read_first>
  <behavior>
    Author these tests FIRST (names match 42-VALIDATION.md test map):
    - test_group_trend_uses_current_membership_retroactively: seed 3 historical snapshots each with asset_risk_exposure_scores dicts; create a group whose CURRENT members are a subset; assert each past point is the average over exactly today's members' scores (retroactive, D-06).
    - test_group_trend_none_for_zero_scored_day: a snapshot day where none of the group's current members have a score → that point's avg_risk_exposure_score is None, not 0.
    - test_cross_tenant_group_id_404: a group_id belonging to tenant_b requested by tenant_a → 404 (list_members None path), no data returned.
    - test_asset_ids_threads_into_aging_and_burndown: group scope narrows aging + burndown to the group's assets (asset_ids filter), tenant-wide when scope=all.
    - test_version_boundary_detected_and_segmented (extend/confirm): synthetic 'v1'→'v2' fixture → the overview trend pivots so no segment spans the boundary and one boundary marker is emitted (verified against the fixture only).
    - test_multiple_version_boundaries_each_marked: synthetic fixture with 3 distinct risk_model_version_snapshot values ('v1'→'v2'→'v3') in one window → detect_version_boundaries returns exactly 2 boundaries and the pivoted series produces 3 non-interpolated segments (every in-window boundary marked, not just first/last — UI-SPEC E2 populated).
    - test_custom_range_span_capped: a from/to span beyond the cap → 422/clamped per the chosen contract; to<from → 422.
  </behavior>
  <action>
    In `backend/app/analytics/service.py`:
      - Extend `get_scoped_trend_series` with a group branch: accept `scope`, `group_id`, and resolved `member_ids: set[str] | None`. When group-scoped, for each snapshot row compute `scoped = [v for k,v in metrics.get("asset_risk_exposure_scores", {}).items() if k in member_ids]`; `avg = round(sum(scoped)/len(scoped), 1) if scoped else None` (None = gap, D-06); keep risk_model_version from the same row so boundary detection still works per scope. Tenant scope path unchanged.
      - Thread `asset_ids` through `get_aging_distribution` and `get_burndown_rate` (params already added in Plan 02) — pass the group's member asset_ids when group-scoped, None when tenant-wide.
      - Extend `get_analytics_overview` to accept `scope`, `group_id`, `start`, `end` (custom range) and orchestrate: resolve members via list_members when group-scoped, derive asset_ids, echo scope + group name for the caption.
    In `backend/app/analytics/router.py`: add `scope: str = Query("all")`, `group_id: uuid.UUID | None = Query(None)`, and custom-range params `start`/`end` (ISO dates) OR keep `days` for presets — support both (preset days OR explicit from/to). Validate: if group_id given, `members = await list_members(db, user.tenant_id, group_id); if members is None: raise HTTPException(404, "Asset group not found")`. Enforce a custom-range span cap constant `MAX_ANALYTICS_WINDOW_DAYS` (~1096) and `end >= start`, raising 422 otherwise (mirror the /trends le=365 defensive idiom).
    In `backend/app/analytics/schemas.py`: add scope-echo / group_name to AnalyticsOverviewResponse for the caption.
    Extend `backend/tests/test_analytics.py` with the <behavior> tests, extending the DailySnapshot fixture to vary risk_model_version_snapshot (the never-before-varied axis).
  </action>
  <acceptance_criteria>
    - `cd backend && ENCRYPTION_KEY=test-key-do-not-use-in-prod-000000000000 JWT_SECRET_KEY=test-jwt-secret uv run pytest tests/test_analytics.py -x` passes (incl. retroactive-membership, None-for-empty-day, IDOR-404, asset_ids-threading, synthetic-boundary, multi-boundary-3-versions→2-markers, span-cap tests).
    - `grep -c "list_members" backend/app/analytics/service.py backend/app/analytics/router.py` ≥ 1 and a `raise HTTPException(status_code=404" guards the None path (no fetch-then-403).
    - The group-average code path returns None (not 0) for an empty scoped list — `grep -n "else None" backend/app/analytics/service.py` present on the avg computation.
    - `grep -n "MAX_ANALYTICS_WINDOW_DAYS\|le=\|ge=" backend/app/analytics/router.py` shows a server-side span/param cap.
    - Every new query filters `tenant_id ==` inline.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=test-key-do-not-use-in-prod-000000000000 JWT_SECRET_KEY=test-jwt-secret uv run pytest tests/test_analytics.py -x</automated>
  </verify>
  <done>Group scope (retroactive intersection with None-for-empty-day), asset_ids threading through all three charts, cross-tenant 404 IDOR guard, custom-range params with a server-side cap, and synthetic-fixture boundary segmentation all pass.</done>
</task>

<task type="auto">
  <name>Task 2: Scope dropdown + custom-range fields + group caption + wiring</name>
  <read_first>
    - @frontend/src/components/analytics/scope-window-controls.tsx (Plan 01 output — extend from 4 presets to 5 + add scope dropdown + date fields + caption)
    - @frontend/src/components/ui/dropdown-menu.tsx — Radix wrapper primitives (9-15), already sunset-styled, reuse verbatim
    - @frontend/src/components/exceptions/exception-grant-dialog.tsx — native <input type="date"> FIELD_CLASS (61-62), field usage + role="alert" error text (313-335)
    - @frontend/src/hooks/use-url-state.ts (enum-clamp: use for the 5-preset window; do NOT use for from/to)
    - @frontend/src/lib/queries/use-asset-groups.ts (existing hook to list AssetGroups for the dropdown)
    - @frontend/src/components/analytics/microcopy.ts (Plan 01/02 output — add scope + caption sections)
    - @frontend/src/lib/queries/use-analytics.ts (Plan 01/02 output — add scope + from/to)
    - @frontend/src/app/(authed)/dashboard/analytics/page.tsx (render the caption; wire scope)
    - @frontend/src/app/(authed)/dashboard/analytics/page.test.tsx (extend)
    - @.planning/phases/42-risk-trend-analytics-burndown/42-UI-SPEC.md — Copywriting Contract (scope labels, custom-range From/To, group caption, custom-range validation text), UI Considerations E1 (overflow: search-in-dropdown; partial: To<From rejection)
  </read_first>
  <action>
    Extend `frontend/src/components/analytics/scope-window-controls.tsx`:
      - Scope dropdown: compose `DropdownMenu`/`Trigger`/`Content`/`Item` — trigger shows 'All (tenant)' or the selected `AssetGroup.name` (ellipsis-truncate), accessible label 'Scope'; items = 'All (tenant)' + groups from useAssetGroups; add an inline controlled `<input>` search filter at the top of the content when the group list is long (client-side filter). Selecting an item updates the scope + group_id URL params and re-scopes every chart.
      - Window: extend the RangeToggle allow-list to `['7d','30d','90d','1y','custom']` via useUrlState; selecting 'Custom range' reveals two native `<input type="date">` From/To fields styled with the exception-grant-dialog FIELD_CLASS; validate To>From client-side, showing `End date must be after start date.` in a `role="alert"` line and firing NO query until valid. from/to are plain URL params (NOT passed through useUrlState's allow-list).
      - Group-scope caption: render `Shows {group name}'s current members, applied retroactively across this window.` directly under the controls whenever scope != 'All (tenant)'.
    Extend `microcopy.ts` with `scope` (dropdown labels, accessible label) + the caption string + custom-range field labels/validation text (transcribe UI-SPEC verbatim).
    Extend `use-analytics.ts`: accept scope/group_id/from/to, include them in the query key and request (map preset→days OR pass from/to); keep staleTime:0.
    Wire the caption + scope into `page.tsx`.
    Extend `page.test.tsx`: scope dropdown lists All + groups; selecting a group shows the caption + re-scopes; custom range with To<From shows the error and fires no query; many-groups search filter narrows the list; a boundaries=[2 items] payload renders 2 ReferenceLine markers (not 1).
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run "src/app/(authed)/dashboard/analytics/page.test.tsx"` passes (scope-select, caption, custom-range validation, search-filter, multi-boundary-renders-2-markers tests).
    - `grep -nE "truncate|text-ellipsis" frontend/src/components/analytics/scope-window-controls.tsx` matches on the scope trigger (long AssetGroup.name ellipsis-truncates, UI-SPEC E1 long-text).
    - `grep -n "type=\"date\"" frontend/src/components/analytics/scope-window-controls.tsx` shows two native date inputs; `grep -rn "react-datepicker\|@mui\|day-picker" frontend/src/components/analytics/` == 0 (no new date dependency).
    - The from/to params are NOT passed through useUrlState's allow-list (grep: useUrlState is used only for the window preset enum).
    - The group caption string matches the UI-SPEC verbatim (grep for "applied retroactively across this window").
    - `grep -rniE "#[0-9a-f]{3,6}" frontend/src/components/analytics/scope-window-controls.tsx` returns no freehand hex.
    - `cd frontend && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/(authed)/dashboard/analytics/page.test.tsx"</automated>
  </verify>
  <done>The searchable scope dropdown re-scopes every chart, the custom date-range preset validates To>From, and the mandatory group-scope caption renders — all without a new dependency; page.test.tsx green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full TREND-01 scoping + TREND-03 boundary rendering on /dashboard/analytics: a searchable scope dropdown (All tenant + each asset group) re-scoping every chart to the group's current members retroactively (with the semantic caption), a Custom date-range window with To>From validation, and — via a synthetic multi-version snapshot fixture — a segmented trend line that annotates a risk-model version boundary as a neutral labeled marker without blending across it.</what-built>
  <how-to-verify>
    1. On /dashboard/analytics, open the scope dropdown → confirm 'All (tenant)' + each asset group; select a group → confirm the trend line, aging chart, and burndown tile all re-scope, and the caption "Shows {group}'s current members, applied retroactively across this window." appears.
    2. With many groups seeded, confirm the in-dropdown search filter narrows the list.
    3. Select 'Custom range' → set To before From → confirm "End date must be after start date." shows and no chart re-fetches; fix it → chart updates.
    4. Confirm a group day with no scored members renders as a gap in the line (not a drop to 0).
    5. TREND-03 (synthetic): seed a tenant with snapshots spanning ≥2 risk_model_version_snapshot values ("v1"→"v2") — see 42-RESEARCH.md Code Examples §5 for the fixture. Open the trend → confirm the line breaks at the boundary date (no diagonal interpolation across it) and a neutral dashed ReferenceLine labeled "v1 → v2" is drawn; hover shows "Model version changed here — scores before and after aren't directly comparable." Confirm this is validated against the synthetic fixture, NOT claimed against production data.
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 42, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/v1/analytics/overview | Session + `scope`/`group_id`/`from`/`to` params cross into the read endpoint |
| service → Postgres | Tenant-scoped reads over daily_snapshots + asset_group_members + vulnerabilities |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-08 | Information Disclosure / Elevation of Privilege | group_id scope param (IDOR) | mitigate | `groups_service.list_members(db, tenant_id, group_id)` returns None for a cross-tenant group (tenant filter in the WHERE clause) → router raises 404, never a fetch-then-403; enforced by `test_cross_tenant_group_id_404` — acceptance criterion on Task 1. |
| T-42-09 | Denial of Service | unbounded custom date range | mitigate | `MAX_ANALYTICS_WINDOW_DAYS` span cap + `end >= start` validated server-side (422 otherwise), mirroring /trends' le=365; `test_custom_range_span_capped` enforces it. |
| T-42-10 | Tampering | reflected from/to params | mitigate | from/to validated as ISO dates server-side and never interpolated unescaped; window preset stays allow-list-clamped via useUrlState (Pitfall 3). |
| T-42-11 | Information Disclosure | group series intersection query | mitigate | The per-asset dict is read from the tenant's own snapshots (already tenant-scoped); member_ids come from list_members (tenant-scoped); no cross-tenant asset score can enter the average. |
</threat_model>

<verification>
- Backend: `cd backend && ENCRYPTION_KEY=... JWT_SECRET_KEY=... uv run pytest tests/test_analytics.py -x` green (full file, all three plans' tests).
- Frontend: `cd frontend && npx vitest run "src/app/(authed)/dashboard/analytics/page.test.tsx"` green; `npx tsc --noEmit` clean.
- Full suites before /gsd-verify-work: `cd backend && uv run pytest tests/ -x` (per-file env vars) and `cd frontend && npm test`.
- Human checkpoint (Task 3) approved, including the synthetic multi-version boundary render.
- No new dependency, no fetch-then-403, no unbounded range, no None→0 collapse (grep/test-confirmed).
</verification>

<success_criteria>
- Tenant / team / asset-group risk-exposure trend lines render over a selectable window incl. custom range (TREND-01, ROADMAP SC1) via a single scope dropdown re-scoping every chart.
- Group series uses current membership retroactively with correct None-gap semantics (D-05/D-06).
- Trends annotate risk-model version boundaries and never blend across them — proven against a synthetic multi-version fixture, the only valid proof today (TREND-03, ROADMAP SC3).
- Cross-tenant group access 404s; custom ranges are span-capped.
</success_criteria>

<output>
After completion, create `.planning/phases/42-risk-trend-analytics-burndown/42-03-SUMMARY.md`
</output>
