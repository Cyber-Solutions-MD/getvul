---
phase: 42-risk-trend-analytics-burndown
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/app/analytics/service.py
  - backend/app/analytics/schemas.py
  - backend/app/vulnerabilities/trends.py
  - backend/tests/test_analytics.py
  - frontend/src/components/analytics/backlog-aging-chart.tsx
  - frontend/src/components/analytics/burndown-tile.tsx
  - frontend/src/components/analytics/microcopy.ts
  - frontend/src/lib/queries/use-analytics.ts
  - frontend/src/app/(authed)/dashboard/analytics/page.tsx
  - frontend/src/app/(authed)/dashboard/analytics/page.test.tsx
autonomous: false
requirements: [TREND-02]

must_haves:
  truths:
    - "get_aging_distribution returns current open findings bucketed into 3 SLA-tier-aligned buckets (within_sla / recently_breached / long_overdue) and stacked by severity, computed live (D-08)"
    - "Aging + burndown exclude status IN (SUPPRESSED, FALSE_POSITIVE) AND findings under an active exception, using active_exception_subquery verbatim (D-10)"
    - "Aging bucket boundaries come from sla_tier_service (critical/high/moderate tier windows + tenant sla_config overrides), never the legacy sla_service.py severity-keyed constants"
    - "get_burndown_rate returns net velocity (avg resolved − new per period) plus a projected days-to-zero, or an explicit growing branch when net-positive (D-09)"
    - "Every new numeric aggregate passes through round(float(x), N) if x is not None else None before entering a Pydantic response (Pitfall 6); the frontend needs no Number() coercion"
    - "The backlog aging chart reuses SEVERITY_FILLS from trend-chart.tsx verbatim; x-axis buckets are TEXT labels (Within SLA / Recently breached / Long overdue), never SLA-tier colors"
    - "The burndown tile shows shrinking (green), growing (red), and a distinct no-change (net-zero) copy branch; it receives already-computed values and never divides client-side"
    - "The '% of open backlog is overdue' headline tile is built and renders '0% of open backlog is overdue' explicitly at zero (UI-SPEC E3 zero-one-many)"
    - "The days-to-clear projection is capped (e.g. '500+ d to clear') rather than showing an absurd multi-thousand-day number (UI-SPEC E4 overflow)"
    - "Zero open findings in scope renders all 3 aging buckets at 0 (a valid 'nothing overdue' state), not an error"
    - {statement: "A finding whose age exactly equals its SLA tier-window boundary falls into exactly one bucket deterministically (no double-count, no drop) at the edge and one step either side", verification: backstop}
  artifacts:
    - path: "backend/app/analytics/service.py"
      provides: "get_aging_distribution + get_burndown_rate; get_analytics_overview extended with aging+burndown keys"
      contains: "active_exception_subquery"
    - path: "backend/app/analytics/schemas.py"
      provides: "AgingBucketResponse, BurndownResponse; AnalyticsOverviewResponse extended"
      contains: "class BurndownResponse"
    - path: "frontend/src/components/analytics/backlog-aging-chart.tsx"
      provides: "recharts stacked BarChart, SEVERITY_FILLS reused, 3 SLA-tier buckets"
      contains: "SEVERITY_FILLS"
    - path: "frontend/src/components/analytics/burndown-tile.tsx"
      provides: "net-velocity + days-to-clear tile, shrinking/growing/no-change branches, capped projection"
      min_lines: 30
  key_links:
    - from: "backend/app/analytics/service.py"
      to: "app.exceptions.service.active_exception_subquery"
      via: "~active_exception_subquery(tenant_id, now) in every open-backlog query"
      pattern: "active_exception_subquery"
    - from: "backend/app/analytics/service.py"
      to: "app.vulnerabilities.sla_tier_service"
      via: "tier_for_score / severity_to_tier / get_tier_policy for bucket boundaries"
      pattern: "sla_tier_service|get_tier_policy|tier_for_score"
    - from: "frontend/src/app/(authed)/dashboard/analytics/page.tsx"
      to: "backlog-aging-chart + burndown-tile"
      via: "rendered in the populated branch under the trend chart"
      pattern: "BacklogAgingChart|BurndownTile"
  prohibitions:
    - "Do NOT use app/vulnerabilities/sla_service.py::DEFAULT_SLA_DAYS or its 5-severity keying — use sla_tier_service.py (Pitfall 2)"
    - "Do NOT hand-roll a status NOT IN (...) exclusion; use active_exception_subquery verbatim (D-10)"
    - "Do NOT color the aging bars by SLA tier; bars are severity-colored, buckets are text labels only (no double-encoding)"
    - "Do NOT use RiskRing for the burndown tile — it is a rate, not a percentage"
    - "Do NOT divide by a denominator inside the burndown component; pass already-computed values as plain props"
    - "Do NOT return raw Decimal aggregates without round(float(x), N) casting"
    - "Do NOT emit freehand hex; route color through CSS variables (severity fills + directional success/danger)"
    - "Do NOT ship the aging/burndown sections without empty/loading/error states"
    - "Do NOT introduce a new charting dependency"
---

<objective>
Horizontal expansion #1 onto the verified tracer: add TREND-02's backlog aging distribution (open findings by SLA-tier bucket × severity) and burndown rate (net velocity + projected days-to-zero) to the same `/analytics/overview` payload and the same `/dashboard/analytics` page, honoring the Phase 39/40 exclusion predicate verbatim and the Phase 36 SLA-tier windows.

Purpose: Answer "how bad is what's left" (aging) and "how fast is it clearing" (burndown), reusing the tracer's module, page, and single-compute-pass loading/error model.
Output: `get_aging_distribution` + `get_burndown_rate` service functions, extended overview schema, `BacklogAgingChart` + `BurndownTile` components wired under the trend line.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-risk-trend-analytics-burndown/42-CONTEXT.md
@.planning/phases/42-risk-trend-analytics-burndown/42-RESEARCH.md
@.planning/phases/42-risk-trend-analytics-burndown/42-PATTERNS.md
@.planning/phases/42-risk-trend-analytics-burndown/42-UI-SPEC.md
@.planning/phases/42-risk-trend-analytics-burndown/42-01-SUMMARY.md
@.claude/skills/sketch-findings-getvul/references/visual-language.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md

<reversibility_ratings>
- Aging bucket boundary definition (recently-breached vs long-overdue split): REVERSIBLE — CONTEXT.md leaves exact thresholds to discretion; a service-side computation, no data/contract lock. No checkpoint:decision.
- Burndown period basis (per-week net) and days-to-clear cap value: REVERSIBLE — presentation choice in the tile + service. No one-way door. No checkpoint:decision.
</reversibility_ratings>
</context>

<artifacts_this_phase_produces>
- Backend functions: `get_aging_distribution(db, tenant_id, *, asset_ids=None, now)`, `get_burndown_rate(db, tenant_id, *, days, asset_ids=None)`; `get_vuln_trends` gains an additive `asset_ids: list[uuid.UUID] | None = None` param; `get_analytics_overview` extended to return `aging` + `burndown`.
- Schemas: `AgingBucketResponse`, `BurndownResponse`; `AnalyticsOverviewResponse` extended.
- Frontend components: `backlog-aging-chart.tsx`, `burndown-tile.tsx`; `microcopy.ts` gains `aging` + `burndown` sections; `use-analytics.ts` type extended; page renders both under the trend line.
(Group scoping via asset_ids, custom range, and synthetic-boundary verification are Plan 03.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aging distribution + burndown rate service functions + schemas + tests</name>
  <read_first>
    - @backend/app/analytics/service.py (Plan 01 output — extend get_analytics_overview additively)
    - @backend/app/analytics/schemas.py (Plan 01 output — add aging/burndown models)
    - @backend/app/vulnerabilities/trends.py — get_vuln_trends (38-126): open = status.in_(["OPEN","IN_PROGRESS"]), resolved = status.in_(["REMEDIATED","SUPPRESSED"]), first_detected_at / remediated_at; Decimal cast idiom (159, 374, 380)
    - @backend/app/vulnerabilities/models.py — first_detected_at, remediated_at, status, severity, sla_due_at, sla_breached (104-108)
    - @backend/app/vulnerabilities/sla_tier_service.py — DEFAULT_TIER_POLICY (49), tier_for_score (57-88), severity_to_tier, get_tier_policy (91-109), compute_sla_state (112-119)
    - @backend/app/exceptions/service.py — active_exception_subquery (50-71), the correlated EXISTS predicate to apply verbatim
    - @backend/app/coverage/service.py — small named boolean-clause builder idiom (54-61)
    - @backend/tests/test_analytics.py (Plan 01 output — extend)
    - @.planning/phases/42-risk-trend-analytics-burndown/42-RESEARCH.md — Pitfalls 2, 5, 6; Pattern 5; Open Question 2 (read stored sla_due_at/sla_breached columns, do not recompute per-row)
  </read_first>
  <behavior>
    Author these tests FIRST (names match 42-VALIDATION.md test map):
    - test_aging_honors_exclusion_predicate: seed OPEN findings + one SUPPRESSED + one FALSE_POSITIVE + one under an active ExceptionRecord; assert the excluded three are absent from all buckets.
    - test_aging_buckets_use_tier_policy: seed findings across tier windows (critical=7d, high=30d, moderate=90d) incl. a tenant sla_config override; assert bucketing follows the resolved tier policy, not sla_service.py's 5-severity constants.
    - test_aging_bucket_edge_deterministic: seed a finding whose age is exactly the tier boundary and one one-day-either-side; assert each lands in exactly one bucket (no double-count, no drop).
    - test_aging_zero_open_renders_three_zero_buckets: no open findings → 3 buckets present, all severity counts 0.
    - test_burndown_projection_branches: shrinking (net<0 → positive days-to-clear), growing (net>0 → growing branch, no clear date), no-change (net==0 → distinct branch); assert days-to-clear is capped at the max.
    - test_burndown_and_aging_numeric_types: assert every numeric field in the response is a JSON number (float/int), never a Decimal-string.
  </behavior>
  <action>
    In `backend/app/analytics/service.py`:
      - Add `async def get_aging_distribution(db, tenant_id, *, asset_ids=None, now)`: live `select` over Vulnerability where tenant_id == tenant_id, status.in_(["OPEN","IN_PROGRESS"]), `~active_exception_subquery(tenant_id, now)`, and (when asset_ids is not None) `Vulnerability.asset_id.in_(asset_ids)` (the param is wired now for Plan 03; default None = tenant-wide). For each finding resolve its tier via sla_tier_service (tier_for_score with severity_to_tier fallback + get_tier_policy override), then classify into `within_sla` / `recently_breached` / `long_overdue` using the finding's stored sla_due_at/sla_breached against its tier window (edge boundary inclusive on one side only — document which; make it deterministic). Aggregate counts per severity per bucket. Also compute `pct_overdue` = (recently_breached + long_overdue findings) / total open, `round(float(...),0)`-cast, None-safe when total==0 → but per UI-SPEC render 0, so return 0 not None here.
      - Add `async def get_burndown_rate(db, tenant_id, *, days, asset_ids=None)`: compose from `get_vuln_trends(db, tenant_id, days=days, asset_ids=asset_ids)` new-vs-resolved daily feed; compute net velocity per week = round(float(avg(resolved − new) * 7), 1); current open backlog count (tenant/scope, same exclusion predicate); projected days-to-clear = open / daily_net when net-shrinking, capped at MAX_PROJECTION_DAYS (e.g. 500 → response flag capped=true); status ∈ {shrinking, growing, no_change}.
      - Extend `get_analytics_overview` to also call these and return keys "aging" and "burndown" (additive; trend/boundaries unchanged).
    In `backend/app/vulnerabilities/trends.py`: add `asset_ids: list[uuid.UUID] | None = None` to `get_vuln_trends`'s signature (after days), and thread `Vulnerability.asset_id.in_(asset_ids)` into each WHERE clause only when asset_ids is not None — default preserves every existing call site byte-for-byte (mirror sla_tier_service.compute_sla_state's excepted_seconds=0 additive idiom).
    In `backend/app/analytics/schemas.py`: add `AgingBucketResponse` {bucket: str, critical: int, high: int, medium: int, low: int}; `BurndownResponse` {status: str, net_per_week: float, open_backlog: int, days_to_clear: int | None, capped: bool}; extend `AnalyticsOverviewResponse` with `aging: list[AgingBucketResponse]`, `aging_pct_overdue: int`, `burndown: BurndownResponse`.
    Extend `backend/tests/test_analytics.py` with the <behavior> tests.
  </action>
  <acceptance_criteria>
    - `cd backend && ENCRYPTION_KEY=test-key-do-not-use-in-prod-000000000000 JWT_SECRET_KEY=test-jwt-secret uv run pytest tests/test_analytics.py -x` passes.
    - `grep -c "active_exception_subquery" backend/app/analytics/service.py` ≥ 1 (applied to open-backlog queries).
    - `grep -c "sla_service" backend/app/analytics/service.py` == 0; a tier reference to `sla_tier_service` / `get_tier_policy` / `tier_for_score` is present (grep, comment-filtered).
    - Every `func.avg`/`func.sum`/division result entering the response passes through `round(float(...))`; `test_burndown_and_aging_numeric_types` green.
    - `get_vuln_trends`'s existing callers are unchanged: `grep -rn "get_vuln_trends(" backend/app` shows the additive param has a default (no call-site edits required).
    - Every new query filters `tenant_id ==` inline.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=test-key-do-not-use-in-prod-000000000000 JWT_SECRET_KEY=test-jwt-secret uv run pytest tests/test_analytics.py -x</automated>
  </verify>
  <done>Aging distribution + burndown rate compute live with the Phase 39 exclusion predicate and Phase 36 tier windows, all numeric fields JSON-number-safe, exposed via the extended overview payload; all backend tests green.</done>
</task>

<task type="auto">
  <name>Task 2: BacklogAgingChart + BurndownTile + page wiring + microcopy</name>
  <read_first>
    - @frontend/src/components/ui/trend-chart.tsx — stacked-bar stackId pattern (252-276), SEVERITY_FILLS export (46-51), SeverityTooltip (84-141), ResponsiveContainer/aria-hidden wrapper, prefers-reduced-motion gating
    - @frontend/src/components/campaigns/campaign-burndown-card.tsx — card chrome (69-73), directional-color copy row (90-99), zero-denominator-guard docstring (19-23), formatMttr null-safe helper (52-58)
    - @frontend/src/components/analytics/microcopy.ts (Plan 01 output — extend with aging/burndown sections)
    - @frontend/src/app/(authed)/dashboard/analytics/page.tsx (Plan 01 output — add sections under the trend chart)
    - @frontend/src/lib/queries/use-analytics.ts (Plan 01 output — extend the response type)
    - @frontend/src/app/(authed)/dashboard/analytics/page.test.tsx (Plan 01 output — extend)
    - @.planning/phases/42-risk-trend-analytics-burndown/42-UI-SPEC.md — Copywriting Contract (aging headings/bucket labels/overdue tile, burndown shrinking/growing/no-change/clear copy), Color (severity fills, directional success/danger, no SLA-tier color on aging), Typography (Display 40px for the tile headline number), UI Considerations E3/E4
  </read_first>
  <action>
    Extend `frontend/src/components/analytics/microcopy.ts` with `aging` (h2 'Backlog aging'; buckets {within_sla:'Within SLA', recently_breached:'Recently breached', long_overdue:'Long overdue'}; overdueTile:(n)=>`${n}% of open backlog is overdue`) and `burndown` (h2 'Burndown'; shrinking/growing/noChange copy, clearShrinking:(n)=>`${n}d to clear at this rate`, clearGrowing 'Backlog growing — no clear date at this rate', clearCapped '500+ d to clear').
    Create `frontend/src/components/analytics/backlog-aging-chart.tsx`: recharts stacked `BarChart` inside an `aria-hidden` ResponsiveContainer; XAxis dataKey = bucket id with a tickFormatter → the 3 locked labels; four `<Bar stackId="s">` low→medium→high→critical importing `SEVERITY_FILLS` from '@/components/ui/trend-chart' verbatim; adapt SeverityTooltip to show per-severity count per bucket; sr-only data table; a Display-size (40px/600) '% of open backlog is overdue' headline tile above/beside the chart rendering the value explicitly (0 shows '0% of open backlog is overdue'). Ignores the window preset (point-in-time, scope only).
    Create `frontend/src/components/analytics/burndown-tile.tsx`: reuse campaign-burndown-card chrome (`rounded-lg border border-border-subtle bg-surface-2 p-4`, aria-label="Burndown", data-testid="burndown-tile"); render a Display-size net-per-week headline number; a directional copy row — `text-success` for shrinking, `text-danger` for growing, muted for no-change; a projected-clear line using the capped/growing/shrinking copy. Receives all values as plain props (net_per_week, status, days_to_clear, capped); NEVER divides client-side; no RiskRing.
    Wire both into `frontend/src/app/(authed)/dashboard/analytics/page.tsx`'s populated branch, in order: RiskTrendChart → BacklogAgingChart → BurndownTile, each an `xl` (32px) gap apart. They share the single query's loading/error/empty branches (D-13 one compute pass) — no independent fetch.
    Extend `use-analytics.ts`'s `AnalyticsOverviewResponse` type with aging/aging_pct_overdue/burndown.
    Extend `page.test.tsx`: populated test asserts aging chart (3 buckets), overdue tile, and burndown tile render; add the zero-open aging case ('0% of open backlog is overdue') and the burndown no-change + capped branches.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run "src/app/(authed)/dashboard/analytics/page.test.tsx"` passes (incl. aging zero-case + burndown branch tests).
    - `grep -n "SEVERITY_FILLS" frontend/src/components/analytics/backlog-aging-chart.tsx` shows an import from '@/components/ui/trend-chart' (not a re-defined map).
    - `grep -rniE "#[0-9a-f]{3,6}" frontend/src/components/analytics/backlog-aging-chart.tsx frontend/src/components/analytics/burndown-tile.tsx` returns no freehand hex.
    - `grep -n "RiskRing" frontend/src/components/analytics/burndown-tile.tsx` == 0.
    - Burndown tile does no division: `grep -nE "/[^/*]" frontend/src/components/analytics/burndown-tile.tsx` shows no arithmetic division of props (values arrive precomputed).
    - `cd frontend && npx tsc --noEmit` clean for the new/changed files.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/(authed)/dashboard/analytics/page.test.tsx"</automated>
  </verify>
  <done>Backlog aging (3 SLA-tier buckets × severity, overdue headline tile) and the burndown tile render under the trend line, sharing the single compute pass, with correct directional colors and capped projections; page.test.tsx green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The aging distribution and burndown sections on /dashboard/analytics: a stacked SLA-tier aging bar chart (Within SLA / Recently breached / Long overdue, colored by severity), a '% of open backlog is overdue' headline tile, and a burndown tile showing net-per-week velocity + projected days-to-clear with shrinking/growing/no-change states.</what-built>
  <how-to-verify>
    1. On /dashboard/analytics, scroll below the trend line — confirm the Backlog aging chart shows 3 buckets with severity-colored stacks and hover tooltips giving exact per-severity counts.
    2. Confirm the "% of open backlog is overdue" tile renders a number; on a clean tenant it should read "0% of open backlog is overdue" (explicit, not blank).
    3. Confirm the Burndown tile shows a net-per-week figure with green (shrinking) / red (growing) directional copy and a "Nd to clear at this rate" (or "no clear date" / "500+ d to clear") line.
    4. Confirm suppressed / false-positive / actively-excepted findings are NOT counted in the aging buckets or backlog (cross-check against a known excepted finding if seeded).
    5. Confirm loading (skeleton) and error (PartialFailureBanner) still cover all three sections as one compute pass.
  </how-to-verify>
  <resume-signal>Type "approved" to unblock Plan 03, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/v1/analytics/overview | Session + `days` param cross into the extended read endpoint |
| service query → Postgres | Tenant-scoped live reads over `vulnerabilities` for aging + burndown |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05 | Information Disclosure | get_aging_distribution / get_burndown_rate | mitigate | Every new Vulnerability query filters `tenant_id == tenant_id` inline; the additive `asset_ids` param is None (tenant-wide) this plan; verified tenant-scoped in tests. |
| T-42-06 | Tampering / integrity of "open backlog" definition | exclusion predicate | mitigate | `~active_exception_subquery(tenant_id, now)` applied verbatim to every open-backlog query so excluded findings cannot inflate aging/burndown (D-10); `test_aging_honors_exclusion_predicate` enforces it. |
| T-42-07 | Denial of Service | live aging scan | accept | Query is tenant-scoped and bounded by the same `days` cap; `vulnerabilities` volume per tenant is modest. Low risk, no new unbounded surface (custom range arrives with its own cap in Plan 03). |
</threat_model>

<verification>
- Backend: `cd backend && ENCRYPTION_KEY=... JWT_SECRET_KEY=... uv run pytest tests/test_analytics.py -x` green.
- Frontend: `cd frontend && npx vitest run "src/app/(authed)/dashboard/analytics/page.test.tsx"` green; `npx tsc --noEmit` clean.
- Human checkpoint (Task 3) approved.
- No sla_service.py usage; no hand-rolled exclusion; no freehand hex; no new dependency (grep-confirmed).
</verification>

<success_criteria>
- Backlog aging (open findings by SLA-tier bucket × severity) and a burndown rate are visible on /dashboard/analytics (TREND-02, ROADMAP SC2).
- Both honor the Phase 39/40 exclusion predicate and the Phase 36 tier windows exactly.
- All numeric fields are JSON-number-safe (no Decimal-string quirk).
</success_criteria>

<output>
After completion, create `.planning/phases/42-risk-trend-analytics-burndown/42-02-SUMMARY.md`
</output>
