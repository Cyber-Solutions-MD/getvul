---
phase: 35-source-aware-filtering-provenance-badges
plan: 05
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - frontend/src/components/assets/assets-chip-bar.tsx
  - frontend/src/components/assets/assets-table.tsx
  - frontend/src/app/(authed)/dashboard/cspm/page.tsx
  - frontend/src/components/cspm/finding-card.tsx
  - frontend/src/components/tickets/tickets-chip-bar.tsx
  - frontend/src/components/tickets/tickets-table.tsx
autonomous: true
requirements: [SRC-01, SRC-02, SRC-03, SRC-04]
must_haves:
  truths:
    - "The Assets chip-bar splits the single stale source axis into TWO axes: a `scanner` axis (SCANNER_SOURCES, with the OR/AND source_mode toggle bound to ?source_mode) and a separate `enrichment_source` axis (JAMF/HUMAANS/INTUNE, plain OR facet, NO AND toggle) — reflecting the SRC-06 partition in the UI, and the stale TENABLE/AWS_INSPECTOR/MOCK values are gone"
    - "SourceBadgeGroup (from Plan 02) is rendered on Assets rows, CSPM finding cards, and Ticket rows — the SAME shared component, consuming each surface's sources/sources_count API fields; a single-source entity never reads as 'confirmed'"
    - "CSPM and Tickets expose a scanner-source filter chip axis; CSPM's includes the OR/AND source_mode toggle (true multi-tool corroboration from Plan 04); every source list is the 6 real VulnSource values, not a stale literal"
    - "Every new/modified filter and badge surface preserves the mandatory empty/loading/error states (state-patterns.md); the badge column renders a neutral empty state for entities with no sources, never a blank/crash"
    - "The OR/AND toggle on every surface is disabled (or no-ops) when fewer than 2 sources are selected (Pitfall 1), with copy-voice-compliant labels reused verbatim from Plan 02 (no 'AND'/'OR' jargon, no generic SaaS phrasing, no 'confirmed')"
  artifacts:
    - path: "frontend/src/components/assets/assets-chip-bar.tsx"
      provides: "Split scanner + enrichment_source axes + source_mode toggle + reconciled 6-value scanner list"
      contains: "enrichment_source"
    - path: "frontend/src/components/assets/assets-table.tsx"
      provides: "SourceBadgeGroup wired into asset rows (scanner sources)"
      contains: "SourceBadgeGroup"
    - path: "frontend/src/app/(authed)/dashboard/cspm/page.tsx"
      provides: "CSPM scanner-source chip axis + OR/AND source_mode toggle (?source_mode)"
      contains: "source_mode"
    - path: "frontend/src/components/cspm/finding-card.tsx"
      provides: "SourceBadgeGroup on CSPM finding cards (group sources)"
      contains: "SourceBadgeGroup"
    - path: "frontend/src/components/tickets/tickets-chip-bar.tsx"
      provides: "Ticket scanner-source chip axis (transitive provenance)"
      contains: "source"
    - path: "frontend/src/components/tickets/tickets-table.tsx"
      provides: "SourceBadgeGroup on ticket rows (transitive union provenance)"
      contains: "SourceBadgeGroup"
  key_links:
    - from: "frontend/src/components/assets/assets-chip-bar.tsx"
      to: "?scanner / ?enrichment_source / ?source_mode URL params"
      via: "two useUrlStateList axes + one useUrlState source_mode toggle"
      pattern: "enrichment_source"
    - from: "frontend/src/components/{assets/assets-table,cspm/finding-card,tickets/tickets-table}.tsx"
      to: "frontend/src/components/vulnerabilities/source-badge-group.tsx"
      via: "render <SourceBadgeGroup sources={...} count={...}/> from each surface's API fields"
      pattern: "SourceBadgeGroup"
---

<objective>
Replicate the proven Plan-02 provenance visual language across the remaining three surfaces. Split the
Assets chip-bar into partitioned scanner + enrichment axes (SRC-06 UI), add scanner-source chip axes + OR/AND
toggles to CSPM and Tickets, and render the shared SourceBadgeGroup on Assets/CSPM/Ticket rows consuming the
sources/sources_count fields Plans 03/04 added. This is the final plan of Phase 35 and of v4.0.

Purpose: The backend for all four entities is complete (Plans 01/03/04); Plan 02 built the shared component
and the OR/AND-toggle pattern on Vulnerabilities. This plan is pure reuse — no new component logic, just
wiring the existing SourceBadgeGroup + the chip-bar toggle pattern onto three more surfaces, honoring each
surface's API shape and the SRC-06 partition. Frontend-only; depends on 02 (component), 03 (assets API), 04
(cspm/ticket API).

Output: assets-chip-bar split + assets-table badges, CSPM page chips + finding-card badges, tickets-chip-bar
chips + tickets-table badges.

MANDATORY per CLAUDE.md: read the sketch-findings-getvul skill (visual-language, interaction-patterns,
state-patterns, copy-voice) BEFORE editing any surface. Every surface keeps empty/loading/error states.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-source-aware-filtering-provenance-badges/35-CONTEXT.md
@.planning/phases/35-source-aware-filtering-provenance-badges/35-PATTERNS.md
@.claude/skills/sketch-findings-getvul/references/interaction-patterns.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md
@frontend/src/components/assets/assets-chip-bar.tsx
@frontend/src/components/tickets/tickets-chip-bar.tsx

<interfaces>
<!-- Reuses Plan 02's SourceBadgeGroup + Plan 03/04's API fields. Confirm exact import path + prop shape
     from 35-02-SUMMARY.md and each row's sources/sources_count field names from 35-03/35-04 SUMMARYs. -->

SourceBadgeGroup (from Plan 02): import from '@/components/vulnerabilities/source-badge-group' (confirm the
path in 35-02-SUMMARY). Props: { sources: string[]; count?: number; className?: string }. Surface-agnostic —
on Assets it may receive enrichment codes (JAMF/…) which already render via the neutral fallback glyph Plan
02 built; pass the scanner subset for the corroboration emphasis and let unknown codes fall back neutrally.

API fields per surface (added by Plans 03/04 — confirm exact names in their SUMMARYs):
- Assets row: sources: string[], sources_count: number (scanner-source count).
- CSPM finding: sources: string[], sources_count: number (group sources over (rule_id,resource_id)).
- Ticket row: sources: string[], sources_count: number (transitive union).

Assets chip-bar split (assets-chip-bar.tsx:22 stale SOURCES + :69-75 single source axis → TWO axes):
```typescript
const SCANNER_SOURCES = ['CROWDSTRIKE','NESSUS','DEFENDER','WIZ','QUALYS','RAPID7'] as const;
const ENRICHMENT_SOURCES = ['JAMF','HUMAANS','INTUNE'] as const;
// axis 1: key:'scanner', allowList:SCANNER_SOURCES, derivedFromCounts:true + the OR/AND source_mode toggle
// axis 2: key:'enrichment_source', allowList:ENRICHMENT_SOURCES, plain OR facet, NO toggle
const [sourceMode, setSourceMode] = useUrlState('source_mode', ['or','and'] as const, 'or');
```
(The Assets list URL param for scanners is ?scanner= per Plan 03's router — align the axis key so
useUrlStateList writes ?scanner=, and ?enrichment_source= for the facet.)

CSPM + Tickets: add a `source` scanner axis (allowList = the 6 VulnSource values, derivedFromCounts from the
by_source facet each already exposes). CSPM gets the OR/AND source_mode toggle (Plan 04 binds ?source_mode at
the router). Tickets: Plan 04 adds a REAL OR-default `?source=` filter to the ticket list endpoint — render a
real server-filtering source chip axis (useUrlStateList writing ?source=), NO AND toggle (SRC-04 corroboration
is scoped to Vulnerabilities/Assets; ticket source filtering is OR-default through the linked vuln). The
SourceBadgeGroup on ticket rows still shows the transitive union provenance (display), distinct from the
filter chip.

Toggle: reuse Plan 02's disabled-below-2-sources rule + copy-voice label verbatim (read 35-02-SUMMARY for
the exact label so all four surfaces match).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assets — split scanner/enrichment chip axes + source_mode toggle + SourceBadgeGroup on rows</name>
  <read_first>
    - 35-02-SUMMARY.md (SourceBadgeGroup import path + prop contract + toggle label) + 35-03-SUMMARY.md (asset row sources field shape + whether source_mode 422s or clamps)
    - frontend/src/components/assets/assets-chip-bar.tsx (full — the stale SOURCES + single source axis to split) + assets-chip-bar.test.tsx
    - frontend/src/components/assets/assets-table.tsx (row/card structure — where to slot the badge, mirroring vuln-table's cluster placement) + assets-table.test.tsx
    - frontend/src/hooks/use-url-state.ts + use-url-state-list.ts
    - .claude/skills/sketch-findings-getvul/references/state-patterns.md (mandatory empty/loading/error)
  </read_first>
  <action>
    In assets-chip-bar.tsx: replace the stale SOURCES with SCANNER_SOURCES (6 real) + ENRICHMENT_SOURCES;
    split the single source axis into a `scanner` axis (writes ?scanner=, derivedFromCounts, + the OR/AND
    source_mode toggle reused from Plan 02) and an `enrichment_source` axis (writes ?enrichment_source=,
    plain OR facet, no toggle). In assets-table.tsx: render <SourceBadgeGroup sources={row.sources}
    count={row.sources_count}/> in the row/card badge cluster and add the sources fields to the row type.
    Preserve empty/loading/error states; neutral badge empty state when no sources. Update
    assets-chip-bar.test.tsx + assets-table.test.tsx for the split axes + badge presence.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/assets/ 2>&1 | tail -5 && grep -q "enrichment_source" src/components/assets/assets-chip-bar.tsx && grep -q "SourceBadgeGroup" src/components/assets/assets-table.tsx && ! grep -q "TENABLE\|AWS_INSPECTOR\|MOCK" src/components/assets/assets-chip-bar.tsx && echo ASSETS-OK</automated>
  </verify>
  <done>Assets chip-bar has partitioned scanner + enrichment_source axes + an OR/AND toggle; the stale fakes are gone; asset rows show SourceBadgeGroup; assets tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: CSPM — scanner-source chip axis + OR/AND source_mode toggle + SourceBadgeGroup on finding cards</name>
  <read_first>
    - 35-02-SUMMARY.md (component + toggle) + 35-04-SUMMARY.md (CSPM MisconfigSummary sources field shape + that ?source_mode is supported)
    - frontend/src/app/(authed)/dashboard/cspm/page.tsx (how CSPM filtering/ChipBar is wired today; the by_source facet consumption) + cspm/page.test.tsx
    - frontend/src/components/cspm/finding-card.tsx (where to slot the badge) + finding-card.test.tsx
    - frontend/src/components/vulnerabilities/chip-bar.tsx (Plan 02's finished source axis + toggle pattern to mirror)
  </read_first>
  <action>
    In cspm/page.tsx: add a scanner-source chip axis (allowList = the 6 VulnSource values, derivedFromCounts
    from the CSPM by_source facet) writing ?source=, plus the OR/AND source_mode toggle writing ?source_mode
    (Plan 04 backend). Mirror Plan 02's chip-bar toggle shape + disabled-below-2 rule + copy label. In
    finding-card.tsx: render <SourceBadgeGroup sources={finding.sources} count={finding.sources_count}/> in
    the card's badge area; add the sources fields to the finding type. Preserve empty/loading/error states.
    Update cspm/page.test.tsx + finding-card.test.tsx.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/cspm/ src/app/\(authed\)/dashboard/cspm/ 2>&1 | tail -5 && grep -q "source_mode" "src/app/(authed)/dashboard/cspm/page.tsx" && grep -q "SourceBadgeGroup" src/components/cspm/finding-card.tsx && echo CSPM-OK</automated>
  </verify>
  <done>CSPM has a scanner-source chip axis + OR/AND toggle (?source_mode); finding cards show SourceBadgeGroup (group sources); CSPM tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Tickets — scanner-source provenance chip axis + SourceBadgeGroup on ticket rows</name>
  <read_first>
    - 35-02-SUMMARY.md (component) + 35-04-SUMMARY.md (TicketSummary sources field shape + whether the ticket LIST endpoint accepts ?source= filtering or is display-only)
    - frontend/src/components/tickets/tickets-chip-bar.tsx (existing chip axes to extend) + its test
    - frontend/src/components/tickets/tickets-table.tsx (row structure; the existing provider-mark placement is the natural neighbor for the badge) + tickets-table.test.tsx
    - frontend/src/components/tickets/provider-mark.tsx (existing per-ticket provider mark — SourceBadgeGroup sits alongside, showing the underlying vuln provenance, distinct from the ticket-provider mark)
  </read_first>
  <action>
    In tickets-chip-bar.tsx: add a REAL scanner-source filter axis (6 VulnSource values, derivedFromCounts if
    a ticket source facet exists) wired via useUrlStateList writing ?source= (Plan 04 added the OR-default
    ticket source filter to the list endpoint). NO AND toggle for Tickets (SRC-04 is scoped to
    Vulnerabilities/Assets). Confirm the exact param name in 35-04-SUMMARY. In tickets-table.tsx: render
    <SourceBadgeGroup sources={row.sources} count={row.sources_count}/>
    on each ticket row (adjacent to, and visually distinct from, the ticket-provider mark — this badge is the
    underlying vuln's transitive provenance, not the Jira/Asana provider). Add the sources fields to the row
    type. Preserve empty/loading/error states. Update tickets-chip-bar + tickets-table tests.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/tickets/ 2>&1 | tail -5 && grep -q "SourceBadgeGroup" src/components/tickets/tickets-table.tsx && grep -Eq "source" src/components/tickets/tickets-chip-bar.tsx && echo TICKETS-OK</automated>
  </verify>
  <done>Ticket rows show SourceBadgeGroup with transitive union provenance (distinct from the ticket-provider mark); the tickets chip-bar exposes a scanner-source axis (filtering or display-only per Plan 04's backend); tickets tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server sources[] (3 surfaces) → SourceBadgeGroup | server source codes must never be concatenated into a CSS-var (handled by Plan 02's literal lookup) |
| ?scanner/?enrichment_source/?source/?source_mode URL params | untrusted values clamped by the URL-state hooks before reaching the API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 (this plan) | Tampering (XSS) | SourceBadgeGroup reuse across 3 surfaces | mitigate | Reuses Plan 02's literal-lookup mark (no concatenation, no hex, no <img>); Assets enrichment codes hit the neutral fallback, never a computed var. |
| T-35-05 (this plan) | Tampering | ?scanner/?enrichment_source/?source/?source_mode | mitigate | useUrlStateList/useUrlState clamp against the per-axis allow-list on read+write (T-12-05); source_mode clamped to ['or','and']. |
| T-35-11 | Repudiation/UX (overclaim) | single-source badge across Assets/CSPM/Tickets | mitigate | Same Plan-02 component — single-source branch has no color/check/confirmed; no per-surface override. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/assets/ src/components/cspm/ src/components/tickets/ "src/app/(authed)/dashboard/cspm/"` green.
- Stale-list gate: `! grep -rq "TENABLE\|AWS_INSPECTOR\|MOCK" frontend/src/components/{assets,vulnerabilities}/`.
- Reuse gate: `grep -rl "SourceBadgeGroup" frontend/src/components/{assets/assets-table,cspm/finding-card,tickets/tickets-table}.tsx` returns all three.
- Partition gate: `grep -q "enrichment_source" frontend/src/components/assets/assets-chip-bar.tsx`.
- Manual (phase UAT): the four surfaces render single vs multi source consistently, never "confirmed"; OR/AND toggles disable below 2; empty/loading/error states intact.
</verification>

<success_criteria>
- SRC-01 (all surfaces): SourceBadgeGroup on Assets, CSPM, Tickets rows — single vs multi distinct, never "confirmed".
- SRC-02/03/04 (all surfaces): scanner-source chip axes + OR/AND toggle where backend-supported; 6 real values.
- SRC-06 (UI): Assets scanner vs enrichment partition visible as two axes.
- v4.0 closes: provenance is a first-class, filterable/badged dimension across all four entities.
</success_criteria>

<output>
After completion, create `.planning/phases/35-source-aware-filtering-provenance-badges/35-05-SUMMARY.md`.
Record: confirmation the ticket source chip is a real server filter (?source=, per Plan 04's backend);
the SourceBadgeGroup placement chosen on each surface; confirmation all stale source fakes are gone
frontend-wide; and any design-token (gradient) gaps still outstanding.
</output>
