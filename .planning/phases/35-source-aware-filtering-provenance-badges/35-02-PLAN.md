---
phase: 35-source-aware-filtering-provenance-badges
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/components/vulnerabilities/source-badge-group.tsx
  - frontend/src/components/vulnerabilities/source-badge-group.test.tsx
  - frontend/src/components/vulnerabilities/vuln-table.tsx
  - frontend/src/components/vulnerabilities/chip-bar.tsx
autonomous: true
requirements: [SRC-01, SRC-02, SRC-03, SRC-04]
must_haves:
  truths:
    - "The shared SourceBadgeGroup renders a single-source finding as ONE neutral/muted provider mark with NO color, NO checkmark, and NO 'confirmed' copy — it is structurally impossible to imply confirmation from one scanner (SRC-01)"
    - "SourceBadgeGroup renders a multi-source-corroborated finding (2+ sources) as a group of provider marks plus a subtle 'N sources' count using the design-system corroboration tint (the --color-success SLA-ok green, reused per CONTEXT A3) — never the word 'confirmed'"
    - "Every provider mark's background is a `var(--gradient-provider-*)` CSS variable resolved via a literal lookup object (never string concatenation, never a raw hex, never an <img>) — mirrors ProviderMark's T-13-14 XSS mitigation"
    - "The Vulnerabilities chip-bar source axis lists EXACTLY the 6 real VulnSource values (CROWDSTRIKE, NESSUS, DEFENDER, WIZ, QUALYS, RAPID7) — the fake TENABLE/AWS_INSPECTOR/MOCK are gone and the previously-missing NESSUS/DEFENDER are present"
    - "The chip-bar exposes an OR/AND toggle bound to ?source_mode via useUrlState('source_mode', ['or','and'], 'or'); the toggle is disabled (or no-ops) when fewer than 2 sources are selected (Pitfall 1)"
    - "SourceBadgeGroup is rendered inline in the vuln-table row's Status/badge cluster (desktop) and the mobile Row-3 cluster, adjacent to the existing KEV/exploit badges, consuming the sources/sources_count fields Plan 01 added to the API"
  artifacts:
    - path: "frontend/src/components/vulnerabilities/source-badge-group.tsx"
      provides: "Shared, reused-by-all-surfaces SourceBadgeGroup: literal SOURCE_GRADIENTS + SOURCE_GLYPH maps, single-vs-multi non-overclaiming state machine, empty/zero-source defensive state"
      contains: "SOURCE_GRADIENTS"
    - path: "frontend/src/components/vulnerabilities/source-badge-group.test.tsx"
      provides: "Asserts no 'confirmed' copy on single-source; CSS-var-not-hex marks; no <img>; multi-source count + corroboration tint; zero-source no-crash"
      contains: "confirmed"
    - path: "frontend/src/components/vulnerabilities/vuln-table.tsx"
      provides: "SourceBadgeGroup wired into desktop Status cluster + mobile Row-3 cluster"
      contains: "SourceBadgeGroup"
    - path: "frontend/src/components/vulnerabilities/chip-bar.tsx"
      provides: "Reconciled 6-value SOURCES list + OR/AND source_mode toggle"
      contains: "source_mode"
  key_links:
    - from: "frontend/src/components/vulnerabilities/vuln-table.tsx"
      to: "frontend/src/components/vulnerabilities/source-badge-group.tsx"
      via: "renders <SourceBadgeGroup sources={row.sources} count={row.sources_count}/> in the badge cluster"
      pattern: "SourceBadgeGroup"
    - from: "frontend/src/components/vulnerabilities/chip-bar.tsx"
      to: "?source_mode URL param"
      via: "useUrlState('source_mode', ['or','and'] as const, 'or')"
      pattern: "source_mode"
---

<objective>
TRACER frontend half — prove the Vulnerabilities entity end-to-end in the UI. Build the SHARED
`SourceBadgeGroup` component (reused by Assets/CSPM/Tickets in Plan 05), wire it into the vuln table's
existing KEV/exploit badge cluster, and add the OR/AND source_mode toggle + the reconciled 6-value source
list to the vuln chip-bar. This is the first surface to consume the sources/sources_count fields Plan 01
added to the API.

Purpose: Establish the non-overclaiming provenance visual language and the OR/AND-toggle chip pattern ONCE,
on one surface, so Plan 05 replicates a proven component across the remaining three surfaces. Pure synthesis
of shipped precedents (35-PATTERNS §frontend): ProviderMark's literal-lookup gradient marks, the
visual-language.md tinted-pill chrome, the ChipBar aria-pressed chip-button shape, and useUrlState.

Output: source-badge-group.tsx (+test), vuln-table.tsx wiring, chip-bar.tsx OR/AND toggle + SOURCES fix.

MANDATORY per CLAUDE.md: read the sketch-findings-getvul skill (visual-language.md, interaction-patterns.md,
state-patterns.md, copy-voice.md) BEFORE writing any component. No freehand hex; no "confirmed"/generic copy.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-source-aware-filtering-provenance-badges/35-CONTEXT.md
@.planning/phases/35-source-aware-filtering-provenance-badges/35-PATTERNS.md
@.claude/skills/sketch-findings-getvul/references/visual-language.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md
@frontend/src/components/tickets/provider-mark.tsx
@frontend/src/components/tickets/provider-mark.test.tsx
@frontend/src/components/vulnerabilities/chip-bar.tsx

<interfaces>
<!-- The API shape Plan 01 landed; the component contract Plan 05 will reuse. -->

Plan 01 added to every vuln list row: `sources: string[]` and `sources_count: number` (single-source
fallback = [source], 1). SourceBadgeGroup consumes exactly these.

SourceBadgeGroup public contract (NEW component, reused across all 4 surfaces — keep it presentational and
surface-agnostic, no vuln-specific imports):
```typescript
type VulnSourceValue = 'CROWDSTRIKE' | 'NESSUS' | 'DEFENDER' | 'WIZ' | 'QUALYS' | 'RAPID7';
interface SourceBadgeGroupProps {
  sources: string[];        // may include enrichment values on the Assets surface — render unknown codes with a neutral fallback glyph
  count?: number;           // sources_count; falls back to sources.length
  className?: string;
}
```
Behavior (CONTEXT A3, SRC-01):
- 0 sources → neutral empty state (a muted em-dash or nothing), never a crash.
- 1 source → ONE mark, neutral/muted, NO color tint, NO check, NO "confirmed" text.
- 2+ sources → stacked marks + a subtle "N sources" label, corroboration tint = the SLA-ok green
  (`background: rgba(74,222,128,0.12); color: var(--color-success); border: 1px solid rgba(74,222,128,0.3)`
  — the visual-language.md `.sla-pill.ok` chrome, reused here per CONTEXT A3; this is a REVIEWABLE new
  mapping, follow the design system's spirit).

Per-source mark maps (literal lookup, NEVER concatenate into a var name — T-13-14):
```typescript
const SOURCE_GRADIENTS: Record<VulnSourceValue, string> = {
  CROWDSTRIKE: 'var(--gradient-provider-crowdstrike)', NESSUS: 'var(--gradient-provider-nessus)',
  DEFENDER: 'var(--gradient-provider-defender)', WIZ: 'var(--gradient-provider-wiz)',
  QUALYS: 'var(--gradient-provider-qualys)', RAPID7: 'var(--gradient-provider-rapid7)',
};
const SOURCE_GLYPH: Record<VulnSourceValue, string> = {
  CROWDSTRIKE: 'C', NESSUS: 'N', DEFENDER: 'D', WIZ: 'W', QUALYS: 'Q', RAPID7: 'R',
};
```
If the 6 `--gradient-provider-*` scanner CSS variables do not yet exist in the design tokens, add them to
the same tokens file that defines `--gradient-provider-jira` etc. (foundation.md / globals), following that
file's existing gradient convention — do NOT invent freehand hex; if the design system has no scanner
gradients, fall back to a single shared neutral scanner-mark treatment (a muted surface-2 square with the
glyph) and flag the token gap in the SUMMARY.

Reconciled SOURCES (chip-bar.tsx:26 — DROP fake TENABLE/AWS_INSPECTOR/MOCK, ADD real NESSUS/DEFENDER):
```typescript
const SOURCES = ['CROWDSTRIKE', 'NESSUS', 'DEFENDER', 'WIZ', 'QUALYS', 'RAPID7'] as const;
```

OR/AND toggle (chip-bar.tsx, sibling control next to the source axis — ChipAxis has no mode field):
```typescript
const [sourceMode, setSourceMode] = useUrlState('source_mode', ['or', 'and'] as const, 'or');
// render a single aria-pressed toggle chip (ChipBar.tsx:134-157 shape); disabled when <2 sources selected
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SourceBadgeGroup component + test (single-source non-overclaiming, multi-source corroboration, no hex/img)</name>
  <read_first>
    - frontend/src/components/tickets/provider-mark.tsx (full — literal SOURCE-map + role="img" + aria-label + size/rounded classes to mirror)
    - frontend/src/components/tickets/provider-mark.test.tsx (full — the `el.style.background).toContain('--gradient-provider-...')` + no-<img> assertion style to copy)
    - .claude/skills/sketch-findings-getvul/references/visual-language.md (the .sev-pill / .sla-pill.ok tinted-pill chrome)
    - .claude/skills/sketch-findings-getvul/references/copy-voice.md (tone — no "confirmed"/"verified"/generic copy)
  </read_first>
  <behavior>
    - renders_single_source_neutral: <SourceBadgeGroup sources={['QUALYS']} count={1}/> shows ONE mark, contains NO text matching /confirmed|verified/i, and applies no --color-success tint.
    - renders_multi_source_corroborated: sources=['QUALYS','RAPID7'], count=2 → 2 marks, a "2 sources" label, and the corroboration (--color-success) tint chrome.
    - marks_use_css_var_not_hex: every mark's style.background contains '--gradient-provider-' (or the neutral fallback class) and NO '#' hex; container has no <img> element.
    - unknown_source_neutral_fallback: an unrecognized code (e.g. 'JAMF' from the Assets surface) renders a neutral fallback mark, not a crash and not a wrong-provider gradient.
    - zero_sources_no_crash: sources=[] renders a neutral empty state (em-dash / nothing), no throw.
  </behavior>
  <action>
    Read the sketch-findings-getvul skill first. Create source-badge-group.tsx per the interfaces contract:
    literal SOURCE_GRADIENTS + SOURCE_GLYPH maps, a per-source mark subcomponent mirroring ProviderMark
    (size-3.5, rounded, role="img", aria-label), and the single(neutral)/multi(corroboration-tint) state
    machine with the "N sources" label. Guard unknown codes with a neutral fallback (lookup miss → muted
    square + first-letter glyph). Add the 6 `--gradient-provider-*` scanner tokens if missing (or use the
    neutral fallback + flag). Create source-badge-group.test.tsx with the behaviors above (copy
    provider-mark.test.tsx's assertion style). NEVER concatenate into a CSS-var name; NEVER a raw hex;
    NEVER the word "confirmed" in single-source copy.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/vulnerabilities/source-badge-group.test.tsx</automated>
  </verify>
  <done>SourceBadgeGroup + test exist and pass; single-source render provably contains no "confirmed"/"verified" and no color tint; marks reference CSS vars (no hex, no <img>); unknown/zero-source cases render neutrally without crashing.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SourceBadgeGroup into vuln-table + add OR/AND source_mode toggle and reconciled SOURCES to the vuln chip-bar</name>
  <read_first>
    - frontend/src/components/vulnerabilities/vuln-table.tsx:288-310 (desktop Status column KEV/exploit cluster) + :378-401 (mobile Row-3 cluster) — the exact slot for SourceBadgeGroup
    - frontend/src/components/vulnerabilities/chip-bar.tsx:26 (stale SOURCES) + :69-74 (sourceAxis) — the list to fix + the axis to keep
    - frontend/src/components/ui/ChipBar.tsx:134-157 (aria-pressed chip-button shape to mirror for the toggle)
    - frontend/src/hooks/use-url-state.ts (singular useUrlState signature/clamp) + use-url-state-list.ts (the ?source= list hook already used by the source axis)
    - .claude/skills/sketch-findings-getvul/references/interaction-patterns.md + state-patterns.md (chip-bar interaction + mandatory empty/loading/error states)
  </read_first>
  <action>
    In vuln-table.tsx: render <SourceBadgeGroup sources={row.sources} count={row.sources_count}/> inline in
    the desktop Status/badge cluster (adjacent to the KEV ★ / ⚡ badges, per visual-language.md's "inline
    alongside the severity pill or CVE") and in the mobile Row-3 cluster. Add sources/sources_count to the
    row TypeScript type consumed here. In chip-bar.tsx: replace the SOURCES constant with the 6-value
    VulnSource list (drop TENABLE/AWS_INSPECTOR/MOCK, add NESSUS/DEFENDER); keep the existing sourceAxis
    (derivedFromCounts stays — counts come from the backend by_source facet). Add a sibling OR/AND toggle
    chip next to the source axis: `useUrlState('source_mode', ['or','and'] as const, 'or')`, rendered with
    the ChipBar aria-pressed chip-button shape, DISABLED (or no-op) when fewer than 2 source values are
    selected (Pitfall 1) — with copy-voice-compliant label (e.g. "All selected" / "Any selected", NOT
    "AND"/"OR" jargon and NOT generic SaaS phrasing). Preserve the existing empty/loading/error states of
    the table; the badge column must render its own neutral empty state when a row has no sources.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/vulnerabilities/ 2>&1 | tail -5 && grep -q "source_mode" src/components/vulnerabilities/chip-bar.tsx && grep -q "SourceBadgeGroup" src/components/vulnerabilities/vuln-table.tsx && ! grep -q "TENABLE\|AWS_INSPECTOR\|MOCK" src/components/vulnerabilities/chip-bar.tsx && echo WIRE-OK</automated>
  </verify>
  <done>Vuln rows show SourceBadgeGroup in the badge cluster; the chip-bar SOURCES list is the 6 real VulnSource values (no fakes); an OR/AND toggle drives ?source_mode and is disabled below 2 selections; existing vuln component tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server sources[] → SourceBadgeGroup CSS-var lookup | server-provided source codes must never be concatenated into a CSS-var/URL |
| ?source_mode / ?source URL params → filter state | untrusted URL values clamped before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | Tampering (XSS) | SourceBadgeGroup gradient/glyph resolution | mitigate | Literal lookup objects only (SOURCE_GRADIENTS/SOURCE_GLYPH), never string concatenation into a `var(--...)` name — mirrors ProviderMark T-13-14; unknown codes hit a neutral fallback, never a computed var. Test asserts no hex + no <img>. |
| T-35-05 | Tampering | ?source_mode / ?source URL params | mitigate | useUrlState/useUrlStateList clamp against the allow-list on read AND write (existing T-12-05 convention); source_mode clamped to ['or','and']. |
| T-35-06 | Repudiation/UX (overclaim) | single-source badge implying "confirmed" | mitigate | Structural: single-source branch has no color/check/confirmed text; test renders_single_source_neutral asserts the absence (SRC-01). |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/vulnerabilities/` green (new + existing).
- `grep -q "SourceBadgeGroup" frontend/src/components/vulnerabilities/vuln-table.tsx` and no TENABLE/AWS_INSPECTOR/MOCK in chip-bar.tsx.
- Manual (deferred to phase UAT): visual check that single vs multi source never reads as "confirmed"; OR/AND toggle disables below 2 selections; badge column empty state renders.
</verification>

<success_criteria>
- SRC-01 (UI): SourceBadgeGroup on every vuln row, single vs multi visually distinct, never "confirmed".
- SRC-02/03/04 (vuln UI): 6-value source chips + OR/AND toggle bound to ?source_mode, disabled below 2.
- Shared component ready for Plan 05 reuse across Assets/CSPM/Tickets.
</success_criteria>

<output>
After completion, create `.planning/phases/35-source-aware-filtering-provenance-badges/35-02-SUMMARY.md`.
Record: whether the 6 `--gradient-provider-*` scanner tokens existed or were added (or the neutral-fallback
path was used); the SourceBadgeGroup import path + prop contract (for Plan 05); the toggle copy chosen
(copy-voice); and confirmation the stale SOURCES fakes are gone.
</output>
