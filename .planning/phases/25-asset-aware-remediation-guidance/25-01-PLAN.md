---
phase: 25-asset-aware-remediation-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/ai/safety.py
  - backend/app/ai/grounding.py
  - backend/tests/test_ai_safety.py
  - backend/tests/test_ai_grounding_remediation_guidance.py
autonomous: true
requirements: [AIR-01]
must_haves:
  truths:
    - "The dangerous-command denylist returns a matched-pattern label for destructive/security-disabling text and None for safe text (D-04/D-05)"
    - "The refuse predicate treats empty string, whitespace, generic placeholders, and sub-minimum-length text as ABSENT — not just None (D-01, RESEARCH Pattern 1 / Pitfall 1)"
    - "The remediation-guidance grounding query is tenant-scoped and returns None for a foreign-tenant or missing finding_id"
    - "Owner-PII fields are never selected by the grounding query (defense-in-depth, D-10 / Phase 24 D-15)"
  artifacts:
    - path: "backend/app/ai/safety.py"
      provides: "DANGEROUS_PATTERNS tuple + contains_dangerous_pattern(candidate) -> str | None"
      contains: "DANGEROUS_PATTERNS"
    - path: "backend/app/ai/grounding.py"
      provides: "get_remediation_guidance_context() + has_actionable_remediation_text()"
      contains: "def has_actionable_remediation_text"
    - path: "backend/tests/test_ai_safety.py"
      provides: "Positive/negative/obfuscated denylist coverage"
    - path: "backend/tests/test_ai_grounding_remediation_guidance.py"
      provides: "Refuse-predicate + tenant-scoped query + PII-exclusion coverage"
  key_links:
    - from: "backend/app/ai/safety.py::contains_dangerous_pattern"
      to: "backend/app/ai/explain.py::_contains_leak_marker haystack idiom"
      via: "reuses the exact summary+business_risk+citations haystack composition"
      pattern: "candidate\\.summary.*candidate\\.business_risk"
    - from: "backend/app/ai/grounding.py::get_remediation_guidance_context"
      to: "Vulnerability outerjoin Asset (tenant-scoped)"
      via: "narrow SELECT of the 12 allowlisted columns only"
      pattern: "Vulnerability\\.tenant_id == tenant_id"
---

<objective>
Build the two anti-fabrication primitives Phase 25's cite-or-refuse core rests on, with zero coupling to the streaming engine yet: (1) the maintained dangerous-command denylist module (D-04/D-05) and (2) the deterministic refuse predicate + narrow, PII-excluding remediation-guidance grounding query (D-01/D-10).

Purpose: These are the highest-risk, highest-fabrication-consequence functions in the phase. Isolating them in Wave 1 lets their positive+negative test suites be crisp before any route or engine wiring exists.
Output: `backend/app/ai/safety.py` (new), additions to `backend/app/ai/grounding.py`, and their two dedicated test files.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-asset-aware-remediation-guidance/25-CONTEXT.md
@.planning/phases/25-asset-aware-remediation-guidance/25-RESEARCH.md
@.planning/phases/25-asset-aware-remediation-guidance/25-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan contributes the following new symbols to the phase-wide artifact set:
- `backend/app/ai/safety.py` — `DANGEROUS_PATTERNS`, `contains_dangerous_pattern(candidate) -> str | None`
- `backend/app/ai/grounding.py` — `get_remediation_guidance_context(db, tenant_id, finding_id)`, `has_actionable_remediation_text(remediation_action, remediation_info)`, `_GENERIC_REMEDIATION_PLACEHOLDERS`, `MIN_REMEDIATION_CHARS`
(Later plans add: the schema variant + prompt-builder quadruplet [02], engine `dangerous_pattern_check` param + new route path [03], frontend section/hooks [04], `TicketCreateRequest.description` [06], copy-in wiring [07].)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dangerous-command denylist module (D-04/D-05)</name>
  <read_first>
    - `.planning/phases/25-asset-aware-remediation-guidance/25-RESEARCH.md` Pattern 3 (the fully-worked `DANGEROUS_PATTERNS` starter set + `contains_dangerous_pattern` implementation) and its "Testing" paragraph (positive/negative/obfuscated cases).
    - `.planning/phases/25-asset-aware-remediation-guidance/25-PATTERNS.md` §"backend/app/ai/safety.py" (the calling-convention analog) and §"No Analog Found" (the denylist CONTENT has no in-repo precedent — use RESEARCH Pattern 3's set, do not invent your own).
    - `backend/app/ai/explain.py` lines 166-182 — `_contains_leak_marker()`: the EXACT haystack-composition idiom to reuse verbatim (`" ".join([candidate.summary, candidate.business_risk, *(c.text for c in candidate.citations)])`).
    - `backend/app/ai/schemas.py` lines 44-67 — `ExplainResponseBase` (the candidate type to import for the parameter annotation).
    - `backend/tests/test_ai_prompt_builder.py` lines 63-90 — the `@pytest.mark.parametrize` positive/negative style to mirror.
  </read_first>
  <behavior>
    - Positive: one denylist hit per pattern category (rm -rf, drop table/database, truncate table, mkfs, dd to /dev/, chmod 777/a+rwx, curl|wget pipe-to-shell, disable firewall/EDR/selinux/apparmor + setenforce 0 + ufw disable). Returns the pattern LABEL string.
    - Obfuscation-resistant (cheap): mixed case, extra/collapsed whitespace, and a dangerous phrase split so it spans `summary` + `business_risk` + a `citation.text` all still match after lowercase+whitespace-normalize.
    - Negative (proves no over-block): "Remove the old log file with rm oldfile.log" (no -rf), "Update the firewall rule to allow port 443" (mentions firewall, does not disable), "Run chmod 644 to restrict permissions" (different mode) — all return None.
  </behavior>
  <action>
    Write `backend/tests/test_ai_safety.py` FIRST (RED), then create `backend/app/ai/safety.py`. Implement `DANGEROUS_PATTERNS: tuple[tuple[str, re.Pattern[str]], ...]` using RESEARCH Pattern 3's 8 categories verbatim (they cover every example D-04 names). Implement `contains_dangerous_pattern(candidate)` that composes the haystack using the identical idiom as `_contains_leak_marker` (summary + business_risk + every citation.text), then `re.sub(r"\s+", " ", haystack.lower())` before scanning; return the matched label on first hit, else None. This is a pure text scan (D-05 "obfuscation-resistant WHERE CHEAP") — do NOT build a shell-AST / base64-decode / command-substitution pipeline (RESEARCH §Don't Hand-Roll). The gate applies uniformly to scanner_verbatim and ai_interpreted text alike — no provenance carve-out (RESEARCH Assumptions A2 / D-04 "on any hit the ENTIRE guidance is refused").
  </action>
  <acceptance_criteria>
    - `backend/app/ai/safety.py` exists exporting `DANGEROUS_PATTERNS` and `contains_dangerous_pattern`.
    - Every category in `DANGEROUS_PATTERNS` has ≥1 passing positive test; the 3 named negatives return None; ≥1 split-across-fields obfuscation test passes.
    - `grep -n "import re" backend/app/ai/safety.py` present; no third-party pattern library imported.
    - Function signature `contains_dangerous_pattern(candidate: ExplainResponseBase) -> str | None`.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_safety.py -q</automated>
  </verify>
  <done>Denylist module and its positive/negative/obfuscated suite are green; the module imports `re` (stdlib) only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refuse predicate + tenant-scoped, PII-excluding grounding query (D-01/D-10)</name>
  <read_first>
    - `.planning/phases/25-asset-aware-remediation-guidance/25-RESEARCH.md` Pattern 1 (the `has_actionable_remediation_text` implementation + `_GENERIC_REMEDIATION_PLACEHOLDERS` + `MIN_REMEDIATION_CHARS`; the empty-string Rapid7 reality) and Pattern 2 (the 12-field `get_remediation_guidance_context` implementation + the explicit owner-PII exclusion list).
    - `.planning/phases/25-asset-aware-remediation-guidance/25-PATTERNS.md` §"backend/app/ai/grounding.py" (the `get_asset_posture()` analog at lines 46-116, the module-docstring convention, and the confirmed column names).
    - `backend/app/ai/grounding.py` lines 46-116 (`get_asset_posture` — narrow-SELECT + tenant-scoped-404 shape) and lines 119-194 (`get_remediation_group` — the WRONG scope, to contrast in the docstring).
    - `backend/app/vulnerabilities/models.py` lines 65-72 and `backend/app/assets/models.py` lines 29-30 — the exact column names to SELECT.
    - `backend/tests/test_ai_explain_host_remediation.py` lines 96-158 — the query-test analog (dict + attribute-object shapes).
  </read_first>
  <behavior>
    - `has_actionable_remediation_text(None, None)` → False; `("", "")` → False (Rapid7 empty-string, Pitfall 1); "Unknown"/"N/A"/"None"/"No remediation info"/"No remediation info available"/"-" → False; a real ≥15-char fix ("Upgrade OpenSSL to 3.0.14 or later.") → True; a 5-char string → False.
    - CrowdStrike's synthesized "Update {product} to the latest version" (≥15 chars, real product name) → True (RESEARCH Assumptions A1 — do NOT add to the placeholder denylist).
    - `get_remediation_guidance_context(db, tenant_a, finding_in_tenant_b)` → None; a valid finding → a 12-key dict whose keys contain none of: assigned_user, directory_user, managed_by, building, serial_number, department.
  </behavior>
  <action>
    Extend `backend/tests/test_ai_grounding_remediation_guidance.py` (new file) FIRST, then add to `backend/app/ai/grounding.py`: `_GENERIC_REMEDIATION_PLACEHOLDERS` (frozenset of casefolded placeholders per RESEARCH Pattern 1), `MIN_REMEDIATION_CHARS = 15`, and `has_actionable_remediation_text(remediation_action, remediation_info)` that `.strip()`s and `.casefold()`s each candidate and length-checks — NEVER `is not None` (D-01, Pitfall 1). Add `get_remediation_guidance_context(db, tenant_id, finding_id)` as a NEW narrow single-row `select(...)` of the 12 columns off `Vulnerability` `.outerjoin(Asset, Vulnerability.asset_id == Asset.id).where(Vulnerability.id == finding_id, Vulnerability.tenant_id == tenant_id)`, `one_or_none()` → None on miss, else the 12-key dict (map `Asset.hostname` → key `asset_hostname` per VULN_ALLOWLIST precedent). Extend the module docstring to state, per the PATTERNS analog, why this is neither `get_vulnerability()` (no OS columns) nor `get_remediation_group()` (cross-asset CVE aggregate, wrong scope for one finding). Owner-PII columns are never named in the SELECT — mirror `get_asset_posture()`'s "never even fetched" discipline (D-10 / Phase 24 D-15 defense-in-depth, threat T-25-01).
  </action>
  <acceptance_criteria>
    - `grep -n "def has_actionable_remediation_text\|def get_remediation_guidance_context" backend/app/ai/grounding.py` returns both.
    - No owner-PII column names appear in the new query: `grep -nE "assigned_user|directory_user|managed_by|building|serial_number|department" backend/app/ai/grounding.py` shows nothing inside the new function body (verify by test asserting returned dict keys too).
    - Test proves foreign-tenant finding_id → None and empty-string remediation_info → has_actionable False.
    - `has_actionable_remediation_text` uses `.strip()`/`.casefold()` and a length check, not `is not None` (grep for `is not None` must NOT be the gate).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_grounding_remediation_guidance.py -q</automated>
  </verify>
  <done>Refuse predicate handles the empty-string/generic/short cases; grounding query is tenant-scoped, returns None on miss, and structurally cannot return owner-PII fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB (Vulnerability/Asset rows) → grounding record | Scanner-derived + asset data crosses into AI-prompt scope; owner PII must not cross |
| Model output text → analyst | Untrusted generated prose could carry a destructive/security-disabling instruction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure | `get_remediation_guidance_context()` | mitigate | Query SELECTs only the 12 allowlisted columns; owner-PII columns (assigned_user/directory_user/managed_by/building/serial_number/department) are never named — defense-in-depth at the query layer, not just prompt-layer (Phase 24 D-15 precedent) |
| T-25-02 | Tampering / Elevation of Privilege | `contains_dangerous_pattern()` | mitigate | Maintained regex denylist over normalized (lowercase+whitespace-collapse) candidate text; returns a label for auditing. Consumed by the engine gate in Plan 03 BEFORE cache-write |
| T-25-03 | Repudiation / Information Disclosure | `has_actionable_remediation_text()` | mitigate | Deterministic pre-generation gate (layer 1 of D-01's two-layer cite-or-refuse) — no model call spent on ungroundable findings; empty/generic text treated as absent so fabricated "steps" are never generated from vacuous input |
</threat_model>

<verification>
- `cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_safety.py tests/test_ai_grounding_remediation_guidance.py -q` green.
- `grep -nE "assigned_user|directory_user|managed_by|building|serial_number|department" backend/app/ai/grounding.py` shows no owner-PII column inside `get_remediation_guidance_context`.
</verification>

<success_criteria>
Denylist module + refuse predicate + PII-excluding grounding query all exist and are proven by dedicated positive/negative test suites. No engine or route wiring yet (that is Plan 03) — these primitives stand alone.
</success_criteria>

<output>
After completion, create `.planning/phases/25-asset-aware-remediation-guidance/25-01-SUMMARY.md`.
</output>
