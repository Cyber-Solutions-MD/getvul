---
phase: 25-asset-aware-remediation-guidance
plan: 04
type: execute
wave: 4
depends_on: [25-03]
files_modified:
  - frontend/src/lib/ai/use-explain-stream.ts
  - frontend/src/lib/queries/use-explain-cache.ts
  - frontend/src/components/ai/ai-explanation-section.tsx
  - frontend/src/components/ai/ai-explanation-section.test.tsx
  - frontend/src/components/vulnerabilities/drill-content.tsx
  - frontend/src/components/vulnerabilities/drill-panel.test.tsx
autonomous: true
requirements: [AIR-01]
must_haves:
  truths:
    - "empty (remediation-guidance-result): no-key → role-gated card (D-23 inherited); cache-miss+Viewer → muted text; cache-miss+no-grounding → insufficient-evidence card with no button (state 3)"
    - "loading (remediation-guidance-result): 'Analyzing this finding…' pulsing-dot reused verbatim from Phase 24; prefers-reduced-motion → instant render, no replay"
    - "error (remediation-guidance-result): THREE distinct typed states — insufficient-evidence (neutral/violet), safety-refusal (danger/red, NEW), provider-busy/budget (amber, inherited) — never a generic error card, never conflated (Pitfall 3)"
    - "populated (remediation-guidance-result): validated cited steps render via the identical AiExplanationCitations renderer, scanner_verbatim tier first/verbatim (D-03)"
    - "overflow (remediation-guidance-result): inherits the drill panel's existing overflow-y-auto container — no independent scroll region"
    - "long-text (remediation-guidance-result): same max_tokens bound inherited from Phase 24's engine; wraps at 420px desktop / full-width mobile, no truncation UI"
    - "empty (remediation-guidance-trigger): the button renders ONLY in the one state where a call would succeed (state 2); it is replaced, not disabled-in-place, otherwise"
    - "zero-one-many (remediation-guidance-citations): inherits Phase 24's schema-required non-empty citations array — zero is structurally impossible when grounded=true"
    - "The new section sits between the raw Remediation section and Activity, mounting AiExplanationSection resourceType='remediation-guidance' with headingId='drill-remediation-guidance-h' (D-06 placement, mobile covered by the same DrillContent insertion)"
    - "The 'unsafe' error kind renders a danger/red card visually distinct from the neutral insufficient-evidence card (the ONE new color usage; token combo from ticket-provider-picker.tsx, no new hex)"
  backstop:
    statement: "A denylist hit on any portion of the generated steps refuses the ENTIRE guidance (D-04) — never a filtered/redacted partial render; the safety-refusal card is the only thing shown."
    verification: backstop
  artifacts:
    - path: "frontend/src/lib/ai/use-explain-stream.ts"
      provides: "'unsafe' member added to the error.kind union (both ExplainStreamState and ErrorEvent)"
      contains: "unsafe"
    - path: "frontend/src/lib/queries/use-explain-cache.ts"
      provides: "groundable?: boolean on the cached:false branch"
      contains: "groundable"
    - path: "frontend/src/components/ai/ai-explanation-section.tsx"
      provides: "danger DegradedCard variant + unsafe-kind branch + groundable===false immediate-refusal branch"
      contains: "groundable"
    - path: "frontend/src/components/vulnerabilities/drill-content.tsx"
      provides: "new <section aria-labelledby='drill-remediation-guidance-h'> mount"
      contains: "drill-remediation-guidance-h"
  key_links:
    - from: "ai-explanation-section.tsx groundable===false branch"
      to: "no trigger button shown"
      via: "checked before the isAnalystOrAbove trigger branch, === false explicitly"
      pattern: "groundable === false"
    - from: "drill-content.tsx new section"
      to: "AiExplanationSection resourceType='remediation-guidance'"
      via: "mounted between raw Remediation and Activity"
      pattern: "resourceType=\"remediation-guidance\""
---

<objective>
Build the frontend half of the tracer: the new "Remediation guidance" drill-panel section that renders cited steps (reusing Phase 24's AI section chrome + two-tier citation component UNCHANGED), plus the two NEW typed states 25-UI-SPEC defines — the neutral insufficient-evidence card (states 3/8) and the danger/red safety-refusal card (state 9) — and the `groundable` pre-signal that shows insufficient-evidence before any click.

Purpose: This closes the end-to-end per-vuln remediation-guidance slice (backend→frontend→cited render→refuse paths). The AIR-02 "copy into ticket description" affordance is intentionally NOT here — it is the expansion built in Plans 06/07 after the tracer gate.
Output: additive type members, the three shared-component branches, and the section mount, all test-covered.

MANDATORY UI DISCIPLINE: read the `sketch-findings-getvul` skill (`.claude/skills/sketch-findings-getvul/`) before touching any component — specifically `references/state-patterns.md` (refuse/insufficient-evidence/safety states), `references/copy-voice.md` (honest refusal copy), `references/foundation.md` (sunset CSS-variable tokens — NO freehand hex, locked Inter+JetBrains Mono), `references/visual-language.md` (citation tiers). Reuse Phase 24's AI section + citation component verbatim per 25-UI-SPEC — do NOT restyle. All copy strings are LOCKED in 25-UI-SPEC's Copywriting Contract — use them exactly.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-asset-aware-remediation-guidance/25-CONTEXT.md
@.planning/phases/25-asset-aware-remediation-guidance/25-RESEARCH.md
@.planning/phases/25-asset-aware-remediation-guidance/25-PATTERNS.md
@.planning/phases/25-asset-aware-remediation-guidance/25-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan contributes (frontend tracer): the `'unsafe'` error-kind union member; the `groundable?: boolean` cache-result field; the `danger` DegradedCard variant + the `unsafe`-kind branch + the `groundable === false` immediate-refusal branch on `AiExplanationSection`; the `drill-remediation-guidance-h` section mount in `drill-content.tsx`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive type members + the two new states + groundable pre-refusal branch</name>
  <read_first>
    - `.claude/skills/sketch-findings-getvul/references/state-patterns.md`, `copy-voice.md`, `foundation.md`, `visual-language.md` (MANDATORY before editing components).
    - `.planning/phases/25-asset-aware-remediation-guidance/25-PATTERNS.md` §"use-explain-stream.ts" (lines 421-434), §"use-explain-cache.ts" (438-444), §"ai-explanation-section.tsx" (448-500 — the danger variant, the unsafe-kind branch mirroring grounded_false, and the groundable===false branch placement BEFORE the isAnalystOrAbove trigger).
    - `.planning/phases/25-asset-aware-remediation-guidance/25-UI-SPEC.md` §Color (the FOUR-state table + the "ONE new red surface" rule), §Copywriting Contract (the LOCKED insufficient-evidence + safety-refusal headings/bodies), §Interaction Contract states 3/8/9.
    - `frontend/src/lib/ai/use-explain-stream.ts` lines 35-45 (both closed unions), `frontend/src/lib/queries/use-explain-cache.ts` (whole file), `frontend/src/components/ai/ai-explanation-section.tsx` lines 30-208 (DegradedCard + the state if/else chain), and `frontend/src/components/vulnerabilities/ticket-provider-picker.tsx` lines 74-79 (the `border-danger bg-danger-soft text-danger` token combo to reuse).
    - `.planning/phases/25-asset-aware-remediation-guidance/25-RESEARCH.md` Pattern 3 (frontend union) + Pattern 4 (groundable === false, not falsy) + Pitfall 3.
  </read_first>
  <behavior>
    - `state.kind === 'unsafe'` renders `variant="danger"` with heading "This guidance was withheld for safety" (LOCKED copy) and no action button — visually distinct from the neutral insufficient-evidence card (Pitfall 3).
    - `cacheQuery.data?.cached === false && cacheQuery.data?.groundable === false` renders the neutral insufficient-evidence card ("Not enough vendor guidance to recommend a fix", LOCKED copy) with NO trigger button, checked BEFORE the Analyst-trigger branch.
    - For `vuln`/`host`/`remediation`-posture mounts (GET never returns `groundable`), the trigger still shows — because the branch checks `=== false` explicitly, not falsy.
  </behavior>
  <action>
    In `use-explain-stream.ts`, add `'unsafe'` to BOTH the `ExplainStreamState` error branch union AND the standalone `ErrorEvent` type (they are hand-synced — touch both; the parsing loop needs zero change, it already forwards `evt.kind`). In `use-explain-cache.ts`, change the `cached: false` branch to `{ cached: false; groundable?: boolean }` (hook body unchanged — `groundable` rides the generic JSON). In `ai-explanation-section.tsx`: (1) extend `DegradedCardProps.variant` to `'neutral' | 'amber' | 'danger'` and add the `danger` chipClass/icon branch using `border-danger bg-danger-soft text-danger` (the exact tokens from `ticket-provider-picker.tsx` — 25-UI-SPEC's ONE new color usage; NO new hex); (2) add `else if (state.phase === 'error' && state.kind === 'unsafe')` immediately after the existing `grounded_false` branch, rendering `variant="danger"` with the LOCKED safety-refusal heading/body and no action; (3) add `else if (cacheQuery.data?.cached === false && cacheQuery.data?.groundable === false)` immediately BEFORE the `isAnalystOrAbove` trigger branch, rendering `variant="neutral"` with the LOCKED insufficient-evidence heading/body and no button. Extend `ai-explanation-section.test.tsx` with fixtures proving all three behaviors (including that the danger card and the neutral card are different — Pitfall 3, and that `groundable === undefined` still shows the trigger).
  </action>
  <acceptance_criteria>
    - `grep -c "'unsafe'" frontend/src/lib/ai/use-explain-stream.ts` ≥ 2 (both unions).
    - `grep -n "groundable" frontend/src/lib/queries/use-explain-cache.ts` present on the `cached: false` branch.
    - `grep -n "groundable === false" frontend/src/components/ai/ai-explanation-section.tsx` present, positioned before the trigger-button branch.
    - `grep -n "border-danger" frontend/src/components/ai/ai-explanation-section.tsx` present; no new hex literal introduced (grep for `#` hex in the new lines shows none).
    - Tests assert the danger card ≠ the neutral card, and that undefined `groundable` still renders the trigger.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run ai-explanation-section</automated>
  </verify>
  <done>The two new typed states and the groundable pre-refusal branch render with LOCKED copy and the single new danger token combo, provably distinct from the neutral refusal, with existing views unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the Remediation guidance section in the drill panel</name>
  <read_first>
    - `.claude/skills/sketch-findings-getvul/references/page-layouts.md` + `state-patterns.md` (MANDATORY).
    - `.planning/phases/25-asset-aware-remediation-guidance/25-PATTERNS.md` §"drill-content.tsx" Analog 1 (the existing `resourceType="vuln"` section mount at lines 267-273; the `headingId` prop is already first-class so a second same-page mount never collides on id).
    - `.planning/phases/25-asset-aware-remediation-guidance/25-UI-SPEC.md` §Interaction Contract — Placement (section sits AFTER the raw "Remediation" section and BEFORE "Activity"; the reading-order rationale reinforces cite-before-interpret).
    - `frontend/src/components/vulnerabilities/drill-content.tsx` around the `drill-remed-h` and `drill-activity-h` sections (the exact insertion point) and the existing `drill-ai-h` mount to copy.
    - `frontend/src/components/vulnerabilities/drill-panel.test.tsx` (the existing "Actions section" assertions to extend).
    - Backend contract: `resourceType="remediation-guidance"` (hyphenated) is keyed by finding UUID (`v.id`), per Plan 03.
  </read_first>
  <action>
    Add a new `<section aria-labelledby="drill-remediation-guidance-h">` in `drill-content.tsx` immediately AFTER the raw `drill-remed-h` section and BEFORE `drill-activity-h`, mounting `<AiExplanationSection resourceType="remediation-guidance" resourceId={v.id ?? idOrCve} headingId="drill-remediation-guidance-h" />`. Mobile is covered by the same insertion because `drill-panel-mobile.tsx` renders `DrillContent` directly for read-only sections (do NOT add a second mobile mount). Do NOT add the `onCopyToDescription` prop yet — that is AIR-02 (Plan 07). Extend `drill-panel.test.tsx` to assert the new section + heading renders with `resourceType="remediation-guidance"`, positioned between the raw Remediation and Activity sections.
  </action>
  <acceptance_criteria>
    - `grep -c "drill-remediation-guidance-h" frontend/src/components/vulnerabilities/drill-content.tsx` equals the number of intended references (the `aria-labelledby` + the `headingId` prop; no duplicate-id collision with the existing `drill-ai-h`).
    - The mount uses `resourceType="remediation-guidance"` and `resourceId={v.id ?? idOrCve}`.
    - `drill-panel.test.tsx` asserts the section renders between raw Remediation and Activity.
    - No `onCopyToDescription` prop is passed in this plan.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run drill-panel</automated>
  </verify>
  <done>The new Remediation guidance section renders in the drill panel (desktop + mobile via the single DrillContent insertion) in the D-06 placement, reusing Phase 24 chrome unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE error events → rendered card | The `unsafe` kind must map to a visually distinct danger surface, never a generic fallthrough |
| GET cache-check `groundable` → trigger visibility | A false groundable must suppress the paid-call trigger client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02 | Tampering | `ai-explanation-section.tsx` unsafe branch | mitigate | The `unsafe` kind renders the safety-refusal card only — never the citation renderer (the engine already withheld/never-cached the content per Plan 03); the frontend never receives or renders the dangerous payload |
| T-25-03 | Repudiation | `ai-explanation-section.tsx` groundable/grounded_false branches | mitigate | Insufficient-evidence renders an honest refusal card (neutral, "a feature not an error") — no fabricated fix is ever presented as vendor fact; visually never confusable with a system error |
| T-25-09 | Denial of Service (cost) | `groundable === false` branch | mitigate | The trigger button is suppressed for known-ungroundable findings, so an Analyst cannot spend a paid model call the backend would refuse anyway |
</threat_model>

<verification>
- `cd frontend && npx vitest run ai-explanation-section drill-panel` green.
- `cd frontend && npx vitest run` (full frontend suite) green before the tracer gate.
</verification>

<success_criteria>
The end-to-end per-vuln remediation-guidance slice is code-complete and test-proven front to back: an Analyst can request guidance and see scanner_verbatim-first cited steps; an ungroundable finding shows the insufficient-evidence card (no button, no call); a denylisted result shows the danger/red safety-refusal card; all reusing Phase 24 chrome unchanged. ROADMAP success criteria 1 and 2 are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/25-asset-aware-remediation-guidance/25-04-SUMMARY.md`.
</output>
