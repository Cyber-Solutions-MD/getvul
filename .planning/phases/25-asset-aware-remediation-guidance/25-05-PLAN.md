---
phase: 25-asset-aware-remediation-guidance
plan: 05
type: execute
wave: 5
depends_on: [25-04]
files_modified: []
autonomous: false
requirements: [AIR-01]
must_haves:
  truths:
    - "The end-to-end per-vuln remediation-guidance tracer (grounding → refuse predicate → prompt → engine safety gate → route → frontend section → cited render → refuse/safety cards) is verified before AIR-02 expansion begins"
  artifacts: []
  key_links: []
---

<objective>
TRACER GATE. Pause after the complete per-vuln remediation-guidance vertical slice (Plans 01–04) and before AIR-02 expansion (Plans 06–07), mirroring Phase 24's Plan 06 tracer gate. Confirm the cite-or-refuse core and the safety-refusal path behave correctly end-to-end before multiplying the surface into the ticket-create flow.

Purpose: Tracer-first discipline (TRACER_MODE) — one production-quality slice proven before expansion. AIR-01's anti-fabrication guarantees are the highest-consequence contract in the milestone; this gate is the moment to catch any drift before it propagates.
Output: a go/no-go signal for AIR-02.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/25-asset-aware-remediation-guidance/25-UI-SPEC.md
@.planning/phases/25-asset-aware-remediation-guidance/25-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full backend + frontend per-vuln remediation-guidance tracer, all automated-test-proven:
    - Backend: `contains_dangerous_pattern` denylist, `has_actionable_remediation_text` refuse predicate + tenant-scoped/PII-excluding `get_remediation_guidance_context`, the `ExplainRemediationGuidanceResponse` schema + prompt-builder quadruplet, the `dangerous_pattern_check` engine gate (refuses before `set_cached`), and the `POST/GET /api/v1/ai/explain-remediation-guidance/{finding_id}` route (D-01 route gate + `groundable` GET field, RBAC inherited).
    - Frontend: the new "Remediation guidance" drill-panel section (Phase 24 chrome reused), the neutral insufficient-evidence card, the danger/red safety-refusal card, and the `groundable` pre-refusal (no button when known-ungroundable).
    Automated coverage already proves the gate logic, schema, denylist, RBAC, cache-check, cross-tenant 404, and the unsafe-not-cached backstop in isolation (per 25-VALIDATION.md). The live visual render is the manual-only item below.
  </what-built>
  <how-to-verify>
    The live check requires the Docker stack + a dev Anthropic key (`GETVUL_DEV_ANTHROPIC_KEY`), which was unprovisioned throughout Phase 24 — so this is the SAME class of live item the user explicitly waived at the 24-06 gate. If a key + stack are available now:
    1. Configure a tenant Anthropic key; open a finding whose raw "Remediation" section shows real vendor solution text.
    2. In the new "Remediation guidance" section click "Get remediation guidance" → expect: "Analyzing this finding…" then cited steps with the `scanner_verbatim` vendor text tinted and rendered FIRST, any AI interpretation marked `ai_interpreted` after it.
    3. Open a finding with blank/empty remediation text → expect the neutral "Not enough vendor guidance to recommend a fix" card with NO button (rendered before any click).
    4. (Safety-refusal — danger/red card) is exercised by the automated held-out backstop test rather than live, since it requires a crafted model response; confirm that test is green.
    If no key/stack is available, review the green automated suites (`tests/test_ai_safety.py`, `tests/test_ai_grounding_remediation_guidance.py`, `tests/test_ai_prompt_builder_remediation_guidance.py`, `tests/test_ai_explain_stream.py`, `tests/test_ai_explain_remediation_guidance.py`, and the frontend `ai-explanation-section`/`drill-panel` suites) and proceed on trust as at 24-06.
  </how-to-verify>
  <resume-signal>Type "approved" (or "skip live verify, proceed on trust", mirroring 24-06) to unblock Plans 06–07, or describe issues to feed a revision.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
No new symbols — this is a verification gate. Output is human sign-off (or proceed-on-trust) plus a recorded decision consumed by the AIR-02 expansion plans (25-06/25-07).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced by this checkpoint — it is a verification gate over the boundaries already established and mitigated in Plans 01–04 (T-25-01 PII exclusion, T-25-02 dangerous-content-never-cached, T-25-03 cite-or-refuse, T-25-04 RBAC). The gate's purpose is to confirm those mitigations hold end-to-end before the AIR-02 surface multiplies.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Repudiation | tracer slice (whole) | mitigate | Human/automated confirmation that cite-or-refuse and safety-refusal behave as specified before expansion — the checkpoint IS the mitigation-verification step |
</threat_model>

<verification>
Human sign-off (or proceed-on-trust) recorded. All Plan 01–04 automated suites green.
</verification>

<success_criteria>
The tracer slice is confirmed (live or on-trust). AIR-02 expansion is unblocked.
</success_criteria>

<output>
After resume, create `.planning/phases/25-asset-aware-remediation-guidance/25-05-SUMMARY.md` recording the decision (approved / proceed-on-trust / issues).
</output>
