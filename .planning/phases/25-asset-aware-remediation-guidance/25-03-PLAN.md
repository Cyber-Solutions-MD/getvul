---
phase: 25-asset-aware-remediation-guidance
plan: 03
type: execute
wave: 3
depends_on: [25-01, 25-02]
files_modified:
  - backend/app/ai/explain.py
  - backend/app/api/v1/ai/explain_remediation_guidance.py
  - backend/app/api/v1/ai/__init__.py
  - backend/tests/test_ai_explain_stream.py
  - backend/tests/test_ai_explain_remediation_guidance.py
autonomous: true
requirements: [AIR-01]
must_haves:
  truths:
    - "A dangerous-pattern hit is refused at the ENGINE level BEFORE set_cached() — dangerous content is never written to the cache and never retrievable via the GET cache-check (D-04, RESEARCH Pattern 3 / Pitfall 2, threat T-25-02)"
    - "The new dangerous_pattern_check param defaults to None and is a proven no-op for the vuln/host/remediation-posture views (backward-compatible, mirrors allowed_source_fields)"
    - "POST /explain-remediation-guidance/{finding_id} is require_analyst; GET is require_viewer; a foreign-tenant finding_id returns 404 (D-17, threat T-25-04)"
    - "The D-01 deterministic gate refuses in the route BEFORE any model call — an ungroundable finding yields a single grounded_false SSE frame and spends no tokens"
    - "The GET cache-check returns groundable on a miss so the insufficient-evidence card can render before any click (UI-SPEC state 3)"
  artifacts:
    - path: "backend/app/ai/explain.py"
      provides: "dangerous_pattern_check optional param on _run_explain_stream() + unsafe_denylisted audit status + unsafe SSE kind"
      contains: "dangerous_pattern_check"
    - path: "backend/app/api/v1/ai/explain_remediation_guidance.py"
      provides: "POST/GET /explain-remediation-guidance/{finding_id} route with the D-01 route gate and groundable GET field"
      contains: "explain-remediation-guidance"
    - path: "backend/app/api/v1/ai/__init__.py"
      provides: "router registration for the new route"
      contains: "explain_remediation_guidance"
  key_links:
    - from: "explain_remediation_guidance.py POST"
      to: "_run_explain_stream(..., dangerous_pattern_check=contains_dangerous_pattern)"
      via: "passes the Plan 01 denylist as the engine gate"
      pattern: "dangerous_pattern_check=contains_dangerous_pattern"
    - from: "explain_remediation_guidance.py POST"
      to: "has_actionable_remediation_text() before dispatch"
      via: "D-01 pre-generation route gate"
      pattern: "has_actionable_remediation_text"
    - from: "_run_explain_stream dangerous_pattern_check"
      to: "audit status=unsafe_denylisted + skip set_cached"
      via: "terminal refusal before the SUCCESS block"
      pattern: "unsafe_denylisted"
---

<objective>
Complete the backend tracer: extend `_run_explain_stream()` with exactly one additive optional parameter (`dangerous_pattern_check`) that runs AFTER the leak-marker check and BEFORE `set_cached()` (the caching-order constraint that makes a route-only filter unsafe — D-04/Pitfall 2), then add the thin `explain-remediation-guidance` route that wires the Plan 01 grounding + denylist and the Plan 02 prompt/schema together, including the D-01 route-level pre-generation gate and the `groundable` GET field.

Purpose: This is the one place the phase touches shared Phase 24 engine code — provably backward-compatible the same way `allowed_source_fields=None` was. After this plan the full backend cite-or-refuse + safety-refusal + asset-aware-grounding path exists and is test-proven.
Output: engine param + new route file + registration + the unsafe-not-cached backstop test + the route RBAC/cache/404/groundable/ungroundable test suite.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-asset-aware-remediation-guidance/25-CONTEXT.md
@.planning/phases/25-asset-aware-remediation-guidance/25-RESEARCH.md
@.planning/phases/25-asset-aware-remediation-guidance/25-PATTERNS.md
@.planning/phases/25-asset-aware-remediation-guidance/25-UI-SPEC.md

<interfaces>
From Plan 01 (backend/app/ai/safety.py):
  contains_dangerous_pattern(candidate: ExplainResponseBase) -> str | None
From Plan 01 (backend/app/ai/grounding.py):
  get_remediation_guidance_context(db, tenant_id, finding_id) -> dict[str, Any] | None
  has_actionable_remediation_text(remediation_action, remediation_info) -> bool
From Plan 02 (backend/app/ai/prompt_builder.py):
  REMEDIATION_GUIDANCE_ALLOWLIST: frozenset[str]
  build_explain_remediation_guidance_prompt(record) -> tuple[str, list[dict[str, str]]]
  remediation_guidance_prompt_version() -> str
From Plan 02 (backend/app/ai/schemas.py):
  class ExplainRemediationGuidanceResponse(ExplainResponseBase)
Existing engine (backend/app/ai/explain.py, reused): _run_explain_stream, _sse_event, _audit, get_model_and_budget
Existing (backend/app/ai/audit.py): audit_log_ai_call(...)
Existing (backend/app/ai/cache.py): build_cache_key, record_hash, get_cached
</interfaces>
</context>

<artifacts_this_phase_produces>
This plan contributes: `_run_explain_stream(dangerous_pattern_check=...)` param, `"unsafe_denylisted"` audit status, `"unsafe"` SSE error kind (backend side); route file `explain_remediation_guidance.py` with path `POST/GET /api/v1/ai/explain-remediation-guidance/{finding_id}`, its `_allowlisted_hash_fields` mirror, the D-01 `_refuse()` branch (`"ungroundable"` audit status), and the `groundable` GET field; router registration.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Engine dangerous_pattern_check param (before set_cached) + unsafe-not-cached backstop</name>
  <read_first>
    - `.planning/phases/25-asset-aware-remediation-guidance/25-PATTERNS.md` §"backend/app/ai/explain.py" (the `allowed_source_fields` extension-point analog; the EXACT insertion point between the leak-marker `return` and the SUCCESS block's `set_cached()`).
    - `.planning/phases/25-asset-aware-remediation-guidance/25-RESEARCH.md` Pattern 3 (the engine-extension argument + the `unsafe` SSE kind + `unsafe_denylisted` audit status) and Pitfall 2.
    - `backend/app/ai/explain.py` lines 258-271 (signature — where the new param sits alongside `allowed_source_fields`), lines 417-423 (the `_contains_leak_marker` terminal branch), line 439 (`set_cached` — the gate must run BEFORE this), and the `_audit`/`_sse_event` helpers.
    - `backend/tests/test_ai_explain_stream.py` (the existing engine test file to extend; find the leak-marker/injection_flagged test as the structural analog for a terminal-refusal test).
  </read_first>
  <behavior>
    - Existing vuln/host/remediation-posture tests remain green with `dangerous_pattern_check=None` (default no-op — no behavior change).
    - When `dangerous_pattern_check` returns a label for a validated candidate: `_run_explain_stream` audits `status="unsafe_denylisted"`, yields exactly one `{type:"error", kind:"unsafe", matched_pattern:<label>}` SSE event, and RETURNS — `set_cached()` is NEVER called and no `summary_delta`/`done` frame is emitted.
    - The check runs AFTER the leak-marker check and BEFORE the SUCCESS block.
  </behavior>
  <action>
    Add `dangerous_pattern_check: Callable[[ExplainResponseBase], str | None] | None = None` to `_run_explain_stream()`'s keyword-only params, placed alongside `allowed_source_fields` (same additive, default-None, no-op-for-other-views precedent — RESEARCH Pattern 3). Insert the gate immediately after the `_contains_leak_marker` block's `return` (line ~423) and before `payload = candidate.model_dump(...)` / `set_cached()` (line ~439): if `dangerous_pattern_check is not None`, call it on the validated `candidate`; on a non-None label, `await _audit(..., status="unsafe_denylisted")` then `yield _sse_event({"type":"error","kind":"unsafe","matched_pattern":matched})` then `return` — never reaching `set_cached()`. `AuditLog.details` is free-form JSONB so the new status needs no migration. Extend `backend/tests/test_ai_explain_stream.py` with a test that injects a fake client returning a candidate containing a denylisted pattern and asserts: (a) the `set_cached` dependency is NEVER invoked (mock/spy it — this is the load-bearing backstop, not just an SSE-content check per Pitfall 2), (b) the emitted SSE kind is `unsafe`, (c) an audit row with `status="unsafe_denylisted"` is written. Add a second test proving a benign candidate with `dangerous_pattern_check` set still reaches `set_cached` + `done` (the gate is not over-eager). Confirm the default-None call path is unchanged by running the existing suite.
  </action>
  <acceptance_criteria>
    - `grep -n "dangerous_pattern_check" backend/app/ai/explain.py` shows the param in the signature and the gate call before `set_cached`.
    - The unsafe test asserts `set_cached` was not called (spy/mock assertion), not merely that the stream text lacked the pattern.
    - Existing `test_ai_explain_stream.py` tests (vuln default-None path) remain green.
    - `grep -n "unsafe_denylisted" backend/app/ai/explain.py` present.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_explain_stream.py -q</automated>
  </verify>
  <done>The engine gate exists at the correct position, refuses before caching, is a proven no-op by default, and the unsafe-not-cached backstop asserts on set_cached being skipped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: New explain-remediation-guidance route (D-01 route gate + groundable GET) + registration</name>
  <read_first>
    - `.planning/phases/25-asset-aware-remediation-guidance/25-PATTERNS.md` §"backend/app/api/v1/ai/explain_remediation_guidance.py" (the FULL `explain_host.py` analog reproduced there: `_allowlisted_hash_fields`, the POST require_analyst shape, the GET require_viewer cache-check, and the TWO new-control-flow parts — the D-01 `_refuse()` branch and the `groundable` GET field) and §"__init__.py" (the 3-line registration pattern).
    - `backend/app/api/v1/ai/explain_host.py` (whole file, 104 lines — the UUID-keyed single-record 404 template; NOT `explain_remediation.py` which is CVE-string-keyed).
    - `.planning/phases/25-asset-aware-remediation-guidance/25-RESEARCH.md` Pattern 4 (the route skeleton, the `_refuse()` synthetic one-frame SSE, the `groundable` field rationale — check `=== False` explicitly) and CONTEXT D-01/D-02/D-06/D-17.
    - `backend/tests/test_ai_explain_host_remediation.py` lines 184-253 (the RBAC-matrix + cache-check + cross-tenant-404 test analog).
  </read_first>
  <behavior>
    - POST require_analyst: a Viewer gets 403; an Analyst+ with a groundable finding streams a validated cited result.
    - GET require_viewer: returns `{"cached": true, ...}` on a hit; on a miss returns `{"cached": false, "groundable": <bool>}` (the ONE divergence from every existing GET route).
    - Cross-tenant / missing finding_id → 404 on both POST and GET.
    - Ungroundable finding (D-01 gate False) → a single `{type:"error", kind:"grounded_false"}` SSE frame, audit `status="ungroundable"`, NO model call (same kind as the model's own grounded=false so the analyst never learns which layer fired — D-02).
    - A candidate carrying a denylisted pattern → `kind:"unsafe"` AND `set_cached` never called (route-level assertion of the Task 1 backstop, using an injected fake client).
  </behavior>
  <action>
    Create `backend/app/api/v1/ai/explain_remediation_guidance.py` by copying `explain_host.py`'s four-part shape and swapping: `get_asset_posture`→`get_remediation_guidance_context`; `build_explain_host_prompt`/`ExplainHostResponse`/`HOST_ALLOWLIST`/`host_prompt_version`→their `remediation_guidance` equivalents; `resource_type="host"`→`"remediation-guidance"` (hyphenated — distinct cache/audit namespace from the `"remediation"` posture view); path `/explain-remediation-guidance/{finding_id}` (UUID). Add the `dangerous_pattern_check=contains_dangerous_pattern` kwarg to the `_run_explain_stream(...)` call. TWO new-control-flow additions per RESEARCH Pattern 4 / PATTERNS: (1) BEFORE calling `_run_explain_stream`, if `not has_actionable_remediation_text(record["remediation_action"], record["remediation_info"])`, return a `StreamingResponse` of a synthetic `_refuse()` generator that writes `audit_log_ai_call(..., resource_type="remediation-guidance", status="ungroundable", cost_estimate_usd=0.0)`, commits, and yields `_sse_event({"type":"error","kind":"grounded_false"})` — no model call; (2) the GET cache-miss returns `{"cached": False, "groundable": has_actionable_remediation_text(record["remediation_action"], record["remediation_info"])}`. Import the private `_run_explain_stream`/`_sse_event`/`get_model_and_budget` across the `app.ai.explain` boundary exactly as existing routes already do. Register in `backend/app/api/v1/ai/__init__.py`: add to the import tuple + one `ai_router.include_router(explain_remediation_guidance.router)` line + a one-line comment in the module block (existing "Plan N's D-xx ..." style). Write `backend/tests/test_ai_explain_remediation_guidance.py` (new) covering the full RBAC matrix, cross-tenant 404, groundable-on-miss, ungroundable single-frame refusal, and the route-level unsafe-not-cached backstop, mirroring `test_ai_explain_host_remediation.py`'s structure and fixtures (`tenant_a`/`tenant_b`/`analyst_user`/`viewer_user`/`client_factory` reused verbatim — commit the session before each HTTP call per the WR-13 contract noted in 24-07).
  </action>
  <acceptance_criteria>
    - `grep -n "explain-remediation-guidance" backend/app/api/v1/ai/explain_remediation_guidance.py` shows both POST and GET on `/{finding_id}`.
    - `grep -n "require_analyst" backend/app/api/v1/ai/explain_remediation_guidance.py` on POST and `require_viewer` on GET.
    - `grep -n "explain_remediation_guidance" backend/app/api/v1/ai/__init__.py` shows import + include_router.
    - Route uses `resource_type="remediation-guidance"` (hyphenated) and passes `dangerous_pattern_check=contains_dangerous_pattern`.
    - Tests prove: Viewer POST → 403; cross-tenant → 404; ungroundable → single grounded_false frame + `status="ungroundable"` + no model call; GET miss returns a `groundable` bool; denylisted candidate → `kind:"unsafe"` and `set_cached` not called.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_explain_remediation_guidance.py -q</automated>
  </verify>
  <done>The route exists, inherits RBAC verbatim, gates ungroundable findings before any model call, returns groundable on GET miss, wires the denylist gate, and is registered — the full backend cite-or-refuse + safety-refusal path is test-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → new SSE route | Untrusted request crosses; role + tenant must be enforced |
| Model output → Redis cache → GET cache-check | Dangerous content must never become cache-retrievable |
| Route → shared engine | New optional param must not alter behavior for existing views |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02 | Information Disclosure / Tampering | `_run_explain_stream` dangerous_pattern_check gate | mitigate | Gate runs before `set_cached()` so a denylisted payload is never written to Redis and never retrievable via the GET cache-check; backstop test asserts `set_cached` is skipped (Pitfall 2) |
| T-25-04 | Elevation of Privilege | `explain_remediation_guidance` POST/GET | mitigate | `require_analyst` on POST (paid trigger), `require_viewer` on GET (cached-only), tenant-scoped 404 — reused verbatim from `explain_host.py` (D-17); RBAC-matrix test proves it |
| T-25-03 | Repudiation | route D-01 `_refuse()` gate | mitigate | Ungroundable findings refused before any model call with an audited `ungroundable` status; same grounded_false SSE frame as the model's own refusal (D-02) so no fabricated fix is ever produced |
| T-25-05 | Tampering | `_run_explain_stream` (inherited) | accept | Prompt-injection defense (untrusted_content_policy + leak-marker check) is inherited unchanged from Phase 24; no new injection surface introduced |
</threat_model>

<verification>
- `cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_explain_stream.py tests/test_ai_explain_remediation_guidance.py -q` green.
- Wave-merge regression: `cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_*.py -q` green (mirrors Phase 24's wave-merge convention).
</verification>

<success_criteria>
The backend remediation-guidance tracer is complete and test-proven: asset-aware grounding + cite-or-refuse (two layers) + engine-level safety-refusal-before-cache + RBAC + groundable pre-signal, all reusing `_run_explain_stream()` with exactly one additive parameter.
</success_criteria>

<output>
After completion, create `.planning/phases/25-asset-aware-remediation-guidance/25-03-SUMMARY.md`.
</output>
