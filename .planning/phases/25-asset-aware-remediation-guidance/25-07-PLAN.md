---
phase: 25-asset-aware-remediation-guidance
plan: 07
type: execute
wave: 7
depends_on: [25-06]
files_modified:
  - frontend/src/components/ui/textarea.tsx
  - frontend/src/lib/mutations/use-create-ticket.ts
  - frontend/src/components/ai/ai-explanation-section.tsx
  - frontend/src/components/ai/ai-explanation-section.test.tsx
  - frontend/src/components/vulnerabilities/drill-content.tsx
  - frontend/src/components/vulnerabilities/drill-panel.test.tsx
  - frontend/src/components/vulnerabilities/drill-panel-mobile.tsx
  - frontend/src/components/vulnerabilities/drill-panel-mobile.test.tsx
autonomous: true
requirements: [AIR-02]
must_haves:
  truths:
    - "The 'Copy into ticket description' text-button appears ONLY once validated, grounded, denylist-clean cited steps have rendered (state 1/7); clicking it stores the plain-text-flattened summary (not tinted HTML) into DrillContent state"
    - "The ticket-create dialog gains a Textarea between TicketProviderPicker and the action row, pre-filled if the analyst copied guidance in, else empty; freely editable; nothing is created until the existing 'Create ticket' confirm click (D-08 review/edit-before-create)"
    - "The textarea is threaded into createTicket.mutateAsync as description (so it reaches the backend body proven in Plan 06 — not a UI-only lie, Pitfall 4)"
    - "BOTH the desktop ConfirmModal branch AND the divergent mobile renderConfirm path render + thread the textarea (Pitfall 5 — mobile is a separate code path)"
    - "empty (ticket-description-textarea): starts empty when guidance was never copied in; LOCKED placeholder copy; never a required field"
    - "populated (ticket-description-textarea): pre-filled with plain-text cited-steps content on copy-in; fully editable afterward"
    - "long-text (ticket-description-textarea): plain textarea wrap, no char-count/truncation UI (bounded by the source result's max_tokens)"
    - "Scope fence (D-09): the textarea carries ONLY the copied remediation-guidance text — no title pre-fill, no asset-context, no Jira/Asana field mapping (those are Phase 27)"
  artifacts:
    - path: "frontend/src/components/ui/textarea.tsx"
      provides: "shadcn Textarea primitive styled to match ai-feedback-control's raw textarea tokens"
      contains: "textarea"
    - path: "frontend/src/lib/mutations/use-create-ticket.ts"
      provides: "description?: string on CreateTicketRequest"
      contains: "description"
    - path: "frontend/src/components/ai/ai-explanation-section.tsx"
      provides: "onCopyToDescription?: (text: string) => void prop + 'Copy into ticket description' button"
      contains: "onCopyToDescription"
    - path: "frontend/src/components/vulnerabilities/drill-content.tsx"
      provides: "description state + renderConfirm arg extension + ConfirmModal Textarea + mutateAsync threading"
      contains: "onDescriptionChange"
    - path: "frontend/src/components/vulnerabilities/drill-panel-mobile.tsx"
      provides: "mirrored Textarea in the mobile renderConfirm path"
      contains: "onDescriptionChange"
  key_links:
    - from: "AiExplanationSection 'Copy into ticket description' button"
      to: "DrillContent setDescription"
      via: "onCopyToDescription callback with plain-text-flattened summary"
      pattern: "onCopyToDescription"
    - from: "DrillContent fireTicket()"
      to: "createTicket.mutateAsync({ ..., description })"
      via: "threads description || undefined into the mutation body"
      pattern: "description: description \\|\\| undefined"
    - from: "drill-panel-mobile.tsx renderConfirm"
      to: "Textarea between TicketProviderPicker and action row"
      via: "destructures the new description/onDescriptionChange args"
      pattern: "onDescriptionChange"
---

<objective>
Deliver AIR-02's user-facing slice: a "Copy into ticket description" affordance under rendered cited steps, and a pre-fillable, freely-editable description `Textarea` inside the EXISTING ticket-create dialog — threaded through to the backend `description` field (Plan 06) so the analyst's reviewed text actually ships. Both the desktop `ConfirmModal` path AND the divergent mobile `renderConfirm` path get the field (Pitfall 5).

Purpose: satisfies AIR-02's "populate a draft ticket description for the analyst to review/edit before creating" using the existing human-click create gate. Strictly description-only (D-09 scope fence) — no title/asset-context/field-mapping (Phase 27).
Output: the shadcn Textarea primitive, the mutation field, the copy-in prop/button, and the desktop + mobile dialog wiring, all test-covered including the mutation-body assertion (not just the DOM).

MANDATORY UI DISCIPLINE: read `sketch-findings-getvul` (`references/interaction-patterns.md`, `state-patterns.md`, `copy-voice.md`, `foundation.md`) before touching components. All copy strings are LOCKED in 25-UI-SPEC's Copywriting Contract — use them exactly. NO freehand hex; locked Inter+JetBrains Mono; loading/empty states already inherited.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-asset-aware-remediation-guidance/25-CONTEXT.md
@.planning/phases/25-asset-aware-remediation-guidance/25-RESEARCH.md
@.planning/phases/25-asset-aware-remediation-guidance/25-PATTERNS.md
@.planning/phases/25-asset-aware-remediation-guidance/25-UI-SPEC.md

<interfaces>
From Plan 06 (backend contract this plan sends to): TicketCreateRequest now accepts an optional `description: str | None` (≤10000 chars, whitespace→None); create_tickets() uses it as the ticket body when non-empty.
Existing (frontend, reused): ConfirmModal children slot (ConfirmModal.tsx:92); TicketProviderPicker {value,onChange} (ticket-provider-picker.tsx:29-32); AiExplanationCitations (unchanged); ai-feedback-control.tsx raw <textarea> (94-103, styling precedent).
</interfaces>
</context>

<artifacts_this_phase_produces>
This plan contributes (AIR-02 frontend): `frontend/src/components/ui/textarea.tsx` (shadcn); `description?: string` on `CreateTicketRequest`; `onCopyToDescription?` prop + "Copy into ticket description" button on `AiExplanationSection`; `description`/`onDescriptionChange` state + threading in `drill-content.tsx` (desktop ConfirmModal) and `drill-panel-mobile.tsx` (mobile renderConfirm).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: shadcn Textarea + mutation description field + copy-in prop/button</name>
  <read_first>
    - `.claude/skills/sketch-findings-getvul/references/interaction-patterns.md`, `foundation.md`, `copy-voice.md` (MANDATORY).
    - `.planning/phases/25-asset-aware-remediation-guidance/25-PATTERNS.md` §"shadcn Textarea primitive" (lines 587-602 — style to match `ai-feedback-control.tsx`'s raw textarea tokens), §"use-create-ticket.ts" (571-583 — add `description?: string`), §"ai-explanation-section.tsx" point 4 (493-500 — the `onCopyToDescription` prop shape + where the button renders: beneath `AiExplanationCitations` in the grounded-done AND cache-hit branches only).
    - `.planning/phases/25-asset-aware-remediation-guidance/25-UI-SPEC.md` §Copywriting ("Copy into ticket description" button label) + §Interaction Contract AIR-02 point 1 (plain-text-flattened summary, not tinted HTML).
    - `frontend/src/components/ai/ai-feedback-control.tsx` lines 94-103 (textarea token classes), `frontend/src/lib/mutations/use-create-ticket.ts` lines 7-13, `frontend/src/components/ai/ai-explanation-section.tsx` grounded-done + cache-hit render branches.
    - 25-UI-SPEC §Registry Safety (`npx shadcn add textarea` — official registry, cleared).
  </read_first>
  <behavior>
    - The "Copy into ticket description" button renders ONLY in the grounded-done and cache-hit branches (state 1/7), never in any degraded/refuse/loading state.
    - Clicking it calls `onCopyToDescription` with the plain-text-flattened `summary` string (no citation-tint HTML).
    - When `onCopyToDescription` is not passed (vuln/host/remediation-posture mounts), no button renders.
  </behavior>
  <action>
    Run `npx shadcn add textarea` to create `frontend/src/components/ui/textarea.tsx`, then restyle it to match `ai-feedback-control.tsx`'s raw `<textarea>` tokens (`border-border-subtle bg-surface ... focus:border-violet`) so it matches the one existing free-text input — NO shadcn un-themed default, NO new hex. Add `description?: string` to `CreateTicketRequest` in `use-create-ticket.ts` (mutation body unchanged — `JSON.stringify(body)` already serializes it). Add an optional `onCopyToDescription?: (text: string) => void` prop to `AiExplanationSection` (controlled callback-up shape, mirroring `TicketProviderPicker`'s `onChange` convention); render a small text-button labelled exactly "Copy into ticket description" beneath `AiExplanationCitations` in the grounded-`done` and cache-hit branches ONLY, calling `onCopyToDescription(plainTextSummary)`. Extend `ai-explanation-section.test.tsx`: button appears in the grounded-done branch and calls the callback with the summary text; absent when the prop is omitted; absent in degraded/refuse states.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/ui/textarea.tsx` exists and uses the sunset token classes (grep `border-border-subtle` and `focus:border-violet`; no `#` hex).
    - `grep -n "description" frontend/src/lib/mutations/use-create-ticket.ts` shows `description?: string` on `CreateTicketRequest`.
    - `grep -n "onCopyToDescription" frontend/src/components/ai/ai-explanation-section.tsx` present; button label is exactly "Copy into ticket description".
    - Tests prove the button renders only in grounded-done/cache-hit, calls the callback with the plain-text summary, and is absent when the prop is omitted.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run ai-explanation-section</automated>
  </verify>
  <done>The Textarea primitive, the mutation field, and the copy-in affordance exist; the button is gated to the cited-steps success states and passes plain text upward.</done>
</task>

<task type="auto">
  <name>Task 2: Desktop — description state, ConfirmModal Textarea, mutation threading</name>
  <read_first>
    - `.claude/skills/sketch-findings-getvul/references/interaction-patterns.md`, `state-patterns.md` (MANDATORY).
    - `.planning/phases/25-asset-aware-remediation-guidance/25-PATTERNS.md` §"drill-content.tsx" Analog 2 (lines 518-546 — the `ticketProvider` state + `renderConfirm` args + inline `ConfirmModal` fallback + `fireTicket` threading; ConfirmModal children slot accepts a second child).
    - `.planning/phases/25-asset-aware-remediation-guidance/25-UI-SPEC.md` §Interaction Contract AIR-02 (the caption + placeholder copy, points 2-5; the scope fence D-09).
    - `frontend/src/components/vulnerabilities/drill-content.tsx` lines 59-66 (`renderConfirm` args type), 81 (`ticketProvider` state), 148-174 (`fireTicket`), 328-349 (the `renderConfirm`/`ConfirmModal` fallback), 267-273 + the remediation-guidance section mount added in Plan 04; `frontend/src/components/ui/ConfirmModal.tsx` line 92 (children slot).
  </read_first>
  <behavior>
    - `onCopyToDescription={setDescription}` is passed to the remediation-guidance `AiExplanationSection` mount; copying in pre-fills the textarea.
    - The desktop `ConfirmModal` fallback renders the `Textarea` (with the LOCKED caption above it) as a second child alongside `TicketProviderPicker`; it starts empty if nothing was copied in; it is freely editable/clearable.
    - `fireTicket()` threads `description: description || undefined` into `createTicket.mutateAsync({...})`; nothing is submitted until the existing "Create ticket" confirm click.
  </behavior>
  <action>
    In `drill-content.tsx`: add `const [description, setDescription] = useState('')`; pass `onCopyToDescription={setDescription}` to the `resourceType="remediation-guidance"` `AiExplanationSection` mount (added in Plan 04). Extend the `renderConfirm` args type with `description: string` + `onDescriptionChange: (v: string) => void` and pass both in the `renderConfirm({...})` call. In the inline `<ConfirmModal>` fallback, add `<Textarea>` as a second child alongside `<TicketProviderPicker>`, with the LOCKED caption "Pre-filled from remediation guidance — review and edit before creating." above it and the LOCKED placeholder "No remediation guidance yet — add a description or leave blank." No char-count, no required validation (D-09 point 4). Thread `description: description || undefined` into `fireTicket()`'s `createTicket.mutateAsync({...})`. Extend `drill-panel.test.tsx` to assert: the textarea renders in the confirm dialog, pre-fills after copy-in, and its value threads into the mutation body (assert on the mutation payload, not just the DOM — Pitfall 4).
  </action>
  <acceptance_criteria>
    - `grep -n "onDescriptionChange" frontend/src/components/vulnerabilities/drill-content.tsx` present in the renderConfirm args type + call.
    - `grep -n "description: description || undefined" frontend/src/components/vulnerabilities/drill-content.tsx` present in `fireTicket`.
    - The caption and placeholder strings match 25-UI-SPEC verbatim.
    - `drill-panel.test.tsx` asserts the mutation body carries the description (not only the DOM value).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run drill-panel</automated>
  </verify>
  <done>Desktop: copy-in pre-fills the dialog textarea, edits are preserved, and the value reaches createTicket.mutateAsync's body — proven at the mutation boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Mobile — mirror the Textarea in the divergent renderConfirm path</name>
  <read_first>
    - `.planning/phases/25-asset-aware-remediation-guidance/25-PATTERNS.md` §"drill-panel-mobile.tsx" (lines 550-567 — the inline `TicketProviderPicker` block inside `Drawer.NestedRoot`; the renderConfirm callback signature at 102-109 that must destructure the two new args) and RESEARCH Pitfall 5 (mobile is a SEPARATE code path, not a shared mount).
    - `frontend/src/components/vulnerabilities/drill-panel-mobile.tsx` lines 102-109 (renderConfirm signature) + 138-145 (the TicketProviderPicker render block + the Cancel/Confirm action row).
    - `.planning/phases/25-asset-aware-remediation-guidance/25-UI-SPEC.md` (same caption/placeholder as desktop; visual hierarchy — textarea subordinate to the picker).
  </read_first>
  <behavior>
    - The mobile `renderConfirm` callback destructures the new `description` + `onDescriptionChange` args (threaded from `drill-content.tsx`'s extended type) and renders a `Textarea` block between the `TicketProviderPicker` div and the Cancel/Confirm action row — same relative position and same LOCKED caption/placeholder as desktop.
    - Mobile ticket creation carries the description exactly as desktop does.
  </behavior>
  <action>
    In `drill-panel-mobile.tsx`, destructure `description` + `onDescriptionChange` in the `renderConfirm` callback (lines 102-109) and render a `<Textarea>` block (with the LOCKED caption) between the `<TicketProviderPicker>` div (138-145) and the Cancel/Confirm button row — mirroring the desktop insertion. Do NOT import `ConfirmModal` (mobile builds its own `Drawer.NestedRoot` markup). Extend `drill-panel-mobile.test.tsx` to assert the mobile confirm path renders the textarea and threads its value (Pitfall 5 — the mobile path must not be silently skipped).
  </action>
  <acceptance_criteria>
    - `grep -n "onDescriptionChange" frontend/src/components/vulnerabilities/drill-panel-mobile.tsx` present in the destructured renderConfirm args + the Textarea.
    - The mobile Textarea sits between the TicketProviderPicker and the action row; caption/placeholder match 25-UI-SPEC verbatim.
    - `drill-panel-mobile.test.tsx` asserts the mobile confirm path renders + threads the textarea (does not merely test desktop).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run drill-panel-mobile</automated>
  </verify>
  <done>The mobile ticket-create path renders and threads the description Textarea identically to desktop — both divergent code paths covered (Pitfall 5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AiExplanationSection → DrillContent state | Copied guidance text crosses via a callback; must be plain text, not executable/tinted markup |
| DrillContent → createTicket mutation → backend | Analyst-edited description crosses to the (Plan 06) bounded backend field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07 | Elevation of Privilege | ticket-create flow | accept | The description rides the EXISTING create-ticket mutation + its existing human-click confirm gate and backend authz (Plan 06) — no new creation path or role bypass introduced |
| T-25-11 | Tampering | `onCopyToDescription` payload | mitigate | The copy-in stores the plain-text-flattened summary string (no HTML), and the backend field is length-bounded + extra=forbid (Plan 06); no markup injection surface |
| T-25-06 | Tampering | `createTicket.mutateAsync` body | mitigate | The description is validated server-side (max_length + whitespace coercion, Plan 06); the client sends `description || undefined`, never an unbounded blob |
</threat_model>

<verification>
- `cd frontend && npx vitest run ai-explanation-section drill-panel drill-panel-mobile` green.
- Full frontend suite `cd frontend && npx vitest run` green; backend `tests/test_ticketing_dispatch.py` green (Plan 06) before `/gsd-verify-work 25`.
</verification>

<success_criteria>
An analyst can copy validated remediation guidance into the ticket-create dialog's description field (desktop + mobile), edit it freely, and create the ticket — with the reviewed text reaching the created ticket body. ROADMAP success criterion 3 (AIR-02) is satisfied, strictly description-only (D-09).
</success_criteria>

<output>
After completion, create `.planning/phases/25-asset-aware-remediation-guidance/25-07-SUMMARY.md`.
</output>
