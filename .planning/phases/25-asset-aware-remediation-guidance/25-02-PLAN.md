---
phase: 25-asset-aware-remediation-guidance
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - backend/app/ai/schemas.py
  - backend/app/ai/prompt_builder.py
  - backend/tests/test_ai_schemas.py
  - backend/tests/test_ai_prompt_builder_remediation_guidance.py
autonomous: true
requirements: [AIR-01]
must_haves:
  truths:
    - "The new response schema reuses ExplainResponseBase with zero new fields — cited steps live as prose inside summary (D-03, matches the two existing 24-08 variants)"
    - "A scanner_verbatim citation's text is provably a substring of the grounding-record field it claims to cite (24-AI-SPEC D4 provenance)"
    - "The remediation-guidance prompt builder constructs the allowlisted object field-by-field — owner PII is structurally impossible to carry (T-25-01)"
    - "The system prompt states cite-verbatim-first + refuse-rather-than-invent as prompt text, backing the code gate (D-01/D-03)"
  artifacts:
    - path: "backend/app/ai/schemas.py"
      provides: "ExplainRemediationGuidanceResponse(ExplainResponseBase)"
      contains: "class ExplainRemediationGuidanceResponse"
    - path: "backend/app/ai/prompt_builder.py"
      provides: "REMEDIATION_GUIDANCE_ALLOWLIST + AllowlistedRemediationGuidance + build_explain_remediation_guidance_prompt() + remediation_guidance_prompt_version()"
      contains: "REMEDIATION_GUIDANCE_ALLOWLIST"
    - path: "backend/tests/test_ai_prompt_builder_remediation_guidance.py"
      provides: "Allowlist PII-exclusion (dict + attribute-object) + injection-isolation coverage"
  key_links:
    - from: "build_explain_remediation_guidance_prompt()"
      to: "AllowlistedRemediationGuidance via _to_allowlisted_remediation_guidance()"
      via: "field-by-field _get_field reads, never a **dict spread"
      pattern: "_to_allowlisted_remediation_guidance"
    - from: "remediation_guidance_prompt_version()"
      to: "prompt_version(..., ExplainRemediationGuidanceResponse)"
      via: "reuses the generalized hashing fn (24-08)"
      pattern: "prompt_version\\("
---

<objective>
Add the 4th allowlist-quadruplet (schema + allowlist + Allowlisted model + prompt builder) for the `remediation-guidance` view, mirroring the `host` quadruplet 24-08 established. Zero new schema fields; the "steps" live as cited prose inside `summary`, exactly as `ExplainHostResponse`/`ExplainRemediationResponse` already prove is sufficient.

Purpose: This is the contract the new route (Plan 03) dispatches against. Interface-first: define the response schema, then the prompt builder that produces the untrusted-content-as-data prompt for it.
Output: `ExplainRemediationGuidanceResponse` in `schemas.py`; the allowlist + prompt-builder symbols in `prompt_builder.py`; a substring-provenance test; a dedicated allowlist/injection test file.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-asset-aware-remediation-guidance/25-CONTEXT.md
@.planning/phases/25-asset-aware-remediation-guidance/25-RESEARCH.md
@.planning/phases/25-asset-aware-remediation-guidance/25-PATTERNS.md
@.planning/phases/24-ai-foundation-explain-this-vuln/24-08-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan contributes: `ExplainRemediationGuidanceResponse` (schemas.py); `REMEDIATION_GUIDANCE_ALLOWLIST`, `AllowlistedRemediationGuidance`, `_to_allowlisted_remediation_guidance()`, `SYSTEM_PROMPT_REMEDIATION_GUIDANCE`, `FEW_SHOT_REMEDIATION_GUIDANCE`, `build_explain_remediation_guidance_prompt()`, `remediation_guidance_prompt_version()` (prompt_builder.py).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Response schema variant + scanner_verbatim substring-provenance test</name>
  <read_first>
    - `.planning/phases/25-asset-aware-remediation-guidance/25-PATTERNS.md` §"backend/app/ai/schemas.py" (the `ExplainHostResponse` zero-new-fields analog at lines 75-79; the `recheck_business_rules()` reuse note).
    - `backend/app/ai/schemas.py` lines 44-79 — `ExplainResponseBase` (summary/business_risk/citations/grounded) + `ExplainHostResponse`/`ExplainRemediationResponse` (the one-line-body-plus-docstring shape to copy verbatim).
    - `.planning/phases/25-asset-aware-remediation-guidance/25-RESEARCH.md` Pattern 6 (lines 499-515 — the substring-provenance test to add) and `backend/tests/test_ai_schemas.py` (the existing file to extend).
  </read_first>
  <behavior>
    - `ExplainRemediationGuidanceResponse` validates the identical payload shape as `ExplainHostResponse` (no extra fields).
    - Provenance: for every citation with `source == "scanner_verbatim"` and a non-null `source_field`, `citation.text` is a substring of `str(record[citation.source_field])`.
  </behavior>
  <action>
    Add `class ExplainRemediationGuidanceResponse(ExplainResponseBase): pass` with a docstring (zero new fields — cited steps are prose in `summary`, per the two existing 24-08 variants). Then add `test_scanner_verbatim_citation_is_substring_of_source_field` to `backend/tests/test_ai_schemas.py` per RESEARCH Pattern 6: construct an `ExplainRemediationGuidanceResponse` fixture whose `scanner_verbatim` citation text appears in a grounding-record `remediation_action` field, and assert the substring relationship. `recheck_business_rules()` is reused UNCHANGED (it already takes `allowed_source_fields`) — do not edit it.
  </action>
  <acceptance_criteria>
    - `grep -n "class ExplainRemediationGuidanceResponse" backend/app/ai/schemas.py` present; body is `pass` (no new fields).
    - `test_scanner_verbatim_citation_is_substring_of_source_field` exists in `test_ai_schemas.py` and passes.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_schemas.py -k "substring or remediation_guidance" -q</automated>
  </verify>
  <done>Schema subclass exists with zero new fields; the provenance property test proves scanner_verbatim citations are real substrings of their claimed source field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Allowlist + prompt-builder quadruplet (cite-verbatim-first system prompt)</name>
  <read_first>
    - `.planning/phases/25-asset-aware-remediation-guidance/25-PATTERNS.md` §"backend/app/ai/prompt_builder.py" (the HOST quadruplet analog at lines 342-579: allowlist declaration, `AllowlistedHostPosture` + `_to_allowlisted_host_posture` field-by-field constructor, `SYSTEM_PROMPT_HOST` + `build_explain_host_prompt` + `host_prompt_version`; and the `_get_field`/`_truncate` shared helpers).
    - `.planning/phases/25-asset-aware-remediation-guidance/25-RESEARCH.md` Pattern 2 (the 12-field `REMEDIATION_GUIDANCE_ALLOWLIST` table + the explicit owner-PII exclusion list).
    - `backend/app/ai/prompt_builder.py` lines 48-67 (`VULN_ALLOWLIST`), 104-113 (`_get_field`), 127-141 (`_truncate`), 342-579 (the HOST quadruplet to mirror), and `FEW_SHOT_HOST`'s second grounded=false exemplar.
    - `backend/tests/test_ai_prompt_builder.py` lines 117-181 (`test_allowlist_enforcement_on_object_with_extra_attributes` — the dict + attribute-object PII test to mirror) and CONTEXT D-01/D-03/D-10.
  </read_first>
  <behavior>
    - Owner PII never reaches the prompt: passing a record carrying assigned_user/directory_user/managed_by/building/serial_number/department (both as a dict AND as an attribute-bearing object) yields prompt text containing none of those field names or their values.
    - Injection isolation: a `remediation_info` value containing "IGNORE ALL PREVIOUS INSTRUCTIONS" is placed inside the `<scanner_data>` block as data, never as an instruction (mirrors the existing injection-isolation test).
    - `remediation_guidance_prompt_version()` returns a stable hash folding SYSTEM_PROMPT + FEW_SHOT + `ExplainRemediationGuidanceResponse`, distinct from `host_prompt_version()`.
  </behavior>
  <action>
    Add `REMEDIATION_GUIDANCE_ALLOWLIST` (frozenset of the 12 fields from RESEARCH Pattern 2: cve_id, severity, exploit_available, cisa_kev, remediation_action, remediation_info, affected_product, affected_version, fixed_version, asset_hostname, os_name, os_version). Add `AllowlistedRemediationGuidance(BaseModel)` with `model_config = {"extra": "forbid"}`, every field `| None = None`, no field for any owner-PII name. Add `_to_allowlisted_remediation_guidance(record)` reading each allowlisted name via the shared `_get_field()` one at a time — NEVER `**record` / `record.__dict__` spread — and applying `_truncate()` to `remediation_action`/`remediation_info` (as `_to_allowlisted_finding` does). Add `SYSTEM_PROMPT_REMEDIATION_GUIDANCE` with the `<untrusted_content_policy>` block AND an explicit instruction: cite the vendor's own solution text verbatim tagged `scanner_verbatim` BEFORE any `ai_interpreted` step, and set `grounded: false` refusing rather than inventing a fix when the cited text is too weak (D-01/D-03 — this is the one place that product intent becomes prompt text). Add `FEW_SHOT_REMEDIATION_GUIDANCE` with two exemplars (2nd demonstrates the grounded=false insufficient-evidence case on a remediation-guidance-shaped input). Add `build_explain_remediation_guidance_prompt(record)` emitting `<scanner_data source="remediation_guidance">...</scanner_data>` (follow the `source="host_posture"` naming convention, NOT the raw connector `source`). Add `remediation_guidance_prompt_version()` calling `prompt_version(SYSTEM_PROMPT_REMEDIATION_GUIDANCE, FEW_SHOT_REMEDIATION_GUIDANCE, ExplainRemediationGuidanceResponse)`. Write `backend/tests/test_ai_prompt_builder_remediation_guidance.py` first (RED) covering the PII-exclusion (dict + object) and injection-isolation behaviors.
  </action>
  <acceptance_criteria>
    - `grep -n "REMEDIATION_GUIDANCE_ALLOWLIST\|def build_explain_remediation_guidance_prompt\|def remediation_guidance_prompt_version\|class AllowlistedRemediationGuidance" backend/app/ai/prompt_builder.py` returns all four.
    - `AllowlistedRemediationGuidance` sets `model_config = {"extra": "forbid"}` and declares exactly the 12 allowlisted fields (no owner-PII field).
    - PII test asserts none of the 6 owner-PII field names/values appear in the built prompt (dict + attribute-object inputs).
    - `_to_allowlisted_remediation_guidance` contains no `**` spread of the record; `grep -c "\\*\\*record" backend/app/ai/prompt_builder.py` unchanged from before this task within the new function.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_prompt_builder_remediation_guidance.py -q</automated>
  </verify>
  <done>The remediation-guidance quadruplet exists, structurally excludes owner PII, isolates injected scanner text as data, and its prompt version hashes distinctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grounding record → model prompt | Allowlisted asset/vuln facts cross into the prompt; owner PII must not |
| Untrusted scanner text (`remediation_info`) → prompt | Attacker-influenced scanner text must be treated as data, not instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure | `AllowlistedRemediationGuidance` / `_to_allowlisted_remediation_guidance()` | mitigate | `extra="forbid"` model + field-by-field `_get_field` reads; no owner-PII field declared, proven by dict+object PII test |
| T-25-05 | Tampering | `SYSTEM_PROMPT_REMEDIATION_GUIDANCE` / `build_explain_remediation_guidance_prompt()` | mitigate | Untrusted scanner text wrapped in `<scanner_data>` with an untrusted_content_policy block (inherited Phase 24 contract); injection-isolation test proves it |
| T-25-03 | Repudiation | `SYSTEM_PROMPT_REMEDIATION_GUIDANCE` | mitigate | Prompt text mandates cite-verbatim-first + refuse-rather-than-invent, backing the deterministic code gate (D-01/D-03); grounded=false few-shot exemplar reinforces refusal |
</threat_model>

<verification>
- `cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_schemas.py tests/test_ai_prompt_builder_remediation_guidance.py -q` green.
</verification>

<success_criteria>
The remediation-guidance schema + prompt-builder quadruplet exists, mirrors the 24-08 host pattern exactly, structurally excludes owner PII, and its system prompt encodes cite-verbatim-first + refuse-rather-than-invent.
</success_criteria>

<output>
After completion, create `.planning/phases/25-asset-aware-remediation-guidance/25-02-SUMMARY.md`.
</output>
