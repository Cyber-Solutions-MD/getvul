---
phase: 15-mobile-a11y-perf-quality-gate
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - frontend/e2e/viewport-scroll.spec.ts
  - frontend/e2e/a11y-routes.spec.ts
  - frontend/e2e/smoke.spec.ts
  - frontend/e2e/reduced-motion.spec.ts
autonomous: false
requirements: [UX-07-01, UX-07-02, UX-07-03, UX-07-04, UX-07-05, UX-07-07]

must_haves:
  truths:
    - "Every authenticated route, at 360/390/768/1280, has no horizontal scroll"
    - "Every authenticated route passes axe with zero critical/serious WCAG 2.1 AA violations"
    - "WCAG 2.2 AA (target-size) runs report-only and prints warnings without failing"
    - "Smoke routes render across Chromium, WebKit, and Firefox"
    - "At <768px the 4-slot bottom-nav is visible and the 'More' sheet opens"
    - "On /login the html data-theme attribute reflects the emulated prefers-color-scheme"
    - "With reducedMotion emulated, the login gradient-drift computed animation-duration is near-zero"
  artifacts:
    - path: "frontend/e2e/viewport-scroll.spec.ts"
      provides: "UX-07-01 — ALL_ROUTES × 4 viewports horizontal-scroll assertion"
      contains: "scrollWidth"
    - path: "frontend/e2e/a11y-routes.spec.ts"
      provides: "UX-07-03 — per-route axe sweep (blocking 2.1 AA) + report-only 2.2 AA + bottom-nav presence at 360px"
      contains: "makeAxeBuilder"
    - path: "frontend/e2e/smoke.spec.ts"
      provides: "UX-07-07 + UX-07-05 — cross-browser smoke + data-theme assertion"
      contains: "data-theme"
    - path: "frontend/e2e/reduced-motion.spec.ts"
      provides: "UX-07-04 — emulated reduced-motion animation-duration assertion"
      contains: "reducedMotion"
  key_links:
    - from: "frontend/e2e/a11y-routes.spec.ts"
      to: "frontend/e2e/fixtures/axe.ts"
      via: "imports makeAxeBuilder fixture"
      pattern: "makeAxeBuilder"
    - from: "frontend/e2e/viewport-scroll.spec.ts"
      to: "ALL_ROUTES list"
      via: "iterates routes × viewports"
      pattern: "for .*VIEWPORTS|for .*ALL_ROUTES"
---

<objective>
Write the Playwright route-level specs that ARE the automated quality gate, run them against the finished UI (mobile-nav + modal conversions from Wave 1 are in place), and fix everything the sweep surfaces (D-09 audit-fix loop). Four specs: `viewport-scroll.spec.ts` (UX-07-01 — every authed route × 360/390/768/1280, no horizontal scroll), `a11y-routes.spec.ts` (UX-07-03 — per-route axe zero critical/serious at WCAG 2.1 AA, plus a 360px bottom-nav presence check and report-only WCAG 2.2 target-size), `smoke.spec.ts` (UX-07-07 cross-browser + UX-07-05 data-theme assertion), and `reduced-motion.spec.ts` (UX-07-04 emulated reduced-motion).

This is a `checkpoint` plan: the audit-fix loop ends when the defect list is empty (D-09), and the human runs the suite against a live `docker-compose` backend (the auth fixture needs it). The plan PAUSES for the human to start the stack and confirm the gate is green.

Purpose: Satisfies UX-07-01, -02, -03, -04, -05, -07 automated coverage. Manual-only items (Safari.app glyphs, focus-not-obscured, dark-OS cold-load white-flash) are routed to Plan 06's HUMAN-UAT.

Output: Four spec files + whatever UI fixes the audit required (recorded in SUMMARY).
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-mobile-a11y-perf-quality-gate/15-CONTEXT.md
@.planning/phases/15-mobile-a11y-perf-quality-gate/15-RESEARCH.md
@.planning/phases/15-mobile-a11y-perf-quality-gate/15-VALIDATION.md

<interfaces>
<!-- Playwright config (Plan 01): projects setup / chromium-a11y / chromium-smoke / webkit-smoke / firefox-smoke; storageState e2e/.auth/state.json; baseURL http://localhost:3000. -->
<!-- axe fixture (Plan 01, e2e/fixtures/axe.ts): makeAxeBuilder() → withTags(['wcag2a','wcag2aa','wcag21aa']); makeAxeBuilderReportOnly() → withTags(['wcag22aa']). Import { test, expect } from './fixtures/axe'. -->
<!-- The 4 audited viewport widths: 360, 390, 768, 1280 (EXACT — not approximations). -->
<!-- ALL_ROUTES (11 authed routes; dynamic [id] routes need a real seed id — DISCOVER it, do not hardcode):
     /dashboard, /dashboard/vulnerabilities, /dashboard/assets, /dashboard/tickets, /dashboard/tickets/rules,
     /dashboard/cspm, /dashboard/connectors, /dashboard/users, /dashboard/settings,
     + /dashboard/assets/<id> and /dashboard/tickets/<id> discovered by visiting the list and clicking the first row. -->
<!-- Smoke routes (D-01): /login, /dashboard, /dashboard/vulnerabilities, + one detail page (asset or ticket). -->
<!-- Horizontal-scroll assertion (Pitfall 2): use Math.max(documentElement.scrollWidth, body.scrollWidth) <= clientWidth; WebKit is stricter — use body-level too. -->
<!-- Auth redirect timing (Pitfall 1): after page.goto, wait for nav landmark visible (e.g. nav[aria-label="Primary navigation"] on desktop, or nav[aria-label="Mobile navigation"] at <768px) OR waitForURL('**/dashboard*'); do NOT rely on networkidle alone. -->
<!-- Bottom-nav (Plan 02): <nav aria-label="Mobile navigation"> with 4 slots, "More" button (MoreHorizontal). At 360px it must be visible; clicking "More" opens a vaul Drawer (role dialog / aria-label "More navigation"). -->
<!-- Theme bootstrap (layout.tsx): sets document.documentElement[data-theme] = stored getvul_theme || (prefers-color-scheme light ? 'light' : 'dark'). Emulate via page.emulateMedia({ colorScheme: 'dark' }). -->
<!-- Reduced motion: page.emulateMedia({ reducedMotion: 'reduce' }); login gradient-mesh el (.bg-gradient-mesh) computed getComputedStyle(el).animationDuration should be ~0.01ms (globals.css blanket) — assert <= 0.02s. -->
<!-- PREREQ: docker-compose up -d postgres redis backend frontend, then npm run build && npm run start (or next dev) on port 3000, before npx playwright test. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write viewport-scroll.spec.ts + reduced-motion.spec.ts</name>
  <read_first>
    - frontend/e2e/playwright.config.ts (Plan 01 — project names, storageState, baseURL)
    - frontend/e2e/fixtures/auth.ts (Plan 01 — base test export)
    - frontend/src/components/shell/sidebar.tsx + bottom-nav.tsx (nav landmark aria-labels to wait on)
    - frontend/src/app/login/page.tsx (.bg-gradient-mesh element for the reduced-motion assertion)
    - frontend/src/app/globals.css (prefers-reduced-motion blanket — confirms expected ~0.01ms duration)
  </read_first>
  <action>
    Create `frontend/e2e/viewport-scroll.spec.ts`:
      - Define `VIEWPORTS = [{width:360,height:812},{width:390,height:844},{width:768,height:1024},{width:1280,height:900}]` and the static `ALL_ROUTES` list (9 static routes above). For the 2 dynamic routes, add a helper `discoverDetailRoute(page, listRoute, rowSelector)` that visits the list, clicks the first row link, and reads `page.url()` — append discovered `/dashboard/assets/<id>` and `/dashboard/tickets/<id>` to the swept set (skip gracefully with `test.skip` if the list is empty).
      - For each viewport (`test.describe` + `test.use({ viewport })`) and each route: `page.goto(route)`, wait for a nav landmark visible (Pitfall 1 — at <768px wait for `nav[aria-label="Mobile navigation"]`, otherwise `nav[aria-label="Primary navigation"]`), then assert no horizontal scroll using `Math.max(documentElement.scrollWidth, body.scrollWidth) <= documentElement.clientWidth` (Pitfall 2). Use the `chromium-a11y` project (default for non-smoke specs).
    Create `frontend/e2e/reduced-motion.spec.ts`:
      - `test.use({ viewport: {width:1280,height:900} })`. In a test: `await page.emulateMedia({ reducedMotion: 'reduce' })`, `page.goto('/login')`, locate `.bg-gradient-mesh`, read `getComputedStyle(el).animationDuration` via `page.evaluate`, assert it parses to `<= 0.02` seconds (the globals.css blanket forces ~0.01ms). Add a second case on `/dashboard` asserting the hero urgency dot (`bg-severity-critical` rounded dot) animation-duration is near-zero under reduced motion.
  </action>
  <verify>
    <automated>cd frontend && grep -q "scrollWidth" e2e/viewport-scroll.spec.ts && grep -Eq "360|390|768|1280" e2e/viewport-scroll.spec.ts && grep -q "reducedMotion" e2e/reduced-motion.spec.ts && npx playwright test --list --config=e2e/playwright.config.ts 2>&1 | grep -Eq "viewport-scroll|reduced-motion" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/e2e/viewport-scroll.spec.ts` contains the four widths `360`, `390`, `768`, `1280` and a `scrollWidth` assertion
    - `frontend/e2e/viewport-scroll.spec.ts` discovers dynamic [id] routes from the list (contains `click` on a row + reads `page.url()`), does NOT hardcode a literal id
    - `frontend/e2e/reduced-motion.spec.ts` contains `emulateMedia` with `reducedMotion: 'reduce'` and an `animationDuration` assertion
    - `cd frontend && npx playwright test --list --config=e2e/playwright.config.ts` lists both specs (exits 0)
  </acceptance_criteria>
  <done>viewport-scroll spec sweeps all routes × 4 widths (with dynamic-route discovery) for horizontal scroll; reduced-motion spec asserts near-zero animation under emulated reduce.</done>
</task>

<task type="auto">
  <name>Task 2: Write a11y-routes.spec.ts + smoke.spec.ts</name>
  <read_first>
    - frontend/e2e/fixtures/axe.ts (Plan 01 — makeAxeBuilder + makeAxeBuilderReportOnly)
    - frontend/e2e/playwright.config.ts (smoke project names: chromium-smoke / webkit-smoke / firefox-smoke)
    - frontend/src/components/shell/bottom-nav.tsx (Plan 02 — aria-label "Mobile navigation", MoreHorizontal "More" button, more-sheet aria-label "More navigation")
    - frontend/src/app/layout.tsx (THEME_BOOTSTRAP_SCRIPT — data-theme on <html>)
    - frontend/src/components/vulnerabilities (severity glyph rendering for the smoke glyph presence check)
  </read_first>
  <action>
    Create `frontend/e2e/a11y-routes.spec.ts` (imports `{ test, expect }` from './fixtures/axe'):
      - For each route in ALL_ROUTES (reuse the same list + dynamic-route discovery as Task 1 — extract it to a shared `e2e/routes.ts` helper to avoid duplication): `page.goto(route)`, wait for nav landmark, run `const results = await makeAxeBuilder().analyze()`, filter `v.impact === 'critical' || v.impact === 'serious'`, `console.error` the filtered list for debuggability, and `expect(blocking).toHaveLength(0)` (BLOCKING — WCAG 2.1 AA per D-03). Then run `makeAxeBuilderReportOnly().analyze()` and `console.warn` any wcag22aa violations WITHOUT failing (REPORT-ONLY — target-size per D-03).
      - Add one `test.describe` at `viewport {width:360,height:812}` asserting: `nav[aria-label="Mobile navigation"]` is visible on /dashboard; clicking the "More" button (`getByRole('button', { name: /more/i })`) opens a dialog with aria-label "More navigation" (the vaul more-sheet). This covers the UX-07-02 bottom-nav presence + sheet-opens behavior.
    Create `frontend/e2e/smoke.spec.ts` (this file is matched by the 3 smoke projects, so it runs in Chromium + WebKit + Firefox):
      - Smoke routes: /login, /dashboard, /dashboard/vulnerabilities, + one detail page (discover an asset id). For each: `page.goto`, assert the page renders (a known landmark/heading visible) and a quick `makeAxeBuilder().analyze()` has zero critical/serious.
      - Severity-glyph presence: on /dashboard/vulnerabilities, assert at least one severity glyph from `■ ▲ ◆ ○ □` is present in the DOM text (legibility at 14px on real Safari DPR is the MANUAL Safari.app item — automated check only asserts presence/rendering, not legibility).
      - Theme assertion (UX-07-05 automatable portion): `await page.emulateMedia({ colorScheme: 'dark' })`, clear localStorage, `page.goto('/login')`, assert `await page.locator('html').getAttribute('data-theme')` === 'dark'. Add a `colorScheme: 'light'` case asserting 'light'. (The pre-paint white-flash check stays MANUAL — Plan 06 HUMAN-UAT.)
  </action>
  <verify>
    <automated>cd frontend && grep -q "makeAxeBuilder" e2e/a11y-routes.spec.ts && grep -q "More navigation" e2e/a11y-routes.spec.ts && grep -q "data-theme" e2e/smoke.spec.ts && grep -Eq "■|▲|◆|○|□" e2e/smoke.spec.ts && npx playwright test --list --config=e2e/playwright.config.ts 2>&1 | grep -Eq "a11y-routes|smoke" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/e2e/a11y-routes.spec.ts` imports `makeAxeBuilder` from './fixtures/axe', filters `critical`/`serious`, and asserts `toHaveLength(0)`
    - `frontend/e2e/a11y-routes.spec.ts` contains a 360px-viewport block asserting `nav[aria-label="Mobile navigation"]` visible and the "More navigation" sheet opens
    - `frontend/e2e/a11y-routes.spec.ts` calls `makeAxeBuilderReportOnly` and uses `console.warn` (report-only, does NOT expect/fail on wcag22aa)
    - `frontend/e2e/smoke.spec.ts` contains a `data-theme` assertion and at least one of the severity glyphs `■ ▲ ◆ ○ □`
    - `cd frontend && npx playwright test --list --config=e2e/playwright.config.ts` lists a11y-routes + smoke (the smoke spec appears under chromium-smoke, webkit-smoke, firefox-smoke projects)
  </acceptance_criteria>
  <done>a11y-routes sweeps every route for blocking 2.1 AA + report-only 2.2 AA + bottom-nav/More-sheet presence; smoke runs across 3 engines with glyph-presence + data-theme assertions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the full Playwright suite against a live backend and close the audit-fix loop</name>
  <what-built>
    Four Playwright specs (viewport-scroll, a11y-routes, smoke, reduced-motion) plus the Wave 0 harness and the Wave 1 mobile-nav + modal conversions. The agent has driven the suite as far as it can in the sandbox, but the auth fixture needs a live backend (POST /auth/login) and the dev server on port 3000, which require the human to start docker-compose.
  </what-built>
  <how-to-verify>
    1. From the repo root: `docker-compose up -d postgres redis backend frontend` (the default admin admin@getvul.local / Admin123! must exist — created by backend/create_admin.py).
    2. Ensure the frontend is served on http://localhost:3000 (the compose `frontend` service, or `cd frontend && npm run build && npm run start`).
    3. `cd frontend && npx playwright test --config=e2e/playwright.config.ts`.
    4. Expected: setup project authenticates (writes e2e/.auth/state.json); viewport-scroll, a11y-routes, reduced-motion pass on chromium-a11y; smoke passes on chromium-smoke + webkit-smoke + firefox-smoke. Zero critical/serious axe violations; zero horizontal-scroll failures.
    5. If any spec fails: per D-09 fix the underlying UI defect (NOT the test) — re-run until green. Only a defect requiring a backend change may be deferred (log it to a v1.x backlog note with a risk statement). Report the final pass count and any deferred items.
  </how-to-verify>
  <resume-signal>Type "approved" when `npx playwright test` is green across all projects (or describe remaining failures / backend-deferred items)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → backend | Specs run authenticated via the storageState JWT from Plan 01; they only read routes, no mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Information Disclosure | Playwright HTML report / failure traces embedding auth state | mitigate | Plan 01 gitignored test-results/, playwright-report/, blob-report/; the report is a local artifact only, not committed |
| T-15-10 | Information Disclosure | console.error/console.warn dumping axe results | accept | axe results contain DOM selectors and rule ids, not secrets; printed to local test output only |
</threat_model>

<verification>
- `cd frontend && npx playwright test --list` lists all 4 specs across the correct projects
- Against a live backend: full suite green (viewport-scroll + a11y-routes + reduced-motion on chromium-a11y; smoke on 3 engines)
- Zero critical/serious axe violations; zero horizontal-scroll failures; WCAG 2.2 report-only warnings logged not failed
- Audit-fix loop closed (D-09): defect list empty or backend-deferred items logged
</verification>

<success_criteria>
- UX-07-01: no horizontal scroll, all routes × 360/390/768/1280
- UX-07-02: bottom-nav visible at 360px, "More" sheet opens
- UX-07-03: zero critical/serious axe (WCAG 2.1 AA), 2.2 AA report-only
- UX-07-04: reduced-motion emulated → near-zero animation duration
- UX-07-05 (automatable part): data-theme reflects emulated prefers-color-scheme
- UX-07-07: smoke green in Chromium/WebKit/Firefox; severity glyphs present
</success_criteria>

<output>
After completion, create `.planning/phases/15-mobile-a11y-perf-quality-gate/15-05-SUMMARY.md`
</output>
