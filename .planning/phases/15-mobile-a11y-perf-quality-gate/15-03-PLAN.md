---
phase: 15-mobile-a11y-perf-quality-gate
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - frontend/src/components/ui/responsive-dialog.tsx
  - frontend/src/components/ui/ConfirmModal.tsx
  - frontend/src/app/(authed)/dashboard/connectors/page.tsx
  - frontend/src/app/login/page.tsx
  - frontend/src/components/dashboard/hero.tsx
autonomous: true
requirements: [UX-07-02, UX-07-04]

must_haves:
  truths:
    - "On mobile (<768px) the ConfirmModal renders as a vaul bottom sheet; on desktop it renders the existing centered modal"
    - "On mobile (<768px) the connector credential form renders as a vaul bottom sheet; on desktop the existing centered dialog"
    - "With prefers-reduced-motion: reduce, the login gradient-mesh drift does not animate"
    - "With prefers-reduced-motion: reduce, the dashboard hero urgency dot does not pulse"
    - "Existing ConfirmModal call sites (connectors, settings, workspace-pane, ticket-bulk-bar, drill-content) keep working unchanged"
  artifacts:
    - path: "frontend/src/components/ui/responsive-dialog.tsx"
      provides: "Responsive Dialog wrapper — desktop centered modal / mobile vaul bottom sheet"
      contains: "useMediaQuery"
      min_lines: 35
    - path: "frontend/src/components/ui/ConfirmModal.tsx"
      provides: "ConfirmModal restructured to use ResponsiveDialog shell (bottom sheet on mobile)"
      contains: "ResponsiveDialog"
  key_links:
    - from: "frontend/src/components/ui/ConfirmModal.tsx"
      to: "frontend/src/components/ui/responsive-dialog.tsx"
      via: "wraps content in ResponsiveDialog"
      pattern: "ResponsiveDialog"
    - from: "frontend/src/app/login/page.tsx"
      to: "globals.css reduced-motion"
      via: "motion-safe: prefix on gradient-drift"
      pattern: "motion-safe:animate-gradient-drift"
    - from: "frontend/src/components/dashboard/hero.tsx"
      to: "globals.css reduced-motion"
      via: "motion-safe: prefix on urgency dot pulse"
      pattern: "motion-safe:animate-pulse"
---

<objective>
Two bounded changes. (1) D-07 — convert app modals to vaul bottom sheets on mobile by introducing a reusable `ResponsiveDialog` wrapper (desktop centered modal / mobile bottom sheet) and routing `ConfirmModal` + the connector credential form through it. (2) D-11 — fix the two CONCRETE reduced-motion gaps the research audit found (login gradient-drift and hero urgency dot both lack a `motion-safe:` guard) and confirm the remaining motion sites.

Research finding #4 narrows D-07 scope: the actual modal surface is `ConfirmModal` (one component used in 5 call sites — converting it once fixes all 5) plus the connector credential form (inline `role="dialog"` div with a custom focus trap). `reassign-owner` is an inline combobox, NOT a modal — DO NOT convert it.

Purpose: Satisfies UX-07-02 (modals → bottom sheets on mobile) and UX-07-04 (reduced-motion honored at every site).

Output: `responsive-dialog.tsx`, refactored `ConfirmModal.tsx`, converted connector form, and two `motion-safe:` fixes.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-mobile-a11y-perf-quality-gate/15-CONTEXT.md
@.planning/phases/15-mobile-a11y-perf-quality-gate/15-RESEARCH.md

MANDATORY design contract (per CLAUDE.md):
@.claude/skills/sketch-findings-getvul/references/foundation.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md
@.claude/skills/sketch-findings-getvul/references/interaction-patterns.md

<interfaces>
<!-- ConfirmModal current API (src/components/ui/ConfirmModal.tsx, DEFAULT export):
     { open, title, message, confirmLabel='Confirm', cancelLabel='Cancel', variant='danger'|'warning'|'info', onConfirm, onCancel }
     Internals: custom focus trap (getFocusable + trapTabKey from './focus-trap'), Esc handler, focuses confirm button on open.
     Desktop shell: outer fixed inset-0 z-50 flex items-center justify-center bg-surface/80 backdrop-blur-sm (onClick=onCancel),
       inner role="dialog" aria-modal aria-labelledby mx-4 w-full max-w-md rounded-xl border border-border-subtle bg-surface-2 p-6 shadow-2xl (onClick stopPropagation).
     btnColor: danger→bg-severity-critical, warning→bg-amber, info→bg-violet -->
<!-- ConfirmModal call sites (must keep working, prop API UNCHANGED):
     connectors/page.tsx, settings/page.tsx, components/settings/workspace-pane.tsx,
     components/tickets/ticket-bulk-bar.tsx, components/vulnerabilities/drill-content.tsx -->
<!-- Connector credential form: inline role="dialog" div in connectors/page.tsx, refs formDialogRef + formTitleId, custom trapTabKey + Esc -->
<!-- useMediaQuery(query) from '@/hooks/use-media-query' — SSR-safe boolean, false on server. Use '(max-width: 767px)' for the mobile branch. -->
<!-- vaul precedent (drill-panel-mobile.tsx): Drawer.Root direction="bottom" > Portal > Overlay (fixed inset-0 z-[9000] bg-bg-darker/60) > Content (fixed inset-x-0 bottom-0 z-[9001] rounded-t-lg border-t border-border-subtle bg-surface) + Drawer.Title sr-only. vaul handles focus trap + Esc internally; the custom focus-trap code is NOT needed in the mobile branch. -->
<!-- Reduced-motion gap sites (VERIFIED line numbers):
     login/page.tsx:110 — className="absolute inset-0 bg-gradient-mesh opacity-80 animate-gradient-drift" → change to motion-safe:animate-gradient-drift
     hero.tsx:116 — className="block h-2 w-2 rounded-full bg-severity-critical animate-pulse" (urgency dot) → change to motion-safe:animate-pulse
     hero.tsx:28 — animate-pulse skeleton (transient loading; acceptable via globals.css blanket; converting is optional/harmless) -->
<!-- globals.css already has a prefers-reduced-motion blanket (animation-duration: 0.01ms !important). The motion-safe: prefix is belt-and-suspenders per D-11. Do NOT add new !important anywhere (UX-F-02). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ResponsiveDialog wrapper and route ConfirmModal through it</name>
  <read_first>
    - frontend/src/components/ui/ConfirmModal.tsx (current full implementation — preserve the public prop API exactly)
    - frontend/src/components/vulnerabilities/drill-panel-mobile.tsx (vaul Drawer pattern to replicate)
    - frontend/src/hooks/use-media-query.ts (SSR-safe hook)
    - frontend/src/components/ui/focus-trap.ts (getFocusable, trapTabKey — kept for desktop branch)
    - frontend/src/app/(authed)/dashboard/settings/page.test.tsx (references ConfirmModal — its role="dialog" assertions must still pass on the jsdom/desktop path)
    - .claude/skills/sketch-findings-getvul/references/interaction-patterns.md (bottom-sheet treatment)
  </read_first>
  <action>
    Create `frontend/src/components/ui/responsive-dialog.tsx` ('use client'):
      - Props: `{ open: boolean; onOpenChange: (open: boolean) => void; ariaLabel?: string; ariaLabelledBy?: string; children: React.ReactNode }`.
      - `const isMobile = useMediaQuery('(max-width: 767px)');`
      - When `!open` return null (matches drill-panel-mobile guard — no lingering dialog role when closed; preserves the `queryByRole('dialog')===null` contract).
      - Mobile branch (isMobile true): vaul `Drawer.Root open onOpenChange direction="bottom"` > `Drawer.Portal` > `Drawer.Overlay` className `fixed inset-0 z-[9000] bg-bg-darker/60` > `Drawer.Content` className `fixed inset-x-0 bottom-0 z-[9001] max-h-[85dvh] overflow-y-auto rounded-t-lg border-t border-border-subtle bg-surface` aria-label={ariaLabel} > `Drawer.Title` sr-only {ariaLabel} + {children}. vaul handles focus trap + Esc — do NOT add a custom trap in this branch.
      - Desktop branch (isMobile false): the existing centered-modal shell — outer `<div className="fixed inset-0 z-[9000] flex items-center justify-center bg-surface/80 backdrop-blur-sm" onClick={() => onOpenChange(false)}>` containing inner `<div role="dialog" aria-modal="true" aria-label={ariaLabel} aria-labelledby={ariaLabelledBy} className="mx-4 w-full max-w-md rounded-xl border border-border-subtle bg-surface-2 p-6 shadow-2xl" onClick={(e)=>e.stopPropagation()}>{children}</div>`. The desktop branch MUST keep producing a `role="dialog"` node so existing tests pass (jsdom reports isMobile=false because useMediaQuery returns false on first render).
    Refactor `frontend/src/components/ui/ConfirmModal.tsx`:
      - Keep the DEFAULT export and the EXACT public prop API (open, title, message, confirmLabel, cancelLabel, variant, onConfirm, onCancel) — call sites must NOT change.
      - Replace the hand-rolled outer fixed/centered shell with `<ResponsiveDialog open={open} onOpenChange={(o)=>{ if(!o) onCancel(); }} ariaLabel={title}>`. Put the title `<h3>`, message `<p>`, and the Cancel/Confirm buttons inside as children, preserving the btnColor variant logic (danger→bg-severity-critical, warning→bg-amber, info→bg-violet) and confirm-button styling.
      - Keep the Esc-to-cancel keydown effect (harmless on both paths; vaul also provides Esc on mobile). Keep "focus confirm button on open" — guard it so it does not fight vaul on mobile (only run when not mobile, or accept vaul tolerates one programmatic focus; document the choice in a comment). Drop the manual trapTabKey loop from the OUTER shell — ResponsiveDialog's desktop branch keeps backdrop-click-to-close; if a desktop focus trap is still wanted, retain trapTabKey scoped to the desktop branch only. Document in a comment.
  </action>
  <verify>
    <automated>cd frontend && grep -q "useMediaQuery" src/components/ui/responsive-dialog.tsx && grep -q "Drawer.Root" src/components/ui/responsive-dialog.tsx && grep -q 'role="dialog"' src/components/ui/responsive-dialog.tsx && grep -q "ResponsiveDialog" src/components/ui/ConfirmModal.tsx && npx vitest run "src/app/(authed)/dashboard/settings" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ui/responsive-dialog.tsx` exists, contains `useMediaQuery('(max-width: 767px)')`, `Drawer.Root`, and a desktop `role="dialog"` node
    - `frontend/src/components/ui/ConfirmModal.tsx` contains `ResponsiveDialog` and still has the DEFAULT export with the same 8 props (open/title/message/confirmLabel/cancelLabel/variant/onConfirm/onCancel)
    - `grep -c "bg-severity-critical\|bg-amber\|bg-violet" frontend/src/components/ui/ConfirmModal.tsx` >= 3 (variant colors preserved)
    - Existing settings tests pass: `cd frontend && npx vitest run "src/app/(authed)/dashboard/settings"` exits 0
    - `cd frontend && npx tsc --noEmit` produces no new errors in responsive-dialog.tsx or ConfirmModal.tsx
  </acceptance_criteria>
  <done>ResponsiveDialog renders desktop centered modal / mobile vaul bottom sheet; ConfirmModal routes through it with an unchanged prop API; all 5 call sites work and existing tests stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the connector credential form to ResponsiveDialog</name>
  <read_first>
    - frontend/src/app/(authed)/dashboard/connectors/page.tsx (the inline role="dialog" credential form with formDialogRef/formTitleId/trapTabKey — convert this surface; leave the page's ConfirmModal usage alone, it is already converted via Task 1)
    - frontend/src/components/ui/responsive-dialog.tsx (created Task 1)
    - frontend/src/hooks/use-media-query.ts
    - .claude/skills/sketch-findings-getvul/references/interaction-patterns.md
    - .claude/skills/sketch-findings-getvul/references/state-patterns.md (the form may have loading/error states — preserve them)
  </read_first>
  <action>
    In `frontend/src/app/(authed)/dashboard/connectors/page.tsx`, replace the inline credential-form modal shell (the `role="dialog"` div guarded by `formDialogRef`/`formTitleId` with the custom `trapTabKey` + Esc handler) with `<ResponsiveDialog open={isFormOpen} onOpenChange={(o)=>{ if(!o) closeForm(); }} ariaLabelledBy={formTitleId}>`. Move ONLY the form chrome (outer fixed wrapper + the role="dialog" div + custom trapTabKey/Esc effect) into ResponsiveDialog; keep the form's fields, submit handler, validation, loading state, and error state EXACTLY as-is as children. The form title `<h2 id={formTitleId}>` stays inside as a child so aria-labelledby resolves. Remove the now-dead custom trapTabKey + Esc keydown effect that only guarded this form (vaul + the desktop ResponsiveDialog branch handle dismissal). Do NOT touch any other connector behavior (sync, delete-via-ConfirmModal, list rendering).
    DO NOT convert reassign-owner anywhere — research confirmed it is an inline combobox, not a modal.
  </action>
  <verify>
    <automated>cd frontend && grep -q "ResponsiveDialog" "src/app/(authed)/dashboard/connectors/page.tsx" && npx tsc --noEmit 2>&1 | grep "connectors/page" && echo HAS_ERRORS || echo OK; npx vitest run src/app 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/(authed)/dashboard/connectors/page.tsx` contains `ResponsiveDialog`
    - The custom credential-form `trapTabKey` loop guarding the inline dialog is removed (the form no longer hand-rolls its own focus trap; `grep -c "trapTabKey" "src/app/(authed)/dashboard/connectors/page.tsx"` is 0, OR any remaining trapTabKey is unrelated to the converted form)
    - `grep -c "reassign\|ReassignCombobox" "src/app/(authed)/dashboard/connectors/page.tsx"` does NOT increase (reassign-owner untouched — likely 0 here anyway)
    - `cd frontend && npx tsc --noEmit` produces no new errors in connectors/page.tsx
    - `cd frontend && npx vitest run src/app` exits 0
  </acceptance_criteria>
  <done>Connector credential form renders as a vaul bottom sheet on mobile / centered dialog on desktop via ResponsiveDialog; its fields/validation/states are unchanged; reassign-owner untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Fix the two reduced-motion gaps (login gradient-drift + hero urgency dot)</name>
  <read_first>
    - frontend/src/app/login/page.tsx (line ~110: animate-gradient-drift on the gradient-mesh div)
    - frontend/src/components/dashboard/hero.tsx (line ~116: animate-pulse on the urgency dot)
    - frontend/src/app/globals.css (confirm the prefers-reduced-motion blanket exists; do NOT add new !important)
    - .claude/skills/sketch-findings-getvul/references/foundation.md (reduce-motion substitutions)
  </read_first>
  <action>
    In `frontend/src/app/login/page.tsx`, change the gradient-mesh class from `animate-gradient-drift` to `motion-safe:animate-gradient-drift` (so the 24s drift loop only runs when the user has NOT requested reduced motion). Keep `bg-gradient-mesh opacity-80` and all other classes intact.
    In `frontend/src/components/dashboard/hero.tsx`, change the urgency-dot class (line ~116, the `bg-severity-critical animate-pulse` dot) from `animate-pulse` to `motion-safe:animate-pulse`. Keep `block h-2 w-2 rounded-full bg-severity-critical` intact. Leave the skeleton `animate-pulse` at line ~28 as-is (transient loading state, covered by the globals.css blanket — acceptable per the research audit) OR optionally convert it to motion-safe: too (harmless). Do NOT change any of the COVERED sites (trend-chart hook, skeleton-table motion-safe:animate-shimmer, sync-status-pill, cspm-status-pill, Toast motion-reduce:transition-none).
    Do NOT introduce any new `!important` (UX-F-02 contract — the only exception is the existing globals.css blanket).
  </action>
  <verify>
    <automated>cd frontend && grep -q "motion-safe:animate-gradient-drift" src/app/login/page.tsx && grep -c "motion-safe:animate-pulse" src/components/dashboard/hero.tsx && ! grep -E "[^-]animate-gradient-drift" src/app/login/page.tsx | grep -v "motion-safe" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "motion-safe:animate-gradient-drift" frontend/src/app/login/page.tsx` is 1
    - `frontend/src/app/login/page.tsx` has NO bare `animate-gradient-drift` without the `motion-safe:` prefix
    - `grep -c "motion-safe:animate-pulse" frontend/src/components/dashboard/hero.tsx` >= 1 (urgency dot fixed)
    - `frontend/src/components/dashboard/hero.tsx` urgency dot (the `bg-severity-critical` dot) uses `motion-safe:animate-pulse`, not bare `animate-pulse`
    - No new `!important` introduced: `git diff -- frontend/src/app/login/page.tsx frontend/src/components/dashboard/hero.tsx | grep "^+" | grep -c "!important"` is 0
  </acceptance_criteria>
  <done>Login gradient-drift and hero urgency dot both gated behind motion-safe:; prefers-reduced-motion now stops both; no new !important; covered sites untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → client | Pure client-side UI shell changes; no new server interaction. The connector credential form already submits to the backend (unchanged); only its container chrome changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Information Disclosure | Connector credential form (submits secrets) | accept | The form's submit handler, field masking, and transport are UNCHANGED by this plan — only the modal container swaps to ResponsiveDialog. No new exposure surface; the credential values never leave the existing handler. |
| T-15-07 | Tampering | ResponsiveDialog dismissal (backdrop/Esc) | mitigate | Both branches always provide a dismissal path (vaul Esc/overlay on mobile, backdrop click + Esc on desktop) so a half-open dialog cannot trap focus or block the form's own cancel handler. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean for the 5 files
- `cd frontend && npx vitest run src/app` and `src/components` green (existing ConfirmModal call sites unaffected)
- responsive-dialog.tsx has both a vaul mobile branch and a desktop role="dialog" branch
- login gradient-drift + hero urgency dot both use motion-safe:
- reassign-owner NOT converted
</verification>

<success_criteria>
- ResponsiveDialog: desktop centered modal / mobile vaul bottom sheet (UX-07-02)
- ConfirmModal (all 5 call sites) + connector credential form route through it; reassign-owner left as a combobox
- prefers-reduced-motion stops the login gradient-drift and the hero urgency-dot pulse (UX-07-04)
- No new !important; foundation tokens only; existing tests stay green
</success_criteria>

<output>
After completion, create `.planning/phases/15-mobile-a11y-perf-quality-gate/15-03-SUMMARY.md`
</output>
