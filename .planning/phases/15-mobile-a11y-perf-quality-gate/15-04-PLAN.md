---
phase: 15-mobile-a11y-perf-quality-gate
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - frontend/src/components/**
  - frontend/src/app/**
autonomous: true
requirements: [UX-07-03]

must_haves:
  truths:
    - "`npm run lint` exits 0 with zero jsx-a11y errors across the whole frontend"
    - "Interactive table/list rows that have onClick are keyboard-operable (tabIndex + onKeyDown or proper role)"
    - "No images/icons lack alt-text or aria-hidden as appropriate"
    - "Every label is associated with its control"
  artifacts:
    - path: "frontend/.eslintrc.json"
      provides: "jsx-a11y at error (created Plan 01) — this plan drives violations to zero"
      contains: "jsx-a11y/no-noninteractive-element-interactions"
  key_links:
    - from: "frontend/package.json"
      to: "frontend/.eslintrc.json"
      via: "npm run lint resolves the config and reports zero a11y errors"
      pattern: "next lint"
---

<objective>
Drive `eslint-plugin-jsx-a11y` to zero errors. Plan 01 created `.eslintrc.json` elevating ~11 jsx-a11y rules from `warn` to `error`; the research audit predicts ~20 violations clustered in table/list components (notably `jsx-a11y/no-noninteractive-element-interactions` on `<tr onClick>` and click handlers without keyboard equivalents). Per D-09, fix EVERYTHING the audit surfaces — no deferral of moderate items. The one exception: any defect that genuinely requires a backend change is logged to a v1.x backlog with a risk note (none expected here — these are pure markup/handler fixes).

This plan runs in Wave 2 (after the nav build and modal conversions land) so the lint pass covers the new `bottom-nav.tsx`, `nav-drawer.tsx`, `nav-more-sheet.tsx`, and `responsive-dialog.tsx` as well as the pre-existing components.

Purpose: Satisfies the `eslint-plugin-jsx-a11y` at-error half of UX-07-03.

Output: A clean `npm run lint` and whatever component edits were required to get there, recorded in the SUMMARY.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-mobile-a11y-perf-quality-gate/15-CONTEXT.md
@.planning/phases/15-mobile-a11y-perf-quality-gate/15-RESEARCH.md

MANDATORY design contract (per CLAUDE.md):
@.claude/skills/sketch-findings-getvul/references/interaction-patterns.md
@.claude/skills/sketch-findings-getvul/references/visual-language.md

<interfaces>
<!-- .eslintrc.json (Plan 01) elevates to error: jsx-a11y/alt-text, aria-props, aria-proptypes, aria-unsupported-elements,
     role-has-required-aria-props, role-supports-aria-props, anchor-is-valid, click-events-have-key-events,
     interactive-supports-focus, no-noninteractive-element-interactions, label-has-associated-control; no-autofocus at warn. -->
<!-- Research Pitfall 6: table rows in VulnTable / TicketsTable etc. already have tabIndex={0} + onKeyDown from Phase 11
     keyboard-nav — verify those satisfy no-noninteractive-element-interactions; if a <tr onClick> lacks an onKeyDown,
     add role="button" or tabIndex + onKeyDown (Enter/Space → same handler). -->
<!-- Correct fix patterns:
     - <tr onClick> needing keyboard: add tabIndex={0} + onKeyDown that fires the handler on Enter/Space, OR move onClick to a real <button> inside a cell.
     - decorative icon: aria-hidden. informative icon/img: alt or aria-label.
     - <label> without htmlFor: add htmlFor + matching id, OR wrap the control.
     - <a> with onClick but no real href: convert to <button>. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage the jsx-a11y violations and fix the non-table cluster</name>
  <read_first>
    - frontend/.eslintrc.json (the rule set now at error)
    - The output of `cd frontend && npm run lint` (run it first to get the actual violation list — do not assume; fix what is reported)
    - .claude/skills/sketch-findings-getvul/references/interaction-patterns.md (correct interactive patterns)
  </read_first>
  <action>
    Run `cd frontend && npm run lint` and capture the full violation list. Categorize each by rule. Fix the NON-table-row cluster first (these are usually quick and isolated):
      - `jsx-a11y/alt-text`: add `alt=""`/`alt="..."` to images, or `aria-hidden` to decorative lucide icons that lack it.
      - `jsx-a11y/anchor-is-valid`: convert `<a onClick>` without a valid href to `<button type="button">`.
      - `jsx-a11y/label-has-associated-control`: add `htmlFor`+`id` or wrap the control inside the label.
      - `jsx-a11y/aria-props` / `aria-proptypes` / `role-has-required-aria-props` / `role-supports-aria-props` / `aria-unsupported-elements`: correct the offending aria attribute or role.
      - `jsx-a11y/interactive-supports-focus`: add `tabIndex={0}` to interactive elements missing focusability.
    Apply the correct accessible pattern — do NOT silence with eslint-disable comments unless a rule is a documented false positive AND you record the justification in the SUMMARY. Use foundation.md tokens for any markup that gains a visible focus style; do not introduce freehand hex.
  </action>
  <verify>
    <automated>cd frontend && npm run lint 2>&1 | grep -Ec "jsx-a11y/(alt-text|anchor-is-valid|label-has-associated-control|aria-props|aria-proptypes|role-has-required-aria-props|role-supports-aria-props|aria-unsupported-elements|interactive-supports-focus)" | grep -q "^0$" && echo NON_TABLE_CLEAN || (npm run lint 2>&1 | grep "jsx-a11y" | head -30)</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run lint 2>&1 | grep -E "jsx-a11y/(alt-text|anchor-is-valid|label-has-associated-control|aria-props|aria-proptypes|role-has-required-aria-props|role-supports-aria-props|aria-unsupported-elements|interactive-supports-focus)"` returns NO lines (zero matches for these rules)
    - No `eslint-disable jsx-a11y` comments added without a recorded justification in the SUMMARY
  </acceptance_criteria>
  <done>The non-table jsx-a11y cluster (alt-text, anchor, label, aria-*, interactive-supports-focus) reports zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the interactive-element cluster and reach zero jsx-a11y errors</name>
  <read_first>
    - The remaining `cd frontend && npm run lint` output after Task 1 (the table/list `no-noninteractive-element-interactions` + `click-events-have-key-events` cluster)
    - frontend/src/components/vulnerabilities (VulnTable + row components — Phase 11 keyboard-nav reference for the correct tabIndex+onKeyDown pattern)
    - frontend/src/components/tickets (TicketsTable rows)
    - .claude/skills/sketch-findings-getvul/references/interaction-patterns.md (row drill-down interaction)
  </read_first>
  <action>
    Fix the interactive-element cluster reported by `npm run lint`:
      - `jsx-a11y/no-noninteractive-element-interactions` and `jsx-a11y/click-events-have-key-events`: for each `<tr onClick>` (or other non-interactive element with onClick) that lacks a keyboard equivalent, add `tabIndex={0}` + an `onKeyDown` that invokes the same handler on Enter and Space (preventDefault on Space to avoid scroll), and add an appropriate `role` (e.g., `role="button"` if the row acts as a button, or keep `role="row"` and move the click target to an in-cell `<button>`). Follow the EXISTING Phase 11 keyboard-nav pattern in the vuln table — many rows already have tabIndex+onKeyDown; if a row already satisfies the handler, the remaining error may need an explicit `role`. Match the established pattern, do not invent a new one.
      - `jsx-a11y/no-autofocus` is at `warn` (not error) — leave autofocus warnings unless trivially removable; they do not block.
    After fixing, run `npm run lint` again. The phase a11y bar (D-09) requires ZERO jsx-a11y errors across the entire frontend. If a violation can only be resolved by a backend change (none expected), log it to a v1.x backlog note in the SUMMARY with a risk statement instead of fixing.
    Re-run the existing unit/component test suite to confirm no behavior regressed from the markup changes.
  </action>
  <verify>
    <automated>cd frontend && npm run lint 2>&1 | grep -c "jsx-a11y" | grep -q "^0$" && echo A11Y_CLEAN || (echo "REMAINING:"; npm run lint 2>&1 | grep "jsx-a11y" | head -40); npx vitest run 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run lint` exits 0 (no ESLint errors) — `npm run lint 2>&1 | grep -c "jsx-a11y"` excluding `warn`-level lines is 0 for error-level violations
    - `cd frontend && npm run lint 2>&1 | grep -E "Error:.*jsx-a11y"` returns no lines
    - Interactive rows have keyboard equivalents: spot-check that `<tr` elements with `onClick` in vuln/ticket tables also have `onKeyDown` (or the click target moved to a `<button>`)
    - `cd frontend && npx vitest run` exits 0 (no test regression from markup changes)
  </acceptance_criteria>
  <done>`npm run lint` reports zero jsx-a11y errors; interactive rows are keyboard-operable; full unit/component suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → author | Static-analysis lint gate; no runtime trust boundary. Fixes are markup/handler-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Tampering | eslint-disable misuse | mitigate | Acceptance criterion forbids silencing jsx-a11y with eslint-disable unless a documented false positive is justified in the SUMMARY — prevents the gate being defeated by suppression instead of fixes. |
</threat_model>

<verification>
- `cd frontend && npm run lint` exits 0 with zero jsx-a11y errors
- `cd frontend && npx vitest run` green (no regression)
- No undocumented eslint-disable jsx-a11y suppressions
</verification>

<success_criteria>
- eslint-plugin-jsx-a11y at error level reports ZERO errors across the frontend (UX-07-03, D-09)
- Interactive rows keyboard-operable per the established Phase 11 pattern
- No behavior regressions; any backend-requiring defect logged to v1.x backlog (none expected)
</success_criteria>

<output>
After completion, create `.planning/phases/15-mobile-a11y-perf-quality-gate/15-04-SUMMARY.md`
</output>
