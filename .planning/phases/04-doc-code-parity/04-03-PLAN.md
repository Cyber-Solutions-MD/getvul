---
phase: 04-doc-code-parity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/config.py
  - backend/pyproject.toml
  - backend/tests/test_aws_removal.py
  - docs/05-configuration.md
  - docs/03-tech-stack.md
  - .env.example
autonomous: false
requirements: [PROD-04-05]
user_setup:
  - service: none
    why: "The root .env file is not git-tracked; removing its AWS lines is an operator step, not automatable via the repo."
    dashboard_config:
      - task: "Remove AWS_REGION and SECRETS_MANAGER_PREFIX lines from the live .env on the dev/prod VM"
        location: "Run in repo root: sed -i '/^AWS_REGION=/d; /^SECRETS_MANAGER_PREFIX=/d' .env (harmless no-op if config.py fields are already removed — pydantic ignores unknown env vars)"
must_haves:
  truths:
    - "boto3 is no longer a declared dependency and is not importable after reinstall"
    - "Settings no longer has aws_region or secrets_manager_prefix fields"
    - "No AWS Secrets Manager / boto3 reference survives anywhere in tracked source or docs"
  artifacts:
    - path: "backend/app/config.py"
      provides: "Settings without aws_region / secrets_manager_prefix"
      excludes: "aws_region, secrets_manager_prefix"
    - path: "backend/pyproject.toml"
      provides: "dependency list without boto3"
      excludes: "boto3"
    - path: "backend/tests/test_aws_removal.py"
      provides: "assertion that boto3 is not importable and Settings lacks the AWS fields"
      contains: "ModuleNotFoundError"
  key_links:
    - from: "pyproject.toml (no boto3)"
      to: "active .venv"
      via: "pip install -e \".[dev]\" flushes the old boto3 install"
      pattern: "import boto3.*ModuleNotFoundError"
---

<objective>
Resolve the dead AWS Secrets Manager configuration by removing it exhaustively (PROD-04-05, D-01/D-02): the two config fields, the boto3 dependency, and every doc/env reference. Nothing consumes these — the deploy model is single-VM and connector credentials use Fernet-in-`.env`.

Purpose: Kill dead config so the code, deps, and docs stop advertising a second secret backend that was never wired. This reaffirms Fernet-in-`.env` as the single credential story.
Output: config.py minus 2 fields, pyproject.toml minus boto3, reinstalled venv, scrubbed docs (05-configuration, 03-tech-stack), scrubbed .env.example, a removal-guard test, and a documented operator step for the untracked `.env`.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-doc-code-parity/04-CONTEXT.md
@.planning/phases/04-doc-code-parity/04-RESEARCH.md

<interfaces>
<!-- Verified 2026-07-02: exact reference set. infra/ grep returned ZERO hits. NO uv.lock exists. -->

Exact scrub set (all confirmed present):
- backend/app/config.py:38-39 → `aws_region: str = "us-east-1"` and `secrets_manager_prefix: str = "getvul/"`
- backend/pyproject.toml:17 → `"boto3>=1.35",`
- docs/05-configuration.md:84-85 → the `AWS_REGION` and `SECRETS_MANAGER_PREFIX` table rows (each references config.py:38/39 and says "Reserved for AWS Secrets Manager … Phase 4 will decide")
- docs/03-tech-stack.md:23 → `| boto3 | \`>=1.35\` | AWS SDK (Secrets Manager — declared, currently unused) |`
- .env.example:14-15 → `AWS_REGION=us-east-1` / `SECRETS_MANAGER_PREFIX=getvul/`
- .env:14-15 → NOT git-tracked → operator step (see user_setup)

Removal mechanic (D-02 corrected by RESEARCH): there is NO uv.lock. Edit pyproject.toml, then `cd backend && pip install -e ".[dev]"` to flush boto3 from the active .venv. Do NOT run `uv sync` / `uv lock` — uv is not installed.

Nothing in backend/app/ reads settings.aws_region or settings.secrets_manager_prefix (grep-confirmed — only config.py defines them).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing boto3/config removal-guard test</name>
  <files>backend/tests/test_aws_removal.py</files>
  <read_first>
    - backend/app/config.py (lines 30-45 — confirm the Settings class holds aws_region and secrets_manager_prefix; note the class name for import)
    - backend/tests/conftest.py (how Settings is imported/used in the suite, if at all)
    - .planning/phases/04-doc-code-parity/04-RESEARCH.md §"Pattern 5" + §"Pitfall 1" (no uv.lock) + §"Pitfall 2" (.env operator step)
  </read_first>
  <behavior>
    - Test `test_boto3_not_installed`: `import boto3` raises `ModuleNotFoundError`.
    - Test `test_settings_has_no_aws_fields`: the Settings model has no `aws_region` and no `secrets_manager_prefix` attribute/field.
    - Both MUST FAIL initially (boto3 still installed, fields still present) — RED.
  </behavior>
  <action>
    Create `backend/tests/test_aws_removal.py`. Import the Settings class the way the app does (confirm exact import path from config.py — e.g. `from app.config import Settings` or the settings instance). Write:

    ```python
    import pytest


    def test_boto3_not_installed():
        with pytest.raises(ModuleNotFoundError):
            import boto3  # noqa: F401


    def test_settings_has_no_aws_fields():
        from app.config import Settings

        field_names = set(Settings.model_fields.keys())  # pydantic v2
        assert "aws_region" not in field_names
        assert "secrets_manager_prefix" not in field_names
    ```

    If the project's pydantic version exposes fields differently, adapt the introspection (e.g. `Settings.__fields__` for pydantic v1) — but the assertions (no `aws_region`, no `secrets_manager_prefix`) are fixed. Run once — both tests MUST FAIL (RED) before Task 2. Commit: `test(04-03): add failing AWS/boto3 removal guard`.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_aws_removal.py -v 2>&1 | grep -E "FAILED|failed" && echo "RED confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_aws_removal.py` exists containing `ModuleNotFoundError`, `aws_region`, `secrets_manager_prefix`
    - Both tests FAIL before Task 2 (boto3 importable, fields present) — RED
  </acceptance_criteria>
  <done>Removal-guard test exists and fails against the current (unremoved) state.</done>
</task>

<task type="auto">
  <name>Task 2: Exhaustive AWS/boto3 removal (config + dep + reinstall + docs + .env.example)</name>
  <files>backend/app/config.py, backend/pyproject.toml, docs/05-configuration.md, docs/03-tech-stack.md, .env.example</files>
  <read_first>
    - backend/app/config.py (lines 36-41 — the two AWS field lines in context)
    - backend/pyproject.toml (lines 14-20 — the dependencies array around boto3 at line 17)
    - docs/05-configuration.md (lines 80-88 — the config table with the two AWS rows)
    - docs/03-tech-stack.md (lines 20-26 — the runtime table with the boto3 row)
    - .env.example (lines 12-16 — the AWS lines at 14-15)
  </read_first>
  <action>
    Remove EVERY reference in this exact set (D-02, D-01). Missing any one leaves a dangling reference (Pitfall — partial scrub).

    1. `backend/app/config.py:38-39` — delete both lines: `aws_region: str = "us-east-1"` and `secrets_manager_prefix: str = "getvul/"`. No tombstone comment (D-03 — clean removal). pydantic will then simply not read those env vars even if the live .env still has them.

    2. `backend/pyproject.toml:17` — delete the line `"boto3>=1.35",` from the dependencies array. Leave the surrounding array valid (no trailing comma issue / no dangling bracket).

    3. Reinstall to flush boto3 from the active venv:
       ```bash
       cd backend && pip install -e ".[dev]"
       ```
       There is NO uv.lock and NO uv (D-02 was corrected by RESEARCH). Do NOT run `uv sync` or `uv lock`.

    4. `docs/05-configuration.md:84-85` — delete the two table rows for `AWS_REGION` and `SECRETS_MANAGER_PREFIX` (the "Reserved for AWS Secrets Manager … Phase 4 will decide" rows). Ensure the table stays well-formed (header + separator + remaining rows intact).

    5. `docs/03-tech-stack.md:23` — delete the boto3 row `| boto3 | \`>=1.35\` | AWS SDK (Secrets Manager — declared, currently unused) |`. Keep the table well-formed.

    6. `.env.example:14-15` — delete `AWS_REGION=us-east-1` and `SECRETS_MANAGER_PREFIX=getvul/`.

    The live root `.env` (lines 14-15) is NOT git-tracked and is handled as an operator step (see the checkpoint task and user_setup) — do NOT attempt to git-edit it here.
  </action>
  <verify>
    <automated>cd backend && pip install -e ".[dev]" >/dev/null 2>&1; python -c "import boto3" 2>&1 | grep -q ModuleNotFoundError && pytest tests/test_aws_removal.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rniE "aws_region|secrets_manager_prefix|boto3" backend/app/ backend/pyproject.toml docs/ .env.example` returns ZERO matches
    - `grep -rniE "aws_region|secrets_manager_prefix|boto3" infra/` returns ZERO matches (re-confirm per D-04)
    - Repo-wide (excluding .planning/ and node_modules): `grep -rniE "aws_region|secrets_manager_prefix|boto3" --include="*.py" --include="*.toml" --include="*.md" --include="*.example" .` returns nothing outside .planning/
    - `cd backend && python -c "import boto3"` errors with `ModuleNotFoundError`
    - `pytest tests/test_aws_removal.py -v` PASSES (both tests GREEN)
    - `backend/pyproject.toml` is valid TOML (`python -c "import tomllib,pathlib; tomllib.loads(pathlib.Path('backend/pyproject.toml').read_text())"` exits 0)
    - No `uv sync` / `uv lock` command was run; no uv.lock created
  </acceptance_criteria>
  <done>All 6 tracked references gone, boto3 uninstalled and unimportable, docs/env.example scrubbed, removal-guard test green, no dangling reference repo-wide.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator — remove AWS lines from the untracked .env</name>
  <what-built>config.py no longer defines aws_region / secrets_manager_prefix, so pydantic already ignores those env vars whether or not they remain in the live .env. This step is hygiene: strip the now-orphaned lines from the untracked .env on the VM.</what-built>
  <read_first>
    - .planning/phases/04-doc-code-parity/04-RESEARCH.md §"Pitfall 2" (why this is manual + harmless)
  </read_first>
  <how-to-verify>
    1. From the repo root on the dev/prod VM, run:
       `sed -i '/^AWS_REGION=/d; /^SECRETS_MANAGER_PREFIX=/d' .env`
    2. Confirm the lines are gone:
       `grep -E "AWS_REGION|SECRETS_MANAGER_PREFIX" .env` should print nothing.
    3. If you lack write access to `.env`, this is a no-op for correctness — config.py no longer reads those vars (pydantic ignores unknown env keys). Note it in the resume signal.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms `grep -E "AWS_REGION|SECRETS_MANAGER_PREFIX" .env` returns nothing, OR confirms the .env edit was skipped as a harmless no-op (config.py fields already removed)
  </acceptance_criteria>
  <resume-signal>Type "done" once .env is scrubbed, or "skipped — no write access (harmless)" to proceed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → application config | Removing dead config/deps reduces attack surface (fewer packages, no advertised-but-unwired secret backend) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | dead AWS Secrets Manager config advertising a second secret backend | mitigate | Exhaustive removal (Task 2) eliminates config/docs that could mislead an operator into believing Secrets Manager is wired, and drops the boto3 SDK (~30MB transitive AWS surface) from the venv/image. |
| — | — | boto3 transitive supply-chain surface | accept→resolved | Removing boto3 also removes botocore/s3transfer; a smaller dependency tree is a supply-chain reduction, not a runtime control that needs ongoing verification beyond the removal-guard test. |
</threat_model>

<verification>
- `cd backend && python -c "import boto3"` → ModuleNotFoundError
- `cd backend && pytest tests/test_aws_removal.py -v` → both pass
- `grep -rniE "aws_region|secrets_manager_prefix|boto3" backend/app/ backend/pyproject.toml docs/ infra/ .env.example` → zero hits
- `backend/pyproject.toml` parses as valid TOML
- Full suite regression: `cd backend && pytest -v` green (Settings still constructs without the AWS fields; no code read them)
</verification>

<success_criteria>
- aws_region / secrets_manager_prefix config removed and boto3 dropped end-to-end (PROD-04-05, SC#5 "removed — no dead config")
- No dangling AWS/boto3 reference anywhere in tracked source or docs (D-02 exhaustive; infra/ re-confirmed clean per D-04)
- Removal-guard test rides the pytest gate; the untracked `.env` cleanup is documented as an operator step
</success_criteria>

<output>
After completion, create `.planning/phases/04-doc-code-parity/04-03-SUMMARY.md`
</output>
