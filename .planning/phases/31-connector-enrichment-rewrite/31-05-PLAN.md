---
phase: 31-connector-enrichment-rewrite
plan: 05
type: execute
wave: 3
depends_on: [01, 03, 04]
files_modified:
  - backend/app/connectors/wiz.py
  - backend/tests/test_connectors/test_wiz_connector.py
  - backend/tests/test_connector_normalization.py
autonomous: true
requirements: [ENRICH-03, ENRICH-04, ENRICH-06]
must_haves:
  truths:
    - "Wiz's native_priority_score and native_priority_rating are explicitly None (no vendor composite — Pitfall 6), while its richer EPSS/exploitability sub-scores land in source_signals (SC#3 null-case, ENRICH-04)"
    - "The Wiz GraphQL query addition is guarded so an unknown-field schema error falls back to the current field set instead of breaking the ENTIRE Wiz sync (A4, the phase's hardest-failure risk)"
    - "A single parametrized sweep proves ALL 6 connectors' _normalize_* set native_priority_score/native_priority_rating/source_signals on every emitted finding — None for Defender/Wiz intentionally (set explicitly, not by omission/crash), populated for the other 4 (ENRICH-06, SC#3)"
  artifacts:
    - path: "backend/app/connectors/wiz.py"
      provides: "GraphQL query gains epssSeverity/epssPercentile/epssProbability/exploitabilityScore/impactScore (guarded); source_signals allowlist; native_priority_* explicit None"
      contains: "source_signals"
    - path: "backend/tests/test_connector_normalization.py"
      provides: "parametrized ENRICH-06 sweep across all 6 connectors"
      contains: "native_priority"
  key_links:
    - from: "backend/app/connectors/wiz.py::VULNERABILITY_QUERY"
      to: "source_signals"
      via: "node.get(...) reads guarded by a schema-error fallback"
      pattern: "epssPercentile"
  prohibitions:
    - "Do NOT synthesize a composite native_priority_score for Wiz — it has no vendor-authored composite; leave it explicitly None (Pitfall 6, D-06)"
    - "Do NOT add the new GraphQL fields unguarded — GraphQL fails the ENTIRE query on an unknown field; wrap in a try/fallback-to-current-query-shape (A4)"
    - "Do NOT read the already-bool()-coerced hasCisaKevExploit for source_signals — read the raw node value / check key presence (Pitfall 2, D-07)"
    - "Do NOT duplicate promoted or PII-adjacent fields into source_signals (D-08/V8)"
---

<objective>
ENRICH-03/04/06 expansion (final connector) + the phase's cross-6 integrity sweep. Wiz has NO
vendor-authored composite priority signal (Pitfall 6), so native_priority_* stay explicitly None
and its genuinely-richer EPSS/exploitability sub-scores go to source_signals. Wiz is isolated in
its own plan because its GraphQL API fails the ENTIRE query on an unknown field (A4) — the phase's
hardest-failure risk — so the field addition needs a schema-error fallback. This plan also owns the
final parametrized ENRICH-06 sweep proving all 6 connectors set the 3 new fields.

Purpose: Complete all 6 connectors and prove the ENRICH-06 "never permanently null/inconsistent"
contract in one sweep. Runs after Plans 03/04 (the sweep needs the other 5 connectors done).
Output: enriched wiz.py + its connector test + the cross-6 normalization sweep.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-connector-enrichment-rewrite/31-RESEARCH.md
@.planning/phases/31-connector-enrichment-rewrite/31-PATTERNS.md
@.planning/phases/31-connector-enrichment-rewrite/31-01-SUMMARY.md
@.planning/phases/31-connector-enrichment-rewrite/31-03-SUMMARY.md
@.planning/phases/31-connector-enrichment-rewrite/31-04-SUMMARY.md

<interfaces>
<!-- NormalizedVulnerability carries native_priority_score/native_priority_rating/source_signals (Plan 01).
     Defender/CrowdStrike/Nessus/Qualys/Rapid7 already thread them (Plans 01/03/04). -->

Wiz (wiz.py:23-65 VULNERABILITY_QUERY, 266-294 fetch_vulnerabilities): add
epssSeverity/epssPercentile/epssProbability/exploitabilityScore/impactScore to the nodes { ... }
block (after hasCisaKevExploit) — GUARDED (A4): try the enriched query; on a GraphQL schema error
fall back to the current field set. Read the new fields off `node` into source_signals. native_priority_score
= None, native_priority_rating = None (explicit — Pitfall 6). For any KEV-ish provenance in source_signals,
read the RAW node value (node.get("hasCisaKevExploit") pre-bool() / key-presence), not the coerced field
(Pitfall 2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wiz — guarded GraphQL sub-score fields → source_signals, native explicit None (RED→GREEN)</name>
  <read_first>
    - backend/app/connectors/wiz.py:23-65 (VULNERABILITY_QUERY) + 266-294 (fetch_vulnerabilities node loop; hasCisaKevExploit bool() coercion at ~283 — Pitfall 2)
    - backend/tests/test_connectors/test_wiz_connector.py (extend; _install_mock_transport; simulate BOTH a successful enriched response AND a GraphQL schema-error response to exercise the fallback)
    - 31-RESEARCH.md Per-Connector table (Wiz row) + Pitfall 6 + Pitfall 2 + Assumptions Log A4 (field names unverified, GraphQL all-or-nothing → guard required)
    - 31-PATTERNS.md §wiz.py (HIGH-RISK PITFALL note)
  </read_first>
  <behavior>
    - Successful enriched query: each node's epssSeverity/epssPercentile/epssProbability/exploitabilityScore/impactScore land in source_signals (raw keys, missing-vs-negative); native_priority_score and native_priority_rating are None.
    - GraphQL schema-error on the enriched query: the code falls back to the current field set and Wiz sync still returns findings (does NOT raise / does NOT drop all findings) — native_priority_* None, source_signals limited to whatever base fields exist.
    - source_signals reads the RAW node values; no PII/promoted keys.
  </behavior>
  <action>
    Extend test_wiz_connector.py with the RED assertions (enriched-success + schema-error-fallback). In wiz.py define the enriched query (add the 5 fields after hasCisaKevExploit) and wrap the fetch so a GraphQL schema error retries with the current/base field set (try enriched → on schema error, fall back). In the node loop set native_priority_score=None, native_priority_rating=None explicitly, and build source_signals inline from raw node reads (the 5 sub-scores + any KEV-ish provenance read pre-bool(), missing-vs-negative, exclude promoted/PII). Comment the A4 assumption + the guard rationale.
  </action>
  <acceptance_criteria>
    - Completion state (measurable): Wiz native_priority_* explicitly None; sub-scores in source_signals via raw reads; an unknown-field GraphQL error falls back gracefully instead of breaking the whole Wiz sync.
    - Automated check passes: `ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret python -m pytest backend/tests/test_connectors/test_wiz_connector.py -x`
  </acceptance_criteria>

  <verify>
    <automated>ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret python -m pytest backend/tests/test_connectors/test_wiz_connector.py -x</automated>
  </verify>
  <done>Wiz native_priority_* explicitly None; sub-scores in source_signals via raw reads; an unknown-field GraphQL error falls back gracefully instead of breaking the whole Wiz sync.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cross-6 ENRICH-06 normalization sweep</name>
  <read_first>
    - backend/tests/test_connector_normalization.py:83-122 (per-connector pure-transform assert style; the existing per-connector coverage-floor pattern to extend)
    - 31-01-SUMMARY.md / 31-03-SUMMARY.md / 31-04-SUMMARY.md (the exact per-connector fixture shapes + field paths each plan used — reuse them so the sweep matches reality)
    - 31-VALIDATION.md Per-Task Verification Map (ENRICH-06 row: parametrized ×6)
  </read_first>
  <behavior>
    - For each of the 6 connectors, a representative _normalize_* call returns finding(s) where native_priority_score, native_priority_rating, and source_signals are all SET attributes (present on the dataclass) — populated for CrowdStrike/Nessus/Qualys/Rapid7, and explicitly None (not AttributeError) for Defender/Wiz.
    - The sweep asserts source_signals is a dict (or None) and contains no PII-adjacent keys for any connector.
  </behavior>
  <action>
    Add a parametrized test (one param per connector, reusing each SUMMARY's fixture shape) to test_connector_normalization.py asserting all 3 new fields are set on every emitted finding, with the None-is-intentional distinction for Defender/Wiz. This is the ENRICH-06 "never permanently null/inconsistent (and null-by-design where legitimate)" proof.
  </action>
  <acceptance_criteria>
    - Completion state (measurable): All 6 connectors proven to set the 3 new dataclass fields; Defender/Wiz None is explicit-by-design, the other 4 populated.
    - Automated check passes: `ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret python -m pytest backend/tests/test_connector_normalization.py -x`
  </acceptance_criteria>

  <verify>
    <automated>ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret python -m pytest backend/tests/test_connector_normalization.py -x</automated>
  </verify>
  <done>All 6 connectors proven to set the 3 new dataclass fields; Defender/Wiz None is explicit-by-design, the other 4 populated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wiz GraphQL response → source_signals | Untrusted scanner-returned node fields cross into persisted JSONB |
| enriched GraphQL query → Wiz API | An incorrect field name can break the entire query (self-inflicted correctness risk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06 | Tampering (self-inflicted) | wiz.py enriched VULNERABILITY_QUERY | mitigate | GraphQL fails the whole query on an unknown field (A4) — wrap the enriched query in a try/fallback-to-current-field-set so a wrong field name degrades to the existing behavior instead of dropping all Wiz findings. Test exercises the schema-error path. |
| T-31-03 | Information Disclosure | wiz.py source_signals allowlist | mitigate | Curated allowlist of raw node keys; exclude hostname/ip_addresses/last_login_user/serial_number and promoted columns (D-08/V8); sweep asserts no PII keys across all 6. |
</threat_model>

<verification>
- Wiz connector test + cross-6 normalization sweep green.
- Regression: `... python -m pytest backend/tests/test_connectors/test_crowdstrike_connector.py backend/tests/test_connectors/test_nessus_connector.py backend/tests/test_connectors/test_qualys_connector.py backend/tests/test_connectors/test_rapid7_connector.py backend/tests/test_connectors/test_defender_connector.py -x` all green (per-file).
- A4 (Wiz field names) unverified — the guard makes a wrong guess degrade gracefully; flag in SUMMARY for empirical re-verify against Wiz's GraphQL schema (introspection).
</verification>

<success_criteria>
- SC#3 null-case: Wiz native_priority_* explicitly None with richer data in source_signals.
- ENRICH-06: all 6 connectors thread the 3 new fields, proven in one parametrized sweep.
</success_criteria>

<output>
After completion, create `.planning/phases/31-connector-enrichment-rewrite/31-05-SUMMARY.md`.
Record the Wiz field-name guard behavior, the final Wiz source_signals allowlist keys, and the
cross-6 sweep result. Flag A4 as needing live GraphQL-schema re-verify.
</output>
