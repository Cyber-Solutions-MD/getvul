---
phase: 19-add-connector-wizard
plan: 01
type: execute
wave: 1
depends_on: [19-00]
files_modified:
  - frontend/src/components/connectors/wizard/wizard-stepper.tsx
  - frontend/src/components/connectors/wizard/credentials-step.tsx
  - frontend/src/components/connectors/wizard/test-step.tsx
autonomous: true
requirements: [UX-D-02-02, UX-D-02-03, UX-D-02-05]
must_haves:
  truths:
    - "The stepper shows all four steps with Provider complete (✓), current step gradient-filled, upcoming steps hollow — status readable in grayscale"
    - "The credentials step renders one input per field, secret fields as password with Eye/EyeOff, and delegates change to the parent hook (never sends ••••••)"
    - "The test step fires the connection test only on explicit button click; success shows a GREEN inline block, failure a RED block"
  artifacts:
    - path: "frontend/src/components/connectors/wizard/wizard-stepper.tsx"
      provides: "Display-only <ol> stepper with aria-current='step'"
      contains: "aria-current"
    - path: "frontend/src/components/connectors/wizard/credentials-step.tsx"
      provides: "Credential inputs + Eye toggle + sync-interval chips"
      contains: "data-eye-toggle"
    - path: "frontend/src/components/connectors/wizard/test-step.tsx"
      provides: "Explicit Test connection button + inline result block (green/red)"
      contains: "useTestConnector"
  key_links:
    - from: "test-step.tsx"
      to: "POST /connectors/test"
      via: "useTestConnector().mutate"
      pattern: "useTestConnector"
    - from: "credentials/test steps"
      to: "use-wizard-state.ts"
      via: "props from useWizardState"
      pattern: "updateField|isTestStale|buildCredentials"
---

<objective>
Build the three Wave-1 leaf components that render inside the wizard: the display-only `WizardStepper`, the `CredentialsStep` (credential inputs + sync-interval chips), and the `TestStep` (explicit test button + inline result). Each consumes the `useWizardState` contract from 19-00 via props; none owns its own step state.

Purpose: These are the visible surfaces of steps 2 and 3 plus the progress indicator. The stepper is net-new UI (no existing design-system pattern) built strictly from `foundation.md` tokens per the approved UI-SPEC.
Output: Three components; `credentials-step.test.tsx` goes GREEN.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-add-connector-wizard/19-UI-SPEC.md
@.claude/skills/sketch-findings-getvul/references/foundation.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md
@.claude/skills/sketch-findings-getvul/references/visual-language.md
@frontend/src/components/connectors/wizard/use-wizard-state.ts
@frontend/src/components/connectors/microcopy.ts

<interfaces>
Consume the 19-00 hook return shape (passed as props into these components):
`{ state, allFieldsFilled, isTestStale, testPassed, canAdvance, updateField, setTestResult, setSyncInterval, advance, back, buildCredentials }` from `useWizardState(fields)`.

Reuse verbatim from connector-form.tsx:
- `isSecretField(name)` name heuristic (lines 38-45): matches `secret|key|password|token`.
- `fieldLabel(name)` (lines 47-51): snake_case → Title Case.
- Sync-interval chip markup (lines 248-270): selected chip uses `border-violet/60 bg-violet/10 text-[var(--color-violet-on-soft)]`.
- Inline result block markup (lines 274-290) — REUSE STRUCTURE, CORRECT COLOR (see Color note).

Hook: `useTestConnector()` → `{ mutate, isPending }`; `mutate(body, { onSuccess, onError })`, body `{ connector_type, credentials }`. From use-connectors-admin.ts.
</interfaces>

**PLANNER DECISION (carried from 19-00):** `fields` is `string[]`. Use `isSecretField()` for password-vs-text; do NOT introduce field objects (no backend change — UX-D-02-05).

**⚠ COLOR RECONCILIATION (UI-SPEC §Color):** the test-PASS block MUST use `--color-success` green (`border-[var(--color-success)]/30 bg-[var(--color-success)]/10 text-[var(--color-success)]`), NOT the current lavender `severity-low`. Failure block stays `severity-critical` red. The source `connector-form.tsx` block is fixed separately in Plan 19-02.
</context>

<tasks>

<task type="auto">
  <name>Task 1: WizardStepper — display-only progress indicator (net-new)</name>
  <files>frontend/src/components/connectors/wizard/wizard-stepper.tsx</files>
  <read_first>
    - .planning/phases/19-add-connector-wizard/19-UI-SPEC.md (§Stepper Visual Spec + §Accessibility Contract "Stepper semantics")
    - .claude/skills/sketch-findings-getvul/references/foundation.md (--color-success, --gradient-sunset, --color-border-subtle, --radius-full, --space-6/--space-4, --motion-fast, --text-xs)
    - .planning/phases/19-add-connector-wizard/19-RESEARCH.md (Pattern 2 — <ol> + aria-current, NOT tablist)
  </read_first>
  <action>
    Create `WizardStepper` (net-new — header comment: `// NET-NEW pattern: no design-system stepper exists (interaction-patterns.md). Built from foundation.md tokens; promote to shared primitive only if a 2nd wizard appears (CONTEXT deferred).`).

    Props: `{ currentStep: 'credentials' | 'test' | 'confirm' }`. Derive the 4-item list from `WIZARD_COPY.stepLabels` (`['Provider','Credentials','Test','Confirm']`). Step 1 (Provider) is ALWAYS `complete`. For steps 2–4: status = `complete` if its index is before currentStep's index, `current` if equal, `upcoming` if after.

    Render `<nav aria-label="Wizard progress"><ol className="flex items-center gap-6 max-[767px]:gap-4">...</ol></nav>`. Each `<li>` is display-only (NOT a button/link — D-04, non-focusable) and contains:
    - a badge (`--radius-full` circle, `h-6 w-6 max-[767px]:h-5 max-[767px]:w-5`):
      - complete → `bg-[var(--color-success)]`, render lucide `CheckCircle2` glyph (NOT the number) + visually-hidden `<span className="sr-only"> (completed)</span>`
      - current → `style={{ background: 'var(--gradient-sunset)' }}` with white number; set `aria-current="step"` on the `<li>`
      - upcoming → `border border-border-subtle` transparent fill, `text-text-faint` number
    - a label (`text-xs font-medium`): complete → `text-text-muted`; current → `text-text font-semibold`; upcoming → `text-text-faint`.
    - a 1px connector rule between badges (`bg-border-subtle`); the segment behind completed steps tints `bg-[var(--color-success)]/40` so progress reads via fill extent, not hue alone.

    Status MUST be conveyed by shape+glyph+weight, NEVER color alone (UI-SPEC). Badge fill cross-fade wrapped in `motion-safe:transition-colors motion-safe:duration-[120ms]`; instant under reduced motion. No transforms, no pulsing.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "wizard-stepper" | grep -qx 0 && grep -q 'aria-current="step"' src/components/connectors/wizard/wizard-stepper.tsx && grep -q "(completed)" src/components/connectors/wizard/wizard-stepper.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '<nav aria-label="Wizard progress"' frontend/src/components/connectors/wizard/wizard-stepper.tsx`
    - `grep -q 'aria-current="step"' frontend/src/components/connectors/wizard/wizard-stepper.tsx`
    - `grep -q "(completed)" frontend/src/components/connectors/wizard/wizard-stepper.tsx` (grayscale-distinguishable completed marker)
    - `grep -q "CheckCircle2" frontend/src/components/connectors/wizard/wizard-stepper.tsx`
    - `grep -q "var(--gradient-sunset)" frontend/src/components/connectors/wizard/wizard-stepper.tsx` (current-badge fill)
    - NO interactive element inside the stepper: `grep -Eqv "<button|<a " frontend/src/components/connectors/wizard/wizard-stepper.tsx` (display-only, D-04)
    - `grep -q "motion-safe:" frontend/src/components/connectors/wizard/wizard-stepper.tsx`
    - No freehand hex: `grep -Eqv "#[0-9A-Fa-f]{6}" frontend/src/components/connectors/wizard/wizard-stepper.tsx`
    - `npx tsc --noEmit` reports no error referencing `wizard-stepper.tsx`
  </acceptance_criteria>
  <done>Display-only 4-step stepper renders complete/current/upcoming with grayscale-distinguishable markers, tokens only, no interactive elements.</done>
</task>

<task type="auto">
  <name>Task 2: CredentialsStep — inputs + Eye toggle + sync-interval chips</name>
  <files>frontend/src/components/connectors/wizard/credentials-step.tsx</files>
  <read_first>
    - frontend/src/components/connectors/connector-form.tsx (lines 38-51 isSecretField/fieldLabel; 199-270 field + sync-interval markup to lift)
    - frontend/src/components/connectors/wizard/use-wizard-state.ts (hook return shape)
    - frontend/src/components/connectors/wizard/credentials-step.test.tsx (the RED assertions this must satisfy)
    - .planning/phases/19-add-connector-wizard/19-UI-SPEC.md (§Typography, §Spacing, §Color, §Responsive touch targets)
  </read_first>
  <action>
    Create `CredentialsStep`. Props: `{ fields: string[]; values: Record<string,string>; onFieldChange: (name:string,value:string)=>void; syncInterval: number; onSyncIntervalChange: (m:number)=>void; headingRef?: React.Ref<HTMLHeadingElement>; headingId?: string }`.

    Render `<section aria-labelledby={headingId}>` containing:
    - `<h3 ref={headingRef} tabIndex={-1} id={headingId} className="text-base font-semibold text-text">Credentials</h3>` (focus target on step change; UI-SPEC A11y).
    - One field per `fields[]`, lifting the markup verbatim from connector-form.tsx lines 199-246: `<label htmlFor={`field-${field}`}>` + `<input id={`field-${field}`} name={field}>`. Use `isSecretField(field)` to choose `type="password"` (default) with an Eye/EyeOff toggle button carrying `data-eye-toggle data-field={field} aria-label={revealed ? 'Hide '+label : 'Show '+label}`. Keep a local `useState` for per-field reveal toggle (visual only). Placeholder: `Paste API token`. Input styling verbatim (`border-border-subtle bg-surface-2`, `focus:ring-2 focus:ring-violet/30`). Field stack uses `space-y-4`.
    - The sync-interval chip group verbatim from lines 248-270 (`SYNC_INTERVALS = [5,15,30,60]`, `{m===60?'1 hr':`${m} min`}`), driven by `syncInterval`/`onSyncIntervalChange`. Selected chip: `border-violet/60 bg-violet/10 text-[var(--color-violet-on-soft)]`.

    Field change calls `onFieldChange(field, value)` — the parent's `updateField` (which handles D-08 invalidation). Do NOT build credentials here; the hook owns `buildCredentials`. On mobile keep interactive controls ≥44px touch target.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/components/connectors/wizard/credentials-step --run 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- credentials-step --run` exits 0 (the 19-00 RED scaffold now GREEN)
    - `grep -q "data-eye-toggle" frontend/src/components/connectors/wizard/credentials-step.tsx`
    - `grep -q "isSecretField" frontend/src/components/connectors/wizard/credentials-step.tsx`
    - `grep -q "aria-labelledby" frontend/src/components/connectors/wizard/credentials-step.tsx` and `grep -q 'tabIndex={-1}' frontend/src/components/connectors/wizard/credentials-step.tsx` (focus-target heading)
    - `grep -q "var(--color-violet-on-soft)" frontend/src/components/connectors/wizard/credentials-step.tsx` (selected sync chip)
    - No freehand hex: `grep -Eqv "#[0-9A-Fa-f]{6}" frontend/src/components/connectors/wizard/credentials-step.tsx`
    - No sentinel in add mode: `grep -c "credentialSentinel\|••••••" frontend/src/components/connectors/wizard/credentials-step.tsx` returns 0
  </acceptance_criteria>
  <done>Credentials step renders inputs, Eye toggles, and sync chips; delegates state to the parent hook; credentials-step.test.tsx green.</done>
</task>

<task type="auto">
  <name>Task 3: TestStep — explicit test button + inline result (green/red, live regions)</name>
  <files>frontend/src/components/connectors/wizard/test-step.tsx</files>
  <read_first>
    - frontend/src/components/connectors/connector-form.tsx (lines 176-189 handleTest; 273-290 result block structure to reuse)
    - frontend/src/lib/queries/use-connectors-admin.ts (useTestConnector signature + TestConnectorResult)
    - .planning/phases/19-add-connector-wizard/19-UI-SPEC.md (§Copywriting test pass/fail, §Accessibility "Live-region announcements", §Color reconciliation)
  </read_first>
  <action>
    Create `TestStep`. Props: `{ connectorType: string; buildCredentials: () => Record<string,string>|undefined; testResult: {success:boolean;message:string}|null; onResult: (r:{success:boolean;message:string})=>void; headingRef?: React.Ref<HTMLHeadingElement>; headingId?: string }`.

    Render `<section aria-labelledby={headingId}>` with `<h3 ref={headingRef} tabIndex={-1} id={headingId} className="text-base font-semibold text-text">Test</h3>`.

    A bordered secondary `Test connection` button (D-06 — NEVER auto-fire on step entry; only on click). Use `useTestConnector()`. On click: `mutate({ connector_type: connectorType.toUpperCase(), credentials: buildCredentials() ?? {} }, { onSuccess: (data) => onResult(data), onError: (err) => onResult({ success:false, message: err.message }) })`. While `isPending`: show `Loader2` with `motion-safe:animate-spin` + `WIZARD_COPY.testingLabel` ('Testing…'); otherwise `WIZARD_COPY.testLabel`.

    Inline result block — REUSE the connector-form.tsx structure (icon + bordered block + message) with CORRECTED colors:
    - success (`testResult?.success === true`): `role="status" aria-live="polite"`, classes `border-[var(--color-success)]/30 bg-[var(--color-success)]/10 text-[var(--color-success)]`, `CheckCircle2` icon, text `✓ ` + `testResult.message`.
    - failure (`testResult && !testResult.success`): `role="alert"`, classes `border-severity-critical/30 bg-severity-critical/10 text-severity-critical`, `XCircle` icon, text `✗ ` + `testResult.message`.

    This step renders ONLY the raw test result. Gating (`Next` disable) and the stale/gate hints live in the wizard footer (19-03) — do not duplicate them here.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "test-step" | grep -qx 0 && grep -q "useTestConnector" src/components/connectors/wizard/test-step.tsx && grep -q "var(--color-success)" src/components/connectors/wizard/test-step.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "useTestConnector" frontend/src/components/connectors/wizard/test-step.tsx`
    - `grep -q 'role="status"' frontend/src/components/connectors/wizard/test-step.tsx` and `grep -q 'aria-live="polite"' frontend/src/components/connectors/wizard/test-step.tsx` (success announced)
    - `grep -q 'role="alert"' frontend/src/components/connectors/wizard/test-step.tsx` (failure announced)
    - `grep -q "var(--color-success)" frontend/src/components/connectors/wizard/test-step.tsx` (GREEN pass block — reconciliation)
    - `grep -q "severity-critical" frontend/src/components/connectors/wizard/test-step.tsx` (RED fail block)
    - `grep -q "motion-safe:animate-spin" frontend/src/components/connectors/wizard/test-step.tsx` (reduced-motion-safe spinner)
    - NO auto-fire: the `mutate` call appears only inside an `onClick` handler — `grep -q "onClick" frontend/src/components/connectors/wizard/test-step.tsx` and there is NO `useEffect` calling `mutate` (`grep -zoP 'useEffect[^}]*mutate' frontend/src/components/connectors/wizard/test-step.tsx` returns nothing)
    - No freehand hex: `grep -Eqv "#[0-9A-Fa-f]{6}" frontend/src/components/connectors/wizard/test-step.tsx`
    - `npx tsc --noEmit` reports no error referencing `test-step.tsx`
  </acceptance_criteria>
  <done>Test step fires only on explicit click (D-06), renders green success / red failure blocks with correct live-region roles, reduced-motion-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| credential inputs → POST /connectors/test | user-entered secrets cross to the backend test endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | Information Disclosure | credentials-step.tsx | mitigate | Add mode starts empty and never renders the `••••••` sentinel; the test call sends `buildCredentials()` (non-empty add-path only), so the sentinel literal cannot enter the test body (D-12). Acceptance criterion asserts zero sentinel occurrences. |
| T-19-03 | Information Disclosure | credentials-step.tsx | accept | Secret fields render as `type="password"` via `isSecretField()`; Eye toggle is explicit user action. Same DOM-exposure profile as the existing ConnectorForm; HTTPS + backend-at-rest encryption unchanged. No new surface. |
| T-19-01 | Tampering (of trust) | test-step.tsx | mitigate | Test fires only on explicit click (D-06) against the current `buildCredentials()`; the wizard footer (19-03) gates advancement on `testPassed`. Acceptance criterion asserts no `useEffect`-driven auto-fire. |
</threat_model>

<verification>
- `npm test -- credentials-step --run` green.
- `npx tsc --noEmit` clean for all three new files.
- Manual grep gates above all pass (stepper display-only, test-step no auto-fire, green success block).
</verification>

<success_criteria>
Stepper, credentials step, and test step exist as token-pure, a11y-correct leaf components consuming the 19-00 hook; the test step is tamper-evident (no auto-fire) and uses the reconciled green success color.
</success_criteria>

<output>
After completion, create `.planning/phases/19-add-connector-wizard/19-01-SUMMARY.md`
</output>
