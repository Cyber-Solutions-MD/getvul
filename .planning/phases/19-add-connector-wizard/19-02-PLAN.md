---
phase: 19-add-connector-wizard
plan: 02
type: execute
wave: 1
depends_on: [19-00]
files_modified:
  - frontend/src/components/connectors/wizard/confirm-step.tsx
  - frontend/src/components/connectors/connector-form.tsx
autonomous: true
requirements: [UX-D-02-04, UX-D-02-05]
must_haves:
  truths:
    - "The confirm step shows provider, ✓ Connection verified, the required scopes with their purpose, and the sync interval before submit"
    - "When a connector type has no permissions, the confirm step shows 'No special scopes required.' (not an error)"
    - "The primary 'Add connector' CTA submits POST /connectors via useCreateConnector with the add-path credentials + selected sync interval"
    - "The edit-mode ConnectorForm test-success block now renders GREEN (--color-success), matching the wizard"
  artifacts:
    - path: "frontend/src/components/connectors/wizard/confirm-step.tsx"
      provides: "Review screen: provider + connection ✓ + permissions[] scope list + sync chips + Add connector CTA"
      contains: "permissions"
  key_links:
    - from: "confirm-step.tsx"
      to: "POST /connectors"
      via: "useCreateConnector().mutate"
      pattern: "useCreateConnector"
    - from: "confirm-step.tsx"
      to: "connector type permissions[]"
      via: "props (scope/access/purpose)"
      pattern: "purpose"
---

<objective>
Build the `ConfirmStep` review screen (step 4) — the "here's what you're granting" summary that renders the required scopes from the connector type's `permissions[]` before the single `Add connector` CTA submits `POST /connectors`. Separately, reconcile the pre-existing test-success color drift in the edit-mode `ConnectorForm` (lavender → green) so both add (wizard) and edit paths agree on the design-system success token.

Purpose: UX-D-02-04 (show required scopes before submit) and UX-D-02-05 (reuse `POST /connectors`, no new backend). The confirm step is the wizard's payoff surface.
Output: `ConfirmStep` component (confirm-step.test.tsx GREEN) + one-line color fix in `connector-form.tsx`.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-add-connector-wizard/19-UI-SPEC.md
@.claude/skills/sketch-findings-getvul/references/foundation.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md
@frontend/src/components/connectors/microcopy.ts

<interfaces>
Permissions shape (from use-connectors-admin.ts, already on the wire):
```typescript
export type ConnectorTypePermission = { scope: string; access: string; purpose: string };
```
`useCreateConnector()` → `{ mutate, isPending }`; `mutate(body, { onSuccess, onError })`, body:
```typescript
{ connector_type: string; credentials: Record<string,string>; sync_interval_minutes?: number }
```
Reference the existing add-mode submit shape (connector-form.tsx handleSave lines 138-153): `connector_type: connectorType.toUpperCase()`, `credentials: credentials ?? {}`, `sync_interval_minutes: syncInterval`.

WIZARD_COPY (from 19-00): `confirmSectionProvider/Connection/Access/Sync`, `confirmConnectionOk` ('✓ Connection verified'), `noScopes` ('No special scopes required.'), `addLabel` ('Add connector').
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConfirmStep — review screen + required scopes + Add connector CTA</name>
  <files>frontend/src/components/connectors/wizard/confirm-step.tsx</files>
  <read_first>
    - frontend/src/components/connectors/wizard/confirm-step.test.tsx (the RED assertions this must satisfy — scope+purpose render, empty-permissions copy, mutate call)
    - frontend/src/components/connectors/connector-form.tsx (lines 138-153 handleSave add-path; 326-340 gradient CTA markup to reuse)
    - .planning/phases/19-add-connector-wizard/19-UI-SPEC.md (§Copywriting Confirm section labels, §Color accent-reserved-for-CTA, §Typography mono for scope strings)
    - .planning/phases/19-add-connector-wizard/19-CONTEXT.md (D-09, D-10)
  </read_first>
  <action>
    Create `ConfirmStep`. Props: `{ providerName: string; connectorType: string; permissions: ConnectorTypePermission[]; syncInterval: number; credentials: Record<string,string>|undefined; onSuccess: ()=>void; headingRef?: React.Ref<HTMLHeadingElement>; headingId?: string }`.

    Render `<section aria-labelledby={headingId}>` with `<h3 ref={headingRef} tabIndex={-1} id={headingId} className="text-base font-semibold text-text">Confirm</h3>` and four review sections (D-09), each a labeled row group using `--color-surface-2`/`--color-border-subtle` review rows:
    1. `Provider` (WIZARD_COPY.confirmSectionProvider) → `{providerName}`.
    2. `Connection` (confirmSectionConnection) → `WIZARD_COPY.confirmConnectionOk` ('✓ Connection verified') in `text-[var(--color-success)]`.
    3. `Required access` (confirmSectionAccess) → the `permissions[]` list (D-10). For each permission render a row: `{scope}` in `font-mono text-sm` (mono for value-shaped strings, copy-voice) followed by `{purpose}` as a `text-xs text-text-muted` caption sub-line. `access` may be shown as a small pill. If `permissions.length === 0`, render the single line `WIZARD_COPY.noScopes` ('No special scopes required.') — this is a VALID expected state, NOT an error (do not render a PartialFailureBanner or error styling).
    4. `Sync interval` (confirmSectionSync) → `{syncInterval === 60 ? '1 hr' : `${syncInterval} min`}` in `font-mono`.

    Primary CTA `Add connector` (WIZARD_COPY.addLabel), gradient (reuse connector-form.tsx lines 326-340 markup: `style={{ background: 'var(--gradient-sunset)' }}`, `shadow-[var(--glow-cta)]`, `text-white font-semibold`). This is the ONLY gradient element on screen (accent reserved for the single forward/primary CTA). On click, use `useCreateConnector()` and call `mutate({ connector_type: connectorType.toUpperCase(), credentials: credentials ?? {}, sync_interval_minutes: syncInterval }, { onSuccess: () => onSuccess(), onError: (err) => setFormError(err.message) })`. Render a submit-error block (reuse connector-form.tsx lines 292-297 red block) surfacing the API message verbatim when the POST fails. Disable the CTA while `isPending` and show `Loader2` (`motion-safe:animate-spin`). The CTA is NOT gated (D-05 — always enabled; step-3 gate already ensures tested creds).
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/components/connectors/wizard/confirm-step --run 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- confirm-step --run` exits 0 (19-00 RED scaffold now GREEN, including the empty-permissions case)
    - `grep -q "useCreateConnector" frontend/src/components/connectors/wizard/confirm-step.tsx`
    - `grep -q "No special scopes required.\|noScopes" frontend/src/components/connectors/wizard/confirm-step.tsx` (empty-permissions valid state)
    - `grep -q "font-mono" frontend/src/components/connectors/wizard/confirm-step.tsx` (scope strings + sync value in mono)
    - `grep -q "purpose" frontend/src/components/connectors/wizard/confirm-step.tsx` (D-10 scope purpose rendered)
    - `grep -q "var(--gradient-sunset)" frontend/src/components/connectors/wizard/confirm-step.tsx` and exactly one gradient CTA (single accent per screen)
    - `grep -q ".toUpperCase()" frontend/src/components/connectors/wizard/confirm-step.tsx` (connector_type uppercased — T-14-09 parity)
    - `grep -q "sync_interval_minutes" frontend/src/components/connectors/wizard/confirm-step.tsx`
    - No freehand hex: `grep -Eqv "#[0-9A-Fa-f]{6}" frontend/src/components/connectors/wizard/confirm-step.tsx`
  </acceptance_criteria>
  <done>Confirm step renders the full review (provider, connection ✓, scopes+purpose, sync), handles empty-permissions gracefully, and submits via useCreateConnector; confirm-step.test.tsx green.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the edit-mode ConnectorForm test-success color (lavender → green)</name>
  <files>frontend/src/components/connectors/connector-form.tsx</files>
  <read_first>
    - frontend/src/components/connectors/connector-form.tsx (lines 274-290 — the testResult block; the success branch uses `severity-low`)
    - .planning/phases/19-add-connector-wizard/19-UI-SPEC.md (§Color reconciliation note — recommend fixing the source form too)
    - .planning/phases/19-add-connector-wizard/19-RESEARCH.md (Pitfall 2 — the lavender-not-green drift)
  </read_first>
  <action>
    In `connector-form.tsx`, change the test-result SUCCESS branch (currently `'border-severity-low/30 bg-severity-low/10 text-severity-low'`, lines ~278-280) to the design-system success token: `'border-[var(--color-success)]/30 bg-[var(--color-success)]/10 text-[var(--color-success)]'`. Leave the FAILURE branch (`severity-critical`) unchanged — it is already correct. Do NOT touch any other behavior in this file (the edit path, sentinel logic, buildCredentials, handleSave are ALL preserved verbatim per D-11). This is a one-line color reconciliation so add (wizard) and edit (form) agree on success = green.

    Add a brief comment on the changed line: `// reconciled lavender→green success token (Phase 19, UI-SPEC §Color / RESEARCH Pitfall 2)`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/components/connectors/connector-form --run 2>&1 | tail -8 && grep -q "var(--color-success)" src/components/connectors/connector-form.tsx && ! grep -q "text-severity-low" src/components/connectors/connector-form.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- connector-form --run` exits 0 (all 5 existing edit/add tests still green — no regression to D-11 edit path)
    - `grep -q "var(--color-success)" frontend/src/components/connectors/connector-form.tsx` (success now green)
    - `grep -c "severity-low" frontend/src/components/connectors/connector-form.tsx` returns 0 (lavender gone)
    - `grep -q "severity-critical" frontend/src/components/connectors/connector-form.tsx` still succeeds (failure block untouched)
    - `grep -q "buildCredentials" frontend/src/components/connectors/connector-form.tsx` and `grep -q "credentialSentinel\|SENTINEL" frontend/src/components/connectors/connector-form.tsx` still succeed (edit path + sentinel preserved, D-11)
  </acceptance_criteria>
  <done>Edit-mode success block is green; the D-11 edit path and sentinel behavior are untouched; connector-form.test.tsx still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confirm step → POST /connectors | tested credentials + sync interval cross to persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering (of trust) | confirm-step.tsx | mitigate | The confirm step is only reachable after the step-3 `testPassed` gate (19-03); it submits the same `buildCredentials()` add-path that was tested. Reaching confirm with stale credentials is structurally prevented by the gate (RESEARCH Open Q2 — single enforcement point). |
| T-19-02 | Information Disclosure | confirm-step.tsx | mitigate | The review renders `permissions[]` (scope/access/purpose) and sync interval only — never the credential VALUES. Credentials are passed to `mutate` but never rendered in the review DOM. |
| T-19-04 | Information Disclosure | connector-form.tsx | accept | Color-only change; no logic, no credential-handling path touched. D-11 edit/sentinel path asserted unchanged by acceptance criteria. |
</threat_model>

<verification>
- `npm test -- confirm-step --run` green (incl. empty-permissions).
- `npm test -- connector-form --run` green (no edit-path regression).
- Grep gates: success green everywhere, no lavender remaining, sentinel/edit path intact.
</verification>

<success_criteria>
Confirm step surfaces required scopes + purpose before a single gradient Add-connector CTA that reuses POST /connectors; empty-permissions is a graceful valid state; both add and edit paths render success in the reconciled green token.
</success_criteria>

<output>
After completion, create `.planning/phases/19-add-connector-wizard/19-02-SUMMARY.md`
</output>
