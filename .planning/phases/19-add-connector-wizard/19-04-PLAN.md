---
phase: 19-add-connector-wizard
plan: 04
type: execute
wave: 3
depends_on: [19-03]
files_modified:
  - frontend/e2e/connector-wizard-a11y.spec.ts
autonomous: false
requirements: [UX-D-02-06]
must_haves:
  truths:
    - "With the add-connector wizard OPEN, /dashboard/connectors reports zero serious/critical axe violations in BOTH light and dark themes"
    - "/dashboard/connectors stays ≤250 KB First-Load JS after the wizard ships"
    - "The wizard renders and is operable inside the vaul mobile bottom sheet without regressing the fixed bottom-nav"
  artifacts:
    - path: "frontend/e2e/connector-wizard-a11y.spec.ts"
      provides: "E2E axe sweep of the OPEN wizard dialog in both themes + mobile-viewport render check"
      contains: "data-add-connector"
  key_links:
    - from: "connector-wizard-a11y.spec.ts"
      to: "the open wizard dialog"
      via: "click [data-add-connector] then axe analyze"
      pattern: "data-add-connector"
---

<objective>
Close the phase quality gate (UX-D-02-06). The existing `e2e/a11y-routes.spec.ts` sweep only exercises the CLOSED connectors grid (RESEARCH flagged this as a pre-existing coverage gap). Add a focused e2e spec that OPENS the wizard dialog and runs the axe WCAG 2.1 AA sweep in both light and dark themes, plus a mobile-viewport render check. Then run the full gate (unit suite, production build + bundle budget ≤250 KB, e2e axe both themes) and paste the evidence, and finish with a human-verify checkpoint for the genuinely-manual mobile/focus/reduced-motion feel that automation cannot assert.

Purpose: UX-D-02-06 — axe both themes + ≤250 KB + vaul mobile. This is the closing gate before `/gsd-verify-work`.
Output: New e2e spec + pasted gate evidence + a human-verify sign-off.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-add-connector-wizard/19-UI-SPEC.md
@.planning/phases/19-add-connector-wizard/19-RESEARCH.md
@frontend/e2e/a11y-routes.spec.ts
@frontend/e2e/routes.ts
@frontend/e2e/fixtures/axe.ts

<interfaces>
Local e2e + perf gate setup (from project memory — REQUIRED reading before running):
- e2e needs a prod build + running server, admin user, :3000 CORS; kill next-server children after; Firefox dark-pref quirks. See `.claude` project memory "Local e2e + perf gate setup".
- axe fixture: `import { test, expect } from './fixtures/axe'` → `makeAxeBuilder()` / `makeAxeBuilderReportOnly()`.
- Light-theme forcing pattern (a11y-routes.spec.ts lines ~80-100): `page.addInitScript(() => localStorage.setItem('getvul_theme','light'))` then defensive `document.documentElement.setAttribute('data-theme','light')` after goto.
- The wizard is opened by clicking a `[data-add-connector]` element (added in 19-03) on `/dashboard/connectors`.
- Bundle gate: `npm run build && npm run perf:budget` (wraps `scripts/check-bundle-all.mjs`); current /dashboard/connectors baseline = 153 kB, budget 250 kB.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E spec — axe-sweep the OPEN wizard in both themes + mobile render</name>
  <files>frontend/e2e/connector-wizard-a11y.spec.ts</files>
  <read_first>
    - frontend/e2e/a11y-routes.spec.ts (both-theme sweep structure + light-theme addInitScript forcing to mirror)
    - frontend/e2e/routes.ts (waitForNav, MOBILE_NAV selector, gotoStable)
    - frontend/e2e/fixtures/axe.ts (makeAxeBuilder fixture)
    - `.claude` project memory: "Local e2e + perf gate setup" (how to run locally)
  </read_first>
  <action>
    Create `e2e/connector-wizard-a11y.spec.ts` importing `{ test, expect } from './fixtures/axe'`. Three tests:

    1. **Dark theme, wizard open (blocking):** `await page.goto('/dashboard/connectors'); await waitForNav(page, 1280);` then click the first `[data-add-connector]` element (`await page.locator('[data-add-connector]').first().click()`). Wait for the dialog: `await page.getByRole('dialog').waitFor()` (desktop) — heading text matches `/Add connector · /`. Run `const results = await makeAxeBuilder().analyze();` filter `impact === 'critical' || 'serious'`; `expect(blocking, 'zero critical/serious axe on open wizard (dark)').toHaveLength(0)`. Log violation details on failure (mirror a11y-routes.spec.ts).

    2. **Light theme, wizard open (blocking):** `beforeEach` seeds `localStorage.getvul_theme='light'` via `addInitScript`; after goto, defensively force `data-theme="light"` on `<html>` and wait 50ms (mirror a11y-routes.spec.ts Pitfall 2). Open the wizard the same way; assert zero critical/serious.

    3. **Mobile vaul render (blocking):** `test.use({ viewport: { width: 390, height: 844 } })` (or set viewport in-test). `goto` + `waitForNav(page, 390)` (expects `nav[aria-label="Mobile navigation"]`). Click `[data-add-connector]`; assert the vaul `Drawer.Content` is visible (`await page.locator('[role="dialog"], .vaul-drawer, [data-vaul-drawer]').first().waitFor()` — use whatever selector the ResponsiveDialog mobile branch exposes; the sheet content contains the stepper `nav[aria-label="Wizard progress"]`). Assert `await expect(page.locator('nav[aria-label="Wizard progress"]')).toBeVisible();` AND the fixed bottom-nav is NOT broken (`await expect(page.locator('nav[aria-label="Mobile navigation"]')).toBeVisible();`). Run axe once on the mobile open state (blocking, zero critical/serious).

    Do NOT hardcode a provider type; rely on `[data-add-connector]` first match (a fresh test tenant has unconfigured types rendering "+ {name}" cards / empty-state CTAs).
  </action>
  <verify>
    <automated>cd frontend && npm run build >/tmp/build19.log 2>&1 && npm run test:e2e -- connector-wizard-a11y 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/e2e/connector-wizard-a11y.spec.ts` exists and `grep -q "data-add-connector" frontend/e2e/connector-wizard-a11y.spec.ts`
    - `grep -q "getvul_theme" frontend/e2e/connector-wizard-a11y.spec.ts` (light-theme forcing) AND `grep -q "makeAxeBuilder" frontend/e2e/connector-wizard-a11y.spec.ts`
    - `grep -q 'nav\[aria-label="Wizard progress"\]' frontend/e2e/connector-wizard-a11y.spec.ts` (asserts wizard rendered in the open dialog)
    - `grep -q "390\|Mobile navigation" frontend/e2e/connector-wizard-a11y.spec.ts` (mobile vaul check + bottom-nav preserved)
    - `npm run test:e2e -- connector-wizard-a11y` passes all three tests (zero critical/serious axe in dark AND light; wizard visible in vaul sheet); paste the run summary into the SUMMARY
  </acceptance_criteria>
  <done>An e2e spec proves the OPEN wizard is axe-clean in both themes and renders in the vaul mobile sheet without regressing the bottom-nav.</done>
</task>

<task type="auto">
  <name>Task 2: Full gate run with pasted evidence (unit + build + bundle ≤250 KB + e2e both themes)</name>
  <files>frontend/e2e/connector-wizard-a11y.spec.ts</files>
  <read_first>
    - .planning/phases/19-add-connector-wizard/19-VALIDATION.md (the per-task verification map + phase-gate commands)
    - `.claude` project memory: "Local e2e + perf gate setup" and "Axe sweep not run during execution" (AA claims are unproven unless the sweep actually ran — you MUST run it)
  </read_first>
  <action>
    Run the full phase gate and paste the ACTUAL output into `19-04-SUMMARY.md` (do not paraphrase — project memory warns that AA claims are treated as unproven unless the sweep genuinely ran):
    1. `npm test -- src/components/connectors --run` — full connectors unit suite (wizard + connector-form) green.
    2. `npm run lint` — clean.
    3. `npm run build` — succeeds; capture the `/dashboard/connectors` First-Load JS line from the build output.
    4. `npm run perf:budget` — passes; paste the connectors-route number and confirm ≤250 KB (baseline was 153 kB; report the delta introduced by the wizard).
    5. `npm run test:e2e -- a11y-routes connector-wizard-a11y` — the closed-route sweep (both themes) AND the new open-wizard sweep (both themes) both green; paste the pass/fail summary lines.

    If `perf:budget` fails, investigate the per-chunk breakdown BEFORE reaching for `next/dynamic({ssr:false})` (RESEARCH Pitfall 3 — 97 kB headroom; a regression is more likely than genuine budget exhaustion). Only apply the Phase-18 `next/dynamic` treatment to the wizard import if a real measurement shows the route approaching 250 kB, and record that decision in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/components/connectors --run 2>&1 | tail -6 && npm run build >/tmp/b19.log 2>&1 && npm run perf:budget 2>&1 | grep -i "connectors\|PASS\|FAIL" | head</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/components/connectors --run` exits 0 (all wizard + connector-form unit + a11y tests green)
    - `npm run lint` exits 0
    - `npm run build` exits 0 and the SUMMARY pastes the `/dashboard/connectors` First-Load JS figure
    - `npm run perf:budget` exits 0; SUMMARY states the connectors number is ≤250 KB with the delta vs the 153 kB baseline
    - `npm run test:e2e -- a11y-routes connector-wizard-a11y` exits 0; SUMMARY pastes the both-theme pass lines (closed grid + open wizard)
    - SUMMARY explicitly records whether `next/dynamic` was needed (expected: NOT needed)
  </acceptance_criteria>
  <done>The full gate is green with pasted evidence: unit + lint + build + bundle ≤250 KB + axe both themes (closed and open wizard).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The four-step add-connector wizard (provider → credentials → test → confirm) inside the existing ResponsiveDialog, with a display-only stepper, test-gated Next, D-08 re-test invalidation, and focus-to-heading on step change. Automated gates (unit, axe both themes on the open dialog, bundle ≤250 KB) are green.
  </what-built>
  <how-to-verify>
    Run the app (`npm run dev`, or the prod build per the local e2e setup) and open `/dashboard/connectors` as an admin.
    1. **Desktop happy path:** click a "+ {provider}" card → dialog opens scoped to that provider (heading "Add connector · {name}"), stepper shows ① Provider ✓ · ② Credentials current. Fill credentials → Next enables → step 3. Click "Test connection" → green ✓ block → Next enables → confirm shows the required scopes + purpose + sync interval → "Add connector" creates it.
    2. **Test gate (D-05/D-07):** on step 3 before testing, confirm Next is not operable and a screen reader (VoiceOver) announces the hint "Test the connection to continue." Force a failing test (bad token) → red ✗ block, stays on step 3.
    3. **Re-test invalidation (D-08):** after a passing test, go Back to credentials, edit one field → the ✓ clears on the FIRST keystroke and Next re-disables with "Credentials changed — re-test to continue."
    4. **Focus/SR:** each Back/Next moves focus to the new step heading and VoiceOver announces the step (both desktop AND a ≤767px mobile/responsive view inside the vaul bottom sheet — this is the new-precedent focus move).
    5. **Reduced motion:** enable "Reduce motion" (macOS) → the stepper fill change is instant, the Testing… spinner does not animate.
    6. **Dismissal (D-13):** backdrop click is a no-op; X and Esc close immediately with no confirm prompt; the fixed bottom-nav is not disturbed on mobile.
    7. **Edit unchanged (D-11):** editing an existing connector still opens the single-step form (no wizard, no provider-pick step), with the sentinel pre-fill.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (which step, expected vs actual)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI a11y/bundle gate → merge | the gate is the last automated defense before the wizard ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering (of trust) | connector-wizard-a11y.spec.ts + human-verify | mitigate | The human-verify checkpoint step 3 explicitly exercises the D-08 first-keystroke invalidation that unit test E (19-00) proves; belt-and-suspenders against a UI-wiring regression that unit tests alone could miss. |
| T-19-06 | Denial of Service (a11y regression) | e2e axe gate | mitigate | Axe both-theme sweep of the OPEN dialog closes the pre-existing coverage gap (RESEARCH: closed-grid-only sweep) so a wizard a11y regression fails CI rather than shipping. |
</threat_model>

<verification>
- New e2e spec passes: open-wizard axe clean in dark + light; vaul mobile render + bottom-nav intact.
- Full gate green with pasted evidence (unit + lint + build + bundle ≤250 KB + e2e both themes).
- Human-verify sign-off recorded.
</verification>

<success_criteria>
UX-D-02-06 proven: /dashboard/connectors passes axe WCAG 2.1 AA in both themes WITH the wizard open, stays ≤250 KB, works in the vaul mobile sheet, and is reduced-motion-safe — with a human sign-off on the interaction feel.
</success_criteria>

<output>
After completion, create `.planning/phases/19-add-connector-wizard/19-04-SUMMARY.md`
</output>
