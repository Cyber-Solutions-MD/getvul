---
phase: 19-add-connector-wizard
plan: 03
type: execute
wave: 2
depends_on: [19-00, 19-01, 19-02]
files_modified:
  - frontend/src/components/connectors/wizard/add-connector-wizard.tsx
  - frontend/src/app/(authed)/dashboard/connectors/page.tsx
autonomous: true
requirements: [UX-D-02-01, UX-D-02-02, UX-D-02-06]
must_haves:
  truths:
    - "Opening the add flow renders the wizard: stepper + one visible step at a time, navigated by Back/Next only"
    - "Next is aria-disabled (still focusable) with an aria-describedby hint until each step's gate is met; the test-step gate hint and the D-08 re-test hint are announced via a polite live region"
    - "Focus moves to the new step's <h3> on every step change, on both desktop and mobile"
    - "Editing an existing connector still opens the unchanged single-step ConnectorForm (D-11)"
  artifacts:
    - path: "frontend/src/components/connectors/wizard/add-connector-wizard.tsx"
      provides: "Wizard container: owns useWizardState, renders stepper + active step + Back/Next footer + focus mgmt + live regions"
      contains: "useWizardState"
    - path: "frontend/src/app/(authed)/dashboard/connectors/page.tsx"
      provides: "Conditional add→AddConnectorWizard / edit→ConnectorForm render; provider-name dialog heading"
      contains: "AddConnectorWizard"
  key_links:
    - from: "page.tsx ResponsiveDialog"
      to: "AddConnectorWizard (add) / ConnectorForm (edit)"
      via: "formState.mode conditional"
      pattern: "mode === 'add'"
    - from: "add-connector-wizard.tsx"
      to: "step components + stepper"
      via: "imports"
      pattern: "WizardStepper|CredentialsStep|TestStep|ConfirmStep"
---

<objective>
Assemble the wizard: `AddConnectorWizard` owns `useWizardState`, renders the `WizardStepper` + exactly one step `<section>` at a time (Credentials → Test → Confirm), and provides the Back/Next footer with the full gating + a11y contract (aria-disabled Next, aria-describedby hints, polite live region for the test-gate + D-08 re-test hints, focus-to-heading on step change for BOTH mobile and desktop). Then wire `page.tsx` so `mode==='add'` renders the wizard while `mode==='edit'` keeps the unchanged single-step `ConnectorForm` (D-11), and the dialog heading shows the picked provider name.

Purpose: This is the integration wave — it makes `add-connector-wizard.test.tsx` and the component a11y test GREEN and delivers UX-D-02-01/02 end-to-end inside the existing ResponsiveDialog (UX-D-02-06 vaul).
Output: Wizard container + page wiring; add-connector-wizard.test.tsx + .a11y.test.tsx GREEN.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-add-connector-wizard/19-UI-SPEC.md
@.planning/phases/19-add-connector-wizard/19-RESEARCH.md
@frontend/src/components/connectors/wizard/use-wizard-state.ts
@frontend/src/components/connectors/wizard/wizard-stepper.tsx
@frontend/src/components/connectors/wizard/credentials-step.tsx
@frontend/src/components/connectors/wizard/test-step.tsx
@frontend/src/components/connectors/wizard/confirm-step.tsx

<interfaces>
From use-wizard-state.ts (19-00):
`const w = useWizardState(fields)` → `{ state, allFieldsFilled, isTestStale, testPassed, canAdvance, updateField, setTestResult, setSyncInterval, advance, back, buildCredentials }`.

Existing ConfirmModal focus-to-ref precedent (frontend/src/components/ui/ConfirmModal.tsx lines 38-51) — adapt WITHOUT the `!isMobile` guard (RESEARCH Pattern 3: content changes post-open, so focus must move on mobile too).

page.tsx current add render (lines 358-385): `<ResponsiveDialog>` → `<div className="p-6">` → `<h2 id={formTitleId}>{mode==='add'?'Add connector':'Edit connector'}</h2>` + X button → `<ConnectorForm mode={formState.mode} .../>`. `formState` = `{ open, mode, connectorType, fields, existing? }`. `typesQuery.data` has `{type,name,permissions}` per type.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AddConnectorWizard container — stepper + steps + gated Back/Next footer + focus + live regions</name>
  <files>frontend/src/components/connectors/wizard/add-connector-wizard.tsx</files>
  <read_first>
    - frontend/src/components/connectors/wizard/add-connector-wizard.test.tsx AND add-connector-wizard.a11y.test.tsx (RED assertions to satisfy)
    - frontend/src/components/ui/ConfirmModal.tsx (lines 38-51 — the focus-to-ref effect pattern to adapt without isMobile guard)
    - .planning/phases/19-add-connector-wizard/19-UI-SPEC.md (§Interaction & Gating Contract, §Accessibility "Disabled-Next pattern" + "Live-region announcements" + "Focus management", §Responsive footer sticky/44px)
    - frontend/src/components/connectors/microcopy.ts (WIZARD_COPY)
  </read_first>
  <action>
    Create `AddConnectorWizard`. Props: `{ connectorType: string; providerName: string; fields: string[]; permissions: ConnectorTypePermission[]; onClose: () => void }`.

    Call `const w = useWizardState(fields)`. Maintain `stepHeadingId` via `useId()` per step. Render top-to-bottom:
    1. `<WizardStepper currentStep={w.state.step} />`
    2. The active step's `<section>` only (conditional on `w.state.step`):
       - `credentials`: `<CredentialsStep fields={fields} values={w.state.values} onFieldChange={w.updateField} syncInterval={w.state.syncInterval} onSyncIntervalChange={w.setSyncInterval} headingRef={headingRef} headingId={credId} />`
       - `test`: `<TestStep connectorType={connectorType} buildCredentials={w.buildCredentials} testResult={w.state.testResult} onResult={w.setTestResult} headingRef={headingRef} headingId={testId} />`
       - `confirm`: `<ConfirmStep providerName={providerName} connectorType={connectorType} permissions={permissions} syncInterval={w.state.syncInterval} credentials={w.buildCredentials()} onSuccess={onClose} headingRef={headingRef} headingId={confirmId} />`
    3. A footer button row (`flex items-center justify-between pt-8`; on mobile `sticky bottom-0 bg-surface` so it stays reachable while fields scroll — UI-SPEC Responsive):
       - LEFT: on step `credentials` render `Cancel` (WIZARD_COPY.cancelLabel) calling `onClose` (nowhere to go back inside the modal — step 1 is the grid). On `test`/`confirm` render `Back` (WIZARD_COPY.backLabel) calling `w.back()`.
       - RIGHT: on `credentials`/`test` render `Next` (WIZARD_COPY.nextLabel); on `confirm` the `Add connector` CTA lives INSIDE ConfirmStep (do NOT duplicate it in the footer — omit the right button on confirm).
       - Buttons render at `min-h-[44px]` on `max-[767px]` (touch target).

    **Gated Next (UX-D-02-02, Disabled-Next pattern — UI-SPEC A11y):** use `aria-disabled={!w.canAdvance}` (NOT native `disabled`, so it stays focusable), `aria-describedby={hintId}`, and a click guard: `onClick={() => { if (!w.canAdvance) return; w.advance(); }}`. Style the disabled look with `opacity-50 cursor-not-allowed` when `!w.canAdvance`. The forward Next uses the gradient (single accent).

    **Hint live region (UX-D-02-02 announce-why):** render `<p id={hintId} aria-live="polite" className="text-xs ...">{hintText}</p>` where `hintText` =
      - on `test` step, `w.state.testResult === null && !w.canAdvance` → `WIZARD_COPY.testGateHint` ('Test the connection to continue.')
      - when `w.isTestStale` (on test OR any step reached after a passing test then edited) → `WIZARD_COPY.retestHint` ('Credentials changed — re-test to continue.')
      - else empty string.

    **Focus management (UX-D-02-06 / RESEARCH Pattern 3):** `const headingRef = useRef<HTMLHeadingElement>(null); useEffect(() => { headingRef.current?.focus(); }, [w.state.step]);` — NO isMobile guard (content changes inside the already-open vaul sheet; comment this rationale referencing RESEARCH Pattern 3 / ConfirmModal divergence).

    Only useCreateConnector/useTestConnector/useConnectorTypes may be used transitively (via steps) — introduce NO new hook. Do NOT re-implement gating; consume `w.canAdvance`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/components/connectors/wizard/add-connector-wizard --run 2>&1 | tail -18</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- add-connector-wizard --run` exits 0 (both add-connector-wizard.test.tsx and add-connector-wizard.a11y.test.tsx now GREEN, incl. `toHaveNoViolations`)
    - `grep -q "useWizardState" frontend/src/components/connectors/wizard/add-connector-wizard.tsx`
    - imports all four children: `grep -q "WizardStepper" ... && grep -q "CredentialsStep" ... && grep -q "TestStep" ... && grep -q "ConfirmStep"` all succeed
    - `grep -q "aria-disabled" frontend/src/components/connectors/wizard/add-connector-wizard.tsx` and `grep -q "aria-describedby" frontend/src/components/connectors/wizard/add-connector-wizard.tsx` (Disabled-Next pattern, focusable)
    - native disabled NOT used on Next: `grep -Eqv "disabled={!" frontend/src/components/connectors/wizard/add-connector-wizard.tsx` (no `disabled={!canAdvance}` — must be aria-disabled)
    - `grep -q "aria-live=\"polite\"" frontend/src/components/connectors/wizard/add-connector-wizard.tsx` (hint region)
    - `grep -q "testGateHint" ... && grep -q "retestHint" ...` (both hints wired)
    - focus effect present with NO isMobile guard: `grep -q "headingRef.current?.focus()" ...` AND `grep -Lq "isMobile" frontend/src/components/connectors/wizard/add-connector-wizard.tsx` (isMobile absent)
    - `grep -q "min-h-\[44px\]" frontend/src/components/connectors/wizard/add-connector-wizard.tsx` (mobile touch target)
    - `npx tsc --noEmit` reports no error referencing `add-connector-wizard.tsx`
  </acceptance_criteria>
  <done>Wizard container renders the gated flow with correct a11y (aria-disabled Next, described hints, live region, focus-to-heading on both platforms); add-connector-wizard tests + a11y test green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire page.tsx — add→wizard, edit→form, provider-name heading, e2e open hook</name>
  <files>frontend/src/app/(authed)/dashboard/connectors/page.tsx</files>
  <read_first>
    - frontend/src/app/(authed)/dashboard/connectors/page.tsx (lines 74-159 formState + openAddForm/openEditForm/?provider= effect; lines 333-385 add-another cards + ResponsiveDialog render)
    - .planning/phases/19-add-connector-wizard/19-CONTEXT.md (D-01 grid IS step 1; D-11 edit unchanged; D-13 dismissal)
    - frontend/src/components/connectors/wizard/add-connector-wizard.tsx (the new component's props)
  </read_first>
  <action>
    Import `AddConnectorWizard`. Inside the `ResponsiveDialog` block (lines ~358-385):
    - Compute `const providerTypeInfo = typesQuery.data?.find((t) => t.type === formState.connectorType);` and `const providerName = providerTypeInfo?.name ?? formState.connectorType;`.
    - Update the `<h2 id={formTitleId}>` heading: for add mode use `Add connector · ${providerName}` (WIZARD_COPY.dialogHeading(providerName)); for edit keep `Edit connector`.
    - Replace the single `<ConnectorForm .../>` render with a conditional:
      - `formState.mode === 'add'` → `<AddConnectorWizard connectorType={formState.connectorType} providerName={providerName} fields={formState.fields} permissions={providerTypeInfo?.permissions ?? []} onClose={closeForm} />`
      - `formState.mode === 'edit'` → the EXISTING `<ConnectorForm mode="edit" connectorType={formState.connectorType} existing={formState.existing} fields={formState.fields} onClose={closeForm} />` UNCHANGED (D-11).
    - Do NOT change `openAddForm`, `openEditForm`, the `?provider=` deep-link effect, `closeForm`, or the `ResponsiveDialog` dismissal wiring (D-01, D-13 preserved — backdrop no-op, X/Esc close immediately).

    **E2E open hook:** on the "Add another" cards (lines ~334-345) and the empty-state CTA (lines ~303-312), add a stable attribute `data-add-connector={t.type}` (add-another) / `data-add-connector={catTypes[0].type}` (empty CTA) so the Wave-3 e2e sweep can deterministically open the wizard without hardcoding a provider. Purely additive attribute; no behavior change.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "connectors/page" | grep -qx 0 && grep -q "AddConnectorWizard" 'src/app/(authed)/dashboard/connectors/page.tsx' && grep -q "data-add-connector" 'src/app/(authed)/dashboard/connectors/page.tsx' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "AddConnectorWizard" 'frontend/src/app/(authed)/dashboard/connectors/page.tsx'`
    - `grep -q "mode === 'add'" 'frontend/src/app/(authed)/dashboard/connectors/page.tsx'` (conditional render)
    - `grep -q "Add connector · \|dialogHeading" 'frontend/src/app/(authed)/dashboard/connectors/page.tsx'` (provider-name heading)
    - edit path preserved: `grep -q 'mode="edit"' 'frontend/src/app/(authed)/dashboard/connectors/page.tsx'` and `grep -q "ConnectorForm" 'frontend/src/app/(authed)/dashboard/connectors/page.tsx'` (D-11)
    - `grep -q "data-add-connector" 'frontend/src/app/(authed)/dashboard/connectors/page.tsx'` (e2e open hook)
    - deep-link + dismissal untouched: `grep -q "searchParams.get('provider')" 'frontend/src/app/(authed)/dashboard/connectors/page.tsx'` and `grep -q "onOpenChange={(o) => { if (!o) closeForm(); }}" 'frontend/src/app/(authed)/dashboard/connectors/page.tsx'`
    - `npx tsc --noEmit` reports no error referencing `connectors/page`
    - `npm run lint` passes (no new lint errors)
  </acceptance_criteria>
  <done>Add flow renders the wizard with a provider-scoped heading; edit flow untouched (D-11); deep-link + dismissal preserved; e2e open hook present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard footer Next → step advance | gating decision point controlling whether untested creds can reach confirm/submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering (of trust) | add-connector-wizard.tsx | mitigate | Next uses `aria-disabled={!w.canAdvance}` PLUS a click-handler guard (`if (!w.canAdvance) return;`) — aria-disabled alone is not enforcement, so the no-op guard is the real gate. On the test step `canAdvance === testPassed` (success AND not stale), enforcing D-05/D-08. |
| T-19-05 | Elevation of Privilege | connectors/page.tsx | accept | Connector admin gating (`require_admin`) is backend-enforced and unchanged; the page already computes `isAdmin` for card affordances. Wizard adds no new privilege path. |
</threat_model>

<verification>
- `npm test -- add-connector-wizard --run` green (unit + a11y).
- `npx tsc --noEmit` + `npm run lint` clean.
- Edit path grep-verified unchanged (D-11); deep-link + dismissal grep-verified intact (D-01/D-13).
</verification>

<success_criteria>
The four-step wizard runs end-to-end inside ResponsiveDialog with correct gating and a11y; the gate is enforced by a real click guard (not aria alone); focus moves to each step heading on both platforms; edit mode still uses the untouched single-step form.
</success_criteria>

<output>
After completion, create `.planning/phases/19-add-connector-wizard/19-03-SUMMARY.md`
</output>
