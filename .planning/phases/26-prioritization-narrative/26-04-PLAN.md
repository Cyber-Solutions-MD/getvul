---
phase: 26-prioritization-narrative
plan: 04
type: execute
wave: 4
depends_on: [26-03]
files_modified:
  - frontend/src/components/ai/ai-explanation-section.tsx
  - frontend/src/lib/queries/use-explain-cache.ts
  - frontend/src/components/vulnerabilities/drill-content.tsx
  - frontend/src/components/ai/ai-explanation-section.test.tsx
  - frontend/src/components/vulnerabilities/drill-panel.test.tsx
  - frontend/tests/no-ai-rank.test.ts
autonomous: true
requirements: [AIP-01]
must_haves:
  truths:
    - "A new 'Prioritization' drill-panel section renders via the SAME AiExplanationSection + AiExplanationCitations components with resourceType='prioritization', placed between 'AI Explanation' and 'Remediation (raw)' (D-03/D-09, UI-SPEC placement)"
    - "On a cache hit the validated cited narrative renders (scanner_verbatim tint + ai_interpreted tag) with NO rank number and NO button; Analyst+ on a not-queued miss sees the 'Explain the priority' btn-secondary trigger; Viewer sees muted 'No prioritization narrative generated yet.' (UI-SPEC states 1/5/6)"
    - "The NEW queued/pending card renders when cache-check returns cached:false AND queued:true — Clock icon, neutral/violet DegradedCard, copy 'Prioritization narrative is being prepared'; Analyst+ gets a subordinate 'Generate it now' link, Viewer gets the same card with NO action (D-02/D-17, UI-SPEC states 3/4)"
    - "No new sort control, list/table column, or numeric AI badge is introduced anywhere — the deterministic ASSET-02 score stays the ONE sortable number (Pitfall #7/SC2, threat T-26-02)"
    - "The queued state is driven ENTIRELY by the backend queued boolean, never inferred client-side from timing (UI-SPEC 'partial' backstop)"
  artifacts:
    - path: "frontend/src/components/ai/ai-explanation-section.tsx"
      provides: "isPrioritization discriminator + resourceType-scoped copy + Clock-icon DegradedCard variant + queued branch"
      contains: "isPrioritization"
    - path: "frontend/src/lib/queries/use-explain-cache.ts"
      provides: "queued?: boolean on the cache-miss result type"
      contains: "queued"
    - path: "frontend/src/components/vulnerabilities/drill-content.tsx"
      provides: "new <section aria-labelledby=\"drill-prioritization-h\"> mount between AI Explanation and Remediation"
      contains: "drill-prioritization-h"
    - path: "frontend/tests/no-ai-rank.test.ts"
      provides: "the No-Rank UI CI check (grep-in-Vitest over components + lib/queries)"
      contains: "no-ai-rank"
  key_links:
    - from: "drill-content.tsx"
      to: "AiExplanationSection resourceType='prioritization' headingId='drill-prioritization-h'"
      via: "section mount between drill-ai-h and drill-remed-h"
      pattern: "resourceType=\"prioritization\""
    - from: "ai-explanation-section.tsx queued branch"
      to: "cacheQuery.data?.cached === false && cacheQuery.data?.queued === true"
      via: "branch placed BEFORE the isAnalystOrAbove trigger branch"
      pattern: "queued === true"
---

<objective>
Ship the frontend half of the tracer slice: add the fifth `resourceType='prioritization'` value to the shared `AiExplanationSection`, mount a new "Prioritization" drill-panel section between "AI Explanation" and "Remediation (raw)", add the ONE genuinely new state (the async-batch "queued / being prepared" card with a Clock icon and a subordinate "Generate it now" escape hatch), thread the `queued?: boolean` cache-check field, and wire the literal, grep-provable No-Rank UI check.

Purpose: This completes the end-to-end on-demand tracer (admin key → analyst click → validated cited streamed narrative → cited render → no rank). The queued branch ships now (structurally guaranteed placement, proven via a fixture test) even though the backend `queued` signal is wired in Plan 06 — the branch stays dark in production until then, which is correct (the batch feature isn't live until Plan 08). SC2 (no AI rank anywhere) is a tracer-critical contract and is enforced here as a CI check, not intention.
Output: the extended shared component + cache-check type + drill mount + component/drill tests + the no-ai-rank CI test.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-prioritization-narrative/26-CONTEXT.md
@.planning/phases/26-prioritization-narrative/26-UI-SPEC.md
@.planning/phases/26-prioritization-narrative/26-PATTERNS.md

MANDATORY UI READ (per CLAUDE.md): before touching any component, read `.claude/skills/sketch-findings-getvul/` — specifically `references/state-patterns.md` (mandatory loading/empty/error state discipline), `references/copy-voice.md` (tone; the LOCKED strings in 26-UI-SPEC.md's Copywriting Contract win over any invented copy), `references/foundation.md` (sunset CSS-variable tokens — NO freehand hex; the pending card reuses the EXISTING --color-violet/--color-surface-2 family), `references/visual-language.md`. Inter + JetBrains Mono are locked; JetBrains Mono is NOT used in this section's copy.

<interfaces>
Existing (backend, from Plan 03): GET /api/v1/ai/explain-prioritization/{finding_id} returns {cached:false} on miss / {cached:true, ...ExplainVulnResponse} on hit. (Plan 06 adds queued to the miss shape.)
Existing shared component (frontend/src/components/ai/ai-explanation-section.tsx): the isRemediationGuidance discriminator (lines ~164-184), the groundable===false branch (lines ~278-287), DegradedCard + DegradedCardProps (lines ~30-74, currently variant 'neutral'|'amber'|'danger', Sparkles/AlertTriangle icons), the headingId prop (Props type lines ~103-128).
Existing hook (frontend/src/lib/queries/use-explain-cache.ts): ExplainCacheResult = { cached:false; groundable?:boolean } | ({ cached:true } & ExplainVulnResponse) (lines ~17-19). Hook body needs zero change — new field rides along in the generic JSON.
Existing (no change needed, confirmed by PATTERNS): use-explain-stream.ts (generic on resourceType), keys.ts (ai.explain generic), ai-explanation-citations.tsx (resourceType-agnostic).
lucide-react: Clock (already a dependency — no new package).
</interfaces>
</context>

<artifacts_this_phase_produces>
This plan contributes: the `isPrioritization` discriminator + resourceType-scoped LOCKED copy + the `Clock`-icon `DegradedCard` variant + the `queued` branch (ai-explanation-section.tsx); `queued?: boolean` on `ExplainCacheResult` (use-explain-cache.ts); the `<section aria-labelledby="drill-prioritization-h">` mount (drill-content.tsx); and the `no-ai-rank` CI check.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: resourceType='prioritization' + queued branch in AiExplanationSection (+ cache-check type)</name>
  <read_first>
    - `.claude/skills/sketch-findings-getvul/references/state-patterns.md` + `references/copy-voice.md` + `references/foundation.md` (MANDATORY — token discipline, LOCKED copy wins, no freehand hex).
    - `.planning/phases/26-prioritization-narrative/26-UI-SPEC.md` §"Copywriting Contract" (every LOCKED string verbatim: heading "Prioritization", CTA "Explain the priority", viewer-empty "No prioritization narrative generated yet.", insufficient-signal heading "Not enough signal to explain priority reliably" + its body, queued heading "Prioritization narrative is being prepared" + body "This finding is in the next scheduled batch — narratives typically land within 24h." + action "Generate it now"), §"Color" (the pending card = neutral/violet SAME family as insufficient-evidence, Clock icon, NOT a new hue; "Explain the priority" is btn-secondary NOT a gradient CTA), §"Section body states" (states 1-12, mutual exclusion, branch ordering).
    - `.planning/phases/26-prioritization-narrative/26-PATTERNS.md` §"Frontend: ai-explanation-section.tsx" (the exact insertion points: the isRemediationGuidance discriminator lines 164-184; the DegradedCardProps `icon?: 'sparkles' | 'clock'` additive field defaulting to sparkles; the queued branch inserted BEFORE the isAnalystOrAbove trigger branch, checked `=== true` explicitly) and §"use-explain-cache.ts" (the `queued?: boolean` addition mirroring `groundable?`).
    - `frontend/src/components/ai/ai-explanation-section.tsx` (the file — the discriminator block, DegradedCard, the groundable===false branch, the isAnalystOrAbove branch).
    - `frontend/src/lib/queries/use-explain-cache.ts` (whole 36-line file).
  </read_first>
  <action>
    In `use-explain-cache.ts`, add `queued?: boolean` to the `{ cached: false; groundable?: boolean }` arm of `ExplainCacheResult` (hook body unchanged — the field rides along in the generic JSON, exactly as `groundable` does). In `ai-explanation-section.tsx`: (1) add `const isPrioritization = resourceType === 'prioritization';` and extend `heading`/`triggerLabel`/`viewerEmptyText`/`insufficientEvidenceCopy` each with a 3rd branch using the LOCKED UI-SPEC strings (heading "Prioritization", trigger "Explain the priority", viewer-empty "No prioritization narrative generated yet.", insufficient heading "Not enough signal to explain priority reliably" + its body naming exploit/KEV/SLA/severity signal). (2) Add `icon?: 'sparkles' | 'clock'` to `DegradedCardProps` (default 'sparkles' so every existing call site is unaffected) and branch the icon render so `variant='neutral'` + `icon='clock'` renders `Clock` (import from lucide-react) — same violet-soft chip, different glyph, per UI-SPEC Color. (3) Insert the NEW queued branch `else if (cacheQuery.data?.cached === false && cacheQuery.data?.queued === true) { ... }` BEFORE the `isAnalystOrAbove` trigger branch (same structural-guarantee placement as `groundable===false`): render the neutral/clock `DegradedCard` with heading "Prioritization narrative is being prepared" + the locked body; for Analyst+ add `action={{ label: 'Generate it now', onClick: () => void start() }}`, for Viewer render the identical card with NO action (D-17). Do NOT restyle or introduce any new visual chrome, factors-chip row, badge, or number — reuse `AiExplanationCitations` unmodified. Extend `ai-explanation-section.test.tsx` (mirroring the existing isRemediationGuidance/groundable assertions): a fixture with `{cached:false, queued:true}` renders the "being prepared" pending card even when the cache-check resolves instantly (proves the state is signal-driven, not a timing heuristic — the UI-SPEC "partial" backstop); Analyst+ sees "Generate it now", Viewer does not; a `{cached:false, queued:undefined}` fixture falls through to the ordinary trigger/viewer-empty branch (queued must not match on undefined).
  </action>
  <acceptance_criteria>
    - `grep -n "isPrioritization" frontend/src/components/ai/ai-explanation-section.tsx` present.
    - `grep -n "queued === true" frontend/src/components/ai/ai-explanation-section.tsx` present and appears BEFORE the isAnalystOrAbove trigger branch (verify ordering by line number).
    - The LOCKED strings "Prioritization narrative is being prepared", "Generate it now", "Explain the priority", "No prioritization narrative generated yet." appear verbatim.
    - `grep -n "queued" frontend/src/lib/queries/use-explain-cache.ts` present on the cached:false arm.
    - Test proves queued:true → pending card (signal-driven backstop); Viewer gets no action; queued:undefined → no pending card.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run ai-explanation-section</automated>
  </verify>
  <done>The shared component renders the prioritization view + the new signal-driven queued card with locked copy and no new numeric chrome; the cache-check type carries queued; the backstop test proves the state is signal-driven.</done>
</task>

<task type="auto">
  <name>Task 2: mount the Prioritization drill section + the No-Rank UI CI check</name>
  <read_first>
    - `.planning/phases/26-prioritization-narrative/26-UI-SPEC.md` §"Interaction Contract — Placement" (the LOCKED order: Header → CVSS → Affected hosts → Description → AI Explanation → **Prioritization (NEW)** → Remediation (raw) → Remediation guidance → Activity → Actions) and §"No-Rank UI Contract" (the forbidden/allowed table + the "Executor-facing check": grep frontend/src/components/**/*.tsx + lib/queries/**/*.ts for priority/rank/ai_score touching any table/sort/column, expecting zero NEW UI matches; the known-allowed exception is watcher-stack.tsx's role-priority comment).
    - `.planning/phases/26-prioritization-narrative/26-PATTERNS.md` §"Frontend: drill-content.tsx" (the section goes BETWEEN drill-ai-h lines 289-295 and drill-remed-h lines 297-305, NOT after remediation-guidance; `onCopyToDescription` is DELIBERATELY omitted on this mount — 3 props only, like the resourceType='vuln' mount) and §"No-rank repo-wide grep check" (No Analog Found — pick ONE mechanism; this plan picks a Vitest fs-based test).
    - `frontend/src/components/vulnerabilities/drill-content.tsx` (the drill-ai-h and drill-remed-h section mounts).
    - `frontend/src/components/vulnerabilities/drill-panel.test.tsx` (the existing section-order/section-mount assertions to extend).
    - `frontend/src/components/tickets/watcher-stack.tsx` (confirm the pre-existing role-priority comment/identifier so the CI check can allowlist exactly it).
  </read_first>
  <action>
    In `drill-content.tsx`, add a new `<section aria-labelledby="drill-prioritization-h">` containing `<AiExplanationSection resourceType="prioritization" resourceId={v.id ?? idOrCve} headingId="drill-prioritization-h" />` inserted immediately AFTER the existing `drill-ai-h` section and BEFORE `drill-remed-h` (the UI-SPEC-locked "explain → prioritize → remediate" reading order). Pass exactly 3 props (resourceType, resourceId, headingId) — do NOT pass `onCopyToDescription` (that prop is scoped to the remediation-guidance mount only). This one insertion covers desktop + mobile (mobile renders DrillContent directly). Do NOT add any list/table column, sort option, chip filter, or numeric badge anywhere. Create `frontend/tests/no-ai-rank.test.ts` (Vitest, using Node `fs`): recursively read every `frontend/src/components/**/*.tsx` and `frontend/src/lib/queries/**/*.ts` file; for each line, after stripping comment-only lines, flag any occurrence of the identifiers `ai_score`, `aiPriority`, `aiRank`, or `priority`/`rank` used in a table/sort/column context (e.g. a `<th>`/column-def/sort-option string). Allowlist the single known pre-existing benign match in `watcher-stack.tsx` (the role-priority dedupe, which is not an AI number). Assert the flagged set is empty (i.e., equals the allowlisted baseline). This is the literal SC2 artifact Phase 28's audit re-checks. Extend `drill-panel.test.tsx` to assert (a) the new Prioritization section mounts with `aria-labelledby="drill-prioritization-h"`, and (b) document order places it after the AI Explanation section and before the Remediation section.
  </action>
  <acceptance_criteria>
    - `grep -n "drill-prioritization-h" frontend/src/components/vulnerabilities/drill-content.tsx` present; the section sits between drill-ai-h and drill-remed-h (verify by line number).
    - `grep -c "onCopyToDescription" ` on the new prioritization `<AiExplanationSection>` mount returns 0 (prop deliberately omitted).
    - `frontend/tests/no-ai-rank.test.ts` exists and passes — zero NEW priority/rank/ai_score identifiers in any table/sort/column definition (watcher-stack role-priority allowlisted).
    - drill-panel.test.tsx asserts the section mounts and its document-order placement.
    - No new sort control / column / numeric badge added (the no-ai-rank test enforces this).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run no-ai-rank drill-panel</automated>
  </verify>
  <done>The Prioritization section mounts in the locked reading order with no new numeric UI chrome; the No-Rank UI contract is a passing CI check; drill-panel tests prove placement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend cache-check payload → UI render | A numeric rank must never be renderable; no sort/column added |
| Backend queued boolean → pending state | State must be signal-driven, never a client timing heuristic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 | Product-integrity (Pitfall #7 / SC2) | drill UI + all list/table views | mitigate | Narrative renders as prose only inside the drill section; zero new sort control/column/numeric badge; the `no-ai-rank` Vitest CI check greps components + lib/queries and fails on any new priority/rank/ai_score identifier in a table/sort/column context. |
| T-26-09 | Repudiation (state-integrity) | queued pending card | mitigate | The queued state derives entirely from the backend `queued` boolean (checked `=== true`), never inferred from elapsed time; the "partial" backstop test proves the pending card renders on an instantly-resolving cache-check with queued:true. |
</threat_model>

<verification>
- `cd frontend && npx vitest run ai-explanation-section no-ai-rank drill-panel` green.
- Wave-merge regression: `cd frontend && npx vitest run` green.
</verification>

<success_criteria>
The end-to-end on-demand tracer is code-complete and testable: a Prioritization drill section renders cited narratives, the new signal-driven queued card is proven, the deterministic score stays the one sortable number (grep-provable), and no restyle of the reused chrome occurred.
</success_criteria>

<output>
After completion, create `.planning/phases/26-prioritization-narrative/26-04-SUMMARY.md`.
</output>
