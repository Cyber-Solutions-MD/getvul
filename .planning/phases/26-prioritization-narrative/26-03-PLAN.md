---
phase: 26-prioritization-narrative
plan: 03
type: execute
wave: 3
depends_on: [26-01, 26-02]
files_modified:
  - backend/app/api/v1/ai/explain_prioritization.py
  - backend/app/api/v1/ai/__init__.py
  - backend/tests/test_ai_explain_prioritization.py
autonomous: true
requirements: [AIP-01]
must_haves:
  truths:
    - "POST /api/v1/ai/explain-prioritization/{finding_id} is require_analyst and streams a validated cited narrative via the UNCHANGED _run_explain_stream() engine with resource_type='prioritization' (D-02/D-09 on-demand fallback, AIP-01)"
    - "GET /api/v1/ai/explain-prioritization/{finding_id} is require_viewer and returns {cached: true, ...} on hit or {cached: false} on miss — a cheap cache-check with NO model call (D-09)"
    - "A Viewer POST returns 403; a cross-tenant or missing finding_id returns 404 on both POST and GET (D-17, threat T-26-05)"
    - "The route passes NO dangerous_pattern_check (prioritization has no destructive-command risk class — RESEARCH Pattern 7 / PATTERNS 'No Analog Found'); it mirrors explain_host.py, not explain_remediation_guidance.py"
  artifacts:
    - path: "backend/app/api/v1/ai/explain_prioritization.py"
      provides: "POST (require_analyst SSE) + GET (require_viewer cache-check) on /explain-prioritization/{finding_id}, UUID-keyed"
      contains: "explain-prioritization"
    - path: "backend/app/api/v1/ai/__init__.py"
      provides: "router registration for explain_prioritization"
      contains: "explain_prioritization"
    - path: "backend/tests/test_ai_explain_prioritization.py"
      provides: "RBAC matrix, cross-tenant 404, cache-check-miss, cache-hit round-trip"
      contains: "explain-prioritization"
  key_links:
    - from: "explain_prioritization.py POST"
      to: "_run_explain_stream(..., resource_type='prioritization', build_prompt=build_explain_prioritization_prompt, response_model=ExplainPrioritizationResponse, allowed_source_fields=PRIORITIZATION_ALLOWLIST, get_prompt_version=prioritization_prompt_version)"
      via: "shared engine reuse, dangerous_pattern_check omitted (None default)"
      pattern: "resource_type=\"prioritization\""
    - from: "explain_prioritization.py GET"
      to: "build_cache_key(user.tenant_id, 'prioritization', str(finding_id), record_hash(...), model, prioritization_prompt_version())"
      via: "cache-check with no model call"
      pattern: "prioritization_prompt_version"
---

<objective>
Complete the on-demand tracer backend: the thin `explain_prioritization.py` route (POST require_analyst SSE dispatch + GET require_viewer cache-check), UUID-keyed, wiring Plan 01's grounding + Plan 02's prompt/schema into the UNCHANGED `_run_explain_stream()` engine with `resource_type="prioritization"`. This mirrors `explain_host.py` exactly — no `dangerous_pattern_check`, no pre-generation refuse gate (prioritization narratives explain drivers, they recommend nothing to execute).

Purpose: This is the last backend piece of the on-demand vertical slice. After it, an Analyst can trigger a cited prioritization narrative and a Viewer can read a cached one — the request-path story is complete, matching Phases 24/25's request path. The `queued` signal (batch concern) is deliberately NOT added here; it arrives in Plan 06 once the AiBatchJob registry exists.
Output: the new route file + registration + the RBAC/cache/404 test suite.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-prioritization-narrative/26-CONTEXT.md
@.planning/phases/26-prioritization-narrative/26-RESEARCH.md
@.planning/phases/26-prioritization-narrative/26-PATTERNS.md

<interfaces>
From Plan 01 (backend/app/ai/grounding.py): get_prioritization_context(db, tenant_id, finding_id) -> dict[str, Any] | None
From Plan 01 (backend/app/ai/schemas.py): class ExplainPrioritizationResponse(ExplainResponseBase)
From Plan 02 (backend/app/ai/prompt_builder.py): PRIORITIZATION_ALLOWLIST, build_explain_prioritization_prompt(record), prioritization_prompt_version()
Existing engine (backend/app/ai/explain.py, reused UNCHANGED): _run_explain_stream(...), get_model_and_budget(db, tenant_id)
Existing (backend/app/ai/cache.py): build_cache_key, record_hash, get_cached
Existing (backend/app/auth/rbac.py): require_analyst, require_viewer
Route analog (backend/app/api/v1/ai/explain_host.py, 103 lines): _allowlisted_hash_fields, the POST/GET two-function UUID-keyed shape, media_type/headers, HTTPException(404)
Registration analog (backend/app/api/v1/ai/__init__.py lines 32-47): import tuple + include_router pattern
</interfaces>
</context>

<artifacts_this_phase_produces>
This plan contributes: `explain_prioritization.py` with `POST/GET /api/v1/ai/explain-prioritization/{finding_id}`, its `_allowlisted_hash_fields` mirror, and router registration. Plan 06 later adds the `queued` field + `_is_finding_queued()` helper to the GET handler (once AiBatchJob exists).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: explain_prioritization.py route (POST/GET) + registration</name>
  <read_first>
    - `.planning/phases/26-prioritization-narrative/26-PATTERNS.md` §"backend/app/api/v1/ai/explain_prioritization.py (new file)" (the FULL `explain_host.py` shape reproduced at lines 420-455; the explicit note this mirrors explain_host.py NOT explain_remediation_guidance.py — no dangerous_pattern_check, no pre-gen gate; the `_allowlisted_hash_fields` re-parse-the-scanner_data pattern) and §"__init__.py — router registration" (the 3-line pattern at lines 470-490).
    - `backend/app/api/v1/ai/explain_host.py` (whole file, 103 lines) — the exact two-function template: `_allowlisted_hash_fields(record)`, `explain_host` POST (require_analyst, 404 if record None, StreamingResponse with media_type='text/event-stream' + Cache-Control:no-cache + X-Accel-Buffering:no), `get_explain_host_cache` GET (require_viewer, `get_model_and_budget`, `record_hash`, `build_cache_key`, `get_cached`, `{"cached": False}` / `{"cached": True, **cached}`).
    - `backend/app/api/v1/ai/__init__.py` lines 14-47 (the per-router comment block + import tuple + include_router lines).
    - `backend/tests/test_ai_explain_remediation_guidance.py` (355 lines) — the RBAC-matrix / cache-check / cross-tenant-404 test structure to mirror: `test_post_as_analyst_..._returns_200_sse_with_headers`, `test_post_as_viewer_returns_403`, `test_get_cache_check_miss_...`, `test_cross_tenant_finding_id_not_resolvable`; reuse the `tenant_a`/`tenant_b`/`analyst_user`/`viewer_user`/`client_factory` fixtures verbatim and commit the session before each HTTP call (WR-13 contract, per 24-07).
  </read_first>
  <behavior>
    - POST as Analyst on a groundable finding → 200 SSE stream with the anti-buffering headers; the engine runs with `resource_type="prioritization"` and NO `dangerous_pattern_check`.
    - POST as Viewer → 403.
    - GET as Viewer → `{"cached": true, ...}` on hit, `{"cached": false}` on miss (no model call).
    - Cross-tenant / missing finding_id → 404 on both POST and GET.
    - The GET cache-key is composed with `resource_type="prioritization"` and `prioritization_prompt_version()` (so a batch-warmed narrative from Plans 07-08 is a plain cache hit here — D-06).
  </behavior>
  <action>
    Create `backend/app/api/v1/ai/explain_prioritization.py` by copying `explain_host.py`'s four-part shape and swapping: `get_asset_posture`→`get_prioritization_context`; `build_explain_host_prompt`/`ExplainHostResponse`/`HOST_ALLOWLIST`/`host_prompt_version`→`build_explain_prioritization_prompt`/`ExplainPrioritizationResponse`/`PRIORITIZATION_ALLOWLIST`/`prioritization_prompt_version`; `resource_type="host"`→`"prioritization"`; the path param `asset_id`→`finding_id` (UUID); route path `/explain-prioritization/{finding_id}`. POST is `require_analyst`, GET is `require_viewer`. Do NOT pass `dangerous_pattern_check` (leave it at the None default — prioritization has no destructive-command class; PATTERNS "No Analog Found" confirms `contains_dangerous_pattern` is not imported here). Do NOT add a pre-generation refuse gate (no `has_actionable_remediation_text` equivalent — D-04 factors are structured, the model's own grounded=false is the backstop). `_allowlisted_hash_fields(record)` re-parses the `<scanner_data>` block exactly as explain_host.py's does (so the GET cache-key matches the POST's, no second allowlist). GET returns `{"cached": False}` on miss (NO `queued` field this plan — Plan 06 adds it). Register in `backend/app/api/v1/ai/__init__.py`: add `explain_prioritization` to the import tuple, add one `ai_router.include_router(explain_prioritization.router)` line, and a one-line comment in the module block following the existing "Plan N's D-xx ..." convention. Write `backend/tests/test_ai_explain_prioritization.py` mirroring `test_ai_explain_remediation_guidance.py`: POST-analyst-200-SSE-with-headers, POST-viewer-403, GET-viewer-cache-miss-{cached:false}, GET-cache-hit-round-trip (seed a cache entry via the real `set_cached`/`build_cache_key` with `resource_type="prioritization"` and assert the GET returns it), cross-tenant-404 (both verbs), missing-finding-404. Inject a fake Anthropic client for the POST-200 path exactly as the remediation-guidance test does.
  </action>
  <acceptance_criteria>
    - `grep -n "explain-prioritization" backend/app/api/v1/ai/explain_prioritization.py` shows both POST and GET on `/{finding_id}`.
    - `grep -n "require_analyst" backend/app/api/v1/ai/explain_prioritization.py` on POST; `require_viewer` on GET.
    - `grep -c "dangerous_pattern_check\|contains_dangerous_pattern\|has_actionable_remediation_text" backend/app/api/v1/ai/explain_prioritization.py` returns 0 (this route imports/uses none of them).
    - `grep -n 'resource_type="prioritization"' backend/app/api/v1/ai/explain_prioritization.py` present (POST engine call).
    - `grep -n "explain_prioritization" backend/app/api/v1/ai/__init__.py` shows import + include_router.
    - Tests prove: Viewer POST→403; cross-tenant→404; GET miss→{cached:false}; GET hit round-trips a seeded prioritization cache entry.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_explain_prioritization.py -q</automated>
  </verify>
  <done>The on-demand prioritization route exists, inherits RBAC + tenant-scoped 404 verbatim, reuses the engine unchanged with resource_type='prioritization' and no safety gate, composes the shared cache key, and is registered + test-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → new SSE/GET route | Untrusted request crosses; role + tenant must be enforced |
| Route → shared engine | Reuse must not alter behavior for existing views (dangerous_pattern_check stays None) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Elevation of Privilege | `explain_prioritization` POST/GET | mitigate | `require_analyst` on POST (paid trigger), `require_viewer` on GET (cached-only), tenant-scoped 404 — reused verbatim from `explain_host.py` (D-17); RBAC-matrix + cross-tenant-404 tests prove it. |
| T-26-07 | Tampering (prompt injection) | `_run_explain_stream` (inherited) | accept | Prompt-injection defense (untrusted_content_policy + leak-marker check) inherited unchanged from Phase 24; this route adds no new injection surface. |
| T-26-02 | Product-integrity (Pitfall #7) | response payload | mitigate | `ExplainPrioritizationResponse` (Plan 01) carries no numeric field; the route never composes a rank number into its GET/SSE payloads. |
</threat_model>

<verification>
- `cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_explain_prioritization.py -q` green.
- Wave-merge regression: `... python -m pytest tests/test_ai_*.py -q` green (mirrors Phase 24/25 wave-merge convention).
</verification>

<success_criteria>
The on-demand prioritization request path is complete and test-proven: Analyst triggers a cited narrative, Viewer reads a cached one, RBAC/tenant-scope inherited verbatim, engine reused unchanged with no safety gate — the backend half of the tracer slice.
</success_criteria>

<output>
After completion, create `.planning/phases/26-prioritization-narrative/26-03-SUMMARY.md`.
</output>
