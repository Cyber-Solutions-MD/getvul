---
phase: 26-prioritization-narrative
plan: 08
type: execute
wave: 8
depends_on: [26-07]
files_modified:
  - backend/app/ai/batch.py
  - backend/app/connectors/scheduler.py
  - backend/tests/test_ai_batch.py
  - backend/tests/test_scheduler_ai_batch.py
autonomous: true
requirements: [AIP-02]
must_haves:
  truths:
    - "`poll_pending_batches()` resumes ALL in_progress `AiBatchJob` rows from Postgres (not memory) on every call — so a batch submitted before a restart is still retrieved (D-05/D-06, RESEARCH #2, threat T-26-08)"
    - "`poll_pending_batches()` takes NO shared/injected client: per `AiBatchJob` row it resolves `key = await get_tenant_anthropic_key(db, job.tenant_id)` and builds a FRESH per-tenant client `(anthropic_client_factory or _default_client_factory)(key)` before that job's `retrieve()`/`results()` — a tenant's key never touches another tenant's batch, no shared/fallback key (D-05/ROADMAP SC3, threat T-24-19/T-26-03)"
    - "For an ended batch, the poller streams the tenant-scoped `client.messages.batches.results()`, matches each line by `custom_id`, rebuilds the SAME cache key `build_cache_key(tenant_id, 'prioritization', custom_id, custom_id_hash_map[custom_id], FROZEN model, FROZEN prompt_version)`, and routes succeeded→`validate_and_cache_batch_result()` / errored|canceled|expired→a distinct audit status (cost 0.0, never cached), then marks the row `completed` (D-06, threat T-26-03/T-26-04)"
    - "The scheduler dispatches BOTH the nightly `run_batch_prewarm()` (24h-gated via `_last_ai_batch_prewarm`) AND the every-tick `poll_pending_batches()` via `asyncio.create_task` with NO client argument — each batch function resolves its own per-tenant keys internally — never an inline `await` — so neither ever stalls a connector-sync tick (D-05 hard constraint, RESEARCH #3/Pitfall 3, threat T-26-12)"
    - "The batch feature is LIVE only at the end of this plan: a real nightly submit + poll cycle runs from the scheduler using each tenant's own key"
  artifacts:
    - path: "backend/app/ai/batch.py"
      provides: "poll_pending_batches() — resume-from-Postgres retrieval + per-tenant BYOK client + result routing + row completion"
      contains: "async def poll_pending_batches"
    - path: "backend/app/connectors/scheduler.py"
      provides: "2 new _scheduler_loop() blocks (nightly submit + every-tick poll), both asyncio.create_task-dispatched with no client arg; _last_ai_batch_prewarm global"
      contains: "run_batch_prewarm"
    - path: "backend/tests/test_scheduler_ai_batch.py"
      provides: "non-blocking dispatch assertion (tick returns before the task completes) + 24h gate for both new blocks"
      contains: "asyncio"
  key_links:
    - from: "poll_pending_batches() per-job"
      to: "get_tenant_anthropic_key(job.tenant_id) then a FRESH (anthropic_client_factory or _default_client_factory)(key) client"
      via: "per-tenant BYOK client construction inside the per-job loop — never a shared/module-level client (T-24-19)"
      pattern: "_default_client_factory"
    - from: "poll_pending_batches()"
      to: "AiBatchJob WHERE status='in_progress' → batches.retrieve → (ended) batches.results → validate_and_cache_batch_result / distinct audit → status='completed'"
      via: "resume-from-Postgres poll loop, cache key rebuilt from frozen model+prompt_version+custom_id_hash_map"
      pattern: "status == \"in_progress\""
    - from: "_scheduler_loop()"
      to: "asyncio.create_task(run_batch_prewarm()) and asyncio.create_task(poll_pending_batches())"
      via: "24h-gated submit block + every-tick poll block, both create_task with no client arg (never inline await)"
      pattern: "asyncio.create_task"
---

<objective>
Wire the batch feature live: add `poll_pending_batches()` (the resume-from-Postgres retriever that builds a per-tenant BYOK client per job and consumes Plan 07's `validate_and_cache_batch_result()`) and the two `_scheduler_loop()` blocks that dispatch both `run_batch_prewarm()` (nightly, 24h-gated) and `poll_pending_batches()` (every tick) via `asyncio.create_task` with NO client argument — never inline. Both blocks reuse the file's own 24h-gate timing idiom but its NON-blocking `trigger_background_sync()` dispatch idiom (NOT the inline-await idiom the file's own ticket-sync/snapshot blocks use — RESEARCH Pitfall 3).

Purpose: This is the final plan; the batch feature goes live only here. Before it, `run_batch_prewarm()`/`validate_and_cache_batch_result()` existed but nothing dispatched them; now the scheduler submits nightly and polls to completion, resuming any in-flight batch across a restart from the durable `AiBatchJob` table, each batch retrieved with its owning tenant's own key.
Output: `poll_pending_batches()`, the two scheduler blocks + the `_last_ai_batch_prewarm` global, and their tests (including the first direct scheduler non-blocking-dispatch test).
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-prioritization-narrative/26-CONTEXT.md
@.planning/phases/26-prioritization-narrative/26-RESEARCH.md
@.planning/phases/26-prioritization-narrative/26-PATTERNS.md

<interfaces>
From Plan 07 (backend/app/ai/batch.py): run_batch_prewarm(*, limit=50, anthropic_client_factory=None) — NO shared client, resolves each tenant's own key internally; validate_and_cache_batch_result(db, redis_client, *, tenant_id, finding_id, raw_text, model, usage, cache_key) -> str; estimate_batch_cost_usd(client, model, requests); get_redis_client(); the own-session/own-client pattern; the `client = (anthropic_client_factory or _default_client_factory)(key)` per-tenant construction convention.
From Plan 06 (backend/app/ai/models.py): AiBatchJob (tenant_id, status in_progress|completed, model + prompt_version frozen, custom_id_hash_map, ended_at).
Reused UNCHANGED (backend/app/ai/explain.py): _default_client_factory(api_key) -> AsyncAnthropic (line 115) — the FRESH per-tenant BYOK client factory (T-24-19), reuse it, do NOT recreate. The `anthropic_client_factory` test-seam convention (lines 271-339).
Reused: cache.py build_cache_key(tenant_id, resource_type, resource_id, hash, model, prompt_version); tenant_keys.py get_tenant_anthropic_key(db, tenant_id) -> str | None (None = key rotated away → skip that job); audit.py audit_log_ai_call(..., user_email="system:scheduler", ...); db/session.py async_session_factory.
Anthropic Batches (anthropic==0.120.2): client.messages.batches.retrieve(batch_id) -> .processing_status ("in_progress"|"canceling"|"ended"); (only when ended) `async for line in await client.messages.batches.results(batch_id)`: line.custom_id, line.result.type ("succeeded"|"errored"|"canceled"|"expired"), line.result.message (a Message, has .content text + .usage) on succeeded. results() raises if called before ended (Pitfall 6 — gate on processing_status=="ended").
scheduler.py structure: globals at lines 18-20 (_running_syncs, _scheduler_task, _last_ticket_sync); _run_single_sync() own-session shape (lines 23-50); trigger_background_sync() the asyncio.create_task dispatch idiom to COPY (lines 53-62); _scheduler_loop() (line 71) with 7 existing one-block-per-concern try/except blocks; the daily-ticket-sync block (lines 152-165) is the 24h-gate timing idiom to copy but the inline-await idiom to AVOID; async_session_factory imported at line 12.
Scheduler test analog: backend/tests/test_connector_health.py::test_scheduler_path_failure_parity (lines 182-199) — the `from app.connectors import scheduler as scheduler_module; await scheduler_module.<fn>(...)` direct-await calling convention.
</interfaces>
</context>

<artifacts_this_phase_produces>
This plan contributes: `poll_pending_batches()` (backend/app/ai/batch.py, per-tenant BYOK client per job) and the two `_scheduler_loop()` dispatch blocks + `_last_ai_batch_prewarm` global (backend/app/connectors/scheduler.py), both dispatched with no client argument. This completes the phase — the batch feature is live: nightly submit + every-tick poll, both non-blocking, resuming across restarts, each batch retrieved with its owning tenant's own key.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: poll_pending_batches() — resume-from-Postgres retrieval + per-tenant BYOK client + result routing</name>
  <read_first>
    - `.planning/phases/26-prioritization-narrative/26-PATTERNS.md` §"backend/app/ai/batch.py — three borrowed shapes" (the own-session `_run_single_sync()` shape for `poll_pending_batches()`; the audit-only refusal shape for errored/canceled/expired) and §"Shared Patterns → Tenant-scoped cache key composition" (the poller rebuilds the SAME `build_cache_key()` the GET route uses, from the FROZEN model+prompt_version on the AiBatchJob row + `custom_id_hash_map[custom_id]` — so a batch-warmed narrative is a plain cache hit; D-06).
    - `backend/app/ai/explain.py` lines 15-17, 115-121, 271-339 (the T-24-19 BYOK contract; `_default_client_factory(api_key) -> AsyncAnthropic` — reuse; the `client = (anthropic_client_factory or _default_client_factory)(key)` per-tenant construction to mirror).
    - `.planning/phases/26-prioritization-narrative/26-RESEARCH.md` §"Pattern 1" (the poll/retrieve/results call shape; results are batch-ATOMIC — request_counts all-zero-except-processing until the whole batch ends, so treat the batch as one on/off signal; the four result types), §"Pattern 8" (succeeded→validate_and_cache_batch_result; errored/canceled/expired→distinct audit status `batch_errored`/`batch_canceled`/`batch_expired`, cost 0.0, never cached — never call the validator on a nonexistent payload), §"Pattern 4" (WHY model+prompt_version are frozen on the row: a mid-window model change must not orphan the cache key), Pitfall 6 (gate results() on processing_status=="ended").
    - `backend/app/ai/batch.py` (Plan 07's file — `validate_and_cache_batch_result()` signature, the own-session/own-client + per-tenant `_default_client_factory(key)` pattern, the imports).
    - `backend/app/connectors/scheduler.py` lines 23-50 (`_run_single_sync()` own-session template).
    - `backend/tests/test_ai_batch.py` (Plan 07's file — the fake-client + `anthropic_client_factory` seam to extend for the poller).
  </read_first>
  <behavior>
    - `poll_pending_batches()` takes NO shared/injected client; `anthropic_client_factory` is only the test seam.
    - `poll_pending_batches()` selects EVERY `AiBatchJob` with `status == "in_progress"` (so a row inserted before a simulated restart — i.e. seeded directly in the DB — is still picked up).
    - Per job it resolves `key = await get_tenant_anthropic_key(db, job.tenant_id)` and builds a FRESH per-tenant client `(anthropic_client_factory or _default_client_factory)(key)` — that job's `retrieve()`/`results()` use THAT tenant-scoped client only (T-24-19); a job whose key has been rotated away (`key is None`) is skipped (log + continue), left in_progress, never retrieved with any other key.
    - A still-processing batch (`retrieve().processing_status != "ended"`) is a no-op left in_progress for the next tick.
    - For an ended batch: each succeeded result is cached + audited `ok` (via `validate_and_cache_batch_result` with the cache key rebuilt from the row's frozen model/prompt_version + `custom_id_hash_map[custom_id]`); an errored/canceled/expired result is audited with its distinct status, cost 0.0, and is NEVER written to the cache; the AiBatchJob row is then set `status="completed"`, `ended_at=now`.
    - The cache key the poller writes is byte-identical to the one the GET route (Plan 03) computes for the same finding/hash/model/prompt_version — a batch-warmed narrative is a plain cache hit (D-06).
    - `results()` is never called unless `processing_status == "ended"` (Pitfall 6).
  </behavior>
  <action>
    Add `async def poll_pending_batches(*, anthropic_client_factory: Callable[[str], AsyncAnthropic] | None = None) -> None` to `backend/app/ai/batch.py` — NO externally-injected shared client (`anthropic_client_factory` is only the test seam, mirroring explain.py lines 271-339) — opening its own `async with async_session_factory() as db:` and a `redis_client = get_redis_client()` (own-session/own-client, since Plan 08 dispatches it via create_task). Query `jobs = (await db.execute(select(AiBatchJob).where(AiBatchJob.status == "in_progress"))).scalars().all()` (resume-from-Postgres — this is what makes restart-resumption work; NO in-memory tracking). For each job: resolve `key = await get_tenant_anthropic_key(db, job.tenant_id)` and `if key is None: continue` (key rotated away — skip, leave in_progress; never retrieve with another tenant's key), then build the tenant-scoped client `client = (anthropic_client_factory or _default_client_factory)(key)` (FRESH per job, T-24-19); `refreshed = await client.messages.batches.retrieve(job.anthropic_batch_id)`; if `refreshed.processing_status != "ended"`: continue (no-op, Pitfall 6). Otherwise `async for line in await client.messages.batches.results(job.anthropic_batch_id)`: `custom_id = line.custom_id`; `the_hash = job.custom_id_hash_map.get(custom_id)`; `cache_key = build_cache_key(job.tenant_id, "prioritization", custom_id, the_hash, job.model, job.prompt_version)` (frozen model/prompt_version from the row — RESEARCH Pattern 4); match `line.result.type`: on `"succeeded"` extract `raw_text` from `line.result.message.content` and `usage = line.result.message.usage`, call `await validate_and_cache_batch_result(db, redis_client, tenant_id=job.tenant_id, finding_id=custom_id, raw_text=raw_text, model=job.model, usage=usage, cache_key=cache_key)`; on `"errored"`/`"canceled"`/`"expired"` call `audit_log_ai_call(db, tenant_id=job.tenant_id, user_email="system:scheduler", model=job.model, usage=None, resource_type="prioritization", resource_id=custom_id, status=f"batch_{line.result.type}", cost_estimate_usd=0.0)` and DO NOT touch the cache (never call the validator — no payload to validate). After the results loop, set `job.status = "completed"`, `job.ended_at = datetime.now(UTC)`, and `await db.commit()`. Wrap the per-job body so one failing job does not abort the others (log + continue), mirroring the file-wide one-boundary-per-concern discipline. (Reuse `_default_client_factory`/`get_tenant_anthropic_key` already imported by Plan 07's batch.py — do not recreate.) Extend `backend/tests/test_ai_batch.py` with a fake batches client injected via the `anthropic_client_factory` seam: `test_poll_uses_per_tenant_key` (two seeded in_progress jobs for two tenants with DISTINCT keys; a recording factory captures the key used per job; assert each job was retrieved with its OWN tenant's key — the T-24-19 poll-side isolation guard), `test_poll_skips_key_rotated_away` (a job whose tenant key is now None is skipped, left in_progress, no retrieve/results call), `test_poll_skips_still_processing` (processing_status != ended → row stays in_progress, no cache write), `test_poll_succeeded_caches_and_completes` (succeeded → cache hit under the SAME build_cache_key the GET route would compute + audit ok + row completed), `test_poll_result_types_errored_canceled_expired` (each gets its distinct `batch_errored`/`batch_canceled`/`batch_expired` audit, cost 0.0, never cached), `test_poll_resumes_seeded_in_progress_row` (a row seeded directly in the DB — simulating a pre-restart submit — is picked up and completed).
  </action>
  <acceptance_criteria>
    - `grep -n "async def poll_pending_batches" backend/app/ai/batch.py` present.
    - `grep -c "async def poll_pending_batches(client" backend/app/ai/batch.py` returns 0 (NO shared/injected client param); `grep -n "get_tenant_anthropic_key" backend/app/ai/batch.py` present inside the poller (per-job key resolution) and `grep -n "_default_client_factory\|anthropic_client_factory" backend/app/ai/batch.py` present (per-job fresh client — T-24-19).
    - `grep -c "AsyncAnthropic()" backend/app/ai/batch.py` returns 0 (never a bare/shared client — the only construction is the per-tenant `_default_client_factory(key)`).
    - `grep -n 'status == "in_progress"' backend/app/ai/batch.py` present (resume-from-Postgres select — RESEARCH #2).
    - `grep -n "processing_status" backend/app/ai/batch.py` gated BEFORE any `.results(` call (Pitfall 6 — verify results() is inside the ended branch).
    - `grep -n "job.model\|job.prompt_version\|custom_id_hash_map" backend/app/ai/batch.py` present in the cache-key rebuild (frozen values — RESEARCH #4).
    - `grep -n "batch_errored\|batch_canceled\|batch_expired" backend/app/ai/batch.py` all present (distinct audit statuses); `grep -c "range(2)\|for attempt" backend/app/ai/batch.py` still 0 (no retry).
    - `grep -n 'status = "completed"\|status=\"completed\"' backend/app/ai/batch.py` present (row lifecycle close).
    - Tests prove: per-tenant-key retrieval (two tenants, two distinct keys), key-rotated-away skip, still-processing no-op, succeeded→same-cache-key hit + completed, errored/canceled/expired distinct-audit-never-cached, seeded-row resumption.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_batch.py -k poll -q</automated>
  </verify>
  <done>poll_pending_batches() resumes every in_progress batch from Postgres, retrieves each with its owning tenant's OWN freshly-constructed BYOK client, routes each result type correctly into the shared cache (same key the GET route reads) or a distinct audit, and closes the row — restart-safe, retry-free, atomic-per-batch, tenant-key-isolated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scheduler dispatch — nightly submit + every-tick poll, both asyncio.create_task, no client arg</name>
  <read_first>
    - `.planning/phases/26-prioritization-narrative/26-PATTERNS.md` §"backend/app/connectors/scheduler.py — add 2 new try/except blocks" (the idiom to COPY — `trigger_background_sync()` `asyncio.create_task` at lines 53-62; the anti-analog to AVOID — the daily-ticket-sync inline `await` at lines 152-165, whose 24h-gate timing IS the part to copy; the synthesized block A and block B verbatim; the `_last_ai_batch_prewarm: datetime | None = None` global added alongside the existing three).
    - `.planning/phases/26-prioritization-narrative/26-RESEARCH.md` §"Pattern 2" (the two-idiom distinction; both blocks own their own db session inside batch.py, detached from the loop's db; both batch functions resolve their own per-tenant keys, so the scheduler passes NO client) and §"System Architecture Diagram" (the scheduler dispatches `run_batch_prewarm(db)`/`poll_pending_batches(db)`-style thin calls with no client param) and Pitfall 3 (BOTH blocks need create_task — leaving poll as an inline await reintroduces the exact stall D-05 forbids).
    - `backend/app/connectors/scheduler.py` lines 18-20 (globals), lines 53-62 (dispatch idiom), lines 71-190 (the loop + its 7 existing blocks + the ticket-sync 24h gate to mirror).
    - `backend/tests/test_connector_health.py` lines 182-199 (the `from app.connectors import scheduler as scheduler_module; await scheduler_module.<fn>(...)` direct-call convention for the new scheduler test).
  </read_first>
  <behavior>
    - Both new blocks call `asyncio.create_task(...)`, never `await ...` inline — a unit that runs the block's body returns control before the dispatched coroutine finishes (the non-blocking assertion: use a fake `run_batch_prewarm`/`poll_pending_batches` that sets an event only after an `await asyncio.sleep(...)`, and assert the block returns before the event is set).
    - Both blocks dispatch the batch functions with NO client argument — the scheduler builds no `AsyncAnthropic`; each batch function resolves its own per-tenant keys internally (T-24-19). The scheduler block is a thin dispatch with no per-tenant logic and no key handling.
    - The nightly submit block is 24h-gated: it dispatches `run_batch_prewarm` only when `_last_ai_batch_prewarm is None` or ≥86400s elapsed, then updates `_last_ai_batch_prewarm`; a second immediate pass does NOT re-dispatch.
    - The poll block dispatches `poll_pending_batches` every tick (no gate).
    - A dispatch exception in either block is caught + logged and does not crash the loop (each block is its own try/except).
  </behavior>
  <action>
    In `backend/app/connectors/scheduler.py`: add `_last_ai_batch_prewarm: datetime | None = None` to the module globals (alongside `_last_ticket_sync`). Inside `_scheduler_loop()`, after the existing blocks, add block A (nightly submit): `global _last_ai_batch_prewarm` then a try/except that computes `now = datetime.now(UTC)` and, if `_last_ai_batch_prewarm is None or (now - _last_ai_batch_prewarm).total_seconds() >= 86400`, does `from app.ai.batch import run_batch_prewarm; asyncio.create_task(run_batch_prewarm())` then sets `_last_ai_batch_prewarm = now`; the except logs `"ai_batch_prewarm_dispatch_error"`. Add block B (every-tick poll): a try/except that does `from app.ai.batch import poll_pending_batches; asyncio.create_task(poll_pending_batches())`; the except logs `"ai_batch_poll_dispatch_error"`. Both blocks MUST use `asyncio.create_task` (never a direct `await`) — this is D-05's hard constraint extended uniformly to the poller per Pitfall 3. Neither block constructs an Anthropic client, imports `AsyncAnthropic`, or resolves any tenant key: `run_batch_prewarm()` and `poll_pending_batches()` each resolve their own per-tenant keys internally (T-24-19), so the scheduler passes NO client and keeps the block a thin dispatch with zero per-tenant logic (matching RESEARCH's System Architecture Diagram, which dispatches the batch functions with no client param). Create `backend/tests/test_scheduler_ai_batch.py` using the `from app.connectors import scheduler as scheduler_module` direct-call convention: (a) `test_batch_blocks_are_non_blocking` — monkeypatch `run_batch_prewarm`/`poll_pending_batches` with slow fakes (set an `asyncio.Event` after `await asyncio.sleep(0.05)`), run the dispatch logic, and assert control returns BEFORE the event is set (proves create_task, not inline await — Pitfall 3 regression guard); (b) `test_nightly_submit_24h_gated` — reset `_last_ai_batch_prewarm`, run once (dispatched), run again immediately (NOT re-dispatched); (c) `test_dispatch_exception_is_caught` — a fake that raises on dispatch does not propagate out of the block; (d) `test_scheduler_builds_no_client` — the dispatched fakes are called with NO client/positional argument (asserts the scheduler passes none — T-24-19 stays inside the batch functions).
  </action>
  <acceptance_criteria>
    - `grep -n "_last_ai_batch_prewarm" backend/app/connectors/scheduler.py` present as a module global + used in the 24h gate.
    - `grep -c "asyncio.create_task(run_batch_prewarm())\|asyncio.create_task(poll_pending_batches())" backend/app/connectors/scheduler.py` returns 2 (BOTH dispatched via create_task with NO client arg — Pitfall 3 + T-24-19).
    - `grep -c "await run_batch_prewarm\|await poll_pending_batches" backend/app/connectors/scheduler.py` returns 0 (no inline await — D-05).
    - `grep -c "AsyncAnthropic" backend/app/connectors/scheduler.py` returns 0 (the scheduler builds no client — each batch function resolves per-tenant keys itself — T-24-19).
    - `grep -n "86400" backend/app/connectors/scheduler.py` shows the nightly gate uses the 24h threshold; `grep -n "ai_batch_prewarm_dispatch_error\|ai_batch_poll_dispatch_error" backend/app/connectors/scheduler.py` both present (per-block try/except).
    - Tests prove: non-blocking dispatch (returns before the slow fake finishes), 24h gating, dispatch-exception containment, and no-client dispatch.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_scheduler_ai_batch.py -q</automated>
  </verify>
  <done>The scheduler dispatches the nightly submit (24h-gated) and the every-tick poll, both via asyncio.create_task with no client argument, neither ever stalling a connector-sync tick — the batch feature is live and each batch resolves its own tenant key internally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Submitted batch (pre-restart) → poller (post-restart) | In-flight batches must be re-findable from Postgres, not lost with the process |
| AiBatchJob row (owning tenant) → per-job Anthropic key | Each job's `retrieve()`/`results()` must use that job's OWNING tenant's own key, resolved fresh per job; no shared/fallback key, no cross-tenant reuse |
| Batch result → tenant cache | A result must be written under the owning tenant's key only, using the frozen model/prompt_version it was submitted with |
| Nightly/poll dispatch → connector-sync tick | Batch work must never block the scheduler tick (D-05 hard constraint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-19 | Information Disclosure / Elevation (BYOK cross-tenant key leakage) | `poll_pending_batches()` per-job client construction + scheduler dispatch | mitigate | The poller takes NO shared client: per `AiBatchJob` row it resolves `key = await get_tenant_anthropic_key(db, job.tenant_id)` and builds a FRESH `(anthropic_client_factory or _default_client_factory)(key)` before that job's `retrieve()`/`results()` — the SAME per-tenant-fresh-client contract explain.py enforces (lines 15-17, 115-121). A key rotated away (None) skips the job rather than retrieving with any other key. The scheduler builds no client at all (`grep -c "AsyncAnthropic"` == 0). `test_poll_uses_per_tenant_key` (two jobs, two distinct keys) + `test_scheduler_builds_no_client` are the regression guards. |
| T-26-12 | Denial of Service (tick stall) | scheduler blocks A + B | mitigate | BOTH `run_batch_prewarm()` and `poll_pending_batches()` are dispatched via `asyncio.create_task`, never an inline `await` (Pitfall 3 — the poller is the easy one to get wrong). The non-blocking test asserts the block returns before a slow fake finishes; the grep gate proves zero inline awaits. |
| T-26-08 | Denial of Service (orphaned spend) | `poll_pending_batches()` resume | mitigate | The poller SELECTs `AiBatchJob WHERE status='in_progress'` from Postgres every call, so a batch submitted before a restart is still retrieved — the durability of Plan 06's table is realized here. Test seeds an in_progress row directly and asserts it is completed. |
| T-26-03 | Information Disclosure (cross-tenant cache) | poller cache write | mitigate | The cache key is built from the AiBatchJob row's own `tenant_id` (one batch = one tenant, set at submit) + `custom_id_hash_map[custom_id]` — a result can never be attributed to a different tenant than the batch it came from; the frozen model/prompt_version guarantee the analyst's GET finds it (D-06). |
| T-26-04 | Tampering (accounted-spend accuracy) | poller → `validate_and_cache_batch_result()` | mitigate | The poller books cost only through Plan 07's validator, which applies the ×0.5 discount; errored/canceled/expired are audited at cost 0.0 (never billed) and never cached. |
</threat_model>

<verification>
- `cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_batch.py tests/test_scheduler_ai_batch.py -q` green.
- Wave-merge / phase-gate regression: `... python -m pytest tests/test_ai_*.py tests/test_scheduler_ai_batch.py -q` green.
</verification>

<success_criteria>
The batch feature is live end-to-end: the scheduler submits one Message Batch per tenant nightly (24h-gated) and polls to completion every tick, both non-blocking and dispatched with no client argument; the poller resumes any in-flight batch from Postgres across a restart, retrieves each with its owning tenant's OWN freshly-constructed BYOK key, and writes each narrative into the same tenant cache the drill panel reads — SC3 satisfied (bulk via Batches API on schedule, asyncio.create_task, each tenant's own key).
</success_criteria>

<output>
After completion, create `.planning/phases/26-prioritization-narrative/26-08-SUMMARY.md`.
</output>
</content>
