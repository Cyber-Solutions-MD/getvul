---
phase: 26-prioritization-narrative
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - backend/app/ai/prompt_builder.py
  - backend/tests/test_ai_prompt_builder_prioritization.py
autonomous: true
requirements: [AIP-01]
must_haves:
  truths:
    - "PRIORITIZATION_ALLOWLIST is exactly the 10 fields {cve_id, cvss_v3_score, epss_score, exploit_available, cisa_kev, exploit_status_name, severity, sla_due_at, sla_breached, department} — no owner-PII field ever appears (D-04/D-15, threat T-26-01)"
    - "AllowlistedPrioritization has model_config extra='forbid' and is built field-by-field via _get_field — a raw PII-bearing row cannot smuggle an extra key through (threat T-26-01)"
    - "SYSTEM_PROMPT_PRIORITIZATION instructs the model to explain the deterministic score's drivers (KEV/exploit/SLA/severity/department) and NEVER assert an independent verdict or invent a number (D-08/D-03 as prompt text)"
    - "build_explain_prioritization_prompt(record) feeds the allowlisted factors as <scanner_data source=\"prioritization\"> data, never as free reasoning (AIP-01/SC1 untrusted-content-as-data contract inherited)"
    - "prioritization_prompt_version() reuses the shared prompt_version(system_prompt, few_shot, response_model) hasher — a stable, distinct-from-other-views hash"
  artifacts:
    - path: "backend/app/ai/prompt_builder.py"
      provides: "PRIORITIZATION_ALLOWLIST, AllowlistedPrioritization, _to_allowlisted_prioritization, SYSTEM_PROMPT_PRIORITIZATION, FEW_SHOT_PRIORITIZATION, build_explain_prioritization_prompt, prioritization_prompt_version"
      contains: "PRIORITIZATION_ALLOWLIST"
    - path: "backend/tests/test_ai_prompt_builder_prioritization.py"
      provides: "10-field allowlist + owner-PII exclusion (dict + attribute-object) + prompt-version stability/distinctness tests"
      contains: "prioritization"
  key_links:
    - from: "build_explain_prioritization_prompt()"
      to: "AllowlistedPrioritization (extra='forbid')"
      via: "_to_allowlisted_prioritization(record) then model_dump into <scanner_data source=\"prioritization\">"
      pattern: "source=\"prioritization\""
    - from: "prioritization_prompt_version()"
      to: "prompt_version(SYSTEM_PROMPT_PRIORITIZATION, FEW_SHOT_PRIORITIZATION, ExplainPrioritizationResponse)"
      via: "shared generalized hasher"
      pattern: "prioritization_prompt_version"
---

<objective>
Add the prompt-builder half of the 5th quadruplet: the `PRIORITIZATION_ALLOWLIST` frozenset, the `AllowlistedPrioritization` mass-assignment-safe model, its field-by-field constructor, the system prompt + two few-shot exemplars, the prompt builder, and the version hasher — mirroring Phase 25's `remediation-guidance` quadruplet almost line-for-line. The system prompt is the one place D-03/D-08's "explain drivers, never assert a verdict or a number" product intent becomes literal prompt text.

Purpose: This completes the grounding→prompt contract so the on-demand route (Plan 03) and the batch submitter (Plans 07-08) both call the identical `build_explain_prioritization_prompt()` — one prompt/schema shared between on-demand and batch (CONTEXT default, confirmed viable).
Output: the 7 new prompt-builder symbols + their dedicated test file (10-field allowlist, owner-PII exclusion from both dict and attribute-object inputs, prompt-version stability/distinctness).
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-prioritization-narrative/26-CONTEXT.md
@.planning/phases/26-prioritization-narrative/26-RESEARCH.md
@.planning/phases/26-prioritization-narrative/26-PATTERNS.md

<interfaces>
From Plan 01 (backend/app/ai/schemas.py): class ExplainPrioritizationResponse(ExplainResponseBase)
Existing (reused unchanged, backend/app/ai/prompt_builder.py):
  _get_field(record, name) — shared Mapping-or-attribute reader (lines ~109-118)
  _render_few_shot(few_shot) — renders exemplars into the system prompt
  prompt_version(system_prompt, few_shot, response_model) -> str — the shared generalized hasher (lines ~317-344)
  REMEDIATION_GUIDANCE quadruplet (lines ~786-1011) — the exact analog: REMEDIATION_GUIDANCE_ALLOWLIST, AllowlistedRemediationGuidance (model_config={"extra":"forbid"}, every field | None = None), _to_allowlisted_remediation_guidance, SYSTEM_PROMPT_REMEDIATION_GUIDANCE, FEW_SHOT_REMEDIATION_GUIDANCE, build_explain_remediation_guidance_prompt, remediation_guidance_prompt_version
</interfaces>
</context>

<artifacts_this_phase_produces>
This plan contributes the prompt-builder quadruplet member: `PRIORITIZATION_ALLOWLIST`, `AllowlistedPrioritization`, `_to_allowlisted_prioritization()`, `SYSTEM_PROMPT_PRIORITIZATION`, `FEW_SHOT_PRIORITIZATION`, `build_explain_prioritization_prompt()`, `prioritization_prompt_version()`. Consumed by Plan 03 (on-demand route) and Plans 07-08 (batch — same builder, no variant).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PRIORITIZATION_ALLOWLIST + AllowlistedPrioritization + _to_allowlisted_prioritization (PII-safe)</name>
  <read_first>
    - `.planning/phases/26-prioritization-narrative/26-PATTERNS.md` §"backend/app/ai/prompt_builder.py — add the 5th allowlist+prompt-builder quadruplet" (the exact `REMEDIATION_GUIDANCE_ALLOWLIST` declaration at lines 799-814, the `AllowlistedRemediationGuidance` + `_to_allowlisted_remediation_guidance` shape at lines 817-863, the note that none of the 10 prioritization fields need `_truncate()` — they are all short bounded strings/scalars, unlike remediation free text).
    - `backend/app/ai/prompt_builder.py` lines 109-118 (`_get_field`), lines 799-863 (the allowlist + Allowlisted model + constructor analog).
    - `backend/tests/test_ai_prompt_builder_remediation_guidance.py` (198 lines) — the exact test names to mirror: `test_remediation_guidance_allowlist_has_12_fields_no_owner_pii`, `test_allowlist_enforcement_excludes_owner_pii_from_dict`, `test_allowlist_enforcement_excludes_owner_pii_from_attribute_object`.
  </read_first>
  <action>
    Add to `backend/app/ai/prompt_builder.py` (alongside the REMEDIATION_GUIDANCE block): `PRIORITIZATION_ALLOWLIST: frozenset[str] = frozenset({"cve_id", "cvss_v3_score", "epss_score", "exploit_available", "cisa_kev", "exploit_status_name", "severity", "sla_due_at", "sla_breached", "department"})` (exactly 10 fields — D-04's 8 factors + `cve_id` + `department`; NO owner-PII). Add `class AllowlistedPrioritization(BaseModel)` with `model_config = {"extra": "forbid"}` and every one of the 10 fields typed `| None = None` (use the model columns' natural types — `cve_id`/`severity`/`exploit_status_name`/`department` as `str | None`, `cvss_v3_score`/`epss_score` as `float | None`, `exploit_available`/`cisa_kev`/`sla_breached` as `bool | None`, `sla_due_at` as `datetime | None`). Add `_to_allowlisted_prioritization(record: Any) -> AllowlistedPrioritization` building each field via `_get_field(record, "<name>")` (NEVER a dict-spread passthrough — this is the mass-assignment defense). No `_truncate()` calls (none of the 10 fields are unbounded free text). Write `backend/tests/test_ai_prompt_builder_prioritization.py` mirroring the remediation-guidance test file: (a) `test_prioritization_allowlist_has_10_fields_no_owner_pii` asserting `len(PRIORITIZATION_ALLOWLIST) == 10` and that `assigned_user`/`managed_by`/`building`/`serial_number` are NOT in the frozenset; (b) `test_allowlist_enforcement_excludes_owner_pii_from_dict` — hand `_to_allowlisted_prioritization()` a dict containing extra PII keys and assert those keys do not appear on the resulting model (extra='forbid' + field-by-field construction drops them); (c) `test_allowlist_enforcement_excludes_owner_pii_from_attribute_object` — same via a fake attribute-bearing object.
  </action>
  <acceptance_criteria>
    - `grep -n "PRIORITIZATION_ALLOWLIST" backend/app/ai/prompt_builder.py` present; the frozenset literal has exactly 10 entries.
    - `grep -c "assigned_user\|managed_by\|serial_number" backend/app/ai/prompt_builder.py` in the PRIORITIZATION block region returns 0.
    - `grep -n "class AllowlistedPrioritization" backend/app/ai/prompt_builder.py` shows `model_config = {"extra": "forbid"}` on the class.
    - Tests prove owner-PII is excluded from both dict and attribute-object inputs.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_prompt_builder_prioritization.py -k "allowlist or pii" -q</automated>
  </verify>
  <done>The allowlist is exactly 10 non-PII fields; the Allowlisted model forbids extras and is built field-by-field; owner-PII exclusion is proven for both input shapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SYSTEM_PROMPT_PRIORITIZATION + FEW_SHOT + builder + version hasher</name>
  <read_first>
    - `.planning/phases/26-prioritization-narrative/26-PATTERNS.md` §"System prompt + few-shot + builder + version functions" (lines 956-1011 of prompt_builder.py to mirror; the note that `SYSTEM_PROMPT_PRIORITIZATION` must state D-08's "explain drivers, never assert a verdict or invent a number" instruction in prompt text; `source="prioritization"` on the `<scanner_data>` tag; `FEW_SHOT_PRIORITIZATION` needs a 2nd exemplar demonstrating `grounded: false` on a too-sparse factor set).
    - `backend/app/ai/prompt_builder.py` lines 317-344 (`prompt_version` — takes `response_model`, reused unchanged), lines 956-1011 (the SYSTEM_PROMPT/FEW_SHOT/build/version analog for remediation-guidance), and the `untrusted_content_policy` block every SYSTEM_PROMPT_* includes.
    - `.planning/phases/26-prioritization-narrative/26-UI-SPEC.md` §"No-Rank UI Contract" (the copy that asserts a verdict is FORBIDDEN — D-08 language: "ranked high because it's KEV-listed, has a public exploit, and its SLA is breached").
    - From Plan 01: `ExplainPrioritizationResponse` (the version hasher's `response_model` argument).
  </read_first>
  <behavior>
    - `build_explain_prioritization_prompt(record)` returns `(SYSTEM_PROMPT_PRIORITIZATION, [{"type":"text","text": '<scanner_data source="prioritization">{...}</scanner_data>'}])` with the allowlisted factors JSON-serialized inside the tag.
    - `prioritization_prompt_version()` returns a stable string across calls AND a string DISTINCT from `host_prompt_version()`/`remediation_guidance_prompt_version()` (proves the hash is sensitive to the different system prompt/few-shot/response_model).
    - The system prompt text contains an explicit instruction to explain the deterministic score's drivers and to NEVER assert an independent priority verdict or output a number.
  </behavior>
  <action>
    Add `SYSTEM_PROMPT_PRIORITIZATION` (include the shared `<untrusted_content_policy>` block and `_render_few_shot(FEW_SHOT_PRIORITIZATION)`, mirroring `SYSTEM_PROMPT_REMEDIATION_GUIDANCE`'s structure). Its own instruction text must state, in the assistant's own words: explain WHY the deterministic risk score is what it is by naming the driving factors (KEV listing, public exploit, EPSS, CVSS, SLA breach, severity, owning department); NEVER assert an independent verdict, NEVER claim the finding is more/less urgent than its score, NEVER output a rank or priority number (D-03/D-08 — this is the prompt-text half of the No-Rank contract). Add `FEW_SHOT_PRIORITIZATION` with two exemplars: exemplar 1 a well-grounded driver explanation (`grounded: true`, cited factors); exemplar 2 a too-sparse factor set (no CVSS/EPSS/exploit/KEV signal) demonstrating `grounded: false` (the insufficient-signal backstop, matching every other view's 2-exemplar convention). Add `build_explain_prioritization_prompt(record: Any) -> tuple[str, list[dict[str, str]]]` calling `_to_allowlisted_prioritization(record)`, `json.dumps(model.model_dump())`, wrapping in `<scanner_data source="prioritization">...</scanner_data>`. Add `prioritization_prompt_version() -> str` returning `prompt_version(SYSTEM_PROMPT_PRIORITIZATION, FEW_SHOT_PRIORITIZATION, ExplainPrioritizationResponse)`. Extend `backend/tests/test_ai_prompt_builder_prioritization.py`: (a) `test_prioritization_prompt_version_is_stable` (two calls equal); (b) `test_prioritization_prompt_version_distinct_from_other_views` (differs from `host_prompt_version()` and `remediation_guidance_prompt_version()`); (c) a test asserting `build_explain_prioritization_prompt()` emits `source="prioritization"` and includes the allowlisted factor values but NOT any PII value; (d) a test asserting the system prompt text contains a no-verdict/no-number instruction (grep the constant for the driver-explanation directive).
  </action>
  <acceptance_criteria>
    - `grep -n "SYSTEM_PROMPT_PRIORITIZATION\|FEW_SHOT_PRIORITIZATION\|def build_explain_prioritization_prompt\|def prioritization_prompt_version" backend/app/ai/prompt_builder.py` shows all four.
    - `grep -n 'source="prioritization"' backend/app/ai/prompt_builder.py` present.
    - Test proves prompt-version stability AND distinctness from host/remediation-guidance versions.
    - Test proves the system prompt forbids asserting a verdict/number (D-08/D-03 prompt-text enforcement).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_prompt_builder_prioritization.py -q</automated>
  </verify>
  <done>The full prioritization prompt-builder quadruplet exists, feeds factors as data with a source="prioritization" tag, encodes D-08/D-03 in prompt text, and produces a stable-and-distinct version hash — shared verbatim by the on-demand and batch paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grounding dict / raw row → AllowlistedPrioritization → prompt | Only allowlisted factors may reach the model; extras (incl. PII) forbidden |
| Scanner text fields → model | Untrusted content must be framed as data, not instructions (inherited) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information Disclosure | `AllowlistedPrioritization` / `_to_allowlisted_prioritization` | mitigate | `model_config={"extra":"forbid"}` + field-by-field `_get_field` construction: even a raw PII-bearing row cannot inject an extra key into the prompt. Tests prove exclusion from dict + attribute inputs. |
| T-26-07 | Tampering (prompt injection) | `SYSTEM_PROMPT_PRIORITIZATION` / `build_explain_prioritization_prompt` | mitigate | Inherits the shared `<untrusted_content_policy>` + `<scanner_data source="prioritization">` data-framing; scanner text is data, never instructions (verbatim reuse of the phase-24 contract). |
| T-26-02 | Product-integrity (Pitfall #7) | `SYSTEM_PROMPT_PRIORITIZATION` | mitigate | Prompt text explicitly forbids asserting an independent verdict or emitting any rank/priority number — the prompt-text half of the No-Rank contract (schema half in Plan 01, UI half in Plan 04). |
</threat_model>

<verification>
- `cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_prompt_builder_prioritization.py -q` green.
- Wave-merge regression: `... python -m pytest tests/test_ai_*.py -q` green.
</verification>

<success_criteria>
The prioritization prompt-builder quadruplet is complete and test-proven: 10-field non-PII allowlist, mass-assignment-safe model, D-08/D-03 encoded as prompt text, and a stable/distinct version hash — ready for both the on-demand route and the batch path to call unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/26-prioritization-narrative/26-02-SUMMARY.md`.
</output>
