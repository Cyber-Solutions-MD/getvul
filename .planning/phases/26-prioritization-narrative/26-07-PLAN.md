---
phase: 26-prioritization-narrative
plan: 07
type: execute
wave: 7
depends_on: [26-06]
files_modified:
  - backend/app/vulnerabilities/service.py
  - backend/app/ai/budget.py
  - backend/app/redis_client.py
  - backend/app/main.py
  - backend/app/ai/batch.py
  - backend/tests/test_top_findings_for_ai_batch.py
  - backend/tests/test_ai_budget.py
  - backend/tests/test_ai_batch.py
autonomous: true
requirements: [AIP-02]
must_haves:
  truths:
    - "`get_top_findings_for_ai_batch(db, tenant_id, limit)` returns the tenant's top-N OPEN+IN_PROGRESS findings ordered by `Asset.risk_score DESC` (the literal ASSET-02 number, Assumption A1) with a KEV→CVSS→SLA per-finding tiebreak, respecting `limit`, `nulls_last` on asset-less findings, excluding REMEDIATED/SUPPRESSED/FALSE_POSITIVE (D-01, threat T-26-03)"
    - "A non-request `get_redis_client()` factory exists so `batch.py` can obtain a Redis client outside FastAPI's `get_redis(request)` path; `main.py`'s lifespan builds `app.state.redis` via the SAME factory (PATTERNS Redis-seam gap resolved, single construction site)"
    - "`estimate_batch_cost_usd()` sums `client.messages.count_tokens()` input tokens + worst-case output (len×MAX_TOKENS) at the model's rate, then applies ×0.5 — the 50% batch discount (RESEARCH #4/Pitfall 4, threat T-26-04)"
    - "`run_batch_prewarm()` loops active tenants, skips tenants with no key (D-23 parity), skips findings whose (hash,model,prompt_version) cache key is already a hit (D-01 freshness, Pitfall 5), pre-estimates cost, and FAILS CLOSED via `would_exceed_budget_for_batch()` — a would-breach batch is skipped + admin-alerted + audited `batch_skipped_budget_exceeded`, never a silent partial (D-07, threat T-26-11)"
    - "On submit, `run_batch_prewarm()` calls `client.messages.batches.create()` reusing the interactive `_build_output_config()` unchanged, then INSERTs an `AiBatchJob` row (status in_progress, model+prompt_version frozen, custom_id_hash_map) synchronously before returning — one batch per tenant, one tenant's key (threat T-26-06/T-26-08)"
    - "`validate_and_cache_batch_result()` is a single-pass validator (schema → recheck_business_rules → grounded → leak-marker → set_cached), NO retry loop, booking cost at ×0.5 for `ok`; errored/canceled/expired never reach it (RESEARCH #8, threat T-26-04)"
  artifacts:
    - path: "backend/app/vulnerabilities/service.py"
      provides: "get_top_findings_for_ai_batch() — D-01 top-N join query"
      contains: "def get_top_findings_for_ai_batch"
    - path: "backend/app/ai/budget.py"
      provides: "get_month_to_date_spend() (extracted) + would_exceed_budget_for_batch() (D-07 pre-submission fail-closed)"
      contains: "def would_exceed_budget_for_batch"
    - path: "backend/app/redis_client.py"
      provides: "get_redis_client() non-request factory"
      contains: "def get_redis_client"
    - path: "backend/app/ai/batch.py"
      provides: "estimate_batch_cost_usd(), run_batch_prewarm(), validate_and_cache_batch_result()"
      contains: "async def run_batch_prewarm"
  key_links:
    - from: "run_batch_prewarm()"
      to: "would_exceed_budget_for_batch() then client.messages.batches.create() then AiBatchJob INSERT"
      via: "fail-closed budget gate → submit → durable-row persist (synchronous, before return)"
      pattern: "would_exceed_budget_for_batch"
    - from: "estimate_batch_cost_usd() / validate_and_cache_batch_result()"
      to: "the 50% batch discount"
      via: "× 0.5 applied to _estimate_cost_usd / the pricing math at every batch cost site"
      pattern: "0.5"
    - from: "run_batch_prewarm() per-finding"
      to: "get_cached() skip-if-fresh"
      via: "record_hash + build_cache_key + get_cached, include only misses (D-01 freshness, Pitfall 5)"
      pattern: "get_cached"
---

<objective>
Build the batch submitter: the D-01 top-N query, the budget pre-estimate helpers, the non-request Redis factory, and the composite `batch.py` (cost estimate at the 50% batch discount, `run_batch_prewarm()` submission orchestrator with fail-closed budget + freshness skip + durable `AiBatchJob` INSERT, and the single-pass `validate_and_cache_batch_result()`). This is RESEARCH's composite new module — no single analog; three existing shapes borrowed (the interactive validation gate, the own-session background task, the audit-only refusal).

Purpose: Deliver the submit path + the per-item result validator so Plan 08's poller has a `validate_and_cache_batch_result()` to call and the scheduler has a `run_batch_prewarm()` to dispatch. Nothing is wired into the scheduler yet — `run_batch_prewarm()`/`validate_and_cache_batch_result()` are directly test-invoked with a fake Anthropic client (the batch feature does not go live until Plan 08).
Output: `get_top_findings_for_ai_batch()`, the budget pre-estimate helpers, `get_redis_client()`, and `batch.py`'s three functions + their tests.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-prioritization-narrative/26-CONTEXT.md
@.planning/phases/26-prioritization-narrative/26-RESEARCH.md
@.planning/phases/26-prioritization-narrative/26-PATTERNS.md

<interfaces>
From Plan 06 (backend/app/ai/models.py): class AiBatchJob (tenant_id, anthropic_batch_id, status, model, prompt_version, custom_id_hash_map, submitted_at, ended_at).
From Plan 01 (grounding.py): get_prioritization_context(db, tenant_id, finding_id) -> dict | None.
From Plan 02 (prompt_builder.py): build_explain_prioritization_prompt(record), prioritization_prompt_version(), PRIORITIZATION_ALLOWLIST.
From Plan 01/02 (schemas.py): ExplainPrioritizationResponse, recheck_business_rules(candidate, allowed_source_fields=...), BusinessRuleError.
Reused UNCHANGED (backend/app/ai/explain.py): MAX_TOKENS=1024 (line 80), _PRICING_PER_MTOK_USD (line 98), _DEFAULT_PRICING_PER_MTOK_USD (line 103), _build_output_config(response_model, model) (line 138), _contains_leak_marker(candidate, system_prompt) (line 166), _estimate_cost_usd(model, usage) (line 185).
Reused UNCHANGED: cache.py build_cache_key(tenant_id, resource_type, resource_id, hash, model, prompt_version) / record_hash(fields) / get_cached(redis, key) / set_cached(redis, key, payload); tenant_keys.py get_tenant_anthropic_key(db, tenant_id) -> str | None (None = inert, D-23); audit.py audit_log_ai_call(db, tenant_id, user_email="system:scheduler", model, usage, resource_type, resource_id, status, cost_estimate_usd); budget.py notify_admins_budget_exceeded(db, tenant_id).
Existing budget.py check_tenant_budget() (lines 41-65) — its month_start + func.sum(AuditLog.details["cost_estimate_usd"]) block is the code to EXTRACT into get_month_to_date_spend().
Vulnerabilities service.py: imports `from sqlalchemy import Select, asc, case, desc, func, nulls_last, or_, select, update` already at line 9; the sort="triage" branch (lines 92-99) is the KEV→CVSS→SLA tiebreak idiom. Asset.risk_score (Integer|None) is already a live sort key in assets/service.py::list_assets() line 70.
VulnStatus enum values: OPEN, IN_PROGRESS, REMEDIATED, SUPPRESSED, FALSE_POSITIVE. "OPEN" (D-01) = OPEN + IN_PROGRESS (matches risk_score.py's own scoring input).
main.py lifespan builds app.state.redis via redis.Redis(connection_pool=redis.BlockingConnectionPool.from_url(settings.redis_url, decode_responses=True, socket_timeout=2.0, ...)) at lines 118-126.
Anthropic Batches (anthropic==0.120.2): client.messages.batches.create(requests=[Request(custom_id=str(finding_id), params=MessageCreateParamsNonStreaming(model, max_tokens, temperature=0, system, messages, output_config))]); client.messages.count_tokens(model, system, messages) -> .input_tokens. custom_id regex ^[a-zA-Z0-9_-]{1,64}$ (a bare UUID is safe).
own-session pattern: from app.db.session import async_session_factory (plain importable callable).
</interfaces>
</context>

<artifacts_this_phase_produces>
This plan contributes: `get_top_findings_for_ai_batch()` (vulnerabilities/service.py), `get_month_to_date_spend()` + `would_exceed_budget_for_batch()` (budget.py), `get_redis_client()` (redis_client.py, main.py rewired to use it), and `batch.py`'s `estimate_batch_cost_usd()` + `run_batch_prewarm()` + `validate_and_cache_batch_result()`. Downstream: Plan 08's `poll_pending_batches()` calls `validate_and_cache_batch_result()`; Plan 08's scheduler block dispatches `run_batch_prewarm()`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get_top_findings_for_ai_batch() — the D-01 top-N query</name>
  <read_first>
    - `.planning/phases/26-prioritization-narrative/26-PATTERNS.md` §"backend/app/vulnerabilities/service.py — add get_top_findings_for_ai_batch()" (the recommended query verbatim; the `sort="triage"` tiebreak analog at lines 92-99; the `Asset.risk_score.desc().nullslast()` idiom from list_assets() line 70; the OPEN+IN_PROGRESS convention confirmed in THREE files; the "use this file's `nulls_last(desc(col))` function-style, not the method-style" note) and §"Corroboration #4" (the test home is `test_triage_sort.py`, NOT the nonexistent `test_vulnerabilities_service.py`).
    - `.planning/phases/26-prioritization-narrative/26-RESEARCH.md` §"Pattern 3" + Assumption A1 (ASSET-02 is per-ASSET `Asset.risk_score`, NOT a per-finding column; the join + `Asset.risk_score` primary + per-finding tiebreak interpretation) and Open Question #2 (no per-asset cap in the first cut).
    - `backend/app/vulnerabilities/service.py` lines 1-99 (the imports line 9 confirming `nulls_last`/`desc`/`asc` are available; the `sort="triage"` branch to mirror).
    - `backend/tests/test_triage_sort.py` (whole file, 95 lines) — the `_seed_vuln()`-style helper + DB-seeded ordering-assertion structure to mirror.
  </read_first>
  <behavior>
    - Seed findings across two assets with different `Asset.risk_score` values (and one asset-less finding); `get_top_findings_for_ai_batch(db, tenant_a, limit=N)` returns finding ids ordered by `Asset.risk_score DESC` first, then KEV desc → CVSS desc → SLA-due asc as a per-finding tiebreak.
    - The asset-less finding (`asset_id IS NULL` → `Asset.risk_score` NULL) sorts LAST (`nulls_last`), never crowding out scored findings.
    - `limit` is respected (returns at most N).
    - REMEDIATED / SUPPRESSED / FALSE_POSITIVE findings are excluded; OPEN and IN_PROGRESS are included.
    - The result is tenant-scoped (tenant_b's findings never appear for tenant_a).
  </behavior>
  <action>
    Add `async def get_top_findings_for_ai_batch(db: AsyncSession, tenant_id: uuid.UUID, limit: int) -> list[uuid.UUID]` to `backend/app/vulnerabilities/service.py`, implementing RESEARCH Pattern 3's recommended query: `select(Vulnerability.id).outerjoin(Asset, Vulnerability.asset_id == Asset.id).where(Vulnerability.tenant_id == tenant_id, Vulnerability.status.in_(["OPEN", "IN_PROGRESS"])).order_by(nulls_last(desc(Asset.risk_score)), desc(Vulnerability.cisa_kev), nulls_last(desc(Vulnerability.cvss_v3_score)), nulls_last(asc(Vulnerability.sla_due_at))).limit(limit)`, returning `[row[0] for row in result.all()]`. Use this file's already-imported function-style `nulls_last(desc(col))` (NOT the method-style `col.desc().nullslast()`). Give it a docstring stating the Assumption A1 interpretation explicitly as a checkable contract: "top-N ranked by the ASSET-02 per-asset `Asset.risk_score` (primary), KEV→CVSS→SLA per-finding tiebreak; 'OPEN' = status IN (OPEN, IN_PROGRESS) to match risk_score.py's own scoring input; no per-asset cap in this cut (Open Question #2, deferred)." Create `backend/tests/test_top_findings_for_ai_batch.py` following `test_triage_sort.py`'s seed-and-assert convention (seed `Asset.risk_score` on the assets): assert the primary-by-risk_score ordering, the KEV/CVSS/SLA tiebreak within equal risk_score, `nulls_last` for the asset-less finding, `limit` respected, REMEDIATED/SUPPRESSED/FALSE_POSITIVE excluded, and tenant-scoping.
  </action>
  <acceptance_criteria>
    - `grep -n "def get_top_findings_for_ai_batch" backend/app/vulnerabilities/service.py` present.
    - `grep -n "nulls_last(desc(Asset.risk_score))" backend/app/vulnerabilities/service.py` present (ASSET-02 primary sort, Assumption A1).
    - `grep -n 'status.in_(\["OPEN", "IN_PROGRESS"\]\|status.in_' backend/app/vulnerabilities/service.py` shows the OPEN+IN_PROGRESS set in the new function.
    - Test asserts risk_score-primary ordering + tiebreak + nulls_last + limit + status-exclusion + tenant-scope.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_top_findings_for_ai_batch.py -q</automated>
  </verify>
  <done>The D-01 top-N query ranks by Asset.risk_score with a KEV/CVSS/SLA tiebreak, tolerates asset-less findings, respects limit + status filter + tenant scope, and is test-proven — the batch-scope selector.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: budget pre-estimate helpers + non-request Redis factory</name>
  <read_first>
    - `.planning/phases/26-prioritization-narrative/26-PATTERNS.md` §"backend/app/ai/budget.py" (the refactor: EXTRACT `check_tenant_budget()`'s month_start+SUM block into `get_month_to_date_spend()` so the public signature/behavior of `check_tenant_budget()` and its every existing caller are unchanged; ADD `would_exceed_budget_for_batch()` as a genuinely new fail-closed pre-check) and §"No Analog Found / Flagged Gaps → Scheduler-context Redis client access" (the confirmed structural gap: `redis_client.py` exposes only `get_redis(request)`; `app.state.redis` is built in main.py AFTER start_scheduler(); pick option (a) — a small `get_redis_client()` factory both main.py's lifespan and batch.py call).
    - `.planning/phases/26-prioritization-narrative/26-RESEARCH.md` §"Pattern 6" (the exact `get_month_to_date_spend`/`would_exceed_budget_for_batch` recommendations; `would_exceed_budget_for_batch` returns True = SKIP when `spent + estimated_batch_cost >= cap`; `monthly_cap_usd is None` → never skip).
    - `backend/app/ai/budget.py` lines 41-108 (`check_tenant_budget` to refactor; `notify_admins_budget_exceeded` — reused verbatim, do not touch).
    - `backend/app/redis_client.py` (whole 13-line file) and `backend/app/main.py` lines 108-152 (the lifespan block that builds `app.state.redis` and the `settings.redis_url`/`BlockingConnectionPool` construction to move behind the factory).
    - `backend/tests/test_ai_budget.py` (the existing budget-guard test structure to extend).
  </read_first>
  <behavior>
    - `get_month_to_date_spend(db, tenant_a)` returns the summed `cost_estimate_usd` of this month's `ai.%` audit rows; `check_tenant_budget()` keeps its exact prior boolean behavior (delegates to the helper) — a regression test on an existing caller shape stays green.
    - `would_exceed_budget_for_batch(db, tenant, cap=None, est)` → False (never skip when no cap); with a cap, → True iff `spent + est >= cap`, False otherwise.
    - `get_redis_client()` returns a `redis.Redis` built from `settings.redis_url` with the SAME `BlockingConnectionPool` options main.py used; main.py's lifespan now assigns `app.state.redis = get_redis_client()` (same runtime behavior).
  </behavior>
  <action>
    In `backend/app/ai/budget.py`: extract the `month_start` + `func.sum(AuditLog.details["cost_estimate_usd"].as_float())` query from `check_tenant_budget()` into `async def get_month_to_date_spend(db, tenant_id) -> float` (returns the sum or 0.0); rewrite `check_tenant_budget()` to call it (keeping the exact `monthly_cap_usd is None → True` and `spent < cap` semantics — zero change to its public contract, so `explain.py` and every route caller need no edit). Add `async def would_exceed_budget_for_batch(db, tenant_id, monthly_cap_usd: float | None, estimated_batch_cost_usd: float) -> bool` returning `False` when `monthly_cap_usd is None`, else `(await get_month_to_date_spend(db, tenant_id) + estimated_batch_cost_usd) >= monthly_cap_usd` (True = fail-closed SKIP). In `backend/app/redis_client.py`: add `def get_redis_client() -> redis.Redis` constructing `redis.Redis(connection_pool=redis.BlockingConnectionPool.from_url(settings.redis_url, decode_responses=True, socket_timeout=2.0, ...))` mirroring main.py lines 118-126 verbatim (import `settings` and `redis.asyncio as redis`; keep `get_redis(request)` unchanged). In `backend/app/main.py`: replace the inline `redis.Redis(connection_pool=...)` construction in the lifespan with `app.state.redis = get_redis_client()` (import from `app.redis_client`) — single construction site, identical runtime behavior. Extend `backend/tests/test_ai_budget.py`: (a) `test_get_month_to_date_spend_sums_ai_rows`; (b) `test_check_tenant_budget_unchanged_behavior` (delegation regression); (c) `test_would_exceed_budget_for_batch_none_cap_never_skips`; (d) `test_would_exceed_budget_for_batch_skips_when_projected_over_cap` and its under-cap complement.
  </action>
  <acceptance_criteria>
    - `grep -n "def get_month_to_date_spend\|def would_exceed_budget_for_batch" backend/app/ai/budget.py` shows both; `grep -c "func.sum(AuditLog.details" backend/app/ai/budget.py` returns 1 (the SUM query lives in ONE place now, not duplicated).
    - `grep -n "def get_redis_client" backend/app/redis_client.py` present; `grep -n "get_redis_client()" backend/app/main.py` shows the lifespan uses the factory.
    - `grep -c "redis.Redis(connection_pool" backend/app/main.py` returns 0 (construction moved into the factory).
    - Tests prove the SUM helper, unchanged check_tenant_budget behavior, and would_exceed_budget_for_batch's none-cap + over/under-cap branches.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_budget.py -q</automated>
  </verify>
  <done>The budget pre-estimate is a reusable fail-closed helper, check_tenant_budget's contract is unchanged, and a non-request Redis factory exists with a single construction site — the two prerequisites batch.py needs from a scheduler context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: batch.py — cost estimate, run_batch_prewarm submit, single-pass validator</name>
  <read_first>
    - `.planning/phases/26-prioritization-narrative/26-PATTERNS.md` §"backend/app/ai/batch.py (new file) — no single exact analog, three borrowed shapes" (the three shapes: the interactive validation-gate chain from explain.py lines 392-489 for `validate_and_cache_batch_result()`; the own-session `_run_single_sync()` shape for `run_batch_prewarm()`; the audit-only refusal shape for errored/canceled/expired; the reused-verbatim list — `_estimate_cost_usd`×0.5, MAX_TOKENS, get_tenant_anthropic_key skip-if-None, build_cache_key/record_hash/get_cached/set_cached, audit_log_ai_call user_email="system:scheduler"; the active-tenant loop shape from scheduler.py lines 136-148).
    - `.planning/phases/26-prioritization-narrative/26-RESEARCH.md` §"Pattern 1" (the exact `client.messages.batches.create()` + `count_tokens()` call shape; custom_id = str(finding_id); output_config carries over unchanged from `_build_output_config()`), §"Pattern 6" (the `estimate_batch_cost_usd()` recommendation incl. the ×0.5), §"Pattern 8" (the `validate_and_cache_batch_result()` single-pass recommendation, no retry loop; errored/canceled/expired never reach it), §"System Architecture Diagram" steps 1-8 (the run_batch_prewarm flow: select tenants → key or skip → top-N → per-finding hash + cache-skip-if-fresh → build prompt → count_tokens → budget pre-check → create → INSERT AiBatchJob), Pitfalls 4 (discount) and 5 (freshness skip).
    - `backend/app/ai/explain.py` lines 80, 98-103, 138-192, 392-489 (MAX_TOKENS, pricing table, `_build_output_config`, `_contains_leak_marker`, `_estimate_cost_usd`, and the success-path validation chain to mirror once — schema validate → recheck_business_rules → grounded → leak-marker → set_cached + audit).
    - From Plans 01/02/06: `get_prioritization_context`, `build_explain_prioritization_prompt`, `prioritization_prompt_version`, `PRIORITIZATION_ALLOWLIST`, `ExplainPrioritizationResponse`, `recheck_business_rules`, `BusinessRuleError`, `AiBatchJob`; from Tasks 1/2: `get_top_findings_for_ai_batch`, `would_exceed_budget_for_batch`, `get_redis_client`.
    - `backend/tests/test_ai_audit.py::test_scheduler_audit` (the `user_email="system:scheduler"` audit contract to reuse) and `backend/tests/test_ai_budget.py` (the fake-client / budget-guard test shapes).
  </read_first>
  <behavior>
    - `estimate_batch_cost_usd(client, model, requests)` sums per-request `count_tokens().input_tokens` + `len(requests)×MAX_TOKENS` worst-case output at the model's `_PRICING_PER_MTOK_USD` rate, then multiplies by 0.5 (a batch of K identical requests costs exactly half the interactive equivalent — the discount check).
    - `run_batch_prewarm()` with a fake tenant that has NO key: skips that tenant, submits nothing (D-23 parity).
    - `run_batch_prewarm()` skips findings whose cache key is already a hit; only misses go into the submitted `requests` (freshness, Pitfall 5).
    - `run_batch_prewarm()` when the pre-estimate would breach the cap: does NOT call `batches.create()`, writes a `batch_skipped_budget_exceeded` audit row, calls `notify_admins_budget_exceeded()`, inserts NO AiBatchJob row (D-07, never a partial).
    - `run_batch_prewarm()` on the OK path: calls `client.messages.batches.create()` once for the tenant, then INSERTs exactly one `AiBatchJob` (status in_progress, model + prompt_version frozen, custom_id_hash_map={str(finding_id): factor_hash}) BEFORE returning (durability, Pitfall 2).
    - `validate_and_cache_batch_result()` on a valid grounded result: set_cached + audit `ok` with cost = `_estimate_cost_usd(model, usage) * 0.5`; on invalid/ungrounded: audit `validation_failed`, no cache write, cost 0.0; on a leak marker: `injection_flagged`, no cache write.
  </behavior>
  <action>
    Create `backend/app/ai/batch.py` with three functions. `async def estimate_batch_cost_usd(client, model, requests) -> float`: look up `input_rate, output_rate = _PRICING_PER_MTOK_USD.get(model, _DEFAULT_PRICING_PER_MTOK_USD)`; sum `(await client.messages.count_tokens(model=model, system=req_system, messages=req_messages)).input_tokens` per request; `worst_case_output = len(requests) * MAX_TOKENS`; return `round(((total_input/1e6)*input_rate + (worst_case_output/1e6)*output_rate) * 0.5, 6)` (the ×0.5 batch discount, RESEARCH Pitfall 4). `async def run_batch_prewarm(client, *, limit=50)`: open its OWN `async with async_session_factory() as db:` and obtain a Redis client via `get_redis_client()` (own-session/own-client shape, since Plan 08 dispatches this via create_task); select active tenants (mirror scheduler.py lines 136-148); per tenant: `key = await get_tenant_anthropic_key(db, tenant.id)`, `if key is None: continue` (D-23); resolve model + cap via `get_model_and_budget`; `finding_ids = await get_top_findings_for_ai_batch(db, tenant.id, limit)`; per finding build `record = await get_prioritization_context(db, tenant.id, fid)`, `system, user_blocks = build_explain_prioritization_prompt(record)`, `the_hash = record_hash(<allowlisted fields parsed from user_blocks, same as the route's _allowlisted_hash_fields>)`, `cache_key = build_cache_key(tenant.id, "prioritization", str(fid), the_hash, model, prioritization_prompt_version())`, and SKIP if `await get_cached(redis, cache_key) is not None` (freshness, Pitfall 5); accumulate the miss set into a `requests` list of `Request(custom_id=str(fid), params=MessageCreateParamsNonStreaming(model=model, max_tokens=MAX_TOKENS, temperature=0, system=system, messages=[{"role":"user","content":user_blocks}], output_config=_build_output_config(ExplainPrioritizationResponse, model)))` and a `custom_id_hash_map={str(fid): the_hash}`; if the miss set is empty, continue; `est = await estimate_batch_cost_usd(client, model, requests)`; if `await would_exceed_budget_for_batch(db, tenant.id, cap, est)`: call `notify_admins_budget_exceeded(db, tenant.id)` + `audit_log_ai_call(db, tenant_id=tenant.id, user_email="system:scheduler", model=model, usage=None, resource_type="prioritization", resource_id="batch", status="batch_skipped_budget_exceeded", cost_estimate_usd=0.0)`, commit, and continue (NO create, NO AiBatchJob — D-07 never a partial); else `batch = await client.messages.batches.create(requests=requests)` then `db.add(AiBatchJob(tenant_id=tenant.id, anthropic_batch_id=batch.id, status="in_progress", model=model, prompt_version=prioritization_prompt_version(), custom_id_hash_map=custom_id_hash_map, submitted_at=datetime.now(UTC)))` and `await db.commit()` BEFORE moving to the next tenant (durability). `async def validate_and_cache_batch_result(db, redis_client, *, tenant_id, finding_id, raw_text, model, usage, cache_key) -> str`: mirror explain.py's success-path chain ONCE, no retry — `try: candidate = ExplainPrioritizationResponse.model_validate_json(raw_text); recheck_business_rules(candidate, allowed_source_fields=PRIORITIZATION_ALLOWLIST) except (ValidationError, BusinessRuleError): status="validation_failed"` else `if not candidate.grounded: status="validation_failed"` elif `_contains_leak_marker(candidate, SYSTEM_PROMPT_PRIORITIZATION): status="injection_flagged"` else `await set_cached(redis_client, cache_key, candidate.model_dump(mode="json")); status="ok"`; `cost = _estimate_cost_usd(model, usage) * 0.5 if status == "ok" else 0.0`; `await audit_log_ai_call(db, tenant_id=tenant_id, user_email="system:scheduler", model=model, usage=usage, resource_type="prioritization", resource_id=finding_id, status=status, cost_estimate_usd=cost)`; return status. Import `_contains_leak_marker`/`_estimate_cost_usd`/`_build_output_config`/`MAX_TOKENS`/`_PRICING_PER_MTOK_USD`/`_DEFAULT_PRICING_PER_MTOK_USD` from `app.ai.explain` and `SYSTEM_PROMPT_PRIORITIZATION` from `app.ai.prompt_builder` (cross-module `_`-prefixed import is the established convention — PATTERNS). Write `backend/tests/test_ai_batch.py` injecting a FAKE Anthropic client (mirroring the existing AI test seam): `test_estimate_batch_cost_discount` (asserts the ×0.5 vs an interactive-rate baseline), `test_run_batch_prewarm_skips_keyless_tenant`, `test_run_batch_prewarm_skips_cached_fresh_finding`, `test_run_batch_prewarm_budget_skip` (no create, `batch_skipped_budget_exceeded` audit + admin notify, no AiBatchJob row), `test_run_batch_prewarm_ok_inserts_durable_registry` (AiBatchJob row present with frozen model/prompt_version + custom_id_hash_map, re-queried in a fresh session), `test_validate_and_cache_ok_books_half_cost`, `test_validate_and_cache_ungrounded_not_cached`, `test_validate_and_cache_leak_marker_flagged`.
  </action>
  <acceptance_criteria>
    - `grep -n "async def estimate_batch_cost_usd\|async def run_batch_prewarm\|async def validate_and_cache_batch_result" backend/app/ai/batch.py` shows all three.
    - `grep -c "0.5" backend/app/ai/batch.py` returns ≥2 (the discount applied at both the estimate site and the validate cost site — Pitfall 4).
    - `grep -n "get_tenant_anthropic_key\|would_exceed_budget_for_batch\|batch_skipped_budget_exceeded\|notify_admins_budget_exceeded" backend/app/ai/batch.py` all present (D-07 fail-closed path).
    - `grep -n "AiBatchJob(" backend/app/ai/batch.py` present and `grep -n "db.commit" backend/app/ai/batch.py` present (durable INSERT before return, Pitfall 2).
    - `grep -n "get_cached" backend/app/ai/batch.py` present (freshness skip, Pitfall 5); `grep -n "output_config=_build_output_config" backend/app/ai/batch.py` present (output_config reused unchanged).
    - `grep -c "range(2)\|for attempt" backend/app/ai/batch.py` returns 0 (single-pass validator, no retry loop — RESEARCH #8).
    - Tests prove: discount, keyless skip, cache-fresh skip, budget-skip (no create/no row), OK-path durable AiBatchJob insert, validator ok/ungrounded/leak branches.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_batch.py -q</automated>
  </verify>
  <done>batch.py submits one Message Batch per tenant (own key, freshness-skipped, fail-closed budget, durable AiBatchJob INSERT, 50% cost) and validates each result single-pass into the shared cache — all with a fake client, not yet scheduler-wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scheduler context (no user) → tenant's Anthropic key | Each batch uses exactly ONE tenant's own key; a tenant's key must never sign another tenant's batch |
| Pre-submission cost estimate → tenant budget cap | A batch that would breach the cap must be refused BEFORE spend, never a silent partial |
| Batch cost math → tenant accounted spend | The 50% discount must be applied consistently or the budget SUM is corrupted |
| DB row (Asset/Vuln factors) → batch prompt | Owner-PII must never reach the prompt (inherited: build_explain_prioritization_prompt is PII-safe) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06 | Elevation of Privilege / Info Disclosure (cross-tenant key) | `run_batch_prewarm()` per-tenant loop | mitigate | `get_tenant_anthropic_key(db, tenant.id)` resolves the key inside the per-tenant loop iteration; one `batches.create()` per tenant covers only that tenant's top-N; the AiBatchJob row records the owning `tenant_id` once at submit. Keyless tenants are skipped (D-23). |
| T-26-11 | Denial of Service / financial | `would_exceed_budget_for_batch()` pre-check | mitigate | Fail-closed BEFORE submission: a would-breach batch is skipped + admin-alerted + audited `batch_skipped_budget_exceeded`, never partially submitted (D-07). Test asserts no `create()` and no AiBatchJob row on the skip path. |
| T-26-04 | Tampering (accounted-spend accuracy) | `estimate_batch_cost_usd()` / `validate_and_cache_batch_result()` | mitigate | The ×0.5 batch discount is applied at BOTH the pre-estimate and the actual-cost audit site; a discount test pins the factor so a future edit that drops it fails. Consistent application keeps `check_tenant_budget()`'s SUM accurate in both directions. |
| T-26-08 | Denial of Service (orphaned spend) | AiBatchJob INSERT in `run_batch_prewarm()` | mitigate | The `AiBatchJob` row is `db.add()`'d + committed synchronously, in the same task that called `create()`, before advancing — never left only in a local variable (Pitfall 2). Test re-queries the row in a fresh session. |
| T-26-01 | Information Disclosure (owner-PII) | batch prompt | mitigate | The batch reuses `build_explain_prioritization_prompt()` (Plan 02) verbatim — the same 10-field non-PII allowlist; no PII path is added on the batch side. |
</threat_model>

<verification>
- `cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_top_findings_for_ai_batch.py tests/test_ai_budget.py tests/test_ai_batch.py -q` green.
- Wave-merge regression: `... python -m pytest tests/test_ai_*.py -q` green (confirms the budget.py refactor broke no existing caller).
</verification>

<success_criteria>
The batch submitter is complete and test-proven (fake client): the D-01 top-N selector, the fail-closed budget pre-estimate at the 50% discount, one durable-registry-backed Message Batch per tenant using that tenant's own key, freshness-skipped, and a single-pass result validator writing the shared cache — ready for Plan 08 to dispatch from the scheduler.
</success_criteria>

<output>
After completion, create `.planning/phases/26-prioritization-narrative/26-07-SUMMARY.md`.
</output>
