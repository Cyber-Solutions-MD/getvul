---
phase: 26-prioritization-narrative
plan: 05
type: execute
wave: 5
depends_on: [26-04]
files_modified: []
autonomous: false
requirements: [AIP-01]
must_haves:
  truths:
    - "The end-to-end on-demand prioritization tracer (grounding query → no-rank schema → prompt-builder quadruplet → on-demand POST/GET route → drill-panel 'Prioritization' section → cited narrative render → queued card shipped-but-dark) is verified before the batch/scheduler expansion (Plans 06-08) multiplies the surface (AIP-01/SC1)"
    - "A human (or proceed-on-trust review of the green suites) confirms a cited prioritization narrative renders as prose with NO AI rank number anywhere — the literal SC2/Pitfall #7 contract — before the async batch path is built on top of it"
    - "The queued 'being prepared' branch is confirmed present-but-dark (backend queued signal not wired until Plan 06), which is the correct state — the batch feature is not live until Plan 08"
  artifacts: []
  key_links: []
---

<objective>
TRACER GATE. Pause after the complete on-demand prioritization vertical slice (Plans 01-04) and before the AIP-02 batch/scheduler expansion (Plans 06-08), mirroring Phase 24's 24-06 and Phase 25's 25-05 tracer gates. Confirm the augment-never-replace core — a cited "what to fix first and why" narrative that renders as prose with no competing AI rank — behaves correctly end-to-end before the async Message Batches path multiplies the surface.

Purpose: Tracer-first discipline — one production-quality slice proven before expansion. SC2 (no AI rank anywhere) is the highest-consequence, costly-to-reverse contract in this phase (Pitfall #7 is owned here); this gate is the moment to catch any drift before it propagates into the batch schema and the scheduled pre-warm path.
Output: a go/no-go signal for the AIP-02 expansion (Plans 06-08).
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/26-prioritization-narrative/26-UI-SPEC.md
@.planning/phases/26-prioritization-narrative/26-VALIDATION.md
@.planning/phases/26-prioritization-narrative/26-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify the on-demand prioritization tracer end-to-end</name>
  <what-built>
    The full backend + frontend on-demand prioritization tracer, all automated-test-proven in isolation (Plans 01-04):
    - Backend: `get_prioritization_context()` (tenant-scoped, owner-PII-excluding grounding query — department only, never `assigned_user`/`managed_by`/`building`/`serial_number`), `ExplainPrioritizationResponse(ExplainResponseBase): pass` (the zero-numeric-field no-rank schema — the literal D-03/SC2 enforcement), the `PRIORITIZATION_ALLOWLIST` + `AllowlistedPrioritization` + `build_explain_prioritization_prompt()` + `prioritization_prompt_version()` quadruplet (D-08 "explain drivers, never assert a verdict or invent a number" as prompt text), and the `POST/GET /api/v1/ai/explain-prioritization/{finding_id}` route (POST require_analyst SSE via the UNCHANGED `_run_explain_stream()` engine with `resource_type="prioritization"` and NO `dangerous_pattern_check`; GET require_viewer cache-check).
    - Frontend: the new "Prioritization" drill-panel section (Phase 24 chrome + citation component reused, placed between "AI Explanation" and "Remediation (raw)"), the Analyst+ "Explain the priority" btn-secondary trigger, the Viewer-empty muted state, the signal-driven "Prioritization narrative is being prepared" queued card (Clock icon, shipped-but-dark until Plan 06 wires the backend `queued` boolean), and the `no-ai-rank` Vitest CI check.
    Automated coverage already proves the grounding PII-exclusion, the no-rank schema field count, the allowlist, the RBAC matrix, the cross-tenant 404, the cache-check, and the signal-driven queued backstop in isolation (per 26-VALIDATION.md). The live visual render is the manual-only item below.
  </what-built>
  <how-to-verify>
    The live check requires the Docker stack + a dev Anthropic key (`GETVUL_DEV_ANTHROPIC_KEY`), which was the SAME class of live item the user waived at the 24-06 and 25-05 gates. If a key + stack are available now (follow the local e2e stack setup from project memory — admin user, :3000 CORS, prod build, Firefox dark pref):
    1. `docker compose up -d`; configure a tenant Anthropic key via the connector wizard.
    2. As an ANALYST: open /vulnerabilities, click a finding row, confirm the "Prioritization" section appears BETWEEN "AI Explanation" and "Remediation (raw)" with sibling-identical chrome.
    3. Click "Explain the priority". Confirm: an "Analyzing…" state, then a plain-English narrative that EXPLAINS the score's drivers (e.g. "ranked high because it's KEV-listed, has a public exploit, and its SLA is breached") with scanner_verbatim factors tinted and ai_interpreted claims tagged — and confirm it NEVER asserts an independent priority number or rank.
    4. NO-RANK sweep (SC2, the tracer-critical contract): scan every list/table view (/vulnerabilities, dashboard, drill panel) and confirm there is NO AI-generated rank column, sort control, or numeric AI badge anywhere — the deterministic ASSET-02 score stays the ONE sortable number. (The `no-ai-rank` Vitest check enforces this in CI; confirm it is green.)
    5. Owner-PII check: confirm the narrative references the owner only as a department (e.g. "owned by Finance"), never a person's name/email.
    6. As a VIEWER: confirm the section shows muted "No prioritization narrative generated yet." with NO trigger button.
    7. Confirm the queued "being prepared" branch is present in code but dark in production (backend `queued` signal arrives in Plan 06) — this is correct; the batch feature is not live until Plan 08.
    If no key/stack is available, review the green automated suites (`tests/test_ai_grounding_prioritization.py`, `tests/test_ai_schemas.py -k prioritization`, `tests/test_ai_prompt_builder_prioritization.py`, `tests/test_ai_explain_prioritization.py`, and the frontend `ai-explanation-section` / `no-ai-rank` / `drill-panel` suites) and proceed on trust as at 24-06 / 25-05.
  </how-to-verify>
  <resume-signal>Type "approved" (or "skip live verify, proceed on trust", mirroring 24-06/25-05) to unblock Plans 06-08, or describe issues to feed a revision.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
No new symbols — this is a verification gate. Output is human sign-off (or proceed-on-trust) plus a recorded go/no-go signal consumed by the AIP-02 expansion plans (26-06 durable registry + queued signal, 26-07 batch submitter, 26-08 scheduler integration).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced by this checkpoint — it is a verification gate over the boundaries already established and mitigated in Plans 01-04 (T-26-01 owner-PII exclusion, T-26-02 no-rank product-integrity, T-26-05 RBAC, T-26-07 prompt-injection-as-data, T-26-09 signal-driven queued state). The gate's purpose is to confirm those mitigations hold end-to-end before the AIP-02 batch surface multiplies.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 | Product-integrity (Pitfall #7 / SC2) | on-demand tracer slice (whole) | mitigate | Human/automated confirmation that the narrative renders as prose with zero AI rank number/column/sort anywhere before the batch schema (Plans 06-07) is built on the same no-rank contract — the checkpoint IS the mitigation-verification step. |
| T-26-01 | Information Disclosure | grounding → prompt → rendered narrative | mitigate | Live confirmation (step 5) that the owner appears only as a department, corroborating the query-layer PII exclusion proven by Plan 01/02 tests. |
</threat_model>

<verification>
Human sign-off (or proceed-on-trust) recorded. All Plan 01-04 automated suites green.
</verification>

<success_criteria>
The on-demand prioritization tracer slice is confirmed (live or on-trust): a cited augment-never-replace narrative renders with no competing AI rank. The AIP-02 batch/scheduler expansion (Plans 06-08) is unblocked.
</success_criteria>

<output>
After resume, create `.planning/phases/26-prioritization-narrative/26-05-SUMMARY.md` recording the decision (approved / proceed-on-trust / issues).
</output>
