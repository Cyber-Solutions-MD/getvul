---
phase: 26-prioritization-narrative
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/ai/grounding.py
  - backend/app/ai/schemas.py
  - backend/tests/test_ai_grounding_prioritization.py
  - backend/tests/test_ai_schemas.py
autonomous: true
requirements: [AIP-01]
must_haves:
  truths:
    - "get_prioritization_context(db, tenant_id, finding_id) returns a tenant-scoped dict of exactly D-04's factor set + cve_id + Asset.department, or None for a missing/cross-tenant finding (AIP-01/SC1)"
    - "The SELECT never fetches Asset.assigned_user / managed_by / building / serial_number — owner-PII is excluded at the query layer, not just the prompt layer (D-04/D-15, threat T-26-01)"
    - "ExplainPrioritizationResponse has exactly the 4 ExplainResponseBase fields (summary, business_risk, citations, grounded) and ZERO numeric rank/priority/score field — the schema-level No-Rank enforcement (D-03/Pitfall #7/SC2, threat T-26-02)"
  artifacts:
    - path: "backend/app/ai/grounding.py"
      provides: "get_prioritization_context() — the 5th grounding-query member, PII-excluding, department-only owner"
      contains: "def get_prioritization_context"
    - path: "backend/app/ai/schemas.py"
      provides: "ExplainPrioritizationResponse(ExplainResponseBase): pass — no-rank schema"
      contains: "class ExplainPrioritizationResponse"
    - path: "backend/tests/test_ai_grounding_prioritization.py"
      provides: "PII-exclusion + tenant-scope + None-on-miss coverage for the grounding query"
      contains: "get_prioritization_context"
  key_links:
    - from: "get_prioritization_context() SELECT"
      to: "Vulnerability factor columns + Asset.department"
      via: "outerjoin(Asset, Vulnerability.asset_id == Asset.id), tenant-scoped where"
      pattern: "Asset.department"
    - from: "ExplainPrioritizationResponse"
      to: "ExplainResponseBase (no numeric field)"
      via: "subclass with pass — structurally cannot hold a rank number"
      pattern: "class ExplainPrioritizationResponse\\(ExplainResponseBase\\)"
---

<objective>
Add the two lowest-risk pieces of the 5th prioritization quadruplet: the tenant-scoped, owner-PII-excluding grounding query (`get_prioritization_context()`) and the zero-new-fields response schema (`ExplainPrioritizationResponse`). The schema subclass IS the literal D-03/Pitfall #7 "no AI rank" enforcement — `ExplainResponseBase` has no numeric field, so a rank number has structurally nowhere to live.

Purpose: De-risk the two grounding-contract seams (D-04 owner-PII exclusion, D-03 no-rank schema) before any prompt/route/UI is built on top of them — both are flagged "costly to reverse" in CONTEXT.
Output: `get_prioritization_context()`, `ExplainPrioritizationResponse`, and their dedicated tests (PII exclusion + no-rank field count).
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-prioritization-narrative/26-CONTEXT.md
@.planning/phases/26-prioritization-narrative/26-RESEARCH.md
@.planning/phases/26-prioritization-narrative/26-PATTERNS.md

<interfaces>
Existing (reused unchanged):
  backend/app/ai/schemas.py: class ExplainResponseBase(BaseModel) — fields: summary: str, business_risk: str, citations: list[Citation] (min_length=1), grounded: bool. NO numeric field.
  backend/app/ai/schemas.py: class ExplainRemediationGuidanceResponse(ExplainResponseBase): pass  (the exact zero-fields subclass analog, lines ~88-93)
  backend/app/ai/grounding.py: get_remediation_guidance_context(db, tenant_id, finding_id) -> dict[str, Any] | None  (the exact structural analog, lines ~264-313)
Vulnerability columns (backend/app/vulnerabilities/models.py): cve_id, cvss_v3_score, epss_score, exploit_available, cisa_kev, exploit_status_name, severity, sla_due_at, sla_breached
Asset columns (backend/app/assets/models.py): department: Mapped[str | None] (String(200)) — ALLOWED. assigned_user, managed_by, building, serial_number — FORBIDDEN (must never be SELECTed).
</interfaces>
</context>

<artifacts_this_phase_produces>
This plan contributes: `backend/app/ai/grounding.py::get_prioritization_context()` (grounding fn, department-only owner, PII-excluded) and `backend/app/ai/schemas.py::ExplainPrioritizationResponse` (the no-rank schema class). Downstream: Plan 02 (prompt-builder) imports the schema for `prioritization_prompt_version()`; Plan 03 (route) imports both.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get_prioritization_context() grounding query (owner-PII excluded)</name>
  <read_first>
    - `.planning/phases/26-prioritization-narrative/26-PATTERNS.md` §"backend/app/ai/grounding.py — add get_prioritization_context()" (the exact `get_remediation_guidance_context()` analog at lines 264-313, the 10-column SELECT list, and the "never even fetched" PII discipline).
    - `backend/app/ai/grounding.py` lines 26-37 (module docstring documenting the PII-exclusion precedent) and lines 264-313 (`get_remediation_guidance_context()` — SELECT + outerjoin(Asset) + `.where(Vulnerability.id==finding_id, Vulnerability.tenant_id==tenant_id)` + `result.one_or_none()` + None-on-miss + dict return).
    - `backend/app/vulnerabilities/models.py` lines 46-81 (confirm the 9 D-04 factor column names exist verbatim) and `backend/app/assets/models.py` lines 60-65 (confirm `department` exists at line 62; confirm `assigned_user`/`managed_by`/`building`/`serial_number` are the columns to NOT select).
    - `backend/tests/test_ai_grounding_remediation_guidance.py` (whole file, 190 lines) — the exact test structure to mirror (seed a Vulnerability+Asset, assert the returned dict keys, assert None on cross-tenant/missing, assert PII columns absent from the returned dict).
  </read_first>
  <action>
    Add `get_prioritization_context(db: AsyncSession, tenant_id: uuid.UUID, finding_id: uuid.UUID) -> dict[str, Any] | None` to `backend/app/ai/grounding.py`, copying `get_remediation_guidance_context()`'s exact shape (outer-join Asset, tenant-scoped `.where`, `one_or_none()`, None-on-miss, dict return). SELECT exactly these 10 columns and NO others: `Vulnerability.cve_id, Vulnerability.cvss_v3_score, Vulnerability.epss_score, Vulnerability.exploit_available, Vulnerability.cisa_kev, Vulnerability.exploit_status_name, Vulnerability.severity, Vulnerability.sla_due_at, Vulnerability.sla_breached` (per D-04, plus `cve_id` per Assumption A4 so the narrative can name the CVE) and `Asset.department` (the ONE allowed owner factor, D-04). Give the function a docstring stating explicitly that `department` is deliberately read (per D-04) while `assigned_user`/`managed_by`/`building`/`serial_number` are deliberately excluded — mirroring how the existing module docstring is explicit about exclusions (threat T-26-01). Return a dict keyed by the same field names. NO deterministic pre-generation refuse predicate is needed here (unlike Phase 25's `has_actionable_remediation_text` — D-04 fields are structured scoring columns, not free text; the model's own `grounded=false` judgment is the backstop). Write `backend/tests/test_ai_grounding_prioritization.py` mirroring `test_ai_grounding_remediation_guidance.py`: (a) a seeded groundable finding returns a dict containing all 10 expected keys; (b) `department` IS present when the joined asset has one; (c) NONE of `assigned_user`/`managed_by`/`building`/`serial_number` appear as keys in the returned dict (positive assertion that PII is never even fetched); (d) a cross-tenant finding_id returns None; (e) a missing finding_id returns None; (f) an asset-less finding (`asset_id IS NULL`) returns a dict with `department` None (outer-join tolerance).
  </action>
  <acceptance_criteria>
    - `grep -n "def get_prioritization_context" backend/app/ai/grounding.py` present.
    - `grep -n "assigned_user\|managed_by\|serial_number\|building" backend/app/ai/grounding.py` returns NO match inside `get_prioritization_context` (grep the function body; the module-level docstring may name them as excluded — scope the check to the function).
    - `grep -n "Asset.department" backend/app/ai/grounding.py` present (the one allowed owner factor).
    - Test asserts the returned dict has no PII keys (positive negative-assertion) and returns None cross-tenant.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_grounding_prioritization.py -q</automated>
  </verify>
  <done>The grounding query returns exactly D-04's factors + cve_id + department, never any owner-PII column, is tenant-scoped with None-on-miss, and is test-proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExplainPrioritizationResponse — the no-rank schema</name>
  <read_first>
    - `.planning/phases/26-prioritization-narrative/26-PATTERNS.md` §"backend/app/ai/schemas.py — add ExplainPrioritizationResponse" (the `ExplainRemediationGuidanceResponse(ExplainResponseBase): pass` analog at lines 88-93; the explanation that `ExplainResponseBase` has zero numeric fields, so no rank field can exist without editing the shared base).
    - `backend/app/ai/schemas.py` lines 44-67 (`ExplainResponseBase` — confirm its 4 fields: `summary`, `business_risk`, `citations`, `grounded`) and lines 88-93 (`ExplainRemediationGuidanceResponse`).
    - `backend/tests/test_ai_schemas.py` (find the existing per-view schema assertions to mirror; add the new no-rank test alongside them).
  </read_first>
  <behavior>
    - Test: `set(ExplainPrioritizationResponse.model_fields) == {"summary", "business_risk", "citations", "grounded"}` — exactly 4 fields, no more.
    - Test: none of the field names or their types introduce a numeric rank/priority/score — assert no field named `priority`/`rank`/`score`/`ai_priority`/`ai_rank` exists on the model (the literal SC2 schema guarantee).
    - Test: `recheck_business_rules(candidate, allowed_source_fields=...)` accepts an `ExplainPrioritizationResponse` unchanged (no new business-rule branch needed).
  </behavior>
  <action>
    Add `class ExplainPrioritizationResponse(ExplainResponseBase): pass` to `backend/app/ai/schemas.py` immediately after `ExplainRemediationGuidanceResponse`, with a docstring stating it carries no additional fields — the prioritization narrative lives as prose in `summary`/`business_risk`, and the DELIBERATE absence of any numeric field IS the D-03/Pitfall #7/SC2 "no AI rank" enforcement (threat T-26-02). Do NOT add any field. `recheck_business_rules()` needs zero changes (already parameterized by `allowed_source_fields`). Add a test to `backend/tests/test_ai_schemas.py`: `test_prioritization_no_rank_field` asserting the model has EXACTLY the 4 base field names and that no `priority`/`rank`/`score` field exists (iterate `model_fields` keys and assert the forbidden identifiers are absent). This test is the schema-side half of the No-Rank contract (the UI-side grep lives in Plan 04).
  </action>
  <acceptance_criteria>
    - `grep -n "class ExplainPrioritizationResponse" backend/app/ai/schemas.py` present with `pass` (no fields).
    - Test `test_prioritization_no_rank_field` proves exactly 4 fields and zero rank/priority/score field.
    - `grep -vn '^#' backend/app/ai/schemas.py | grep -c "priority\|rank\|ai_score"` on the ExplainPrioritizationResponse block region returns 0 (the class adds no such identifier).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_schemas.py -k prioritization -q</automated>
  </verify>
  <done>ExplainPrioritizationResponse exists as a zero-field subclass; the no-rank schema guarantee is test-proven (exactly 4 base fields, no numeric verdict field).</done>
</task>

</tasks>

<artifacts_this_phase_produces>
(See per-plan block above.) Full phase artifact inventory: grounding fn (this plan), no-rank schema class (this plan), prompt-builder quadruplet (Plan 02), on-demand route + registration (Plan 03), frontend section + queued branch + no-rank CI check (Plan 04), tracer gate (Plan 05), AiBatchJob model + migration 033 + Redis non-request factory + queued route signal (Plan 06), budget batch helpers + top-N query + batch.py estimate/validate (Plan 07), batch.py orchestrators + scheduler submit/poll blocks (Plan 08).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB row (Asset PII columns) → grounding dict → prompt | Owner-PII must never cross into the model input; excluded at the SELECT |
| Model output schema → any UI number | A numeric rank must be structurally impossible, not merely unrendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information Disclosure | `get_prioritization_context()` SELECT | mitigate | Query selects only D-04's factor columns + `cve_id` + `Asset.department`; `assigned_user`/`managed_by`/`building`/`serial_number` are never fetched (defense-in-depth, mirrors every prior grounding fn). Test asserts these keys are absent from the returned dict. |
| T-26-02 | Product-integrity (Pitfall #7) | `ExplainPrioritizationResponse` | mitigate | Zero-field subclass of a base class that has no numeric field — a rank/priority/score field cannot be added without a reviewable diff to the shared `ExplainResponseBase`. Test asserts exactly 4 fields. |
</threat_model>

<verification>
- `cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test python -m pytest tests/test_ai_grounding_prioritization.py tests/test_ai_schemas.py -q` green.
</verification>

<success_criteria>
The prioritization grounding query returns only allowlisted factors (owner = department, never PII) and the response schema is a proven no-rank contract — the two costly-to-reverse D-03/D-04 seams are locked and test-proven before anything is built on them.
</success_criteria>

<output>
After completion, create `.planning/phases/26-prioritization-narrative/26-01-SUMMARY.md`.
</output>
