---
phase: 38-remediation-campaigns
plan: 04
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - frontend/src/lib/queries/keys.ts
  - frontend/src/lib/queries/use-campaigns.ts
  - frontend/src/lib/queries/use-campaigns.test.ts
  - frontend/src/components/campaigns/campaign-status-ribbon.tsx
  - frontend/src/components/campaigns/campaign-status-ribbon.test.tsx
  - frontend/src/components/campaigns/campaigns-chip-bar.tsx
  - frontend/src/components/campaigns/campaigns-table.tsx
  - frontend/src/components/campaigns/campaigns-table.test.tsx
  - frontend/src/app/(authed)/dashboard/campaigns/page.tsx
  - frontend/src/components/shell/nav-items.ts
autonomous: true
requirements: [CAMP-01]

user_setup: []

must_haves:
  truths:
    # --- goal-backward: CAMP-01 dedicated campaign view ---
    - "A Campaigns item appears in WORKFLOW_ITEMS nav and routes to /dashboard/campaigns"
    - "/dashboard/campaigns lists campaigns via GET /api/v1/campaigns, each row: remediation label (mono) · member count · % remediated (mono tabular-nums) · MTTR (mono) · status pill · owner-ticket count"
    - "Clicking a campaign row navigates to /dashboard/campaigns/{id} (full navigation to the Plan 05 detail page, UI-SPEC Layout §2)"
    - "The status pill is violet for ACTIVE and green for COMPLETE (status color family, never severity red/orange/yellow)"
    - "use-campaigns detail/list hooks set staleTime:0 (compute-on-read is never cached as authoritative, D-07)"
    # --- UI-SPEC covered lift (E1) ---
    - "Empty state (zero campaigns) shows heading 'No campaigns yet' + body + CTA 'View remediation groups'"
    - "Loading shows shimmer skeleton rows (state-patterns.md §1), never a black screen"
    - "The list renders the WR-13 mutually-exclusive branch order: error > loading > empty > data"
    # --- UI-SPEC zero-one-many gotcha (covered) ---
    - "Count strings singularize at M=1 (never '1 findings') across member count, owner count, and any '{M} findings' copy"
  truths_backstop:
    # --- UI-SPEC backstop lift (E1) ---
    - statement: "List-fetch failure renders the full-screen error card + Retry (state-patterns.md §Error), distinct from action errors"
      verification: backstop
    - statement: "Many campaigns -> table pagination 'Showing 1–10 of N' (interaction-patterns.md §Pagination), mono numbers"
      verification: backstop
    - statement: "Long remediation label -> ellipsis + full string in title tooltip (existing table-cell truncation)"
      verification: backstop
  prohibitions:
    - "Never use severity red/orange/yellow for campaign status/progress — status family (violet/amber/green) only (design-system rule)"
    - "Never hand-pick hex colors — consume sunset.css CSS variables from foundation.md"
    - "Never ship this screen without empty/loading/error states (v1 audit top pain point)"
    - "Never make campaigns-table.tsx navigate on its own (no useRouter inside the table) — the page owns navigation and passes it via the onRowClick prop, mirroring tickets-table.tsx"
  artifacts:
    - path: "frontend/src/app/(authed)/dashboard/campaigns/page.tsx"
      provides: "campaign list view (ErrorBoundary>Suspense>Inner) + onRowClick router.push wiring"
      min_lines: 40
    - path: "frontend/src/lib/queries/use-campaigns.ts"
      provides: "useCampaigns list hook + useCampaignDetail"
      exports: ["useCampaigns", "useCampaignDetail"]
    - path: "frontend/src/components/shell/nav-items.ts"
      provides: "Campaigns WORKFLOW_ITEMS entry"
      contains: "Campaigns"
  key_links:
    - from: "frontend/src/lib/queries/use-campaigns.ts"
      to: "/api/v1/campaigns"
      via: "api() fetch"
      pattern: "api/v1/campaigns"
    - from: "frontend/src/app/(authed)/dashboard/campaigns/page.tsx"
      to: "frontend/src/components/campaigns/campaigns-table.tsx"
      via: "renders table with useCampaigns data"
      pattern: "CampaignsTable"
    - from: "frontend/src/app/(authed)/dashboard/campaigns/page.tsx"
      to: "/dashboard/campaigns/{id} (Plan 05 detail page)"
      via: "onRowClick handler via useRouter.push, passed to CampaignsTable"
      pattern: "router\\.push.*campaigns"
---

<objective>
Deliver the CAMP-01 dedicated campaign view: a `Campaigns` nav entry + `/dashboard/campaigns` list
page (chip-bar status filter + table) consuming `GET /api/v1/campaigns` from Plan 01. This is the
visible completion of the tracer's "read it back in the campaign view." Follows the design system
verbatim (status color family, JetBrains Mono numerics, empty/loading/error states, copy-voice).
Each row click navigates to the Plan 05 campaign detail page (UI-SPEC Layout §2).

Purpose: give the analyst a navigable list of campaigns with live per-row progress, and a click-through
to each campaign's detail.
Output: nav item, list page (with row-click navigation), table/chip-bar/status-ribbon components, use-campaigns hook + tests.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/38-remediation-campaigns/38-UI-SPEC.md
@.planning/phases/38-remediation-campaigns/38-PATTERNS.md
@.planning/phases/38-remediation-campaigns/38-01-SUMMARY.md
@.claude/skills/sketch-findings-getvul/SKILL.md
@.claude/skills/sketch-findings-getvul/references/visual-language.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md

<interfaces>
<!-- Analogs (read at execute time): 38-PATTERNS.md maps each file precisely. -->
Hook analog: frontend/src/lib/queries/use-vuln-escalations.ts (KEY DEVIATION: staleTime:0, not 30_000).
keys.ts: add a `campaigns: { all, list, detail }` block (analog: tickets block :65-73).
Response type (from Plan 01 GET /campaigns): { id, remediation_id, status: 'ACTIVE'|'COMPLETE',
  total, open, in_progress, done, pct_remediated, mttr_seconds: number|null }.
  NOTE: mttr_seconds may be null this wave (full MTTR lands in Plan 03) — render "—" when null.
List page analog: frontend/src/app/(authed)/dashboard/tickets/page.tsx (ErrorBoundary>Suspense>Inner,
  WR-13 branch order error>loading>empty>data).
Row-click navigation analog: tickets/page.tsx owns a `const router = useRouter()` and passes
  `onRowClick={(t) => ...}` into TicketsTable; TicketsTable declares `onRowClick: (row) => void` in its
  props and invokes it on `<tr onClick>` + Enter/Space keydown. The table NEVER calls useRouter itself.
  Campaigns mirror this: campaigns/page.tsx passes `onRowClick={(c) => router.push('/dashboard/campaigns/' + c.id)}`.
Chip-bar analog: components/tickets/tickets-chip-bar.tsx (1 axis only: status ACTIVE/COMPLETE).
Table analog: components/tickets/tickets-table.tsx (mono + tabular-nums numeric cells; onRowClick prop).
Status ribbon analog: components/assets/severity-ribbon.tsx — RE-SKIN with status palette (violet/amber/green), NEVER severity colors.
Nav analog: components/shell/nav-items.ts:34-46 WORKFLOW_ITEMS; add { label:'Campaigns', href:'/dashboard/campaigns', icon: Target } (no chip, D-N-01). lucide install needs --legacy-peer-deps.
Copy (verbatim from UI-SPEC Copywriting Contract): empty heading "No campaigns yet"; body per contract; CTA "View remediation groups".
</interfaces>
</context>

<artifacts_this_phase_produces>
- **Routes:** `/dashboard/campaigns` (list page; rows navigate to `/dashboard/campaigns/{id}`).
- **Components:** `CampaignsTable` (row-click via `onRowClick` prop), `CampaignsChipBar`, `CampaignStatusRibbon` (status-colored).
- **Hooks:** `useCampaigns` (list), `useCampaignDetail` (id); `queryKeys.campaigns` block.
- **Nav:** `Campaigns` entry in `WORKFLOW_ITEMS` (`nav-items.ts`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: use-campaigns hook + keys.ts block + campaign-status-ribbon</name>
  <files>frontend/src/lib/queries/keys.ts, frontend/src/lib/queries/use-campaigns.ts, frontend/src/lib/queries/use-campaigns.test.ts, frontend/src/components/campaigns/campaign-status-ribbon.tsx, frontend/src/components/campaigns/campaign-status-ribbon.test.tsx</files>
  <read_first>
    - frontend/src/lib/queries/use-vuln-escalations.ts (hook shape)
    - frontend/src/lib/queries/keys.ts:65-73 (tickets block analog)
    - frontend/src/components/assets/severity-ribbon.tsx (ribbon layout to re-skin with status palette)
    - .claude/skills/sketch-findings-getvul/references/visual-language.md (status vs severity color families)
  </read_first>
  <behavior>
    - Test: useCampaigns calls api('/api/v1/campaigns') and returns the list; useCampaignDetail(id) hits /api/v1/campaigns/{id} and is disabled for empty id.
    - Test: both hooks use staleTime:0.
    - Test: CampaignStatusRibbon renders violet for ACTIVE, green for COMPLETE; never applies a severity (red/orange/yellow) class.
  </behavior>
  <action>
    Add the `campaigns: { all: ['campaigns'], list: (opts)=>..., detail: (id)=>... }` block to `keys.ts`. Create `use-campaigns.ts` with `useCampaigns(filters)` (list) and `useCampaignDetail(id)` mirroring use-vuln-escalations.ts but `staleTime: 0` (D-07); export the `CampaignDetail`/`CampaignSummary` TS types. Create `campaign-status-ribbon.tsx` re-skinning the severity-ribbon layout with the status palette (`--color-violet` ACTIVE, `--color-success` COMPLETE, `--color-amber` for the in-progress bucket) via CSS variables — no hex. Write the two test files asserting hook URL/staleTime and ribbon color-class mapping.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- use-campaigns campaign-status-ribbon</automated>
  </verify>
  <acceptance_criteria>
    - `use-campaigns.test.ts` green: correct URLs, staleTime:0, disabled on empty id.
    - `campaign-status-ribbon.test.tsx` green: ACTIVE->violet class, COMPLETE->green class; a grep for severity classes in the component returns none.
    - `npm run build` (or `tsc --noEmit`) clean for the new files.
  </acceptance_criteria>
  <done>Hook + keys block + status ribbon shipped and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: campaigns list page + table (row-click nav) + chip-bar + Campaigns nav item</name>
  <files>frontend/src/components/campaigns/campaigns-chip-bar.tsx, frontend/src/components/campaigns/campaigns-table.tsx, frontend/src/components/campaigns/campaigns-table.test.tsx, frontend/src/app/(authed)/dashboard/campaigns/page.tsx, frontend/src/components/shell/nav-items.ts</files>
  <read_first>
    - frontend/src/app/(authed)/dashboard/tickets/page.tsx (list page composition, WR-13 branch order, useRouter + onRowClick wiring)
    - frontend/src/components/tickets/tickets-table.tsx (mono/tabular-nums cells; the onRowClick prop contract — table invokes, page owns navigation)
    - frontend/src/components/tickets/tickets-chip-bar.tsx (ChipBar composition, 1-axis)
    - frontend/src/components/shell/nav-items.ts:34-46 (WORKFLOW_ITEMS)
    - .planning/phases/38-remediation-campaigns/38-UI-SPEC.md (Copywriting Contract + Layout §2 row-click navigation contract)
  </read_first>
  <behavior>
    - Test: CampaignsTable renders columns remediation label (mono), member count, % remediated (mono tabular-nums), MTTR (mono, "—" when null), status pill, owner-ticket count.
    - Test: at M=1 the member-count/owner-count copy is singular ("1 finding", not "1 findings").
    - Test: empty data -> "No campaigns yet" empty state with the "View remediation groups" CTA.
    - Test: error branch renders before loading before empty before data (WR-13).
    - Test: clicking a campaign row invokes the `onRowClick` prop with that campaign (assert the handler fires with the row; e.g. spy passed as onRowClick is called once with the clicked campaign, mirroring tickets-table's row-click test) — Enter/Space on a focused row also fires it.
  </behavior>
  <action>
    Create `campaigns-chip-bar.tsx` (single `status` axis: Active/Complete, ChipBar allow-list-as-constant). Create `campaigns-table.tsx` mirroring tickets-table.tsx chrome with the UI-SPEC columns, JetBrains Mono + tabular-nums on numerics, status pill via CampaignStatusRibbon; render the em-dash for null MTTR; singularize count strings at 1. Declare an `onRowClick: (campaign) => void` prop on CampaignsTable and invoke it on the row `onClick` plus Enter/Space keydown (exactly like tickets-table.tsx) — the table itself must NOT import or call useRouter. Create `campaigns/page.tsx` as ErrorBoundary>Suspense>Inner with the WR-13 branch order (error=PartialFailureBanner/full-screen error card, loading=SkeletonTable, empty=EmptyState with UI-SPEC copy, else table+Pagination), useDocumentTitle; the Inner component owns a `useRouter()` and passes `onRowClick={(c) => router.push('/dashboard/campaigns/' + c.id)}` into CampaignsTable, routing to the Plan 05 detail page per UI-SPEC Layout §2 (full navigation, not the drill-panel alternative). Add the Campaigns entry to `WORKFLOW_ITEMS` (label Campaigns, href /dashboard/campaigns, icon Target, no chip). Write `campaigns-table.test.tsx` for columns, singularization, empty state, and the row-click handler assertion.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- campaigns-table && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `campaigns-table.test.tsx` green: all UI-SPEC columns present, mono/tabular-nums on numerics, singular at M=1, empty state copy exact, and the onRowClick handler fires with the clicked campaign on row click (and Enter/Space).
    - Row-click navigation wired: `grep -n "router.push" frontend/src/app/(authed)/dashboard/campaigns/page.tsx` shows the push to `/dashboard/campaigns/{id}`; campaigns-table.tsx contains no `useRouter` import (page owns navigation).
    - Nav grep: `grep -n "Campaigns" frontend/src/components/shell/nav-items.ts` shows the new WORKFLOW_ITEMS entry.
    - `npm run build` succeeds; the campaigns route is within bundle budget (≤250 KB First-Load).
  </acceptance_criteria>
  <done>List view navigable, each row clicks through to the campaign detail page, columns + states + singularization correct, build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API (GET /campaigns) | read-only; RBAC + tenant scoping enforced server-side (Plan 01) |

## STRIDE Threat Register (ASVS L1 · block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-09 | I (XSS) | remediation label / owner strings rendered in table cells | mitigate | React auto-escapes text nodes; ChipBar allow-list clamps filter values (T-12-05 precedent); no dangerouslySetInnerHTML |
| T-38-04 | E | client-side nav gate | accept | frontend nav is not a security control; authority is the server RBAC on GET /campaigns (Plan 01) — never trust a frontend-only gate |
</threat_model>

<verification>
- `npm run test -- campaigns` + `npm run build` green.
- Manual (dev stack): Campaigns nav item visible; /dashboard/campaigns lists seeded campaigns (or the empty state) with violet/green pills; clicking a row lands on /dashboard/campaigns/{id}.
</verification>

<success_criteria>
- Campaigns is a navigable dedicated view listing live per-campaign progress, with each row clicking through to the campaign detail page.
- Design system honored: status color family, mono numerics, empty/loading/error states, exact copy.
</success_criteria>

<output>
After completion, create `.planning/phases/38-remediation-campaigns/38-04-SUMMARY.md`.
</output>
