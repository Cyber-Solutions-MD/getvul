---
phase: 38
slug: remediation-campaigns
status: draft
shadcn_initialized: true
preset: "style=new-york, baseColor=zinc, cssVariables=true, iconLibrary=lucide (sunset theme overrides zinc via CSS vars — see foundation.md)"
created: 2026-08-17
---

# Phase 38 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized — `frontend/components.json`, pre-existing, not re-run this phase) |
| Preset | style: new-york · baseColor: zinc · cssVariables: true · rsc: true — production consumes `sunset.css` variables on top per `sketch-findings-getvul/references/foundation.md` (do not hand-pick zinc/default shadcn hex) |
| Component library | Radix primitives via shadcn (`@radix-ui/react-progress`, `@radix-ui/react-dialog`, etc. — already vendored under `frontend/src/components/ui/`) |
| Icon library | lucide-react (`^0.383.0`, `--legacy-peer-deps` required per project memory) |
| Font | Inter (UI/prose) + JetBrains Mono (CVE IDs, hostnames, durations, counts, IDs) — locked, no substitution |

**No new shadcn blocks/registries required this phase.** All primitives needed (Progress, Dialog/AlertDialog for confirmations, Table, Badge, Card, Tabs-free two-column layout) already exist in `frontend/src/components/ui/`. Registry Safety section below is a no-op declaration.

---

## Spacing Scale

Declared values (must be multiples of 4) — inherited verbatim from `foundation.md`, no phase-specific override:

| Token | Value | Usage |
|-------|-------|-------|
| xs (`--space-1`) | 4px | Icon gaps, pill internal gaps |
| sm (`--space-2`/`--space-3`) | 8px / 12px | Compact element spacing, chip padding |
| md (`--space-4`/`--space-5`) | 16px / 20px | Default element spacing, card padding (cards = 20px per foundation.md) |
| lg (`--space-6`) | 24px | Section padding, page-head to body-grid gap |
| xl (`--space-8`) | 32px | Layout gaps (e.g. body-grid main-vs-rail gutter) |
| 2xl (`--space-12`) | 48px | Major section breaks |
| 3xl (`--space-16`) | 64px | Page-level spacing |

Exceptions: none. The campaign burndown ring (see Visuals below) reuses the asset-detail risk-ring's exact dimensions (no new size token).

---

## Typography

Inherited verbatim from `foundation.md`'s 1.25 modular scale — no new sizes introduced this phase.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (`--text-sm`) | 400 | 1.5 (`--leading-base`) |
| Label (uppercase section headers, e.g. drill `<h4>`, campaign card labels) | 11px (`--text-xs` variant, tracking-wide) | 500 | 1.3 (`--leading-snug`) |
| Heading (card titles, campaign detail `<h2>`) | 24px (`--text-2xl`) | 600 | 1.2 (`--leading-tight`) |
| Display (campaign list page title, burndown % stat) | 32px (`--text-3xl`) | 700 | 1.1 (`--leading-tight`) |

Mono (`--font-mono`) required for: campaign remediation identifier (CVE / patch string), MTTR durations, host counts, ticket IDs, percentages in the burndown stat — anything "terminal-pasteable" per project convention. Tabular-nums (`font-variant-numeric: tabular-nums`) on all numeric cells (owner ticket counts, member counts, % remediated).

---

## Color

Inherited verbatim from `foundation.md` / `visual-language.md` — no new hues introduced. This phase is 100% a workflow surface (list + detail), no marketing/hero surface.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--color-bg` `#0E0B1A` (dark) / cream light-theme equivalent | Page background |
| Secondary (30%) | `--color-surface` `#1A1430` + `--color-surface-2` `#241B40` | Campaign list rows, campaign detail cards, right-rail cards |
| Accent (10%) | `--gradient-sunset` (pink→violet→amber) | Reserved for: (1) the primary "Start campaign" / "Bulk-create tickets" gradient CTA button, (2) the campaign burndown ring's gradient stroke (reuses asset-detail risk-ring pattern), (3) the "active" state of the list/detail view-toggle if used. Never used for decorative fills, table row backgrounds, or more than 2 elements per screen. |
| Destructive | `--color-danger` `#F87171` (dark) / `#DC2626` (light) | "Close campaign early" confirmation only — routed through the existing `AlertDialog` destructive-action pattern, never a plain button color |

Status-workflow colors (separate family from severity, per `visual-language.md`) drive the campaign progress breakdown:

| Campaign member bucket | Color | Token |
|---|---|---|
| Open (not yet ticketed / not in progress) | Violet | `--color-violet` |
| In progress (ticket open, not yet rescan-verified) | Amber | `--color-amber` |
| Done (rescan-verified REMEDIATED, D-09) | Green | `--color-success` |

These three buckets are the campaign's %-breakdown (mirrors the existing severity-breakdown ribbon component shape from `visual-language.md`, re-skinned with status colors instead of severity colors). Do not reuse severity red/orange/yellow here — campaign progress is a status concept, not a severity concept, and mixing the families breaks the "eye separates them" rule the design system establishes.

Campaign lifecycle **status pill** (Active / Complete) reuses the existing status-pill chrome:

| Campaign status | Color | Token |
|---|---|---|
| Active | Violet | `--color-violet` (open-style pill) |
| Complete | Green | `--color-success` (completed-style pill) |

(No "Blocked" campaign status exists — campaigns don't have a blocked lifecycle state per the CONTEXT decisions; D-13/D-14 only define active↔complete, both derived.)

---

## Copywriting Contract

Voice: peer, not butler. Sentence case. No exclamation marks, no "Please," no "Unable to." Numerals not words. Mono for CVE IDs / durations / counts. See `copy-voice.md` for the full rulebook — the rows below are the phase-specific instances.

| Element | Copy |
|---------|------|
| Primary CTA (launch, from remediation-grouped view) | `Start campaign` |
| Primary CTA (campaign detail, un-ticketed members exist) | `Create tickets` (bulk-create/adopt run — D-06/D-08); button label becomes `Create N tickets` when N un-ticketed members are known, mirroring the dashboard hero's number-in-headline pattern |
| Secondary action (campaign detail) | `Close campaign` (manual early close, D-13) — routed through a confirmation, never a bare click-to-close |
| Destructive confirmation | `Close campaign`: `Close "{remediation label}" early? {N} of {M} findings aren't rescan-verified yet — they'll stop being tracked here. This can't be undone from the campaign view.` (AlertDialog, destructive-styled confirm button, cancel default-focused) |
| Duplicate-launch redirect toast (D-11 — launching an already-active group) | `Campaign already running for {remediation label} — opening it.` (info-toned, 6s auto-dismiss per `state-patterns.md` toast convention) |
| Empty state heading (campaign list, zero campaigns ever launched) | `No campaigns yet` |
| Empty state body | `Group findings that share a fix into a campaign from the Remediations view, and bulk-create tickets in one action instead of ticketing hosts one at a time.` |
| Empty state CTA | `View remediation groups` (links to the remediation-grouped list where "Start campaign" lives) |
| Empty state (campaign detail, zero un-ticketed members left to create) | `Every member is already ticketed` — inline note above a disabled/absent `Create tickets` button, not a full empty-card (this is a sub-section empty, not a page empty) |
| Error state (bulk-create partial failure — some tickets created, some failed) | `{N} of {M} tickets created` heading + amber inline banner: `{K} failed — {provider} returned HTTP {code} · Request ID {id}` + `Retry failed` button. Mirrors the existing partial-failure ticketing pattern (`state-patterns.md` §3), amber not red — the campaign itself is not broken. |
| Live-progress microcopy (burndown card) | `{pct}% remediated` (headline stat, mono) + `{open} open · {in_progress} in progress · {done} done` (breakdown row, mono counts) + `Campaign MTTR: {duration}` (mono duration, e.g. `4d 6h`) |
| Live-membership caveat (D-03 — denominator can grow) | `{M} findings currently match this fix — including any discovered by later scans.` (small muted note under the member count, sets the "live" expectation so a growing denominator never reads as a bug) |
| New-joiner-untracked note (D-10) | `{N} newly matched findings aren't ticketed yet.` + `Create tickets` link-through — surfaces D-10's "stays un-ticketed until re-run" behavior explicitly rather than silently |
| Reactivation note (D-14 — completed campaign reopens) | Status pill flips Complete→Active with no modal; a small activity-timeline entry reads: `Campaign reactivated — {finding ref} reopened` (mono ref), consistent with the existing timeline copy shape (`Actor verb ref · time`) |
| Audit-adjacent confirmation toast (create) | `Campaign started for {remediation label}.` |
| Audit-adjacent confirmation toast (bulk-assign run) | `{N} tickets created, {M} adopted.` (adopted = D-06 pre-existing tickets folded in) |

---

## UI Considerations

Applicable state considerations resolved: 9 covered, 3 backstop, 0 unresolved.

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | Campaign list (zero campaigns ever launched) | ✅ covered | Empty state renders the documented "No campaigns yet" heading + body + "View remediation groups" CTA (see Copywriting Contract) |
| empty | Campaign detail, bulk-create sub-section (zero un-ticketed members) | ✅ covered | Renders the documented "Every member is already ticketed" inline note, `Create tickets` action absent/disabled rather than erroring |
| loading | Campaign list / detail initial fetch | ✅ covered | Skeleton rows/cards per `state-patterns.md` §1 (shimmer skeleton, no black screen); compute-on-read (D-07) means every view load re-aggregates, so loading state is not a one-time cold-start concern |
| error | Bulk-create partial failure (some tickets fail) | ✅ covered | Amber partial-failure banner + per-failure detail + `Retry failed`, per documented Error state row (never a full-page error when some tickets did succeed) |
| error | Campaign action network/5xx failure (create/close) | 🧪 backstop | Falls back to the existing global toast-error pattern (red-tinted, manual-dismiss per `state-patterns.md` "Toast notifications") — no campaign-specific copy authored; verify at execution time that the generic toast fires and is legible |
| populated | Campaign detail with live burndown | ✅ covered | Status-color 3-bucket breakdown (open/in-progress/done) + mono MTTR stat, per Color and Copywriting Contract rows above |
| populated | Campaign list with mixed active/complete campaigns | ✅ covered | Status pill (violet Active / green Complete) sorts/distinguishes rows; reuses existing status-pill chrome, no new visual language |
| partial | Live membership growth mid-campaign (D-03) | ✅ covered | "{M} findings currently match..." caveat note documented in Copywriting Contract — denominator growth is explained, not silent |
| partial | New live-joined members untracked by tickets (D-10) | ✅ covered | "{N} newly matched findings aren't ticketed yet" note + `Create tickets` link-through, documented above |
| overflow | Many owners in one campaign (D-04 one-ticket-per-owner, campaign could span 20+ owners) | 🧪 backstop | Owner/ticket breakdown list should follow the existing table pagination pattern (`interaction-patterns.md` §Pagination, mono numbers, "Showing 1–10 of N") if the per-owner ticket list exceeds ~10 rows; no dedicated campaign-scale mockup exists — verify the planner wires pagination or a scroll container rather than an unbounded list |
| overflow | Long remediation label (product/patch name) as campaign title | 🧪 backstop | Truncate with ellipsis + full string in a `title` attribute / tooltip, matching the existing table-cell truncation convention used for long vuln titles; no campaign-specific mockup — verify at execution |
| zero-one-many | Campaign with exactly 1 member vs. many | ✅ covered | Breakdown/MTTR copy uses numerals throughout (`1 finding` not pluralization-fragile phrasing avoided by using counts, e.g. "{M} findings currently match" reads fine at M=1 as "1 findings" — **planner must singularize this string at M=1**, flagged here so it isn't missed) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Progress, Card, Table, Badge, AlertDialog, Toast (all already vendored under `frontend/src/components/ui/`, pre-existing from Phases 9–37) | not required — no new installs this phase |
| third-party | none declared | not applicable |

No new shadcn `init` or `add` run required. If the planner determines a new primitive is genuinely needed (e.g. a segmented status-breakdown bar not covered by `Progress`), it must be built as a local composition of existing tokens/primitives — not a new third-party registry block — per this phase's "no new infra" scope discipline.

---

## Layout & Interaction Reuse (non-template addendum — binds planner/executor)

This phase introduces **two views**, both composed from already-validated patterns in `sketch-findings-getvul` — no new layout pattern is invented:

1. **Campaign entry point** — lives on the existing remediation-grouped view (a **list-with-side-panel-drill-down** pattern per `page-layouts.md` §3; this list itself does not yet have a frontend page and must be built this phase per `get_remediations_grouped()` — reuse the chip-bar-filter + table shell verbatim). Each grouped row gets a `Start campaign` secondary/CTA action; if D-11 finds an existing active campaign for that `remediation_id`, clicking routes straight to the campaign detail (no duplicate-create dialog, per the "opens the existing campaign" decision) — surface the redirect via the documented toast copy.

2. **Campaign list view** (dedicated view per CAMP-01 "dedicated campaign view") — **list-with-side-panel-drill-down** pattern (`page-layouts.md` §3): chip-bar filters (status: Active/Complete), table columns = remediation label (mono) · member count · % remediated (mono, tabular-nums) · MTTR (mono) · status pill · owner-ticket count. Row click → 420px drill panel (not full navigation) showing a condensed version of the burndown, OR full navigation to a campaign detail page — **planner's discretion**, but if a full page is chosen it MUST use pattern 3 below, and if a side panel is chosen it MUST follow `interaction-patterns.md` §Drill-down side panel verbatim (420px, slide-in, × close, pink inset border on active row).

3. **Campaign detail** (if implemented as its own page rather than the drill panel) — **two-column with sticky metadata rail** pattern (`page-layouts.md` §4): main column = member-findings table (reuse existing vuln-table chrome, filtered to this campaign's `remediation_id`) + owner/ticket breakdown card; right rail = burndown card (reuses the asset-detail risk-ring gradient-stroke SVG pattern, but the ring encodes "% remediated" instead of risk score, sunset-gradient stroke) + campaign MTTR + lifecycle actions (`Create tickets` CTA, `Close campaign` secondary).

4. **Navigation entry** — add a `Campaigns` item to `WORKFLOW_ITEMS` in `frontend/src/components/shell/nav-items.ts` (grouped alongside Tickets/Rules/Connectors/Asset groups, consistent with the existing single-source-of-truth nav pattern). No chip per the existing D-N-01 convention (campaigns aren't one of the three chip-carrying destinations: vuln_open / asset_total / ticket_open) unless the planner deliberately extends `ChipKey` — flagged as planner discretion, not decided here.

5. **Bulk-create action** reuses the existing **bulk-action floating bar** visual chrome (`interaction-patterns.md` §Bulk action floating bar) is NOT applicable here (that pattern is for ad-hoc row-selection bulk actions on a list; campaign bulk-create is a single deterministic action against the whole campaign, triggered from the detail page's primary CTA, not a floating selection bar). Do not build a checkbox-selection UI for campaign members — D-02 already fixed "whole group, always."

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
