---
phase: 38-remediation-campaigns
plan: 05
type: execute
wave: 4
depends_on: ["02", "03", "04"]
files_modified:
  - frontend/src/lib/queries/keys.ts
  - frontend/src/lib/queries/use-remediations-grouped.ts
  - frontend/src/lib/queries/use-campaign-mutations.ts
  - frontend/src/components/campaigns/campaign-burndown-card.tsx
  - frontend/src/components/campaigns/campaign-burndown-card.test.tsx
  - frontend/src/app/(authed)/dashboard/campaigns/[id]/page.tsx
  - frontend/src/app/(authed)/dashboard/vulnerabilities/remediations/page.tsx
  - frontend/src/components/campaigns/remediations-table.tsx
  - frontend/src/components/campaigns/remediations-table.test.tsx
autonomous: false          # ends with a checkpoint:human-verify of the full lifecycle flow
requirements: [CAMP-01, CAMP-02, CAMP-03]

user_setup: []

must_haves:
  truths:
    # --- goal-backward: CAMP-01 entry point ---
    - "/dashboard/vulnerabilities/remediations lists remediation groups via GET /api/v1/vulnerabilities/remediations/grouped, each row with a 'Start campaign' CTA (this page is new — zero prior consumers, Pitfall 8)"
    - "Clicking 'Start campaign' POSTs /api/v1/campaigns; if the group already has an active campaign it routes to the existing campaign detail and shows the D-11 redirect toast 'Campaign already running for {label} — opening it.'"
    # --- goal-backward: CAMP-02 bulk create from UI ---
    - "The campaign detail 'Create tickets' CTA POSTs /{id}/bulk-assign; the label reads 'Create N tickets' when N un-ticketed members are known"
    - "A partial bulk-create failure renders the amber partial-failure banner '{N} of {M} tickets created' + '{K} failed …' + 'Retry failed' (never red — the campaign isn't broken)"
    # --- goal-backward: CAMP-03 burndown ---
    - "The campaign detail rail shows a burndown ring (RiskRing reused, score = pct_remediated, sunset-gradient stroke) + '{pct}% remediated' + '{open} open · {in_progress} in progress · {done} done' + 'Campaign MTTR: {duration}'"
    - "'Close campaign' is a secondary action routed through a destructive AlertDialog (never a bare click), with the exact UI-SPEC confirmation copy"
    # --- UI-SPEC covered lift (E2/E3/E4/E6) ---
    - "Burndown card on a zero-member campaign shows 0% and '0 open · 0 in progress · 0 done', never crashes on a 0/0 denominator (E6)"
    - "D-03 live-growth caveat ('{M} findings currently match this fix — including any discovered by later scans.') and the D-10 new-joiner-untracked note render on the detail (E2 partial)"
    - "Count strings singularize at M=1 across the burndown breakdown, member count, and owner count"
  truths_backstop:
    # --- UI-SPEC backstop lift (E2/E3/E5/E6) ---
    - statement: "Detail-fetch and remediation-grouped-fetch failures render the full-screen error card + Retry (state-patterns.md §Error)"
      verification: backstop
    - statement: "Many owners (D-04, 20+) -> per-owner list is a paginated/scroll container, not an unbounded list (E2 overflow)"
      verification: backstop
    - statement: "Long remediation label as detail title -> ellipsis + title tooltip; in the AlertDialog body it WRAPS (not width-clamped) — verify wrap not clip (E2/E5 long-text)"
      verification: backstop
    - statement: "Many remediation groups on the entry point -> same table pagination as the campaign list (E3 overflow)"
      verification: backstop
    - statement: "Burndown card load-error rides the E2 detail full-screen error card; no independent card-level fallback (E6)"
      verification: backstop
  prohibitions:
    - "Never use severity red/orange/yellow for the burndown ring/breakdown — status family (violet/amber/green) + the sunset-gradient stroke only"
    - "Never close a campaign on a bare click — always route through the destructive AlertDialog confirmation"
    - "Never build a checkbox row-selection UI for campaign members — D-02 fixed 'whole group, always'; the bulk-action floating bar pattern does not apply"
    - "Never hand-pick hex colors — consume foundation.md CSS variables; never fork the RiskRing SVG math (wrap it)"
  artifacts:
    - path: "frontend/src/app/(authed)/dashboard/campaigns/[id]/page.tsx"
      provides: "two-column campaign detail (member table + burndown rail + lifecycle actions)"
      min_lines: 50
    - path: "frontend/src/app/(authed)/dashboard/vulnerabilities/remediations/page.tsx"
      provides: "remediation-grouped entry point with Start campaign CTA"
      min_lines: 40
    - path: "frontend/src/components/campaigns/campaign-burndown-card.tsx"
      provides: "RiskRing-composed burndown (score=pct_remediated)"
      contains: "RiskRing"
  key_links:
    - from: "frontend/src/app/(authed)/dashboard/vulnerabilities/remediations/page.tsx"
      to: "/api/v1/campaigns (POST)"
      via: "Start campaign mutation -> route to /dashboard/campaigns/{id}"
      pattern: "campaigns"
    - from: "frontend/src/app/(authed)/dashboard/campaigns/[id]/page.tsx"
      to: "/api/v1/campaigns/{id}/bulk-assign and /close"
      via: "Create tickets + Close campaign mutations"
      pattern: "bulk-assign"
---

<objective>
Deliver the CAMP-01 entry point, the CAMP-03 burndown detail, and the CAMP-02 bulk-create UI —
completing the analyst's end-to-end loop. A new remediation-grouped list page (zero prior frontend
consumers, Pitfall 8) carries the "Start campaign" CTA (D-11 open-existing redirect); the campaign
detail page shows the reused-RiskRing burndown + member table + owner/ticket breakdown, with
"Create tickets" (bulk-assign) and a destructive-AlertDialog "Close campaign". Ends with a
human-verify of the full create→bulk-assign→close flow (e2e is seed-gated per project memory).

Purpose: close the analyst loop — group, ticket, and watch a campaign burn down from the UI.
Output: entry-point page, detail page, burndown card, mutations hook, remediations table + tests.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/38-remediation-campaigns/38-UI-SPEC.md
@.planning/phases/38-remediation-campaigns/38-PATTERNS.md
@.planning/phases/38-remediation-campaigns/38-02-SUMMARY.md
@.planning/phases/38-remediation-campaigns/38-03-SUMMARY.md
@.planning/phases/38-remediation-campaigns/38-04-SUMMARY.md
@.claude/skills/sketch-findings-getvul/references/page-layouts.md
@.claude/skills/sketch-findings-getvul/references/interaction-patterns.md
@.claude/skills/sketch-findings-getvul/references/state-patterns.md
@.claude/skills/sketch-findings-getvul/references/copy-voice.md

<interfaces>
<!-- Analogs mapped in 38-PATTERNS.md; UI-SPEC Layout & Interaction Reuse §1-5 binds this plan. -->
Entry-point page analog: tickets/page.tsx (list shell); data via a NEW use-remediations-grouped.ts
  hook (analog use-vuln-escalations.ts) hitting GET /api/v1/vulnerabilities/remediations/grouped.
  Add a `remediationsGrouped` block to keys.ts.
Detail page analog: assets/[id]/page.tsx (two-column sticky rail). Main = member-findings table
  (reuse vuln-table chrome filtered to remediation_id) + owner/ticket breakdown card. Rail =
  campaign-burndown-card + MTTR + lifecycle actions.
Burndown card: wrap frontend/src/components/ui/RiskRing.tsx (score=pct_remediated), sunset-gradient
  stroke ALWAYS; do NOT fork the SVG math. Status-family breakdown row (violet/amber/green).
Mutations: use-campaign-mutations.ts (start=POST /campaigns, bulkAssign=POST /{id}/bulk-assign,
  close=POST /{id}/close). Start handles already_existed=true -> route to detail + redirect toast.
Confirm dialog: frontend/src/components/ui/ConfirmModal.tsx (variant="danger") — UI-SPEC close copy.
Partial-failure: frontend/src/components/states/partial-failure-banner.tsx in props mode
  (errors={ErrorRow[]}, onRetry) — amber, {N} of {M} tickets created.
Copy: all strings VERBATIM from UI-SPEC Copywriting Contract (Start campaign, Create N tickets,
  Close campaign confirmation, D-11 redirect toast, live-membership caveat, new-joiner note).
</interfaces>
</context>

<artifacts_this_phase_produces>
- **Routes:** `/dashboard/vulnerabilities/remediations` (entry point), `/dashboard/campaigns/[id]` (detail).
- **Components:** `CampaignBurndownCard` (RiskRing-composed), `RemediationsTable`.
- **Hooks:** `useRemediationsGrouped`, `useCampaignMutations` (start / bulkAssign / close); `queryKeys.remediationsGrouped` block.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: remediation-grouped entry-point page + Start campaign mutation + D-11 redirect</name>
  <files>frontend/src/lib/queries/keys.ts, frontend/src/lib/queries/use-remediations-grouped.ts, frontend/src/lib/queries/use-campaign-mutations.ts, frontend/src/components/campaigns/remediations-table.tsx, frontend/src/components/campaigns/remediations-table.test.tsx, frontend/src/app/(authed)/dashboard/vulnerabilities/remediations/page.tsx</files>
  <read_first>
    - frontend/src/app/(authed)/dashboard/tickets/page.tsx (list shell, WR-13 branch order)
    - frontend/src/lib/queries/use-vuln-escalations.ts (query hook shape)
    - backend/app/vulnerabilities/router.py:781-808 (GET /remediations/grouped response shape)
    - .planning/phases/38-remediation-campaigns/38-UI-SPEC.md (Copywriting Contract + Layout §1)
  </read_first>
  <behavior>
    - Test: RemediationsTable renders grouped rows with a 'Start campaign' CTA per row; member count singularizes at 1.
    - Test: Start campaign mutation POSTs /api/v1/campaigns; on already_existed=true it routes to /dashboard/campaigns/{id} and fires the D-11 info toast.
    - Test: empty -> "No remediation groups yet"; error > loading > empty > data branch order.
  </behavior>
  <action>
    Add `remediationsGrouped` + confirm a `campaigns` mutation surface to `keys.ts`. Create `use-remediations-grouped.ts` (query hook over GET /api/v1/vulnerabilities/remediations/grouped). Create `use-campaign-mutations.ts` with `useStartCampaign` (POST /campaigns; onSuccess: route to `/dashboard/campaigns/{id}`; if `already_existed` fire the D-11 info toast copy), plus `useBulkAssign` and `useCloseCampaign` (used in Task 2). Create `remediations-table.tsx` (chip-bar + table shell) with a `Start campaign` CTA per grouped row. Create the entry-point `remediations/page.tsx` (ErrorBoundary>Suspense>Inner, WR-13 branch order, UI-SPEC empty copy). Write `remediations-table.test.tsx`.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- remediations-table && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `remediations-table.test.tsx` green: Start campaign CTA per row, singular at M=1, empty-state copy exact.
    - Start-campaign mutation test: POST + route to detail; already_existed -> redirect toast with exact D-11 copy.
    - `npm run build` clean; new route within bundle budget.
  </acceptance_criteria>
  <done>Entry point + Start campaign + D-11 redirect shipped and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: campaign detail page + burndown card + Create tickets + Close campaign</name>
  <files>frontend/src/components/campaigns/campaign-burndown-card.tsx, frontend/src/components/campaigns/campaign-burndown-card.test.tsx, frontend/src/app/(authed)/dashboard/campaigns/[id]/page.tsx</files>
  <read_first>
    - frontend/src/app/(authed)/dashboard/assets/[id]/page.tsx (two-column sticky rail)
    - frontend/src/components/ui/RiskRing.tsx (reuse, score=pct_remediated — do not fork)
    - frontend/src/components/ui/ConfirmModal.tsx (variant="danger")
    - frontend/src/components/states/partial-failure-banner.tsx (props mode, ErrorRow[])
    - .planning/phases/38-remediation-campaigns/38-UI-SPEC.md (Layout §3, Copywriting Contract)
  </read_first>
  <behavior>
    - Test: CampaignBurndownCard wraps RiskRing with score=pct_remediated; renders "{pct}% remediated" + "{open} open · {in_progress} in progress · {done} done" + "Campaign MTTR: {duration}"; MTTR "—" when null.
    - Test: zero-member campaign -> 0% + "0 open · 0 in progress · 0 done", no crash (E6).
    - Test: breakdown counts singularize at 1.
    - Test: Create tickets label reads "Create N tickets" when N un-ticketed known; Close campaign opens the danger AlertDialog with the exact UI-SPEC copy; partial failure -> amber partial-failure banner.
  </behavior>
  <action>
    Create `campaign-burndown-card.tsx` wrapping `RiskRing` (score=pct_remediated, sunset gradient) + a status-family breakdown row + MTTR line (format seconds to "{d}d {h}h", "—" when null). Create the detail `[id]/page.tsx` (two-column sticky rail per assets/[id]): main = member-findings table (reuse vuln-table chrome filtered to the campaign's remediation_id) + owner/ticket breakdown + the D-03 live-growth caveat + D-10 new-joiner note; rail = burndown card + `Create tickets` CTA (label `Create N tickets`; wires `useBulkAssign`; partial-failure banner on partial result) + `Close campaign` secondary (opens ConfirmModal variant="danger" with exact copy; wires `useCloseCampaign`). All copy verbatim from the UI-SPEC. Write `campaign-burndown-card.test.tsx`.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- campaign-burndown-card && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `campaign-burndown-card.test.tsx` green: RiskRing wrapped (not forked), breakdown + MTTR copy, "—" on null, 0/0 no-crash, singular at M=1.
    - Detail page renders the D-03 caveat + D-10 note; Close campaign is dialog-gated; partial failure is amber.
    - `npm run build` clean; detail route within bundle budget.
  </acceptance_criteria>
  <done>Detail + burndown + Create tickets + Close campaign shipped and tested.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the full create → bulk-assign → close lifecycle in the browser</name>
  <what-built>The end-to-end campaign loop: Start campaign (from /dashboard/vulnerabilities/remediations) → campaign detail with live burndown → Create tickets (per-owner bulk-assign) → Close campaign (danger dialog). E2E is seed-gated/destructive per project memory (getvul-kanban-e2e-seed-gated-destructive), so a scripted Playwright run over the shared dev stack cannot deterministically drive the 100%-remediated auto-complete path — hence a human-verify checkpoint over the run-and-observe flow instead.</what-built>
  <how-to-verify>
    1. Run the local dev stack (or prod build per getvul-local-e2e-perf-gate); log in as an analyst.
    2. Visit /dashboard/vulnerabilities/remediations — confirm grouped rows render with a "Start campaign" CTA and the empty/loading states behave.
    3. Click "Start campaign" on a group → lands on /dashboard/campaigns/{id}; the burndown ring shows a % remediated, the breakdown reads "{open} open · {in_progress} in progress · {done} done", and the D-03 live-growth caveat is present.
    4. Click "Start campaign" AGAIN on the same group → confirm it routes to the existing campaign and shows the toast "Campaign already running for {label} — opening it." (D-11, no duplicate).
    5. Click "Create N tickets" → confirm per-owner tickets are created (check the toast "{N} tickets created, {M} adopted."); re-run and confirm it adopts (does not duplicate).
    6. Confirm the Campaigns nav item lists the new campaign with a violet ACTIVE pill.
    7. Click "Close campaign" → confirm the destructive AlertDialog with the exact copy, cancel-default-focused; confirm closes it and the pill/behavior updates.
    8. Confirm status/progress colors are the status family (violet/amber/green), never severity red/orange/yellow.
  </how-to-verify>
  <read_first>
    - Project memory: getvul-kanban-e2e-seed-gated-destructive, getvul-local-e2e-perf-gate
    - .planning/phases/38-remediation-campaigns/38-UI-SPEC.md (Copywriting Contract)
  </read_first>
  <acceptance_criteria>
    - Human confirms steps 2–8 behave as described, or reports specific deviations.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API (POST /campaigns, /bulk-assign, /close) | analyst-gated mutations; authority is server-side (Plans 01–03) |

## STRIDE Threat Register (ASVS L1 · block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-09 | I (XSS) | remediation label / owner / provider strings in tables, toasts, dialog body | mitigate | React text-node escaping; no dangerouslySetInnerHTML; ChipBar allow-list clamp |
| T-38-04 | E | client-side action gates (Create tickets / Close) | accept | frontend gates are UX only; server enforces require_analyst on every mutation (Plans 02/03) |
| T-38-10 | T | mutation payloads (provider/project_key) | mitigate | server-side `extra="forbid"` (Plan 02) is the authority; client sends only the declared fields |
</threat_model>

<verification>
- `npm run test -- campaigns remediations campaign-burndown-card` + `npm run build` green.
- Human-verify checkpoint (Task 3) approved.
</verification>

<success_criteria>
- Analyst can group → start a campaign → bulk-create per-owner tickets → watch burndown → close, entirely from the UI.
- D-11 open-existing redirect, D-03 caveat, D-10 note, and status-family colors all present.
- Design system + copy-voice honored verbatim.
</success_criteria>

<output>
After completion, create `.planning/phases/38-remediation-campaigns/38-05-SUMMARY.md`.
</output>
