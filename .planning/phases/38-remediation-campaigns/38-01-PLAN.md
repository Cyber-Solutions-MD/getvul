---
phase: 38-remediation-campaigns
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/049_add_campaigns.py
  - backend/app/campaigns/__init__.py
  - backend/app/campaigns/models.py
  - backend/app/campaigns/schemas.py
  - backend/app/campaigns/service.py
  - backend/app/campaigns/router.py
  - backend/app/main.py
  - backend/tests/test_campaigns.py
autonomous: false          # contains the D-11 one-way reversibility checkpoint:decision
requirements: [CAMP-01, CAMP-04]

user_setup: []

assumption_delta_decision:
  primary_noun: "remediation_id-keyed campaign"
  kind: pluralization
  term: "multiple (across multiple assets and owners)"
  decision: promote
  rationale: >
    Per D-01/D-02/D-03, the campaign's identity is the EXISTING Vulnerability.remediation_id
    grouping promoted to a first-class persisted, dynamically-membered entity — NOT a new
    parallel asset-list grouping and NOT an add-alongside. Campaign membership is always the
    live `WHERE remediation_id = :x` join; the Campaign row stores identity + lifecycle only.
  add_alongside_debt: none
  contract_test: >
    test_live_membership_grows (Plan 03) asserts a campaign's membership derives live from
    remediation_id (a finding on a newly-seen asset joins the denominator with no membership row).

must_haves:
  truths:
    # --- goal-backward (CAMP-01 create + persist + read; CAMP-04 audit) ---
    - "POST /api/v1/campaigns with a remediation_id persists exactly one campaigns row and GET /api/v1/campaigns reads it back"
    - "POST /api/v1/campaigns writes exactly one audit row action=campaign.create only when a NEW campaign is created (not when opening an existing one) — every campaign write routes through app/audit.py::audit(), fail-closed (D-15)"
    - "GET /api/v1/campaigns and GET /api/v1/campaigns/{id} require_viewer; POST /api/v1/campaigns requires require_analyst (viewer gets 403) — all campaign writes are analyst-gated (D-16)"
    - "Every campaign query filters Campaign.tenant_id == user.tenant_id in the WHERE clause (no cross-tenant read)"
    # --- edge probe lift: CAMP-01 adjacency (D-11) ---
    - "Launching a campaign on a remediation_id that already has an active campaign (closed_at IS NULL) returns the existing campaign with already_existed=true, no duplicate row, no second audit row"
    # --- edge probe lift: CAMP-01 empty ---
    - "A remediation_id with zero actionable members (or a single-finding group) creates/reads a campaign that renders pct_remediated=0 with no ZeroDivisionError / 500"
    # --- edge probe lift: CAMP-04 empty ---
    - "A zero-member campaign read returns total=0/open=0/in_progress=0/done=0 and writes no auto-complete audit row (0/0 is not 100%)"
  truths_backstop:
    # --- edge probe lift: CAMP-01 ordering (backstop) ---
    - statement: "GET /api/v1/campaigns returns a deterministic, stable order (created_at DESC, id tiebreak) so equal-timestamp campaigns never reorder between reads"
      verification: backstop
    # --- edge probe lift: CAMP-04 ordering (backstop) ---
    - statement: "When two lifecycle transitions would be detected on one read, audit rows are emitted in a stable, single-pass order (never duplicated across reads)"
      verification: backstop
  prohibitions:
    - "The campaigns table must never store a denormalized progress/percentage/MTTR column or a member-count snapshot (compute-on-read only, D-07)"
    - "The campaigns table must never add a campaign_members join table or a frozen asset-list snapshot (live membership only, D-02/D-03)"
    - "The D-11 uniqueness must never be expressed as a UniqueConstraint (Postgres has no partial UNIQUE CONSTRAINT) — only a partial UNIQUE INDEX with postgresql_where"
  artifacts:
    - path: "backend/app/campaigns/models.py"
      provides: "Campaign SQLAlchemy model (identity + lifecycle only)"
      contains: "class Campaign"
    - path: "backend/alembic/versions/049_add_campaigns.py"
      provides: "campaigns table + partial unique index uq_campaign_active_remediation"
      contains: "postgresql_where"
    - path: "backend/app/campaigns/service.py"
      provides: "get_or_create_campaign + minimal get_campaign_progress"
      contains: "get_or_create_campaign"
    - path: "backend/app/campaigns/router.py"
      provides: "POST / , GET / , GET /{id}"
      exports: ["router"]
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/campaigns/router.py"
      via: "app.include_router(campaigns_router, prefix='/api/v1/campaigns')"
      pattern: "campaigns_router"
    - from: "backend/app/campaigns/service.py"
      to: "backend/app/vulnerabilities/models.py::Vulnerability"
      via: "live WHERE remediation_id join for progress counts"
      pattern: "remediation_id"
---

<objective>
TRACER SLICE (verified before any expansion): prove the campaign persistence vertical end-to-end
— an analyst creates a campaign from a remediation group (`POST /api/v1/campaigns {remediation_id}`),
the row persists in a new `campaigns` table, and it reads back via `GET /api/v1/campaigns` with a
live-computed progress snapshot. This is the thinnest production-quality slice of CAMP-01 + CAMP-04:
migration + model + race-safe get-or-create + list/detail read + audit + RBAC.

The risky seam this tracer proves is DB persistence + the D-11 partial-unique-index get-or-create
race + fail-closed audit — NOT rendering. The visible campaign-list view consumes this exact contract
in Plan 04 (wave 2); the per-owner bulk ticketing (CAMP-02, Plan 02) and full progress/MTTR + lifecycle
(CAMP-03, Plan 03) expand on top of the same persisted identity row.

Purpose: establish the campaign identity + read contract every other plan builds on.
Output: `backend/app/campaigns/` module, migration 049, and a green `test_campaigns.py` CAMP-01/04 core.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-remediation-campaigns/38-CONTEXT.md
@.planning/phases/38-remediation-campaigns/38-RESEARCH.md
@.planning/phases/38-remediation-campaigns/38-PATTERNS.md

<interfaces>
<!-- Contracts the executor implements. Full code templates live in 38-RESEARCH.md Code Examples 1–4
     and 38-PATTERNS.md — read those; do not re-derive. Key identifiers pinned here: -->

Campaign columns (identity + lifecycle ONLY — no progress/label snapshot, D-07/Pattern 2):
  id (UUID pk), tenant_id (FK tenants CASCADE, indexed), remediation_id (String(200), indexed),
  created_by_user_id (FK users SET NULL), closed_at (DateTime tz, nullable),
  closed_by_user_id (FK users SET NULL), close_trigger (String(20) nullable: "manual"|"auto_complete"),
  created_at / updated_at (TimestampMixin).

Partial unique index (D-11, Pitfall 3): Index("uq_campaign_active_remediation", "tenant_id",
  "remediation_id", unique=True, postgresql_where=text("closed_at IS NULL")) in __table_args__.
  NEVER UniqueConstraint.

Derived display_status (never stored): closed_at IS NOT NULL OR (done==total AND total>0) => "COMPLETE"; else "ACTIVE".

Endpoints:
  POST /api/v1/campaigns  {remediation_id}  -> require_analyst -> {id, remediation_id, already_existed}
  GET  /api/v1/campaigns                    -> require_viewer  -> [CampaignSummary...]
  GET  /api/v1/campaigns/{id}               -> require_viewer  -> CampaignDetail

Minimal compute-on-read progress (Pitfall 2 filter — NEVER _base_open_vulns()):
  WHERE Vulnerability.tenant_id == tenant_id AND Vulnerability.remediation_id == campaign.remediation_id
        AND Vulnerability.status.in_(["OPEN","IN_PROGRESS","REMEDIATED"])   # excludes SUPPRESSED/FALSE_POSITIVE (D-18)
  pct_remediated = round(done/total*100) if total else 0                     # Pitfall 5 zero-guard

Registration analog: backend/app/main.py cspm_router (import :33, include_router :315).
</interfaces>
</context>

<artifacts_this_phase_produces>
<!-- Source-grounding excludes these newly-created symbols from drift verification. -->
- **Table:** `campaigns` (migration `049_add_campaigns.py`) with indexes `ix_campaigns_tenant_id`,
  `ix_campaigns_remediation_id`, `uq_campaign_active_remediation` (partial unique, `WHERE closed_at IS NULL`).
- **Model:** `app.campaigns.models.Campaign` — columns: id, tenant_id, remediation_id,
  created_by_user_id, closed_at, closed_by_user_id, close_trigger, created_at, updated_at.
- **No status enum column** — display status is DERIVED (`ACTIVE`/`COMPLETE`); `close_trigger` values `manual` / `auto_complete`.
- **Schemas:** `CampaignCreateRequest`, `CampaignSummary`, `CampaignDetail` (`app.campaigns.schemas`).
- **Service fns:** `get_or_create_campaign`, `get_campaign_progress` (`app.campaigns.service`).
- **Endpoints:** `POST /api/v1/campaigns`, `GET /api/v1/campaigns`, `GET /api/v1/campaigns/{id}`.
- **Audit action strings:** `campaign.create` (this plan); `campaign.bulk_assign` (Plan 02);
  `campaign.close`, `campaign.reactivate` (Plan 03). System actor `system:campaign-complete` (Plan 03).
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the D-11 one-way partial-unique-index schema before migrating</name>
  <read_first>
    - .planning/phases/38-remediation-campaigns/38-CONTEXT.md §D-11 (one-way reversibility)
    - .planning/phases/38-remediation-campaigns/38-RESEARCH.md Code Example 1 + 2, Pitfall 3
    - backend/alembic/versions/020_add_sla_tracking.py (postgresql_where precedent)
  </read_first>
  <decision>Apply the D-11 uniqueness as a Postgres PARTIAL UNIQUE INDEX on (tenant_id, remediation_id) WHERE closed_at IS NULL, shipped in migration 049. This is a one-way schema commitment (relaxing it later = a migration + dedup of any duplicates created).</decision>
  <context>D-11 is LOCKED in CONTEXT.md; this gate confirms the irreversible DB shape before it lands, per REVERSIBILITY_GATES. Postgres has no partial UNIQUE CONSTRAINT — a partial UNIQUE INDEX is the only mechanism (Pitfall 3). The alternative (a full unique constraint on tenant+remediation_id) would forbid ever re-launching a campaign on a remediation_id after an earlier one closed, breaking D-13/D-17.</context>
  <options>
    <option id="partial-unique-index">
      <name>Partial unique index WHERE closed_at IS NULL (recommended, matches D-11 + all research)</name>
      <pros>Exactly one ACTIVE campaign per (tenant, remediation_id); a closed campaign's remediation_id can get a fresh campaign; proven precedent (020_add_sla_tracking.py); race-safe via IntegrityError.</pros>
      <cons>One-way: relaxing later needs a migration + dedup.</cons>
    </option>
    <option id="full-unique-constraint">
      <name>Full unique constraint on (tenant_id, remediation_id)</name>
      <pros>Simplest DDL.</pros>
      <cons>Rejected — forbids re-launching after close (breaks D-13/D-17); contradicts CONTEXT.</cons>
    </option>
  </options>
  <resume-signal>Select: partial-unique-index (recommended) or partial-unique-index-with-changes</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: campaigns table migration + Campaign model + module skeleton</name>
  <files>backend/alembic/versions/049_add_campaigns.py, backend/app/campaigns/__init__.py, backend/app/campaigns/models.py, backend/tests/test_campaigns.py</files>
  <read_first>
    - backend/alembic/versions/047_add_remediation_events.py (table-create shape)
    - backend/alembic/versions/020_add_sla_tracking.py (postgresql_where mechanics)
    - backend/app/vulnerabilities/models.py:253-297 (RemediationEvent FK/Mixin conventions)
    - backend/app/db/base.py (Base / TimestampMixin / UUIDPrimaryKeyMixin)
    - .planning/phases/38-remediation-campaigns/38-RESEARCH.md Code Examples 1 & 2
  </read_first>
  <behavior>
    - Test: `alembic upgrade head` creates a `campaigns` table with a partial unique index whose predicate is `closed_at IS NULL`.
    - Test: two active campaigns for the same (tenant_id, remediation_id) with closed_at NULL raise IntegrityError at the DB.
    - Test: a campaign whose closed_at is set does NOT block a second active campaign on the same remediation_id.
  </behavior>
  <action>
    Confirm the current alembic head with `ls backend/alembic/versions | tail -3` (expected `048_add_clean_scan_streak`); set `down_revision` to the actual head. Create `049_add_campaigns.py` per 38-RESEARCH.md Code Example 2: `campaigns` table with columns id/tenant_id/remediation_id/created_by_user_id/closed_at/closed_by_user_id/close_trigger/created_at/updated_at, plus `ix_campaigns_tenant_id`, `ix_campaigns_remediation_id`, and `op.create_index("uq_campaign_active_remediation", "campaigns", ["tenant_id","remediation_id"], unique=True, postgresql_where=sa.text("closed_at IS NULL"))`. Create `app/campaigns/__init__.py` and `app/campaigns/models.py::Campaign` per Code Example 1 (Index+text imports; FK ondelete CASCADE for tenant, SET NULL for user FKs; docstring cites D-01/D-02/D-07/D-11). Seed `backend/tests/test_campaigns.py` with the Wave-0 unit tests `test_campaign_unique_active_index` and `test_new_campaign_after_close` (direct model insert + flush, IntegrityError assertion mirroring sla_tier_service test style).
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret alembic upgrade head && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret pytest tests/test_campaigns.py::test_campaign_unique_active_index tests/test_campaigns.py::test_new_campaign_after_close -x</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` succeeds and creates index `uq_campaign_active_remediation` with `WHERE (closed_at IS NULL)` (verify via `\d+ campaigns` or migration introspection).
    - `test_campaign_unique_active_index` proves a duplicate active row raises IntegrityError.
    - `test_new_campaign_after_close` proves a closed campaign's remediation_id accepts a new active campaign.
    - `alembic downgrade -1` cleanly drops the table + indexes.
  </acceptance_criteria>
  <done>Migration applied, Campaign model importable, both DB-constraint tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: get-or-create service + schemas + router (POST/GET/GET-detail) with audit + RBAC</name>
  <files>backend/app/campaigns/schemas.py, backend/app/campaigns/service.py, backend/app/campaigns/router.py, backend/app/main.py, backend/tests/test_campaigns.py</files>
  <read_first>
    - backend/app/vulnerabilities/sla_tier_service.py:406-428 (IntegrityError + begin_nested race-safe insert)
    - backend/app/vulnerabilities/router.py:781-871 (remediations_grouped read + suppress_remediation write+audit precedent)
    - backend/app/vulnerabilities/remediation_service.py:14-32 (_base_open_vulns — DO NOT reuse, Pitfall 2)
    - backend/app/ticketing/schemas.py:69 (extra="forbid" convention)
    - backend/app/audit.py:143-196 (audit() signature, fail-closed)
    - backend/app/auth/rbac.py:50-53 (require_analyst/require_viewer)
    - backend/app/main.py:33,309-320 (cspm_router registration analog)
    - .planning/phases/38-remediation-campaigns/38-RESEARCH.md Code Examples 3 & 4
  </read_first>
  <behavior>
    - Test: POST /api/v1/campaigns with a fresh remediation_id -> 200, persists a row, already_existed=false, writes one campaign.create audit row.
    - Test: POST again on the same active remediation_id -> 200, already_existed=true, NO new row, NO second audit row.
    - Test: GET /api/v1/campaigns/{id} returns total/open/in_progress/done/pct_remediated computed over status IN (OPEN,IN_PROGRESS,REMEDIATED); a REMEDIATED member IS counted in done (Pitfall 2 regression).
    - Test: zero-member remediation_id -> pct_remediated=0, no 500 (Pitfall 5).
    - Test: viewer role -> 403 on POST; viewer CAN GET.
    - Test: a campaign from tenant A is invisible to tenant B (tenant scoping / IDOR).
  </behavior>
  <action>
    `schemas.py`: `CampaignCreateRequest(model_config=ConfigDict(extra="forbid"))` with `remediation_id: str`; `CampaignSummary` / `CampaignDetail` response models exposing id, remediation_id, derived status ("ACTIVE"/"COMPLETE"), total, open, in_progress, done, pct_remediated. `service.py`: `get_or_create_campaign(db, tenant_id, remediation_id, user) -> tuple[Campaign, bool]` (SELECT active-first, INSERT in `db.begin_nested()`, catch IntegrityError, re-SELECT) per Code Example 3; `get_campaign_progress(db, tenant_id, remediation_id) -> dict` per Code Example 3 with the corrected `status.in_(["OPEN","IN_PROGRESS","REMEDIATED"])` filter and `round(done/total*100) if total else 0`; a `list_campaigns` reader ordered `created_at DESC, id`. `router.py`: `POST ""` (require_analyst; audit `campaign.create` only when is_new; commit), `GET ""` (require_viewer; list + per-row progress), `GET "/{campaign_id}"` (require_viewer; `_get_campaign_or_404` scoped by tenant_id + progress). Derive display status in the response layer (closed_at OR done==total>0 => COMPLETE). Register in `main.py` alongside cspm_router: `app.include_router(campaigns_router, prefix="/api/v1/campaigns", tags=["Campaigns"])`. Extend `test_campaigns.py` with `test_create_campaign_new`, `test_create_campaign_reopens_existing`, `test_progress_counts_include_remediated`, `test_progress_zero_member_no_crash`, `test_campaign_rbac`, and a cross-tenant isolation test.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") JWT_SECRET_KEY=test-secret pytest tests/test_campaigns.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `POST /api/v1/campaigns` returns 200 + persists a row + writes exactly one `campaign.create` audit row (assert via audit_logs query).
    - Re-POST on an active remediation_id returns `already_existed=true`, adds no row, adds no audit row.
    - `test_progress_counts_include_remediated` proves a REMEDIATED member appears in `done` (not filtered out).
    - `test_progress_zero_member_no_crash` returns pct_remediated=0 with HTTP 200.
    - `test_campaign_rbac`: viewer 403 on POST, 200 on GET.
    - Cross-tenant test: tenant B GET on tenant A's campaign id -> 404.
  </acceptance_criteria>
  <done>All test_campaigns.py cases green; POST→persist→GET readback proven; module registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (POST/GET campaigns) | untrusted remediation_id + campaign_id path params cross here |
| API → DB | every query must be tenant-scoped |

## STRIDE Threat Register (ASVS L1 · block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | I/E | GET /campaigns/{id} (IDOR via guessed campaign_id) | mitigate | `_get_campaign_or_404` includes `Campaign.tenant_id == user.tenant_id` in the WHERE clause, not a post-fetch filter; cross-tenant test asserts 404 |
| T-38-02 | T | POST /campaigns body | mitigate | `CampaignCreateRequest` uses `model_config = ConfigDict(extra="forbid")` (ASVS V5 mass-assignment) |
| T-38-03 | R | campaign.create audit write | mitigate | call `audit()` as-is (fail-closed); never wrap in try/except; commit immediately after |
| T-38-04 | E | POST /campaigns RBAC | mitigate | `Depends(require_analyst)` on POST; `require_viewer` on GETs (D-16); rbac test asserts viewer 403 |
| T-38-06 | T (race) | D-11 concurrent launch | mitigate | partial unique index + `begin_nested()` + IntegrityError re-SELECT; `test_campaign_unique_active_index` proves DB-level rejection |
</threat_model>

<verification>
- `alembic upgrade head` then `alembic downgrade -1` both clean.
- `pytest backend/tests/test_campaigns.py -x` fully green (run per-file with ENCRYPTION_KEY + JWT_SECRET_KEY set).
- Round-trip smoke: `POST /api/v1/campaigns {remediation_id}` then `GET /api/v1/campaigns` shows the row.
</verification>

<success_criteria>
- A campaign can be created from a remediation_id, persists to `campaigns`, and reads back with a live progress snapshot (tracer proven at the API contract).
- D-11 get-or-create is race-safe and audited exactly once per new campaign.
- RBAC + tenant-scoping enforced on all three endpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/38-remediation-campaigns/38-01-SUMMARY.md`.
</output>
