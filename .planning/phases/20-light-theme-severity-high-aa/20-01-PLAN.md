---
phase: 20-light-theme-severity-high-aa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/globals.css
  - .claude/skills/sketch-findings-getvul/references/foundation.md
  - .claude/skills/sketch-findings-getvul/references/visual-language.md
  - .claude/skills/sketch-findings-getvul/sources/themes/sunset.css
autonomous: true
requirements: [UX-D-03-02, UX-D-03-03]

must_haves:
  truths:
    - "A --color-severity-high-on-soft token exists that resolves to a light value clearing WCAG 2.1 AA (>=4.5:1) on the warm-cream light surfaces (bg #FAF7F2 / surface-2 #F7F2EA / bg-severity-high/10 tint)"
    - "In dark mode the token is byte-identical to the existing --color-severity-high (#FB923C) so dark rendering is unchanged (dark no-op)"
    - "The token change is reconciled into the design-system skill (foundation.md + visual-language.md + sunset.css) so the skill and the live CSS agree"
  artifacts:
    - path: "frontend/src/app/globals.css"
      provides: "--color-severity-high-on-soft in the light block (#9A3412) and the dark block (#FB923C)"
      contains: "--color-severity-high-on-soft"
    - path: ".claude/skills/sketch-findings-getvul/references/foundation.md"
      provides: "severity-high on-soft light value annotated in the severity token table"
      contains: "severity-high-on-soft"
    - path: ".claude/skills/sketch-findings-getvul/references/visual-language.md"
      provides: "severity-high row in the light-mode 'Text on -soft fills' table"
      contains: "severity-high-on-soft"
    - path: ".claude/skills/sketch-findings-getvul/sources/themes/sunset.css"
      provides: "--color-severity-high-on-soft in the :root[data-theme=light] block"
      contains: "--color-severity-high-on-soft"
  key_links:
    - from: "frontend/src/app/globals.css light block"
      to: "consumers (Plan 20-02) via text-[var(--color-severity-high-on-soft)]"
      via: "CSS custom property cascade"
      pattern: "--color-severity-high-on-soft:\\s*#9A3412"
    - from: "frontend/src/app/globals.css dark block"
      to: "dark-mode no-op (= #FB923C, current severity-high dark value)"
      via: "CSS custom property cascade"
      pattern: "--color-severity-high-on-soft:\\s*#FB923C"
---

<objective>
Add the missing `--color-severity-high-on-soft` design token (light + dark values) and reconcile
it into the `sketch-findings-getvul` design skill.

Root cause (from `.planning/v2.2-MILESTONE-AUDIT.md`): `--color-severity-high` (#EA580C, orange-600)
has NO light-mode `-on-soft` variant. It is used bare as `text-severity-high` across ~18 components,
and on the light soft surface `#F7F2EA` it measures **3.19:1** (needs 4.5:1). Phase 16's WR-04 added
`-on-soft` variants for amber/pink/violet exactly this way but never covered severity-high. This plan
adds the token; Plan 20-02 migrates the consumers and proves the sweep green.

Purpose: establish the single source of truth (the CSS custom property) that the consumer migration
in Plan 20-02 builds against, and keep the design skill authoritative (CLAUDE.md: a token change that
is not reconciled into the skill is incomplete).
Output: `--color-severity-high-on-soft` in globals.css (both theme blocks) + three reconciled skill files.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-MILESTONE-AUDIT.md
@.planning/phases/16-light-theme-visual-completion/16-VERIFICATION.md

<chosen-values>
Light value: **#9A3412** (Tailwind orange-800).
  - on surface-2 #F7F2EA:              6.56:1  (target >=4.5:1) ✓
  - on bg #FAF7F2:                     6.84:1  ✓
  - on the bg-severity-high/10 tint (#F8E7DB, = 10% #EA580C over #FAF7F2): 6.07:1  ✓
  (The current bare #EA580C measures 3.19:1 on #F7F2EA — this is the exact 3.19:1 the audit reports.)

Dark value: **#FB923C** (Tailwind orange-400) = the current --color-severity-high dark value in
sunset.css :root. Setting the dark on-soft to this makes the Plan 20-02 migration a TRUE dark no-op
(byte-identical), exactly mirroring the WR-02 amber-on-soft precedent (amber-on-soft dark = #F59E0B =
old text-amber). Dark rendering does not change, so the dark axe sweep stays green.

Distinctness note (UX-D-03-03): #9A3412 (High) sits near #B45309 (Medium, amber-700) in the dark
orange/brown range in light mode. This is acceptable and consistent with the design system's existing
light-mode severity choices — distinctness is preserved by the locked THREE-AXIS encoding (color +
Unicode glyph ▲ vs ◆ + text label), not by hue alone. Do NOT pick a hue that collides with red
(#DC2626 critical); orange-800 stays clearly warmer/oranger than red.
</chosen-values>

<precedent>
The amber/pink/violet on-soft tokens (Phase 16 / BL-04) are the pattern to mirror EXACTLY. In
frontend/src/app/globals.css they appear twice:
  - light block :root[data-theme="light"] (lines ~51-55): #5B21B6 / #9D174D / #92400E
  - dark block  :root[data-theme="dark"]  (lines ~75-86): #C4B5FD / #F472B6 / #F59E0B
sunset.css carries the light values in its :root[data-theme="light"] block (lines ~171-173) with a
matching dark comment; foundation.md annotates them (lines ~33-36); visual-language.md has the
"Text on -soft fills" light-mode table (lines ~112-120). Add severity-high-on-soft to the SAME places
in the SAME style, with the same "was dark ... / light ..." comment convention.
</precedent>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --color-severity-high-on-soft to globals.css (light + dark blocks)</name>
  <read_first>
    - frontend/src/app/globals.css — the :root[data-theme="light"] on-soft block (lines ~51-55, the
      amber/pink/violet-on-soft trio) and the :root[data-theme="dark"] on-soft block (lines ~84-86,
      the #C4B5FD/#F472B6/#F59E0B trio with the WR-03 comment). Add the new token alongside each.
  </read_first>
  <files>frontend/src/app/globals.css</files>
  <action>
    In the light block (`:root[data-theme="light"]`), immediately after the three existing on-soft
    declarations (`--color-violet-on-soft` / `--color-pink-on-soft` / `--color-amber-on-soft`), add:

      --color-severity-high-on-soft: #9A3412;  /* orange-800 — text on severity-high /10 tint & cream (6.56:1 on #F7F2EA); base #EA580C failed at 3.19:1 (Phase 20, UX-D-03-02/03) */

    In the dark block (`:root[data-theme="dark"]`), immediately after the three existing dark on-soft
    declarations (`--color-violet-on-soft` / `--color-pink-on-soft` / `--color-amber-on-soft`), add:

      --color-severity-high-on-soft: #FB923C;  /* = current --color-severity-high dark value → dark no-op (mirrors WR-02 amber-on-soft) */

    Do NOT touch --color-severity-high itself (stays #EA580C light / #FB923C dark). Do NOT touch any
    other token. This is purely additive.
  </action>
  <acceptance_criteria>
    - `grep -c -- '--color-severity-high-on-soft' frontend/src/app/globals.css` returns exactly `2`
    - `grep -nE -- '--color-severity-high-on-soft:\s*#9A3412' frontend/src/app/globals.css` matches inside the light block
    - `grep -nE -- '--color-severity-high-on-soft:\s*#FB923C' frontend/src/app/globals.css` matches inside the dark block
    - `grep -c ':root\[data-theme="light"\]' frontend/src/app/globals.css` still returns `1` (no duplicate selector introduced)
    - `--color-severity-high:` still resolves to `#EA580C` in the light block (unchanged): `grep -nE -- '--color-severity-high:\s*#EA580C' frontend/src/app/globals.css` matches
  </acceptance_criteria>
  <verify>
    <automated>grep -c -- '--color-severity-high-on-soft' frontend/src/app/globals.css | grep -qx 2 && echo TOKEN_ADDED</automated>
  </verify>
  <done>globals.css carries `--color-severity-high-on-soft` = #9A3412 in the light block and #FB923C in the dark block; the base severity-high token and light-block selector are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the token into the design-system skill (foundation.md + visual-language.md + sunset.css)</name>
  <read_first>
    - .claude/skills/sketch-findings-getvul/references/foundation.md — the on-soft annotation block (lines ~33-36) and the "Severity colors" table (lines ~51-59).
    - .claude/skills/sketch-findings-getvul/references/visual-language.md — the "Text on -soft fills" light-mode variants table (lines ~112-120) and the light-mode severity table (lines ~21-27).
    - .claude/skills/sketch-findings-getvul/sources/themes/sunset.css — the on-soft trio in the :root[data-theme="light"] block (lines ~171-173).
  </read_first>
  <files>.claude/skills/sketch-findings-getvul/references/foundation.md, .claude/skills/sketch-findings-getvul/references/visual-language.md, .claude/skills/sketch-findings-getvul/sources/themes/sunset.css</files>
  <action>
    Mirror the token into all three skill files, matching the existing on-soft documentation style:

    1. sunset.css — in the `:root[data-theme="light"]` on-soft block (right after `--color-amber-on-soft: #92400E;`), add:
         --color-severity-high-on-soft: #9A3412;  /* orange-800 — text on severity-high /10 tint & cream, 6.56:1 on #F7F2EA (Phase 20) */
       (sunset.css is a vendored, non-runtime copy — this keeps the skill authoritative per CLAUDE.md; it does NOT need a dark-block entry since the skill's dark :root does not vendor on-soft severity, but add a trailing comment noting the dark no-op value #FB923C for parity.)

    2. foundation.md — extend the on-soft annotation comment (the block that lists
       "pink-on-soft #F472B6, violet-on-soft #C4B5FD, amber-on-soft #F59E0B ... Light-mode on-soft: ...")
       to include `severity-high-on-soft #9A3412 (light) / #FB923C (dark)`. Also add a one-line note under
       the "Severity colors" table that severity-high has an `-on-soft` text variant for light soft/tinted
       fills (#9A3412, 6.56:1 on #F7F2EA), because the bare #EA580C fails 4.5:1 on those surfaces.

    3. visual-language.md — add a `severity-high` row to the light-mode "Text on -soft fills" table
       (the table with the #5B21B6 / #9D174D / #92400E rows):
         | `--color-severity-high` /10 tint | `--color-severity-high-on-soft` | `#9A3412` (orange-800) | ~6.07:1 on the /10 tint (6.56:1 on #F7F2EA) |
       And add a sentence to the light-mode severity section noting that where severity-high renders as
       TEXT/glyph on a soft/tinted cream surface, `--color-severity-high-on-soft` is used (the bare
       token is reserved for borders, ring strokes, chart lines, and background fills). Reference
       Phase 20 / UX-D-03-02,-03 the way the existing rows reference 16-01-SUMMARY.md.
  </action>
  <acceptance_criteria>
    - `grep -c 'severity-high-on-soft' .claude/skills/sketch-findings-getvul/sources/themes/sunset.css` returns `>=1`
    - `grep -nE -- '--color-severity-high-on-soft:\s*#9A3412' .claude/skills/sketch-findings-getvul/sources/themes/sunset.css` matches in the light block
    - `grep -c 'severity-high-on-soft' .claude/skills/sketch-findings-getvul/references/foundation.md` returns `>=1`
    - `grep -c 'severity-high-on-soft' .claude/skills/sketch-findings-getvul/references/visual-language.md` returns `>=1`
    - `grep -c '9A3412' .claude/skills/sketch-findings-getvul/references/visual-language.md` returns `>=1`
  </acceptance_criteria>
  <verify>
    <automated>for f in .claude/skills/sketch-findings-getvul/sources/themes/sunset.css .claude/skills/sketch-findings-getvul/references/foundation.md .claude/skills/sketch-findings-getvul/references/visual-language.md; do grep -q 'severity-high-on-soft' "$f" || { echo "MISSING in $f"; exit 1; }; done; echo RECONCILED</automated>
  </verify>
  <done>All three skill files carry the severity-high on-soft variant with its light hex (#9A3412) and measured ratio, in the same style as the amber/pink/violet on-soft rows; the skill and globals.css now agree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced or affected. This plan edits CSS custom-property declarations and three markdown/CSS
design-documentation files. There is no I/O, no request handling, no user input, no auth/authz path,
no data flow, and no dependency change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Spoofing | (none) | accept | No identity/auth surface touched — CSS token + docs only. N/A. |
| T-20-02 | Tampering | (none) | accept | No persisted data or request path. A static color value cannot be tampered to escalate. N/A. |
| T-20-03 | Repudiation | (none) | accept | No audit-relevant action. N/A. |
| T-20-04 | Information Disclosure | (none) | accept | No data read/written; hex color values are not sensitive. N/A. |
| T-20-05 | Denial of Service | (none) | accept | CSS custom property; no runtime cost, no input to exhaust. N/A. |
| T-20-06 | Elevation of Privilege | (none) | accept | No privilege boundary exists in a CSS/docs change. N/A. |

No high/medium threats. This change introduces zero new attack surface (ASVS L1: nothing in scope).
</threat_model>

<verification>
- globals.css contains `--color-severity-high-on-soft` exactly twice (light #9A3412, dark #FB923C); base severity-high unchanged; single light selector preserved.
- All three skill files (`sunset.css`, `foundation.md`, `visual-language.md`) reference `severity-high-on-soft`.
- No consumer files touched in this plan (that is Plan 20-02).
</verification>

<success_criteria>
- The token exists in both theme blocks with the chosen, contrast-verified values.
- The design skill is reconciled (skill == live CSS), satisfying CLAUDE.md's reconciliation rule.
- Nothing else is modified; dark rendering is guaranteed unchanged (dark value = existing severity-high dark).
</success_criteria>

<output>
After completion, create `.planning/phases/20-light-theme-severity-high-aa/20-01-SUMMARY.md`.
</output>
