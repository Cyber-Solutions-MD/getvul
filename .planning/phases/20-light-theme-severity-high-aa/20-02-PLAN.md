---
phase: 20-light-theme-severity-high-aa
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx
  - frontend/src/components/ui/RiskRing.tsx
  - frontend/src/components/ui/trend-chart.tsx
  - frontend/src/components/tickets/severity-glyph.ts
  - frontend/src/components/tickets/vuln-count.tsx
  - frontend/src/components/tickets/sla-pill.tsx
  - frontend/src/components/tickets/ticket-drill-content.tsx
  - frontend/src/components/dashboard/top5-card.tsx
  - frontend/src/components/users/directory-table.tsx
  - frontend/src/components/assets/asset-vulns-list.tsx
  - frontend/src/components/assets/remediation-timeline.tsx
  - frontend/src/components/assets/assets-table.tsx
  - frontend/src/components/assets/severity-ribbon.tsx
  - frontend/src/components/assets/risk-card.tsx
  - frontend/src/components/cspm/microcopy.ts
  - frontend/src/components/vulnerabilities/chip-bar.tsx
  - frontend/src/components/vulnerabilities/vuln-table.tsx
  - frontend/src/components/tickets/sla-pill.test.tsx
  - frontend/src/components/tickets/vuln-count.test.tsx
  - frontend/src/components/assets/severity-ribbon.test.tsx
  - frontend/src/components/ui/ChipBar.test.tsx
autonomous: true
requirements: [UX-D-03-02, UX-D-03-03]

must_haves:
  truths:
    - "Every foreground `text-severity-high` site that renders severity-high TEXT or a glyph now uses `text-[var(--color-severity-high-on-soft)]` — so on light cream surfaces the High pill/glyph/score can read at >=4.5:1"
    - "Non-foreground uses of severity-high (background fills `bg-severity-high`, borders `border-severity-high`, the RiskRing SVG gradient stroke, the trend-chart `var(--color-severity-high)` line) are UNCHANGED"
    - "Dark mode is visually unchanged (the on-soft token resolves to #FB923C = the current dark severity-high), so no dark regression is introduced"
    - "Unit tests that asserted `text-severity-high` on migrated components now assert `text-[var(--color-severity-high-on-soft)]` and pass; the full unit suite and typecheck stay green"
    - "No route exceeds 250 KB First-Load JS; the change is CSS-class-string only so the JS bundle delta is 0 (proven by `next build`, no live server needed)"
  artifacts:
    - path: "frontend/src/components/tickets/sla-pill.tsx"
      provides: "Soon SLA tier text via the on-soft token (keeps border-severity-high/30 bg-severity-high/10)"
      contains: "text-[var(--color-severity-high-on-soft)]"
    - path: "frontend/src/components/vulnerabilities/vuln-table.tsx"
      provides: "High severity glyph color + Soon SLA band tone via the on-soft token — the /dashboard/vulnerabilities failing site"
      contains: "text-[var(--color-severity-high-on-soft)]"
    - path: "frontend/src/components/dashboard/top5-card.tsx"
      provides: "HIGH glyph color via the on-soft token"
      contains: "text-[var(--color-severity-high-on-soft)]"
  key_links:
    - from: "the ~18 migrated consumer sites"
      to: "globals.css --color-severity-high-on-soft (from Plan 20-01)"
      via: "Tailwind JIT arbitrary value text-[var(--color-severity-high-on-soft)]"
      pattern: "text-\\[var\\(--color-severity-high-on-soft\\)\\]"
---

<objective>
Migrate the ~18 foreground `text-severity-high` sites to `text-[var(--color-severity-high-on-soft)]`
(the token added in Plan 20-01), update the affected unit-test assertions, and prove no regression
statically (grep-clean foreground, unit suite + typecheck green, `next build` bundle budget held).

The live-server axe sweep — the phase's real acceptance arbiter — is deliberately split into its own
plan **20-03** (own wave) so this plan's executor is not asked to both edit 21 files AND stand up the
full docker/prod-build/Playwright gate in one context (which risks context exhaustion and rushed
evidence, the exact failure mode the audit caught in Phase 16).

Purpose: on light cream surfaces, severity-high text/glyphs currently read at 3.19:1 (fails WCAG AA
4.5:1); after migration they read at >=6:1 via the on-soft token, while dark is a byte-identical no-op.
Output: migrated consumers + updated tests, statically verified; 20-03 then proves it live.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2.2-MILESTONE-AUDIT.md
@.planning/phases/20-light-theme-severity-high-aa/20-01-PLAN.md
@.planning/phases/16-light-theme-visual-completion/16-VERIFICATION.md

<migration-rule>
MECHANICAL RULE: replace the FOREGROUND text/glyph color class `text-severity-high` with
`text-[var(--color-severity-high-on-soft)]`. In combined class strings, migrate ONLY the `text-`
token and LEAVE `border-severity-high/NN` and `bg-severity-high/NN` in place (the border/fill still
use the base accent — same as the WR-04 amber/pink/violet precedent, where only the text lifted).

This is safe to apply to every foreground occurrence because in DARK the on-soft token resolves to
#FB923C = the current dark severity-high, i.e. a byte-identical no-op; in LIGHT it lifts #EA580C
(3.19:1) → #9A3412 (>=6:1). The live axe sweep in Plan 20-03 is the final arbiter.

DO NOT migrate (these are not foreground text — leave untouched):
  - frontend/src/app/login/page.tsx:139  → `bg-severity-high` (a color swatch dot; /login is not in the authed sweep anyway)
  - frontend/src/components/ui/trend-chart.tsx:48 → `high: 'var(--color-severity-high)'` (chart LINE stroke — a graphic, governed by the 3:1 rule, not 4.5:1)
  - the RiskRing SVG gradient stops (#EC4899/#A78BFA/#F59E0B) — brand asset, unrelated
</migration-rule>

<foreground-sites>
18 source sites to migrate `text-severity-high` → `text-[var(--color-severity-high-on-soft)]`:
  1. frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx:76   (SEVERITY glyph map `high`)
  2. frontend/src/components/ui/RiskRing.tsx:42                      (BAND_TINT `high` — center score text)
  3. frontend/src/components/ui/trend-chart.tsx:121                 (aria-hidden legend arrow span)
  4. frontend/src/components/tickets/severity-glyph.ts:23           (SEVERITY_CLASS `high` glyph map)
  5. frontend/src/components/tickets/vuln-count.tsx:37              (High count span) — ALSO update the doc comment on line 4
  6. frontend/src/components/tickets/sla-pill.tsx:52                (Soon tier — migrate only the text- token; keep border-/bg-)
  7. frontend/src/components/tickets/ticket-drill-content.tsx:70    (glyph map `high`)
  8. frontend/src/components/dashboard/top5-card.tsx:16             (GLYPH_COLOR `HIGH`)
  9. frontend/src/components/users/directory-table.tsx:33           (score>=50 branch — migrate only text-; keep border-/bg-)
 10. frontend/src/components/assets/asset-vulns-list.tsx:16         (HIGH tint)
 11. frontend/src/components/assets/remediation-timeline.tsx:33     (IN_PROGRESS — migrate only text-; keep border-/bg-)
 12. frontend/src/components/assets/assets-table.tsx:34             (BAND_TINT `high`)
 13. frontend/src/components/assets/severity-ribbon.tsx:16          (High ribbon entry tint)
 14. frontend/src/components/assets/risk-card.tsx:64                (tintClass="text-severity-high")
 15. frontend/src/components/cspm/microcopy.ts:48                   (HIGH tint)
 16. frontend/src/components/vulnerabilities/chip-bar.tsx:19        (severity glyph map `high`)
 17. frontend/src/components/vulnerabilities/vuln-table.tsx:65      (GLYPH_COLOR `high`)  ← /dashboard/vulnerabilities failure
 18. frontend/src/components/vulnerabilities/vuln-table.tsx:104     (slaBand Soon tone `text-severity-high`)

Line numbers are from the 2026-07-20 grep and may drift — locate each by the class string, not the line.
</foreground-sites>

<test-sites>
4 test files that assert on / carry `text-severity-high` (update to the on-soft class + prose):
  - frontend/src/components/tickets/sla-pill.test.tsx:33         (assertion → text-[var(--color-severity-high-on-soft)])
  - frontend/src/components/tickets/vuln-count.test.tsx:18       (assertion) + line 6 (it() title prose)
  - frontend/src/components/assets/severity-ribbon.test.tsx:19   (assertion → on-soft)
  - frontend/src/components/ui/ChipBar.test.tsx:35               (SEVERITY_AXIS fixture glyphClassName — ChipBar is a passthrough primitive, so any string passes; update it to the on-soft class to keep the repo grep clean and consistent)
</test-sites>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the 18 foreground text-severity-high sites to the on-soft token</name>
  <read_first>
    - .planning/phases/20-light-theme-severity-high-aa/20-01-PLAN.md — confirm the token name and chosen values.
    - Each file in <foreground-sites> — read the specific class string in context to confirm it is a
      FOREGROUND text/glyph color (migrate) and not a `bg-`/`border-`/`var()` use (leave). For the
      combined-class files (sla-pill.tsx, directory-table.tsx, remediation-timeline.tsx) confirm you
      change ONLY the `text-severity-high` token within the string.
    - frontend/src/components/vulnerabilities/vuln-table.tsx (lines ~63-69 GLYPH_COLOR, ~93-109 slaBand) — this is the /dashboard/vulnerabilities site that fails the live sweep; both the glyph map `high` AND the Soon SLA `tone` must migrate.
  </read_first>
  <files>frontend/src/app/(authed)/dashboard/tickets/[id]/page.tsx, frontend/src/components/ui/RiskRing.tsx, frontend/src/components/ui/trend-chart.tsx, frontend/src/components/tickets/severity-glyph.ts, frontend/src/components/tickets/vuln-count.tsx, frontend/src/components/tickets/sla-pill.tsx, frontend/src/components/tickets/ticket-drill-content.tsx, frontend/src/components/dashboard/top5-card.tsx, frontend/src/components/users/directory-table.tsx, frontend/src/components/assets/asset-vulns-list.tsx, frontend/src/components/assets/remediation-timeline.tsx, frontend/src/components/assets/assets-table.tsx, frontend/src/components/assets/severity-ribbon.tsx, frontend/src/components/assets/risk-card.tsx, frontend/src/components/cspm/microcopy.ts, frontend/src/components/vulnerabilities/chip-bar.tsx, frontend/src/components/vulnerabilities/vuln-table.tsx</files>
  <action>
    For each of the 18 sites in <foreground-sites>, replace the foreground class `text-severity-high`
    with `text-[var(--color-severity-high-on-soft)]`. Per <migration-rule>:
      - Standalone maps/tints (page.tsx:76, RiskRing:42, trend-chart:121, severity-glyph:23,
        vuln-count:37, ticket-drill-content:70, top5-card:16, asset-vulns-list:16, assets-table:34,
        severity-ribbon:16, risk-card:64, cspm/microcopy:48, chip-bar:19, vuln-table:65, vuln-table:104):
        swap the whole value string `text-severity-high` → `text-[var(--color-severity-high-on-soft)]`.
      - Combined strings — change ONLY the text token, keep the rest verbatim:
          sla-pill.tsx:52          `border-severity-high/30 bg-severity-high/10 text-severity-high`
                                 → `border-severity-high/30 bg-severity-high/10 text-[var(--color-severity-high-on-soft)]`
          directory-table.tsx:33   `text-severity-high border-severity-high/30 bg-severity-high/10`
                                 → `text-[var(--color-severity-high-on-soft)] border-severity-high/30 bg-severity-high/10`
          remediation-timeline.tsx:33 `border-severity-high/40 bg-severity-high/10 text-severity-high`
                                 → `border-severity-high/40 bg-severity-high/10 text-[var(--color-severity-high-on-soft)]`
    Also update the vuln-count.tsx doc comment (line ~4: "high text-severity-high") to say
    "high text-[var(--color-severity-high-on-soft)]" so the repo grep in Task 2 lands at zero.
    Do NOT touch login/page.tsx (bg-), trend-chart.tsx:48 (var stroke), or any `bg-severity-high` /
    `border-severity-high` token.
  </action>
  <acceptance_criteria>
    - `grep -rn 'text-severity-high' frontend/src --include='*.tsx' --include='*.ts' | grep -v '\.test\.'` returns 0 matches (all NON-test foreground + prose references resolved)
    - `grep -rl 'text-\[var(--color-severity-high-on-soft)\]' frontend/src --include='*.tsx' --include='*.ts' | grep -v '\.test\.' | wc -l` returns `>= 17` (17 source files; vuln-table has 2 hits in one file)
    - severity-high base token STILL present for non-text uses: `grep -rc 'bg-severity-high' frontend/src | grep -v ':0' | head` shows sla-pill.tsx / directory-table.tsx / remediation-timeline.tsx / login/page.tsx still contain `bg-severity-high`
    - trend-chart.tsx still contains the chart-stroke `var(--color-severity-high)`: `grep -c 'var(--color-severity-high)' frontend/src/components/ui/trend-chart.tsx` returns `>=1`
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -rn 'text-severity-high' frontend/src --include='*.tsx' --include='*.ts' | grep -v '\.test\.' | wc -l | tr -d ' ')" = "0" && echo SOURCE_MIGRATED</automated>
  </verify>
  <done>All 18 foreground sites use `text-[var(--color-severity-high-on-soft)]`; `bg-`/`border-`/`var()` severity-high uses are untouched; no non-test `text-severity-high` remains in frontend/src.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update the affected unit-test assertions and run the unit suite</name>
  <read_first>
    - frontend/src/components/tickets/sla-pill.test.tsx (line ~33 assertion on the Soon pill)
    - frontend/src/components/tickets/vuln-count.test.tsx (line ~6 it() title, line ~18 assertion)
    - frontend/src/components/assets/severity-ribbon.test.tsx (line ~19 assertion)
    - frontend/src/components/ui/ChipBar.test.tsx (line ~35 SEVERITY_AXIS fixture glyphClassName)
  </read_first>
  <behavior>
    - sla-pill.test.tsx: "Soon" tier pill className contains `text-[var(--color-severity-high-on-soft)]` (was text-severity-high); other tiers (critical/low/faint) assertions unchanged and still pass.
    - vuln-count.test.tsx: the High span className contains `text-[var(--color-severity-high-on-soft)]`; the critical span still asserts `text-severity-critical`.
    - severity-ribbon.test.tsx: the High ribbon entry className contains `text-[var(--color-severity-high-on-soft)]`; zero-count dimming (text-text-faint) assertions unchanged.
    - ChipBar.test.tsx: SEVERITY_AXIS high-chip fixture glyphClassName is `text-[var(--color-severity-high-on-soft)]`; the ChipBar passthrough render test still passes (primitive renders whatever class it is given).
  </behavior>
  <files>frontend/src/components/tickets/sla-pill.test.tsx, frontend/src/components/tickets/vuln-count.test.tsx, frontend/src/components/assets/severity-ribbon.test.tsx, frontend/src/components/ui/ChipBar.test.tsx</files>
  <action>
    In each test file, replace the `text-severity-high` occurrence (assertion string and/or fixture)
    with `text-[var(--color-severity-high-on-soft)]`. In vuln-count.test.tsx also update the it()
    title prose on line ~6 ("...H with text-severity-high") to reference the new class so the repo
    grep is clean. Leave critical/medium/low/faint assertions untouched. Then run the unit suite for
    these files.
  </action>
  <acceptance_criteria>
    - `grep -rn 'text-severity-high' frontend/src` returns 0 matches (source + tests + prose all resolved)
    - `grep -rc 'text-\[var(--color-severity-high-on-soft)\]' frontend/src/components/tickets/sla-pill.test.tsx frontend/src/components/tickets/vuln-count.test.tsx frontend/src/components/assets/severity-ribbon.test.tsx frontend/src/components/ui/ChipBar.test.tsx` shows `>=1` in each
    - `cd frontend && npx vitest run src/components/tickets/sla-pill.test.tsx src/components/tickets/vuln-count.test.tsx src/components/assets/severity-ribbon.test.tsx src/components/ui/ChipBar.test.tsx` exits 0 (all pass)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/tickets/sla-pill.test.tsx src/components/tickets/vuln-count.test.tsx src/components/assets/severity-ribbon.test.tsx src/components/ui/ChipBar.test.tsx && test "$(grep -rn 'text-severity-high' src | wc -l | tr -d ' ')" = "0" && echo TESTS_GREEN_AND_GREP_CLEAN</automated>
  </verify>
  <done>The four test files assert/carry the on-soft class, the migrated-component unit tests pass, and `grep -rn 'text-severity-high' frontend/src` is empty.</done>
</task>

<task type="auto">
  <name>Task 3: Static no-regression gate — full unit suite, typecheck, and bundle budget</name>
  <read_first>
    - frontend/package.json — the `build`, `lint`, `test`, `perf:budget` scripts.
    - frontend/scripts/check-bundle-all.mjs — the `perf:budget` route-size check (≤250 KB assertion), if present.
  </read_first>
  <files>.planning/phases/20-light-theme-severity-high-aa/20-02-SUMMARY.md</files>
  <action>
    Prove the migration introduces no regression WITHOUT a live server (the live axe sweep is Plan 20-03):
    1. Full unit suite: `cd frontend && npx vitest run` — all tests pass (no unrelated breakage from the class swaps).
    2. Typecheck: `npx tsc --noEmit` — clean (the arbitrary-value class strings are plain string literals, so this should be a no-op, but confirm).
    3. Production build + bundle budget: `npm run build`, then `npm run perf:budget` (or read the `next build`
       route table). Capture the route table into 20-02-SUMMARY.md. Assert every route's First Load JS is
       <= 250 KB and state that the JS bundle delta vs. the pre-migration build is 0 — this is a CSS-class-string
       change only (no new import, no new dependency), so First-Load JS cannot change.
    Do NOT attempt the live docker/Playwright axe sweep here — that is Plan 20-03's sole job.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run` exits 0 (full suite green).
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && npm run build` exits 0; the pasted route table in 20-02-SUMMARY.md shows every route <= 250 KB First Load JS.
    - `cd frontend && npm run perf:budget` exits 0 (bundle budget check passes) OR the pasted route table demonstrates the ≤250 KB budget explicitly.
    - 20-02-SUMMARY.md states the JS bundle delta is 0 (CSS-class-string change only).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run && npx tsc --noEmit && npm run build && echo STATIC_GATE_GREEN</automated>
  </verify>
  <done>Full unit suite + typecheck pass, `next build` succeeds with all routes ≤250 KB, and the SUMMARY records the 0-KB JS delta. The consumer migration is proven regression-free; live axe evidence follows in Plan 20-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced or affected. This plan swaps Tailwind CSS class strings in components, updates unit-test
assertion strings, and runs unit/typecheck/build. No I/O, no request handling, no user input parsing,
no auth/authz path, no data flow, and no dependency added or upgraded.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Spoofing | (none) | accept | No identity/auth surface touched — CSS class strings + tests only. N/A. |
| T-20-08 | Tampering | (none) | accept | No persisted data or request path; a foreground color class cannot be tampered to escalate. N/A. |
| T-20-09 | Repudiation | (none) | accept | No audit-relevant action changed. N/A. |
| T-20-10 | Information Disclosure | (none) | accept | Rendering only; no data read/written; severity is already displayed to the authed analyst. N/A. |
| T-20-11 | Denial of Service | (none) | accept | CSS class strings only; 0 KB JS delta, no runtime input to exhaust. N/A. |
| T-20-12 | Elevation of Privilege | (none) | accept | No privilege boundary in a color-token consumer swap. N/A. |

No high/medium threats. This change introduces zero new attack surface (ASVS L1: nothing in scope).
</threat_model>

<verification>
- `grep -rn 'text-severity-high' frontend/src` returns 0 (all foreground text/glyph + test + prose migrated).
- `bg-severity-high` / `border-severity-high` / `var(--color-severity-high)` uses remain (non-text, untouched).
- Full `npx vitest run` + `npx tsc --noEmit` green (no unrelated regressions).
- `next build` succeeds; every route ≤ 250 KB; JS bundle delta 0 — recorded in the SUMMARY.
- Live prod-build axe sweep (both themes) is proven in Plan 20-03.
</verification>

<success_criteria>
- All 18 foreground severity-high text/glyph sites consume the on-soft token; non-text uses untouched (UX-D-03-02, UX-D-03-03 at the consumer level).
- Severity-high text/glyphs/pills are legible (>=4.5:1 in light via the token) and distinct via the three-axis encoding on light surfaces (UX-D-03-03).
- CSS-only: 0 KB First-Load-JS delta; no route over 250 KB (proven statically).
- Green light-theme axe sweep (UX-D-03-05) is delivered by Plan 20-03.
</success_criteria>

<output>
After completion, create `.planning/phases/20-light-theme-severity-high-aa/20-02-SUMMARY.md`
(include the `next build` route table and the 0-KB-JS-delta statement).
</output>
