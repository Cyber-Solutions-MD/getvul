---
phase: 22-kanban-wizard-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/connector-wizard-a11y.spec.ts
autonomous: true
requirements: [UX-D-02-06]
tags: [e2e, a11y, axe, wizard, connectors]

must_haves:
  truths:
    - "Running connector-wizard-a11y.spec.ts live, the wizard Test step's loader, success, and error states each report 0 serious/critical axe violations in both dark and light themes"
    - "The wizard Confirm step's permission list, sync display, and submit-error states each report 0 serious/critical axe violations in both dark and light themes"
    - "The Test and Confirm steps are driven deterministically via page.route() mocking of POST /connectors/test and POST /connectors — no real outbound provider call, no DB row created"
  artifacts:
    - path: "frontend/e2e/connector-wizard-a11y.spec.ts"
      provides: "Test-step axe sweep (loader/success/error x dark/light)"
      contains: "Test step"
    - path: "frontend/e2e/connector-wizard-a11y.spec.ts"
      provides: "Confirm-step axe sweep (permissions/sync/submit-error x dark/light)"
      contains: "Confirm step"
    - path: "frontend/e2e/connector-wizard-a11y.spec.ts"
      provides: "deterministic connector-test/create mocking"
      contains: "connectors/test"
  key_links:
    - from: "frontend/e2e/connector-wizard-a11y.spec.ts"
      to: "**/api/v1/connectors/test"
      via: "page.route() fulfilling success (200 {success:true}) and error (200 {success:false})"
      pattern: "connectors/test"
    - from: "frontend/e2e/connector-wizard-a11y.spec.ts"
      to: "**/api/v1/connectors"
      via: "page.route() fulfilling 500 for the Confirm-step submit-error sweep"
      pattern: "route.fulfill"
    - from: "frontend/e2e/connector-wizard-a11y.spec.ts"
      to: "makeAxeBuilder().analyze()"
      via: "filter violations by impact critical/serious, expect length 0"
      pattern: "makeAxeBuilder"
---

<objective>
Close the Phase-19 wizard axe-coverage warning by EXTENDING
`frontend/e2e/connector-wizard-a11y.spec.ts`, which today only opens the dialog and sweeps
the Credentials step. Drive the wizard forward and axe-sweep the two later in-dialog steps —
Test (loader / success / error) and Confirm (permission list / sync display / submit-error) —
in BOTH themes, asserting 0 serious/critical violations.

The Test step's real endpoint (`POST /api/v1/connectors/test`) makes live outbound HTTP calls
to provider APIs (CrowdStrike/Nessus/Defender) with no sandbox type, so a "success" state is
not reachable deterministically without real credentials; and `POST /api/v1/connectors` stores
whatever it is given (no server-side re-validation), so a real create would leave a DB row.
Therefore both are mocked with `page.route()` — the codebase's own precedent
(tickets-kanban.spec.ts:117). `GET /connectors/types` is safe to hit for real (static metadata,
no side effects) and backs the Confirm step's permission list.

Purpose: extend the wizard a11y sweep from 1 of 3 in-dialog steps to all 3, closing UX-D-02-06's
coverage warning with live-verified evidence.
Output: 2 new describe blocks in an existing spec, verified GREEN against a live prod build with
pasted terminal output. NO production code changes anticipated (test is the arbiter if a real
defect surfaces).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-kanban-wizard-test-coverage/22-RESEARCH.md
@.planning/phases/22-kanban-wizard-test-coverage/22-VALIDATION.md

# The spec being extended (existing describe blocks + theme-forcing + axe fixture to reuse):
@frontend/e2e/connector-wizard-a11y.spec.ts
# The production components whose real DOM states each sweep targets:
@frontend/src/components/connectors/wizard/add-connector-wizard.tsx
@frontend/src/components/connectors/wizard/test-step.tsx
@frontend/src/components/connectors/wizard/confirm-step.tsx
@frontend/src/components/connectors/wizard/use-wizard-state.ts

<interfaces>
<!-- Real contracts the executor must drive/assert. Extracted from source — do NOT re-explore. -->

Axe fixture (reuse verbatim — connector-wizard-a11y.spec.ts already imports it):
  `import { test, expect } from './fixtures/axe';`  -> provides `makeAxeBuilder`
  `import { waitForNav, MOBILE_NAV } from './routes';`
  Blocking-violation filter pattern (copy from the existing spec, lines 34-51):
    `const results = await makeAxeBuilder().analyze();`
    `const blocking = results.violations.filter(v => v.impact === 'critical' || v.impact === 'serious');`
    `expect(blocking, '<label>').toHaveLength(0);`

Light-theme forcing (reuse verbatim — existing spec lines 59-84):
  `test.beforeEach` with `page.addInitScript(() => localStorage.setItem('getvul_theme','light'))`
  THEN, after goto, defensively re-assert:
    `if ((await page.locator('html').getAttribute('data-theme')) !== 'light') { await page.evaluate(() => document.documentElement.setAttribute('data-theme','light')); await page.waitForTimeout(50); }`

Opening the wizard (existing spec lines 30-32):
  `await page.goto('/dashboard/connectors'); await waitForNav(page, 1280);`
  `await page.locator('[data-add-connector]').first().click();`
  `await page.getByRole('dialog').waitFor();`
  `await expect(page.getByRole('heading', { name: /Add connector · /i })).toBeVisible();`
  -> Wizard opens on the CREDENTIALS step (provider already picked on the grid). Stepper:
     `nav[aria-label="Wizard progress"]`.

Endpoints (frontend/src/lib/queries/use-connectors-admin.ts):
  POST /api/v1/connectors/test   body { connector_type, credentials, config? } -> { success, message, scopes? }
  POST /api/v1/connectors        body { connector_type, credentials, sync_interval_minutes } -> ConnectorConfigResponse
  GET  /api/v1/connectors/types  -> ConnectorTypeInfo[] (real, safe to hit; backs permissions + fields)

Advancing the gate (use-wizard-state.ts canAdvanceFrom):
  - credentials step: gate opens only when `fields.length > 0 && every field non-empty`
    (line 61). CredentialsStep renders one <input> per field. Fill EVERY rendered input in
    the dialog (provider-agnostic — do NOT hardcode a connector type's field names; a fresh
    tenant may have any subset configured). A robust approach:
      `const inputs = page.getByRole('dialog').locator('input');`
      `const n = await inputs.count(); for (let i=0;i<n;i++) await inputs.nth(i).fill('test-value');`
    (Only text/password credential inputs need filling to open the gate; the sync-interval
    control is a select/segmented control, not a required text field.)
  - Then click Next: `await page.getByRole('button', { name: 'Next', exact: true }).click();`  -> Test step.
  - test step: gate opens only when `testResult.success === true && !credentialsChangedSinceTest`
    (line 63). Must click "Test connection", let the mocked result resolve, THEN Next -> Confirm.

Test step DOM (test-step.tsx):
  - Trigger button: `getByRole('button', { name: /test connection/i })`.
  - While pending: button is `disabled`, shows a `Loader2` spinner + text "Testing…"
    (WIZARD_COPY.testingLabel). Assert pending via `await expect(button).toBeDisabled();`
    before analyze() for the LOADER sweep.
  - Success: `<div role="status" aria-live="polite">` containing `✓ {message}` (green tokens).
  - Error:   `<div role="alert">` containing `✗ {message}` (severity-critical-on-soft tokens).

Confirm step DOM (confirm-step.tsx):
  - Heading `Confirm`; sections Provider / Connection (`✓ Connection verified`) /
    "Required access" (permissions <ul><li> with scope + access badge + purpose, OR
    "No special scopes required." when empty) / "Sync interval".
  - Submit-error: after clicking the gradient CTA `getByRole('button', { name: /add connector/i })`,
    a `<div role="alert">` renders the mocked 500's message.

page.route() mocking (mirror tickets-kanban.spec.ts:117 precedent):
  Loader: delay before fulfilling so the pending UI is sweepable:
    `await page.route('**/api/v1/connectors/test', async (route) => {`
    `  await new Promise(r => setTimeout(r, 400));`
    `  await route.fulfill({ status: 200, contentType: 'application/json',`
    `    body: JSON.stringify({ success: true, message: 'Successfully authenticated' }) });`
    `});`
  Success: same, no delay (or short), body { success: true, message: 'Successfully authenticated' }.
  Error:   body { success: false, message: 'Authentication failed: HTTP 401' } (still status 200 —
           the frontend reads the `success` boolean, not the HTTP code; see test-step.tsx handleTest).
  Confirm submit-error: `await page.route('**/api/v1/connectors', route => route.fulfill({ status: 500, body: '{}' }));`
  IMPORTANT: register a route ONCE per test and use `route.fulfill` from the current handler;
  to switch success->error within one test, call `await page.unroute('**/api/v1/connectors/test')`
  before re-registering, OR prefer one test per state (cleaner, matches the existing spec's
  one-assertion-per-test shape). Do NOT mock `GET /connectors/types` — the Confirm permission
  list must sweep the REAL metadata.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Test step" axe sweep (loader / success / error x dark/light)</name>
  <read_first>
    - frontend/e2e/connector-wizard-a11y.spec.ts (existing dark + light describe blocks, lines 22-108 — mirror their structure, theme-forcing beforeEach, and blocking-filter pattern; reuse the axe fixture import)
    - frontend/src/components/connectors/wizard/test-step.tsx (the exact loader/success/error DOM the sweep targets)
    - frontend/src/components/connectors/wizard/use-wizard-state.ts (canAdvanceFrom lines 54-69 — the credentials gate the test must satisfy to reach the Test step)
    - frontend/src/components/connectors/wizard/add-connector-wizard.tsx (step rendering + Next button, to know the click sequence)
  </read_first>
  <files>frontend/e2e/connector-wizard-a11y.spec.ts</files>
  <action>
    Add a describe block titled EXACTLY containing `'Test step'` (VALIDATION uses `-g "Test step"`),
    e.g. `test.describe('Add-connector wizard — Test step axe sweep (both themes, blocking)', ...)`.

    Create a small local helper (inside this file) that: opens the wizard (goto
    /dashboard/connectors, waitForNav 1280, click `[data-add-connector].first()`, wait for
    dialog + the `Add connector · ` heading), fills every credential input in the dialog
    (`getByRole('dialog').locator('input')` loop, fill 'test-value'), and clicks
    `getByRole('button', { name: 'Next', exact: true })` to reach the Test step. Accept a
    `theme: 'dark' | 'light'` parameter and apply the existing spec's light-forcing
    (addInitScript localStorage + defensive re-assert) when theme==='light'.

    Then, for EACH theme (dark and light) add tests that mock `**/api/v1/connectors/test`
    via `page.route()` and sweep each state — prefer one test per state per theme (clean,
    matches the existing spec):

    - LOADER: mock with a ~400ms delay before fulfilling (success body). Click "Test
      connection". Assert the button is disabled (`await expect(getByRole('button',
      {name:/test connection/i})).toBeDisabled();`) so the pending UI is present, THEN run the
      blocking-filter axe sweep, THEN await the result to settle. Title contains "loader".
    - SUCCESS: mock 200 `{ success: true, message: 'Successfully authenticated' }`. Click
      "Test connection", wait for the `role="status"` block containing `✓` to be visible,
      then axe-sweep. Title contains "success".
    - ERROR: mock 200 `{ success: false, message: 'Authentication failed: HTTP 401' }`. Click
      "Test connection", wait for the `role="alert"` block containing `✗` to be visible, then
      axe-sweep. Title contains "error".

    Use the exact blocking-violation filter + `console.error` diagnostic + `expect(blocking,
    '<label>').toHaveLength(0)` pattern from the existing spec (lines 34-52). Emit a distinct
    label per state+theme so a failure names which state broke.

    Do NOT mock `GET /connectors/types`. Do NOT modify production source.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test e2e/connector-wizard-a11y.spec.ts -g "Test step" --config=e2e/playwright.config.ts --project=chromium-a11y</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Test step" frontend/e2e/connector-wizard-a11y.spec.ts` returns >= 1
    - `grep -c "connectors/test" frontend/e2e/connector-wizard-a11y.spec.ts` returns >= 1 (page.route glob)
    - `grep -c "makeAxeBuilder" frontend/e2e/connector-wizard-a11y.spec.ts` shows an INCREASE over the original (original had 3; must be >= 6 to cover loader/success/error)
    - `grep -c "toHaveLength(0)" frontend/e2e/connector-wizard-a11y.spec.ts` increased accordingly
    - The block sweeps BOTH themes (grep shows `getvul_theme` light-forcing referenced within/for the new block, and dark tests present)
    - No mock of `**/api/v1/connectors/types` exists (Confirm plan uses the real endpoint; this task must not stub it): `grep -c "connectors/types" frontend/e2e/connector-wizard-a11y.spec.ts` returns 0
    - `git diff --stat` shows ONLY frontend/e2e/connector-wizard-a11y.spec.ts changed
  </acceptance_criteria>
  <done>The `-g "Test step"` run passes live: loader, success, and error states each report 0 serious/critical axe violations in both dark and light themes, driven via page.route() mocks (no real outbound call).</done>
</task>

<task type="auto">
  <name>Task 2: Add "Confirm step" axe sweep (permissions / sync / submit-error x dark/light)</name>
  <read_first>
    - frontend/e2e/connector-wizard-a11y.spec.ts (the Task-1 helper to reuse for driving credentials->test; mirror its theme handling)
    - frontend/src/components/connectors/wizard/confirm-step.tsx (permission <ul><li>, sync display, role="alert" submit-error, gradient CTA — the exact DOM to sweep)
    - frontend/src/lib/queries/use-connectors-admin.ts (endpoints: GET /connectors/types is real; POST /connectors is mocked for submit-error)
  </read_first>
  <files>frontend/e2e/connector-wizard-a11y.spec.ts</files>
  <action>
    Add a describe block titled EXACTLY containing `'Confirm step'` (VALIDATION uses
    `-g "Confirm step"`).

    Reuse/extend the Task-1 helper to drive all the way to Confirm: open wizard -> fill
    credentials -> Next -> Test step -> mock `**/api/v1/connectors/test` 200 `{success:true,...}`
    -> click "Test connection" -> wait for the success `role="status"` block -> click
    `getByRole('button', { name: 'Next', exact: true })` -> Confirm step (assert the `Confirm`
    heading is visible). Do NOT mock `GET /connectors/types` — the "Required access" permission
    list must render REAL metadata.

    For EACH theme (dark + light):
    - REVIEW STATE: on the Confirm step (permission list / connection-verified / sync interval
      all rendered, before submit), run the blocking-filter axe sweep. Title contains
      "permission" or "review". This is the primary Confirm-step coverage.
    - SUBMIT-ERROR STATE: register `page.route('**/api/v1/connectors', route =>
      route.fulfill({ status: 500, body: '{}' }))`, click the CTA
      `getByRole('button', { name: /add connector/i })`, wait for the `role="alert"` submit-error
      block to be visible, then axe-sweep. Title contains "submit-error". (The 500 mock ensures
      no real DB row is created — see RESEARCH: create_connector does not re-validate.)

    Use the same blocking-filter + labeled `expect(...).toHaveLength(0)` pattern. Do NOT modify
    production source.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test e2e/connector-wizard-a11y.spec.ts -g "Confirm step" --config=e2e/playwright.config.ts --project=chromium-a11y</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Confirm step" frontend/e2e/connector-wizard-a11y.spec.ts` returns >= 1
    - `grep -c "add connector" frontend/e2e/connector-wizard-a11y.spec.ts` (case-insensitive) shows the CTA click: `grep -ci "name: /add connector/i" frontend/e2e/connector-wizard-a11y.spec.ts` returns >= 1
    - `grep -c "role.*alert\|'alert'\|\"alert\"" frontend/e2e/connector-wizard-a11y.spec.ts` OR a `getByRole('alert')` reference exists for the submit-error assertion
    - Still 0 occurrences of `connectors/types` being mocked: `grep -c "route.*connectors/types\|connectors/types.*fulfill" frontend/e2e/connector-wizard-a11y.spec.ts` returns 0
    - The submit-error test mocks `**/api/v1/connectors` with a 500: `grep -c "status: 500" frontend/e2e/connector-wizard-a11y.spec.ts` returns >= 1
    - Both themes covered in the new block
    - `git diff --stat` shows ONLY frontend/e2e/connector-wizard-a11y.spec.ts changed
  </acceptance_criteria>
  <done>The `-g "Confirm step"` run passes live: the review state (real permissions/sync) and the submit-error state each report 0 serious/critical axe violations in both themes, with POST /connectors mocked to 500 (no DB row created).</done>
</task>

<task type="auto">
  <name>Task 3: Live prod-build gate run of the full wizard a11y spec + pasted evidence</name>
  <read_first>
    - .planning/phases/22-kanban-wizard-test-coverage/22-VALIDATION.md (Sampling Rate: per-wave-merge runs the FULL spec file — guards the pre-existing Credentials/mobile-vaul tests too)
    - .planning/phases/21-page-transition-verification/21-01-SUMMARY.md (validated local e2e recipe — prod build + :3000 + admin login; do NOT skip)
    - memory getvul-local-e2e-perf-gate (kill next start AND next-server children before re-serving)
  </read_first>
  <files>frontend/e2e/connector-wizard-a11y.spec.ts</files>
  <action>
    Run the FULL `connector-wizard-a11y.spec.ts` (all describe blocks — the pre-existing
    open-wizard dark/light + mobile-vaul sweeps AND the two new blocks) against a real
    PRODUCTION build, following project memory `getvul-local-e2e-perf-gate`:
    1. Docker backend up on :8000; admin@getvul.local / Admin123! present.
    2. `cd frontend && npm run build && npm run start` on :3000 (kill stale `next start` +
       `next-server` children first).
    3. `cd frontend && npx playwright test e2e/connector-wizard-a11y.spec.ts --config=e2e/playwright.config.ts --project=chromium-a11y`
    4. Paste the UNEDITED transcript (per-test pass/fail) into the SUMMARY. Authoring is NOT
       acceptance — only a live green run is (memory `getvul-axe-sweep-not-run-during-exec`).
    5. If a real axe violation surfaces on the Test/Confirm DOM, that is a genuine production
       defect: fix the minimal token/markup issue (this is the one path where production source
       may change — the test is the arbiter), re-run, and record the fix explicitly in the
       SUMMARY. If the failure is a test-driving bug (wrong selector/timing), fix the test.
    6. Regression guard: `npm run perf:budget` — confirm `/dashboard/connectors` stays
       <= 250 KB (baseline 156 kB per 19-VERIFICATION.md). Paste the line.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test e2e/connector-wizard-a11y.spec.ts --config=e2e/playwright.config.ts --project=chromium-a11y</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains a pasted, unedited Playwright transcript for the full connector-wizard-a11y.spec.ts showing per-test results (not paraphrased)
    - The transcript shows 0 failed
    - The transcript shows the Test-step and Confirm-step tests RAN in both themes (not skipped)
    - The SUMMARY contains a pasted `npm run perf:budget` line for /dashboard/connectors showing <= 250 KB
    - If any production source was touched, the SUMMARY documents the exact axe violation that justified it
  </acceptance_criteria>
  <done>The full connector-wizard-a11y.spec.ts runs against a live prod build with 0 failures, evidenced by pasted terminal output; the connectors route is confirmed <= 250 KB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test-runner -> app under test | Test drives a real browser (admin storageState) against a local prod build. No new production request path. |
| Playwright `page.route` -> `**/api/v1/connectors/test` and `**/api/v1/connectors` | Mocked test-runner traffic only. Mocking `POST /connectors/test` deliberately AVOIDS real outbound calls to provider APIs (CrowdStrike/Nessus/Defender); mocking `POST /connectors` to 500 AVOIDS creating a real ConnectorConfig row (backend does not re-validate — RESEARCH §Security Domain). |
| Real `GET /api/v1/connectors/types` | Hit for real (static metadata, no side effects) to sweep the Confirm permission list against true data. Read-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | Tampering | e2e spec (test-only) | accept | No production code path added; the mock intercepts only test-runner traffic. Zero new attack surface (ASVS L1 "nothing in scope"). |
| T-22-02-02 | Information Disclosure | credential inputs filled with 'test-value' | accept | Dummy non-secret values, never sent to a real backend (POST /connectors/test is mocked). No real secret is transmitted or stored. |
| T-22-02-03 | Tampering (DB pollution) | POST /connectors | mitigate | Confirm-step submit is mocked to 500 so `create_connector` is never reached — no unvalidated credential row is persisted, per the deliberate mocking choice in 22-RESEARCH. |

ASVS L1 review: test-authoring-only plan (per 22-RESEARCH §Security Domain). No new auth,
input-validation, or crypto surface. The pre-existing (out-of-scope) create-without-revalidation
gap is mocked around, not introduced or worsened.
</threat_model>

<verification>
- `npx playwright test e2e/connector-wizard-a11y.spec.ts --config=e2e/playwright.config.ts --project=chromium-a11y` runs green (0 failed) against a live prod build.
- Test step (loader/success/error) sweeps report 0 serious/critical axe violations, both themes.
- Confirm step (review permissions/sync + submit-error) sweeps report 0 serious/critical, both themes.
- `GET /connectors/types` is NOT mocked (real permissions swept); `POST /connectors/test` and
  `POST /connectors` ARE mocked (no outbound call, no DB row).
- `npm run perf:budget`: /dashboard/connectors <= 250 KB.
- No production source modified (unless a real axe defect was surfaced and explicitly recorded).
</verification>

<success_criteria>
Plan complete when:
- connector-wizard-a11y.spec.ts contains a "Test step" block (loader/success/error x dark/light) and a "Confirm step" block (permissions/sync + submit-error x dark/light).
- Both new blocks drive the wizard via page.route() mocks and axe-sweep with the existing blocking-filter, asserting 0 serious/critical.
- The full spec runs green live against a prod build with pasted, unedited terminal evidence.
- The connectors route stays <= 250 KB.
- No production regression.
</success_criteria>

<output>
After completion, create `.planning/phases/22-kanban-wizard-test-coverage/22-02-SUMMARY.md`
including the pasted live Playwright transcript and the perf:budget line.
</output>
