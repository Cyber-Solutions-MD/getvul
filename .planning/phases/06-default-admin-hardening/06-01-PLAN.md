---
phase: 06-default-admin-hardening
plan: 01
type: execute
wave: 1
depends_on: [00]
files_modified:
  - backend/alembic/versions/029_add_must_change_password.py
  - backend/app/tenants/models.py
  - backend/create_admin.py
autonomous: true
requirements: [PROD-06-01]
must_haves:
  truths:
    - "The users table has a must_change_password boolean column, NOT NULL, server_default false, after alembic upgrade head"
    - "A user seeded by create_admin.py has must_change_password = true"
    - "The User ORM model exposes .must_change_password so backend code can read it without AttributeError"
  artifacts:
    - path: "backend/alembic/versions/029_add_must_change_password.py"
      provides: "Additive migration adding users.must_change_password"
      contains: "must_change_password"
    - path: "backend/app/tenants/models.py"
      provides: "User.must_change_password mapped column"
      contains: "must_change_password"
    - path: "backend/create_admin.py"
      provides: "Seed sets must_change_password=true on the OWNER admin"
      contains: "must_change_password"
  key_links:
    - from: "backend/app/tenants/models.py"
      to: "users.must_change_password column"
      via: "SQLAlchemy mapped_column(Boolean, ...)"
      pattern: "must_change_password"
    - from: "backend/create_admin.py"
      to: "users.must_change_password"
      via: "INSERT column + value true"
      pattern: "must_change_password"
---

<objective>
Deliver the persistence layer for forced rotation (PROD-06-01): a new Alembic migration 029 adding users.must_change_password (boolean, NOT NULL, server_default false), the matching User ORM column, and the create_admin.py seed setting the flag true on the OWNER admin. Apply the migration so downstream waves can read the column.

Purpose: Waves 2 and 3 read user.must_change_password. Without the column plus applied migration they raise AttributeError or DB errors. This plan is the foundation and MUST complete (including alembic upgrade head) before Wave 2 runs.
Output: migration 029, model column, seed change, migration applied to the running DB.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-default-admin-hardening/06-RESEARCH.md
@.planning/phases/06-default-admin-hardening/06-CONTEXT.md

<interfaces>
Confirmed against live code. Executor uses these directly — no exploration needed.

Alembic head (current): 028_add_ticket_watchers
Next revision fields (hand-written, no autogenerate):
  revision = "029_add_must_change_password"
  down_revision = "028_add_ticket_watchers"
  branch_labels = None
  depends_on = None

Column-add pattern (from 011_add_password_auth.py):
  op.add_column("users", sa.Column("must_change_password", sa.Boolean(), server_default="false", nullable=False))

create_admin.py INSERT (lines 42-44) — hand-written SQL, explicit column list:
  "INSERT INTO users (id, tenant_id, email, display_name, role, password_hash, is_active, idp_subject, idp_source) "
  "VALUES (gen_random_uuid(), :tid, 'admin@getvul.local', 'Admin', 'OWNER', :pw, true, 'local-admin', 'local')"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 029 + User model column</name>
  <read_first>
    - backend/alembic/versions/028_add_ticket_watchers.py (current head — confirm revision id string exactly, and file header conventions)
    - backend/alembic/versions/011_add_password_auth.py (the Boolean column-add pattern to mirror)
    - backend/app/tenants/models.py (the User model — find password_history near line 65; place the new column near it and match the existing Boolean-column style used for is_active)
  </read_first>
  <files>backend/alembic/versions/029_add_must_change_password.py, backend/app/tenants/models.py</files>
  <action>
Create backend/alembic/versions/029_add_must_change_password.py with these exact revision fields and body:

  revision = "029_add_must_change_password"
  down_revision = "028_add_ticket_watchers"
  branch_labels = None
  depends_on = None

  def upgrade() -> None:
      op.add_column(
          "users",
          sa.Column("must_change_password", sa.Boolean(), server_default="false", nullable=False),
      )

  def downgrade() -> None:
      op.drop_column("users", "must_change_password")

Include the standard imports (import sqlalchemy as sa; from alembic import op) matching the file header style of 028.

In backend/app/tenants/models.py, add to the User model (near password_history, matching the existing is_active Boolean pattern):

  must_change_password: Mapped[bool] = mapped_column(
      Boolean, default=False, server_default="false"
  )

Confirm Boolean and mapped_column/Mapped are already imported at the top of models.py (they are, per is_active); do NOT re-import. The column must be non-null in DB — the migration enforces nullable=False; the ORM default=False covers ORM-created rows.
  </action>
  <verify>
    <automated>cd backend && python -c "import ast; ast.parse(open('alembic/versions/029_add_must_change_password.py').read()); ast.parse(open('app/tenants/models.py').read()); print('parse ok')" && grep -q 'revision = "029_add_must_change_password"' alembic/versions/029_add_must_change_password.py && grep -q 'down_revision = "028_add_ticket_watchers"' alembic/versions/029_add_must_change_password.py && grep -q 'nullable=False' alembic/versions/029_add_must_change_password.py && grep -q 'server_default="false"' alembic/versions/029_add_must_change_password.py && grep -q 'must_change_password' app/tenants/models.py
  </verify>
  <acceptance_criteria>
    - grep -q 'revision = "029_add_must_change_password"' backend/alembic/versions/029_add_must_change_password.py succeeds
    - grep -q 'down_revision = "028_add_ticket_watchers"' backend/alembic/versions/029_add_must_change_password.py succeeds
    - grep -q 'server_default="false"' AND grep -q 'nullable=False' in 029_add_must_change_password.py both succeed
    - grep -q 'def downgrade' AND grep -q 'drop_column("users", "must_change_password")' in 029 both succeed
    - grep -c 'must_change_password' backend/app/tenants/models.py returns >= 1
  </acceptance_criteria>
  <done>Migration 029 exists with correct revision chain and a real downgrade; User model has the mapped column; both files parse.</done>
</task>

<task type="auto">
  <name>Task 2: Seed flag in create_admin.py + apply migration + green column/seed tests</name>
  <read_first>
    - backend/create_admin.py (the INSERT SQL string, lines ~42-44 — the explicit column + value lists and bind params :tid/:pw)
    - backend/tests/test_admin_hardening.py (the test_migration_column + test_seed_flag cases this task must turn green)
    - .planning/phases/06-default-admin-hardening/06-VALIDATION.md (env-var + per-file run requirement)
    - Makefile or docs/ (find the canonical migration-apply command if alembic is not run directly on the host)
  </read_first>
  <files>backend/create_admin.py</files>
  <action>
Edit the create_admin.py INSERT so the seeded OWNER admin sets the flag true (D-02). Append must_change_password to the column list and the literal true to the values list, matching the existing is_active/true SQL-literal style:

  "INSERT INTO users (id, tenant_id, email, display_name, role, password_hash, is_active, idp_subject, idp_source, must_change_password) "
  "VALUES (gen_random_uuid(), :tid, 'admin@getvul.local', 'Admin', 'OWNER', :pw, true, 'local-admin', 'local', true)"

Preserve the exact existing bind-parameter names (:tid, :pw) and the rest of the row verbatim — only append the one column and the literal true.

Then APPLY the migration so downstream waves can read the column:

  cd backend && alembic upgrade head

Confirm the head advanced (alembic current shows 029_add_must_change_password). If the DB is only reachable through Docker, use the project's canonical path instead (e.g. docker compose exec backend alembic upgrade head) — check Makefile/docs for the established command.

Then run the two column/seed tests to green (env vars per MEMORY.md; per-file).
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$ENCRYPTION_KEY JWT_SECRET_KEY=$JWT_SECRET_KEY python -m pytest tests/test_admin_hardening.py::test_migration_column tests/test_admin_hardening.py::test_seed_flag -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'must_change_password' backend/create_admin.py succeeds AND the values list contains a literal true aligned to that column
    - alembic current reports 029_add_must_change_password as applied (head)
    - pytest tests/test_admin_hardening.py::test_migration_column -x exits 0
    - pytest tests/test_admin_hardening.py::test_seed_flag -x exits 0
  </acceptance_criteria>
  <done>Seed sets the flag true; migration applied to head 029; both PROD-06-01 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration -> DB schema | The migration mutates the shared users table; a wrong default or nullability weakens the whole enforcement gate. |
| seed script -> DB row | create_admin.py writes the privileged OWNER row; the flag value here is the entire point of PROD-06-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Elevation of Privilege | Seed writes flag=false, admin never forced to rotate default Admin123! | mitigate | Task 2 sets literal true in the INSERT; test_seed_flag asserts must_change_password is True on the seeded row. |
| T-06-01-02 | Tampering | Nullable column lets a row bypass the gate with NULL (falsy) flag | mitigate | Migration uses nullable=False + server_default false so every existing and future row has a concrete boolean; test_migration_column asserts NOT NULL. |
| T-06-01-03 | Denial of Service | Migration not applied before Wave 2 -> AttributeError storms on every authenticated request | mitigate | Task 2 runs alembic upgrade head and asserts alembic current == 029 before Wave 2 depends_on is satisfied; wave ordering enforces sequence. |
</threat_model>

<verification>
- alembic upgrade head applies cleanly; alembic downgrade -1 (spot-check, then re-upgrade) drops/re-adds the column without error.
- cd backend && ENCRYPTION_KEY=$ENCRYPTION_KEY JWT_SECRET_KEY=$JWT_SECRET_KEY python -m pytest tests/test_admin_hardening.py::test_migration_column tests/test_admin_hardening.py::test_seed_flag -x → both pass.
</verification>

<success_criteria>
- users.must_change_password exists (boolean, NOT NULL, default false), the ORM exposes it, create_admin.py seeds it true, migration is applied to head 029, and both PROD-06-01 tests are green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-default-admin-hardening/06-01-SUMMARY.md`
</output>
