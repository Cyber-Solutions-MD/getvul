---
phase: 06-default-admin-hardening
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_admin_hardening.py
  - frontend/src/app/change-password/change-password.test.tsx
autonomous: true
requirements: [PROD-06-01, PROD-06-02, PROD-06-03, PROD-06-04]
must_haves:
  truths:
    - "Both test files exist and are collectible/runnable by their test runners"
    - "All 12 backend pytest node IDs from 06-VALIDATION.md are declared and initially fail (RED)"
    - "The frontend change-password Vitest file is collectible and its cases initially fail (RED)"
  artifacts:
    - path: "backend/tests/test_admin_hardening.py"
      provides: "12 pytest test cases (stubs) that Waves 1-2 make green"
      contains: "def test_migration_column"
    - path: "frontend/src/app/change-password/change-password.test.tsx"
      provides: "Vitest redirect-gate + form + state test cases (stubs) Wave 3 makes green"
      contains: "describe"
  key_links:
    - from: "backend/tests/test_admin_hardening.py"
      to: "conftest.py fixtures"
      via: "db_session, tenant_a, client_factory"
      pattern: "def test_.*(db_session|client_factory|tenant_a)"
---

<objective>
Create the two test files that every downstream task verifies against, so Waves 1-3 have concrete automated targets (Nyquist compliance). These are RED stubs: each test asserts the not-yet-built behavior and MUST fail until the implementing task lands. No production code changes in this plan.

Purpose: Nyquist requires every task to have an automated verify or a Wave 0 dependency. This plan is that dependency.
Output: `backend/tests/test_admin_hardening.py` (12 cases) + `frontend/src/app/change-password/change-password.test.tsx`.
</objective>

<execution_context>
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chemencedji/Desktop/getvul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-default-admin-hardening/06-VALIDATION.md
@.planning/phases/06-default-admin-hardening/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend test scaffold — test_admin_hardening.py (12 RED cases)</name>
  <read_first>
    - backend/tests/conftest.py (fixtures: db_session, tenant_a, client_factory — confirm exact signatures/names before use)
    - backend/tests/test_auth.py (established pattern for login + JWT tests, fixture usage, how a client is built and how a login token is obtained)
    - backend/app/auth/jwt.py (create_access_token / decode_token / TokenPayload — for the round-trip test target shape)
    - .planning/phases/06-default-admin-hardening/06-VALIDATION.md (the exact 12 node IDs — copy names verbatim)
  </read_first>
  <files>backend/tests/test_admin_hardening.py</files>
  <action>
Create `backend/tests/test_admin_hardening.py` with EXACTLY these 12 async test functions (node IDs are contractually fixed by 06-VALIDATION.md — names must match character-for-character):

- `test_migration_column` — asserts a query against `users` selecting `must_change_password` succeeds and the column is boolean NOT NULL with server_default false (query information_schema, or insert a user without the flag and assert it reads `False`).
- `test_seed_flag` — asserts a user created via the `create_admin.py` seed path has `must_change_password is True`. (In the RED stub, import may not exist yet — mark with the assertion that will pass once Wave 1 lands.)
- `test_jwt_claim_round_trip` — build an access token with `create_access_token(..., must_change_password=True)`; `decode_token` returns a payload whose `must_change_password is True`; default (omitted kwarg) → `False`.
- `test_current_user_claim` — a flagged access token routed through `get_current_user` yields `CurrentUser.must_change_password is True`.
- `test_enforcement_blocks` — flagged user hits a NON-allowlist authenticated route (e.g. `GET /vulnerabilities` or any `/api`/protected route that depends on `get_current_user`); asserts HTTP 403 and JSON body `{"reason": "password_change_required"}` (assert `resp.json()["detail"]["reason"] == "password_change_required"` — match the shape the endpoint raises; adjust to `resp.json()["reason"]` only if the handler returns a bare body).
- `test_enforcement_allowlist_me` — flagged user hits `GET /auth/me` → HTTP 200.
- `test_enforcement_allowlist_change` — flagged user hits `POST /auth/change-password` → NOT 403 (allowlist pass; may 400 on bad body, but never 403 for the flag).
- `test_unflagged_user_unblocked` — unflagged user hits the same non-allowlist route as `test_enforcement_blocks` → NOT 403 (no flag interference).
- `test_rotation_clears_flag` — flagged user POSTs a valid rotation to `/auth/change-password`; afterward the DB row has `must_change_password is False`.
- `test_rotation_audit_event` — after a successful flagged rotation, an `audit_logs` row exists with `action == "auth.first_login_rotation"` for that user.
- `test_rotation_fresh_tokens` — the `/auth/change-password` response (when the flag was set) contains fresh `access_token`; decoding it yields `must_change_password is False`.
- `test_refresh_reads_current_flag` — after rotation (flag now False in DB), calling `/auth/refresh` yields an access token whose decoded `must_change_password is False`.

Each stub body: set up whatever fixtures apply (`db_session`, `tenant_a`, `client_factory`) then a single assertion that captures the target behavior. Where the production symbol does not exist yet (e.g. `must_change_password` kwarg), write the call/assert as it WILL be after Wave 1/2 so the test fails RED now (ImportError/AttributeError/AssertionError all count as RED). Do NOT use xfail/skip — these must genuinely fail until implemented.

Use fixtures exactly as they appear in conftest.py. Reuse the login/token helper pattern from test_auth.py rather than hand-rolling token creation for the HTTP-level tests.
  </action>
  <verify>
    <automated>cd backend && ENCRYPTION_KEY=$ENCRYPTION_KEY JWT_SECRET_KEY=$JWT_SECRET_KEY python -m pytest tests/test_admin_hardening.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_admin_hardening.py --collect-only -q` lists exactly these 12 node IDs: test_migration_column, test_seed_flag, test_jwt_claim_round_trip, test_current_user_claim, test_enforcement_blocks, test_enforcement_allowlist_me, test_enforcement_allowlist_change, test_unflagged_user_unblocked, test_rotation_clears_flag, test_rotation_audit_event, test_rotation_fresh_tokens, test_refresh_reads_current_flag
    - `grep -c "^async def test_\|^def test_" backend/tests/test_admin_hardening.py` returns 12
    - Running the file (not collect-only) shows all 12 FAILING or ERRORING (RED) — no passes, no skips: `cd backend && ENCRYPTION_KEY=$ENCRYPTION_KEY JWT_SECRET_KEY=$JWT_SECRET_KEY python -m pytest tests/test_admin_hardening.py -q` reports 0 passed
    - `grep -c "xfail\|@pytest.mark.skip" backend/tests/test_admin_hardening.py` returns 0
  </acceptance_criteria>
  <done>File exists, collects 12 named cases, all RED, no skip/xfail markers, fixtures wired from conftest.py.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend test scaffold — change-password.test.tsx (RED)</name>
  <read_first>
    - frontend/src/lib/auth.tsx (AuthProvider, useAuth, User interface, redirect-gate useEffect — the gate under test)
    - frontend/src/app/login/page.tsx (form + ErrorAlert + success-state patterns to mirror in test expectations)
    - frontend/vitest.config.mts (jsdom + testing-library setup)
    - an existing Vitest file that mocks next/navigation router.replace (e.g. any *.test.tsx under frontend/src that tests a redirect) to copy the router mock pattern
  </read_first>
  <files>frontend/src/app/change-password/change-password.test.tsx</files>
  <action>
Create `frontend/src/app/change-password/change-password.test.tsx` as a Vitest + @testing-library/react suite with a top-level `describe('change-password', ...)`. Mock `next/navigation` (`useRouter` → `{ replace: vi.fn() }`, `useSearchParams`, `usePathname`) and `global.fetch`.

Cases (RED — the page + gate do not exist yet):
1. `redirect gate fires when must_change_password is true` — render `AuthProvider` (or the exported gate logic) with a `user` whose `must_change_password === true` on a protected pathname; assert `router.replace` called with `'/change-password'`.
2. `renders the rotation form` — render the `/change-password` page; assert current-password, new-password, and confirm-password fields are present (getByLabelText).
3. `shows an error on wrong current password` — mock fetch to resolve 400 with `{ detail: "Current password is incorrect" }`; submit; assert an `role="alert"` (ErrorAlert) renders with the message.
4. `redirects to /dashboard on success` — mock fetch to resolve 200 with fresh `{ access_token, refresh_token }`; submit valid matching passwords; assert `router.replace` called with `'/dashboard'` (or the sanitized `?next`).

Import the page as `import Page from './page'` (the module Wave 3 creates). This import failing is acceptable RED. Do NOT skip/`.only`.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- change-password 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at frontend/src/app/change-password/change-password.test.tsx
    - `grep -c "it(\|test(" frontend/src/app/change-password/change-password.test.tsx` returns >= 4
    - `cd frontend && npm run test -- change-password` collects the suite and reports failures (RED) — 0 passing assertions for the not-yet-built page
    - `grep -c "\.skip\|\.only\|it.todo" frontend/src/app/change-password/change-password.test.tsx` returns 0
  </acceptance_criteria>
  <done>Vitest file exists with >=4 cases, all RED, no skip/only, router + fetch mocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Test scaffolding only — no runtime code, no input crosses a boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-00-01 | Tampering | Test stubs assert wrong shape, masking a real defect later | mitigate | Node IDs + response-body shape (`password_change_required`, `auth.first_login_rotation`) copied verbatim from 06-VALIDATION.md and ROADMAP SC; tests must be RED before implementation (proves they exercise real behavior, not tautologies). |
</threat_model>

<verification>
- Backend: `cd backend && ENCRYPTION_KEY=$ENCRYPTION_KEY JWT_SECRET_KEY=$JWT_SECRET_KEY python -m pytest tests/test_admin_hardening.py -q` → 12 collected, 0 passed (all RED).
- Frontend: `cd frontend && npm run test -- change-password` → suite collected, all RED.
</verification>

<success_criteria>
- Both files exist, are runner-collectible, contain the exact/enough named cases, and are entirely RED with no skip/xfail/only markers.
</success_criteria>

<output>
After completion, create `.planning/phases/06-default-admin-hardening/06-00-SUMMARY.md`
</output>
