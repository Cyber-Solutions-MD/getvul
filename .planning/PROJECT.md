# GetVul

## What This Is

GetVul is a unified vulnerability management platform that aggregates findings from ~6 enterprise vulnerability scanners (CrowdStrike Spotlight, Nessus, Defender for Endpoint, Wiz, Qualys VMDR, Rapid7 InsightVM), normalizes them into a multi-tenant Postgres database, enriches assets with identity/MDM/HR data (Google/Azure/Okta/Humaans/Jamf/Intune), and lets a security team triage and create Jira/Asana tickets through a Next.js dashboard. It's a single-repo full-stack web app (FastAPI + Next.js 15 + Nginx + Postgres + Redis) packaged as Docker Compose, deployable to a single Linux VM via [install.sh](install.sh).

## Core Value

A vuln-triage analyst can open one dashboard, see the same CVE-on-host correlated across multiple scanners, identify the asset's owner from IdP/MDM/HR, and ship a Jira/Asana ticket — without ever opening a scanner console.

## Requirements

### Validated

<!-- Shipped and confirmed valuable. Inferred from existing code at audit time (2026-05-08). -->

- ✓ **VULN-01**: Aggregate vulnerabilities from CrowdStrike, Nessus, Defender, Wiz — v0.1
- ✓ **VULN-02**: Cross-source CVE correlation per asset — v0.1
- ✓ **VULN-03**: CVE ignore + remediation suppress with audit trail — v0.1
- ✓ **VULN-04**: Per-host and per-remediation drill-down views — v0.1
- ✓ **ASSET-01**: Auto-classify devices (workstation/server/network/mobile/other) — v0.1
- ✓ **ASSET-02**: Risk score per asset (piecewise log curve, severity weights, exploit/KEV multipliers) — v0.1
- ✓ **ASSET-03**: MDM enrichment (Jamf, Intune) and HR enrichment (Humaans) — v0.1
- ✓ **AUTH-01**: Email/password login with bcrypt + configurable password policy — v0.1
- ✓ **AUTH-02**: Google + Azure OIDC SSO with per-tenant enforcement — v0.1
- ✓ **AUTH-03**: JWT access (15m) + refresh (7d) with auto-refresh — v0.1
- ✓ **AUTH-04**: Email-based password reset with single-use token — v0.1
- ✓ **TENANT-01**: All queries scoped by `tenant_id` from JWT — v0.1
- ✓ **RBAC-01**: Owner > Admin > Analyst > Viewer with per-route enforcement — v0.1
- ✓ **TKT-01**: Jira + Asana ticket create/update/close/comment/delete — v0.1
- ✓ **TKT-02**: Saved-filter automation rules with daily ticket-status sync — v0.1
- ✓ **SLA-01**: Per-severity SLA tracking with breach + at-risk detection — v0.1
- ✓ **CSPM-01**: CSPM findings with compliance frameworks, cloud resources, trends — v0.1
- ✓ **NOTIF-01**: In-app notifications with 4 alert categories + SMTP email — v0.1
- ✓ **AUDIT-01**: Full audit log with CEF-format syslog forwarding — v0.1
- ✓ **EXP-01**: CSV exports + PDF/CSV/TXT executive summary with branding — v0.1
- ✓ **EXP-02**: Scheduled reports via SMTP — v0.1
- ✓ **DEPLOY-01**: One-command install.sh on a single VM (Docker + TLS + admin + seed) — v0.1
- ✓ **SEARCH-01**: Global search across vulns, assets, users, tickets, CSPM (Cmd+K) — v0.1

### Validated — v2.0 UI/UX Redesign (SHIPPED 2026-06-30)

<!-- Frontend rebuild against the validated Wiz-inspired sunset-palette design system. Backed by 6 sketches + 43 design decisions in .claude/skills/sketch-findings-getvul/. Full archive: .planning/milestones/v2.0-*. Audit: tech_debt (0 blockers, 48/48 wired). -->

- [x] **UX-01**: `/login` rebuilt — split-screen with gradient mesh + SSO-primary form *(Phase 09, 2026-05-13 — token system, 6 primitives, persistent shell, sanitized `?next=` redirect, three-mode state machine; 12 manual browser smoke checks pending in 09-HUMAN-UAT.md)*
- [x] **UX-02**: `/dashboard` rebuilt — action-first hero with stat strip + activity feed *(Phase 10, 2026-05-18 — TanStack Query data layer + Card/Stat/StatStrip/ActivityFeed/TrendChart primitives (chart route-split) + quiet-win empty state + bundle-budget enforcer)*
- [x] **UX-03**: `/vulnerabilities` rebuilt — chip-bar filters + side-panel drill-down + 4 canonical state-pattern primitives (SkeletonTable, EmptyState, PartialFailureBanner, PerSourceStatusStrip) consumed verbatim by Phases 12-14 *(Phase 11, 2026-05-27 — backend faceted/grouped API + 8/8 plans + 291/291 frontend tests + 658→250 line page rewrite + 5 Phase 10 retrofit sites onto canonical primitives + vaul mobile drawer; 8 manual browser smoke checks pending in 11-HUMAN-UAT.md)*
- [x] **UX-04**: `/assets` + `/assets/[id]` rebuilt — list + two-column detail with metadata rail + risk ring *(Phase 12, 2026-06-01 — 8/8 plans + 426/426 frontend tests + backend tags/sla_breach/os_family/reassign/tickets-asset_id deltas + 4 UI primitives (RiskRing, Breadcrumb, Avatar, generic descriptor-driven ChipBar) + 5 TanStack hooks + list page rewrite + two-column detail with right rail; 5 manual browser items in 12-HUMAN-UAT.md + 1 missed slice fixed in iter-2 code review + 4 info-level polish items deferred)*
- [x] **UX-05**: `/tickets` + `/tickets/[id]` rebuilt — list + detail with provider identity (Jira/Asana/GitHub) *(Phase 13, 2026-06-02 — 9/9 plans across 3 waves; backend: alembic 026/027/028 (ticket_comments, ticket.blocked+blocked_reason+sla_due_at, ticket_watchers) + Jira/GitHub connector stubs + service/router for SLA recompute, list reshape, detail endpoint, comment/blocked/watch routes; frontend: queryKeys.tickets namespace + 3 provider gradient tokens + 4 list primitives (ProviderMark/StatusPill/SlaPill/VulnCount) + generalized DrillPanel (idKey) + 5 detail primitives (WatcherStack/ActivityTimeline/CommentInput/BlockedToggle/TicketAssetCard) + standalone /tickets/rules route + full v1 /tickets rewrite + two-column /tickets/[id]; 534/534 frontend tests. Code review caught + fixed a systematic snake_case↔camelCase frontend/backend contract break (15/15 Critical+Warning) + SC3 Completed-color fix; 2 live-data items in 13-HUMAN-UAT.md pending)*
- [x] **UX-06**: Remaining screens (CSPM, connectors, users, settings) rebuilt against established primitives *(Phase 14, 2026-06-03 — 6/6 plans across 2 waves; Wave 0 foundation: 14-provider ConnectorMark + 12 gradient tokens, SyncStatusPill, queryKeys cspm/settings/directoryUsers namespaces, sunset-restyled ConfirmModal/ExportButton, SettingsSidebarShell (RBAC-gated) + SaveBar + useDirtyState; Wave 1 screens: /connectors category grid + sentinel-passthrough form, /cspm chip-bar + finding drill + bulk bar + compliance strip, /users directory/groups toggle + source pills + export, /settings 6-pane sidebar-of-categories (replaces v1 4-tab) + unsaved guard. 676/676 frontend tests; zero raw-palette/tab-pattern leftovers. Code review: 1 Critical (SMTP password sentinel) + 9 Warnings all fixed (passwordTouched tracking, directory pagination, dialog a11y/focus-trap, prod auth redirect, OWNER role-select guard); 4 live-browser items in 14-HUMAN-UAT.md pending. Execution note: a parallel-worktree stale-base bug poisoned plan 14-00's branch with a phase-11–13 revert — detected pre-merge, scoped files cherry-picked instead, Wave 1 screens then run sequentially on main without worktrees)*
- [x] **UX-07**: Mobile + a11y + perf quality-gate pass closing the milestone *(Phase 15, 2026-06-30 — 6/6 plans; three-tier mobile nav + bottom-nav/More sheet, ResponsiveDialog (vaul), jsx-a11y at error (0 errors), Playwright route gate (viewport/axe/smoke/reduced-motion) + bundle budget + Lighthouse. Gate GREEN on prod build: 28 Playwright passed, 15/15 routes ≤250 KB, Lighthouse /login 97/95 + /dashboard 90/95. ~18 D-09 audit-fix defects resolved; mobile table card-view collapse built. Pending: Safari.app glyph human spot-check (non-blocking))*

### Deferred — v1.0 Production Readiness (resume as v1.1 when ready)

<!-- Originally planned as v1.0 Phases 2–8. v1.0 Phase 1 (Multi-Replica State) shipped 2026-05-09. The remaining backend hardening work is parked while v2.0 UI/UX Redesign takes precedence — backend and frontend don't share files, so v1.0 can resume in parallel or sequentially without conflict. -->

- [x] **PROD-01**: Multi-replica safe — OIDC state and rate limiter on Redis (Phase 01, 2026-05-09) ✓
- [x] **PROD-02**: CI gating — push/PR triggers re-enabled, no `|| true` masks, mypy baseline gate, `main` branch protection requires 4 checks green + a PR (Phase 02, 2026-07-01) ✓
- [x] **PROD-03**: Single canonical update path — auto-update cron hard-removed; CD pinned to `refs/tags/$DEPLOY_TAG` with rollback runbook (Phase 03, 2026-07-02) ✓
- [ ] **PROD-04**: Doc/code parity — CSP/COOP headers shipped, README scanner count, `VulnSource` enum *(deferred)*
- [ ] **PROD-05**: Encryption-key backup/rotation story documented and supported *(deferred)*
- [x] **PROD-06**: Default-admin hardening — forced first-login rotation: `users.must_change_password` flag (migration 029, seeded true on the OWNER admin), 403 enforcement gate on all non-allowlist routes while flagged, rotation clears the flag + emits `auth.first_login_rotation` + reissues flag-free tokens + rejects `Admin123!`, and a frontend `/change-password` redirect gate (Phase 06, 2026-07-09) ✓
- [ ] **PROD-07**: Health endpoint checks DB + Redis connectivity *(deferred)*
- [ ] **PROD-08**: Connector and ticket-rule test coverage above zero *(deferred)*

### Out of Scope

- **Multi-region HA / Kubernetes deployment** — single-VM topology is the explicit deployment model; HA is a future major version.
- **Scanner-less CVE feeds (NVD/OSV ingest without a scanner)** — GetVul aggregates *scanner output*, not raw vuln intel.
- **Self-scanning capability** — GetVul orchestrates other scanners, it is not one.
- **Per-tenant SaaS (multiple customer orgs in one deploy)** — multi-tenancy exists in the schema but the single-process scheduler and in-memory state make this unsupported in practice; one VM per customer is the model.
- **Real-time websocket dashboards** — polling is sufficient at current scale.
- **Mobile native apps** — web-responsive UI covers mobile use today.

## Context

**Stack at audit time (commit `8cede77`, 2026-05-08):**
- Backend: Python 3.12, FastAPI ≥0.115, SQLAlchemy 2.0 async, Pydantic v2, Alembic 1.14, asyncpg, Redis, bcrypt, Fernet (cryptography), python-jose, fpdf2, structlog
- Frontend: Next.js 15.5, React 19, TypeScript 5.5, Tailwind 3.4, lucide-react, recharts
- Infra: Docker Compose (5 services: nginx, postgres:16, redis:7, backend, frontend); Terraform under `infra/gcp/` (primary), `infra/aws/`, `infra/azure/` (secondary)
- 24 Alembic migrations
- CI: GitHub Actions — backend (ruff/mypy/pytest), frontend (lint/tsc/build), terraform validate, semgrep SAST, OWASP ZAP DAST. **All triggers are currently `workflow_dispatch` only — push/PR commented out.**

**Recent activity themes (last ~30 commits):**
- asyncpg type-handling fixes in seed data and migrations
- install.sh polish — TLS gen, admin user, seed data, hourly auto-update
- CSPM compliance UI alignment with backend
- Frontend hardening: relative API URLs, dark/light theme, mobile responsive, global search

**Known issues to address (from audit §7):**
- `VulnSource` enum at [backend/app/vulnerabilities/models.py:31](backend/app/vulnerabilities/models.py#L31) is stale — only 4 sources, but Qualys/Rapid7 connectors exist.
- ~~OIDC state in `_pending_states` dict~~ — resolved in Phase 01 (Redis SET NX EX 600 + GETDEL, fail-closed 503 on Redis unreachable).
- ~~Rate limiter in `_rate_limit_store` defaultdict~~ — resolved in Phase 01 (Redis sorted-set sliding window via MULTI/EXEC, fail-OPEN; doc/security.md:20 updated to "Redis-backed sliding window").
- Background scheduler runs in-process ([backend/app/connectors/scheduler.py](backend/app/connectors/scheduler.py)) — multiple replicas would double-execute.
- `aws_region` / `secrets_manager_prefix` in config.py — declared, `boto3` in deps, never used.
- CSP/COOP headers documented but not implemented.
- Default admin `admin@getvul.local / Admin123!` created by [install.sh](install.sh); no first-login forced rotation.

## Constraints

- **Tech stack**: Python 3.12 + FastAPI backend, Next.js 15 + React 19 frontend, Postgres 16, Redis 7, Nginx — locked, no language migrations in current milestone.
- **Deployment topology**: Single VM running Docker Compose — HA / multi-replica is explicitly Out of Scope for now, but PROD-01 must remove the *blockers* to multi-replica even if we don't run multi-replica yet.
- **Tenant isolation**: Every domain table includes `tenant_id`; every query scopes by `user.tenant_id` from JWT. No new feature may bypass this.
- **Compatibility**: Existing tenants' encrypted connector credentials must continue to decrypt — no Fernet key rotation without a documented migration.
- **Security**: No regressions to existing tenant isolation, audit logging, or RBAC. New features must register audit events.
- **Operator UX**: One-command install (`install.sh`) must remain functional; any new env var needs a sensible default.

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Single-VM Docker Compose deploy | Target customer is one ops team running one instance; HA is non-goal at this stage | ✓ Good |
| In-process background scheduler (no Celery/Arq) | Simpler ops; one container to scale | ⚠️ Revisit — blocks horizontal scale (PROD-01) |
| Fernet symmetric encryption for connector creds | Simple, no KMS dependency, key in `.env` | ⚠️ Revisit — needs documented backup/rotation (PROD-05) |
| Single-VM auto-update via hourly cron + GH-Actions release CD | Two paths added at different times | ⚠️ Revisit — pick one (PROD-03) |
| Multi-tenant schema even though deploys are single-tenant | Future-proofs SaaS option without rework | — Pending |
| Postgres 16 + JSONB for flexible enrichment payloads | Avoids per-vendor side tables | ✓ Good |
| Cross-source correlation via `vulnerability_correlations` table with FK per source | Materialized for fast read queries | ✓ Good |
| v2.0 sunset palette is dark-first; light theme is architecture-only | Sketch direction is dark; full light-mode visual QA scoped separately (UX-D-03) | ✓ Good — shipped; light-mode WCAG deferred |
| Phase 11 state primitives are the single source for all list screens | Avoids per-screen re-implementation; consistent loading/empty/error | ✓ Good — consumed verbatim by Phases 12–14 |
| DrillPanel generalized additively (idKey/renderContent), `cveId` back-compat alias | One panel serves vuln/ticket/asset/finding callers | ✓ Good |
| `(authed)` route group is single chrome owner + `force-dynamic` | Authed routes are client-rendered behind useAuth; resolves useSearchParams prerender | ✓ Good |

## Current State

**Shipped:** v0.1 feature set + **v1.0 Phase 1** (multi-replica state, 2026-05-09) + **v1.0 Phase 2** (CI gating, 2026-07-01 — CI armed on push/PR/nightly, `|| true` masks removed, mypy baseline gate, `main` branch protection requiring 4 checks green + a PR; failing-check merge-block proven live) + **v1.0 Phase 3** (update-path reconciliation, 2026-07-02 — auto-update cron hard-removed, CD pinned to a release tag with allowlist-validated `release_tag` dispatch + rollback runbook; a critical cd.yml command-injection RCE was caught in review and fixed) + **v1.0 Phase 4** (doc/code parity, 2026-07-03 — CSP + COOP headers now emitted (scoped off the debug-only Swagger/ReDoc routes), README↔overview scanner counts reconciled, `VulnSource` enum extended to all 6 sources with a tenant-scoped source-filter regression test, and the dead AWS Secrets Manager / boto3 config removed end-to-end. Validated PROD-04-01..05) + **v1.0 Phase 5** (encryption-key lifecycle, 2026-07-06 — `python -m app.encryption` CLI with single-transaction abort-all-or-nothing key rotation (pre-flight + post-verify decrypt-all, dry-run, `system:cli` audit row, no key material logged), a startup secrets check that hard-fails production on placeholder/unset/invalid `ENCRYPTION_KEY`/`JWT_SECRET_KEY`, and a backup & rotation runbook with ≤15-min RTO in `docs/16-security.md`. Two code-review blockers around the abort contract were caught and fixed. Gap closure 2026-07-08 (plan 05-03): the documented standalone `python -m app.encryption rotate` CLI had crashed with `NoReferencedTableError` (audit_logs FK targets unregistered in the CLI import graph) — fixed with a function-local `app.tenants.models` import and a subprocess regression test exercising the real operator path; re-verified 4/4 must-haves, UAT 10/10. Validated PROD-05-01..04) + **v2.0 UI/UX Redesign** (Phases 9–15, 2026-06-30 — every authenticated screen rebuilt on the sunset design system; quality gate green on the production build). v2.0 archive: `.planning/milestones/v2.0-*`. + **v1.0 Phase 6** (default-admin hardening, 2026-07-09 — a fresh install.sh deploy can no longer coast on the default `admin@getvul.local` / `Admin123!`: migration 029 adds `users.must_change_password` (NOT NULL, server_default false), `create_admin.py` seeds it true on the OWNER admin, the JWT claim threads through `CurrentUser`, `get_current_user` 403-gates every non-allowlist route with `password_change_required` while flagged, `/auth/change-password` clears the flag + emits `auth.first_login_rotation` + reissues flag-free tokens + rejects reusing `Admin123!`, and a `/change-password` page (Phase 9 primitives + sunset tokens) with an `auth.tsx` redirect gate completes the loop. Code review caught the primary-login-path gap — the `/auth/login` UserInfo omitted the flag so the SPA redirect only fired after a reload — fixed inline (add field to schema + populate in `issue_tokens`) with backend + frontend regression tests. Verified 5/5 must-haves. Validated PROD-06-01..04) + **v1.0 Phase 7** (health & observability, 2026-07-10 — liveness/readiness split: `GET /health` is now a no-dependency liveness probe while `GET /ready` checks Postgres `SELECT 1` + Redis `PING` each bounded by a 500ms `asyncio.wait_for` and returns a top-level-shape `JSONResponse` 503 on failure; nginx gained a named `upstream backend` with `/ready` locations in both server blocks and both compose files flipped their healthcheck to `/ready`; `RequestIdMiddleware` (outermost) honors/validates `X-Request-ID` and binds it to structlog contextvars; `configure_logging()` runs first in lifespan to emit unified JSON logs in production (ConsoleRenderer in dev) with case-insensitive + nested sensitive-key redaction and probe-path access-log suppression; operator failure-mode runbook added to `docs/15-monitoring-logging.md`. Code review found the D-17 redaction only scrubbed exact-case top-level keys (title-cased/nested credentials leaked) — hardened with recursion + case-folding and two regression tests; also fixed the `/ready` probe binding `async_session_factory` at import time (defeated the outage-simulation test). Verified 5/5 must-haves; Nyquist suite 11/11. Validated PROD-07-01..04) + **v2.2 Phase 16** (light-theme visual completion, 2026-07-16 — every authenticated route is now WCAG 2.1 AA in light mode, verified by an axe sweep that was actually executed against a live prod build (both the dark and light `e2e/a11y-routes.spec.ts` describe blocks report 0 critical/serious on every swept route — the process gap prior runs had only claimed). Gap-closure plan 16-03 landed the `--color-info` light override (WR-01), migrated every base `text-{accent}` on a same-hue soft/tinted fill to `text-[var(--color-{accent}-on-soft)]` (WR-02 + the systemic WR-04/04b sweep across ~18 sites), and root-caused a vendoring gap where the dark `--color-*-on-soft` tokens were undefined in the app — vendored them into the globals.css dark block (WR-03) so on-soft consumers resolve correctly in both themes. Full unit suite 685/685, tsc + semgrep + code review clean, human visual sweep approved. Validated UX-D-03-01..05).

> Phase 03 leaves one open item: a live dry-run rollback on a test VM (SC#4, tracked in `03-HUMAN-UAT.md`), plus non-blocking cd.yml warnings WR-02/03/04 logged in `03-REVIEW.md`.

> Note: the armed CI + branch protection are configured on the `Cyber-Solutions-MD/getvul` remote, but local `main` is ~286 commits ahead of `origin/main` and the armed `ci.yml` is not yet on remote `main` — pushing this work to remote is a separate deployment step.

**Tech debt (tracked in [BACKLOG.md](BACKLOG.md), non-blocking):** legacy-route 308 redirects; dead `/integrations` redirect; `useDocumentTitle` missing on 5 pages; dark-theme contrast DESIGN-SYSTEM GAPs to reconcile into sketch-findings; Nyquist validation partial on 5 phases; Safari.app glyph human spot-check.

## Next Milestone (candidate): v1.1 — Production Readiness (deferred backend hardening)

The deferred v1.0 Phases 2–8 (CI gating, update-path reconciliation, doc/code parity, encryption-key lifecycle, default-admin hardening, health/observability, test-coverage floor) remain the strongest next-milestone candidate — they were always backend hardening parked behind the UI rebuild, and they don't share files with the frontend. Start with `/gsd-new-milestone` (or resume the existing PROD-02..08 roadmap entries) when ready. Alternatively, a v2.x UI polish milestone (light-theme QA / UX-D-* deferred ideas + the backlog above).

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `/gsd-transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd-complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-07-16 after v2.2 Phase 16 (Light-theme Visual Completion) — UX-D-03-01..05 complete: every authenticated route verified WCAG 2.1 AA in light mode. The defining outcome of the 16-03 gap-closure was executing the light+dark axe sweep against a live Next.js prod build (backend up, admin seeded) — the exact process gap flagged in prior runs where AA was claimed but the sweep never ran; both describe blocks green (0 critical/serious on every swept route). Fixes: `--color-info` light override + skill mirror (WR-01); systemic migration of every base `text-{accent}` on a same-hue soft/tinted fill to the `--color-{accent}-on-soft` token across ~18 call sites (WR-02 status-pill/profile-pane, WR-03 EmptyState.Suggestion, WR-04 workspace-pane/activity-feed/cspm+connector pills/okta source-pill/chip-bar/pagination/timelines/KEV badges, WR-04b saml-pane + partial-failure-banner). Root cause found via the axe run: the app's vendored sunset.css never carried the dark `--color-*-on-soft` tokens the design-system skill defines, so in dark those consumers silently fell back to inherited text and the plan's "byte-identical dark" claim did not hold — vendored the dark values into the globals.css `:root[data-theme="dark"]` block (WR-03). 685/685 unit tests, tsc/semgrep/code-review clean, human visual sweep approved. Note: base accent text on *plain* (non-soft) backgrounds remains a separate un-addressed class (base `--color-violet/pink/amber` are not overridden in light) — not flagged by the sweep, backlog candidate. Prior footer: 2026-07-10 after v1.0 Phase 7 (Health & Observability) — PROD-07-01..04 complete: `/health` (liveness, no deps) split from `/ready` (Postgres `SELECT 1` + Redis `PING`, each ≤500ms `asyncio.wait_for`, top-level `JSONResponse` 503 on failure); nginx named `upstream backend` + `/ready` locations in both server blocks; both compose files healthcheck `/ready` (urlopen `timeout=4`, `start_period` 60s); `RequestIdMiddleware` (outermost) validates/mints `X-Request-ID` → structlog contextvars; `configure_logging()` first in lifespan → unified JSON logs in prod / ConsoleRenderer in dev, case-insensitive + recursive sensitive-key redaction, probe-path access-log suppression; failure-mode operator runbook in `docs/15-monitoring-logging.md`. Executed via `/gsd-execute-phase 7` (3 plans: Wave 0 Nyquist RED scaffold → Wave 1 slice A wiring + slice B structlog). Code review caught a **critical D-17 gap** — `redact_sensitive_keys` only scrubbed exact-case top-level keys, so title-cased `Authorization`/`Cookie` (arriving via `foreign_pre_chain`) and nested credentials leaked in cleartext; hardened with case-folding + recursion into dicts/lists and two regression tests. Also fixed the `/ready` probe binding `async_session_factory` at import time (the outage-simulation test's monkeypatch never landed → false 200); resolved by importing at call time, mirroring the lifespan pattern. WR-02 (CORS wildcard `allow_origins`) confirmed a **pre-existing scaffold bug**, out of phase scope. Verified 5/5 must-haves; Nyquist suite 11/11 GREEN on live Postgres+Redis. Execution note: the sole Wave-0 worktree executor tripped the **stale-base hazard** again — an uncommitted STATE.md aborted the post-wave merge, the branch was then deleted, and a follow-up fast-forward silently pulled in the stale tree (would have reverted phases 01–15 planning + prior source); caught via post-merge diff-stat, hard-reset to clean base, and the 2 scoped files re-applied surgically. Wave 1 was consequently run **sequentially on main (no worktrees)** to sidestep the hazard. Prior footer: 2026-07-09 after v1.0 Phase 6 (Default Admin Hardening) — PROD-06-01..04 complete: forced first-login password rotation for the install.sh-seeded admin. Migration 029 + `users.must_change_password` (NOT NULL, server_default false), seeded true on the OWNER admin; JWT claim → `CurrentUser`; `get_current_user` 403 allowlist gate (`password_change_required`); `/auth/change-password` clears the flag, emits `auth.first_login_rotation`, reissues flag-free tokens, rejects `Admin123!`; frontend `/change-password` page + `auth.tsx` redirect gate. Executed via `/gsd-execute-phase 6` (4 plans, 3 waves, worktree-parallel). Verifier first pass flagged an SC#4 blocker (code review WR-01): the `/auth/login` UserInfo omitted `must_change_password`, so the SPA redirect gate never fired on the primary login path — only after a hard reload hit `/auth/me`. Fixed inline (commit db20589): added the field to the `UserInfo` schema + populated it in `issue_tokens()`, plus backend (`test_auth.py::TestLoginResponseFlag`) and frontend (post-login no-reload redirect) regression tests; re-verified 5/5. Execution note: both Wave-1 parallel executors hit the stale-base hazard and self-recovered via the branch-check reset, and both authored `test_admin_hardening.py` (add/add conflict resolved in favor of owner 06-00). 3 non-blocking code-review findings (WR-03/04/05) deferred to `/gsd-code-review-fix 06`. Prior footer: 2026-07-08 after v1.0 Phase 5 gap closure (plan 05-03, `--gaps-only`) — the documented standalone `python -m app.encryption rotate` CLI (UAT Test 5) had crashed with `sqlalchemy.exc.NoReferencedTableError`: the CLI import graph never registered the `users`/`tenants` FK targets before the `AuditLog` write, so mapper configuration failed. Fixed with a function-local `from app.tenants import models` inside `rotate_credentials()`, guarded by a subprocess regression test (`test_rotate_cli_subprocess_completes_and_audits`) that runs the real operator command in a fresh interpreter. Code review: 0 blockers (1 warning + 2 info, advisory — test-robustness only). Re-verified 4/4 must-haves; UAT now 10/10; 1 live-Docker smoke-test remains pending in `05-HUMAN-UAT.md` (prod-mode container-exit propagation — decision logic already human-verified). Prior footer: 2026-07-06 after v1.0 Phase 5 (Encryption Key Lifecycle) — PROD-05-01..04 complete: `python -m app.encryption` rotate/verify/generate-key CLI with single-transaction abort-all-or-nothing re-encryption (pre-flight + post-verify decrypt-all, dry-run, `system:cli` audit row, no key material logged), startup secrets check hard-failing production on placeholder/unset/invalid `ENCRYPTION_KEY`/`JWT_SECRET_KEY`, and a ≤15-min-RTO backup & rotation runbook in `docs/16-security.md`. Code review caught 2 abort-contract blockers (malformed-JSON `AttributeError`; unguarded CLI dry-run count) — both fixed inline with a regression test. Execution note: a parallel executor worktree hit the stale-base hazard again (fast-forwarded silently, would have reverted phases 1–15 incl. Phase 4 CSP/COOP); caught via post-merge diff-stat, reset to clean base, scoped additions re-applied surgically, no data lost. Prior footer: 2026-07-03 after v1.0 Phase 4 (Doc/Code Parity) — PROD-04-01..05 complete: CSP + COOP headers emitted (scoped off debug-only Swagger/ReDoc routes after a code-review catch), README↔overview scanner counts reconciled, `VulnSource` extended to all 6 sources with a tenant-scoped source-filter regression test, dead AWS Secrets Manager / boto3 config removed end-to-end. Execution note: a parallel executor worktree hit the stale-base hazard (would have reverted phases 3–15 + resurrected auto-update.sh); caught pre-merge, salvaged scoped files only, no data lost. Prior footer: 2026-07-02 after v1.0 Phase 3 (Update Path Reconciliation) — PROD-03 complete: auto-update cron hard-removed, CD pinned to `refs/tags/$DEPLOY_TAG` with allowlist-validated `release_tag` dispatch + rollback runbook; review caught + fixed a critical cd.yml command-injection RCE. Prior footer: 2026-07-01 after v1.0 Phase 2 (CI Gating) — PROD-02 complete: CI armed, masks removed, mypy baseline gate (with pipefail fix), live `main` branch protection proven to block a failing-check merge. Prior footer: 2026-06-30 after v2.0 UI/UX Redesign milestone — Phases 9–15 shipped (every authenticated screen rebuilt on the sunset design system). Phase 15 closed the milestone: full mobile/a11y/perf quality gate GREEN on the production build (Playwright 28 passed, bundle 15/15 ≤250 KB, Lighthouse /login 97/95 + /dashboard 90/95), ~18 D-09 audit-fix defects resolved, mobile table card-view collapse built, CSPM mobile drill fixed post-audit. Milestone audit: tech_debt — 0 blockers, 48/48 requirements wired; tech-debt + Nyquist partials logged to BACKLOG.md. v2.0 archived to `.planning/milestones/v2.0-*`. Next candidate: v1.1 deferred backend hardening (PROD-02..08).*

<details>
<summary>Prior milestone footer (v2.0 in-flight — Phase 14)</summary>

*2026-06-03 — Phase 14 (Remaining Screens: CSPM, connectors, users, settings) complete: 6/6 plans across 2 waves. Wave 0 landed the connector-domain + settings foundation (14-provider ConnectorMark + 12 gradient tokens, SyncStatusPill, queryKeys cspm/settings/directoryUsers namespaces, restyled ConfirmModal/ExportButton, RBAC-gated SettingsSidebarShell + SaveBar + useDirtyState); Wave 1 rebuilt all four screens against established primitives (/connectors category grid + sentinel-passthrough credential form, /cspm chip-bar + finding drill + bulk bar + compliance framework strip, /users directory/groups segmented toggle + enrichment-source pills + export, /settings 6-pane sidebar-of-categories replacing the v1 4-tab layout with unsaved-changes guard). 676/676 frontend tests green; zero raw-palette or tab-pattern leftovers in any phase-14 source file. Code review found 1 Critical (brittle SMTP password sentinel — risk of persisting the mask as the real secret) + 9 Warnings, all fixed across 8 atomic commits (explicit passwordTouched tracking, directory pagination + stable select-all, onDirtyChange-based dirty state, dialog role/aria-modal/focus-trap, prod /login redirect instead of dev-token fallback, OWNER removed from per-row role select). Verification 6/6 must-haves; 4 live-browser/backend items pending in 14-HUMAN-UAT.md. Execution incident: the parallel-worktree isolation created plan 14-00's worktree from a stale base, and the branch-base-check's `git reset --soft` left a full phase-11–13 revert staged that the agent's first commit swept in — caught before merge, only the 14-00/14-01-scoped files were cherry-picked onto main (phases 11–13 verified intact), and Wave 1 was then executed sequentially on main with worktree isolation disabled to prevent recurrence. v1.0 Phase 13 (`/tickets` list + detail) shipped 2026-06-02: 9/9 plans across 3 waves. Backend: alembic 026/027/028 (ticket_comments, ticket.blocked + blocked_reason + sla_due_at + backfill, ticket_watchers) + ORM models + Pydantic schemas, Jira/GitHub connector stubs (auth + create + read-back, no sync), service/router layer (recompute_ticket_sla, list_tickets reshape with blocked/sla/external_status, GET /{id} detail, comment/blocked/watch routes with _resolve_group IDOR guard, bulk block/unblock). Frontend: queryKeys.tickets namespace (single source), 3 provider gradient CSS tokens, 4 list primitives (ProviderMark/StatusPill/SlaPill/VulnCount), additively generalized DrillPanel/DrillPanelMobile (idKey contract) + TicketDrillContent, 5 detail primitives (WatcherStack/ActivityTimeline/CommentInput/shared BlockedToggle/TicketAssetCard), standalone /tickets/rules sunset route (v1 ?tab=rules retired), full v1 /tickets rewrite (1239→421 lines, ErrorBoundary>Suspense>Inner, 4-axis chip-bar, 8-col table, List/Board toggle, bulk bar, optimistic mark-blocked), two-column /tickets/[id] (1fr_340px + sticky rail, 3 detail hooks). Tests: 534/534 frontend green across 91 files. Code review found + fixed a systematic snake_case (backend) ↔ camelCase (new frontend) contract break — 15/15 Critical+Warning fixed by aligning frontend to the codebase-wide snake_case convention (no transform layer); SC3 Completed-pill color corrected to success green. Verification 6/6 must-haves; 2 live-connector-data items (assignee resolution, chip-filter semantics) pending in 13-HUMAN-UAT.md.*

*Previous: 2026-05-27 — Phase 11 (`/vulnerabilities` + state patterns) complete: backend faceted-counts / by-host grouping / expanded sort axes, 4 canonical cross-phase state primitives (SkeletonTable, EmptyState, PartialFailureBanner, PerSourceStatusStrip — locked APIs for Phases 12-14), live `/dashboard/vulnerabilities` page rewrite (658→250 lines), 5 Phase 10 retrofit sites onto canonical primitives. Tests: 291/291 green.*

</details>
