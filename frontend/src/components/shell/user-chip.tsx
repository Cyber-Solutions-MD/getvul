'use client';
// Stub — full implementation lands in Task 2.
// Task 1 needs a real export here so Topbar's `import { UserChip } from './user-chip'`
// resolves and the build passes ahead of Task 2's interactive D-38 dropdown.
export function UserChip() {
  return null;
}
